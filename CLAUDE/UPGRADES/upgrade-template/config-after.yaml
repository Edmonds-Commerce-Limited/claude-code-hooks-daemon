# Example configuration AFTER upgrade (vX.Z)
#
# This file shows the complete configuration after the upgrade.
# Demonstrates the final target state with all new features.

version: "1.0"

daemon:
  idle_timeout_seconds: 600
  log_level: INFO

handlers:
  pre_tool_use:
    destructive_git:
      enabled: true
      priority: 10

  # NEW: Handler added in this upgrade
  event_type:
    new_handler:
      enabled: true
      priority: 40
      option1: value1  # Comment explaining new option
      option2: value2  # Comment explaining new option

  # Add other handlers showing the complete state AFTER upgrade
