# Plan 00273 — Journal 26-08-26

> **Append-only activity log** for plan 00273. One file per day
> (`00273-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 07:04 · action · — — plan scaffolded

Plan 00273 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 13:30 · action · P1 — Phases 1–2 executed in isolated worktree

Worktree `.claude/worktrees/agent-a4ff553e443d90fc7-0b00a249`, branch
`plan/00273-hook-input-payload-validation`.

- Prototyped the AST read-surface scan first: 7 event packages + shared
  utils; the ONLY field appearing in no vendored `input_example` is
  `stopHookActive` (deliberate legacy camelCase fallback in
  `utils/stop_hook_helpers.py`).
- TDD (RED → GREEN): `tests/unit/qa/test_check_input_contract.py` (22 tests,
  including the success-criterion mutation test renaming a field in a
  synthetic `Stop.json` example), then
  `scripts/qa/check_input_contract.py` applying Technical Decision 1's
  superset rule. String-literal and `HookInputField.X` reads handled; nested
  `tool_input` reads record only the top-level key; StatusLine/nitpick
  excluded by construction; shared utils checked against the union of
  examples.
- Added `contracts/claude-code-hooks/INPUT-ALLOWLIST.yaml` with the one
  allowlisted gap (`unknown-input-field:(shared):stopHookActive`), with
  stale/malformed-entry validation mirroring `check_hook_contract.py`.
- Wired into `scripts/qa/llm_qa.py` (registry entry `input_contract`, before
  smoke_test) and `scripts/qa/run_all.sh` (check 24 + summary row).
- Task 1.4: added step 7 (input re-triage) to
  `docs/guides/HOOK-CONTRACT-REFRESH.md`, naming the both-names-present
  rename blind spot the manual diff covers.
- Wrote `INVENTORY.md` (inventory + nested-`tool_input` and project-handler
  known-gap appendices + Task 1.3 field triage) and `DECISIONS.md`.

## 13:35 · decision · T2.1 — Phase 2 runtime advisory: NO-GO

Absence is indistinguishable from documented conditionality without a
required/optional substrate, so a runtime detector adds false signal, not
coverage; the refresh-time re-run of the static check gives the same rename
signal. Tasks 2.2–2.4 cancelled per the plan's conditional; no config key,
so no `config-changes` manifest entry needed.

## 13:36 · handoff · P3 — Phase 3 deferred to main checkout

The dual-channel sentinel experiment needs the dogfood daemon on main; a
worktree agent cannot run it. Tasks 3.1–3.3 remain ⬜ for a main-checkout
session.

## 13:50 · finding · — — worktree QA run: new checks green; 4 environment-only failures

`llm_qa.py all` in the worktree: 19/25 passed initially; after black
autofixed the new test file, the targeted re-run
(`format lint type_check input_contract hook_contract`) is 5/5 green and
the 22 new unit tests pass. The remaining failures are worktree-environment
artifacts unrelated to this diff (no `src/` change in this plan):

- `tests`: 14284 passed / 0 failed (`passed_all: true`), but the coverage
  figure collapses to 4.1% because the shared venv's editable install points
  `claude_code_hooks_daemon` at `/workspace/src`, so worktree runs measure
  the main checkout's files.
- `semgrep`: "semgrep not installed" in this worktree environment.
- `project_handlers`: 0 tests collected (same shared-venv path issue).
- `smoke_test`: probes the live daemon, which this worktree agent must not
  restart; expected to pass on main.

Daemon restart verification also deferred to main for the same reason
(and this plan ships no `src/` change to load anyway).

## 14:10 · action · — — code review findings folded (FIX FIRST verdict)

All findings from the coordinator's review addressed:

- MAJOR 1: `core/` folded into the SHARED surface (front_controller,
  mode_interceptor, session_state, core/utils all read hook_input). New
  shared fields `terminal_columns` + `context_window` are StatusLine-payload
  reads made by shared code — allowlisted with reasons; `cwd`/`model`/
  `tool_input`/`tool_name` are covered by the example union. Docstring Scope
  list and INVENTORY.md updated to state the shared surface accurately.
- MAJOR 2: allowlist protocol extracted into shared
  `scripts/qa/contract_allowlist.py` (Finding, Report, load_allowlist_file,
  apply_allowlist, named YAML-key constants, stale/malformed rules).
  `check_hook_contract.py` re-exports the shared names so its 32 tests stay
  green unchanged; new `tests/unit/qa/test_contract_allowlist.py` covers the
  shared module directly.
- MINOR 3: malformed-allowlist tests added (missing reason / missing link)
  plus a `--report-stdout` test.
- MINOR 4: events with reads but no vendored example are listed in
  `--inventory` output as SKIPPED, and carried on `InputReport.skipped_events`.
- MINOR 5: AST scan shape limits (aliasing, renamed param, `in` tests,
  attribute access) recorded in INVENTORY.md as a known gap.
- MINOR 6: a contract JSON missing its `event` key now raises ValueError
  naming the file; `main` catches it and exits 2 (tests added).
- MINOR 7: violation message reworded to "appears in no vendored {event}
  input example" / shared: "in no vendored input example".
- SUGGESTION 8: `scan()` returns `InputReport` carrying `read_surface` +
  `skipped_events`, so `--inventory` no longer re-scans the tree.

Verification: 69 tests green across the three checker test files;
format/lint/type_check/input_contract/hook_contract all pass; real-tree
inventory shows the shared surface with 3 allowlisted gaps.
