# Plan 00284 — Journal 26-08-28

> **Append-only activity log** for plan 00284. One file per day
> (`00284-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 12:08 · action · — — plan scaffolded

Plan 00284 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 12:15 · action · T1.1 — plan authored, review dispatch next

- User directive: enforce a standardised documentation strategy — one SSoT in
  `/CLAUDE/`; all other surfaces (`.claude/rules`, skills, agents, sub-folder
  CLAUDE.md, comments, issues/PRs, `docs/`, plan folders) POINT, never duplicate;
  plan folders may draft docs but the canonical version is promoted to project
  level. Enforcement modelled on the plan QA system (real-time handler + bulk
  scan + sweep).
- Mid-turn user refinements: (1) `docs/` = humans (terse, digestible), `CLAUDE/` =
  agents (verbose, dense) — recorded as PLAN.md Decision 1, human docs may point
  at `CLAUDE/` for depth; (2) roll in the progressive-disclosure plans — 00132 to
  be superseded, 00131 deferred residue absorbed, 00116 reconciled.
- Dedupe scout run BEFORE scaffolding: no live plan covers this. Adjacent:
  00131/00132/00116 (slices of the strand), 00144/00172 (the architectural
  reference pattern).
- PLAN.md written; README index row added. Next: dispatch the Fable review agent
  (Task 1.1) → `REVIEW-fable.md`, then commit+push the scaffold.

## 12:25 · action · T1.1 — scaffold pushed; Fable review dispatched; recovery cron reused

- Scaffold committed `dc5ca2eb` + stats recount `4c791b96`, both pushed.
- Fable review agent dispatched (read-only, reports to `REVIEW-fable.md`):
  evidence audit of this repo's doc surfaces, client-drift scenarios, proposed
  ruleset, enforcement signal candidates with FP assessments, open questions.
- Recovery cron: CronList shows exactly ONE recurring failsafe cron
  (id `0837855f`, hourly at :37) — reused per one-cron-per-session rule;
  nothing created.

## 12:40 · decision · — — user directive: ship a hooks-daemon-docs-qa agent

- User: "we can and should ship a hooks-daemon-docs-qa agent which can scan all
  project docs looking for conflicting truths, distributed truths etc".
- Folded into PLAN.md: new goal (agent shipped via the Plan 00279 generic agent
  install subsystem, like plan-dedupe-scout), Task 2.3 (agent design + invocation
  routes: on-demand / sweep-suggested / idle-housekeeping), success criterion
  (dogfood report against this repo as its acceptance test).
- Non-Goal refined to keep the split clean: deterministic checks own the
  mechanically-checkable subset (exact/near-copy, stale pointers); the AGENT owns
  the semantic half (conflicting truths, paraphrase drift) and only reports.

## 13:05 · finding · T1.1 — Fable review delivered (REVIEW-fable.md, 565 lines)

- Verdict: strategy is right and half-adopted already — the generated routing
  tables stayed accurate while every hand-maintained restating surface drifted.
- Strongest evidence: five agent-facing files still instruct `run_all.sh`, which
  `enforce_llm_qa` now DENIES (active harm); QA check count stated as 4/5/7/8 vs
  actual 24; four incompatible release-step numberings; ~130 unpromoted plan
  supporting docs; agents (no charter) hold most violations while skills (with a
  charter) are mostly clean — charter presence predicts outcomes.
- Ruleset: 12 draft rules; pointer-vs-duplicate bounded by CONTENT CLASS not
  sentence count (≤3 orientation sentences + role-specific notes = pointer; any
  copied table/fence/procedure/derived fact = duplicate); derived facts stated
  only by their source; generated-doc manifest; plan-completion promotion triage.
- Enforcement: ship `pointer-resolves` (link-graph) first; sibling `docs_qa`
  package on the plan_qa template, NOT an extension of markdown_organization;
  advisory-first everywhere, block as per-project ratchet.
- 7 questions for the human in its §E (each with a recommendation); the plan's
  own 5 Open Questions answered explicitly in its final section.
- Task 1.1 ticked ✅. Next: user decisions on §E → Task 1.2 (write
  CLAUDE/DocumentationStrategy.md) + Task 1.3 (handler shape — review recommends
  sibling docs_qa) + Task 1.4 (supersede 00132, safe now the review is done).

## 13:20 · action · T1.4 — 00132 superseded; review amended for agent split

- Task 1.4 done at `3eba4374`: 00132 → Superseded, archived to `Cancelled/`
  (the repo's superseded convention per 00174/00199 precedent), README row +
  stats recounted (36 active, 7 cancelled), 00131 Dormant header now names
  00284 as tracker of its Phase 4/6 residue.
- Review agent amended REVIEW-fable.md for the docs-qa agent scope: new R13
  (two instruments, deterministic vs agent, no deterministic check may attempt
  a semantic judgement), D.0 ownership table over all nine signals (shingling
  demoted to agent pre-filter — minhash cannot tell duplicate from
  CONTRADICTION, the worse defect), and D.10 agent design (inventory pass →
  topic-sharded deep reads; report with stable finding ids + an
  "adjudicated as fine" list feeding the grandfathering allowlist; invocation:
  on-demand primary + sweep-suggested + idle-housekeeping specialist; first
  real run doubles as the Task 1.2 dogfood acceptance test).
- Awaiting user decisions on the review's §E (7 questions, presented with
  recommendations).

## 14:10 · decision · T1.2 — all seven §E questions settled with the user

Walked the user through the seven questions one at a time (switched to
plain-text Q&A after the AskUserQuestion widget twice failed to render its
surrounding context for them). Outcomes, recorded in PLAN.md Technical
Decisions 1–7:

1. Two docs sites (human + agent); tree NAMES configurable, the SPLIT
   mandatory when enabled; agent tree always owns depth; plan dir under it.
2. NEW `ssot-quote` mechanism: verbatim excerpts allowed anywhere when wrapped
   in source-naming metadata; checker verifies quote-vs-source and reports
   drift. Replaces the idiom-allowlist idea entirely (user's own design).
3. Gut the drifted heavyweight human docs NOW — on a FEATURE BRANCH with a
   final cross-check before merge (user explicit: no hacking on default).
4. Sub-CLAUDE.md: path-proximate hints stay; deeper ruleset delegated to the
   review agent → `RULESET-sub-claude-md.md` (outside-reader test, registry,
   tiered budgets). FIRM user rule: `.claude/rules/*.md` are pointers ONLY.
5. Sibling `docs_qa/` package on the plan_qa template (accepted rec).
6. Block-capable, default-warn; block is the per-project ratchet — "deny is
   the point", e.g. verbose content into a rules file.
7. Comments: docs-qa agent explicitly hunts verbose comment blocks and treats
   them AS documentation to SSoT-cross-check; deterministic find/list helper
   scripts ship in a docs-qa SKILL, which the user then clarified is a
   SHIM/helper for the sub-agent (thin dispatch entry point, scripts included,
   no doc body of its own).

Tasks: 1.3 ticked ✅ (Decision 5); new tasks 1.5 (feature-branch doc gutting),
2.4 (ssot-quote design), 2.5 (skill shim). Next: commit this bookkeeping, then
branch and start Task 1.2 (DocumentationStrategy.md) + Task 1.5.

## 15:00 · action · T1.2/T1.5 — branch work done, cross-check dispatched

- Branch `plan/00284-docs-ssot-phase1` (pushed).
- T1.2 ✅ at `e4a4dd26`: `CLAUDE/DocumentationStrategy.md` (R1–R13 promoted
  from the review through the seven decisions), root CLAUDE.md pointer +
  CLAUDE/ index row per R11.
- T1.5 edits at `01be33ef` (net −2,318 lines): PLAN_SYSTEM.md 1,580→~85
  (docs-gutting agent; corrected hand-mkdir creation, stale enum, completion
  dates, auto-close example, wrong handler metadata); new docs/QA.md replaces
  the QA doc pair (git rm'd; dangling ref in run-qa-runner.sh comment fixed);
  agent-facing run_all.sh instructions → llm_qa.py all across 8 files
  (qa-cmd-fixes agent; instruct-vs-describe rule); stale counts stripped per
  R5 ("(8 checks)", "20/20 QA", PlanWorkflow What-Gets-Checked table — which
  also UNDERSTATED the security policy — and llm_qa.py's own "all 7"
  docstring); CONTRIBUTING skeleton corrected to the enforced four-method
  event-base contract.
- QA 25/25 green; daemon restarted RUNNING.
- Final cross-check (Decision 3's merge gate) dispatched to the review agent:
  branch diff vs the now-canonical ruleset, information-loss audit on the
  deleted QA docs, missed-site sweep. Merge waits on its verdict.

## 15:45 · action · T1.5 — cross-check passed, branch merged, Phase 1 complete

- Cross-check verdict: mergeable after 2 MUST-FIXes; everything else clean.
  Notable: it caught freshly-written FALSE canon in docs/QA.md (claimed the
  client QA runner runs eslint/typescript/prettier/cspell; the module
  implements Python tools only — the doc had copied the wrapper's own broken
  default). Fixed the doc AND the wrapper default (dogfooding rule, DBF).
- MUST-FIX 2: created `.claude/agents/CLAUDE.md` charter (R7c had pointed at
  a nonexistent file — the policy doc violating its own R6), mirroring the
  skills charter; closes the review's charter-gap finding properly.
- All 7 SHOULD-FIX one-liners fixed (remaining instructive run_all.sh sites);
  bulk deferrals (AgentTeam ~20, Worktree ~14, PlanWorkflow 3-digit
  numbering, SKILL.md step numberings, CLAUDE/CLAUDE.md prose index, both
  rules files' pointer-only failures) recorded as Task 3.2 worklist.
- QA 25/25 green twice (before commit, after fixes); daemon RUNNING.
- Merged `--no-ff` to main at `4bb90f73` (net −2,073 lines); branch deleted
  local + remote via `git branch -d` (clean ancestry). Tasks 1.1–1.5 all ✅ —
  Phase 1 complete. Next: Phase 2 (docs_qa core + surfaces + agent + quote
  mechanism + skill design).

## 16:30 · action · P2 — design drafted, critiqued, amended; Phase 2 complete

- `DESIGN-enforcement.md` drafted (Tasks 2.1–2.5) at `bca2815f`; critique
  pass dispatched to the review agent (worries: quote-span robustness,
  rules-file day-one block, config placement, hidden semantics in the table).
- Critique verdict: sound skeleton, both headline FP disciplines survived,
  structural block-eligibility endorsed; 2 MUST + 5 SHOULD, all applied:
  - ssot-quote hardening: both sides through the real mdformat-gfm pipeline
    (escaping/renumbering beats whitespace collapse), fence-aware span
    extraction, one pinned slug algorithm + prefer explicit anchors, minimum
    quote length, single-section constraint.
  - Cold/stale-corpus rule: no DENY on a cold index — degrade to advisory,
    build at startup/SessionStart, atomic index writes; corpus scope excludes
    CHANGELOG/RELEASES/Plan-Completed.
  - Config typing goes in config/models.py (real plan_qa precedent); docs_qa
    keeps a plain mirror only.
  - R5 traced to the existing doc_truth check + fact registry; R9/R4a/R3 and
    the dropped agent/skill size signal each given a tracing sentence.
  - rules-file-shape gets worse-only semantics structurally (every client
    starts non-compliant); generated-doc-hand-edit gains a SWEEP half;
    pointer-resolves block scope narrowed to NEW missing-file links (anchors
    advisory until slugs soak); at-import-census skips backticked mentions;
    restart-snippet canonical section to live OUTSIDE generated blocks
    (CodeLifecycle/General.md).
- Tasks 2.1–2.5 ✅ — Phase 2 complete. Next: Phase 3 step 1
  (pointer-resolves + corpus + CLI skeleton, TDD).

## 17:15 · action · T3.1a — first implementation slice delivered

- python-developer agent delivered 3.1a at `62203a92` (pushed): typed
  `documentation:` config block, docs_qa package (types/policy/context/corpus
  with cold-index rule + atomic cache/checks registry/runner/report),
  pointer-resolves (EDIT block-eligible on NEW missing-file links only, all
  the FP skips from the critique), `docs-qa` CLI wired like plan-qa.
- 90 new tests, 100% coverage on the new package, full QA 25/25, daemon
  restart RUNNING throughout. Verified here: commit on main, `--lint` clean on
  DocumentationStrategy.md, live `--sweep` surfaces real dead links in old
  upgrade docs (recorded, untouched — they are candidates for grandfathering
  or fixing when dogfood enablement lands).
- Agent deviations accepted: one error-hiding exclusion for the corrupt-cache
  fail-open contract (existing repo convention); plan-archive corpus exclusion
  approximated from the agent-tree name (noted on 3.1e to consider wiring to
  `plan_workflow.directory`); one defensive branch kept with direct test.
- Next: slice 3.1b (generated-doc-hand-edit + manifest).

## 17:50 · action · T3.1b — generated-doc guard delivered

- Delivered at `d1a8a85a`: EDIT half blocks hand-edits to manifest-declared
  generated docs naming the regenerate command; SWEEP half checks the real
  `docs_generator.py` version marker against version.py (narrow regex, silent
  skip when unrecognised — no invented heuristics). 28 tests, package still
  100%, QA 25/25, daemon RUNNING.
- Verified here: `--lint .claude/HOOKS-DAEMON.md` correctly BLOCK-flags the
  hand-edit route (exit 1) and the sweep reads v3.56.0 as current.
- Accepted deviation: `matched_manifest_entry` made public so the CLI can lint
  the manifest's own default entry (was previously rejected as out-of-scope by
  the corpus tree check — a real gap, closed).
- Next: 3.1c — rules-file-shape (worse-only) + the three surface handlers,
  including dogfood enablement in this repo's config (warn modes).

## 18:55 · action · T3.1c — check + handlers delivered; DOGFOOD IS LIVE

- Delivered at `1e0fbf8e` + `9282f28c`: rules-file-shape (five metrics, all
  worse-only tiered, 15-line body budget constant per RULESET §3; both of this
  repo's real non-compliant rules files proven still-editable),
  DocsQaEditHandler (prio 47, structural deny-eligibility) and
  DocsQaSweepHandler (prio 64, explicit corpus build per cold-index rule),
  full DI threading mirroring the plan_workflow idiom, shipped template +
  example config entries (disabled upstream), dogfood ENABLED here
  (edit_mode warn, sweep advise, grandfather CLAUDE/UPGRADES/**).
- Live socket probes: sweep renders a real drift report (both rules files +
  dead links; corpus 201 docs persisted); a warn-mode rules-file fence write
  correctly ADVISES not denies. The system is now watching this repo's docs.
- Commit gate deliberately deferred to 3.1e with STAGED context — matches
  Plan 00144's own land-last rollout precedent.
- Agent handled shared-tree concurrency correctly: excluded the sibling
  bootstrap-fix agent's in-flight Plan 00285 files from its commits.
- Note: /goal slot displacement advisory fired (00285 vs 00284 both live) —
  expected with two concurrent plans; the ledger keeps both owed.
- Next: 3.1d — ssot-quote verifier per the hardened §2.4 spec.

## 19:55 · action · T3.1d — ssot-quote mechanism delivered

- Delivered at `2503a881`: quotes.py machinery (pinned slug algorithm,
  fence-aware span/marker parsing, normalisation via the SAME mdformat
  pipeline as markdown_table_formatter, 80-char minimum floor with rationale),
  quote-drift (EDIT block-eligible + SWEEP advise, four distinct message
  shapes), quote-source-stale (EDIT advise-only via corpus reverse index,
  cold-safe by construction; no SWEEP half — quote-drift's SWEEP already
  re-verifies, recorded in docstring), corpus quote index (refs only, never
  cached bodies). 61 tests; whole package still 100% (616 lines/192 branches).
- Live probe: deliberately drifted quote through the real socket → warn-mode
  advisory naming the anchor and remedy; no file created.
- Judgement calls accepted: span excludes the heading's own line; char floor
  over line floor (its first real-content test quote failed at 79 chars —
  the floor working as intended).
- Meanwhile Plan 00285 (bootstrap $0 clobber) completed and archived by the
  sibling agent — see that plan's folder for its record.
- Next: 3.1e — STAGED context + commit gate + budgets/registry +
  at-import-census + remaining STAGED checks.

## 20:40 · decision · — — orchestration correction: one slice per agent

- User flagged the build agent approaching ~1M tokens. Correct call: I ran ONE
  agent through five sequential slices to keep architectural context warm, but
  each slice compounds (TDD cycles, QA runs, probes, repo reads) and the
  continuity stopped paying for itself around 3.1c. The briefs + design doc
  were always self-contained enough for a fresh agent per slice.
- Corrective action: build agent instructed to finish ONLY the in-flight unit
  (3.1e commit 2), push, and hand over; a FRESH agent takes 3.1e commit 3
  (at-import-census + module-doc-budget) and each later slice gets its own
  agent. Process rule for the rest of this plan: one slice, one agent, fresh
  context; the written design docs ARE the handover.
