# Plan 00291 Journal — 2026-09-10

## 09:05 · handoff · session opened in worktree-plan-00291 to close out the plan

Picked up where the 2026-09-09 session left off: three staged changes
(PLAN.md ticks, the journal day-file, release-notes callout 15) plus Task
4.2 still open. Read the full PLAN.md and confirmed every other task and
its cited commit were real; Task 4.2 ("Full QA green; daemon restart
verified; UPGRADES manifests updated") was the only remaining item.

Restarted the worktree daemon (it was NOT RUNNING at session start) and
confirmed RUNNING. Ran `./scripts/qa/llm_qa.py all`: 22/26 PASSED, four
real findings, all introduced by this plan's own commits:

- `magic_values`: `tests/integration/test_init_sh_discovery_pid_path.py`
  used a bare `timeout=60` instead of `Timeout.REQUEST_LONG`.
- `error_hiding`: three `scripts/qa/error_hiding_exclusions.json` entries
  for `scripts/upgrade.sh`/`scripts/upgrade_version.sh` had drifted off
  their target lines (438→531, 450→511, 812→873) because this plan's own
  edits added code above them — same code, same rationale, just moved;
  realigned rather than deleted. `install/install_stamp.py::read_install_stamp`
  (new in this plan) had no exclusion entry yet for its documented
  return-None-on-error contract; added one, following the exact shape of
  the sibling `daemon/metadata.py::read_daemon_metadata` entry.
- `capture_corruption`: `print_branch_install_banner` in
  `scripts/install/branch_install.sh` wrote its eleven-line warning banner
  to stdout instead of stderr — the documented v3.10.0 SEV-1
  log-helper-stdout pattern this audit exists to catch. Fixed by routing
  every line through `>&2`; updated the two tests that asserted the
  banner text against `stdout` (`test_branch_install_gate.py`,
  `test_guarded_branch_install.py`) to assert it against `stderr` instead
  and that `stdout` is empty. Confirmed none of the three real call sites
  in `upgrade_version.sh` capture the function's output. `upgrade.sh`
  carries its own inline copy of the same banner at top level (not inside
  a function, per its own doc comment about not being able to source
  `branch_install.sh` from a curl-fetched copy) — correctly unflagged,
  since nothing captures a whole script invocation via `$(...)`.
- `tests`: the three failures were exactly the self-scan tests mirroring
  the two audits above (`test_audit_capture_corruption`,
  `test_audit_error_hiding` x2) — no separate cause.

Re-ran full QA after the fixes: `llm_qa.py all` is 26/26 PASSED
(21655 passed, 0 failed, 21 skipped, coverage 95.4%).

Ticked Task 4.2 and every substantive Success Criterion in PLAN.md, and
added the holding-area criterion the `plan-done-requires-holding-area`
project handler requires (citing release-notes callouts 14 and 15, both
already staged under `CLAUDE/UPGRADES/UNRELEASED/release-notes/`).

Tried flipping `**Status**` to `Complete` to close the plan, per the
delegating instruction to leave `CLAUDE/Plan/README.md` for the
coordinating session on `main`. That combination is not committable in
this repo: `terminal-state-atomic` and `location-status-coherence` both
BLOCK a staged terminal-status flip whose plan folder has not also moved to
`Completed/` (with the README row) in the same commit — the two are
enforced as one atomic unit, not two independent steps, by this project's
own `CLAUDE/Plan/CLAUDE.md` rule. Reverting the header to `In Progress`
with every checkbox ticked is equally blocked (`header-body-coherence`:
header contradicts a fully-ticked body). Resolution: header stays
`In Progress`, every substantive Success Criterion is ticked, and one new,
explicitly UNticked criterion names the remaining atomic step (folder move
+ README + status flip) as the coordinating session's job on `main` — this
is the only state that is both honest and passes every staged/edit-time
plan-QA gate. Verified clean: `plan-qa --check-staged` reports 0 findings.

## 09:52 · handoff · pre-shutdown checkpoint

Merged `origin/main` (35e96847, pushed) resolving 5 conflicts
(`scripts/upgrade.sh`, `daemon/cli.py`, `install/config_cli.py`,
`install/config_migrations.py`, `install/truth_changes.py`) by combining
both sides — my `TARGET_SEMVER`/`include_unreleased` plumbing plus main's
`--project-root`/`report_dir` offload work from Plan 00329. Verified with
`bash -n`/`py_compile` per file, then `./scripts/qa/llm_qa.py all`:
26/26 PASSED (21747 passed, 0 failed, coverage 95.4%) — no real failures,
nothing to fix. Pushed. This was reported to the coordinating session.

State at this checkpoint: branch is fully mergeable with `main`
(`git merge-base --is-ancestor origin/main HEAD` true), working tree clean
except two untracked dirs — `CLAUDE/Plan/00291-.../subagent-reports/`
(this plan's own finish/handoff reports, intended to be committed) and a
stray `.claude/reports/v2.2.0-to-v2.15.2/` left over from an offload test
run (not real content, deliberately not committed). Nothing else
outstanding: the only remaining Success Criterion is the coordinator's
atomic archive step (folder move + README + Status flip to Complete) on
`main`, as already recorded above. Next session: nothing to resume beyond
that coordinator step; if new work arrives, start from a clean
`git fetch && git status`.
