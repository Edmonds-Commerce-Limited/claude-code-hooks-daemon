# Plan 00291 — Journal 26-08-30

> **Append-only activity log** for plan 00291. One file per day
> (`00291-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 08:58 · action · — — plan scaffolded

Plan 00291 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 09:12 · decision · — — plan filed from canary findings

Source: php-qa-ci canary migration run
(`untracked/reports/canary-php-qa-ci-upgrade-26-08-30.md`) — a real v3.41.0
client fresh-cloned and upgraded to main (9bf05f06). Upgrade succeeded but
not via the documented route: `upgrade_version.sh` hard-failed on the
no-venv fresh-clone state and `install_version.sh` was used instead.
Findings 1–4 of the report become Phases 1–2; the owner's guarded
branch-install ruling ("not something normal users can do — guarded, non
obvious, clear warnings") becomes Phase 3; the canary re-run is Phase 4.

Dedupe scout checked 36 live plans: no overlap; Plan 00176 (settings.json
merge preservation) shares the upgrade surface but solves a separate
problem and proceeds independently.

The report's finding 5 (`quarantine_artefact_read_guard` false-positive on
unexpanded shell globs) is NOT in this plan — it is a main-repo handler bug
being fixed directly under the dogfooding bug lifecycle in this session.

Failsafe recovery cron: session cron `0837855f` already covers this plan;
nothing created.
