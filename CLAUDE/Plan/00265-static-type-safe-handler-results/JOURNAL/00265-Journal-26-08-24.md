# Plan 00265 — Journal 26-08-24

> **Append-only activity log** for plan 00265. One file per day
> (`00265-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 10:07 · action · — — plan scaffolded

Plan 00265 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 10:14 · finding · — — the design was verified before the plan was written

The plan arose from a question during the response-contract work: whether Python
can do what PHP does with a wide interface type narrowed by each implementation.
Rather than answer from memory, two probes were run under this project's strict
mypy — one generic, one against the real `HookResult` and `Handler`.

Every deliberate violation was caught:

```
probe:28: error: Argument "decision" to "AdvisoryResult" has incompatible type
          "Literal[Decision.DENY]"; expected "Literal[Decision.ALLOW, Decision.CONTINUE]"  [arg-type]
probe:65: error: Return type "HookResult" of "handle" incompatible with return
          type "AdvisoryResult" in supertype "SessionStartHandler"  [override]
probe:69: error: "type[AdvisoryOnly]" has no attribute "deny"  [attr-defined]
probe:46: error: Incompatible return value type (got "WideResult", expected "AdvisoryResult")  [return-value]
```

The correct handler compiled clean with **nothing extra declared** — it inherits
the constraint from its event base. That is what separates this from the
AST-based guards shipped earlier today: those detect a bug someone already
wrote; this makes it unwritable.

## 10:16 · finding · P4 — Pydantic makes this stronger than the PHP equivalent

`validate_assignment` is already `True` on `HookResult`, so an out-of-tier
decision raises `ValidationError` on **both** construction and mutation:

```
validate_assignment: True
construct: rejected -> ValidationError
mutate:    rejected -> ValidationError
```

Consequence for Phase 4: `merge_pseudo_results` writes
`real.result.decision = Decision.DENY` directly. Under narrowed types that is a
type error AND a runtime raise — correct, since it is an unchecked path that can
write a decision the trigger's event cannot carry, but it means Phase 4 must
land BEFORE the trigger events are reparented. Otherwise dispatch starts raising
where it previously degraded silently.

## 10:18 · decision · — — per-event bases, not `Handler[ResultT]`

Both encodings type-check. They differ in what happens when a developer forgets:
a generic parameter must be declared per handler, so a forgotten declaration
silently loses protection — the exact failure mode this plan exists to remove. A
per-event base is inherited, so it cannot be forgotten, only actively overridden
(which mypy rejects as `[override]`).

## 10:20 · thought · — — friction recorded before it bites someone

pyright reports `reportIncompatibleVariableOverride` on the narrowed field: it
demands invariance for a mutable attribute where mypy demands only a subtype.
This project's QA gate is mypy, so nothing fails — but the IDE shows it, and the
tempting "fix" is to widen the field, which would undo the whole plan. Recorded
in `PLAN.md` and to be repeated in the class docstrings.

## 10:22 · action · — — housekeeping

- Dedupe scout checked 34 live plans: nothing covers this. Closest were 00242
  (terminal handlers — runtime decision merging, not typing) and 00170 (event
  wiring); both superficial overlaps only.
- `CronList` showed only the background-process watchdog `341807e1`, so a
  failsafe recovery cron was created: **`dd181bf3`**, hourly at :23,
  session-only.
- Plan index statistics were recounted from disk while adding the row: 35 root,
  217 `Completed/`, 6 `Cancelled/`, counter 265. The previous reconciliation
  entry recorded 35 / 216 against stats reading 34 / 217 — it disagreed with
  itself and with disk, and was corrected in the same edit.

## 10:41 · finding · P1 — `Decision.CONTINUE` is vestigial

While deciding which decisions belong in each tier, checked whether CONTINUE is
deliverable rather than assuming. It is — valid on all 31 events — but the
reason is that it does nothing:

- `grep -rn "Decision.CONTINUE" src/` returns **only** its own definition in
  `hook_result.py`. Every other `CONTINUE` hit in `src/` is either a comment or
  the unrelated `AUTO-CONTINUE` prose in the stop handler.
- No formatter branches on it, so it serialises to `{}` on every event.
- No response schema has a `continue` key, so it has no wire representation at
  all.
- `chain.py` and `verdict_log.py` both already class it with ALLOW as
  "lax"/"advisory".

So it is ALLOW-equivalent in practice. Included it in all three tiers: it IS
deliverable everywhere, and excluding it would break nothing today while being a
gratuitous incompatibility for any client handler that names it. Recorded here
rather than acted on — removing a public enum member is a separate decision.

## 10:52 · decision · P1 — `result_type_for_event` FAILS FAST on an unknown event

First implementation returned the advisory tier for an unrecognised event name,
because an unknown event resolves to `{ALLOW, CONTINUE}` which matches
`AdvisoryResult` exactly. The RED test caught it.

That default is the worst possible one: the wrong answer is indistinguishable
from the right one. A typo for a refusal-capable event would silently forbid
every handler built on it from denying. Now raises.

Deliberately the OPPOSITE policy to
`decision_capability.undeliverable_decisions`, which returns nothing for an
unknown event. That one is a diagnostic run against a CLIENT's handlers, where
refusing to judge is the safe failure; this one picks the type a handler is
built on, where guessing is not. Both docstrings say so, so the asymmetry does
not read as an inconsistency.

## 11:04 · finding · T1.4 — the runtime tests would not have noticed mypy stopping

`test_result_types.py` covers the runtime half thoroughly and would stay fully
green if mypy stopped enforcing anything — a loosened `Literal`, a relaxed
strictness setting, a plugin change. Since the whole plan is about STATIC
safety, that is the guarantee least covered by its own tests.

Added `tests/integration/test_static_type_safety_is_enforced.py`: runs the real
type checker over a fixture of deliberate violations and asserts each is caught.
The fixture carries its own expectations as `VIOLATION: <code>` markers, so
adding a case extends the test automatically — no second list to keep in step.

**Verified non-vacuous by sabotage.** Widened `AdvisoryResult` to include DENY
and re-ran; the guard failed naming exactly the two violations that stopped
being caught (the DENY construction and the DENY assignment), then passed again
on revert.

## 11:09 · finding · T1.4 — black moved a marker away from its error

First QA run failed three checks, and one was subtle: black reformatted the
fixture, wrapping a call across three lines. mypy reports a multi-line call at
its FIRST line, so the trailing `VIOLATION:` marker ended up two lines away from
the error it describes.

The fixture had been excluded from ruff and mypy but not from black — black has
its own `extend-exclude` block in `pyproject.toml`. Added it there too, restored
the one-line form, and documented the constraint in the fixture docstring.

Worth noting the failure mode was loud, not silent: a decoupled marker fails
`test_each_marked_line_is_reported` AND `test_nothing_unexpected_is_reported`
simultaneously, so drift cannot pass quietly.

Other two QA failures were mechanical: the same black run, and a `magic-timeout`
violation for a literal `300` — replaced with `Timeout.QA_TEST_TIMEOUT`, which
is documented as being for exactly this ("mypy, individual tool checks").

## 11:14 · handoff · P1 — Phase 1 complete

QA 23/23, daemon RUNNING. Phase 1 delivered: three tiers, derived membership,
runtime enforcement on construction and mutation, and a proven-non-vacuous
static guard.

Next is Phase 2 (per-event handler bases). Phase 4 (`merge_pseudo_results`) must
land before Phase 3 reparents the trigger events — see the 10:16 entry.

## 12:58 · action · P2 — Phase 2 complete: the constraint is now inherited

`core/handler_bases.py` adds three abstract tier bases (`AdvisoryHandler`,
`BlockingHandler`, `GatingHandler`) that re-declare `handle()` with the narrowed
return type, plus one alias per wired event. 113 unit tests, QA 23/23, daemon
RUNNING.

The measured payoff is that a handler now inherits the constraint with NOTHING
declared. Three new fixture cases prove it — all reported by a real mypy run:

```
decisions.py:71: error: Argument "decision" to "AdvisoryResult" has incompatible
  type "Literal[Decision.DENY]"  [arg-type]
decisions.py:86: error: Return type "HookResult" of "handle" incompatible with
  return type "AdvisoryResult" in supertype
  "claude_code_hooks_daemon.core.handler_bases.AdvisoryHandler"  [override]
decisions.py:103: error: Argument "decision" to "BlockingResult" has
  incompatible type "Literal[Decision.ASK]"  [arg-type]
```

The `[override]` line is the important one: it is the escape hatch closing. A
handler cannot widen its way back to plain `HookResult`.

### Decision: per-event names are ALIASES, not classes

Verified mypy enforces a narrowed return type identically through an alias —
the `[override]` error above was produced through `SessionStartHandlerBase`, and
mypy even names the real base (`AdvisoryHandler`) in the message. So an event
costs one line rather than a class body.

The aliases add no type-level strictness over the three tier classes. They exist
so a handler author never has to know which tier their event is in — precisely
the knowledge this plan removes the need for. Package-to-tier correctness is
enforced by the sweep in `test_handler_bases.py`, not by the naming.

`{Event}HandlerBase` rather than `{Event}Handler` because `WorktreeCreateHandler`
and `WorktreeRemoveHandler` are already concrete handler class names; a test
asserts no base name collides with a shipped handler, so the reason cannot be
quietly undone later.

### Finding: `[type-abstract]` rejects a container, not a return

The first cut of `handler_base_for_event` used a `dict[type[HookResult],
type[Handler]]` lookup and mypy reported three `[type-abstract]` errors — it
refuses an ABSTRACT class placed into a `type[X]` container literal. Probed the
exact boundary rather than guessing: RETURNING an abstract class from a function
annotated `-> type[Handler]` is fine; only the container is rejected.

These bases must stay abstract (a concrete `handle` stub would let a subclass
forget to implement it and silently inherit the stub), so the table is simply
not available. Rewrote as three direct returns and recorded why in the
docstring, so nobody "tidies" it back into a dict.

### Coverage the sweep adds

`TestEveryWiredEventHasABase` is parametrised over EVENT metadata, not over
existing handler packages — so an event that gains its first handler later
already has a correctly-tiered base waiting, rather than the gap being noticed
when someone writes the handler.

Next: **Phase 4, not Phase 3.** `merge_pseudo_results` mutates
`real.result.decision`; once trigger-event handlers are reparented that becomes
both a mypy error and a runtime `ValidationError`, so dispatch would start
raising where it previously degraded silently.

## 13:16 · action · P4 — the mutation path is closed

`merge_pseudo_results` now takes the REAL event name, clamps to what that event
can carry, and CONSTRUCTS the merged result instead of mutating one. QA 23/23,
daemon RUNNING, no errors in the log.

### `event_name` is required, not defaulted

The merge cannot be done correctly without it: a pseudo-event has no response of
its own, so its decision is delivered under the real event's, and triggers are
per-project CONFIG. The same handler is deliverable under one project's config
and dropped under another's.

Derived the deliverable set via `decisions_of(result_type_for_event(name))`
rather than calling `decisions_carried_by` directly — same answer, but that
route RAISES on an unknown event. `decisions_carried_by` resolves an unknown
name to `{ALLOW, CONTINUE}`, which is indistinguishable from a correct advisory
answer, so a typo would have clamped every refusal in silence. Same trap Phase 1
documented on `result_type_for_event`; worth writing down that it bites twice.

### Decision: clamp, don't discard

A clamped refusal keeps its message. The reason is appended to context — which
is exactly what an advisory event CAN deliver — and the clamp is logged at
ERROR naming the event and the decision.

The ERROR matters *because* nothing visibly breaks: the guidance still reaches
Claude, so a misbound refusing handler would otherwise look like it was working.
Deliberately did NOT invent a promotion (ASK→DENY on an event with no `ask`):
nothing else in the system does that, and inventing one here would make the
clamp stronger than what the handler asked for.

### Finding: the controller test could not have caught this

`test_pseudo_event_context_merged_into_result` built the chain result from a
`MagicMock`. A mock absorbs any operation the merge performs, so it could not
distinguish a working merge from a broken one — and when the merge switched from
mutating to constructing, the mock swallowed the `TypeError` into
`process_event`'s broad `except`, surfacing as a daemon-error result rather than
a merge failure.

Rebuilt it on a real `ChainExecutionResult`, and added a second test asserting
the CONTROLLER hands the merge the event it will serialise under. That pairing
is invisible in unit tests of the merge itself — only the controller can get it
wrong — and it now also asserts the result carries no daemon-error context, so a
swallowed crash cannot read as a successful clamp.

### API note for Phase 5

`merge_pseudo_results` is exported from `core`, so the required third argument is
a signature change to a public name. Nothing outside this repository is known to
call it (it is dispatch plumbing, not a project-handler extension point), but it
belongs in the truth-changes manifest with the rest of Phase 5.

Phase 3 is now unblocked: reparenting the trigger events can no longer make
dispatch raise.

## 13:35 · finding · — the sweep was blind to the commonest refusal shape

Found while working out what Phase 3 has to change: `decisions_referenced_by`
only recognised `Decision.DENY` as an attribute reference. It never saw
`HookResult.deny(...)` — which names neither the enum nor the member, and is
how most handlers actually refuse.

Measured before fixing, across this repository's 84 concrete handlers: exactly
one refused that way and the scan saw nothing (`PlanNumberHelperHandler`). It
cost nothing here only because that handler is on `PreToolUse`, which can deny.

**The gap that matters is the client's.** `validate-project-handlers` shares
this primitive, so a project handler refusing on `SessionStart` through the
factory passed the pre-flight check in silence — and a project handler is
exactly the population with no other guard.

DBF: the defect worth fixing was the blind guard, not the one handler. The scan
now recognises `<Something>Result.allow|deny|ask(...)`, matched by the `Result`
SUFFIX rather than a fixed class list, so the narrowed tiers, a future tier, and
a client's own `HookResult` subclass are all covered. `error` and
`configuration_error` are deliberately excluded — both are fail-open ALLOW, so
naming them would add noise and never a finding.

The sweep still passes with the wider detection, which is the useful part: it
now sees 39 of 84 handlers refusing, and none of them refuses on an event that
cannot carry it.

A `self.policy.deny(...)` lookalike does not match, because the receiver is an
Attribute rather than a Name — asserted, since a diagnostic a client is asked to
trust must not report a defect that is not there.

## 14:05 · decision · P3 — the factories decide whether Phase 3 is writable at all

Started Phase 3 and immediately hit the thing that decides its whole shape:
handlers build results with `allow`/`deny`/`ask`, not `__init__`. Those
classmethods construct via `cls(...)`, so `AdvisoryResult.allow()` has ALWAYS
returned an `AdvisoryResult` — but they were ANNOTATED `-> "HookResult"`, so
mypy typed the result as the wide class and rejected it from a handler declared
`-> AdvisoryResult`.

Probed the options against real mypy before choosing, and the probe changed the
answer:

- Annotating all three with `Self` makes the tiers usable, but
  `AdvisoryResult.deny(...)` then type-checks — the inherited method returns
  `Self` = `AdvisoryResult` — and only Pydantic stops it, at runtime, when that
  handler first executes.
- Leaving all three wide catches that, but breaks `allow` for every tier.

**So they are split deliberately.** `allow` returns `Self` on the base (safe:
ALLOW is deliverable on every event). `deny` and `ask` stay WIDE on the base,
and only the tiers that can actually refuse override them with a `Self` return.
`AdvisoryResult.deny(...)` therefore yields a `HookResult` and is rejected by the
handler's own return annotation. Recorded as an assertion, not a comment —
widening the base back would silently delete the static half while every runtime
test kept passing.

That last point is the reason for a second fixture file, `factory_forms.py`. It
pins BOTH directions: marked lines must error, and every unmarked line must NOT.
A violations-only fixture cannot express "this must compile", and that is exactly
the half that would otherwise be discovered by 84 handlers failing at once.
Every runtime assertion in `test_result_types.py` passed before this change and
after it, which is precisely why it could not have caught any of it.

### Finding: the pilot reparent broke two tests, and both were DRY debt

Reparenting the single `pre_compact` handler made two unrelated tests fail:
`test_all_pre_compact_handlers_in_config` and
`test_all_production_handlers_are_enabled`. Both reported a phantom handler
called `advisory`.

Cause: the four-condition "is this a discoverable handler?" predicate was
hand-copied FIVE times — three in `handlers/registry.py`, twice more in test
helpers — and the two copies in tests had drifted:

- one omitted `inspect.isabstract` entirely;
- the other substituted `"Base" not in attr.__name__`, which **cannot** work,
  because a per-event alias shares the class object: `PreCompactHandlerBase`
  reports its `__name__` as `AdvisoryHandler`.

Neither cost anything until a handler module first imported another `Handler`
subclass — which is exactly what reparenting does, 84 times over. Production
discovery was correct throughout (it has the abstract check), which is why the
daemon started fine while the tests failed.

Fixed at the root: one `is_discoverable_handler` TypeGuard in `registry.py`, used
by all five sites, with its own unit tests including the aliased-base case. Doing
this now rather than after the bulk reparent means the remaining 83 handlers
cannot re-trip it.

## 14:55 — Phase 3 complete: 83 handlers reparented, and three scanners fixed first

Six `python-developer` agents reparented the remaining packages in parallel:
`session_start` (11), `status_line` (13), `user_prompt_submit` (4),
`worktree_create` (1), `worktree_remove` (1), `post_tool_use` (7), `stop` (1),
`pre_tool_use` (42), `permission_request` (1). With the `pre_compact` pilot that
is all 84.

Verification after they finished:

- `mypy src/claude_code_hooks_daemon/` — **Success: no issues found in 375
  source files**
- `pytest tests/unit/core/test_handler_bases.py` — **118 passed**, which
  includes the Task 3.4 sweeps

Task 3.4 turned out to need two sweeps, not one. `issubclass` proves a handler
INHERITS its event's base; it says nothing about whether the override re-widened
the return type. So the second sweep reads `get_type_hints(cls.handle)["return"]`
and asserts it IS `result_type_for_event(event_name)`. Both carry a vacuity guard
(`> 50` handlers) so a discovery bug cannot make them pass by finding nothing.

### What I did BEFORE letting the agents loose, and why it mattered

The pilot's DRY finding (13:35 entry) said the discovery predicate was copied
five times. Rather than fix only the copies that had already broken, I grepped
every `issubclass(..., Handler)` in `src/` and `scripts/` and found three more
scanners that the bulk reparent would have broken:

- `scripts/handler_status.py` — instantiates each class immediately, so an
  abstract base would have raised `TypeError`, not merely mis-counted;
- `config/validator.py` — same predicate, same drift risk;
- `handlers/project_loader.py` — the CLIENT-FACING one. Its abstract-method
  diagnostic would have blamed an *imported base* for a missing method, telling
  a user their handler file was broken when it was not. Fixed with
  `__module__ == module.__name__` and a RED/GREEN test.

That third one is the reason DBF is worth the detour: the failure would have
appeared in someone else's repository, as a confident and wrong error message,
with nothing in this repository's tests able to see it.

## 15:05 — Phase 5: what to tell client projects, and what NOT to require

Decision 3 recorded: event bases are **recommended** for project handlers, not
required. A static guard only pays where a type-checker runs, and most client
projects do not run mypy over `.claude/project-handlers/`. Requiring the base
there would break every existing project handler on upgrade in exchange for
nothing at runtime — while the guard that actually protects them,
`validate-project-handlers`, works either way.

Three truth-changes entries rather than the one the plan anticipated. The extra
two are consequences a reader would otherwise meet as a surprise:

- the factory-form fix means the validator can now report handlers that have
  been quietly wrong since they were written (it could not see
  `HookResult.deny(...)` before);
- `merge_pseudo_results` gained a required `event_name` argument, so any caller
  outside this repository breaks loudly rather than silently clamping.

The post-upgrade task is framed as an audit, not a migration: it explains that
a NEW warning is the old rule finally able to see the common case, gives the
three honest fixes, and says to ask the user before changing behaviour — moving
a handler to a different event or downgrading a refusal to context are both
judgement calls about intent that the code cannot settle.

## 15:20 — Dogfooding: the bug this plan exists to prevent was in a SHIPPED EXAMPLE

While checking which handlers still subclass `Handler` directly (project
handlers may, by Decision 3), I looked at the three examples under
`examples/project-handlers/` — the files clients are explicitly told to copy.

`session_start/branch_naming_enforcer.py` ended with:

    return HookResult.deny(reason=f"Branch '{branch}' does not follow ...")

on **SessionStart**, which cannot carry a refusal. Every one of its claims was
false in the same direction:

- module docstring: "Blocks sessions on non-conforming branches" — it never did;
- `terminal=False,  # Change to True for strict enforcement` — flipping that
  flag would not have helped either; the event cannot refuse at any priority;
- `README.md`: "**Branch Naming Enforcer (SessionStart, Blocking)** … Denies
  sessions … Terminal (blocking)" — while the code said `terminal=False`;
- and the unit test `test_denies_bad_branch_name` asserted `Decision.DENY`
  **and passed**, because `HookResult.deny()` really does set the decision. The
  loss happens later, during serialisation. A unit test on a handler cannot
  observe a refusal dropped on the wire.

It was also written in the factory form (`HookResult.deny(...)`), so the
validator's AST scan could not see it before this morning's fix — and examples
are not covered by the built-in handler sweep at all. Three guards, and the bug
sat in the shipping documentation.

Fixed as an advisory, which is what the event can actually do: reparented to
`SessionStartHandlerBase`, refusal turned into named context (branch, expected
patterns, the `git branch -m` to fix it), docstring rewritten to say *why* it
advises and to point at `PreToolUse` for anyone who genuinely wants to block,
README entry corrected, and the acceptance tests extended to cover the
non-conforming branch — the conforming case alone passes just as happily
against a handler whose other path does nothing, which is exactly the state
this shipped in.

Two new tests, and the second is the point of the whole plan:

    def test_cannot_refuse_because_session_start_cannot(self):
        with pytest.raises(ValidationError):
            self.handler.handle({}).decision = Decision.DENY

10 passed. The bug is not merely fixed, it is now unwritable in that file.

Checked the other four for the same shape: `build_asset_watcher` and
`vendor_changes_reminder` never refuse; this repo's own `release_blocker`
(Stop) and `enforce_llm_qa` (PreToolUse) both sit on events that CAN deny. One
instance, and it was the one being handed to users.

## 15:35 — Task 5.2 was half-done, and the missing half contradicted an enforced test

I updated `HANDLER_DEVELOPMENT.md` and `PROJECT_HANDLERS.md` and treated Task
5.2 as finished. It was not. Grepping for `class .*(Handler)` across the repo
found **eight** documents teaching the old skeleton, including the root
`CLAUDE.md` — which is resident in every session and is therefore the most-read
handler guidance in the project.

That one is not merely stale, it is now self-contradictory: Task 3.4's sweep
REQUIRES every handler in `src/` to subclass its event's base, so a contributor
who followed the skeleton in `CLAUDE.md` would write a handler that fails this
repository's own test suite. Its example also returned
`HookResult(decision=Decision.DENY)` with no event named — copy that onto
`SessionStart` and you have written the exact bug this plan exists to prevent.

Rewritten with the tier table, the event base, and an explicit closing note
that the DENY is valid *only* because the example is on `PreToolUse`.

Two smaller ones fixed with it:

- `handlers/status_line/CLAUDE.md` told status-line authors to have the error
  path return `HookResult(context=[])`. Now `AdvisoryResult(context=[])`.
- `pre_tool_use/markdown_organization.py` had three `Returns:` docstrings still
  naming `HookResult` while the signatures said `GatingResult`. The reparenting
  agent converted the code and left the prose — a type-checker cannot see that
  divergence, which is why it survived a clean mypy run over 375 files.

**The lesson, and it is the DBF one again**: "update the docs" is not a task an
agent can self-certify as complete, because the thing being updated is
distributed and there is no guard that fails when a doc goes stale. I only found
these by grepping for the OLD pattern rather than by reviewing the files I had
already decided were relevant. A `doc_truth` rule that fails when a documented
handler skeleton does not compile against the current bases would have caught
all eight mechanically — worth its own plan.

Also normalised an inconsistency the six parallel agents introduced: bases were
imported identically in all 82 files, but result types split 69 via
`core` and 10 via `core.result_types`. Both work; one obvious way is better.

## 15:50 — The daemon auto-committed my hand-written CLAUDE.md edit as "Auto:"

Restarting the daemon to verify the normalised imports load produced TWO commits
I did not make:

    fb54599a  Auto: hooks daemon regenerated CLAUDE.md handler guidance   (65+/65-)
    b2c38307  Auto: hooks daemon committed CLAUDE.md                      (25+/6-)

The first is legitimate — that is the generated `<hooksdaemon>` section, which
the daemon owns. **The second is my Handler Skeleton rewrite**, verified by
diffing it: 3 occurrences of `PreToolUseHandlerBase`, and its 25 added lines are
exactly the ones I authored minutes earlier.

So the auto-commit does not distinguish between the section it REGENERATED and
unrelated hand-authored content elsewhere in the same file. Anything uncommitted
in `CLAUDE.md` at restart time is swept into a commit captioned as machine
output.

Why this matters beyond tidiness:

- **Authorship is misreported.** Someone reading `git log` sees "Auto: hooks
  daemon committed CLAUDE.md" and will not look there for a deliberate
  documentation decision. The reasoning for that rewrite lives in this plan,
  and the commit severs the link.
- **It is the mirror of the hazard RELEASING.md Step 13 already warns about** —
  that the generated `CLAUDE.md` can land in git while the SOURCE handler behind
  it does not. Same root cause, opposite direction: the auto-commit's staging is
  file-scoped when the daemon's ownership is section-scoped.
- **It commits work the author had not chosen to commit.** I was deliberately
  batching that edit into the Plan 00265 commit.

Nothing was lost and nothing else was swept in — both commits touch only
`CLAUDE.md`, and they are unpushed. I am deliberately NOT rewriting them:
`--amend` is forbidden here, and a `reset --soft` to re-caption two commits whose
CONTENT is correct is cleverness with no safety benefit. The plan commit will
name `b2c38307` instead so the trail is explicit.

**Follow-up needed** (not part of 00265): the auto-commit should stage only the
`<hooksdaemon>` region, or refuse when `CLAUDE.md` carries edits outside it —
committing on a human's behalf under a machine byline is the defect, not the
regeneration itself.

## 16:15 — The independent review earned its cost, and three findings were mine

An adversarial review of the 82-file diff came back REQUEST CHANGES — with
nothing wrong in the handler code. Worth recording both halves.

**The retype itself is clean, and measurably so.** The reviewer AST-normalised
every function in all 81 reparented modules (rewriting the four result-type
names to one token, stripping docstrings) instead of reading the diff, and found
every function body IDENTICAL modulo the rename. Class-level and module-level
statements byte-identical. No priority, terminal flag, reason string or regex
touched. It also scanned every `Decision.DENY`/`.deny()`/`.ASK`/`.ask()` against
the enclosing class's tier: zero mismatches. Zero `type: ignore`, zero `cast(`,
zero `Any` return annotations added. `get_claude_md`/`get_acceptance_tests`/
`matches`/`__init__` AST-identical throughout.

It also confirmed the two things I had found independently, and corrected my
read of one: the three stale docstrings in `markdown_organization.py` are not a
category an agent skipped — the FOURTH helper in that same file was updated, so
it is one agent missing three of four inside one file.

**What it found that I had missed, and the three that were my own errors:**

- `truth-changes/vUNRELEASED.yaml` claimed "All 84 built-in handlers now
  subclass their event's base". It is 82 of 84 — and the SAME entry then
  explains why the two `nitpick` handlers are exempt. A self-contradicting
  false claim, in a manifest that ships to clients. Mine.
- `HANDLER_DEVELOPMENT.md` had a code block that does not type-check: I updated
  the signature to `-> GatingResult` and added a docstring saying "not
  `HookResult`", then left the body returning `HookResult(`. Two more sections
  in the same file were left on the old form entirely. Mine.
- `PROJECT_HANDLERS.md` kept its `### HookResult` reference section ~15 lines
  below the new tier table, teaching
  `HookResult(decision=Decision.DENY, reason="Branch name does not match convention")`
  — which is VERBATIM the branch-naming defect the new post-upgrade task
  documents as its motivating example. Mine, and the worst of the three: the
  file argues against the bug in one section and demonstrates it in the next.
- `skills/hooks-daemon/dev-handlers.md` — the shipped skill an agent LOADS when
  asked to write a handler. It taught the old pattern, so it would keep
  generating pre-00265 code in client repos indefinitely. I had not thought to
  look at skills at all.
- `.claude/agents/python-developer.md` — same class of problem: the agent
  definition my own sub-agents are spawned from taught the old skeleton.
- `examples/project-handlers/` was half migrated: I converted
  `branch_naming_enforcer` thoroughly and left both siblings and the
  "create your own" README template on the old pattern — having had that README
  open to fix the prose above the template.

**The pattern in all six is the same one, and it is not carelessness.** Each
time I fixed the thing I was LOOKING at and not the thing next to it. Grepping
for the old pattern is what finds these; reading the files you already decided
were relevant is what misses them.

**Two findings I am deliberately NOT acting on**, with reasons rather than
silence:

- The `BranchNamingEnforcerHandler` class name and `branch-naming-enforcer`
  handler id still say "enforcer" though it now advises. The post-upgrade task
  tells clients to search for exactly that id, so renaming would break the
  instruction that helps them find their broken copy. The docstrings and README
  now state plainly that it advises. Keeping the name is the cheaper honesty.
- `project_loader.py`'s `__module__` narrowing means an abstract intermediate
  base a client defines in a SHARED module and imports into an event dir no
  longer gets the "missing required method" diagnostic when that event file is
  scanned. That is the intended behaviour, not a regression: the class is
  abstract by design, and blaming an imported base for the methods it declares
  is precisely the false positive the fix removed.
