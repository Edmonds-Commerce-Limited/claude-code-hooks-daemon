# Plan 00285 — Journal 26-08-28

> **Append-only activity log** for plan 00285. One file per day
> (`00285-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 15:04 · action · — — plan scaffolded

Plan 00285 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 15:40 · finding · P1 — shim location makes the report's "alternative" fix unviable

Read `_resolve-venv.sh` and its call sites. It is a skill-only file that
ships alongside the three wrappers under
`.claude/skills/hooks-daemon/scripts/`, NOT inside `DAEMON_DIR`
(`.claude/hooks-daemon/`) — confirmed via `install/templates/hooks-daemon`,
which shows `DAEMON_DIR` only ever ships
`$DAEMON_DIR/scripts/lib/resolve_venv.sh` (the canonical library), never a
`_resolve-venv.sh` shim. So the report's "alternative" (keep the shim,
anchor it via `DAEMON_DIR`) does not actually work — there is no
`DAEMON_DIR`-relative path to the shim either, since the shim's own sibling
location is exactly what the bootstrap re-exec destroys. Going direct to the
canonical library (the report's "preferred" fix) is the only sound option.

## 16:05 · action · T1.2 — RED: reproduced the failure with a new acceptance test

Added `test_diagnostic_script_survives_relocated_invocation` (parametrized
over all three scripts) to `tests/acceptance/test_diagnostic_scripts.py`. It
copies each script to an unrelated directory (no sibling `_resolve-venv.sh`),
runs it with `HOOKS_DAEMON_SKIP_BOOTSTRAP=1` (isolating the bug from the
bootstrap network path), and asserts venv resolution succeeds. Confirmed RED
against the unfixed scripts — the exact production symptom reproduced:
`.../relocated-invocation-site/daemon-cli.sh: line 142:
.../relocated-invocation-site/_resolve-venv.sh: No such file or directory`.

## 16:20 · action · T2.1-2.3 — GREEN: fixed all three scripts

Changed `daemon-cli.sh`, `health-check.sh`, `init-handlers.sh` to source
`$DAEMON_DIR/scripts/lib/resolve_venv.sh` directly and call
`resolve_venv_python "$DAEMON_DIR"`, mirroring `install/templates/hooks-daemon`'s
existing pattern (missing-lib and resolution-failure both get a clear
stderr directive + exit 5). New acceptance test now GREEN (15/15 in
`test_diagnostic_scripts.py`). Updated `test_skill_scripts_venv_resolution.py`
(`test_wrapper_sources_canonical_resolver` replaces the stale
`test_wrapper_sources_resolver`, now also asserts no `$0`-relative
resolution remains) and `test_health_check_honest_failure.py` (stub moved
to `scripts/lib/resolve_venv.sh` under the fixture's `DAEMON_DIR`). Full
dependent-test sweep (35 tests across the three files) green; shellcheck
clean on all three scripts (`--norc -x --source-path=SCRIPTDIR`).
Committed at 1ffb5e4a.

## 16:45 · action · T3.1 — DBF guard added to audit_shell.py

Extended `scripts/qa/audit_shell.py` (already wired as the `shell_audit`
QA check) with a new rule `bootstrap-reexec-dollar0-source`: flags
`dirname "$0"` / `${0%/*}` appearing after a `# === SELF-BOOTSTRAP BEGIN`
marker in a script (a reference before the marker is unaffected by the
re-exec, so it's exempt). Also widened the default scan to cover BOTH
`scripts/` and the skill scripts directory — previously only `scripts/`
was scanned by default, so the rule would never have fired on the actual
offending files. TDD: wrote 5 new unit tests in `test_audit_shell.py`
first, confirmed RED (2 of 5 failing, as expected — the "must flag" cases),
implemented the rule, confirmed GREEN (21/21 in the file). Verified against
git history: running the new rule against the pre-fix daemon-cli.sh (commit
73fe4e37c) reports the violation at line 142 — the exact line the field
report named. A full `audit_shell.py` scan of the current tree reports zero
violations. black/ruff/mypy clean.
