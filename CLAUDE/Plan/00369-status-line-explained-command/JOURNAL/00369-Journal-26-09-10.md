# Plan 00369 — Journal 26-09-10

> **Append-only activity log** for plan 00369. One file per day
> (`00369-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 11:21 · action · — — plan scaffolded

Plan 00369 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 11:25 · action · — — PLAN.md filled in, README indexed

PLAN.md filled in (Overview, Goals, Non-Goals, 4-phase Tasks, Success
Criteria including the release-notes callout holding criterion), README.md
row added under "Status Line / Agent View" and stats bumped (369 total / 17
active / 359 folders / 356 distinct).

## 11:27 · finding · P1 — the roster: 14 concrete handlers, not 13

`src/claude_code_hooks_daemon/handlers/status_line/` has 19 `.py` files.
`__init__.py`'s `__all__` exports only 13 handler classes — it is missing
`StartupCleanupHandler` (the literal "🧹 1 stale" glyph from the owner's
field report), which is nonetheless discovered and registered correctly by
`HandlerRegistry` (directory-glob discovery, not the `__all__` list). Total
concrete `StatusLineHandlerBase` subclasses: `AccountDisplayHandler`,
`ContextSidecarHandler`, `CurrentTimeHandler`, `DaemonStatsHandler`,
`DowngradeIndicatorHandler`, `EnvironmentIndicatorHandler`, `GitBranchHandler`,
`GitRepoNameHandler`, `ModelContextHandler`, `MultithreadIndicatorHandler`,
`StartupCleanupHandler`, `SupervisorIndicatorHandler`, `UpgradeNotifierHandler`,
`WorkingDirectoryHandler` = 14. Five non-handler helper modules
(`context_tiers`, `downgrade_state`, `mtime_cache`, `settings_reader`,
`thread_registry`) carry no `Handler` subclass and need no explanation.

## 11:29 · decision · P1 — `explain_segment()` is read-only; the CLI icon
line is a labelled reference, not a live replay

Several handlers mutate on-disk state inside `handle()` (`ContextSidecarHandler`
writes a sidecar file, `MultithreadIndicatorHandler` upserts a heartbeat,
`DowngradeIndicatorHandler` persists per-session counters) — calling `handle()`
from a one-shot introspection CLI would pollute the REAL daemon's on-disk
state with synthetic CLI-session entries (e.g. inflating the live `🧵 Y/X`
thread count). `explain_segment()` is therefore a separate, explicitly
read-only method: each handler computes `current_value` from a read-only
peek at the same on-disk/live state `handle()` would read, without ever
calling `handle()` itself from the CLI path. Consequence: the CLI's rendered
"icon line" is a REFERENCE line built from each handler's own glyphs, labelled
as such — not a byte-identical replay of a live status-line render, which is
impossible anyway for model/context/effort/workspace-diff segments that only
exist inside a live Claude Code render payload. Recorded as a Non-Goal in
PLAN.md.

## 11:31 · action · — — worktree next

`git worktree add untracked/worktrees/worktree-plan-00369 -b worktree-plan-00369`
next, then all further work happens there.
