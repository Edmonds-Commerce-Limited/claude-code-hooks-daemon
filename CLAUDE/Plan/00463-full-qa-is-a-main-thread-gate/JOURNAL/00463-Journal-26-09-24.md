# Plan 00463 — Journal 26-09-24

_Scaffolded by `mkplan.bash`; timestamps in this file are UTC._

> **Append-only activity log** for plan 00463. One file per day
> (`00463-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` UTC 24h (the date is in the filename, also UTC); times increase
>   down the file. A file with no sentinel line above predates this system —
>   its times are local, zone unrecorded (legacy; never rewritten).
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 14:04 · action · — — plan scaffolded

Plan 00463 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 14:05 · decision · —   — owner request and the coordinator's amendment

The owner asked whether sub-agents should be restricted from running full
QA: targeted QA in agents, the full gate on the main thread only, enforced
by the daemon through marked command patterns. They pointed at resource
exhaustion and the full run that happens at merge anyway.

Measured when the request arrived: five `llm_qa.py all` runs were executing
at once, one each in the issue-55, 00460, 00461, 00462 and N22 worktrees,
on 8 cores with a load average of about 4.6. The per-checkout run lock
(Plan 00262) does not span worktrees.

Coordinator view, as given to the owner: agree, with one amendment. The
full gate must stay BEFORE the merge, run by the coordinator on the branch
head one at a time, not moved to after the merge on `main`. Cross-cutting
checks break from distant changes, and a first full run on `main` would
land every such break there. Two dependencies are flagged for Task 1.1:
whether teammates and Workflow agents carry `agent_id`, and the deadlock
with orchestrator-only mode (00418) once that mode denies the main
thread's Bash.
