# Plan 00135 Journal — 2026-09-08

## Evidence that this plan's slice list is delivered

Recorded, not acted on. This entry does not change the plan's status: flipping
a High-priority plan that carries three hostile audits to a terminal state is
the owner's call, and everything below is reversible reading rather than a
change. It also answers the plan-QA sweep's journal-freshness advisory, which
has been reporting this plan quiet for seven days while the thing it plans runs
in production.

### What the plan said it would build

Since the 2026-07-09 Fable audit, the live plan of record is not this
document's Phases 1–4 (ARCH-A / tmux, explicitly shelved as the documented
fallback) but the slice list in `plan-audit-fable-1.md` §5. The last journal
entry, dated 2026-09-01, ends:

> Next: Slice 1 (observe-only `context_sidecar` status-line handler, pure
> `src/`, dogfoodable in this ccy session), then Slice 2 (`claude-supervise`
> TDD'd, `--dry-run` default).

### What exists now

Both of those, under the names the audit chose:

```
src/claude_code_hooks_daemon/handlers/status_line/context_sidecar.py
src/claude_code_hooks_daemon/handlers/status_line/supervisor_indicator.py
.claude/ccy/claude-supervise.py            (6163 lines, git-tracked)
```

- **Slice 1** — `context_sidecar` is the status-line handler the audit
  specified, by that name.
- **Slice 2** — `claude-supervise` exists as a tracked, versioned artefact and
  is covered by QA: `run_tests.sh` passes `--cov=.claude/ccy` alongside
  `--cov=src/...`. `supervisor_indicator` is the status-line segment the audit
  proposed in place of the second tmux pane (`🤖 dry-run` / `🤖 armed 2/5`).
  The rail set the slice enumerates — frozen allowlist, `max_total_cost_usd`,
  wall-clock deadman, ledger-before-send, abort file, drift latch — is present
  in the supervisor source.
- **Slice 3** — armed and integrated. The supervisor's own docstring describes
  injecting "a real /compact when the context goes red", gated on idle plus
  cooldown and cap, with CRITICAL bypassing the cooldown. It is not theoretical
  in this checkout: the two `/compact` commands that arrived in the session
  writing this entry came from it.

### Lineage

Twenty-three completed plans in `Completed/` carry `supervisor` or `ccy` in
their names (00147 through 00339), and four more are active (00160, 00166,
00168, 00319). The supervisor workstream has been proceeding under its own
plan numbers for months. This document reads as its ancestor design record
rather than as its live tracker — which is consistent with its own header,
where the plan of record was handed to the audit's slice list.

### What would argue against a terminal status

Stated so the decision is made on both sides:

- The deferred item (PostCompact re-orientation) is listed as roadmap, not as
  cut, so some scope remains nominally open.
- The ARCH-A fallback is documented rather than deleted, and the plan asks that
  the SB-1 enforcement fix be recorded "if ARCH-A is ever revived" — this
  document is where that instruction lives.
- Slice 2 recommended `src/claude_code_hooks_daemon/supervise/` with a console
  entry point; what shipped lives at `.claude/ccy/claude-supervise.py`. That is
  a different location, though it satisfies the stated properties (tracked,
  versioned, QA-covered) and the global supervisor contract documents that path
  as canonical.

### Suggestion

Mark **Superseded**, pointing at the 00147→00339 lineage, and carry the ARCH-A
revival note somewhere that survives archival — the fallback instruction is the
one piece of this document with no home elsewhere.
