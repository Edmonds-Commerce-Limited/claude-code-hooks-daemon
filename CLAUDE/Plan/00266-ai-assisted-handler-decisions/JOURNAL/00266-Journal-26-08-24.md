# Plan 00266 — Journal 26-08-24

> **Append-only activity log** for plan 00266. One file per day
> (`00266-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 14:32 · action · — — plan scaffolded

Plan 00266 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 16:55 — Four of the five open questions settled, and a doc bug in our own tree

Re-fetched `code.claude.com/docs/en/hooks` asking the five gaps directly, after
two `claude-code-guide` agents went idle without their reports reaching this
session. A targeted second fetch answered four of them, which is worth noting
as method: the first pass read the page, the second pass INTERROGATED it.

**`.claude/hooks.json` does not exist.** Hooks live in the `settings.json`
family (`~/.claude/`, `.claude/settings.json`, `.claude/settings.local.json`),
plus managed policy settings and skill/subagent frontmatter. There IS a
`hooks/hooks.json` — but it belongs to PLUGINS, which is almost certainly where
our filename came from.

That makes it a doc-truth bug in THIS repo, not a gap in the plan:
`CLAUDE/ARCHITECTURE.md` had it in a configuration-locations table and in a
worked example, and `CLAUDE/HANDLER_DEVELOPMENT.md` told contributors to "use
`.claude/hooks.json` with agent-based hooks". Anyone following that guidance
would have created a file Claude Code never reads and concluded their hook
silently did not fire. Fixed in both.

**Coexistence: "All matching hooks run in parallel."** A native `prompt`/`agent`
hook and the daemon's `command` hook both run, concurrently. Combined with the
code reading earlier today — the legacy detector skips entries with no
`command` key, and reconcile leaves existing event keys untouched — native
LLM hooks are adoptable here today with no code change.

**The nuance I nearly published wrong.** My first draft of the fix said the
parallelism means a slow agent hook "does not serialise behind the daemon's
~1.8ms dispatch", which is true and reads as though the latency problem had
gone away. It has not. Parallel means the hooks do not COMPOUND — the cost is
the slowest hook, not the sum — while the tool call still blocks on that
slowest one. A `prompt` hook on `PreToolUse` still turns a 45ms round trip into
a multi-second one. I killed the QA run to correct it before it landed, because
a half-true performance claim in ARCHITECTURE.md is exactly the kind of thing
someone builds on.

**Still unanswered, and it is the one that matters most**: the page says
NOTHING about cost, billing or rate-limit consumption for `prompt`/`agent`
hooks. A design that fires a model call per event cannot be costed from the
documentation. Any proposal must treat per-invocation cost as an unknown to be
MEASURED, not estimated. Nor is an introduction version annotated, so no
minimum Claude Code version can be asserted.

Also unconfirmed: there is no documented precedence rule for hooks that
DISAGREE — only that they all run in parallel. The `deny > defer > ask > allow`
ladder is the general multi-hook rule; whether a `prompt` hook's verdict folds
into it identically remains inference.

## 2026-08-24 — Handoff resolved, plan docs settled

The `ai-handlers-planner` agent and I were editing `PLAN.md` and
`RESEARCH-claude-code-native-hooks.md` concurrently. Its two `Write` attempts
were denied by `write_clobber_guard` — correctly: my edits had landed after its
read, so a `Write` would have silently reverted them. I stopped editing both
files, stopped the in-flight QA run so it was not working against a moving
gate, and handed the files over with an explicit list of the bullets I had
already replaced, telling it to use `Edit` rather than `Write`.

That is worth recording as the general lesson: `write_clobber_guard` did not
just prevent data loss here, it surfaced a coordination problem we would
otherwise have discovered only by noticing missing prose later. Two agents on
one file need an explicit owner, not careful timing.

Two contradictions I flagged rather than fixed myself, both now resolved by the
planner:

- The Overview called native hooks "real, **free**, and run in parallel" while
  the Headline finding seventeen lines below said the latency is NOT free.
  "Free" had meant free to *adopt* — true, and worth saying — but one clause
  from "run in parallel" it reads as free of latency, and the headline is the
  part people skim. Now reads "adoptable with no code change … costs nothing to
  adopt, but still costs seconds per invocation".
- `RESEARCH.md` described a native hook as sitting "alongside (or instead of) a
  `command` hook". "Instead of" is a footgun: `reconcile_settings_hooks` is
  additive per **event**, not per entry, and its own docstring confirms present
  events "including any client-added custom entries — are left untouched". So
  replacing the daemon wrapper means the self-heal never restores it and every
  `command`-dispatched handler on that event goes dark. Rewritten to say
  alongside-only, with the mechanism spelled out.

Plan status stays **In Progress**: Phase 1 (research) is done and answers the
user's question, but Phases 2–5 are build work nobody has asked for yet. The
plan is the deliverable; the code is a proposal.
