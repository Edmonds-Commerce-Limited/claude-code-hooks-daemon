# Plan 00298 — Journal 26-09-01

> **Append-only activity log** for plan 00298. One file per day
> (`00298-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 06:23 · action · — — plan scaffolded

Plan 00298 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 06:30 · action · — — PLAN.md and BRAINSTORM.md filled in

Filed at the owner's direct instruction after last night's incident: the
hourly failsafe recovery cron fired ~14 times overnight while the session
was stably blocked only on two pending human decisions, each tick costing a
full model turn to produce a guaranteed no-op ("token burning for the sake
of it once we hit a full blockage").

Read `recovery_cron_advisor.py` (post_tool_use) and `auto_continue_stop.py`
(stop) to ground the brainstorm. Key finding: the waste is NOT a deny-loop —
`auto_continue_stop.py` Branch 2 cleanly allows a `STOPPING BECAUSE:`
explained stop — the waste is the per-tick model turn itself, since the
cron's canonical prompt reaches the model on every tick regardless of
session state.

Wrote `BRAINSTORM.md` evaluating 5 ideas (session-side backoff, daemon-side
`UserPromptSubmit` suppression, a Stop-recorded blocked-state marker, cadence
baked into the cron prompt, and explicit distinction of blockage types).
Recommendation: the blocked-state marker (Stop handler) + UserPromptSubmit
suppression keyed on the canonical cron prompt text is the only
deterministic, zero-token option; session/prompt-text-only backoff ideas are
convention-only and still cost a turn per tick, so they're documented as
fallback only, not primary.

Filled in `PLAN.md`: Overview cites the incident evidence, Goals/Non-Goals
scope this plan to design-only (no daemon source edits here — that's a
follow-on plan per `src/CLAUDE.md`'s daemon-source restriction), Phase 1
is owner review of the brainstorm, Phase 2 is filing the implementation
plan once confirmed.

## 06:35 · handoff · — — ready for owner review

Design complete: `PLAN.md` + `BRAINSTORM.md` in this folder. Next: add the
README.md Active Plans row, bump the Statistics Active count, commit and
push. Task 1.1 (owner review) is next — once confirmed, file the Phase 2
implementation plan per BRAINSTORM.md's recommendation (Stop-handler marker
+ UserPromptSubmit suppression + bounded expiry).

## 08:20 · decision · P1 — owner approved with brittleness caveat, build minimal

Owner reviewed BRAINSTORM.md's recommendation directly in this session
rather than via a separate implementation plan: approved for implementation,
but with an explicit caveat — "sounds complex and brittle to me." Ruling:
implement it here (superseding the original Phase 2 "file a follow-on plan"
task) but build the MINIMAL version — one marker, one session-scoped
validity check, no fallback chains — and make every failure mode fail OPEN
so the worst case is always "the cron tick reaches the model as today," never
a stuck suppression.

## 08:40 · action · T2.1-T2.3 — implementation (TDD)

Read `auto_continue_stop.py`, `recovery_cron_advisor.py`,
`idle_housekeeping_advisor.py` and `standing_authorisations.py` to ground
conventions (marker persistence idiom from `goal_ledger.py`/`private_io.py`,
canonical-cron-prompt marker idiom already duplicated informally in
`idle_housekeeping_advisor._RECOVERY_MARKER`).

Built, test-first throughout:
- `utils/blockage_marker.py` — the shared marker primitive (session_id +
  recorded_at JSON, atomic tmp+replace write, owner-only 0600, fail-open
  read/write/clear, `marker_is_valid` session+expiry check). 19 unit tests.
- `recovery_cron_advisor.CANONICAL_CRON_PROMPT_MARKER` — exported so the new
  suppression handler shares the marker constant instead of duplicating the
  string (per the task instruction). Pinned by a substring test.
- `auto_continue_stop.py` Branch 2: added `_HUMAN_BLOCKED_PATTERNS` (4 narrow
  regexes: "blocked only on human input", "blocked only on owner/user input",
  "need user input", "waiting on/for the owner's/user's decision/input/
  response/answer") and `_maybe_record_human_blocked_marker`, called only on
  the STOPPING BECAUSE: ALLOW path. Regression test pins the BRAINSTORM.md
  false-positive example ("waiting for human review of this PR... but
  background polling continues") does NOT arm the marker.
- `handlers/user_prompt_submit/failsafe_cron_blockage_suppressor.py` (new):
  matches only a delivered canonical-cron-prompt tick; DENYs (blocks/erases
  the prompt) only when a valid marker exists for the session; fails open on
  no marker, wrong session, expired marker, corrupt marker, and missing
  project context. `terminal=False` deliberately — `idle_housekeeping_
  advisory` and `standing_authorisations` key off the same canonical prompt
  and must keep running on every non-suppressed tick; a non-terminal DENY
  still survives later handlers per `core/router.py`.

New `HandlerID`/`Priority` constants (priority 37, ahead of
`LSP_ENFORCEMENT`=38, workflow band). New `RuleID.FAILSAFE_CRON_SUPPRESSED`
+ a single `Rule` so the handler's DENY path satisfies rule-parity.

## 09:05 · finding · — — full-suite self-scan flagged the new fail-open sites

`tests/unit/qa/test_audit_error_hiding.py`'s whole-repo self-scan (not
`error_hiding_blocker`, the live PreToolUse handler, which only checks
CONTENT BEING WRITTEN) flagged 5 sites across the new fail-open code as
`return-none-on-error`/`log-and-continue`. All five are the deliberate
fail-open contract this plan requires, not concealed failures — added
documented entries to `scripts/qa/error_hiding_exclusions.json` (mirroring
the existing `goal_ledger.py` entries) rather than restructuring working
code to dodge a pattern-matcher. Re-ran the self-scan: 37/37 pass, no
stale-exclusion warnings.

`semgrep`-based tests errored (`not installed in this venv`) — pre-existing
environment gap in this worktree, unrelated to this plan's changes; the
identical errors do not reference any file touched here.

## 09:20 · action · T2.5 — docs

Added `docs/guides/HANDLER_REFERENCE.md` entries: a new
`failsafe_cron_blockage_suppressor` section (options, config example,
fail-open/never-terminal notes) and a "Human-input blockage marker" addendum
under the existing `auto_continue_stop` entry. `scripts/qa/
check_handler_reference.py` passes (no drift).

## 09:35 · handoff · — — implementation complete, QA green, ready to commit

All new/changed test files pass (`blockage_marker`, `failsafe_cron_blockage_
suppressor`, `auto_continue_stop`, `recovery_cron_advisor`,
`test_rule_parity`, `test_audit_error_hiding`); `mypy --strict`, `black -l
100`, `ruff check` clean on every touched file; full `tests/unit` suite green
(pre-existing semgrep-environment errors aside). Next: commit on this
worktree's branch (do not push per task instructions), then report back with
the branch/commit and QA summary.
