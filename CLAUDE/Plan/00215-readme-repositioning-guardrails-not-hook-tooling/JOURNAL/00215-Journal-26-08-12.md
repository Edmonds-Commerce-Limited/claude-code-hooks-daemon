# Plan 00215 — Journal 26-08-12

> **Append-only activity log** for plan 00215. One file per day
> (`00215-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 13:19 · action · — — plan scaffolded

Plan 00215 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 13:48 · action · P1-P3 — README repositioned, all numbers re-measured

Read `PLAN.md` and `REQUESTS.md` in full, then re-measured every figure the
request cited rather than copying it:

- Forwarder scripts in `.claude/hooks/`: 31 (32 entries minus the `handlers/`
  support subdirectory) — matches the request's 31.
- `def test_` occurrences under `tests/`: 10,658 (via `grep -rn`), not the
  request's ~9,888.
- `find src -name "*.py" -exec wc -l` total: 69,419 lines; `find tests -name
  "*.py" -exec wc -l` total: 157,974 lines — not the request's 63,509 /
  145,073. Ratio 157974/69419 ≈ 2.28 → rounds to the same "2.3×" the request
  used, despite the absolute numbers differing (codebase has grown since the
  request was written against v3.51.0).
- `pytest --collect-only -q` against this worktree (via the shared dogfood
  venv, read-only): 11,820 tests collected right now. Left the top badge
  ("11400+") untouched — it is still a true floor and the task didn't ask to
  change it, only to add the ratio beside it.
- Priority bands: re-read `constants/priority.py`'s `PriorityRange` class.
  Actual: `## What's Built In` had 4 categories (not 5), `## Writing Custom
  Handlers` lists 5 bands (not 6) — the request's "5 vs 6" didn't match either.
  Real gap: no "Advisory" category existed for the 56-60 band. Fixed by adding
  `### Advisory` to `## What's Built In` (british_english, daemon_docs_guard)
  — did NOT touch `## Writing Custom Handlers`, which is on the explicit
  do-not-rewrite list, so its "56–79" figure (actually 56–60 per source) was
  left alone rather than silently drifting further from a section I'm not
  allowed to edit.
- Copyright: LICENSE says "Copyright (c) 2025 Edmonds Commerce Limited" —
  matches the README's existing License section exactly. Used the GitHub org
  URL already used elsewhere in the file for the new top-of-file maintainer
  credit, rather than an unverified external site.

Applied the full restructure via one `Write` (strapline → `## What this
solves` → `## Why a daemon rather than plain hooks` (retitled from `## Why
Use This?`) → `## What's Built In` (+ new `### Advisory`) → `## Deterministic
vs Agent-Based Hooks` (promoted) → `## Installation & Updates` → rest
unchanged). Added a Platform badge (Task 2.4) and the source/test ratio line
(Task 2.2). Did **not** add an origin-story line — Non-Goals gates that on a
real detail from the maintainer, and none was supplied; left absent, not
invented.

Did not touch `## Project-Level Handlers`, `## Writing Custom Handlers`,
`## Configuration`, `## Git Integration`, `## Troubleshooting`, or the
generated handler-count claim, per the explicit do-not-rewrite list.

`llm_qa.py` hardcodes `untracked/venv/bin/python` (not the fingerprint-keyed
resolver), and this fresh worktree has no venv of its own. Rather than
building one or touching the QA script, symlinked
`untracked/venv -> /workspace/untracked/venv-workspace-py311-81c29529` (the
shared dogfood venv) inside this worktree's gitignored `untracked/` — local
to this worktree only, nothing tracked touched.

Committed at this point (commit c65f90d5) per the coordinator's instruction:
the work was sitting uncommitted while `llm_qa.py all` ran in the background
(~13 min, contending with several sibling agents' own pytest runs on the same
host). Committed README.md, PLAN.md and this journal file first, then let the
QA run finish rather than blocking further.

## 13:54 · finding · T3.2 — in-worktree QA result: 17/20, real gate is post-merge

`llm_qa.py all` finished: 17/20 PASSED, 3/20 FAILED. `doc_truth: 0
violations` and `british_english: 0 violations` — the two checks most
relevant to a README edit — both passed cleanly. The 3 failures are
worktree/shared-venv artefacts, not README regressions:

- `tests`: 11805 passed, 0 failed, 15 skipped, but exit code non-zero and
  coverage reported as an implausible 4.2% — almost certainly the borrowed
  shared venv's coverage data getting stomped by concurrent sibling-agent
  pytest runs on the same host.
- `smoke_test`: 0/3 probes passed — needs a live daemon socket that only
  exists for the real project root; a worktree cannot provide one.
- `canonical_callers`: did not run.

Per the coordinator: this in-worktree run is informational only. The
authoritative gate is the full suite re-run on the merged tree against the
real project root, which the coordinator owns after merge.
