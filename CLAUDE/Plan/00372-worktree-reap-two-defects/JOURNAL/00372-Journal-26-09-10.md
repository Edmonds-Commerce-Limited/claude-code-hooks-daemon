# Plan 00372 — Journal 26-09-10

> **Append-only activity log** for plan 00372. One file per day
> (`00372-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 12:41 · action · — — plan scaffolded

Plan 00372 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 12:55 · finding · — — Defect 2 reproduced in a throwaway repo

`git branch -d refs/heads/testbranch` fails with `error: branch
'refs/heads/testbranch' not found.`, while `git branch -d testbranch` (bare
name) succeeds — including when a same-named tag also exists, where it
correctly deletes the branch and leaves the tag alone. `git branch -d
heads/testbranch` (no `refs/` prefix) also fails. So `git branch -d`
resolves its argument only inside `refs/heads/` directly; it is not a
general ref-resolving command, and `branch_ref()`'s "bare name is ambiguous"
rationale (Plan 00254) does not apply to it. Grep confirms `worktree_reaping.py`
is the ONLY call site in the codebase that feeds `branch_ref()` output to
`branch -d`; every other `branch_ref()` use feeds a genuinely ambiguity-prone
command (`rev-parse`, `cherry`, `merge-base`, `diff` ranges).

## 13:00 · finding · — — age proxy chosen for Defect 1

Confirmed `.git` in a linked worktree is a plain pointer file `git worktree
add` writes once, and its mtime survives `status`/`add`/`commit` inside the
worktree unchanged (tested live: mtime identical before and after a commit 7s
later). Chosen as the age proxy — one `stat()`, no extra git subprocess.

## 13:20 · decision · P1 — guard design: age + live process, both independent

Two additive signals to `WorktreeState`: `live_process_pids` (one `/proc`
scan per collection run, matched against each worktree's resolved path,
excluding the reaper's own pid) and `age_seconds` (from the `.git` pointer
mtime, `None` on failure treated as "too young" — same unknown-is-not-safe
rule as the existing counts). `MINIMUM_AGE_SECONDS = 900` (15 min): generous
because over-caution only costs disk, guessing wrong the other way costs
work. Rejected a full recursive file-mtime scan (team lead's other named
candidate) as redundant with the live-process signal for the "actively
writing" case, and unbounded-cost on a large worktree for no extra coverage
of the actual gap, which is the creation-to-first-activity race.

## 13:45 · action · T1.1-T1.3 — Defect 2 fixed and pushed

RED: a `_RealisticFakeGit` mimicking real git's `branch -d` constraint
reproduced the exact live message
(`... but git kept the branch X: error: branch 'refs/heads/X' not found.`)
in `test_worktree_reap_action.py`. GREEN: both `reap_worktree` and
`prune_branch` now pass the bare name; corrected the two comments that
repeated the disproven "bare name risks a same-named tag" claim for `-d`
specifically. Updated the three sibling test files that hard-coded the
buggy full-ref argv as expected, including `test_orphaned_branches.py`'s
docstring narrative. 16+16 tests green. Committed `edfacfdc`, pushed.

## 14:30 · action · T2.1-T2.4 — Defect 1 fixed and pushed

RED: renamed `test_a_worktree_at_the_base_is_reapable` (real git, was
asserting the live incident's exact shape reapable) to assert it refused;
added a companion backdating the `.git` mtime to prove discrimination, and
a third starting a real `sleep 30` subprocess with cwd inside an old,
backdated worktree to prove the two signals are independent. GREEN:
`WorktreeState.live_process_pids`/`.age_seconds`, two new `reap_refusal_reason`
checks, `collect_worktree_states(process_cwds_fn=, age_fn=)` DI mirroring
`run_fn`. Renamed the two default-supplier functions to drop their leading
underscore (`default_process_cwds`, `default_worktree_age`) so
`cmd_worktree_reap` could reference the exact same default each accepts,
after multiple heterogeneously-typed `**dict` unpacks at the CLI call site
turned out not to type-check under pyright (each `**dict[str, T]` unpack
must be checked against every keyword parameter, not just the one whose
literal key it holds). 104 tests green, 100% line/branch coverage on
`worktree_reaping.py`. Pyright clean on every touched file (10 pre-existing
unrelated errors elsewhere in the repo, none in files this plan touched).
Committed `259227ef`, pushed. Release-notes callout `24-...md` added and
pushed (`8f6207eb`).

## 15:00 · handoff · — — next: daemon restart + full QA

All code/tests done, PLAN.md tasks ticked through Phase 2 and 3.2. Next:
restart this worktree's own daemon (acceptance probes dispatch through the
live daemon), then `./scripts/qa/llm_qa.py all`, then set Status Complete
and write the build report.

## 15:30 · action · T3.1 — daemon restarted, full QA run, 5 findings fixed

`bin/hooks-daemon restart` — RUNNING, 31/31 per-event listeners. First
`llm_qa.py all`: 22/27 passed. Fixed all five real findings:

- `magic_values`: a bare `timeout=5` in my new subprocess test → replaced
  with `Timeout.PROCESS_KILL_WAIT` (its documented "wait for SIGTERM before
  SIGKILL" meaning matches exactly).
- `error_hiding`: two deliberate error-to-sentinel patterns in
  `default_process_cwds`/`default_worktree_age` (silent-continue,
  return-none-on-error) — both are this module's own established idiom
  (matches `_count`/`_unlanded`'s existing UNKNOWN-sentinel contract) and
  the project has a self-service, documented allowlist for exactly this
  (`scripts/qa/error_hiding_exclusions.json`, ~30 existing entries in the
  same voice). Added two entries rather than changing the design.
- `tests`: the one failing test (`test_audit_error_hiding.py`'s self-scan)
  was the SAME root cause as `error_hiding` above — resolved by the same fix.
- `format`: `black` (the actual gate, `run_format_check.sh`, auto-fixes) and
  `ruff format` disagree on wrapping style for a few constructs; black is
  authoritative since it is what the gate runs. Applied black's fixes to
  every file this plan touched.
- `pyright`: unchanged at 10 errors both before and after, all four in files
  this plan never touched (`result_types.py`, three test files under
  `handlers/`/`status_line/`) — pre-existing, out of scope.

Re-ran full QA: 25/27 passed, only `pyright`'s 10 pre-existing errors and
(transiently) `format` oscillating once more on `test_orphaned_branches.py`
before a second `run_format_check.sh` pass stabilised it, with a printed
warning that black cannot fully AST-verify py313-target output under this
worktree's Python 3.11 interpreter — a repo-wide environment quirk, not
something this plan's code caused; noted for the coordinator rather than
chased further.

**Scope note**: the same `run_format_check.sh` run also auto-reformatted
`tests/unit/core/test_claude_md_injector.py`, a file this plan never
touches. Left that diff uncommitted in the worktree (not staged) rather
than committing an unrelated file or using a blocked destructive command
to discard it.

104/104 worktree-reap tests green, lint clean, tests green. Status → Complete.
