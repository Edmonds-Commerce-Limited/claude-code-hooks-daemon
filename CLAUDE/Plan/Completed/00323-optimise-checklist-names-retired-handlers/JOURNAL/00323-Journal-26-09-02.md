# Plan 00323 — Journal 26-09-02

> **Append-only activity log** for plan 00323. One file per day
> (`00323-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 15:44 · action · — — plan scaffolded

Plan 00323 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 16:05 · finding · — — found by running the step, not by reading it

Plan 00322 ended with "so run it". Scoring this repo against the checklist
produced four MISSING entries — `bash_error_detector`,
`yolo_container_detection`, `validate_plan_number`,
`plan_completion_advisor` — which `bin/hooks-daemon handlers` confirmed are
not registered at all. All four are in `RETIRED_HANDLERS`, deleted by Plan
00237.

So this repo, which enables essentially everything, scored 25/29 and was
told to enable four handlers that cannot be enabled. Every client running
the review got the same phantom deficit.

## 16:20 · action · P1-P2 — fixed and locked

Areas 3/4/5 lose the dead items (7→5, 6→5, 5→4), total 29→25, report
template rows and the Step 5 example recommendation follow. Two contract
tests: no `RETIRED_HANDLERS` name may appear anywhere in the script, and
every dotted `handlers.<event>.<name>` reference must resolve to a live
`HandlerID.config_key`. Both were RED on the four names before the fix.

Worth noting for the next retirement: the instruction body is a shell
heredoc, so nothing else in the build would ever have caught this.
