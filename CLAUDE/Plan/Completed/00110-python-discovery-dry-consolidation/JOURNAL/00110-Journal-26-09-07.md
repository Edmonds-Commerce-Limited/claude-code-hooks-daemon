# Plan 00110 — Journal 26-09-07

> **Append-only activity log** for plan 00110. One file per day
> (`00110-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 19:20 · finding · P1-P6 — the plan was 6 phases behind its own checkboxes

Plan 00341 flipped this plan's header from `Not Started` to `In Progress`
because 8 of 60 boxes were ticked. Auditing what those 60 boxes actually
describe: **Phases 1 through 6 have all shipped.** The checkboxes were simply
never ticked.

This is the same rot Plan 00341 addressed, one level down. `header-body-coherence`
cannot see it, because the header is now honest — it is the TASK tree that
lies, and nothing checks a task tree against the artefacts it claims.

Verified on disk, not inferred:

| Claim | Artefact |
| --- | --- |
| Phase 1 — design both helper APIs | `scripts/lib/python_discovery.sh`; `find_latest_python` / `find_latest_python_or_explain` in `daemon/paths.py` |
| Phase 2 — TDD bash helper | `tests/acceptance/test_python_discovery_bash.py` |
| Phase 3 — TDD python helper | `tests/unit/daemon/test_paths_python_discovery.py` |
| Task 6.1 — shared fixture set | `tests/fixtures/python_discovery/` — 7 curated JSON fixtures |
| Task 6.2 — 50 randomised parity fixtures | `tests/integration/test_python_discovery_parity.py`, incl. `test_property_run_count_meets_plan_quota` guarding the quota |

Phase 4 is itself the proof the earlier phases landed: every one of its tasks
describes MIGRATING a WET site onto `find_latest_python`, which cannot have
happened if the helper did not exist. Task 4.1 records replacing 200 lines with
a 25-line wrapper that sources the Phase 2 artefact.

Ran the tests rather than trusting the file listing: **107 passed** —
90 across `test_python_discovery_parity.py` + `test_paths_python_discovery.py`,
17 across `test_python_discovery_bash.py` + `test_skill_install_python_discovery.py`.

Success criterion checked live too: `_COMPATIBLE_PYTHON_CANDIDATES` and
`_is_python_at_least_311` survive in `src/` and `scripts/` only as two COMMENTS
recording their own removal — exactly the survivor set Task 4.6 documented.

Also cleared the standing `path-existence` advisory: the plan's `src/.../`
ellipsis shorthand was unresolvable to the checker. Replaced with the real
paths, all four of which exist.

## 19:25 · blocker · P7 — the remaining work is human-gated, and that is the answer

Phase 7 is the release. `/release` may only be invoked by a human in the
current session: a release is a decision about SCOPE, which is not visible from
inside the repository, and no state file exists so none is in progress. An
agent must never start one.

So this plan's goal-ledger entry cannot be cleared by working harder on it.
Everything an agent can do is done. Phase 7 is marked AWAITING A HUMAN in
PLAN.md so the next reader does not re-derive this.
