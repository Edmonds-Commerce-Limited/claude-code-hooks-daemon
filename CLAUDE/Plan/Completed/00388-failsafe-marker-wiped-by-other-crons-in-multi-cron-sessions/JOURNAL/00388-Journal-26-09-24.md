# Plan 00388 — Journal 26-09-24

_Scaffolded by `mkplan.bash`; timestamps in this file are UTC._

> **Append-only activity log** for plan 00388. One file per day
> (`00388-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM, ONLY with
> `mkplan.bash --journal <plan-number> <category> <body-file>` (it stamps the
> real UTC time; a hand-written entry is denied); NEVER edit earlier entries
> (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` UTC 24h (the date is in the filename, also UTC); times increase
>   down the file. A file with no sentinel line above predates this system —
>   its times are local, zone unrecorded (legacy; never rewritten).
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 18:45 · finding · —

Ruled 2′ (unattended, coordinator brief) and built on branch worktree-d-cron.

Reproduced first against the unchanged handler, with real prompts lifted from this
repository's session transcripts (`untracked/scratch/probe_marker_cleared_by_other_cron.py`):

```text
issue-sdlc (real, delivered)             matches=True decision=allow marker_survived=False
watchdog (real, agent-composed)          matches=True decision=allow marker_survived=False
issue-sdlc with [tick:job:issue-sdlc]    matches=True decision=allow marker_survived=False
watchdog with [tick:watchdog]            matches=True decision=allow marker_survived=False
```

After the fix, the last two rows read `deny/True` and `allow/True`. The first two
are unchanged: they are the residual 2′ accepts. Text the daemon never wrote has
no sentinel, and it still reads as the human.

Transcripts showed seven different agent-composed watchdog prompts across two
sessions. That is direct evidence for supplying the watchdog prompt verbatim.

Sweep: two other sites classify "is this prompt automated": `standing_authorisations`
and `skill_scan.constants`. Both knew only the failsafe literal, and both now
recognise the sentinel. The Defence is a `reaches classify_tick` row in
`scripts/qa/declared-invariant-pairs.yaml`. It was proved red against the pre-fix
sources and green after the fix.
