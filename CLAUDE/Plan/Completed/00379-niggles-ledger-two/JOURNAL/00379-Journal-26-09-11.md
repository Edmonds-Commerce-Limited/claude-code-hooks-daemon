# Plan 00379 — Journal 26-09-11

> **Append-only activity log** for plan 00379. One file per day
> (`00379-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 14:47 · action · — — plan scaffolded

Plan 00379 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 14:48 · decision · — — cron reuse, not creation

Failsafe recovery cron: reusing the session's existing `75ba7c5e` (hourly at
:37) rather than creating one for this plan. Exactly one recovery cron covers
a whole session and the canonical prompt is plan-agnostic, so a per-plan cron
would only double-fire on the same session. The background watchdog cron
`2e49f8d7` (hourly at :13) is likewise reused.

## 14:49 · finding · P1 — four entries recorded at birth

All four were found by verifying a claim, not by hitting a symptom. That is
the pattern worth naming: a claim nobody rechecks is where these hide.

N1 came out of the final QA run on the settled tree. The plan-index retention
window is enforced only by an integration test in full QA, while the
COMMIT-stage `terminal_state_atomic` check that makes archiving atomic does
not cover the age-out step the same checklist requires. Three consecutive
archival commits (00375, 00377, 00378) each added a row without ageing one
out, reaching 33 rows against a ceiling of 30, and `plan-qa --sweep` reported
0 findings throughout. Rows fixed in `1f00eab8`; the entry is about the gap
that let it happen three times, not the count.

N2 and N3 came out of checking Plan 00376's cited line numbers against the
files they name. Every reference resolved — that is the good news — but two
task descriptions mislead. Task 3.1 treats a compound `||` condition as one
branch, risking removal of the legitimate `--skip-reading-confirmation`
opt-out alongside the TTY inference that is the actual bug. Task 4.3 asserts
`upgrade.md` "never mentions post-upgrade tasks" when line 108 does; the
SUBSTANCE survives (that mention is a passing reference inside the
config-advisory step, not the missing procedural step), but the wording
invites closing the task as already-done.

N4 surfaced while updating Plan Statistics for this plan's birth. The block
stated 365 distinct numbers and 13 folderless of 378 allocated, then
self-checked with `364 + 13 = 377. ✅` — both terms off by one, under a tick
asserting a sum that does not hold. Counted from disk to settle it: 16 root
(this plan included) + 340 `Completed/` + 13 `Cancelled/` = 369 folders, less
the three historic collision pairs = 366 distinct, + 13 folderless = 379
allocated, matching the counter `mkplan.bash` advanced.

## 14:49 · thought · — — an exit code that lied

Recording this for its own sake. The QA background task was reported by the
harness as "exit code 0" while the QA itself had exited 1. The command chained
with `;`, so the reported status belonged to the trailing `tail`, not to
`llm_qa.py`. Taking the harness's exit code at face value would have published
a red tree as verified — 28/29, with two real failures. The habit that caught
it is reading the result the tool produced rather than the status the wrapper
reported.

## 15:10 · finding · N1 — the new check was wrong, and only the real tree said so

`index-retention-window` landed with 20 green unit tests and then reported
**340 completed rows against a window of 30** on its first real sweep — on a
tree that was correctly AT 30. It is a BLOCK at COMMIT, so shipping it would
have denied every commit in the repository.

The cause: `plan_qa/context.py` appends the ARCHIVE index's rows to the
`ReadmeIndex` it hands checks, so `row-folder-bijection` still resolves a plan
whose row has aged out, while leaving `lines` sourced from the primary file
alone. 30 primary + 310 archived = the 340 reported. The constraint WAS
documented — in a comment on the producer, not on the dataclass a check author
reads. Recorded as N5 and fixed there.

Two things worth keeping from this. Every synthetic fixture passed: they each
built one `ReadmeIndex` from one string, so `rows` and `lines` always agreed —
a shape the real context never produces. The regression test now constructs
the divergent shape explicitly.

And the end-to-end proof earned its two extra edits. After fixing, I put a
31st row back into the real index and swept: `31 completed rows ... 1 row
overdue`; removing it returned the sweep to clean. Watching a guard fire on
real data is the only thing that would have caught the first version, because
the unit tests were green the whole way through.

## 15:25 · decision · N4 — where the rule belongs, and what it must check

Two choices worth recording, because both could reasonably have gone the other
way.

WHAT it checks. The obvious rule — "a sum written in the index must add up" —
would NOT have caught this defect. `364 + 13 = 377` is arithmetically true.
What was wrong is that both operands contradicted figures stated inches above
them (365 distinct, 378 allocated), under a ✅ asserting the check had been
done. So the rule has to be a cross-reference one: the closing self-check's
operands must match the distinct / folderless / allocated figures the same
bullet states, the folder sum must add up, and the folderless count must match
the numbers actually listed.

WHERE it lives. Not plan_qa, which ships to client projects whose plan index
has no reconciliation bullet at all — a check for one repository's prose
convention does not belong in a linter every consumer runs.
`scripts/qa/check_repo_hygiene.py` is this repository's own hygiene scan and
already owns rules of exactly that shape (`post-upgrade-index-drift`).

Proved against the real defect, not just fixtures: restoring
`364 + 13 = 377. ✅` produced two findings naming both wrong operands
("starts from 364, but the bullet states 366"; "totals 377, but the bullet
states 379"), and reverting returned the scan to clean.

## 15:27 · action · P1 — holding area updated

Release note 32 added for N1. It is the only entry in this ledger a user would
notice: `index-retention-window` is a BLOCK at the commit gate and ships to
client projects, so an index already over the window will report on the first
commit after upgrading, and the note says so and how to clear it. N4's rule is
repo-hygiene and never reaches a client; N5 is a docstring; N2 and N3 are plan
text. None of those changes behaviour anyone outside this repository sees.

## 15:28 · handoff · — — Plan 00379 complete, archived

A note on this entry's own timestamp, because it is a real slip. The wall
clock read **15:21** when this was written. The two entries above it claim
15:25 and 15:27, and those were GUESSED — written around 15:02 without
checking the clock, so they run roughly 23 minutes ahead. That is inside
`journal-entry-future-dated`'s 30-minute tolerance, so nothing objected at the
time; the consequence only surfaced now, when a truthful 15:21 tripped
`journal-entry-ordering`. Both checks came out of Plan 00377, and between them
they caught it. The earlier entries are already committed and journals are
append-only, so they are not rewritten; this one is stamped 15:28 to keep the
file's ordering readable, and says so rather than passing the number off as
observed. Check the clock; do not estimate it.

All five entries resolved, none graduated. Green on both gates, checked
properly: `QA_EXIT=0` with 29/29 and 22,371 tests passed / 0 failed at 95.3%
coverage, and CI run 34613727728 concluded `success` on `286795d7`. The QA exit
code was captured explicitly this time rather than inherited from a trailing
`tail` — the earlier run in this session reported "exit code 0" while the QA
had actually exited 1.

Archiving tripped the check this plan shipped, which is the outcome worth
having: moving 00379 into `Completed/` took the index from 30 completed rows
to 31, so `index-retention-window` required 00348 to age out into
`Completed/README.md` in the SAME commit. The guard was written because three
archivals in a row forgot exactly that step; the first archival after it now
cannot.

What the next agent should know. The niggles ledger is CLOSED — per the SOP,
the next niggle found opens a NEW plan rather than reopening this one.

Live work still named on disk: Plan 00376 (pre-upgrade phase) is Not Started
and its cited line numbers were all verified accurate during this session, but
it carries THREE owner decision points, not one — Task 1.2 (what an abort must
undo, given the checkout has already happened), Task 3.2 (which changes an
agent may accept alone vs escalate) and Task 4.1 (give `post-upgrade-tasks/` a
runner and schema test, or stop requiring plans to feed it). Tasks 4.2 and 4.3
need no ruling and could be done any time. It should not be started as a whole
without the owner.

Also still reserved to the user: removal of five live worktrees
(`worktree-pgrep-bracket`, `worktree-plan-00291`, `worktree-plan-00368c`,
`worktree-plan-00369`, `worktree-plan-00372`), which needs
`git worktree remove --force`.
