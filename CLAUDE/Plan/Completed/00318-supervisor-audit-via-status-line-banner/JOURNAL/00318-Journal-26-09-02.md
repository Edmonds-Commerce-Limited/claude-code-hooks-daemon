# Plan 00318 — Journal 26-09-02

> **Append-only activity log** for plan 00318. One file per day
> (`00318-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 10:00 · action · — — plan scaffolded

Plan 00318 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## Live pipeline verified (Phase 3)

Daemon restarted (pid 141595) and worker hot-reloaded (pid 104085, started
10:07:35, after the 10:07:30 source edit) — new code live on both sides.

Path agreement checked first, because a writer/reader split would have made
the banner silently invisible: the supervisor's `_daemon_untracked_dir()` and
the daemon's `ProjectContext.daemon_untracked_dir()` both resolve to
`/workspace/untracked`, and `decide_once` writes via `sidecar_dir.parent`,
which is the same directory the host's own `StatusMessagePoster` uses.

A clearly-labelled probe banner rendered through the LIVE handler as:

```
| <green> 🎩 🧾 Plan 00318 banner probe (not a real audit) (30s) <reset>
```

and the countdown decremented across successive renders (19s, 15s, 11s)
before the segment disappeared on expiry.

Outstanding for full closure: one REAL supervisor action (a model switch
re-applying its coupled effort) producing the banner with no chat injection.
That arrives naturally on the owner's next model change; the injection path
for the audit family no longer exists in code, so the only way an audit can
surface now is the banner.
