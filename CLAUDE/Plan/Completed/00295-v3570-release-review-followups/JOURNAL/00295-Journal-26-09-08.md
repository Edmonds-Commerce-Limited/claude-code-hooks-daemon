# Plan 00295 — Journal 26-09-08

> **Append-only activity log** for plan 00295. One file per day
> (`00295-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.

## 14:44 · action · P3 — Phase 3 merged and dispositioned

Phase 3 landed on main at merge `420735b3` from a worktree branch whose
commits map one-to-one onto the tasks: 3.1 `91882c36`, 3.2 `ae3adc35`, 3.3
`b1106921`, 3.4 `02890b1f`, 3.5 `13677f21`, 3.6 `6baedd48`, 3.7 `adec60ee`
(documented, not evicted), and Task 3.9's four sub-items `23ccaa1c`,
`09ac3a8d`, `1017e928`, `5c10d368`. The worktree agent left no journal entry
of its own, so the mapping above was reconstructed from `git log` of the
branch.

Task 3.9's three remaining sub-items were found already closed on
inspection rather than by a commit on this branch: the stale
`expected_message_patterns` by Plan 00319 Task 4.6's acceptance contract test
(every BLOCKING pattern is driven through the real handler); test 87's
`[[ ... ]]` wrapper by Plan 00305 Task 2.6's quoted-argument narrowing (the
producer named is `pytest`, and the fixture is now the deliberate case for
it); the Python TDD deny by the strategies all sharing the
`BLOCKED [R-TDD-TEST-FIRST]` short form.

Release consequence recorded as
`UNRELEASED/release-notes/10-v3570-review-followups-closed.md`. The "Full QA
green" criterion waits on the full run of the merged tree.
