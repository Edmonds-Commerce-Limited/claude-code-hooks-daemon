# Plan 00177 — Journal 26-07-20

> **Append-only activity log** for plan 00177. One file per day
> (`00177-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 08:38 · action · — — plan scaffolded

Plan 00177 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 08:40 · finding · P0 — report verified against upstream tree

All claims in the downstream field report confirmed against v3.44.0 source:
`get_transcript_reader` (stop_hook_helpers.py:55) makes a fresh whole-file
`TranscriptReader` per call; `auto_continue_stop` triggers matches()+handle()+
Branch-3 reloads plus a 6-iteration freshness poll (`_await_fresh_assistant_message`),
each a whole-file parse; `init.sh` `emit_error_json` collapses every non-
`invalid_hook_input` Stop error (incl. `socket_timeout`) to "daemon not running".
Caveat: the report's cited in-tree reference `backlog_stop_blocker.py` is a
DOWNSTREAM project handler — absent upstream — so the tail read is written fresh.

## 08:55 · decision · P1 — fail-open on read-side Stop timeout

Chose to fail-OPEN (allow the stop) for a `socket_timeout` on Stop/SubagentStop
(daemon reached+alive, handler just slow) rather than keep the fail-closed block
that wedged the session into a 30s re-fire loop. Genuine-down
(socket_not_found/connection_refused) stays fail-closed. Added
`CLAUDE_HOOKS_SOCKET_TIMEOUT` override (default 30) — operator lever + makes the
timeout path fast-testable.

## 09:00 · action · P2+P3 — bounded tail read landed

`TranscriptReader.load_tail()` added (shared `_ingest_record` extracted from
`_parse`); wired into `get_transcript_reader` and the freshness poll. 396
stop+transcript unit tests pass; daemon restarted RUNNING.

## 09:05 · finding · T4.2 — live probe: 30s → 0.72s on 150 MB

Live Stop probe through `.claude/hooks/stop` on a 150 MB synthetic transcript.
First reading was 30s + fail-open `{}` + honest "daemon is alive" stderr — the
30s was CPU contention from a concurrent full-QA run (direct `load_tail` timed at
0.017s). Re-probe with CPU free: **0.72s**, correct ALLOW (STOPPING BECAUSE
recognised). End-to-end fix confirmed.

## 09:10 · action · T4.3 — housekeeping

Field report moved `untracked/hooks-daemon-handler-failure.md` →
`UPSTREAM-FIELD-REPORT.md` in this plan folder (tracked). Follow-ups captured in
PLAN.md non-goals: per-dispatch memoisation (report §3); ccy supervisor lifetime
injection-cap reset (report Appendix A).
