# Plan 00167 — Journal 26-07-16

> **Append-only activity log** for plan 00167. One file per day
> (`00167-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 07:50 · action · — — plan scaffolded

Plan 00167 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 08:05 · finding · — — status-line investigation (pre-plan research)

Root-caused the four status-line questions from two research agents +
direct code reading:

- **Assembly**: `core/hook_result.py:164-185` joins enabled `status_line`
  fragments with `" | "` and returns `{"text": text}`. ZERO width /
  truncation / wrapping logic — narrow terminals lose right-hand segments
  off the edge.
- **Wrap is feasible**: Claude Code officially renders multi-line status
  (one terminal row per `\n`), and exposes terminal size via `COLUMNS`/
  `LINES` **environment variables** (v2.1.153+), NOT in the JSON payload.
- **Transport gap**: the daemon is a separate process and does not inherit
  `COLUMNS`. The jq-free transport (`init.sh` `send_request_stdin`, Status
  branch ~L960) runs in the wrapper's env and already rewrites the Status
  payload — the correct single injection point to forward width/height.
- **Cron**: NO scheduled-cron data reaches the status line — not in Claude
  Code's Status JSON, and nothing daemon-side is reachable. Non-Goal.
- **Unused Status-input fields**: top-level `version`, `cwd`,
  `output_style`, `exceeds_200k_tokens`, `rate_limits.*`, `prompt_id`, plus
  nested `agent.*`/`pr.*`/`worktree.*`/`vim.*`. `rate_limits` + `version`
  are the highest-value unused fields.

## 08:06 · finding · P3 — upgrade notifier is entangled with daemon_stats

The `📦 vX → vY` upgrade arrow is rendered INSIDE `daemon_stats.py:112-134`
(reads `version_check_cache.json`, produced by the separate
`version_check` SessionStart handler). `daemon_stats` is enabled in this
repo (`.claude/hooks-daemon.yaml:478`), so dropping the health line today
also drops the upgrade prompt. Plan: extract an on-by-default
`upgrade_notifier` status handler, then disable `daemon_stats`.

## 08:07 · decision · — — wrap via multi-line, forward width via transport

Adopted Decisions 1-3 in PLAN.md: (1) wrap at `" | "` boundaries into
multiple rows rather than truncate; (2) inject `COLUMNS`/`LINES` into the
Status payload in `init.sh` rather than read them daemon-side; (3) extract
the upgrade notifier into its own SRP handler. Plan drafted, README index
row added, stats bumped to 167/Active 24.

## 08:20 · finding · — — cron data IS on disk (corrects earlier "no")

User pushed back on the flat "no cron data" answer — dogfooded it. Result:
the Status **input JSON** carries no cron data (confirmed), BUT the harness
persists self-wake / scheduled-task state on disk:

- `~/.claude/jobs/<daemonShort>/state.json` records
  `"inFlight": {"tasks": N, "kinds": ["session_cron"]}` and
  `"selfWake": true` for backgrounded/self-waking jobs.
- `CronList` is the live per-session API (returned "No scheduled jobs" for
  this session right now — nothing active to render).
- `~/.claude/jobs/<id>/pins.json` and per-session `~/.claude/tasks/` dirs
  also exist.

Implication: a status handler COULD surface a "⏰ N crons" indicator by
reading `~/.claude/jobs/*/state.json` (or a CronList-equivalent store) from
disk — it is feasible, not impossible. Reframed in PLAN.md from a hard
Non-Goal to a documented future enhancement.

## 08:22 · decision · P3/P4 — daemon_stats rollout: off-by-default for clients, ON here

User correction: dropping `daemon_stats` is NOT a per-repo tweak. Direction:

- `daemon_stats` is developer diagnostics — keep it **off by default** for
  client repos (it already returns `get_default_enabled() == False`, so the
  default is correct; the work is the client-facing MESSAGING).
- The NEW `upgrade_notifier` becomes the on-by-default carrier of the `📦`
  upgrade prompt, so clients lose nothing by having `daemon_stats` off.
- **This repo is the ONE exception** — it is the daemon's own dev repo, so
  `daemon_stats` stays ENABLED here (reverses the earlier "disable it in
  this repo" task).
- Upgrade messaging must **strongly recommend** client projects that have
  explicitly enabled `daemon_stats` turn it off now that the upgrade prompt
  is a separate handler. Needs a `config-changes/v{X.Y.Z}.yaml` entry
  (recommended) + post-upgrade guidance.

## 10:41 · action · P2 — status-line wrapping shipped + live-verified

Execution split: dispatched a background `python-developer` agent (`p167agent`)
for the independent Phase 1 (init.sh COLUMNS/LINES transport + schema) and
Phase 3 (extract `upgrade_notifier`); I drove Phase 2 (the wrap on the core
assembly path) myself since it threads width through `to_json` and 3 call sites.

Phase 2 (TDD, `core/hook_result.py`):

- `_display_width` — ANSI-stripped display width; East-Asian W/F = 2 cols;
  combining marks + format chars (ZWJ U+200D / VS16) = 0; dependency-free, ±1
  col (fine because we only ever break at whole-segment boundaries).
- `_wrap_status_parts` — greedy first-fit packing of the normalised segments
  into rows ≤ width, joined by `\n` (Claude Code renders one row per newline);
  an oversize segment gets its own row.
- `to_json` gains `terminal_columns`; wraps in the Status branch when it is a
  positive int, else the single-line join (backwards-compatible). Threaded from
  `controller.py` (the DaemonController path — `HookInput` is `extra="allow"` so
  the forwarded `terminal_columns` survives pydantic), `server.py` (legacy), and
  `front_controller.py`.

Verify: 91 `test_hook_result` tests pass; 1080 core/daemon tests pass; mypy 0
errors on all 4 changed files; black + ruff clean. Daemon restarted RUNNING with
the combined tree (my Phase 2 + the agent's P1/P3 — imports clean). **Live probe**
of `.claude/hooks/status-line`: `COLUMNS=36` → wrapped to 5 rows at ` | `
boundaries (nothing lost); `COLUMNS=200` → one line. That single probe confirms
the WHOLE path end-to-end, so the agent's Phase 1 transport+schema is proven
working too. Committing Phase 2; then integrate the agent's P1/P3 and do P4
(rollout messaging) + P5 (docs).

## 11:10 · action · P1/P3/P4 — agent P1+P3 reviewed, integrated; rollout messaging done

Reviewed the `p167agent` background agent's uncommitted P1 (init.sh COLUMNS/LINES
transport + schema) and P3 (upgrade_notifier extraction) diffs directly:

- **P1 transport** (`init.sh`): forwards `COLUMNS`/`LINES` from the wrapper env
  into the Status payload as `terminal_columns`/`terminal_lines`, skipping
  non-digit values so older clients degrade cleanly. Schema
  (`input_schemas.py`) declares both as `["integer","null"]`. Clean.
- **P3 extraction** (`upgrade_notifier.py`): the `📦 vX → vY` block moved
  VERBATIM out of `daemon_stats.py` into a new on-by-default SRP handler
  (`HandlerID.UPGRADE_NOTIFIER`, `Priority = 32`), same stale-cache defence
  vs `__version__`, same fail-open contract. `daemon_stats.py` lost the block
  plus its now-unused `json`/`ProjectContext` imports; the 4 arrow tests moved
  to `test_upgrade_notifier.py` (100% cov). Faithful, minimal.

Full `llm_qa.py all` on the integrated tree (my supervisor fix + agent P1/P3 +
P4 doc): **13/13 PASSED**, 10238 tests, 95.3% cov. Daemon restarted RUNNING;
`upgrade_notifier` loads at priority 32 (confirmed in regenerated
`.claude/HOOKS-DAEMON.md`).

P4 (rollout messaging):

- `config-changes/v3.43.0.yaml`: `added` upgrade_notifier (`recommended: true`)
  + `changed` daemon_stats (`recommended: true`, `recommended_value: false`) so
  `check-config-migrations` actively nudges any client holding `daemon_stats:
  true` to turn it off now the arrow is a separate handler.
- Decision: NO separate post-upgrade-task file and NO `truth-changes/` entry —
  the `recommended` config-changes entries already ARE the promoted upgrade
  guidance, and no client-facing doc ever asserted the `📦` arrow lived in
  `daemon_stats` (the status_line table describes it as uptime/memory/log/errors
  only), so no documented truth was invalidated.
- `daemon_stats` stays `enabled: true` here (dev-repo exception, annotated in
  the yaml); `upgrade_notifier` `enabled: true`.

Remaining: P5 docs (status_line/CLAUDE.md + Architecture/StatusLine.md: new
handler, wrap behaviour, forwarded width fields, cron on-disk-feasibility
answer, unused-field inventory) then final dogfood.

## 11:11 · finding · — — dogfooding: supervisor 🎩 icon fixed en route

While verifying the live status line, the user flagged the 🎩 supervisor icon had
vanished. Root cause: `supervisor_indicator._resolve_state` rendered
NOT_CONFIGURED whenever the supervisor status file was ABSENT, but the file was
observed going missing while `claude-supervise.py --arm` (host pid) was still
alive. Fixed by grounding detection in the live PROCESS: trust the status-file
pid when live, else scan `/proc` for a live `claude-supervise` (host preferred
over `--worker`), else orange/none by status-file presence. 100% cov (33 tests),
live probe now shows green-bg 🎩. Committed separately (dogfooding bug, not part
of this plan's scope). Also added the three fail-open readers to
`error_hiding_exclusions.json` (they were previously un-excluded and failing the
gate).

## 11:30 · action · P5 — docs written, plan complete

Phase 5 docs:

- `CLAUDE/Architecture/StatusLine.md`: new "Terminal-width-aware wrapping"
  subsection (transport → schema → `to_json` wrap → `_display_width`); corrected
  the Output Format section (normalised ` | ` re-join, not "join with space");
  `UpgradeNotifierHandler` detail block + priority-32 table row; daemon_stats
  "arrow removed / OFF by default" note; new "Status Input Fields" section
  (used-vs-unused inventory + the "crons are feasible from disk, not in the
  payload" answer). Bumped "Last Updated" to July 2026.
- `handlers/status_line/CLAUDE.md`: upgrade_notifier row + wrap note.

Task 5.2 verification: full `llm_qa.py all` green; daemon RUNNING; live dogfood
at `COLUMNS=40` → 5 wrapped rows (nothing lost, green 🎩, no `📦` in the health
line), `COLUMNS=220` → 1 row.

All 5 phases done, all success criteria met. Flipping status to Complete and
moving the folder to `Completed/` with the README row/stats update in one commit
(terminal-state-atomic). Recovery cron KEPT — session continues with Plan 00168
remaining work (compaction dogfood). handoff → pick up Plan 00168 Phase 5.
