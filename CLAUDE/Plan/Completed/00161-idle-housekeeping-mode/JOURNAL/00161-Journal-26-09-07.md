# Plan 00161 — Journal 26-09-07

> **Append-only activity log** for plan 00161. One file per day
> (`00161-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 19:10 · finding · P2 — the whole plan shipped on 2026-07-14 and nobody ticked it

Opened this plan expecting Phase 2 to be unbuilt — the index row said
"awaiting Phase 2 build" and the header said `In Progress`. Every deliverable
was already on disk, shipped by two commits on the day the plan was written:

```
127c3a3c 2026-07-14 Plan 00161: idle_housekeeping_advisory beta handler + reports guide
4f99de53 2026-07-14 Plan 00161: idle housekeeping - project custom guidance doc option
```

Audited each Phase 2 task against the tree rather than against the commit
messages:

| Task | Claim | Verified |
| --- | --- | --- |
| 2.1 | handler exists, under test | `handlers/user_prompt_submit/idle_housekeeping_advisor.py`; 23 tests pass |
| 2.2 | guidance dispatches sub-agents | guidance text names the sub-agent dispatch and the report file |
| 2.3 | dogfood-enabled here | `.claude/hooks-daemon.yaml:800` — `enabled: true`, `priority: 56` |
| 2.4 | reports guide | `docs/guides/CREATING_REPORTS.md` |
| 2.5 | ships OFF, configurable, documented | `get_default_enabled() → False`; all three options; `get_claude_md()`; `get_acceptance_tests()`; `CLAUDE/UPGRADES/config-changes/v3.40.0.yaml` at `recommended: false` |

Task 2.3's plan text asked for an `extra_allowed_markdown_paths` entry for
`untracked/`. There is none, and that is correct rather than missed:
`untracked/` is already covered by the handler's built-in defaults, so
declaring it would have been a redundant override that future readers would
have to reconcile against the default list. Recorded on the task so the
absence is not read later as an unfinished step.

## 19:14 · finding · — — 55 days In Progress, and this is the THIRD one today

Third plan this session found shipped-but-unticked (00110 and 00161 by audit,
00341's original trigger before them). 00341 shipped the fix for the
HEADER-level case: a ticked box now falsifies a `Not Started` header, and
shipping `src/` code for a plan named in the commit subject does too.

Neither of those catches this one. `127c3a3c` names plan 00161 in its subject
and ships `src/` — exactly 00341's second signal — but the header already said
`In Progress`, which is not a falsified state. The plan was truthful the whole
time about being started and silent about being finished, and nothing checks
the second thing. The gap is real but it is not this plan's, so it is recorded
here as an observation rather than filed on a guess: a TASK-level rule
("every task ticked + terminal work shipped, yet the header is non-terminal")
would need measuring against the same replay harness 00341 and 00343 used
before anyone can say whether it false-fires on long-running plans that
legitimately hold a completed phase open.

## 19:18 · decision · — — closing as Complete, not carrying the Tier-M increment

Decision 4 fixed the MVP as **report-only**, with the auto-fix mode
(BRAINSTORM §E.2 Tier-M) named as a deliberate later increment rather than
descoped work. That increment is not a loose end on this plan: the success
criteria ask for a report-first MVP dogfooded here, which is what shipped.
Holding the plan open for it would put an unbuildable phase on the goal
ledger — the same failure Plan 00337 split rather than carried, for the same
reason.

Decision 6's `hooks-daemon-` prefix convention is likewise governance for a
deployment mechanism that has not landed; the MVP dispatches generic
sub-agents and needs nothing from it.

## 19:22 · action · — — closed out

Three success criteria ticked against verified evidence, header flipped to
`Complete`, folder archived to `Completed/`. 00161 is below the main index's
30-highest-numbered retention window, so its row goes straight into
`Completed/README.md` — no rotation, and the main index's completed count is
unchanged.

Cron: reused the session's existing failsafe recovery cron `5398b3f9` — one
per session, never one per plan. Nothing created.
