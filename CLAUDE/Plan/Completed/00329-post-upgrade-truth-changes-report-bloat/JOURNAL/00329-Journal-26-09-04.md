# Plan 00329 — Journal 26-09-04

> **Append-only activity log** for plan 00329. One file per day
> (`00329-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 09:35 · action · — — plan scaffolded

Plan 00329 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 09:38 · finding · — — the measurements this plan is built on

Reported by the owner as "post upgrade the project agent is given a ~100kb
file of context ... it doesn't actually read it, so it doesn't follow the
instructions". Confirmed, and the source is `check-truth-changes` (step 4 of
the upgrade skill):

```
v3.0.0  -> v3.61.0 : 89,580 bytes, 260 lines, 74 entries
v3.50.0 -> v3.61.0 : 72,352 bytes, 188 lines, 50 entries
```

Fifty entries for an eleven-minor-version hop. `--format {text,json}` is the
only option; JSON is larger. `install/truth_changes.py` was checked for any
mitigation and has none — no dedup, collapse, cap or truncation.

## 09:40 · finding · — — the half that matters more than size

The report replays superseded truths rather than the current delta. Verified
directly in the emitted output, the plan-creation truth appears three times:

- v3.23.0 — WAS "no bundled scaffolding script" → NOW "run `mkplan.bash`"
- v3.25.0 — WAS "installer-deployed only" → NOW "deployed from config SSoT"
- v3.26.0 — WAS "deployed from config SSoT" → NOW "opt-in, defaults false"

Only v3.26.0 is current. The skill says to update the project's doc to assert
each `now`, so following it literally writes a claim and then contradicts it
twice. `## Notes & Updates` → `## Delivery & Milestones` repeats the shape
across v3.40.0 and v3.49.1.

This is why Phase 1 leads on collapsing rather than bounding: bounding a
report that instructs wrong work just truncates the wrong work.

Noted in passing — the daemon ships `subagent_report_size_blocker` ("write
large reports to a file") and its own upgrade flow does the thing that
handler exists to prevent.

## 09:41 · decision · — — filed as its own plan, not folded into the release

A dedupe scout checked 36 live plans and found no owner. Closest adjacents
are 00293 (tool-SCHEMA token bloat, not CLI output), 00319 (unbounded growth
in supervisor internals) and 00264 (outbound GitHub comment caps) — none
cover this.

Kept out of the pending v3.62.0 release: the defect is pre-existing and that
release adds one version's worth of entries to it, whereas holding the
release keeps a supervisor context-loss fix unshipped. Owner's call on
sequencing.

## 09:42 · action · — — failsafe recovery cron

`CronList` showed exactly one already live for this session — id `5398b3f9`,
hourly at `:23`. Reused per the one-cron-per-SESSION rule; none created.

## 10:05 · decision · P0,P3 — owner steer: measure wider, and chunk carefully

Owner: *"im not sure if its just truth changes bloat, we'd have to try a
canary upgrade to fully diagnose what all the bloat is ... claude code simply
ignores large blobs — context defence ... I guess we'd need to find a way to
chunk it somehow and advise that the project agent uses sub agents to parse
each chunk?"*

Both accepted, with one ordering constraint added.

**Widened (Phase 0).** This plan had measured ONE source. `check-config-
migrations` adds 19,400 bytes on the same range, taking a single hop past 91KB
before the upgrade script's stdout, post-upgrade-tasks, `optimise` and the
regenerated `CLAUDE.md`. Phase 0 now measures the whole envelope through a
canary rather than trusting the first thing found. Task 0.2 also refuses to
assume the mechanism — truncation DROPS instructions whereas skimming
deprioritises them, and the remedy differs.

**Chunking accepted, but ordered AFTER collapsing (Phase 3).** Chunking an
uncollapsed report is worse than not chunking it. Superseded chains mean two
chunks can carry contradictory instructions about the same document; one agent
reading sequentially lands on the current truth because the later entry
overwrites the earlier, but N agents in parallel race for the same file with
an arbitrary winner. Parallelism converts a self-correcting sequence into a
non-deterministic one.

Chunking by TOPIC rather than size resolves it: topics are disjoint (so
parallel agents cannot collide on a file), a topic is a coherent unit of work,
and the topic key is the same mechanism Phase 1 needs for collapsing. One
addition buys collapsing, safe chunking and a natural subagent unit.
