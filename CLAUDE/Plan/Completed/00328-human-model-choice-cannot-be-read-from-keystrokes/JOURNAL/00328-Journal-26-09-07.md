# Plan 00328 — Journal 26-09-07

> **Append-only activity log** for plan 00328. One file per day
> (`00328-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 19:40 · finding · T2.1 — settings.json fails on an unmeasured premise

Evaluated `~/.claude/settings.json` as Task 2.1 asked. It holds the family and
the daemon already has an mtime-cached reader for it
(`handlers/status_line/settings_reader.py`), so the plumbing would be nearly
free — and it is still the wrong signal. Three defects, written up in
`DETECTION-CHANNELS.md`; the third is disqualifying.

The reproduction's own evidence turns out not to support what it was cited for.
It read settings.json mtime 07:58:56 as the human's pick landing. But `/effort
low` was injected at 07:58:56 and effort writes the same file (`effortLevel`
plus the per-model `modelSettings` block), so that timestamp attributes to
either cause. Only a change in the `model` VALUE is usable, not the mtime.

And the value cannot discriminate unless the automatic downgrade does not write
`model`. Nothing measured says it does not. Both candidate causes predict
`opus`, which is what the file held. The product behaviour argues for it — the
key is "your default for new sessions", and silently rewriting a persisted
default off one downgraded request would be a real bug — but that is an
argument, and this repo has been burned twice this fortnight substituting one
for a measurement (00341's replay found a 25% false-positive shape, 00343's
found 7 of 18).

If the premise is false, settings.json reports "the human chose opus" for every
automatic downgrade, the auto-restore quietly becomes dead code, and it looks
like success from the outside.

## 19:46 · finding · T2.1 — Claude Code already writes the automatic downgrade down

Looking for a way to measure the premise, found the thing the premise was a
proxy for. Claude Code emits the safety downgrade into the session transcript
as a structured record, and this daemon **already parses that exact shape** in
`handlers/session_start/model_fallback_detector.py`. It was simply never wired
to the supervisor.

Scanned all 25 transcripts under `~/.claude/projects/-workspace/`. 25 genuine
records, three sessions, 2026-08-26 to 2026-09-02, every one the fable security
downgrade this plan exists for:

```json
{"originalModel": "claude-fable-5", "fallbackModel": "claude-opus-4-8",
 "apiRefusalCategory": "cyber", "scope": "session",
 "ts": "2026-08-27T09:34:10.341Z"}
```

Uniform across all 25 — same pair, same category, same scope. Two shapes per
event: a `fallback` content block in the assistant message, then a standalone
`subtype` record 7–90s later (13 blocks / 12 subtypes, so they pair but not
exactly; a consumer takes either and dedupes).

## 19:52 · decision · T2.2/T2.3 — arm positively, and the question changes shape

The plan has been asking the harder question. Everything built so far tries to
recognise the HUMAN so the restore can be suppressed — a negative inference
("the family changed and I did not cause it, so a human did") that needs the
set of non-human causes to be closed and observable. That is why it keeps
failing: 00316 inferred it from keystrokes the picker never emits, and both
`note_model_reading` and `downgrade_state.py` infer it from a high-water mark
that a human picking opus reproduces exactly.

The owner's scope ruling makes the positive form available: the restore
counteracts the automated fable security downgrade and nothing else. Arm on the
transcript record and a human change needs no recognition at all — it emits no
such record.

That reorders Phase 2. Old 2.2 (delete the keystroke machinery *if* 2.1 is
sound) becomes new 2.3 and gets bigger: the typed parser, stem match, picker
wildcard, its session key and the restore-steal guard all answer "was that the
human?", and that question stops being asked, so they are redundant rather than
replaced. Old 2.3 (fall back to Phase 1 only) is dropped — it was the branch
for "no sound channel exists", and one does.

Two constraints on the build, both recorded on the task so they are not
rediscovered:

- The status-line event does NOT carry `transcript_path` — nothing under
  `handlers/status_line/` references it — so the sidecar the supervisor reads
  cannot be the producer as it stands. `PostToolUse`/`Stop` do carry it.
- The largest transcript here is ~100 MB. `model_fallback_detector` reads whole
  files because it runs once at SessionStart; a per-event consumer must scan
  the tail from a recorded offset.

The supervisor must not read transcripts itself — Plan 00317's audit keeps the
PTY host thin, and the daemon already sees `transcript_path` on every hook
event and already owns the parser. Producer in the daemon, consumer in the
supervisor, over the file channel they already share.

## 19:55 · handoff · P2 — channel chosen, build open

Task 2.1 closed. Next is Task 2.2: the daemon-side producer (incremental
transcript tail scan → a per-session fact file the supervisor reads), TDD, then
Task 2.3's deletion once the positive arming is proven. Phase 1 remains the
floor throughout — one failed restore, no escalation — so a half-built Phase 2
cannot regress the field behaviour.

Cron: reused the session's existing failsafe recovery cron `5398b3f9`. Nothing
created.

## 20:05 · action · T2.2 — attribution built, three pieces

Shipped in one slice, TDD throughout:

- `utils/model_fallback_records.py` — recognition of both transcript shapes
  plus a bounded tail scan (20 tests). The SessionStart
  `model_fallback_detector` was refactored onto it, so the codebase has ONE
  parser rather than two that drift; its own 40 tests still pass, and the
  three that reached into the private `_extract_fallback_block` moved with the
  code they were testing.
- `handlers/post_tool_use/model_downgrade_recorder.py` — silent, enabled by
  default, publishes `<session>.model-downgrade` into the `context-sidecar`
  directory both sides already share (18 tests).
- Supervisor: `load_model_downgrade_signal` + `note_machine_downgrade`, now
  the only thing that opens a downgrade episode (12 tests).

## 20:08 · decision · T2.2 — the signal is REQUIRED, not merely preferred

Considered making the record a preference — use it when present, fall back to
the high-water inference when absent. Rejected. The fallback is exactly the
defect: a session whose recorder is off would keep overriding the human, and
"preferred" hides that behind a case nobody tests. The plan's Goal says a human
model change is never overridden "whichever UI route produced it", and only the
required form delivers that.

The cost is real and belongs on the record: turn `model_downgrade_recorder`
off and the auto-restore stops entirely. That is the safe direction — Phase 1
already caps a futile restore at one attempt — but it must not be silent, so a
ranked drop with no record now emits `downgrade fable -> opus is unattributed
(no model_downgrade_recorder signal) — no restore` to decision.log, once per
drop rather than once per tick. Without that line, "the recorder is disabled"
and "this session was never downgraded" look identical from the log.

Only the EPISODE is withheld. The per-model effort floor still applies to an
unattributed change — the human picked that family, so its configured minimum
is what they asked for.

## 20:12 · finding · T2.2 — 27 existing tests encoded the defect

Every supervisor test that meant "the safety classifier substituted the model"
drove a fable → opus sidecar transition and nothing else, because that WAS the
rule. With attribution required they all failed, which is the correct signal:
the old harness could not express the difference between a machine downgrade
and a human one, and neither could the code.

Fixed by giving them a way to say which they mean —
`conftest.write_attributed_downgrade()` — and calling it from the four driving
helpers plus the eight tests that set up their own transitions. The tests that
mean "the human changed model" deliberately do NOT call it; that distinction is
now visible in the test source, where it was previously invisible everywhere.

727 supervise tests pass.

## 20:15 · action · — — worker reload verified, not assumed

Per `.claude/ccy/CLAUDE.md`: supervisor file mtime `19:49:37`, worker pid
1707760 started `19:49:43` — six seconds later, so the running worker is this
code. Checked rather than inferred from the edit.

## 20:16 · handoff · P2 — attribution live, deletion next

Task 2.2 closed. Next is Task 2.3: delete the typed-argument parser, the stem
match, the picker wildcard, its session key and the restore-steal guard. They
all answer "was that the human?", and nothing asks any more. The third success
criterion (tests for the four observed input shapes) is deliberately left
unticked until then — the deletion is what closes it, not a claim made ahead of
it.

Cron: reused the session's existing failsafe recovery cron `5398b3f9`. Nothing
created.

## 20:20 · finding · T2.2 — the adversarial live check, and it passes

Worth doing rather than reasoning about, because this session is the worst
possible input: an 80 MB transcript stuffed with the literal token
`refusal_fallback` from the source I have been writing and the plan prose about
it. If the recognition were a substring match it would fire on my own commit
message.

```
lines containing 'refusal_fallback': 33
genuine records the parser finds:     0
signal files written for this session: 0
```

The prefilter/parse split is what makes that hold: the token gates a JSON
parse, and only a parsed payload with `subtype == model_refusal_fallback` or an
assistant-message block whose `type` is `fallback` counts. A tool result
carrying my source as a STRING never reaches that shape.

Also confirms the handler is live end to end — the daemon is running with it
enabled, PostToolUse fires on every tool call, and it correctly wrote nothing.

## 20:24 · finding · T2.2 — two registration gates the new handler had to pass

Neither is a defect; both are the repo refusing to let a handler exist without
a decision recorded about it, and worth naming so the next handler author is
not surprised.

- `test_example_config_includes_all_library_handlers` — every library handler
  must appear in `.claude/hooks-daemon.yaml.example`, enabled or disabled, so a
  user can see what exists. Added, with the disable-consequence spelled out in
  the comment rather than left for them to discover.
- `test_no_handler_is_unclassified` — every handler needs a recorded
  `get_claude_md()` verdict with a REASON (Plan 00234's audit). The reason
  matters more than usual here: this handler never speaks, so nothing about it
  is learnable at fire time, and what a reader needs standing is what turning
  it off costs them.

## 20:26 · handoff · P2 — Task 2.2 shipped; 2.3 is bigger than "delete"

Recording the coupling before it is rediscovered mid-deletion.
`write_manual_model_marker` is not dead weight: the daemon's
`downgrade_indicator` status-line handler READS that marker to suppress a false
"you are downgraded" badge when the human changed model themselves. Delete the
writer without moving that consumer and the status line starts lying in exactly
the case this plan is about.

So Task 2.3 has two halves: migrate `downgrade_indicator` onto the same
attributed signal (it currently runs the same high-water guess the supervisor
just stopped running), THEN delete the keystroke machinery. The `/effort` and
`/compact` recognition in `HumanInputLine` stays — only the MODEL half goes.

## 20:52 · decision · T2.3a — the signal file needed an owner outside both consumers

Half one is in. The status line and the supervisor now read the same record,
which forced a question half two would otherwise have asked badly: *where does
the signal file's schema live?*

It could not stay in the recorder. Two consumers reaching into a PostToolUse
handler for a path constant and a JSON layout is the shape the deleted
`_MANUAL_MODEL_MARKER_SUBDIR` had — a constant two deployables kept in step by
hand, which is precisely the coupling that let the old marker drift. So the
schema moved to `utils/model_downgrade_signal.py`: `DowngradeSignal`,
`signal_path`, `read_downgrade_signal`, `write_downgrade_signal`. The recorder
now only resolves the project's untracked dir and calls it; the status line
only reads it.

## 20:55 · finding · T2.3a — `manual` and `attributed` are not the same parameter renamed

`evaluate_downgrade` took `manual: bool`; it now takes `attributed: bool` and
the condition is inverted. That reads like a rename, and it is not — the
DEFAULT moved, and the default is the whole safety argument.

`manual=False` defaulted to *show the badge*: a caller that forgot the argument
told someone they were degraded because they had picked Opus themselves.
`attributed=False` defaults to *say nothing*. The unattributed drop is now also
treated as a fresh high-water — the human's chosen family becomes the new peak
— so a genuine substitution BELOW their choice is still caught. Both directions
are covered by tests (`test_attribution_defaults_off_so_a_caller_must_be_explicit`,
`test_a_later_attributed_drop_below_a_chosen_family_is_still_caught`).

The old test file could not express the difference at all: every downgrade test
just dropped the model and expected a badge. Those four tests now publish a
signal first, which is the point — a badge with nothing behind it is the defect.

## 20:57 · finding · T2.3a — a moved function leaves a live exclusion pointing at nothing

The error-hiding audit failed with six violations, and four of them were the
same move seen twice: `write_downgrade_signal` had left the recorder, so its
exclusion matched no finding (`stale-exclusion`) while the code itself was
flagged fresh at its new home. `_parse_manual_marker`'s exclusion was stale for
the other reason — the function is deleted.

Worth naming because the two failure modes look identical in the report and
have opposite fixes: a DRIFTED entry must be realigned (it is silently
exempting an innocent site), a DEAD one must be removed (it would otherwise
cover a future re-introduction). The audit says so in the message; it cannot
tell you which you have.

## 21:20 · decision · T2.3b — deleting the parser found a coupling nothing named

The keystroke machinery is out: both latches, the picker wildcard and its
session key, the restore-steal guard, the stem match, `write_manual_model_marker`,
the `TickFacts` fields, the host-side consumption and the state round-trip.

One thing had to be REPLACED rather than removed. `note_manual_model_command`
also cleared the manual `/effort` latch — a model spell was bounded by a typed
command, so a spell that began at the picker never ended and the latch pinned
effort to a level chosen under the previous family. That was a live bug hiding
inside the machinery being deleted, not something the deletion introduced.

The replacement is simpler and does not depend on recognising anything: a spell
is bounded by the family OBSERVED on screen, so `note_model_reading` clears the
latch on any family change. `test_manual_effort_survives_a_reading_on_the_same_family`
pins the other half — a re-render of the same family is not a change.

## 21:24 · finding · T2.3b — asserting a deletion needs absence tests, not silence

Rewriting `test_manual_model_choice.py` raised the question of what a deletion
should be tested for. Dropping the tests leaves nothing; keeping them inverted
("the latch does not fire") tests a dormant recogniser that could be re-wired.

So the tests assert ABSENCE: `hasattr(HumanInputLine(), "take_model_submitted")`
is False, `TickFacts` has no `human_model_command`, `export_state()` carries no
`manual_model_family`. The picker wildcard has now been reintroduced twice in
this codebase's history, each time by someone reaching for a channel that was
still sitting there. An absent attribute makes that a test failure instead of a
judgement call.

One import path deserved its own test rather than an absence assertion:
`test_legacy_state_with_the_deleted_manual_fields_imports_cleanly`. Host and
worker are separate processes that reload at different moments, so a worker
mid-upgrade really can be handed the OLD payload shape — import has to ignore
keys it no longer knows rather than raise.
