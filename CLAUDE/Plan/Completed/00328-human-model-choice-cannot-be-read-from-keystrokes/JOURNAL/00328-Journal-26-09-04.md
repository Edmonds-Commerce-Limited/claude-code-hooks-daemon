# Plan 00328 — Journal 26-09-04

> **Append-only activity log** for plan 00328. One file per day
> (`00328-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 08:01 · action · — — plan scaffolded

Plan 00328 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 08:19 · action · P1 — Phase 1 delivered and verified live

Committed as `c4e22ac0`. Worker reload confirmed per the
`.claude/ccy/CLAUDE.md` contract: pid 21053 started 08:19:22 against a file
mtime of 08:19:20, so the running worker is the new code, not the old.

## 08:45 · finding · P1 — full regression, and two numbers that needed settling

Full suite at `c4e22ac0`: **17,656 passed, 5 skipped, 1 xfailed, 3 errors**.
Neither anomaly was mine, but both were worth pinning rather than waving past:

- **The 3 errors are environmental and pre-existing.** `semgrep` is a declared
  dev dependency (`pyproject.toml:60`, `semgrep>=1.100`) that was absent from
  the venv, so `tests/unit/qa/test_semgrep_unbounded_source_reads.py` errored
  on 3 of its 4 tests. Confirmed identical at baseline `acbb5fdd` in a
  throwaway worktree; the file is untouched by this plan's commits.
- **The test-count delta is exactly +5** — this plan's new file and nothing
  else. Collected IDs: 17,665 at HEAD vs 17,660 at `acbb5fdd`. An earlier
  report of a 17,650 baseline was off by one (the true figure was 17,651),
  which had made the delta look like +6 and implied a sixth test nobody wrote.

Installed the missing dependency; it also lifted `click` 8.3.1 → 8.4.2, a
transitive bump broad enough to warrant a full re-run rather than just the
3 recovered tests. Re-run: **17,659 passed, 5 skipped, 1 xfailed, 0 errors**
(exit 0). The +3 is exactly the recovered semgrep tests.

## 08:47 · action · — — failsafe recovery cron

`CronList` was empty, so one was created per the advisor's canonical prompt:
id `5398b3f9`, hourly at `:23`, `durable:false`, `recurring:true`.

## 08:48 · handoff · P2 — next entry point

Phase 1 is complete, live and green. Phase 2 (Task 2.1: evaluate
`~/.claude/settings.json` as the human-intent channel) is **not started and
should not be started without the owner's steer** — Task 2.2 deletes a
substantial amount of Plan 00316/00317 keystroke machinery, which is a
design call rather than a mechanical follow-on.

Worth carrying forward: Phase 1 deliberately does **not** remove the *first*
futile `/model` injection. Recognition still cannot see a human's picker
choice, so on an allowance-exhausted account the reproduction now yields one
injection and one 429 instead of the full cascade. Removing that last
injection is precisely what Phase 2 is for.
