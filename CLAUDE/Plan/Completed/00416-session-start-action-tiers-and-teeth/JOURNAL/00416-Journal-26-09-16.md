# Plan 00416 — Journal 26-09-16

> **Append-only activity log** for plan 00416. One file per day
> (`00416-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 11:36 · action · T2.1 — ticked as closed-by-decision, not closed-by-delivery

Task 2.1 was the last unticked task, and its deliverable was a ruling rather
than code. Marked `- [x] ❌` — the repository's existing form for a task struck
on a design call (`00102` T5.3, `00110` T7.1/T7.2, `00107` T3.2), with `❌`
carrying the core workflow's "task no longer needed" meaning. The tick text now
says at the tick, not thirty lines down, that the deliverable is
`fable-cron-assertor-tier-decision.md` and that no code was written.

The alternative form in the tree is `- [x] ✅` over a struck title (`00375`
T2.1, `00230` T2.2), which those plans use when the SUBSTANCE still shipped by
another route. Nothing shipped here, so `❌` is the honest one.

## 11:38 · finding · — — degraded mode silences this plan's headline deliverable

Recorded in `PLAN.md` under "The three layers", beside layer 3, because that is
where the limitation has to travel with the feature.

00412's `fable-degraded-mode-decision.md` (closed today) establishes that the
daemon's degraded-mode early return covers EVERY event tier, not just
`PreToolUse`: `process_event` returns before the router and only the
degraded-mode safety net runs, which constructs `DestructiveGitHandler` alone.
So `Stop` cron enforcement does not fire while the daemon is degraded.

Pre-existing daemon behaviour, not a regression this plan introduced, and not a
reason to hold the plan open — the supervisor backstop in
`DESIGN-cron-enforcement.md` already owns the session the daemon cannot see.
Written down so the next reader of the enforcement design does not rediscover
it from a blocked-stop-that-never-came.
