# Plan 00324 — Journal 26-09-02

> **Append-only activity log** for plan 00324. One file per day
> (`00324-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 15:57 · action · — — plan scaffolded

Plan 00324 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 16:40 · decision · — — filed because noting a defect is not fixing it

Plan 00322 established the mechanism (Claude Code loads a skill's markdown
only) and fixed the one skill it was moving, then reported the other four as
"worth a look" — which is a hand-back, and the nitpick detector was right to
call it. Filed and fixed instead.

## 16:48 · finding · T1.1 — two exemptions the first test draft got wrong

The contract test initially flagged six scripts, not four:
`docs-qa/scripts/_locate-cli.sh` and
`hooks-daemon/scripts/_resolve-venv.sh` are SOURCED by sibling scripts
(`source "$SCRIPT_DIR/_locate-cli.sh"`), never invoked by an agent, so
demanding a markdown mention would be demanding a reference to something no
agent should run. Leading-underscore names are now exempt — the convention
was already there, the test just had to respect it.

After the exemption it named exactly `configure`, `mode`, `acceptance-test`
and `release`.

## 16:55 · action · T2.1 — landed

Each of the four SKILL.md files opens with a "Run this first" block naming
`${CLAUDE_SKILL_DIR:-.claude/skills/<name>}/invoke.sh` with the caller's
arguments passed through (all four scripts read args: `${*:-summary}`,
`${*:-get}`, `${1:-all}`, `${1:-auto}`). QA 25/25.
