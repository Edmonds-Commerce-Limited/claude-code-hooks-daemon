# Plan 00358 — Journal 26-09-08

> **Append-only activity log** for plan 00358. One file per day
> (`00358-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 11:26 · action · — — plan scaffolded

Plan 00358 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 13:35 · decision · T1.1 — conftest session guard, not an activation check

Chosen: `tests/source_tree_guard.py`, run from `pytest_sessionstart`. The
defect is what Python imports, and only Python can say that; a
`venv-include.bash` check sees a path, not an import. The nested pytest runs
in the integration suite write their own conftest into a temp directory and
never load this one, so they need no exemption.

## 13:36 · finding · — — the guard matters for today's other work

Seven worktree sub-agents are fixing the release-review ledgers right now.
Until this lands in their branches, a green run they report may describe
main's source; so every branch is re-run on `main` after merge, and their
own results are treated as unverified.

## 13:38 · handoff · — — shipped

Guard, unit tests, handler guidance, Worktree.md note and the release-notes
callout are on main in the same commit as the archive move.
