# Plan 00275 — Journal 26-08-26

> **Append-only activity log** for plan 00275. One file per day
> (`00275-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 09:33 · action · — — plan scaffolded

Plan 00275 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

User request: a PreToolUse guard blocking git commits whose messages carry
GitHub auto-closing keywords (they auto-close issues/PRs on merge and cannot
be disabled repo-side); must catch `-m` messages AND `-F <file>` scratch-file
messages; suggested enabled by default. Failsafe recovery cron: REUSING the
session's existing cron `f07b4ca0` (hourly at :23); nothing created.
Worktree executor sub-agent dispatched to draft the plan and implement.

## 11:35 · action · P1–P3 — handler delivered, QA green in worktree

- Grammar verified via WebFetch against docs.github.com "Linking a pull
  request to an issue": nine keywords (close/closes/closed, fix/fixes/fixed,
  resolve/resolves/resolved), case-insensitive, optional colon; references
  `#N`, `owner/repo#N` (documented), plus `GH-N` and full issue/PR URLs.
- TDD: test file first (48 tests) → handler
  `src/claude_code_hooks_daemon/handlers/pre_tool_use/github_auto_close_keywords.py`.
- Scope decisions in PLAN.md: `git merge -m`/`git tag -m` covered;
  `-t/--template` ignored; `gh issue close` excluded; escape hatch
  `MUST_AUTO_CLOSE_BECAUSE="reason"`; `mode: warn|block`; non-terminal.
- Registered everywhere: HandlerID meta, Priority 18, pre_tool_use
  import/__all__, init_config default (enabled: true), dogfood yaml,
  yaml.example, HANDLER_REFERENCE.md (section + summary row), UNRELEASED
  config-changes manifest (recommended: true, enabled-by-default noted).
- Guidance coverage verdict recorded (T1 earns guidance); evasion suite
  classification added (git -C / --no-pager / line-continuations deny;
  keyword-alone, rewrite, grep and hatch allowed).
- error_hiding audit fix: replaced exception-swallow around the -F read with
  an up-front is_file/os.access check + debug log.
- Display name set equal to the config key so the repo-hygiene orphan check
  resolves the CLAUDE.md marker via module stems even where its imported
  HandlerID predates this handler (worktree QA).
- Started a worktree-scoped daemon (own socket in worktree untracked/) for
  the QA smoke probes; dogfood daemon untouched.
- Full QA: 24/24 PASSED (14,137 passing tests, coverage 95.1%).

## 11:40 · handoff · P4 — landing left to the main session

Commits on the worktree branch: 3ba8bccf (Phase 1), 971e6f49 (Phase 2), plus
the final QA-fix/plan-update commit. Not merged, not pushed. Main session:
merge, restart dogfood daemon, verify RUNNING, then flip the plan to
Complete with the archive move + README update in one commit (Task 4.1).

## 12:05 · action · — — implementation-review fixes applied (FIX FIRST verdict)

All eleven review items applied:

1. -F read hardened: up-front is_file/os.access probe, 64 KiB byte cap
   (`_MAX_MESSAGE_FILE_BYTES`), `read_bytes().decode(errors="replace")` — a
   binary file can no longer raise UnicodeDecodeError out of matches() and
   abort the PreToolUse chain. Docstring rewritten; binary + oversized
   regression tests added.
2. gh pr coverage: `gh pr create|edit` segments scanned (`--body`/`-b`
   inline, `--body-file` read under the same file rules); guidance,
   HANDLER_REFERENCE and acceptance tests updated; 4 new unit tests.
3. Segment scoping: command split on `&&`/`||`/`;`, only text from the
   `git commit|merge|tag` / `gh pr create|edit` token onward is scanned —
   reading is never blocked. Both reviewer compound-command negatives added.
4. `[ \t]*` replaces `\s*` — keyword and reference must share a line;
   multi-paragraph negative added.
5. -F/--body-file extraction is per message segment, so `grep -F` before
   the git segment never triggers a file read (regression test added).
6. constants/handlers.py: entry moved below GIT_MESSAGE_BACKTICK so the
   Plan 00219 comment is no longer orphaned.
7. Escape hatch now case-sensitive (sibling convention); deny message notes
   the reason must be non-empty and QUOTED; case/unquoted negatives added.
8. `_mode` normalised with strip/lower; unknown values fail CLOSED (denies)
   — documented as deliberately stricter than git_stash; tests added.
9. Per-dispatch match cache keyed on the command string — the -F file is
   stat'd/read once per dispatch (TOCTOU between matches() and handle()).
10. HANDLER_REFERENCE summary row moved to sorted position (18 before the
    20s). Display-name convention re-checked: the orphan finding is caused
    by the venv's editable install resolving HandlerID from the MAIN
    checkout, so it does reproduce whenever generated CLAUDE.md guidance is
    ahead of the installed constants (any worktree QA run) — keeping
    display_name == config key is the robust spelling; documented at the
    constant.
11. The three field-report-shaped tests are the regression tests in items
    1/3/4.

Unit tests now 63 in the handler file; full QA re-run: 24/24 PASSED.

## 12:10 · finding · — — correction

The handler's unit test file runs 64 tests, not 63 as the previous entry
stated (miscount; corrected here per append-only convention).
