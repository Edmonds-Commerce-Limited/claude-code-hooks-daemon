# Plan 00330 — Journal 26-09-08

> **Append-only activity log** for plan 00330. One file per day
> (`00330-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 11:20 · action · P3 — housekeeping pass as data, CLI verb, advisory reuse (`59b11a0a`)

`daemon/housekeeping.py` is a pure step list: 12 report-only steps, then
`format-markdown` and `regenerate-docs` (unconfirmed), then six HELD steps,
then `optimise` last. `plan_pass(apply)` assigns RUN/HELD; `render_procedure`
prints the sub-agent procedure. `cmd_housekeeping` (`--apply STEP`, `--list`,
`--reports-dir`, `--project-root`) prints it and runs nothing. The idle
advisory's "candidate audits" list is now derived from `report_only_steps()`
and points at `bin/hooks-daemon housekeeping`.

Two choices worth recording:

- `optimise` is a step with no CLI verb (`cli_argv=()`, `via_skill=
  "optimise"`), invoked through its existing skill entry point — the other
  agent owns the checklist and `optimise-invoke.sh`, so nothing here touches
  them.
- A step's verb is tested against the real argparse (`--help` exits 0), so a
  renamed CLI verb fails the housekeeping tests, not a client's pass.

## 11:40 · action · T1.1 — Decision 4 surface trim (`f458057e`)

SKILL.md routes 8 arms (`install`, `upgrade`, `optimise|optimize`,
`housekeeping`, `report`, `restart|bug-report`, `health`, help). The 10
documented-only subcommands live in a "Capabilities (CLI verbs)" section
naming the exact `bin/hooks-daemon` verb; `health.md`, `regen-docs.md`,
`rule-explain.md`, `check.md`, `dev-handlers.md` now show the CLI form, and
`housekeeping.md` is new. `SkillCommand.RULE_EXPLAIN` became
`CliCommand.EXPLAIN_RULE`, so the terse block-message pointer prints
`bin/hooks-daemon explain-rule <ID>`.

Findings from the tests that guard the surface:

- `test_skill_scripts_are_referenced` demands `init-handlers.sh` be named
  AND invoked with arguments by some skill markdown; `dev-handlers.md` keeps
  a `bash .../init-handlers.sh "$@"` line for that reason rather than the
  script being deleted (Non-Goals forbid removing the capability).
- `test_cli_config_validate.TestSkillTextRoutesOnlyKnownVerbs` asserted
  `config-validate`/`validate-config` were ROUTED; rewritten to assert the
  skill DOCUMENTS both spellings and the parser accepts the alias.
- Deployed `.claude/skills/hooks-daemon/` is byte-copied from source;
  `diff -r` clean.

## 11:50 · handoff · P3 — Phase 3 complete; Phase 4 gate not started here

Release-notes callout `29-one-housekeeping-command-and-a-smaller-skill-surface.md`
written. Phases 2 and 4 are with the concurrent agent. Not done on purpose:
generated `.claude/HOOKS-DAEMON.md` / `CLAUDE.md` block (the advisory's
`get_claude_md` text changed) — regenerates on the next daemon restart in
the main checkout; no restart from this worktree.

## 12:10 · action · P2 — relevance primitive and overrides, `bf5da1f5`

`core/relevance.py` (stdlib-only: `Relevance`, `RelevanceContext`,
`detect_languages`), `Handler.get_relevance()` defaulting to always-relevant,
and overrides on eleven handlers. Two shared predicates rather than seven
copies: `utils.ccy_supervisor.supervisor_relevance` (armed = `ccy.env`
exports the wrapper, the same test `ccy_supervisor_integrity` uses) and
`handlers.utils.quarantine.quarantine_agent_relevance` (the
`hooks-daemon-opus-security` template deployed under `.claude/agents/`).

Findings while placing them:

- **No standalone PHP guard exists.** The ruling names "the PHP guards";
  the registry has none — PHP is a strategy inside the multi-language
  handlers (`qa_suppression`, `lint_on_edit`, …) and the one PHP-specific
  handler is in `RETIRED_HANDLERS`. Nothing to override; recorded in the
  Task 2.2 tick.
- `validate_eslint_on_write` is language-conditional in the same way the npm
  handlers are, so it got an override too (JS/TS toolchain markers), even
  though the ruling did not list it.
- `lsp_enforcement` decides at fire time from `ENABLE_LSP_TOOL` in the
  environment. The review runs in a CLI subprocess that inherits the session
  env, and additionally reads `.claude/settings.json`'s `env` block, which is
  where a project pins it. In this repo the env var is set, so
  `lsp_enforcement` scores as relevant here.
- `NpmCommandHandler()` and `ValidateEslintOnWriteHandler()` read
  `ProjectContext` in their constructors, so the checklist builder — which
  instantiates every class exactly as `register_all` does — needs it
  initialised; the CLI verb does that, and the unit tests initialise it
  against this repo's config.

## 12:40 · action · P2 — derived checklist, areas, CLI verb, `bf5da1f5`

`config_optimisation/areas.py` (six areas, event rules before tag rules, as
§1.3 measured) and `config_optimisation/checklist.py` (`build_checklist`,
`render_report`, `report_as_json`). Enabled-state resolution mirrors the two
registration paths rather than re-deciding it: `register_all`'s
`enabled`/`enable_tags`/`disable_tags` gates for built-ins, and the
pseudo-event dispatcher's two gates for nitpick — including the asymmetry
that an absent `handlers.<event>.<name>` block is ENABLED while an absent
`pseudo_events.nitpick` section is DISABLED. A first cut got the second one
wrong (`_block()` returned `{}` for an absent section, which then read as
"present with defaults"); the unit test caught it.

`registry.iter_builtin_handler_classes()` walks exactly what `register_all`
walks. `discover()` differs: it also finds `handlers/nitpick/`, which is a
pseudo-event with its own registry, so the iterator's contract test
subtracts those two.

Rendered for this repository:

```
Overall: 110/110 relevant handlers enabled (100%) · 6 not applicable here
```

Six not applicable: the flaggable trio (no quarantine agent deployed), the
two npm handlers (no `package.json`), `validate_eslint_on_write`. Every
fully-enabled area collapses to one line, so the whole report is ~35 lines
for 116 handlers.

## 13:05 · action · P4 — the surface gate, `bf5da1f5`

`tests/integration/test_skill_surface_coherence.py`. Its first RED run found
one real drift and four over-matches, which is the right ratio for a new
gate:

- **Real**: none in the end — `report.md` names `transcript_archiver`, but
  as "the retired `transcript_archiver` handler", which is documentation of
  the retirement and the one legitimate way to name it. The rule now
  exempts a line that says retired/removed.
- Over-matches fixed structurally, not by allowlist: `cleanup` is both a
  retired handler and an English word (rule: code context only — backticks
  or a `handlers.<event>.` path); `DAEMON_CLI wrapper/path/with` is prose
  (rule: `DAEMON_CLI` counts only at the start of a line, which is how the
  procedure writes a command); `bin/hooks-daemon wrapper not found` is an
  `echo` (rule: echo lines print, never run); `validate-config` is an
  argparse alias (rule: aliases are verbs); `my_handler.priority` is a
  worked example (rule: `my_`/`your_`/`example_`/`custom_` segments are
  placeholders).
- `config_optimisation_reminder` appears in the procedure's Step 7 prose as
  a backticked mention; the no-hand-written-list check exempts backticked
  names, since a checklist entry is bare.

Task 4.3 needed no wiring: the gate is under `tests/`, `run_tests.sh` runs
`tests/`, and `llm_qa.py all` is RELEASING.md Step 8. Step 8 now names it.

QA: `ruff`, `black` (authority), `mypy --strict` on every touched file,
`llm_qa.py skill_refs handler_reference doc_truth` (3/3), docs-qa lint on
the edited docs, full `tests/unit` plus the touched integration tests:
17405 passed.

## 13:10 · handoff · P3 — Phases 2 and 4 delivered; Phase 3 open

Phases 2 and 4 are ticked in `PLAN.md` against `bf5da1f5`. Phase 3 (the
housekeeping command, Decisions 5–7) is untouched. Deployed skill copies
under `.claude/skills/hooks-daemon/` were refreshed by copying from source
and `test_deployed_skill_trees.py` passes. Not pushed, not merged.

## 19:30 · handoff · — — Complete

Both phase branches merged on main (`7b7d8c9a` after `59b11a0a`,
`f458057e`, `bf5da1f5`); the one whole-tree finding was a raw `.is_file()`
in the LSP relevance probe, moved onto the EACCES-safe predicate. Full QA
26/26 after the daemon restart; `optimise-checklist` scores 116 handlers
live (110 relevant and enabled here, 6 not applicable with reasons) and
`housekeeping --list` shows the 20 steps in the ruled order. Plan archived.
