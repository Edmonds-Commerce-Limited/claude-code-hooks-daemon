# Plan 00311 — Journal 26-09-08

> **Append-only activity log** for plan 00311. One file per day
> (`00311-Journal-YY-MM-DD.md`). Entry grammar: `## HH:MM · category · REF`,
> categories `action | finding | decision | thought | blocker | handoff`.

## 14:05 · decision · — — the ledger is worked, not scheduled

Owner ruling today: release-review findings are defects to fix, not items
to park. A worktree sub-agent took all five; each got a test first.

## 14:06 · finding · T1.3 — the "verified correct" item was not

The second look the review asked for found `--pathspec-from-file=<file>`:
git reads the file as pathspecs and echoes an unmatched line verbatim in
its own error, so `git rm --cached --pathspec-from-file=<protected>` was a
content-disclosure route through the hygiene exemption. Fixed by voiding
the exemption when that flag is present.

## 14:07 · decision · T1.2 — keep the gates, fix the residual

A from-scratch re-derivation of the glob heuristics was attempted and
rejected: no single formula reproduced the 131-test adversarial corpus
without widening somewhere. The Bash-surface residual (one-line
`python -c "import ..."` denied) is fixed on its own.

## 14:08 · handoff · — — closed

Merged at `170103e7`; full QA on main green after the merge. Release-notes
callout 07 written; plan archived.
