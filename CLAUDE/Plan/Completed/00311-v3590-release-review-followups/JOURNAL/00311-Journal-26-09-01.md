# Plan 00311 — Journal 26-09-01

> **Append-only activity log** for plan 00311. One file per day
> (`00311-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 19:46 · action · — — plan scaffolded

Plan 00311 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 21:50 · finding · — — acceptance run caught a live-inert handler pair (fixed in-release)

Step 12.4 execution of playbook Tests 232/234 showed no dispatch_declaration
advisory. Daemon logs proved the PreToolUse payload now arrives with
`tool_name: "Agent"` (Claude Code 2.1.252) while `dispatch_declaration` and
`agent_isolation_advisor` matched only `ToolName.TASK` — both handlers were
silently inert in the live harness despite green unit tests. Fixed TDD in
this release: shared `SUBAGENT_DISPATCH_TOOL_NAMES = {"Task", "Agent"}`
constant, both matchers updated, Agent-name regression tests added, CHANGELOG
and release notes updated. FAIL-FAST cycle restarted per RELEASING.md.
Lesson for the ledger: unit tests pin the daemon's *expected* input shape;
only a live acceptance call pins the harness's *actual* shape — tool-name
drift between Claude Code versions needs a live probe per release.

## 22:20 — Round-2 acceptance run complete (Step 12 done)

Full 274-test playbook re-executed in the main thread after the Agent
tool-name fix (e7af7783): zero failures. Critical re-verification: an
undeclared Agent dispatch now surfaces the DISPATCH DECLARATION advisory
(Test 232) and a plan-folder-naming dispatch stays silent (Test 234) — the
fix is confirmed live. Two config-scoped observations recorded as passes:
bash_safe_mode is silent on read-only sequences here (only_with_mutator:
true) and docs_qa_edit delivers block findings as warnings (edit_mode:
warn) — both are this repo's declared rollout policy, not defects.
Proceeding to Step 13 (commit/push), then tag and GitHub release.
