# Plan 00296 — Journal 26-09-01

> **Append-only activity log** for plan 00296. One file per day
> (`00296-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 06:45 · action · T3.4 — monorepo detector + advisory landed

`handlers/session_start/monorepo_detector.py` (`MonorepoDetectorHandler`,
`Priority.MONOREPO_DETECTOR = 66`, widening `PriorityRange.ADVISORY_MAX` to
66). Reuses `_manifest_in` from `core/workspace.py` for both the root check
and the below-root walk — no second manifest table. Silent when: a root
manifest exists (ordinary single project), `projects:` is already declared
(read straight off the injected `_project_registry.projects`, no new config
wiring needed), or nothing is found below the root either.

The walk (`_find_manifest_dirs`) skips `CORE_VENDORED_BUILD_DIR_NAMES` and any
dotdir, does not descend into a directory holding its own `.git` (a different
repository, not a sub-project — per `WorkspaceResolution.md`'s documented
limit), stops at a directory once a manifest is found in it, and is bounded to
4 levels below the root so a huge repo cannot make session start slow. The
advisory names every workspace found and prints a paste-ready `projects:` YAML
block via `yaml.safe_dump`.

13 new tests (`tests/unit/handlers/session_start/test_monorepo_detector.py`)
cover: root-manifest silence, declared-projects silence, the advisory shape
and content, vendor-dir exclusion, nested-git-repo exclusion, and the depth
bound. Full `tests/unit/handlers/` (5263 passed), `mypy --strict`, `black
-l 100`, `ruff check`, and `scripts/qa/check_handler_reference.py` all clean.

## 06:50 · action · — — priority range docs updated for the widened ADVISORY_MAX

`CLAUDE/HANDLER_DEVELOPMENT.md`'s Priority Ranges table is the canonical
source (`56-65` → `56-66`); `CONTRIBUTING.md` and `docs/guides/CONFIGURATION.md`
carry an `ssot-quote` of the same table, updated identically. Added a
`#### monorepo_detector` section to `docs/guides/HANDLER_REFERENCE.md`
alongside `tool_disable_advisor`.

## 09:00 · action · T2.5+T3.5 — markdown_organization resolves declared `projects:` first

`_declared_subproject_relative()` resolves a normalized markdown path
through the injected `_project_registry` via the same `resolve_workspace()`
helper `npm_command`/`lint_on_edit`/`tdd_enforcement` use — a declared
`projects:` root that CONTAINS the path is stripped to a sub-project-relative
path exactly like `strip_monorepo_prefix()` already did for
`monorepo_subproject_patterns`. `matches()` now tries the declared registry
FIRST and falls back to the deprecated pattern alias
(`self._declared_subproject_relative(normalized) or
self.strip_monorepo_prefix(normalized)`), so both mechanisms are unioned —
neither shadows the other (pinned by
`test_declared_projects_and_pattern_alias_are_unioned`).

No inference: when `_project_registry` is `None` or resolves the path to the
repository root (nothing declared covers it), `_declared_subproject_relative`
returns `None` and behaviour is identical to before this task — pinned by
`test_no_registry_injected_is_byte_identical_to_before` and the anti-inference
pin `test_anti_inference_undeclared_subproject_runs_from_repository_root`
(a manifest-shaped-looking sibling directory that was never declared still
gets repo-root treatment, i.e. blocked).

New test class `TestDeclaredProjectsSubprojects` in
`tests/unit/handlers/pre_tool_use/test_markdown_organization.py` deliberately
exercises `RELEASES/` paths rather than `CLAUDE/Plan/...` ones: `normalize_path`
already strips anything containing a `CLAUDE/` marker regardless of any
monorepo mechanism (see the pre-existing
`test_subproject_without_monorepo_config_falls_through_to_normalize`), so a
`CLAUDE/`-shaped path would not isolate the declared-`projects:` behaviour
under test. `RELEASES/` has no such marker.

Docs updated: `_RULE_WRONG_LOCATION.verbose`, `get_claude_md()`, and
`strip_monorepo_prefix()`'s docstring all now point at `projects:` as the
primary mechanism and name `monorepo_subproject_patterns` as a deprecated
alias, cross-referencing `CLAUDE/Code/WorkspaceResolution.md`.

QA: `tests/unit/handlers/pre_tool_use/test_markdown_organization.py` (205
passed), full `tests/unit/handlers/` (5257 passed, 1 skipped, 1 xfailed —
both pre-existing and unrelated). `mypy --strict` clean on the handler; the
28 `mypy --strict` errors in the test file are pre-existing (same count,
same lines, confirmed via `git stash`), not introduced by this task. `black
--line-length 100` and `ruff check` clean on both changed files.

## 09:00 · handoff · — — Tasks 2.5 and 3.5 done

Both ticked in PLAN.md. Phase 2 is now fully complete; Phase 3 remaining
work is Task 3.4 (monorepo detector advisory) and Task 3.6 (rewrite
`WorkspaceResolution.md` around declared-or-root — its "other config
surfaces" section already documents the `markdown_organization` alias
correctly, so 3.6 is really about the rest of that doc, not this handler).

## 09:05 · action · — — journal merge-conflict resolution (coordinator)

The T3.4 and T2.5/T3.5 worktree agents each created this day-file in
parallel; the add/add conflict was resolved by keeping the full header and
both agents' entries. The out-of-order timestamps above reflect the two
agents' own clocks, not a rewrite.

## 09:15 · action · T3.6 — `projects:` documented in the config reference

Added a `## Projects (monorepo boundaries)` section to
`docs/guides/CONFIGURATION.md` between `ccy Container Workflow` and `Handler
Configuration`: schema table (`name`/`root` required, `kind`/`bin_dirs`
optional), the zero-config default (omitted == one project at repo root,
byte-identical), a YAML example including a manifest-less `infra/` project
with explicit `kind`/`bin_dirs`, the repo-root-relative zero-absolute-paths
rule, a note that `monorepo_detector` prints a paste-ready block, and that
`markdown_organization.monorepo_subproject_patterns` is a deprecated alias.
Depth stays in `CLAUDE/Code/WorkspaceResolution.md` (linked, not restated),
per `docs/CLAUDE.md`'s SSoT routing table and the human-docs rule. Also added
`projects:` to the "Config Structure Overview" top-level-sections YAML
skeleton for consistency. No other config-reference surface (examples/,
HANDLER_REFERENCE.md) lists top-level config keys, so no further satellite
edits were needed. Task 3.6 ticked in PLAN.md.

## 06:46 · decision · T4.1 — degradation surfaced via a concrete `Handler` hook, wired only into `check`

Added `Handler.get_enforcement_status(project_root: Path) -> list[str]`
(`core/handler.py`) — concrete, default `[]`, so the vast majority of
handlers with no on-disk-dependent posture need no change. A handler whose
strictness is decided by a cheap on-disk probe overrides it to report the
degraded condition at a given root.

**Scope chosen deliberately narrow**, per the plan's "smallest design that
satisfies the success criterion" steer: implemented for the three handlers
the survey named, wired into `check` output ONLY (not `handlers`):

- `npm_command` / `validate_eslint_on_write`: both already probe
  `has_llm_commands_in_package_json()` per invocation (Plan 00296 Task 2.1/2.3)
  — `get_enforcement_status()` just re-runs that same cheap read at the
  requested root. `validate_eslint_on_write` honours its pinned
  `workspace_root` test seam over the passed root, matching `_workspace_for`'s
  own precedence.
- `lint_on_edit`: probes only each active strategy's EXTENDED lint command
  (the one that "may or may not be installed" — the built-in default, e.g.
  `bash -n`, is always available, so its absence is not a degradation worth
  reporting). Reuses `_resolve_executable`'s existing filesystem checks
  (`is_file`/`shutil.which`) — no subprocess run. Required a small addition,
  `LintStrategyRegistry.strategies()` (dedup by identity), since the registry
  previously only exposed lookup-by-file and a bare language-name list.

**Why `check` only, not `handlers`**: `handlers` lists every registered
handler on every invocation, including from scripts that parse it — adding a
filesystem probe per handler there would slow the common case for a
diagnostic nobody asked for on that call. `check` is already the on-demand,
deliberately verbose audit (Plan 00128) that reads config and runs several
probes per invocation, so one more line-item fits its existing cost profile.

**CLI wiring** (`_collect_enforcement_status_lines` in `daemon/cli.py`):
loads `.claude/hooks-daemon.yaml` best-effort (falls back to
`ProjectRegistry.single_project()` on any load/validation error — `check` is
also how a broken config gets diagnosed, so it must not raise), builds a
`ProjectRegistry`, and evaluates all three handlers at the repository root
AND every declared `projects:` root. Handlers are constructed directly
(mirroring the existing `OptimalConfigCheckerHandler()` /
`GitFilemodeCheckerHandler()` pattern in `cmd_check`), bypassing the registry/
daemon lifecycle entirely — deliberate, since a `handlers`-wide sweep was
explicitly out of scope. `npm_command`/`validate_eslint_on_write`
construction probes `ProjectContext.project_root()` for state this function
never reads (their per-root posture comes from `get_enforcement_status()`,
not the constructor default); `ProjectContext` is patched to `project_path`
for the duration of construction only, matching those handlers' own test
suites (e.g. `test_validate_eslint_on_write.py`'s `mock_project_context`
fixture) rather than depending on a live daemon-initialised singleton.

**Known limitation, accepted**: a pure-non-Node repository with no
`package.json` anywhere reports `npm_command`/`validate_eslint_on_write` as
"degraded" at every root, which is technically true (raw npm/npx commands
would only get an advisory) but is noise for a repository that never runs
npm at all. This mirrors those handlers' own existing advisory-mode
semantics (`get_claude_md()` already documents "operates in advisory mode"
for exactly this case) — `check` is reporting the same fact the handler
already discloses per-invocation, just proactively. Not fixed here: doing so
would need `check` to know whether npm is even relevant to the repo, which
is exactly the kind of inference this plan's Goals reject.

QA: new/changed tests in `tests/unit/core/test_handler.py`,
`tests/unit/daemon/test_cli_check.py`, `tests/unit/daemon/
test_cli_enforcement_status.py` (new file), `tests/unit/strategies/lint/
test_registry.py`, `tests/unit/handlers/pre_tool_use/test_npm_command.py`,
`tests/unit/handlers/post_tool_use/test_lint_on_edit.py`,
`tests/unit/handlers/post_tool_use/test_validate_eslint_on_write.py` — all
pass. Full `tests/unit/handlers/` (5266 passed) and `tests/unit/daemon/`
(1414 passed) both green, no regressions. `mypy --strict` clean on every
changed source file; pre-existing unrelated errors in `test_npm_command.py`/
`test_lint_on_edit.py` confirmed via `git stash` (identical count and lines
before/after this change — `MYPYPATH` had to point at THIS worktree's `src/`
explicitly, since the installed editable package otherwise resolves against
the main checkout and reports false `attr-defined` errors for methods that
only exist in the worktree). `black -l 100` and `ruff check` clean on every
changed file.

## 09:25 · action · — — journal merge-conflict resolution (coordinator)

The T4.1 worktree agent's entry (06:46 by its clock) conflicted with the
T3.6/coordinator entries added on main in parallel; resolved by keeping both
sides in full. Timestamps reflect each agent's own clock, not a rewrite.

## 12:00 · handoff · — — plan closed out and archived

Verified all four Success Criteria against merged `main`: the fixture
monorepo enforcement path (`npm_command`, `lint_on_edit`,
`tdd_enforcement`), the undeclared-monorepo advisory path
(`monorepo_detector`), single-project byte-identical behaviour, and `check`
degradation visibility — each pinned by the tests cited in PLAN.md's
Success Criteria section. 594 targeted tests pass; a full `tests/unit` run
(14,464 passed) surfaced three genuine but undocumented error-hiding sites
in this plan's own merged code (best-effort `check` config fallback,
expected out-of-scope path in `markdown_organization`, best-effort
directory-walk skip in `monorepo_detector`) — recorded in
`scripts/qa/error_hiding_exclusions.json` with rationale, restoring
`test_audit_error_hiding.py::TestRealRepoSelfScan` to green. The two
`test_project_context.py` order-dependent failures noted as known/expected
did not reproduce in this run; documented as pre-existing regardless.
PLAN.md Status set to Complete, Success Criteria ticked, folder archived to
`CLAUDE/Plan/Completed/`.
