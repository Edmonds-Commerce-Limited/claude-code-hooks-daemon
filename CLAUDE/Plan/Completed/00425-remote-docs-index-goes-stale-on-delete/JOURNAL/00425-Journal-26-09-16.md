# Plan 00425 — Journal 26-09-16

> **Append-only activity log** for plan 00425. One file per day
> (`00425-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 17:37 · action · — — plan scaffolded

Plan 00425 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 17:40 · finding · — — the issue's "Where" is wrong, and it changes the fix

Issue #43 says `_regenerate_remote_docs_index` "is called from the `add` path
only". It is not — it sits after the action dispatch in `cmd_remote_docs` and
runs for `add` **and** `refresh`. Reproduced: `refresh --all` cleared the stale
row.

That cuts both ways. It narrows the gap to `list` and `check`, but it also means
the only existing repair is a full network re-fetch of the whole corpus to
correct a purely local rendering — which strengthens the reporter's "no supported
way to bring the index back into line" point rather than weakening it.

Reproduction script: `untracked/scratch/repro43.py`. Output with one of two
captures deleted:

```
after list, index still names a: True
running `remote-docs check`:
remote-docs: all vendored documents are fresh
check exit code: 0
after check, index still names a: True

after `refresh --all`, index still names a: False
```

The middle three lines are the sharpest form of the defect: the command whose
job is to report problems asserts health while the index is demonstrably wrong.

## 17:41 · finding · — — root cause is the assumption, not the call sites

`remote_docs/index.py` records the original decision at the source: regenerate on
`add` and `refresh` "rather than on demand, because an index that silently goes
stale is worse than no index". That reasoning holds and is not what failed.

What failed is its assumption that every change to the tree arrives through the
CLI. Deletion is the one mutation that does not — `rm` runs no command — so no
regeneration hook can fire, whichever commands carry one. Adding regeneration to
more commands cannot fix this, because the project may never run one afterwards.
Detection is the load-bearing half.

## 17:42 · decision · P1/P2 — the issue's two suggestions are incompatible

#43 offers (a) regenerate at the end of `list`/`check`/`refresh`, or (b) a
`remote-docs index` command plus `check` reporting staleness, as alternatives.
They are not: taking (a) forecloses (b). A `check` that regenerates the index can
never *report* it as stale, because it would have just repaired it — and a
read-only reporting command would start writing files as a side effect, which
matters to anything running `check` in CI.

Going with (b). Recorded (a) as a Non-Goal with this reasoning.

## 17:43 · decision · P2 — simplicity check before adding CLI surface

Looked for an existing home for "re-render a generated artefact" before adding a
verb; there is none (`repair` is venv-specific). So `index` is a fourth verb on a
subcommand that already has four, and its body is the existing
`_regenerate_remote_docs_index` call.

Two things that check surfaced for the implementer:

- `_remote_docs_check` receives `tree` only, but the index lives at
  `<project_root>/.claude/REMOTE-DOCS.md` — the signature has to widen.
- `render_index` is pure and path-ordered with no generation timestamp
  (`test_rewriting_is_idempotent` already pins this), so staleness detection is
  an exact string comparison with nothing to churn.

Flagged the new-verb judgement call on the issue itself, so the owner can object
before merge rather than after.

## 17:44 · action · — — session infrastructure

Failsafe recovery cron: reusing the one already established this session,
`f5473232` (recurring, :47). Created nothing new — one per session, not one per
plan.

## 18:20 · finding · P1/P2 — review found two defects the tests did not

The sub-agent's implementation passed its own tests and the design held up. Two
defects surfaced only from asking Step 6's "does this match too WIDELY?"
question and probing it (`untracked/scratch/probe43.py`):

1. **Over-match.** With no tree and no index, `check` went from exit 0 "all
   vendored documents are fresh" to exit 1 "index out of date" — so every
   project that never uses remote-docs would have had a green CI turn red. Fixed
   by gating the finding on `index_exists or list_documents(tree)`.
2. **The summary counted the index as a document** — "1 document(s) need
   attention" when no document did.

Both were fixed, with RED first. Worth noting the shape: a suite can be entirely
green and still miss the case nobody thought to write, and the question that
found these was about the SHAPE of the match, not about any particular input.

## 18:35 · finding · — — a test that had never been seen to fail

`test_check_stays_quiet_for_a_project_that_vendors_nothing` passed the first
time it ran, because the sub-agent had already fixed the over-match on disk
while the review was being written. A test that has only ever been green proves
nothing. Reintroduced the guard deliberately and confirmed it fails:

```
FAILED ...TestListAndCheck::test_check_stays_quiet_for_a_project_that_vendors_nothing - assert 1 == 0
```

Then restored the source and verified it was byte-identical to HEAD before
committing.

## 18:40 · decision · — — consolidated the duplicated tests

Both ends fixed the same two findings independently, leaving two tests per
scenario. Kept the stronger assertions and deleted the weaker: the survivors
also assert the "all vendored documents are fresh" message, and assert
`document(s) need attention` is absent for ANY count rather than enumerating
`0 document(s)` and `1 document(s)` — which would have sailed past
`2 document(s)`.

## 18:45 · finding · — — the earlier QA_EXIT=1 was self-inflicted

The first full QA run reported `QA_EXIT=1`: 18 errored tests and smoke 0/3, all
acceptance probes dispatching against the live daemon socket, reporting
`STALE DAEMON: source_fingerprint 66707663ed38 ... does not match ... d5c7af76b4b7`.

Cause: the worktree daemon had been restarted BEFORE any source changed, so it
was serving pre-change code. Restarting it cleared all 18 — the final run was
`QA_EXIT=0`, 35/35, 24917 passed, smoke 3/3. Distinct from ledger 00422 N6's
fault 2 (the playbook harness HANG): nothing hung here, the tests errored fast.

Also stopped an intermediate QA run rather than trusting it, because a commit
landed after it started — a result measured against a tree that moved
underneath it is not a result.

## 19:05 · handoff · — — plan complete

Merged `9282c9ea`, CI green (run 35136177334), worktree and branch reaped, issue
#43 closed as completed. The original reproduction was re-run against merged
`main` and no longer holds. Release note is in the pending-release holding area;
this plan never releases, tags or publishes.
