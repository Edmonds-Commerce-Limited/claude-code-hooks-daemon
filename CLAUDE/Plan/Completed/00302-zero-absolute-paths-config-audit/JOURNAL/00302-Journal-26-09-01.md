# Plan 00302 — Journal 26-09-01

> **Append-only activity log** for plan 00302. One file per day
> (`00302-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 08:10 · action · — — plan scaffolded

Plan 00302 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 09:05 · decision · — — failsafe recovery cron reused, nothing created

This session's existing failsafe recovery cron `2494b387` is reused for this
plan's execution — no new cron was created.

## 09:10 · finding · — — plugins/project_handlers absolute paths are a tested feature, not a bug

Attempted a hard `field_validator` on `PluginConfig.path`/`PluginsConfig.paths`
and `ProjectHandlersConfig.path` first. Running the full plugin/project-handler
suites surfaced ~36 failures, several explicitly named for the absolute case
(`tests/unit/test_plugin_loader.py::test_load_from_plugins_config_absolute_path_in_plugin`,
`tests/unit/daemon/test_controller_project_handlers.py::test_load_project_handlers_uses_absolute_path_as_is`).
Both surfaces load EXTERNAL code that may legitimately live outside the
repository — reverted the hard validators, documented both as exemptions in
the field description instead (matches the task's "if a surface legitimately
NEEDS an absolute path... document the exemption explicitly" instruction).

## 09:40 · action · — — shared validator extracted, hard-error + degrade surfaces done

`utils/repo_relative_path.normalise_repo_relative_path` created (pydantic-free,
TDD'd first). `config/models.py`'s `_repo_relative_path` now delegates to it.
Hard-error `field_validator`s added for `plan_workflow.directory`/
`workflow_docs` and `documentation.trees.agent`/`human`. Fail-open degrade
(catch `ValueError`, log, fall back to default) added for
`sensitive_content.secret_word_list_path`, `daemon.payload_capture.dir`, and
`model_fallback_detector.snapshot_dir` — matching each surface's existing
fail-open/advisory contract rather than raising.

## 10:00 · action · — — manifests + docs + full QA

Added `CLAUDE/UPGRADES/UNRELEASED/config-changes/v3.58.0.yaml` and
`truth-changes/v3.58.0.yaml` entries for every newly-rejected/degraded shape,
and a pointer paragraph in `docs/guides/CONFIGURATION.md`. Full affected test
suites green (1711 passed across `tests/unit/config/`, `tests/config/`,
`tests/unit/utils/`, `tests/unit/daemon/`, plugin/project-handler suites,
model_fallback_detector, sensitive_content). `mypy --strict`, `black -l 100`,
`ruff check` clean on every changed file.

## 10:05 · handoff · — — plan complete

`PLAN.md` filled in with the full audit table and marked Complete. Ready to
commit on this worktree's branch (not pushed).
