# Plan 00370 — Journal 26-09-10

> **Append-only activity log** for plan 00370. One file per day
> (`00370-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 11:31 · action · — — plan scaffolded

Plan 00370 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 11:52 · action · — — PLAN.md filled in, README row + stats added

Read `CLAUDE/PROJECT_HANDLERS.md`, the three existing project handlers
(`enforce_llm_qa`, `plan_done_requires_holding_area`, `release_blocker`) and
their co-located tests, `constants/handlers.py`'s `RETIRED_HANDLERS`
mechanism (Plan 00233 precedent — a retired config key is accepted silently
rather than hard-erroring), and the config-changes schema. Filled in
`PLAN.md` (Overview/Goals/Non-Goals/Tasks/Success Criteria, including the
holding-area criterion for `plan_done_requires_holding_area`). Added the
README index row under "Handler UX Adjustments".

Recounted plan stats from disk rather than incrementing blindly: Active
root folders = 18 (the 00370 scaffold itself, `find` regex needs to include
the three pre-padding `001-`/`002-`/`003-` names or it undercounts —
verified Completed = 329 the same way). `git config
hooksdaemon.latestPlanNumber` already reads 370 (mkplan.bash advanced it
when scaffolding this plan). `bin/hooks-daemon plan-qa` confirmed only two
findings before the fix: missing README row, Active 17→18 — both now
addressed. Updated Total 369→370, Active 17→18, folder reconciliation
17+329+13=359/356 distinct → 18+329+13=360/357 distinct, 356+13=369 →
357+13=370.

Precedent check for the Non-Goal on a versioned upgrade guide: grepped
historical `config-changes/*.yaml` for `removed:` entries with handler
keys. v2.11.0 and v3.58.0 removed handlers and are `breaking: true`;
v3.53.0 removed SIXTEEN handler keys in one release and stayed
`breaking: false`, because `RETIRED_HANDLERS` makes a leftover key a
silent accept rather than a validation error — matching what Task 2.6 will
prove for `daemon_restart_verifier`. Following the v3.53.0 precedent: no
versioned upgrade guide needed, `config-changes/v3.64.0.yaml` `removed:`
entry with `breaking:` left as whatever the other two in-flight plans
(00367, 00368) already set on that shared file (currently `false`).

Next: `plan-qa --check-staged`, commit `Plan 00370: filed`, push, then
create the worktree and start Phase 1 (project handler).

## 12:15 · action · P1 — worktree set up, project handler built (TDD)

Filed on main (85ed8bd0), pushed clean (no concurrent-push conflict this
time). `git worktree add` first, but `scripts/setup_worktree.sh` refuses an
already-existing directory, so removed it and the empty branch (identical
to main, no lost work) and re-ran the script properly for a fingerprint-keyed
venv + dev extras. Worktree daemon restarted, RUNNING.

Wrote the test file first (`test_daemon_restart_verifier.py`), then the
handler. First attempt used `ProjectContext.project_root()` in `__init__`
like the built-in did — broke every test with `RuntimeError: ProjectContext
not initialized`, because none of the three existing project handlers use
ProjectContext at all (it needs daemon-startup wiring the bare pytest run
doesn't have). Switched to `Path(__file__).resolve().parents[3]`: a project
handler's location relative to the project root is fixed by convention, so
this needs no fixture, no fork, no daemon state — cheaper than the git-remote
check it replaces. 97/97 project-handler tests green,
`validate-project-handlers` OK.

## 12:22 · action · P2 — built-in removed

Deleted source + test. Removed `HandlerID.DAEMON_RESTART_VERIFIER`,
`Priority.DAEMON_RESTART_VERIFIER`, the `HandlerKey` literal entry, the
`_STRICT_ONLY_HANDLERS` list entry. Added a `RETIRED_HANDLERS` entry
(`constants/handlers.py`) so a leftover client config key validates
silently instead of hard-erroring — this is the actual mechanism Plan
00233 built for exactly this situation, found by reading
`config/validator.py`'s unknown-handler branch rather than guessing.
Removed the shipped key from `.claude/hooks-daemon.yaml` (priority 23 in
THIS project's real config, not the default 10 — every project can
override), `.claude/hooks-daemon.yaml.example`, and `init_config.py`'s
scaffold text. Updated every doc mention (HANDLER_REFERENCE.md — two
places, LLM-INSTALL.md — two places, BASELINE.md, README.md) and the two
comments in `daemon/validation.py` that named the handler as the hot
per-event caller of `is_hooks_daemon_repo` (the memoisation rationale is
now honest: the sole remaining caller is `daemon/cli.py`'s one-shot
install-time check).

Updated four test files that carried the removed class name in
per-handler exemption/rationale maps
(test_blocking_handler_evasion.py, test_claude_md_guidance_coverage.py,
test_doc_truth_check.py, test_validation.py) — none of these actually
exercised the deleted class, they just documented it by name, so this was
straightforward.

Task 2.6 regression test added to test_handler_name_validation.py. Ran
the full unit suite: 17873 passed. Ran the full integration suite in the
background (>300s) — FIRST run surfaced a real bug: `check_repo_hygiene.py`'s
`orphaned-handler-guidance` rule flagged the new project handler's own
`get_claude_md()` content as orphaned. Root cause: the rule already looks
for project-handler modules (added when the OTHER three project handlers
were built), but only matches by exact module-STEM (snake_case filename).
Every project handler so far used `get_claude_md() -> None`, so this is the
FIRST case where a project handler's kebab-case `handler_id` (the marker
the injector actually writes) needed to resolve against its snake_case
filename — nobody had hit the mismatch before. Wrote a regression test
first (confirmed RED), then fixed `_known_handler_identities` to add the
hyphenated form of each project-handler module stem. Second integration
run: 3313 passed, 0 failed, 5 skipped (environment-only skips, e.g. no
built relay binary here).

Also hit and fixed a real priority collision: my handler's priority=10
(matching the old built-in) collided with `destructive_git` in THIS
project's actual config (many defaults share 10, but this project's real
`.claude/hooks-daemon.yaml` spreads the safety cluster across 10-23,
leaving nothing free below 24).
`test_project_handler_priority_collisions.py` caught it immediately —
moved to priority 24, the first free slot, verified by the same test
rather than picked by feel.

## 12:58 · action · P3 — upgrade path + release notes

This worktree branched from `main` before Plan 00367/00368 landed
`config-changes/v3.64.0.yaml` there, so the file did not exist yet in this
checkout — wrote it fresh with only my `removed:` entry, `breaking: false`
(matching the v3.53.0 precedent recorded in Task 2.6's decision above, not
the v2.11.0/v3.58.0 one), additive so it merges with whatever the other two
branches land in their own copies. `git mv`'d release-notes callout 22 to
its new filename and rewrote it for the removal + re-home. Added a
dogfooding pointer to `CLAUDE/PROJECT_HANDLERS.md`'s Overview (picked over
SELF_INSTALL.md: readers already there, building a project handler, are
the ones served by "see this repo's own examples").

Next: QA (`./scripts/qa/llm_qa.py all`, backgrounded — takes >590s),
`test-project-handlers --verbose`, worktree daemon restart + live commit
verification, then commit in units and push.

## 13:20 · finding · P4 — first QA run: 2 flaky smoke-test failures, not a regression

`llm_qa.py all` (backgrounded, ~590s): 24/26 tools passed. `format`: 1 file
(black auto-fixes by default, so this self-resolved — confirmed with
`black --check`, unchanged). `tests`: 21729 passed, 2 failed —
`test_daemon_smoke.py::test_daemon_processes_session_start_hook` and
`::test_daemon_handles_invalid_hook_input`. Read both: neither asserts
anything about handler count, handler names, or project handlers — they
spin up a throwaway daemon subprocess and assert the response to a hook
event is a dict. Ran the file alone: 10/10 passed. Reran the FULL test
suite via `run_tests.sh` standalone (not inside the full QA pipeline):
21731 passed, 0 failed, 493s. Reran the full `llm_qa.py all` a second
time: 26/26, 21731 passed, 0 failed. Conclusion: the two failures were
transient resource contention from the full pipeline's concurrent tool
load, not caused by this plan — confirmed by reproduction failing twice,
not asserted from a single clean rerun.

## 13:35 · action · P4 — verification, ticked, delivery commit

`test-project-handlers --verbose`: 97/97. Worktree daemon restarted
(PID 726429), confirmed RUNNING. Staged everything except the plan folder;
found and removed one stray untracked file
(`.claude/reports/v2.2.0-to-v2.15.2/ADVISORY.md`) that some test run had
generated as a side effect — not part of this plan's deliverable, deleted
before committing. Delivery commit `4c5bb582` made FROM the worktree; its
own PreToolUse hook printed the new PROJECT handler's advisory line — the
Task 4.3 / primary Success Criterion live-verified by the commit that
delivers it. PLAN.md ticked throughout, Status -> Complete, Delivery &
Milestones filled in.

## 13:40 · handoff · — ready for review/merge

All four phases complete, all Success Criteria checked, QA green (26/26,
twice). Two commits: `85ed8bd0` on `main` (filing) and `4c5bb582` on
`worktree-plan-00370` (implementation). Next: commit this JOURNAL +
PLAN.md update, push the branch, write the build report to
`subagent-reports/build-report.md`. Per the assigning instructions,
README archival/stats and the merge back to `main` are NOT done from this
worktree.
