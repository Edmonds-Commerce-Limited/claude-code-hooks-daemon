# Plan 00169 — Journal 26-07-16

> **Append-only activity log** for plan 00169. One file per day
> (`00169-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 12:13 · action · — — plan scaffolded

Plan 00169 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 12:15 · decision · P1 — research approach: 5-way parallel fan-out

User asked (pre-release) for online research + brainstorm on prior art / SOTA
for the kind of tooling this repo is (Claude Code hooks / agent guardrails /
AI-dev tooling) and novel feature ideas. Scoped as a research + ideation plan
(no code ships). Chose Sub-Agent Orchestration: five background research agents,
one per angle, each doing WebSearch/WebFetch and returning a sourced report:

- `research-hooks` — community Claude Code hooks prior art (awesome lists, repos, official hook spec surface)
- `research-clis` — adjacent agentic-CLI guardrails (Codex CLI, Gemini CLI, Aider, Cursor, Windsurf, Continue, Cline, opencode, AGENTS.md)
- `research-policy` — SOTA policy/guardrail engines + sandboxing + secret/supply-chain + LLM-I/O guardrails (OPA/Cedar, Landlock/seatbelt/bubblewrap, gitleaks, NeMo Guardrails, Presidio)
- `research-observability` — agent observability/telemetry/eval (OTEL GenAI, Claude Code native OTEL, LangSmith/Langfuse/Helicone, local usage dashboards, per-session metrics)
- `research-context` — context/compaction/memory + checkpoint/resume + multi-agent orchestration safety

## 12:16 · action · P1 — recovery cron armed + index wired

Non-durable hourly failsafe recovery cron armed (id `4c8c64ca`, `37 * * * *`).
README index row added under a new "Research / Roadmap" section; stats bumped
(Total 168→169, Active 24→25) — index-at-birth for the commit. Agents running
in background; will synthesise on return.

## 12:34 · finding · P1 — all five reports in; convergent signal

All five research agents returned sourced reports. Strong convergence across
independent angles:

- **Secrets/`.env` protection** is our #1 security gap (flagged by hooks + policy).
- **OS sandboxing** (bubblewrap/Seatbelt) is the leap from blocklist→deny-by-default;
  Anthropic cut permission prompts 84% with it. String matching can't see child
  processes (`npm install`→postinstall→`curl`).
- **Hook layer is a unique observability vantage** — we know *which* handler blocked
  and *why*; native Claude Code OTEL only records `source: hook`. Guardrail-block
  analytics + session scorecard = features only we can build well.
- **allow/ask/deny + glob precedence** is the universal permission vocabulary;
  allowlist-first beats denylist (Cursor's denylist shown routable).
- **Shadow-git per-turn checkpointing** (revert code, keep context) is our clearest
  capability gap vs Cline/Roo/pi-rewind.
- **Continuous stale-tool-result clearing** > late red-line `/compact`.
- We're **ahead of upstream on auto-resume**; native `StopFailure` is a cleaner
  primitive to rebuild the recovery cron on.

Newer hook-spec capabilities we underuse: `StopFailure`, `updatedInput`,
`updatedToolOutput`, `PostToolBatch`, `InstructionsLoaded`, `ConfigChange`,
`TaskCreated/Completed`, `TeammateIdle`.

## 12:40 · decision · P2/P3 — dossier delivered, plan complete

Synthesised into three tracked docs: `RESEARCH-FINDINGS.md` (deduped, sourced),
`GAP-ANALYSIS.md` (have/partial/missing vs our ~37+ handlers — ~30 misses
concentrated in secrets/sandboxing, observability, checkpoint/rewind, context
hygiene), `FEATURE-BACKLOG.md` (27 ranked briefs, novelty-marked). Recommended
first graduation wave: F1 secret-blocker+redaction, F2 guardrail-block analytics +
session scorecard (✦ unique to us), F3 StopFailure recovery, F4 updatedInput
auto-fix, F5 protect-tests/gaming detector; flagship bet F8 OS-sandbox mode (L).

Research + ideation only — nothing shipped. Each survivor graduates into its own
plan when the user picks. Plan marked Complete; keeping recovery cron (session
still live, release + follow-ups may follow).

## 12:40 · handoff · — — next entry point

Plan 00169 complete and archived to `Completed/`. Follow-up work = new plans per
graduated feature (see FEATURE-BACKLOG.md §Graduation). Pending release
(v3.43.0) is unblocked by this plan; user to decide which features to build
before/after release.
