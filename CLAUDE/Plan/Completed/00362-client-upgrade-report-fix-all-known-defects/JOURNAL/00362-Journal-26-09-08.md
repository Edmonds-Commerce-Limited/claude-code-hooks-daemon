# Plan 00362 — Journal 26-09-08

> **Append-only activity log** for plan 00362. One file per day
> (`00362-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 15:03 · action · — — plan scaffolded

Plan 00362 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 15:08 · finding · P1 — report imported, two findings verified

Failsafe recovery cron for this session: `4c23c3d0` (pre-existing, reused).
The owner's goal for this push: fix all known defects across all plans; the
next release is a stability release.

- The client report (`untracked/hooks-daemon-issues.md`, a PHP project's
  v3.41.0 → v3.62.1 upgrade) is imported as
  `REPORT-client-upgrade-v3.41-to-v3.62.1.md` with identifiers stripped.
- Verified: `gh release view` on v3.62.1 lists NO assets, so every skill
  wrapper's self-bootstrap (`health-check.sh`, `daemon-cli.sh`,
  `init-handlers.sh` all download `bootstrap-checksums.txt` from
  `releases/latest/download/`) 404s on every client.
- Verified: nothing under `config/` or the CLI warns on an unknown handler
  key for an event (the only "unknown handler" string is the CLI's
  explain-handler miss).
- Not reproduced: `SKILL.md` on main routes `config-validate`, not
  `validate-config`; the alias is still worth adding (Task 1.7) since the
  report saw the mismatch on a deployed copy.
- A separate agent is sweeping the 25 live plans for recorded defects; its
  ledger lands here as `DEFECT-LEDGER.md` and becomes Phase 2's task list.

## 16:40 · progress · Task 1.6 — nested mirror test roots (worktree fix-1-6)

- Reproduced with a unit test: `src/PackageType/OptimiseProbe.php` with a
  test at `tests/Small/PackageType/OptimiseProbeTest.php` was denied; the
  five candidates in the report's deny output are the only ones searched.
- `test_path_map` entries take `mirror: true` (default false; the flat
  contract is pinned byte-for-byte). The mirror strips the glob's LITERAL
  leading segments (`src` for `src/**`, `apps/app/src` for
  `apps/app/src/**`, nothing for `**/Rules/**`) from the workspace-relative
  source path and reproduces the rest under `test_dir`. A non-boolean
  `mirror` skips the entry with a warning, like every other malformed line.
- A nested literal `layout.test_dirs` entry (`tests/Small`; not a bare name,
  not a glob) is also a mirror root with no map entry, anchored on the
  segments before `src/` (or a bare declared `source_dirs` name) exactly as
  the built-in `tests/<mirror>` resolver is — so it needs no project root and
  zero-config candidates are unchanged (pinned). Order: declared map, then
  layout roots, then inference; every root is listed in a deny.
- Dead end avoided: `setup_worktree.sh` creates a NEW worktree, so inside an
  existing one the venv is built with the same `uv sync --frozen --extra dev`
  it runs, into `untracked/venv-wt/`. `--frozen` alone omits pytest.
- Docs: handler `get_claude_md`, `HANDLER_REFERENCE.md` (also drops a stale
  "or absolute" for `test_dir`), `LayoutConfig.test_dirs`,
  `WorkspaceResolution.md`; release-note callout 13 and an UNRELEASED
  config-changes manifest (`v3.63.0.yaml` — version is a guess for the
  release agent to confirm).
## 15:14 · finding · T1.1 — why v3.62.1 shipped with no assets

Every earlier release checked (v3.40.0, v3.55.0, v3.61.0, v3.62.0) carries
the five bootstrap assets; v3.62.1 alone has none. The manifest recipe
existed only as a prose loop in `CLAUDE/development/RELEASING.md` Step 14.
The procedure a release actually follows — `.claude/skills/release/invoke.sh`
Stage 4 and `.claude/agents/release-agent.md` Stage 3 — showed
`gh release create vX.Y.Z --title ... --notes-file ... --latest` with no
artifacts and no reference to the manifest at all, and nothing tested that
the two agreed. `scripts/release/build_bootstrap_checksums.sh` was already
tested (`tests/integration/test_build_bootstrap_checksums.py`) but nothing
invoked it from the followed procedure.

## 15:14 · action · T1.1 — pipeline step + local-copy fallback

- New `scripts/release/publish_bootstrap_assets.sh vX.Y.Z [--build-only]`:
  stages the four skill scripts from `src/.../skills/hooks-daemon/scripts/`
  in a fixed order, builds the manifest via `build_bootstrap_checksums.sh`,
  refuses to build if any script in the skill tree carries a
  `SELF-BOOTSTRAP BEGIN` stanza that is not in the bundle, runs
  `gh release upload <tag> ... --clobber`, then reads the release back and
  fails unless every asset is listed. `HOOKS_DAEMON_GH_BIN` and
  `HOOKS_DAEMON_RELEASE_ARTIFACTS_DIR` make it testable without GitHub.
- `tests/integration/test_publish_bootstrap_assets.py` pins the manifest
  coverage (by scanning the skill tree for the stanza), determinism,
  `--build-only` never calling gh, the upload/read-back contract, and that
  all three procedure documents invoke the script AFTER `gh release create`.
- RELEASING.md Step 14, `invoke.sh` Stage 4 and `release-agent.md` Stage 3
  now all run the script; the relay-asset block no longer offers a second
  `gh release create` form.
- The shared bootstrap stanza (identical in daemon-cli.sh, health-check.sh,
  init-handlers.sh, and their deployed `.claude/` copies) now falls back to
  the installed local copy on a manifest or fresh-script download failure:
  one `Warning:` line naming the URL and curl exit code, no cache marker
  written (so the next run retries). Checksum mismatch and a manifest with
  no entry still abort. `tests/acceptance/test_diagnostic_scripts.py`
  replaces the abort-pinning case with three fallback cases plus a
  stanza-parity check across the three scripts.
- Not done here by design: attaching assets to the real v3.62.1 release.
  The command for the owner/coordinator, from main after this branch merges:
  `scripts/release/publish_bootstrap_assets.sh v3.62.1`. Until that runs,
  the fallback in this branch is not on any client either — it ships with
  the next release.
## 15:30 · delivery · Task 2.8 — D14, R2 fixed; D19, D21 found already closed

Worktree branch `agent-a52ac373230130055-b55b4903`, code commit `144d8dbb`.

- D14: `RECOMMENDED_REFRESH_INTERVAL_S = 1` (public) in
  `suggest_statusline.py`; `install_version.sh`'s fallback heredoc gains
  `"refreshInterval": 1`. The bash heredoc cannot import the constant, so
  `test_install_version_fallback_settings.py` and `test_suggest_statusline.py`
  pin the heredoc, the repo's `.claude/settings.json` and the suggestion text
  to it.
- R2: `utils/temp_names.unique_temp_path(final_path)` — pid, thread ident,
  random token — replaces all nine hand-rolled `.{stem}.{pid}.tmp` names.
  `test_temp_names.py` greps `src/` for the old form and checks every listed
  writer imports the helper. One existing test patched
  `compaction_signal.os.replace`, which only ever worked by reaching the
  shared `os` module through the handler's import; it now patches
  `os.replace` directly.
- D19: `pyright .claude/ccy/claude-supervise.py` reports 0 errors —
  `resolved_stdin_fd: int` already exists at every deref site. No edit to the
  supervisor; ticked as already fixed.
- D21: the blank-`PYTHON_CMD` guard from Plan 00353 (`883990ea8`) already
  closes the `[Errno 13]` report. Added the happy-path test the task asked
  for: a resolvable interpreter yields "Daemon Status" and "Installed
  Handlers" with no `Errno` line.
- Worktree setup: `scripts/setup_worktree.sh` creates a NEW worktree, so
  inside an existing one the venv was built with the same `uv sync --frozen
  --extra dev` the helper runs, at `untracked/venv-worktree-py311`.
- Plans 00175 (1.1–1.3) and 00159 (1.1–1.3, 2.1) ticked with the hash;
  00159 Task 1.4 (daemon restart) left for the coordinator's merge.
## 15:30 · finding · T2.11 — hook contract re-audited against 2.1.263: no drifted claim

R1 measured. Upstream `hooks.md` moved from `d514bf57…` (2.1.252) to
`c30a50b8…`; the raw diff is four prose hunks (PermissionRequest's
`permission_suggestions` wording and `setMode` note, DirectoryAdded's
does-not-fire list). All 33 schemas UNCHANGED, no NEW-UPSTREAM event, both
contract QA checks clean with no stale allowlist entry. `META.json` now
records 2.1.263 with the hash and byte count computed from the capture;
`TestMetaProvenance` pins them. Worklist and per-event table in Plan 00327's
`DELTA-2.1.252-to-2.1.263.md`; Plan 00327 Phase 1 (and 2.1–2.3) ticked
there. No release-notes callout, because no claim changed. The capture was
taken with `remote-docs add --verbatim` and then kept untracked, as Plan
00327 Task 1.1 requires.

## 15:50 · action · P2 — every Phase 1 and Phase 2 branch merged on main

Merged in order after the earlier batch: 2.6 (`e8b4b62e`), 2.10, 1.2, 2.9;
callouts renumbered 24–26 (every agent picks 13). One content conflict on
the 1.2 merge (`config_migrations.py` imports from 1.2 and 2.2 — both
kept). The merged branches were re-tested on main, not trusted from their
worktrees: targeted suites green, `mypy --strict` clean, ruff clean; black
(the QA authority, which disagrees with ruff format) rewrote ten test files
from the agent branches and that was committed. Ten merged worktrees
removed; six older worktrees with unmerged commits from earlier plans
(00116 and others) were deliberately left alone. Daemon restarted on the
merged tree (PID 91957). Full `llm_qa.py all` run in progress.

## 15:52 · finding · T3.1 — client-mode smoke passes on all three points

Built with `scripts/dummy-client-repo.sh create` (production installer,
dedicated hostname), destroyed after.

- Bootstrap fallback: `health-check.sh` with `HOOKS_DAEMON_BOOTSTRAP_FORCE=1`
  and an unreachable `HOOKS_DAEMON_BOOTSTRAP_BASE_URL` prints one
  `Warning: failed to download bootstrap-checksums.txt … (curl exit 6) —
  continuing with the installed local copy` line and then reports
  `Daemon: RUNNING` (exit 0). Note the wrapper needs `HOSTNAME` to match the
  dummy's dedicated hostname or it reports the dogfood daemon's absence.
- `echd-capture` is deployed at `.claude/hooks-daemon/bin/echd-capture` and
  the client's pipe_blocker guidance names that path, not the bare name.
- TDD mirror root: with `test_path_map: [{source_glob: "src/**", test_dir:
  "tests/Small", mirror: true}]`, `config-validate` is clean; a real
  `PreToolUse` Write of `src/Foo/Bar.php` through the deployed
  `.claude/hooks/pre-tool-use` returns `{}` when `tests/Small/Foo/BarTest.php`
  exists and a deny naming `tests/Small/Foo/BazTest.php` when it does not.

## 16:20 · finding · P3 — first full QA on the merged tree: 21/26, seven causes

Each agent's branch was green in isolation; the whole-tree checks caught
what a branch cannot see. Fixed on main in one commit:

- `test_bash_write_blindness_coverage`: Task 2.4's rewrite of the
  sensitive_content guidance dropped the sentence naming the Bash file-write
  gap. Restored as "a Bash command that writes a FILE is NOT checked at write
  time; the commit is", which is now also the true statement.
- error_hiding: two exclusions drifted (Task 1.3 moved the pipe_blocker path
  lookup, so the finding's rule changed to `return-none-on-error`; Task 2.2
  shifted `upgrade_version.sh` by eight lines). Realigned, not widened.
- input_contract: `scratchpad_dir` (Task 1.4) is a payload field no vendored
  example carries; allowlisted with the reason.
- `test_dogfooding_hook_scripts`: Task 2.5 hand-edited the deployed
  `.claude/hooks/worktree-create` with header comments the generator does
  not emit. Regenerated from the installer path so the tracked file is the
  generated form.
- `test_deployed_skill_trees`: Task 1.2 edited the deployed
  `.claude/skills/hooks-daemon/upgrade.md`, not its source; source synced.
- magic_values: a bare `timeout=30` in Task 2.5's test.
- dependencies: the fingerprint venv wanted the editable package
  re-installed after the merges; `uv sync --frozen --all-extras` into it.

Still open: four acceptance upgrade tests fail because their fixture pins
the daemon clone to the newest tag (v3.62.1) and then runs HEAD's
`install_version.sh` against it, which now imports `deploy_echd_capture`
(Task 1.3) that the tag's src lacks. A real client never mixes an installer
and a src from different versions, so this is the fixture's design, not a
product defect; an agent is making the fixture faithful.

## 17:40 · handoff · P3 — full QA 26/26, plan closed

The acceptance fixture now installs the pinned clone with its own
`install_version.sh` (`830b8b9b`; a real client never mixes an installer
and a src from different tags). Second full QA run after the daemon
restart: 26/26, 20822 tests, coverage 95.3%. Closed on the strength of it:
Plans 00159, 00189, 00252, 00361 and this one, all archived with README
rows. Plan 00327 closed earlier today with the `contract-status` verb.

Left for the owner, none of it a defect:

- `release-slate-check` still says "a human decides": Plans 00135 and
  00330 are mid-work, Plan 00242 is high priority and not started. Every
  ledger item is fixed or ruled out, so the `accept-wip` decision at
  `/release` is the only thing between main and the stability release.
- Plan 00330 Phases 2–4 wait on the decisions its `SURFACE-INVENTORY.md`
  lists; the proposal on the table replaces the opt-out mechanism with a
  per-handler relevance predicate and treats "optimal" as every relevant
  handler enabled.
- The owner-decision list from the ledger sweep: cancel 00085, 00129,
  00204, 00280; close 00135 or keep; close 00160 citing 00166; ratchets
  00144/00163; rescopes 00170/00175/00242.
- `untracked/hooks-daemon-issues.md` is imported here and can be deleted.
