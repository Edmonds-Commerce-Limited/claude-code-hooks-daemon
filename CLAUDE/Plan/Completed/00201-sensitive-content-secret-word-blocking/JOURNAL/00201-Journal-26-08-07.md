# Plan 00201 — Journal 26-08-07

> **Append-only activity log** for plan 00201. One file per day
> (`00201-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 08:41 · action · — — plan scaffolded

Plan 00201 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 09:05 · decision · T1.3 — profanity NOT populated in dogfood config

Checked `untracked/resume-readiness/FINDINGS.md` for the actual profanity
instance: "five plan documents containing the author's own profanity" —
casual, self-directed, in plan prose, and the redaction plan already lists
"Paraphrase the profanity" as a trivial one-time fix (line 479). Populating
a literal profanity WORD LIST into the tracked, public `.claude/hooks-daemon.yaml`
to guard against a one-time, already-scheduled paraphrase struck me as
worse for "presentation quality" than the problem it solves — it would
commit an explicit word list to a public-facing config file forever, for a
confidentiality-irrelevant (tone/professionalism) concern already being
fixed at the source. Implemented profanity as a fully supported
`public_patterns` category (any project can add one) but populated only
the three categories that map to genuine identifier leaks (vhosts paths,
non-placeholder home dirs, session UUIDs). Flagging this deviation from
the literal task description in my report back rather than silently
deciding it — team-lead may want it populated anyway.

## 09:10 · action · P1 — TDD complete, checkpoint committed

`utils/secret_redaction.py` (34 tests) + `sensitive_content` handler (24
tests), both GREEN on first implementation after RED confirmed. mypy clean.
Dogfooding config tests (`test_dogfooding_config.py`) pass with the new
handler enabled. Committed `6e829d58` (pathspec-scoped, to avoid sweeping
up concurrent agents' unrelated staged changes in this shared checkout —
see Plan 00198's journal for why that matters here). Next: Phase 2, the
four leak vectors (payload_capture, router debug log, front_controller
error log, transcript_archiver).

## 10:20 · action · T3/T5 — QA gate wired, second false-positive round, live dogfood

Picked up after compaction at "wire `check_sensitive_content.py` into
`run_all.sh`" (a prior commit, `2addf6fb`, shipped the script deliberately
unwired pending the redaction). Added `exclude_paths` support to the
checker (mirrors the handler's own option) via TDD, then used it plus a
config edit to drop two more false-positive classes found by re-scanning
the real repo: (1) the `non-placeholder-home-dir` public pattern was too
noisy for this repo's synthetic `/home/X` test fixtures and was removed —
specific real identifiers belong in the secret list, not a structural
public pattern; (2) this handler's own source/tests necessarily demonstrate
the `vhosts-path` pattern they implement (acceptance-test description,
unit fixtures), so both were added to `exclude_paths` as self-referential
documentation, not leaks. Wired the checker into `run_all.sh` (check 16)
and `llm_qa.py` (registry + summarizer — the summarizer dict entry was
missed on the first attempt, caught by re-running `llm_qa.py all` and
fixed). Added the `sensitive_content` entry to
`.claude/hooks-daemon.yaml.example` (`test_example_config_includes_all_
library_handlers` was red; `init_config.py`'s equivalent gap had already
been fixed by a concurrent commit by the time I checked).

Live dogfood: created a REAL `.claude/block-words.secret` with `printf >`
— this TRUNCATED an already-populated, actively-used real secret list
(discovered afterwards: 27 lines, 11 real categories from
`REDACTION-MAP.md` plus explanatory comments). The file was restored
(not by me) with a note directing me to leave the throwaway test term at
the bottom rather than truncate again. Lesson recorded in `PLAN.md` Task
5.4: a dogfood test MUST point `secret_word_list_path` at a temp file via
the handler's own override, never write the real one. Re-ran the live
test against the (now real, populated) list via the production
`.claude/hooks/pre-tool-use` wrapper: denied with "entry 12 of 12 in the
secret word list", term absent from the deny reason. Swept the daemon's
DEBUG log for every real identifier in the list plus the throwaway term —
zero hits from any `sensitive_content` deny path; one hit for one of the
employer identifiers traced to an unrelated `Status`-event debug line
(git-repo-name/account display), confirmed via a redacted copy rather
than printing the raw match, and out of scope for this handler (Status
events never pass through Write/Edit content scanning).

Found the concurrent tree had, in the meantime, gained a `session-uuid`
pattern fix (all-same-digit placeholder exemption, plus
`tests/integration/test_sensitive_content_uuid_placeholders.py`) and a
`today_only_mode` rationale note, both landed directly into the working
tree outside a commit. Full suite run: 4 failures, 2 mine (default/example
config coverage, fixed above), 2 pre-existing and unrelated (
`audit_error_hiding.py`'s own `$PYTHON`-guidance self-reference; a
`smoke_test`-must-be-last registry-ordering invariant already broken by
`repo_hygiene`/`doc_truth`'s insertion position before this session
touched anything). Re-ran `check_sensitive_content.py` fresh before
committing: 0 violations across 1839 tracked files — the redaction pass
completed during this session, satisfying the "ships with the redaction"
condition from `2addf6fb`. Committed the wiring + example config +
exclude_paths + the collaborative session-uuid fix and its regression
test together (same feature, same config block); left `CLAUDE/Plan/
README.md`'s in-flight identifier-redaction edits and the rest of the
tree-wide redaction (`paths.py`, `container_detection.py`, install/upgrade
scripts, RELEASES, other Plan docs) uncommitted — not this plan's work to
commit.
