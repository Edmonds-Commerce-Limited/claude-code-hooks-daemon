# Plan 00274 — Journal 26-08-26

> **Append-only activity log** for plan 00274. One file per day
> (`00274-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 08:11 · action · — — plan scaffolded

Plan 00274 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 08:12 · decision · — — filed standalone despite dedupe-scout merge advice

User request: a mechanism to help projects notice when it is probably time to
create a skill (repeated workloads / recurring confusion in user prompts);
SessionStart-triggered with a ≥1-week TTL, Haiku-powered aggregation,
configurable on/off + cadence, manually runnable, dogfooded here.

Dedupe scout (run BEFORE filing) named Plan 00161 (idle housekeeping —
delivery vehicle, superset), Plan 00266 (AI-assisted handler decisions —
Dormant Haiku research awaiting a concrete use case) and Plan 00085 (reminder
pseudo-events), recommending a merge into 00161. Filed standalone instead:
the transcript-mining/clustering pipeline is a substantial deliverable of its
own; the plan depends on 00161 infrastructure and gives 00266 its concrete
use case. User is watching and can override.

Failsafe recovery cron: REUSED the session's existing recurring cron
`764ca903` (hourly at :23); no new cron created.

## 08:12 · action · — — brainstorm sub-agent dispatched

Sub-agent tasked with drafting `BRAINSTORM.md` and filling `PLAN.md`.

## 09:10 · action · — — prototype built and dogfood-tested

Prototype built in `prototype/` (`skill_scan.py` + colocated tests + README)
by a sub-agent that died on a session limit mid-task; main thread resumed,
verified and ran it. Results against this repo's transcripts:

- 25/25 prototype unit tests pass (venv python).
- Full scan: 7 transcript files, 160,495 lines, 20,829 `type: user` records
  → **142 genuine human prompts** after field + content-marker filters →
  130 clusters; Haiku payload 13,409 chars (well under the 50k cap).
- Dry-run payload inspected: normalised, redacted, no secret material.
- Haiku stage degraded gracefully: headless `claude -p` reports "Not logged
  in" inside this container (nested CLI invocation carries no auth), so the
  report shipped with the deterministic-clusters half plus the logged skip.
  FINDING for Task/design: the "reuse user auth via headless CLI" assumption
  (Decision 3) fails in containerised sessions — the real feature needs an
  auth-detection preflight and possibly an API-key fallback after all.
- Report at `untracked/reports/skill-opportunities-2026-08-26.md`.

Status flipped to In Progress (prototype work has genuinely begun).

## 2026-08-26 — Phase 1 closed; rollout authorised (worktree agent)

User authorised full rollout. Tasks 1.2–1.4 resolved and recorded as
Decisions 4–8 in PLAN.md:

- Clustering comparison run over the real corpus (143 prompts): token-set
  Jaccard vs character trigrams at thresholds 0.3–0.6 — structurally
  identical results at 0.4+; trigrams 4–5x slower with one looser merge at
  0.3. Chose token-set Jaccard, threshold 0.5.
- v1 is CLI-auth only (no API-key fallback); no-auth is a first-class
  fail-open path (in-container "Not logged in" observed in the dogfood run).
- Dated report files; workloads vs corrections report sections;
  cross-project out of scope.

Next: Phase 2 TDD port of the prototype into
`src/claude_code_hooks_daemon/skill_scan/`.

## 2026-08-26 — Phases 2-4 built and QA-green (worktree agent)

- Ported the prototype into `src/claude_code_hooks_daemon/skill_scan/`
  (constants, models, extraction, clustering, digest, invoker, state,
  report, pipeline) — TDD throughout, 92 package unit tests plus 7 CLI and
  13 handler tests.
- `bin/hooks-daemon skill-scan` CLI: TTL gate, --force, --window-days,
  --dry-run (privacy audit view), fail-open model stage, state recording
  (success vs attempt).
- `skill_opportunity_detector` SessionStart handler: version_check TTL
  pattern, advisory-only, broad fail-open; classified T4 in the guidance
  coverage suite (fire-time advisory carries the full remedy, matching the
  ContractStalenessHandler precedent) so it returns no resident guidance.
- Integration classifications: guidance-coverage exemption, dogfooding
  opt-in set, default-enabled template consistency
  (get_default_enabled → False + template line + expected-keys set),
  error-hiding exclusions (4 reasoned entries), bandit nosec on the one
  subprocess site.
- Config: example + init template + this repo's yaml stanza staged with
  enabled: false (dogfood flip is a main-thread act post-merge);
  HANDLER_REFERENCE entry; config-changes UNRELEASED manifest entry.
- Full QA in the worktree: 24/24 PASSED, 14,193 tests, coverage 95.1%;
  isolated worktree daemon started RUNNING (import verification) and
  stopped again.
- Client-mode verified: dummy-client-repo production install, then
  `cli skill-scan --dry-run` — clean empty-window run, exit 0. Fixture
  destroyed.
- Production extraction sanity-run over the real corpus: 8 files,
  160,835 lines, 20,869 user records → 144 genuine prompts → 131
  clusters (matches the prototype numbers).

Remaining (main thread, post-merge): enable the handler in
.claude/hooks-daemon.yaml, restart+verify the dogfood daemon, run
`skill-scan --force` in an authenticated environment and review the
report with the user (Task 5.2), then close the plan.

## 2026-08-26 — Implementation-review fixes applied (worktree agent)

Coordinator review verdict FIX FIRST; all five fixes applied:

1. Privacy: representatives are now passed through normalise() before
   redaction in BOTH build_digest and write_report — the docstring/header
   claims are now true. New tests assert an embedded path reaches neither
   the digest nor the report.
2. cli.py: HandlerID.SENSITIVE_CONTENT.config_key replaces the magic
   "sensitive_content" string.
3. cli.py: an empty transcript window now records an ATTEMPT (not a
   completed scan) and prints a warning naming the transcript directory
   read — a mistyped dir can no longer silence the advisory for the whole
   interval. CLI test updated to the new contract.
4. Priority band: documented advisory band widened 56-60 → 56-65 in root
   CLAUDE.md; PriorityRange.ADVISORY_MAX = 65 with a rationale comment.
   skill_opportunity_detector stays at 61.
5. test_report canary assertion now checks the full canary line text.

Full QA re-run: 24/24 PASSED (14,195 tests, coverage 95.1%); worktree
daemon started RUNNING for the smoke probes and stopped again.

---

## Decision 9 redesign and plan closure (main session)

The Task 5.2 dogfood run hit the headless-auth wall: `skill-scan --force`
completed but its model stage was skipped with "Not logged in" (`claude -p`
has no auth in this container). The user's directive was categorical: we are
already inside an authenticated Claude Code session with the Agent tool —
shelling out to a second model process is the wrong mechanism, and no
fallback path is to be kept.

Redesigned accordingly (Decision 9): deleted `skill_scan/invoker.py`
(`ClaudeCliInvoker`, `ModelInvoker`, `parse_model_output`), the
`options.model` config key and the model constants. `run_scan` now builds
the rubric prompt and `write_report` embeds it under `## Judging (subagent
task)` with a `## Findings` placeholder; the CLI prints the report path and
the dispatch instruction. Handler advisory rewritten lean (numbered steps,
no rationale prose — per user feedback that guidance prompts must not
journal design reasoning). HANDLER_REFERENCE.md updated; config-changes
UNRELEASED manifest records the removed `options.model` key. Tests rewritten
across pipeline/report/CLI/models/constants (101 skill-scan-adjacent tests
green); full QA 25/25; daemon restarted RUNNING; `skill-scan --force`
re-verified live end-to-end against real transcripts (139 genuine prompts).

Dogfood judging (an in-session subagent over the real digest): zero workload
candidates — every multi-session cluster is noise (the tool's own rubric
echoed into transcripts, "continue"-style filler, pasted console output);
one marginal correction ("finish work, don't defer") that overlaps the
active `nitpick.dismissive_language` handler; release/configure-covered
clusters correctly suppressed. Findings appended to the report's
`## Findings`; outcome: no new skill created. All tasks and success criteria
hold — plan flipped Complete and archived.
