# Plan 00360 — Journal 26-09-08

> **Append-only activity log** for plan 00360. One file per day
> (`00360-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 12:41 · action · — — plan scaffolded

Plan 00360 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 12:45 · decision · T1.3 — the gate is a PROJECT handler, not a daemon one

The owner asked that the rule bite, and that it be this project's only. The
holding-area layout (`CLAUDE/UPGRADES/UNRELEASED/` and its four shapes) is
this repository's convention; a client project names its own, so a daemon
handler would either hard-code our layout or need config nobody asked for.
`.claude/project-handlers/pre_tool_use/plan_done_requires_holding_area.py`
is co-located with its tests and never ships. Recovery cron reused: `4c23c3d0`.

## 12:48 · finding · T1.1–T1.2 — the closed plans did not meet the bar

Of the seven plans closed on the ruling today, only 00110 had a holding-area
artefact (its post-upgrade task). Five now have a `release-notes/` callout
written in the notes' voice; 00250 declares no release-bound consequences
(CI workflow and tests only). Each archived PLAN.md's Success Criteria cite
the artefact — a deliberate correction of archived plans, shipped at `4611d22d`.

## 12:52 · action · T1.3 — denied live

After a daemon restart the handler listed at priority 20, and a real Write
flipping a probe PLAN.md to Complete with no criterion was denied with the
guidance text. Project-handler suite green.

## 13:30 · action · P2 — the release consumes the area

RELEASING.md Step 5 folds every callout into the Highlights; Step 6 gains a
"Move UNRELEASED release-notes" block with the same ABORT shape as the
post-upgrade tasks; Step 7's checklist and the manual recipe name it; the
release agent and `invoke.sh` name it. `release-slate-check` lists callouts
by title under "This release will say" (JSON: `pending_release_notes`), with
no effect on the verdict — verified live against the six pending callouts.

## 13:31 · decision · T3.1 — pin the schema, not the emptiness

"Holds only its README after a release" is not a property main has between
releases, so the integration test reads the REAL holding area and fails CI
on a callout the release could not fold in (name, title, plan, audience,
body). The emptying is pinned by the Step 6 ABORT.

## 14:46 · action · — — closing: criteria ticked, own callout written

The three Success Criteria are ticked with their evidence (four plans have
already closed through the holding area today; Step 5/6 fold-and-move with
ABORT; `SlateReport.pending_release_notes` informational). This plan's own
consequence, the slate check listing, is
`UNRELEASED/release-notes/12-release-slate-lists-pending-notes.md`. Task 3.2
waits on the full QA run of the merged tree; the daemon was restarted after
the last merge and reports RUNNING.
