# Plan 00211 — Journal 26-08-12

> **Append-only activity log** for plan 00211. One file per day
> (`00211-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 10:23 · action · — — plan scaffolded

Plan 00211 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 11:24 · handoff · P5 — implementation complete, QA at 18/20, post-merge verification outstanding

All five phases implemented and committed (9 commits total, see git log
for hashes): `plan_qa/remedy.py` single source of truth (EXTRACT listed
first), folder-shape hint on `plan-doc-size`, `has_staged_supporting_doc`
teaching `plan-shrink-without-journal` about extraction,
`install/templates/PlanJournalling.md` + `CLAUDE/PlanJournalling.md`
synced byte-identically with the three-remedy world and supporting-docs
layout, and a DBF doc-parity regression test
(`tests/unit/install/test_plan_journalling_doc_parity.py`) — confirmed it
fails against the pre-fix template via `git show` on the introducing
commit before writing the fix.

`./scripts/qa/llm_qa.py all` reaches 18/20 PASSED. Two real issues this
surfaced that WERE mine got fixed: 3 files needing `black` (2 mine, 1
unrelated pre-existing drift in `test_extractor.py`), and this plan's own
README index row breaking `test_no_index_row_is_a_paragraph` (589 chars,
over the 500 limit — shortened to a link + status + one clause).

Remaining 2/20 failures are OUT OF SCOPE: the 5 failing tests all trace to
Plan 00208's `CommentChangelogHandler`/`CommentSizeHandler` work already
on the base branch before this plan started (`git merge-base
--is-ancestor 52a5901b HEAD` confirms it predates my first commit).
`smoke_test` needs a live daemon socket, which cannot exist for a
worktree project root.

**Environment note for future worktree sessions**: the worktree's own
`untracked/` has no venv. Two gitignored symlinks were created pointing
at the shared `/workspace/untracked/venv-workspace-py311-81c29529`: a
legacy `untracked/venv` (matches `llm_qa.py`'s hardcoded `VENV_PYTHON`
constant) and a glob-matching `untracked/venv-worktree-py311-shared`
(matches `resolve_venv_python`'s `venv-*` glob, used internally by
`run_canonical_callers_check.sh` and friends). Without the second one,
`canonical_callers` silently produces no output rather than failing
loudly — worth a look as its own small bug if it recurs for other agents.
pytest/QA invocations also need `PYTHONPATH=<worktree>/src` prepended so
the venv's editable install (which points at `/workspace/src`) doesn't
shadow the worktree's own source.

**OUTSTANDING for the post-merge tree** (cannot be done from a worktree,
per task constraints — not attempted, not faked):
- `./bin/hooks-daemon restart` + `./bin/hooks-daemon status` showing
  RUNNING against the real project root
- A live `plan-qa --lint <PLAN.md>` / daemon-injected CLAUDE.md probe
  confirming the regenerated guidance actually reads as intended in a
  real session

Plan Status left as **In Progress** (not Complete) until those two are
done — CLAUDE.md's daemon-restart rule is non-negotiable and this plan
cannot self-certify it from here.

## Closing entry — merged and verified against the real project root

The worktree was right to refuse to self-certify, and both outstanding
items are now discharged in the tree where they were always going to be
answerable.

Merged to `main` with `--no-ff` (no squash — this project treats severed
ancestry as a safety problem, not a tidiness preference). The merge hit
one conflict, in the plan-index statistics, and it was worth the trouble:
both sides of the conflict were wrong in the SAME way. Each itemised
00038 as a deleted draft whose folder is in fact present, and neither
accounted for six other absent numbers or for plans 1–3 existing under
pre-zero-padding `001-`/`002-`/`003-` names. Recounted from disk rather
than by picking a side, and the figures now reconcile exactly.

Verification performed post-merge:

- `./bin/hooks-daemon restart` → `RUNNING`, no load errors in the log.
- Live `PreToolUse`/`Write` probe of an over-threshold `PLAN.md` returned
  the three-remedy text with **EXTRACT listed first**, followed by the
  folder-shape hint and its "suggestion based on folder shape, not a
  diagnosis" caveat — the honest framing the field report asked for,
  intact in the deployed guidance rather than only in the source.
- `plan-qa --sweep` → no structural drift.
- `./scripts/qa/llm_qa.py all` → **20/20**, so the two checks the worktree
  could not satisfy were environmental exactly as it argued.

Also staged a `truth-changes` manifest for the next release: the
"exactly two remedies" claim is now false, and client projects are likely
to have repeated it in their own docs. Without that entry the fix would
have shipped silently and left every downstream copy of the old claim
uncorrected.

Status flipped to Complete and the folder moved to `Completed/` in one
commit alongside the index row and statistics recount, per
`terminal-state-atomic`.
