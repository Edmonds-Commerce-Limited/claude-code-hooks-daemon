# Plan 00186 — Journal 26-07-21

> **Append-only activity log** for plan 00186. One file per day
> (`00186-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 10:27 · action · — — plan scaffolded

Plan 00186 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 10:40 · action · T1/T2/T3 — all three review follow-ups implemented

- **F1 (atomic write)** — RED tests (no temp residue on success; original file
  byte-intact if `os.replace` fails) → GREEN: stage merged JSON in a sibling
  `.tmp.registration-repair`, then `os.replace`. One-shot backup unchanged.
- **F2 (drift guard)** — 3 new asserts in `test_settings_sources_ssot_drift.py`:
  install.py `_HOOKS_WITH_TIMEOUT` == `_BASH_KEYS_WITH_TIMEOUT`, inline timeout
  literal == `_DEFAULT_HOOK_TIMEOUT_SECONDS`, `_hook_cmd` template ==
  `_HOOK_COMMAND_TEMPLATE`. All agree today; future divergence now fails.
- **F3 (double-reconcile)** — documented the intentional reuse of the fail-safe
  writer in `cmd_reconcile_settings` (backup/atomic/malformed guards in ONE place).

## 10:44 · finding · T1.2 — first F1 write tripped error_hiding (log-and-continue)

The initial F1 patch added a nested `except OSError: logger.debug(...)` to clean
up the temp on a failed replace → QA `error_hiding` flagged it (`log-and-continue`,
`settings_repair.py:109`). Fix: match the house pattern in `utils/retention.py`
(atomic trim) — do NOT clean up the temp on failure (harmless; the next repair
overwrites it before its own replace); the outer `except` just logs+returns.
Relaxed the failure-case test to assert only the critical guarantee (live file
byte-intact) and tolerate the leftover temp. QA re-run 13/13.

## 10:46 · handoff · — — Plan 00186 complete; closing atomically

All 3 findings fixed + verified. Full `llm_qa.py all` 13/13 (10520 tests, 95.2%
cov), daemon RUNNING. Closing now (status → Complete, `git mv` to Completed/,
README move + stats) then push. This closes the v3.48.0 review loop per
RELEASING.md "Never drop a finding". Recovery cron 88ad018c kept running.
