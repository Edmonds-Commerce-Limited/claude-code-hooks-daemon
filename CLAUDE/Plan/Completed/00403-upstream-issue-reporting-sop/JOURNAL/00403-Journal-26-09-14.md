# Plan 00403 — Journal 26-09-14

> **Append-only activity log** for plan 00403. One file per day
> (`00403-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 15:27 · action · — — plan scaffolded

Plan 00403 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 18:50 · handoff · — — closed, after re-verifying rather than trusting the ticks

Status flipped to Complete and the folder archived. Full QA 30/30 at
`786f4dc5`; CI green on `e40f91a4` and `38c97c45`.

Five of the six success criteria were ticked BEFORE the last two changes
landed — `--web`, and the correction to what the acknowledgement claim
promises. Ticks written before a change are not evidence about the state after
it, so the load-bearing one was re-run rather than re-read: the generator was
invoked end to end, and the document it produced was ALLOWED by the gate,
DENIED once a log line was appended, and ALLOWED again when restored
byte-for-byte. The printed filing command still carries `--repo` with its
explanation, which was the first of this plan's four found-by-using-it defects.

Two things were added at closing time rather than at authoring time, both
because the work moved past the document:

- `--web` as a deliberate hole, recorded in `DECISIONS.md` as the sixth
  decision. A gate whose only escape is evasion teaches evasion.
- `FOUND-BY-USING-IT.md`, holding the four defects that only running the thing
  exposed, because their shared cause is reusable: the generator is correct in
  the environment it is DEVELOPED in and wrong in the one it SHIPS to, and
  every test runs in the developing one.

The closing check that mattered most was the cheapest. Archiving moved the
folder and `docs_qa` immediately reported two dead relative links in Plan
00405 pointing at the old location — drift created by the archival itself, in
the same commit that created it. Worth noting that `00404`'s link to this plan
needed NO change, because both now sit in `Completed/`; editing it anyway
would have rewritten an archived plan for nothing, and the archive-immutability
advisory said so.
