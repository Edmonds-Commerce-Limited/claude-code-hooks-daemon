# Plan 00394 — Journal 26-09-24

_Scaffolded by `mkplan.bash`; timestamps in this file are UTC._

> **Append-only activity log** for plan 00394. One file per day
> (`00394-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM, ONLY with
> `mkplan.bash --journal <plan-number> <category> <body-file>` (it stamps the
> real UTC time; a hand-written entry is denied); NEVER edit earlier entries
> (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` UTC 24h (the date is in the filename, also UTC); times increase
>   down the file. A file with no sentinel line above predates this system —
>   its times are local, zone unrecorded (legacy; never rewritten).
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 18:45 · decision · —

Ruled options 1 and 2 (unattended, coordinator brief) and built on branch worktree-d-cron.

Declaring the failsafe cron (option 1) makes `cron_stop_enforcer` require it. The
completion advice and the canonical prompt both say to CronDelete it once the
session is finished. Left alone, the two would have fought at every clean session
end, exactly as `fable-blast-radius-policy-decision.md` predicted. Where the
failsafe cron is declared, the completion advice now says to keep it.

The declared schedule is `47 * * * *` because the enforcer compares schedules
exactly and this session's live failsafe cron runs at :47. Any other minute would
have demanded a second failsafe cron in this very session after the merge.

The live failsafe cron was created from the pre-sentinel prompt. It still
satisfies the declaration because `cron_enforcement` strips sentinels before
comparing, which a test pins.
