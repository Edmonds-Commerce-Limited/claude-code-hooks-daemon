# Plan 00365 — Journal 26-09-09

> **Append-only activity log** for plan 00365. One file per day
> (`00365-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 16:36 · action · — — plan scaffolded

Plan 00365 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 16:40 · decision · — — filed from a field report; the declaration is the fix

Report imported to `FIELD-REPORT.md` with the reporter's org, package and
repository names replaced; source deleted from `untracked/`. Dedupe scout
checked 18 live plans: none covers this.

Reproduced on main before filing (handler instantiated against this repo's
own config):

```
vendor/org-a/pkg-a/vendor/org-b/pkg-b/docs/upgrading.md -> strip: vendor/org-b/pkg-b/docs/upgrading.md  blocked: True
vendor/org-b/pkg-b/docs/upgrading.md                     -> strip: docs/upgrading.md                     blocked: False
with extra_allowed_markdown_paths ['^vendor/org-a/pkg-a/vendor/org-b/pkg-b/']:  blocked: True
```

Owner's ruling, given while the plan was being filed: the correct solution
is to define the project properly in the top-level config with the directory
set to whatever it is — vendored, vendored-inside-vendored — and have that
declaration work. Task 1.2 is therefore the primary fix and the deny message
must name `projects:`.

Failsafe recovery cron for this session: `8cc209e9` (shared with Plan 00364).

## 17:20 · action · P1 — fixed with TDD; live-probed through the worktree's own hook wrapper

Eleven regression tests appended to `test_markdown_organization.py`
(`TestNestedDependencyTreesAndDeclaredProjectsAtAnyDepth`): 8 RED before the
change, 3 pinning behaviour that must survive it (a new misplaced file inside
a nested vendor tree stays blocked; a package-relative extra pattern still
matches). All 212 handler tests green after; 3,435 across the PreToolUse
suites plus the acceptance and guidance contract tests.

The fix, in `matches()`: the existing-file exemption first
(`path_is_file(..., unreadable_means=False)` — an unstattable path is still
judged); then the DECLARED project, looked up by the repository-relative,
case-preserved path rather than the marker-stripped one (`normalize_path`
turns `vendor/org/pkg/tools/qa/docs/guide.md` into `docs/guide.md`, which can
never sit under a declared root — that was a second, hidden reason the
declaration had no effect); then the dependency inference, which now recurses
through nested `vendor/`/`node_modules/`; and `extra_allowed_markdown_paths`
tried against both spellings. The rule's `fix` line names `projects:`.

Live probe through the worktree's deployed `.claude/hooks/pre-tool-use`
against the worktree's own daemon (main's daemon does not have this code):

```
Edit  <worktree>/stray-existing-00365.md (exists)  -> allow
Write <worktree>/stray-new-00365.md      (new)     -> deny, reason names `projects:`
```

The allow proves the answer came from THIS checkout's daemon — Plan 00364
Task 5.1's fix, observed end to end without any environment override.

Also on this branch: `scripts/setup_worktree.sh` step 5 asked a `pip` that a
uv-managed venv does not ship, inside a `$(...)` assignment under `set -e`,
so every run aborted silently after venv creation and Plan 00364's step 9
(`hooks-daemon.env` provisioning) never executed. It now asks the interpreter
for the editable root.
