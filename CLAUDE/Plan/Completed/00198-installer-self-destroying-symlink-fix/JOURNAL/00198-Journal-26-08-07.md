# Plan 00198 — Journal 26-08-07

> **Append-only activity log** for plan 00198. One file per day
> (`00198-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 08:20 · action · — — plan scaffolded

Plan 00198 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 08:20 · finding · — — root cause traced beyond the reported scope

Team-lead's brief scoped the fix to the deprecated `install.py`. While
tracing "whatever creates `.claude/init.sh`" I found `install_version.sh`
and `upgrade_version.sh` BOTH refuse to run in self-install mode at all
(`mode_guard.sh::ensure_normal_mode_only`) — so the deprecated `install.py`
is the ONLY thing that ever performed this repo's self-install. But
`scripts/install/slash_commands.sh` (current, non-deprecated,
production-shipped) has the IDENTICAL `source == dest` collapse in its own
self-install branches (`deploy_slash_commands`, `deploy_single_slash_command`)
— unreachable today only because both real orchestrators hardcode `"normal"`,
never `"self-install"`. Decision: fix it too (team-lead's item 5 explicitly
authorised "the same hazard... in any other install.py deploy helper" and
this is the same hazard, live, in tested/documented library code) rather
than leave a second copy of the exact same landmine.

## 08:20 · action · T1-T2 — TDD both fixes

RED: `tests/integration/test_repo_symlink_hygiene.py` (git-tracked-symlink
scan) failed 3/4 against the two live tracked symlinks — had to redesign
around `Path.resolve()` raising `RuntimeError` on a genuine self-loop
(ELOOP) rather than failing cleanly; switched to pure `os.path.normpath`
arithmetic against the git blob content (never touches the filesystem via
resolve/realpath), and confirmed `.exists()` safely swallows ELOOP.
`tests/unit/install/test_slash_commands_self_install_safety.py` (sources
the real bash lib via subprocess) failed 4/4 against the live shell bug —
same OSError/ELOOP shape, same fix pattern (wrap the read in try/except and
assert on it).

GREEN: deleted `.claude/commands/hooks-daemon-update.md` (git rm) + removed
`copy_slash_commands()` + call site from `install.py`; fixed
`copy_init_script()` to `os.path.relpath()` the symlink target; re-created
the tracked `.claude/init.sh` as `../init.sh` (verified resolves).  Added a
bash `-ef` (same device+inode) guard to both `slash_commands.sh` functions,
mirroring the pattern already proven in `deploy_init_script` — in
self-install mode the guard now skips the destructive unlink+relink
entirely since source and target are the same file already.

Full suite: 10,838 tests passed (0 failed). QA `llm_qa.py all`: 14/14
(2 files auto-fixed by black on first run, clean on re-run). Daemon
restarted -> RUNNING, no import errors, no code touched handlers so no
protection regression risk. Next: client-mode verification via
`scripts/dummy-client-repo.sh` (uses a `git worktree add --detach HEAD`,
so must run AFTER committing).
