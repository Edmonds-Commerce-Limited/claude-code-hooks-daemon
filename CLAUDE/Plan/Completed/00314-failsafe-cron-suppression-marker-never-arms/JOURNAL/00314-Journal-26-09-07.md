# Plan 00314 — Journal 26-09-07

> **Append-only activity log** for plan 00314. One file per day
> (`00314-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 16:05 · finding · T1.4 — the live evidence this plan was waiting for

Task 1.4 needed a real stop to arm the marker, and the field this plan added
is what makes the answer readable five days later:

```
$ grep -o 'marker_written[^,}]*' untracked/stop-events.jsonl | sort | uniq -c
     16 marker_written": true
```

Sixteen arms, all successful. That is Task 1.4's first half satisfied by
observation, and it also settles the plan's original mystery in the only way
available: the 2026-09-01/02 non-arm has never recurred, and if it does, the
record will now say so instead of vanishing with the log ring.

## 16:09 · finding · T1.4 — the suppression half is unit-pinned, not observed

Being exact about what the evidence covers. Nothing on disk shows a delivered
cron tick DENIED by `R-FAILSAFE-CRON-SUPPRESSED`: the daemon's log ring is
in-memory and today's restarts cleared it, and `stop-events.jsonl` records
Stop events, not UserPromptSubmit verdicts. So that half rests on
`tests/unit/handlers/user_prompt_submit/test_failsafe_cron_blockage_suppressor.py`
— 22 tests, including `test_valid_marker_for_same_session_suppresses` and
`test_cron_prompt_with_valid_marker_is_still_denied`.

Recorded rather than papered over. The success criterion says "observed live",
and half of it was not, so it is ticked with the distinction written next to
it. A criterion quietly ticked on weaker evidence than it asks for is worse
than one ticked with its limit stated.

## 16:14 · action · P1 — closed out late, by Plan 00337

The work shipped on 2026-09-02 in `923fd583`; this document still read
`Status: Not Started` with all four boxes `⬜`. The executor journalled the
commit and never flipped them.

Verified against the code before flipping anything rather than trusting the
commit message: `_HUMAN_BLOCKED_PATTERNS` pattern 4 does carry `human`
alongside `owner|user`, and `marker_written` is written by the Stop handler.
Both present, so Tasks 1.1–1.3 are genuinely done.

Nothing further is queued here. Plan 00337 Task 3.4 supersedes the remaining
pattern-widening idea: the mechanism moves to an explicit declaration and the
regexes stay only as a compatibility fallback.
