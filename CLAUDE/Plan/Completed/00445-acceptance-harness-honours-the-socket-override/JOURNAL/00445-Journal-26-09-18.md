# Plan 00445 — Journal 26-09-18

_Scaffolded by `mkplan.bash`; timestamps in this file are UTC._

> **Append-only activity log** for plan 00445. One file per day
> (`00445-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` UTC 24h (the date is in the filename, also UTC); times increase
>   down the file. A file with no sentinel line above predates this system —
>   its times are local, zone unrecorded (legacy; never rewritten).
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 04:31 · action · — — plan scaffolded

Plan 00445 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 04:35 · action · — — failsafe cron reused, not created

`CronList` showed exactly one recurring failsafe recovery job, `f5473232`
(hourly at :47). Reusing it; nothing created. One per SESSION, never one per
plan — a second would only double-fire on the same session.

## 04:36 · finding · — — the dedupe check, and its count

The scout reported `Checked 29 live plans.` against the 29 I stated from my own
disk recount, plus 392 archived. It named no plan covering this remedy — only
ledger 00422's N6 entry, which is where the work comes from, and three
completed plans adjacent to it: 00431 and 00443 (both fault 1) and 00243 (the
harness's execution mechanics). The count reconciling is the part that makes
the verdict usable; an unreconciled one is what Plan 00434 exists for.

## 04:38 · finding · P1 — the measurement this plan exists to invert

| Configuration                                                     | Result                         |
| ----------------------------------------------------------------- | ------------------------------ |
| Worktree, natural (fitting) socket path, no override              | 5 passed in 13.91s             |
| Same worktree, `CLAUDE_HOOKS_SOCKET_PATH` at another fitting path | hangs — four dots, then killed |

The first row is the one that settles it: a worktree is not the variable, so
N6's heading ("stalls in a worktree anyway") was wrong. The override is.

Controls run before accepting the explanation, because two earlier hypotheses
were wrong first — a PID-path mismatch (disproved: the pidfile held the running
daemon's pid), then a per-dispatch stall (disproved by the first control):

- All 224 probes dispatched from a plain script under the same override, with
  `build_event` and the fixture actions replicated: none slower than a second.
- One wrapper dispatch from the shell under the override: 0.2s.
- A throwaway test under `tests/acceptance/`, with the variable exported in the
  calling shell: `IN_PROCESS=[<unset>] SUBPROC_SEES=[<unset>]`.

The third is the defect in one line. It is also why Task 1.1 asks for a RED
observation rather than a test written alongside the fix: this guard is about
an environment, which is exactly where a test passes by luck.

## 04:39 · handoff · P1 — next entry point

PLAN.md is filled in and the README row is pending. Next action is Task 1.1:
write the RED test and WATCH it fail before touching `conftest.py`.
