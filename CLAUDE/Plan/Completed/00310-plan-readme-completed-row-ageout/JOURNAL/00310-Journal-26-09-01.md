# Plan 00310 — Journal 26-09-01

> **Append-only activity log** for plan 00310. One file per day
> (`00310-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 18:34 · action · — — plan scaffolded

Plan 00310 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 18:35 · action · — — plan filed from owner ruling after two ceiling breaches

- README breached its 130KB ceiling twice today; measurement: Completed
  section ~107KB of ~130KB (253 completed rows, accumulating forever),
  Active ~18KB. Owner ruling: done-and-old rows age out of the main README.
  Plan proposes a retention window (most recent ~30 completed rows) with a
  verbatim archive index at `Completed/README.md`, an enforcement test that
  fires on the cause (row count) not the symptom (bytes), and a one-time
  backlog migration.
- Session failsafe recovery cron: reusing the single session cron id
  `892eef87`; nothing new created.

## 19:10 · action · P1/P2 — rule settled, enforced, documented, backlog migrated

- Settled retention rule: main README keeps the 30 highest-numbered
  completed rows (numeric, not document-position — the doc is roughly but
  not strictly chronological). Archive: `CLAUDE/Plan/Completed/README.md`,
  same row shape, one `## Completed Plans (Archive)` heading, links with
  the `Completed/` prefix stripped (archive already lives inside
  `Completed/`). Main README's Completed header links to the archive.
- Enforcement: added
  `test_completed_rows_stays_within_the_retention_window` to
  `tests/integration/test_plan_index_navigability.py`, counting `- [` rows
  in the Completed Plans section, failing above 30 with a message naming
  the archive path and the checklist step. Existing byte-ceiling test kept
  unchanged.
- Docs: added step 5 to the Plan Completion Checklist in
  `CLAUDE/PlanWorkflow.md` (checklist steps renumbered 5-7); added the same
  rule to `CLAUDE/Plan/CLAUDE.md` local conventions.
- Migration: split all 253 completed rows by plan number — kept 271-306
  (30 rows, ~34KB) in the main README, moved 223 rows (numbers 1-270,
  including multi-line sub-bullet rows moved whole) verbatim into the new
  archive file (~97KB). Cancelled section and Plan Statistics untouched.
- Verification: `pytest tests/integration/test_plan_index_navigability.py`
  — 4 passed (row count exactly 30, at the ceiling by design). Main
  README size dropped ~130,420 → ~33,700 bytes.

## 19:35 · finding · P2 — row-folder-bijection needed a fix, not a workaround

- `plan-qa --sweep` initially reported 223 `row-folder-bijection` findings:
  every migrated plan's folder read as unindexed because that check only
  parsed the main README. Fixed in `src/.../plan_qa/context.py`: the archive
  index is now merged into the same `ReadmeIndex` (rows only — stats/`lines`
  stay sourced from the primary file), with archive row links rewritten to
  be relative to `plan_dir` so the link-resolution check still finds the
  real folder. Unit tests added in `tests/unit/plan_qa/test_context.py`.
  Daemon restarted afterward to pick up the source change; `plan-qa --sweep`
  confirmed clean post-restart (0 findings).
- Archived Plan 00310 itself per the checklist: added its own row, then
  aged out plan 00271 (now the lowest-numbered of the 31-row window) to the
  archive to hold the retention window at 30. Stats updated to match the
  real folder move (Active 41→40, Completed 253→254).
- Delivered at `fddd6890`. This is the handoff entry: plan complete, no
  further work pending.
