# Plan 00294 — Journal 26-08-30

> **Append-only activity log** for plan 00294. One file per day
> (`00294-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 20:49 · action · — — plan scaffolded

Plan 00294 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 21:00 · action · P1 — Phases 1–2 started; status flipped to In Progress

Sub-agent picked up Phases 1 and 2 (toggle command + integration/safety
proofs). Phase 3 (re-enable + canary) stays with the coordinator. Context
absorbed: forwarder_generator strip-then-reapply transform, socket-stdin
probe manner (`tests/integration/test_forwarder_socket_stdin.py`), the
daemon binding per-event sockets only when a transport rung is enabled
(`server._bind_event_sockets`), and `get_untracked_dir`'s
self-install-vs-client resolution. Design decided: new
`install/transport_toggle.py` (config flip via targeted comment-preserving
line edit + orchestration + state file in the daemon untracked dir) and
`install/transport_verify.py` (socket-stdin probes: pre-tool-use JSON,
status-line raw, stop hard-block exit-2, listener count), wired as a
`transport on|off|status` CLI subcommand; acceptance fixture is an
isolated fake-client project at a short /tmp path with its own
`.claude/hooks-daemon/untracked` so probes can never touch this repo's
live daemon.

## 21:20 · action · P1 — Phase 1 complete at unit level (Tasks 1.1–1.3)

New modules `install/transport_verify.py` (probes: forwarder-guard-state,
listener-count / no-event-listeners, pre-tool-use-json, status-line-raw,
stop-hard-block — all socket-stdin, real payload shapes) and
`install/transport_toggle.py` (targeted comment-preserving `relay_enabled:`
line flip, orchestration with injectable restart/verify, auto-revert
verified by the same probes, state file
`transport-toggle-state.json` in the daemon untracked dir), plus
`cmd_transport` + `transport on|off|status` parser in `daemon/cli.py`.
52 new unit tests green (21 verify + 24 toggle + 7 CLI); mypy --strict,
black, ruff clean on all touched files. Notable decisions: the off-state
listener probe checks for LIVE accepting listeners (a stale socket file
without a listener is harmless and must not fail the revert); the config
flip refuses to act unless exactly one `relay_enabled:` line exists.
Full `tests/unit/install tests/unit/daemon` run: 2 failures both in
`test_default_enabled_template_consistency` naming `tool_disable_advisor`
— another agent's uncommitted Plan 00293 work in this shared tree, not
mine (verified `git diff` of cli.py holds only the transport hunks).
Failsafe recovery cron d7de5dc7 created (none was listed).

## 21:45 · action · P2 — Phase 2 complete: acceptance proofs + docs

`tests/acceptance/test_transport_toggle_cycle.py` (8 tests, ~9s): isolated
client-layout fixture at a short /tmp path (own config with a comment
sentinel, copies of this repo's deployed forwarders + init.sh, its own
`.claude/hooks-daemon/untracked`, real relay binary copied in, real daemon
started from the repo venv interpreter) — proves on→verify→off→verify,
both idempotent no-ops, comment preservation through the real CLI, and the
induced-failure case: a broken relay binary (prints garbage, exit 0, never
falls back) fails the pre-tool-use-json probe, `transport on` exits
non-zero naming it, and the off state is restored end-to-end with the
revert re-verified (state file: verified=false, reverted=true,
revert_verified=true). Fix found en route: the default restart_fn captured
the restart CLI's output — the daemonised child inherits that pipe, so
run() would wait on the DAEMON (the daemon-smoke suite's documented trap);
switched to DEVNULL. Docs: HANDLER_REFERENCE transport section,
DESIGN-socket-relay.md §6.4 (archived-plan edit, deliberate — it is the
transport design SSoT), UNRELEASED truth-changes v3.57.0 entry.
Gate: tests/unit/install+daemon 2223 passed, tests/acceptance 68 passed,
doc-truth/forwarder-socket integration 54 passed; black/ruff/mypy --strict
clean on all touched files.

## 21:50 · handoff · P3 — Phases 1–2 done; Phase 3 stays with coordinator

This repo's `relay_enabled` is still FALSE (deliberate — re-enabling is
the coordinator's Phase 3 act, now one `bin/hooks-daemon transport on`
away, with `transport off` as the first response to any regression).
Canary parity (Task 3.2) also outstanding. CLI usage:
`bin/hooks-daemon transport on|off|status [--json]`.

## 22:05 · action · P1 — error-hiding self-scan findings fixed properly

Team-lead relayed 4 findings from
`tests/unit/qa/test_audit_error_hiding.py::TestRealRepoSelfScan` against
the new transport modules. All fixed by explicit handling, no exclusion
entries: probe_status_line's brace-but-not-JSON case and
probe_no_event_listeners' connect-refused case are both the PASSING
outcome, so each now logs debug and flows through a named variable
instead of `pass`/`continue`; read_last_toggle_state logs a warning on an
unreadable state file before reporting no-record; _relay_binary_facts
logs a warning when the binary cannot be hashed. Self-scan green, 89
transport+audit unit tests green, acceptance cycle re-run green (8
passed), ruff/black/mypy --strict clean.

## 21:17 · action · T3.1 — relay dogfood RE-ENABLED via the new toggle; soak started

Coordinator ran `bin/hooks-daemon transport on` in this repo: config flipped
(comment-preserving), forwarders regenerated with catalogue-derived guards,
daemon restarted, built-in verification PASSED (forwarder-guard-state,
31/31 per-event listeners, pre-tool-use JSON decision, status-line raw,
stop hard-block exit-2). `transport status`: Active rung relay, 31
listeners, binary sha256 f6484c23eb2d, "transport on: verified". Stale
suspension comment in hooks-daemon.yaml replaced with the live-dogfood note
(regression response: `transport off` first, diagnose second). Soak window
runs from here — live session hook traffic now rides the relay; zero hook
errors observed at soak start. Task 3.1 flipped to in-progress; remaining:
soak health confirmation + Task 3.2 canary toggle cycle.

## 21:38 · finding · T3.2 — canary run 4 (main @ 6359083f): toggle safety contract HOLDS; two client-side enable defects found

Canary reset pristine (rm -rf + reclone at bfa5f28), daemon cloned from
/workspace at 6359083f, fresh install_version.sh (exit 0),
enforce_single_daemon_process forced false before any start. F1 regression
re-verified FIXED: zero relay-guard markers and zero /workspace/untracked
literals in all 40 deployed hook files; routing proven by the run-3 method
(canary daemon stopped, socket-stdin PreToolUse probe through the deployed
pre-tool-use forwarder auto-started the CANARY daemon on its own socket,
returned exit 0 + JSON allow verdict; workspace daemon pid unchanged
throughout). Toggle cycle CLIENT-SIDE: safety semantics all held — every
failure was clean, auto-revert restored + re-verified the prior state,
`transport off` when already off is an exit-0 no-op (twice), status
honestly reports "transport on: FAILED (auto-reverted)". But `transport on`
cannot succeed on this client, two defects:

- **D1 (blocks Task 3.2)**: `probe_forwarder_guard_state`
  (`src/claude_code_hooks_daemon/install/transport_verify.py:347`)
  enumerates EVERY file in `.claude/hooks/` and treats anything outside
  RELAY_EXCLUDED_EVENT_FILE_NAMES as a relay-eligible forwarder. php-qa-ci
  ships its own hook scripts there (php-qa-ci__*.py, CLAUDE.md, README.md,
  test-all-hooks.sh), so verification always fails naming those files and
  the toggle auto-reverts. Fix direction: enumerate the catalogue's known
  forwarder event-file names, not directory contents.
- **D2**: `run_toggle` (`install/transport_toggle.py`) has NO relay-build
  step — `relay_source: build` is honoured only by the installer's Step 7b.
  A client whose untracked/bin has no hooks-relay gets guards that always
  fall through to legacy (probes still pass), i.e. `transport on` verifies
  "green" without a relay actually engaging. Binary was absent before and
  after the cycle here.
- **D3 (minor, expected-by-design?)**: a fresh client install has no
  `relay_enabled:` line at all, so `transport on` refuses ("found 0")
  before doing anything — clean, but means the documented client-side
  toggle needs a seeded transport block first (this run seeded one, off).

End state: canary daemon STOPPED, no orphan processes, canary tree left as
evidence (seeded transport block + toggle-state file, guards stripped);
workspace daemon RUNNING pid 2475067 with 31/31 listeners before, during
and after. Task 3.2 NOT ticked — blocked on D1 (and D2 for a meaningful
green).

## 21:56 · action · T3.2 — canary run 5 (main @ 4f8941da): D1-D3 fixes verified, full toggle cycle GREEN — Task 3.2 ticked

Fifth pristine cycle (rm -rf + reclone at bfa5f28, daemon cloned from
/workspace at 4f8941da with the D1-D3 fixes, install_version.sh exit 0,
enforce_single_daemon_process forced false, transport deliberately NOT
hand-seeded to exercise D3). Results:

- **D3 verified**: first `transport on` on the key-less config SEEDED
  `daemon.transport.relay_enabled: false` itself, then refused cleanly
  pre-flip ("relay_source is null — set relay_source: build|download"),
  changing no other state. Choosing build-vs-download stays a client
  decision, which is reasonable; noted that a fresh client needs that one
  config line before enable can proceed.
- **D2 verified**: with `relay_source: build` set, `transport on` exited 0
  "flipped, forwarders regenerated, daemon restarted, verified" — a real
  client-side rustc build produced the canary's own
  `untracked/bin/hooks-relay` (570,056 bytes, sha256 76c950c2f3e4) plus its
  `.route` file. Relay rung ACTIVE: 31/31 per-event listeners, direct
  `hooks-relay <pre-tool-use.sock> --no-fallback` returned a real `{}`
  verdict (exit 0), and the socket-stdin PreToolUse probe through the
  deployed forwarder passed.
- **D1 verified**: 27 forwarders guarded; php-qa-ci's own hook files
  (php-qa-ci__*.py, CLAUDE.md, README.md, test-all-hooks.sh) and
  stop/subagent-stop all guard-free; verification green with them present.
  Guards carry only canary-local paths — zero daemon-repo literals
  (checked precisely for /workspace/untracked/{bin,events-,daemon-}).
- **Off leg**: `transport off` exit 0 verified, guards fully stripped
  (zero relay references), second `off` an exit-0 "already disabled" no-op,
  status honest ("transport off: verified"), 0 listeners.

End state: canary daemon STOPPED, zero orphan processes, tree left as
evidence; workspace daemon UNDISTURBED pid 2537891 with 31/31 listeners
before, during and after (pid differs from run 4 because the coordinator's
own toggle work restarted it between runs). Task 3.2 ticked ✅.

## 22:35 · action · T3.2 — D1/D2/D3 fixed TDD-first (implementer)

All three canary-run-4 defects fixed, RED tests first:

- **D1**: `probe_forwarder_guard_state` now iterates
  `wired_event_metas()` bash_keys — the expected forwarder set derives
  from the event catalogue, never from directory contents, so client-owned
  files in `.claude/hooks/` (php-qa-ci__*.py, CLAUDE.md, README.md,
  test-all-hooks.sh — now planted in the acceptance fixture too) are never
  verification's business. Bonus check while there: a relay-INELIGIBLE
  wired forwarder carrying a guard now fails the on-state explicitly.
- **D2**: two halves. (a) `ensure_relay_binary` runs when ENABLING, BEFORE
  the config flip: present+executable binary short-circuits; absent binary
  with `relay_source: null` refuses loudly (exit 1, nothing flipped, no
  revert needed — deliberately placed pre-flip so a provisioning failure
  leaves the project untouched rather than doing a revert dance; the CLI
  prints "No state was changed" so it cannot read as a no-op); configured
  `relay_source` delegates to `deploy_relay_if_configured` — the
  installer's own Step 7b routine — with `daemon_dir =
  get_untracked_dir(project_root).parent` (correct in both install modes)
  and `version_tag=v{__version__}`. (b) new `relay-rung-active` probe in
  the on-state verification: drives the resolved relay binary DIRECTLY
  against the pre-tool-use socket with `--no-fallback`, so a silent
  fallback can no longer masquerade as green. Acceptance proves a REAL
  rustc musl build via `relay_source: build` from the fixture's own
  daemon dir (relay source copied in) — ~4s, well inside the 300s bound.
- **D3**: `seed_relay_enabled_line` inserts `relay_enabled: false`
  comment-preservingly (under a sole existing `transport:` line, else as a
  new block under top-level `daemon:`); anything ambiguous (no `daemon:`,
  multiple `transport:` lines) refuses naming the exact YAML to add.
  `run_toggle` seeds before reading, so `transport off` on a fresh config
  is a seeded no-op and `transport on` proceeds. Chose insertion over
  refusal because the seeded value is the shipped default (false) — it
  materialises the documented key without changing behaviour.

Tests: 72 transport unit tests (incl. 20 new), CLI ordering test for the
pre-flip refusal, 11 acceptance tests (foreign-file fixture, seeded-key,
null-source refusal, real-build enable). Gates: unit
install+daemon+qa 2745 passed (3 errors = semgrep missing from this venv,
pre-existing environmental, unrelated); error-hiding self-scan green;
mypy --strict/black/ruff clean. This repo's live dogfood untouched and
healthy throughout: relay rung, 31/31 listeners, `transport status` green.

## 22:06 · handoff · — — plan CLOSED Complete; all gates green

Closing state: soak window 21:16→22:06 with the relay rung live in this repo
(31/31 listeners, status line healthy, zero hook errors — Task 3.1 ✅);
canary run 5 green on a pristine php-qa-ci client including a real
client-side rustc relay build and the full on/verify/off/strip cycle
(Task 3.2 ✅, 17f9446e); definitive QA gate 25/25 PASSED on the final tree
(16,124+ tests, coverage 95.2%) plus full acceptance suite green (71).
Supporting docs disposition: PROMOTE — nothing to promote beyond what
already landed in HANDLER_REFERENCE + DESIGN-socket-relay §6.4 + the
UNRELEASED manifests; the plan folder holds only PLAN.md and this journal.
Status flipped Complete with the atomic archive move in this commit.
