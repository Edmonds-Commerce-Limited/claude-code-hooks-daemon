# Plan 00305 — Journal 26-09-01

> **Append-only activity log** for plan 00305. One file per day
> (`00305-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 09:25 · action · — — plan scaffolded

Plan 00305 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 09:30 · action · — — filed mid-release as deferred-findings ledger

Filed during the v3.58.0 release pipeline as the ledger for the Code
Review Gate's deferred non-blocking findings (3/4/5); findings 1/2/6 were
fixed before the release shipped. Failsafe recovery cron: REUSED the
session's existing cron `2494b387` (hourly at :23) — none created.

## Phase 1 complete

Tasks 1.1-1.3 implemented with TDD (delegated agent's work, finished in
main thread after the user stopped background agents): mock.patch removed
from daemon/cli.py via optional project_root threading; {REPO_ROOT}
placement field_validators on PluginConfig.path/ProjectHandlersConfig.path;
absolute secret_word_list_path degrade surfaced via CLI status/degraded
reporting (new tests/unit/daemon/test_cli_secret_redaction_status.py).
Full QA 25/25 green. Daemon restarted before commit.

## Phase 2 complete

Tasks 2.1-2.6 implemented (python-developer subagent). All fixes are to
`get_acceptance_tests()` definitions (the playbook's SOURCE), never the
generated playbook itself.

- T2.1: replaced stale long-form expected-message literals
  (`SECURITY ANTIPATTERN BLOCKED`, `TDD REQUIRED`, `dangerous permissions`,
  `Error-hiding pattern detected`) with the live short-form
  `BLOCKED \[R-*\]` pattern across all 12 security strategies, 11 TDD
  strategies, 5 error_hiding strategies, and dangerous_permissions.py.
  Same drift found and fixed in artifact_publish_blocker.py (stale
  `BLOCKED: publishing an artefact`) and secret_file_guard.py (stale
  `SECRET FILE PROTECTED`, 4 occurrences) while in those files for T2.3/T2.5.
- T2.2: strategies/lint/python_strategy.py Test 124 `valid.py` fixture
  content now ends `\n` (was missing, tripping ruff W292).
- T2.3: artifact_publish_blocker.py Tests 39/40 now `requires_main_thread:
  True` with safety_notes documenting the sub-agent/source_disable SKIP.
- T2.4: validate_eslint_on_write.py Test 110 description now documents the
  tracked-package.json-with-`llm:`-scripts precondition.
- T2.5: reproduced the secret_file_guard false positive
  (`[pass_result, fail_result]` matching `*vault_pass*`) — root cause:
  `find_protected_mention`'s glob-shape gate treated a lone unmatched `[`
  (Python list syntax) as a wildcard, and the leading-wildcard overlap
  check then compared `pass_result`'s `pass` edge against the
  `vault_pass` stem. Fixed via `_is_glob_shaped`/`_has_leading_wildcard`/
  `_has_trailing_wildcard` in utils/secret_file_matching.py, requiring a
  COMPLETE bracket expression (matched `]`) before `[`/`]` count as
  wildcards. Pinned with
  `test_python_list_literal_is_not_matched` in
  tests/unit/utils/test_secret_file_matching.py (red before the fix,
  confirmed via direct pytest run; green after). 109/109 in that file +
  test_secret_file_guard.py pass.
- T2.6: reproduced pipe_blocker's producer misattribution for
  `[[ "python -m pytest tests/ | tail -5" == 0 ]]` (headline named `[[
  unrecognized` with a fabricated `^[[\b` extra_whitelist suggestion, and
  the printed remediation was malformed). Extended
  `_substitution_content_start` in pipe_blocker.py to also narrow to a
  DOUBLE-quoted argument's content start (not just an open `$( )`/backtick
  substitution frame) — single-quote handling deliberately left untouched,
  since a single quote suppresses the pipe itself and two pre-existing
  tests (`TestNonSubstitutingTextIsInert`) pin that as intentional.
  Regression-tested in
  `TestQuotedArgumentPipeNamesTheInnerProducer` (test_pipe_blocker_command_substitution.py);
  full pipe_blocker + shell_segmentation suite (339 tests) green, including
  the two pre-existing single-quote tests unchanged.

QA: 1968 targeted unit tests green across strategies/handlers touched.
Playbook regenerated (`./bin/hooks-daemon generate-playbook`) and
spot-checked: no stale long-form literals remain anywhere in the 7642-line
output; Tests 39/40/110/124/87 all read correctly.

## Plan closed out

Phase 2 delivered as 277a47bd (all six drift tasks plus two live bug fixes:
secret_file_matching bracket-expression discipline, pipe_blocker
quoted-argument producer attribution). Status flipped Complete, folder
archived, README row + stats updated. Follow-up finding (over-broad
R-SECRET-BASH-MENTION on plain words containing "secret") ledgered as
Plan 00306.
