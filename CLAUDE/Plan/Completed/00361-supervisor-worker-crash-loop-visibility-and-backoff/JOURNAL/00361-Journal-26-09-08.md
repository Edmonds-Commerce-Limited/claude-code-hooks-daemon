# Plan 00361 — Journal 26-09-08

> **Append-only activity log** for plan 00361. One file per day
> (`00361-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 14:26 · action · — — plan scaffolded

Plan 00361 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 14:27 · finding · P1 — the worker error log dates the instability

Trigger: the owner's compaction note "i think we've had supervisor
regressions in latest releases, it seems to be much less stable than it
was". Failsafe recovery cron for this session: `4c23c3d0` (pre-existing,
reused; no new cron created).

- `untracked/claude-supervise-worker.err.log`: 165 fatal tracebacks in the
  undated block between the `2026-09-07 10:38` and `2026-09-08 11:30`
  diagnostic lines. Error kinds: 87 + 1 `take_model_submitted` missing on
  `HumanInputLine`; 33 `note_model_reading()` missing `now_wall`; 21
  `_min_effort_levels_from_env` not defined; 8 `TickFacts.__init__()`
  unexpected `human_model_command`; 6 + 6 + 3 missing `CompactStateMachine`
  private methods.
- The module-level `raise SystemExit(main())` line numbers in those
  tracebacks (6174 to 6485) match none of the eleven committed versions of
  the file since v3.62.0 (6163, 6235, 6288, 6294, 6333, 6351, 6354, 6386,
  6400, 6522), so every crash ran an intermediate on-disk edit state.
- `untracked/supervise/decision.log` has zero lines mentioning the worker or
  the in-process fallback: the host's `worker.decide()` returning `None`
  falls back silently by design (Plan 00164 Phase 4).
- The only ARMED host start since 2026-09-04 is 2026-09-08 11:15:57, so on
  2026-09-07 the in-process fallback ran host code from 2026-09-04 08:05.

## 14:34 · action · P2 — guard, transitions and crash record implemented

- Tests first: `tests/unit/supervise/test_worker_crash_loop.py` (18 tests,
  RED on the crash record, guard, decider wiring, worker accessors and
  fallback transitions).
- Implementation in `.claude/ccy/claude-supervise.py`: `WorkerCrashGuard`,
  `FallbackTransitions`, `PolicyWorker.source_fingerprint` /
  `on_disk_fingerprint()` / `exit_code()`, `_make_worker_decider(worker, log)`,
  the `--worker` crash record in `main()`, and the transition log in
  `supervise()`.
- Every edit was kept self-consistent so the live worker never reloaded a
  broken state: the worker respawned at pid 906006 (14:31:23) with no crash
  record written.
- Supervisor suite: 744 passed. mypy strict and pyright clean on the file.

## 15:05 · action · P2 — Phase 2 committed and pushed

Delivered at `da5b7258`; Tasks 2.1 to 2.4 ticked. Full QA on the tree after
the merges: tests 20283 passed, coverage 95.2%. Phase 3's live check of the
host-side `decision.log` lines needs a ccy session restart (host code never
reloads), so the plan stays In Progress until the owner restarts ccy and the
lines are observed.
