# Plan 00466 — Journal 26-09-24

_Scaffolded by `mkplan.bash`; timestamps in this file are UTC._

> **Append-only activity log** for plan 00466. One file per day
> (`00466-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` UTC 24h (the date is in the filename, also UTC); times increase
>   down the file. A file with no sentinel line above predates this system —
>   its times are local, zone unrecorded (legacy; never rewritten).
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 15:40 · action · — — plan scaffolded

Plan 00466 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 16:16 · finding · N2   — setup_worktree.sh sends agents to the denied full-suite runner

Filed N2: `scripts/setup_worktree.sh` tells every agent to run the full suite through `run_all.sh`. The line is in its agent prompt template, and the same command is in its "Run QA" hint and in Step 7's QA check. `enforce_llm_qa` denies that script, and Plan 00463 makes full QA a coordinator-only integration gate. Graduated to 00463, which owns the sub-agent QA policy; its agent has been sent the finding.

## 16:16 · finding · N3   — goal_injection fires on any edit of an In Progress plan

Filed N3, found live. With the supervisor's goal set to Plan 00461, a ledger-row Edit to this plan's PLAN.md drew `GOAL DISPLACED ... 00461 ... superseded by Plan 00466's goal`. `goal_injection.handle()` checks the post-write file for an In Progress status line and never checks for a transition. The first edit in a session to any In Progress plan therefore emits a goal signal and records the plan as owed work. A fix agent is dispatched with TDD: emit only on a real flip to In Progress.

## 16:55 · finding · N7   — CLAUDE.md guidance block order is not deterministic

Filed N7. The main daemon's restart after batch A auto-committed `ce31d6d8`, a pure reorder of the handler markers in CLAUDE.md, although the integration daemon had just regenerated the same tree. `_collect_tiers()` emits in `self._handlers` order and never sorts, so the block's order is not a function of the handler set. This causes spurious restart commits and CLAUDE.md merge conflicts, two of them while building batch A. Queued for a fix agent: a total order plus a byte-identical-output test.

## 16:59 · finding · N4   — secret_file_guard leading-wildcard overlap false positive

Reproduced N4 (`secret_file_guard` false positive on `*words[position + 1 :]`)
via both the Bash-mention route and a direct `find_protected_mention_detail`
call: confirmed the matched glob is `*.vault-password` and the matched token
is `*words[position`, with a 4-char coincidental overlap ("word") between the
token's residue and the stem's tail.

Root cause pinned in `_glob_token_overlaps_stem`
(`src/claude_code_hooks_daemon/utils/secret_file_matching.py`): the
leading-wildcard branch's partial suffix/prefix overlap test is only a valid
truncation model when the PATTERN also has a trailing wildcard (so content
can follow the overlap). `*.vault-password` is the sole leading-only pattern
in the shipped defaults, so it was the only one exposed.

Fix: added `pattern_has_trailing_wildcard` to `_glob_token_overlaps_stem`;
the leading-wildcard branch now also requires
`pattern_has_trailing_wildcard or stem_basename.endswith(residue)`. RED test
`test_leading_wildcard_python_splat_operator_is_not_matched` confirmed
failing pre-fix, passing after, plus a paired
`test_leading_wildcard_full_suffix_of_anchored_stem_still_matched` regression
guard. Full `test_secret_file_matching.py` (190) and `test_secret_file_guard.py`
(76) pass.

## 17:07 · finding · N5   — secret_file_guard security fail-open on empty path-mention token

N5 reproduced via the coordinator's saved triggering payloads
(`untracked/scratch/n5_triggering_edits.jsonl`), replayed through the real
daemon with a full traceback. Root cause pinned:
`_normalised_token_forms` (`secret_file_matching.py`) strips a token equal
to a home/pwd prefix (e.g. a quoted `"~/"` literal) down to an empty
string, which reaches `path_matches_globs("")` → `_candidate_paths` →
`os.path.relpath("", root)` → `ValueError: no path specified`.

Severity: the crash is inside `secret_file_guard.matches()`, and the
daemon's non-strict per-handler catch treats the exception as "did not
match" — so the WHOLE secret-file guard is skipped for that write. Fixed
at both layers per the coordinator's instruction: (a)
`_normalised_token_forms` never emits an empty form (guard on
`len(token) > len(prefix)`), (b) `_candidate_paths` is now total on an
empty `file_path` (returns early, no `os.path.relpath` call at all — a
second line of defence for any other caller). Fail-safe pinned with a RED
test: content with BOTH a home-prefix token and a genuine protected
mention (`id_rsa`) still denies. A corpus test over every operand shape
from the live payloads confirms the mention scan never raises.

RED confirmed failing pre-fix (`test_path_exclusion.py ::test_empty_file_path_with_a_project_root_does_not_raise`,
`test_secret_file_matching.py::TestBareHomePrefixTokenDoesNotCrash`, 6
tests), GREEN after. Full `test_secret_file_matching.py` (196),
`test_path_exclusion.py` (55) and `test_secret_file_guard.py` (76) pass —
327 total. Replayed all three original triggering payloads through a real
`DaemonController.process_request` end to end post-fix: no crash (denied
for an unrelated, correct reason — the synthetic repro root differs from
the payload's literal absolute path).

## 17:20 · finding · N6   — enforce_llm_qa denies a prose mention of the denied full-suite runner in an unrelated argument

N6 reproduced from the coordinator's live report: `enforce_llm_qa`'s
substring-plus-leading-word-allowlist check (`_is_inspection_only`) denied a
`mkplan.bash --journal ... --title "... run_all.sh ..."` journal entry
because `run_all.sh` appeared only as prose inside the quoted `--title`
argument, and the segment's own leading word (`mkplan.bash`) wasn't on the
inspection/VCS allowlist.

Replaced the check with a `shlex`-based real-invocation detector
(`_has_real_invocation`/`_segment_executes_script`/`_word_names_the_script`),
reusing this project's established `shlex.split()` + `try/except ValueError`
pattern (`project_containment.py::_tokenise`) rather than a third parser. A
segment now matches only when the script is named at the command's own head,
or as an argument to a wrapper (bash/sh/env/timeout/nice/exec) — recursing
into a `-c` subshell argument and into `$(...)`/backtick substitution inner
text. A prose token merely containing the name (a whole quoted `--title`
phrase is one shlex token) no longer counts. `_INSPECTION_COMMANDS`/
`_VCS_COMMANDS` allowlists deleted (YAGNI): with no wrapper match and no
script-name match, nothing can execute regardless of what the head verb is.
A segment that fails to tokenise falls back to the old conservative
substring check (still denies) — no bypass opened.

Found and fixed a second instance of the same false-positive class in the
handler's own acceptance-test fixture: `echo "./scripts/qa/run_all.sh"` was
denied only because `echo` was absent from the allowlist, not because it
invoked anything. Replaced with `bash -n scripts/qa/run_all.sh` (a genuine
wrapper-invocation shape; `-n` keeps it parse-only and harmless if the block
ever regresses).

RED tests confirmed failing pre-fix:
`test_does_not_match_a_prose_mention_in_a_quoted_title_flag` (the exact live
shape) and `test_does_not_match_an_unrelated_wrapper_invocation`. Four
companion "must still deny" regression guards added alongside (a `gh --body`
prose mention, `cd ... && ./run_all.sh`, `sh -c './run_all.sh'`, bare
`$(./run_all.sh)`). All 41 tests in `test_enforce_llm_qa.py` pass, and all
198 tests under `bin/hooks-daemon test-project-handlers --verbose` pass.

## 17:34 · finding · N8   — reference freshness says BLOCKED on an allowed call

Filed N8, found live. A `block_once` repeat, and every `advise`-mode result, of `reference_repo_freshness` is an ALLOW whose context is the verbose DENY rendering, starting `BLOCKED [R-REFERENCE-REPO-NOT-VERIFIED]` (`_verdict`, lines 565 and 575-576). A `mv` of a clone under `untracked/repos/` ran while its context said BLOCKED. The remedy renders the allow paths in advisory form, plus a class test over every handler's allow paths.

## 17:37 · finding · N9   — docs_qa judges gitignored markdown

Filed N9. After the Defence Before Fix plugin install (Plan 00467), `llm_qa.py docs_qa` FAILS locally with 12 `source-tree-markdown` findings, all in the gitignored `.claude/ccy/plugins/` tree. It reported 0 at batch A's gate. The docs corpus walks the filesystem without honouring `.gitignore`. This blocks the next integration gate, so a fix agent is dispatched: build the corpus from `git ls-files --cached --others --exclude-standard`, and audit the other QA corpora for the same assumption.

## 17:44 · finding · N10-N11   — review filed N10 and N11; lost headings restored

The review of the N3-N7 fix branches (subagent-reports/260924-n466-review-opus-5-5.md: 0 blocker, 4 major, 7 minor, 7 nit) is committed.

- It surfaced two secret_file_guard problems, filed as N10 and N11: a mid-name wildcard gets past the guard (pre-existing on main), and the guard fails OPEN on any exception under the default `strict_mode: false`.
- The review also caught the coordinator's ledger edits dropping the next entry's `### Nn —` prefix each time an entry was inserted, losing the N3, N7 and N8 headings. They are restored here.
- Both fix branches go back for M1-M4 and the minors. N10 and N11 go to the guard-defects branch, which already owns this guard.

## 17:50 · action · N1   — N1 remedied: run-probe every venv-fallback acceptance site

Fixed N1: `resolve_venv_python`'s fallback accepted a venv interpreter that
cannot run on this host, on executable-bit alone (`[ -x ]` / `os.access(X_OK)`).

Found every implementation that trusted a venv interpreter's executable bit:
the bash canonical library `scripts/lib/resolve_venv.sh::_rv_pick_python`
(the only bash site not delegating to it, per `check_canonical_callers.sh`'s
own static enforcement) and the Python SSOT
`resolve_existing_venv_python_with_diagnostics` in
`src/claude_code_hooks_daemon/daemon/paths.py` (steps 2 metadata, 3
fingerprint-keyed, 4 scan-fallback, 5 legacy, via a shared `_pick_interpreter`
closure). `resolve_existing_venv_python` (the simpler, no-diagnostics
function) was deliberately left un-probed: it runs on the per-user-turn hot
path (`daemon_upgrade_detector`) and neither of its current callers execute
the returned interpreter -- `daemon_upgrade_detector` only reads
`.daemon-metadata.json`, and `client_validator.py::validate_daemon_can_start`
already runs its own `subprocess.run(..., timeout=...)` probe before
executing.

TDD: RED tests confirmed failing against the pre-fix code first, in
`tests/unit/daemon/test_paths_resolve_venv_diagnostics.py::TestRunnabilityProbe`
(7 new tests) and a new `tests/integration/test_resolve_venv_runnability_probe.py`
(7 tests exercising `_rv_pick_python`/`resolve_venv_python` directly in
bash). Both cover: a fake executable that exits non-zero, a dangling
symlink, a hanging candidate with the bound respected, fall-through to a
good second candidate, all-candidates-bad reaching the venv-free path, and
the slug-exact/fingerprint-keyed venv unaffected when it genuinely works.

GREEN: both new test files pass in full, plus the full existing
venv-resolution regression surface (239 tests: parity matrix, hot-path
latency/cache, pipefail cascade, bin-wrapper, skill-scripts, install
resolver, client validator, daemon_upgrade_detector, multi-host NFS
fail-fast) -- no regressions.

Probe cost measured directly against this worktree's own real venv
`bin/python`: an early sleep-1-poll-loop draft cost ~1005ms per candidate
(the loop always sleeps a full second even for a candidate that exits in
milliseconds, since the first poll almost always lands before the process
has exited). Replaced with a watchdog subprocess
(`( sleep N; kill -KILL "$pid" ) &` + a blocking `wait "$pid"`, no polling):
measured ~15-40ms per candidate. Only reached on a resolver-cache MISS (the
bash hot-path cache in `_rv_resolve_python_impl` is checked before
`_rv_pick_python` is ever called), so none of this lands on the
steady-state per-hook path.

Verified the bash implementation's -e safety directly: `_rv_candidate_runs`
is always called as the condition of an `if` inside `_rv_pick_python`
(`if _rv_candidate_runs "$candidate"; then ...`), and bash suspends
`set -e` for the ENTIRE body of a function called in that position --
confirmed empirically both for the direct case and for the full nested
`resolve_venv_python` -> `$(...)` -> `_rv_pick_python` -> `_rv_candidate_runs`
chain used in production. A candidate's output is captured to a private
temp file (not `/dev/null`, not the caller's inherited stdout/stderr) so an
orphaned descendant of a killed candidate (this script has no job control,
so `kill -KILL "$pid"` reaches only the immediate process) cannot hold the
caller's own pipe open past the bound.

QA: targeted `llm_qa.py format lint type_check pyright magic_values
error_hiding shell_check docs_qa plan_qa` -- 9/9 passed. Worktree daemon
restarted and verified before committing.

## 17:57 · action · N9   — docs_qa and doc_truth now filter through git, not a raw filesystem walk

Fixed N9: `docs_qa` and `doc_truth` now filter file discovery through
`git`'s own view of the tree instead of a raw filesystem walk.

Added `git_visible_paths(project_root)` to `utils/git_repo.py`: one combined
`git ls-files --cached --others --exclude-standard -z` call per corpus
build, returning `None` outside a git repository so callers keep their
pre-existing unfiltered behaviour there. `docs_qa/corpus.py`'s
`iter_markdown_paths` and `iter_corpus_paths` both filter through it, with a
named exception (`_GITIGNORED_MARKDOWN_INCLUDES`) preserving
`.claude/ccy/CLAUDE.md`'s deliberate inclusion despite being gitignored, and
a directory-descent rule so the walk still physically reaches it.

The class audit surfaced a second live instance of the same defect in
`scripts/qa/check_doc_truth.py`'s `_iter_markdown`: its directory denylist
named `marketplaces` but not the sibling `cache/` directory, so a plugin's
cached spec markdown could still reach `_check_shell_fences` as a false
finding. Reproduced directly with a git-ignored fixture and fixed the same
way.

The remaining named corpora (plan_qa, doc_snippets, repo_hygiene,
sensitive_content, british_english, magic_values, error_hiding,
handler_reference) were audited and left unmigrated, each for a stated
reason: three are already git-native by design, three are scoped to
directories with no gitignore gap, one never walks a directory at all, and
one is scoped to the configured plan directory only. Pinned the whole table
as a ratchet in `tests/unit/qa/test_qa_corpus_git_visibility_audit.py`,
mirroring `test_qa_package_dependency_direction.py`'s shape: every
`MIGRATED` entry is verified by AST to actually import and call
`git_visible_paths`, every declared source path is checked to still exist,
and every `ALLOWLISTED` entry must carry a non-trivial reason.

TDD: RED tests confirmed first against unmodified code
(`tests/unit/docs_qa/test_corpus.py::TestIterCorpusPathsGitIgnore`,
`TestIterMarkdownPathsGitIgnore`, and
`tests/integration/test_doc_truth_check.py::test_does_not_scan_a_gitignored_vendored_plugin_install`),
then made GREEN by the fix. Added
`tests/unit/utils/test_git_repo.py::TestGitVisiblePaths` for the shared
helper directly. Targeted QA: 576 tests across the touched directories, plus
`llm_qa.py format lint type_check pyright magic_values error_hiding docs_qa plan_qa` — all pass. Worktree daemon restarted and verified RUNNING before
committing.

Marked N9 Remedied in PLAN.md and NIGGLES.md, and added release note 13 (24 after integration)
under `CLAUDE/UPGRADES/UNRELEASED/release-notes/`.

## 18:24 · action · N8   — N8 remedied: ALLOW no longer carries the DENY headline

N8 remedied: `reference_repo_freshness` no longer renders the DENY headline
on an ALLOW result.

`RuleFormatter` gained a fourth rendering, `advisory(rule)` (`core/rule.py`)
— same `rule_id` and `Rule.verbose` content as `verbose()`, headed
`ADVISORY` instead of `BLOCKED`, reusing the existing "ADVISORY:" convention
rather than inventing a new format. `reference_repo_freshness.handle()` now
computes `_is_blocking(session_id, subject, mode)` before building the
message and selects `verbose()`/`advisory()` accordingly in `_not_verified`
and `_stale`; `_verdict()` itself is unchanged and stays the sole place that
decides and records. `_is_blocking` is pinned against `_verdict`'s own
decision by `TestIsBlockingMatchesVerdict`.

An AST-based static sweep of every handler for `Decision.ALLOW` built from
`formatter.verbose`/`terse` content found exactly one confirmed occurrence
(`reference_repo_freshness`, the fix above) and two structurally similar but
safe call sites, manually verified: `lint_on_edit._run_lint_command`
(`language_name` parameter) and `plan_qa_edit._advisory_result` (`findings`
parameter). `lsp_enforcement`, named as a suspect, was confirmed already
correct — its `block_once` repeat and `advisory` mode both return a plain
`dynamic_detail` string with no rule-id/BLOCKED prefix.

The class-wide guard lives at
`tests/integration/test_allow_never_carries_deny_headline.py`: it drives
every handler's own declared BLOCKING acceptance test twice against the same
instance, replicating the history-recording step
`DaemonController.dispatch()` performs after every route so a handler whose
block-once state lives in the shared `HandlerHistory` data layer (not an
in-instance dict, e.g. `lsp_enforcement`) genuinely sees its repeat call
transition to ALLOW, then asserts the repeat's `reason`/`context` never
contains the `"BLOCKED ["` signature. Confirmed RED against a deliberately
reintroduced defect in `lsp_enforcement` (caught it), then GREEN once
reverted. `reference_repo_freshness`'s own regression coverage lives in its
unit tests instead (`TestBlockOnce`, `TestConfiguredModes`,
`TestNotVerified`), RED/GREEN-verified the same way, because its only DENY
acceptance test declares `harness_cannot_produce` — no fixture can build a
real governed checkout, so the integration harness cannot reach it.

NIGGLES.md and PLAN.md N8 rows marked Remedied.

## 20:15 · finding · —   — merge conflict dropped the M1/N11/N10/N5-N6 entries; restored

Restoring the record of the M1/N11/N10/N5-N6 fix-turn journal entries: a
merge-conflict resolution in this file accidentally dropped their bodies
(the `plan_journal_guard` blocked a hand-spliced restoration, since restoring
deleted content reads as a net content increase against the file's
already-modified on-disk state; the sanctioned fix is a fresh entry here,
not a hand-edit). The work itself was never lost -- it is committed
(`d0de526d`) and described in full in the NIGGLES.md N4/N5/N6/N10/N11
entries. Summary: M1 restored `enforce_llm_qa` to deny-by-default via
punctuation-aware shlex tokenisation; N11/M4 made `secret_file_guard` and
`project_containment` fail closed structurally on any internal exception,
with `sensitive_content` and `destructive_git` audited and found already
safe; N10 closed the interior-wildcard bypass via a two-glob intersection
DP (`_globs_can_intersect`); m1 corrected the N4 leading-wildcard fix for
tokens with a second wildcard after the leading one; and the N5/N6 ledger
entries plus release notes 14/15 were corrected per the review's fact
findings (m2/m3).

## 21:03 · finding · —   — Phase 3: guard-defects review 2 fixed (B1, M1, M2, m1-m4, n1, n3, n4)

Phase 3: fixed every finding in guard-defects security review 2
(`subagent-reports/260924-n466-guards-review2-opus-5-5.md`) on
worktree-n466-guard-defects, TDD-first throughout.

- **B1** (timing fail-open): `_globs_can_intersect` now collapses `*` runs
  and caps DP work at `_DP_MAX_CELLS`, fail-closed past the cap;
  `iter_protected_mentions`/`find_protected_mention_detail` gained an
  optional `deadline` parameter, wired into both `secret_file_guard.py`
  call sites via `SCAN_DEADLINE_SECONDS`. 5 timing tests pin the review's
  own measured shapes.
- **M1** (`enforce_llm_qa` deny-by-default, 19 regressions): string-executor
  arguments (`*sh -c/-lc`, `eval`, `ssh`, `watch`, `su -c`, `timeout … <shell> -c`, `python -c`) are re-parsed recursively; `<`/`>` added to
  `_PUNCTUATION_CHARS` so a glued redirection splits into its own token; a
  glob/brace word is checked against the script after expansion. The
  brace-vs-punctuation-char conflict (`{`/`}` split off before a real brace
  group can be recognised) is resolved by matching brace words against the
  RAW segment text, not the shlex token stream. 8 new test methods, all 19
  regressions pinned, 53/53 passing.
- **M2** (secret_file_guard interior-wildcard gaps): edge-open tokens now
  reach the DP-intersection check too, gated by a new degenerate-orientation
  guard (own finding: a leading-wildcard token vs a trailing-wildcard
  pattern intersects unconditionally for ANY literal, which would have
  denied ordinary globs like `*.py` against `.vault-pass*` — not in the
  review, caught by this branch's own RED test before it shipped); an
  unenumerable bracket expression is substituted with `?` in the DP (plus a
  POSIX-named-class regex fix, since the general bracket regex left the
  outer `]` behind); both-edges patterns get a filesystem-truth route
  (`_both_edges_glob_mention`, gated by a cheap literal-overlap pre-filter
  after real disk I/O measurably broke this module's own B1 timing budgets
  when called unconditionally); brace groups are expanded against the raw
  command text before tokenising (mirrors M1's fix, and its own first
  attempt used a `\S*{...}\S*` regex that turned out to be catastrophically
  slow on adversarial input with no braces at all — fixed with a bounded
  manual boundary scan). 16 new tests across 4 classes, 463/463 passing in
  the combined secret_file_matching + secret_file_guard + path_exclusion +
  project_containment suite.
- **m1** (`handle()` outside the fail-closed wrapper): both guards' `handle()`
  now wraps its post-match body in its own try/except, denying on any
  exception downstream of a real match. 6 new tests (3 per guard).
- **m2** (`matches()`/`handle()` independent re-evaluation): both guards now
  cache one evaluation per dispatch (a `_DispatchKey`-keyed one-shot bridge,
  the same shape `sensitive_content` already uses), so a transient raise
  `matches()` saw cannot be silently overwritten by a clean re-evaluation
  inside `handle()`. 2 new tests, each confirmed RED against the pre-fix
  code path before the fix was restored.
- **n1** (exception message echoed unfiltered): both guards' error routes
  now put only the exception TYPE in the deny reason; the message stays in
  the log. 5 new/updated tests.
- **n3** (`enforce_llm_qa` data-consumer exemption too broad): the exemption
  now requires the head to be spelled exactly as the bare trusted word (no
  path prefix), and is voided for `git bisect run`, `git rebase -x/--exec`, `git -c alias.NAME=!…`, and `rg --pre`. 6 new tests.
- **n4** (no chain-level test caught m1): added
  `HandlerChain.execute(hook_input, strict_mode=False)` tests for both
  guards, confirming the fail-closed behaviour holds at the layer the
  review's own claim is actually about.
- **m3** (destructive_git's super-linear `strip_inert_spans`, pre-existing,
  out of this branch's diff per the review's own framing): recorded as a
  new ledger entry, N32, rather than fixed here.
- **m4** (ledger/release-note overstatement): corrected N5, N6, N10, N11 in
  NIGGLES.md and release notes 13 and 15 to remove/qualify the claims the
  review found false or since-closed by this session's own fixes. Team-lead
  additionally asked for the N5/N11 "strict_mode: true, so here the crash
  denied" text specifically — both corrected, pointing at N24 (already on
  main, out of this branch's scope) as the source of truth.
- New release notes 30-36 (this branch's own numbering range, team-lead's
  instruction — main's run to 20, a concurrent B2 branch takes 21-29,
  renumbered at integration) for the eight user-visible fixes above.
- M3/N24 (`daemon.strict_mode` inert) deliberately left untouched, per
  team-lead's explicit instruction — it is filed on main as N24 for a
  separate agent.

Targeted QA only, no new QA exclusions. Full targeted regression run before
committing: 645 passed (secret_file_matching, secret_file_guard,
project_containment, path_exclusion, flaggable_content_channel_guard,
quarantine_artefact_read_guard, payload_capture, enforce_llm_qa), plus the
full project-handler suite (216 passed) run separately after the n3 fix.

## 21:07 · finding · —   — Correction: dropped duplicate N32, m3 points at N25 instead

Correction to the prior finding entry: dropped the new N32 ledger entry for
m3's destructive_git `strip_inert_spans` slowness. Team-lead flagged that
this is already filed on main as N25 (a different, concurrent agent is
fixing it there with a linear rewrite plus a 200 KB timing test, alongside
the chain deadline), and that main's own N32 is already a different entry
(the pipe_blocker quoted backtick-pipe shape) — so a second N32 here would
collide at merge. NIGGLES.md and PLAN.md are reverted to not carry a new
entry; m3 is instead referenced by N25 in the phase-3 report. Confirmed
against `git show main:...NIGGLES.md` that N25 already covers both
timing measurements (destructive_git's 99s and this branch's own B1 DP
fix), so nothing from m3 is uncovered there except sensitive_content's
NUL-byte Write-path exception, noted separately in the report per
team-lead's instruction.

## 20:21 · action · —   — N3 majors/minors and N7 minor fixed from the review

Fixed all review findings against this branch
(`subagent-reports/260924-n466-review-opus-5-5.md`): majors M2 and M3, minors
m4/m5/m6, and nits n3/n6/n7 (n4 acknowledged, unchanged by design).

**M2**: `_maybe_refresh_on_retirement` gated on the in-memory `self._fired`
latch, which a daemon restart empties; N3's flip-only rule meant a later
non-flip write never re-latched either, so a plan flipped in one daemon
lifetime and completed in the next never dropped out of the combined
`/goal` signal. Fixed by asking the persistent `GoalLedger`
(`has_live_entry`) instead. RED test used a fresh handler instance to
simulate the restart.

**M3**: Plan 00269's "goal survives a session restart" contract (first edit
to an already-In-Progress plan in a NEW session re-fires) was removed with
no replacement. Restored via two new `GoalLedger` methods
(`session_has_entries`, `reassert_session` — ownership transfer only, no
displacement bookkeeping) and `_maybe_reassert_for_new_session`: a session
with zero ledger entries that touches an already-live plan without a real
flip gets its own signal, no new ledger record, no displacement advisory.
Release note 13 and the module docstring corrected.

**m4**: an Edit's `old_string` carrying only the bare status value (no
`**Status**:` prefix) missed a genuine flip. Fixed by reconstructing the
pre-edit text from disk (reversing the edit's own substitution) instead of
giving up when `old_string` alone has no Status line.

**m5**: a Write in a nested repository (linked worktree, any nested clone)
read HEAD from the project root's repo, which never tracks the nested path,
so it always answered "absent" and misread the write as a genuine flip.
`project_relative_head_text` now resolves the file's OWN enclosing repo via
`GitRepo.resolve_for` before reading HEAD.

**m6**: the N7 fix's own commit/ledger text blamed `pkgutil.walk_packages()`
for the unsorted discovery order; it actually already sorts. The real
source was `HandlerRegistry`'s two `event_dir.glob("*.py")` loops
(`registry.py:467`, `:511`), now wrapped in `sorted(...)` to match the
existing pattern at line 198. Ledger text corrected.

**n3**: `git_facts.py` had started importing `core.project_context`,
breaking its own "docs QA depends on this module alone" claim.
`project_relative_head_text` now takes `project_root` as a parameter.

**n6**: the promoted CLAUDE.md tier's alphabetical sort lost the config
author's `promoted_handlers` order (most-triggered guidance first). Now
sorted by each entry's index in that list instead, kept alongside the
pre-existing frozenset used for the membership check.

**n7**: release note 13's title said "already-terminal-status"; corrected
(In Progress is not terminal).

**n4**: acknowledged, not changed — `_is_inside_project` fails open on an
uninitialised ProjectContext while `project_relative_head_text` lets it
propagate; both new M2/M3 helpers now follow the propagate convention for
consistency (and because `error_hiding` flagged the None-returning one).
The review itself called this unreachable on the real dispatch path.

Targeted QA (format, lint, type_check, pyright, magic_values, error_hiding,
docs_qa, plan_qa) all pass, no exclusions. 518 tests pass across every
touched handler/utils/core/daemon test file. CLAUDE.md and
.claude/HOOKS-DAEMON.md regenerated in this worktree; verified idempotent
(regenerate-docs run twice, no further diff). Daemon restarted and confirmed
RUNNING before commit.

This entry re-records (via the stamping tool, current time) an activity log
entry originally written during an earlier segment of this same work, lost
to a merge-conflict resolution of this day-file against main's own N1/N8
entries -- the review-fix work it describes landed in commit `56d5b9a3`,
well before this entry's timestamp.

## 20:21 · action · —   — second review's RV-M1 and RV-m1..m5/n1..n3 fixed

Fixed all findings from the second review pass
(`subagent-reports/260924-n466-goalflip-review2-opus-5-5.md`): major RV-M1,
minors RV-m1 through RV-m5, nits RV-n1 through RV-n3. This review found the
first pass's M2 retirement-refresh and M3 re-assert worked AGAINST each
other, breaking retraction once a second session touched a plan.

**RV-M1**: `reassert_session`'s single-owner TRANSFER broke retraction the
moment a second session touched a plan, since `has_live_entry`'s exact
`session_id` match stopped matching the original flipping session. Fixed by
making ownership ADDITIVE: `GoalLedgerEntry` gained a `sessions: list[str]`
field that `record_emission`/`reassert_session` both append to, never
overwrite; a new `GoalLedger.owning_sessions(plan_number)` feeds
`_maybe_refresh_on_retirement`, which now refreshes EVERY owning session's
own combined signal on a terminal write. RV-m2 (a concurrent reconciliation
racing the terminal write) is subsumed by the same fix via a
terminal-status grace window on `owning_sessions`.

**RV-m3**: a same-session-id resume never got its `/goal` back, because the
M3 gate read the PERSISTED ledger, which still "knows" the session from its
own earlier real flip even after its signal file was lost across a
restart. Fixed with a second in-memory latch, reset every daemon lifetime
and independent of the real-flip latch.

**RV-m1**: the m4 reconstruction reversed the FIRST occurrence of
`new_string`, not necessarily the actual edit site -- a table cell or
title sharing the Status value's text could reconstruct the wrong span.
Fixed: `_is_flip_via_reconstruction` now tries every occurrence as a
candidate, keeps only those whose reversal leaves `old_string` unique (the
Edit tool's own precondition), and reports a flip only when every
surviving candidate agrees; `replace_all` instead requires `old_string`
absent from the post-edit text before reversing.

**RV-m5**: `GoalLedger.entries()` now catches `ValueError` (covers
`UnicodeDecodeError`) alongside `OSError`, so a non-UTF-8 ledger file is
treated as corrupt rather than raising past this fail-open API --
significant because this repo runs `strict_mode: true`.

**RV-n1**: corrected the m6 fix narrative in 4 locations
(`claude_md_injector.py`, `docs_generator.py`, both files' test
docstrings), which still blamed `pkgutil.walk_packages()` instead of
`HandlerRegistry.register_all`'s own previously-unsorted glob loops.

**RV-n2**: unified every ledger-opening call site in `GoalInjectionHandler`
behind one `_open_ledger()` helper, resolving the fail-open convention
split the first review's n4 left standing. Its failure path assigns to a
local instead of returning directly from the `except` block, since
`error_hiding`'s `return-none-on-error` check flags a literal
`return None`/bare `return` inside an except handler specifically.

**RV-n3**: closed the test gap for the RV-M1/RV-m1 scenarios (new test
classes `TestOwnershipSurvivesASecondSession`,
`TestResumedSameSessionReassertion`, plus RV-m1 collision tests in
`TestStatusFlipDetection`), and made the registry's
glob-order-independence test assert its `_sorted is False` precondition
explicitly instead of silently relying on it.

Release note 13 and the module/class docstrings in `goal_injection.py`
corrected again to describe the additive ownership and per-daemon-lifetime
reassert latch. NIGGLES.md N3 write-up extended with a "Third review pass"
section; N3 stays ✅ Remedied.

Targeted QA (format via black, ruff lint, mypy, pyright, magic_values,
error_hiding, plan_qa, docs_qa) all pass, no new exclusions. 320 tests pass
across the touched handler/utils/core/daemon test files. Commit:
`05557ece`. Daemon restarted and confirmed running before committing.

## 20:37 · action · —

Remedied N24: `daemon.strict_mode` never reached the live daemon.

Part 1: `DaemonController` gained a narrow-slice `_strict_mode`, threaded
through `initialise(strict_mode=...)` from `_build_initialised_controller`
(`daemon/cli.py`), mirroring the `chain`/`verdict_log` DI idiom. `process_event`
now reads `self._strict_mode` instead of the never-populated `self._config`.

Part 2: `HandlerChain.execute` now denies unconditionally, on a raise from
`matches()` or `handle()`, when the handler is tagged both `HandlerTag.SAFETY`
and `HandlerTag.BLOCKING` -- independent of `strict_mode`.

Added an acceptance-level live-daemon check
(`.claude/project-handlers/pre_tool_use/n24_strict_mode_probe.py` +
`tests/acceptance/test_n24_strict_mode_probe_socket.py`) using a
`synthetic_source: n24-probe` marker instead of depending on N5's lifecycle.

Corrected the N5/N11 false "this repository runs strict_mode: true, so here
the crash denied" claim in NIGGLES.md. Added release note 40 and a
truth-changes entry (v3.67.0.yaml, topic daemon-config) plus a post-upgrade
task (workflow-change, notification severity) for projects that may see new
denials from a previously-silent handler crash.

Targeted QA (format, lint, type_check, pyright, error_hiding, docs_qa,
handler_reference, doc_truth, plan_qa) all pass. tests/unit/daemon and
tests/unit/core: 4294 passed, 1 skipped (pre-existing, unrelated). Live
acceptance probe passes against this worktree's own restarted daemon.

## 21:14 · action · —   — N25 chain deadline + linear strip_inert_spans + hostile-input harness remedied

N25 (a slow handler runs out the client's 30s budget, and a client timeout
is an ALLOW for the whole PreToolUse chain) is remedied on this branch,
alongside N24.

All three candidate remedies landed:

1. `ChainConfig.deadline_seconds` (default 20s, `Timeout.CHAIN_DEADLINE_DEFAULT`)
   is threaded through `DaemonController.initialise()` -> `process_event()`
   -> `EventRouter.route()` -> `HandlerChain.execute()`, the same narrow-slice
   DI idiom N24 used for `strict_mode`. Once the per-event deadline is
   exceeded mid-chain, every remaining `SAFETY`+`BLOCKING` handler is denied
   under N24's fail-closed rule ("not judged in time"); every other
   remaining handler is skipped with an advisory note. `deadline_seconds: null` disables enforcement.
2. `strip_inert_spans` (`shell_segmentation.py`) is now linear. Three
   separate per-match helpers were re-deriving prefix-dependent state from
   scratch (`command[:match_start]`) instead of advancing incrementally in
   guaranteed left-to-right match order; replaced with three stateful
   tracker classes. Review 2's exact repros: 99.179s -> 0.139s (40000
   repeated `-m` flags), 98.342s -> 0.453s (5000 quoted-delimiter heredocs).
3. `tests/unit/handlers/test_safety_handlers_hostile_input_performance.py`
   drives all 23 `HandlerTag.SAFETY` `pre_tool_use` handlers with hostile
   quote/backslash/wildcard/nesting shapes under a 5s bound at 100 KB. Found
   no other super-linear handler. `SecretFileGuardHandler` is notably slower
   than its peers but confirmed LINEAR up to 800 KB, so it is noted, not
   fixed — out of this niggle's scope.

Targeted QA (format, lint, type_check, pyright, error_hiding, docs_qa,
handler_reference, doc_truth, plan_qa): 9/9 passed.
`tests/unit/daemon` + `tests/unit/core` + `tests/unit/config/test_chain_config.py`

- the two new performance test files + `tests/unit/handlers/pre_tool_use/`
- `tests/unit/handlers/test_destructive_git.py`: 8370 passed, 1 pre-existing
  unrelated skip, 2 pre-existing unrelated xfail.

NIGGLES.md and PLAN.md both updated to ✅ Remedied for N25. Release note 41
and a `v3.67.0.yaml` truth-changes entry added.

## 21:38 · action · —   — N24 NUL-byte realpath sweep: 4 handlers fixed, sensitive_content denies explicitly

N24's NUL-byte realpath sweep is done, added to the same branch.

The guard-defects fuzzer (review 2, m3) found `sensitive_content` raising
`ValueError: embedded null byte` from a `realpath` call on 485/12000 fuzzed
Write/Edit paths. With N24's Part 2 fix already landed this had stopped
being a silent bypass (it now denies as "evaluation error, denied for
safety" via the chain-level fail-closed catch-all), but the raise itself
was still worth fixing at its source, and sensitive_content specifically
gets a clearer, handler-level reason.

Root-caused and fixed four places:

1. `core/workspace.py`'s `ProjectRegistry.for_path`/`layout_for` — shared
   root cause behind THREE handlers via the `Handler.layout_for()` base
   method (`error_hiding_blocker`, `security_antipattern`,
   `secret_file_guard`'s `_is_excluded`), plus `sensitive_content` itself.
   New `_resolve_or_self` helper catches `(OSError, ValueError)`, falls back
   to the unresolved path (which safely fails to match any real declared
   project), matching both methods' own "never returns None" contract.
2. `secret_file_guard`'s own separate raise in
   `utils/secret_file_matching.py`'s `path_is_protected` — widened
   `except OSError` to `(OSError, ValueError)`.
3. `issue_filing_gate`'s `_read` (`path.stat()` on a `--body-file` path) —
   same widening.
4. `project_containment` was already safe (its existing `except ValueError:`
   already covers `.resolve()`'s NUL-byte raise the same way it covers
   `.relative_to()`'s mismatch).

`sensitive_content` gets a DIFFERENT, deliberate treatment: a Write/Edit
`file_path` carrying a NUL byte is now denied OUTRIGHT with a clear reason
("file_path contains an embedded NUL byte...") before any haystack
computation runs — a NUL byte is a classic path-truncation attack shape, so
letting the now-safe fallback silently continue to ordinary scanning would
have been the wrong verdict even though it no longer crashes.

Swept all 23 `HandlerTag.SAFETY` `pre_tool_use` handlers (the same dynamic
discovery N25 Task 3 used) with NUL-bearing Write/Edit/Bash payloads after
all four fixes: none raise.

TDD throughout — RED test per fix:
`tests/unit/core/test_project_registry.py::TestAnUnresolvableFilePathDoesNotRaise`,
`tests/unit/utils/test_secret_file_matching.py::TestPathIsProtected::test_nul_byte_in_path_does_not_raise`,
`tests/unit/handlers/pre_tool_use/test_issue_filing_gate.py::TestABodyItDidNot::test_a_nul_byte_in_the_body_file_path_is_denied_not_raised`,
`tests/unit/handlers/pre_tool_use/test_sensitive_content.py::TestNulByteFilePathIsDenied`.

Targeted QA (format, lint, type_check, pyright, error_hiding, docs_qa,
handler_reference, doc_truth, plan_qa): 9/9. Broad regression
(`tests/unit/handlers/pre_tool_use/`, `test_destructive_git.py`,
`tests/unit/core/`, `tests/unit/daemon/`, `test_acceptance_contract.py`,
`test_every_handler_response_validates.py`, `test_rule_parity.py`, plus the
four new/touched test files): 8718 passed, 1 pre-existing unrelated skip, 2
pre-existing unrelated xfail.

NIGGLES.md's N24 entry updated in place with this follow-up write-up.

## 21:52 · action · —   — N24 combinatorial harness extension (review 3) + secret_file_guard 1MB/4MB timing, N34 filed

Guard-defects review 3 follow-up: combinatorial hostile-input harness
extension, plus secret_file_guard's deadline margin at 1 MB/4 MB.

Extended `tests/unit/handlers/test_safety_handlers_hostile_input_performance.py`
with `TestCombinatorialSmallInputShapesStayLinear`: brace expansion
(x16/x20/x24), nested braces, `/**/` and `**/*` globs, bracket classes, and
deep `eval`/`bash -c` nesting -- all under 300 bytes, applied to both a
Bash command and a Write file_path. This is a different failure class from
the existing 100 KB-scale shapes: state space grows with a repetition
count/nesting depth, not with byte count (review 3 measured `echo {a,b}`
x20, 110 bytes, at 36s on one guard).

Also swept this repository's own `.claude/project-handlers/` best-effort
(`enforce_llm_qa` included) via the same `ProjectHandlerLoader` the daemon
uses -- 28 handlers total (23 built-in SAFETY + 5 project handlers).

Result: clean on this branch, 27/27 pass in ~7s, confirmed at higher
repetition counts too (28/32/40/60) via an ad hoc probe. Per
untracked/scratch/coordinator-queue.md, the actual review-3 bugs
(exponential brace expansion, the /\*\*/ FS walk, the enforce_llm_qa
regex/braces issue) live on the guard-defects/463 branches, which own
those modules and are still being fixed there -- this branch's current
code does not exhibit them, so a clean sweep here is the expected,
correct result. The harness itself is the deliverable: a "class detector"
that will catch a regression of this shape wherever it lands.

Measured secret_file_guard's wall-clock at 1 MB and 4 MB as requested:

- 1 MB: 12.315s (61.6% of the 20s chain.deadline_seconds budget)
- 4 MB: 48.958s (244.8% -- past BOTH the 20s daemon deadline AND the 30s
  client socket timeout)

This is still LINEAR scaling (consistent with the up-to-800KB measurements
under N25 Task 3), just with a steep constant factor (~12us/byte). More
importantly, it surfaces a real gap in N25's own deadline enforcement:
`HandlerChain.execute`'s deadline check runs BETWEEN handlers, with no way
to interrupt one already running -- so a single handler slow enough within
its OWN execution reproduces the exact bypass N25 was built to close,
regardless of the inter-handler check. Filed as a new niggle, N34, with
three candidate remedies (cheapen secret_file_guard's constant factor; an
externally-enforced per-handler wall-clock budget; a hard size cap). Not
fixed here -- the ask was specifically to measure and report the margin.

Targeted QA (format, lint, type_check, pyright, error_hiding, docs_qa,
handler_reference, doc_truth, plan_qa): 9/9. Broader regression
(test_safety_handlers_hostile_input_performance.py, tests/unit/daemon,
tests/unit/core, tests/unit/config/test_chain_config.py): 4339 passed, 1
pre-existing unrelated skip.

Merged main (57a255cb..3104434b) to pick up N32/N33 before adding N34.

## 22:28 · finding · —   — N34 remedied: bounded per-handler dispatch + SAFETY size cap

N34 remedied: an externally enforced per-handler chain deadline plus a
SAFETY input-size cap, closing the gap where `HandlerChain`'s own deadline
check only ran BETWEEN handlers and could not bound a handler slow within
its own `matches()`/`handle()` call.

New module `core/bounded_dispatch.py` (`BoundedDispatcher`): runs a
handler's combined `matches()`+`handle()` call on its own daemon thread and
waits on it via `Future.result(timeout=remaining)`, where `remaining` is
whatever is left of the chain's `deadline_seconds` budget. On expiry the
same N25 fail-closed verdict applies (SAFETY+BLOCKING denies "not judged in
time"; anything else is skipped with an advisory), now shared between the
pre-check and this new post-dispatch-timeout case via one `_record_unjudged`
helper in `chain.py`. The overrunning call is abandoned, never killed
(Python cannot force-stop a thread), and logged again at WARNING with its
real elapsed time whenever it finishes. Concurrency is bounded via a
semaphore (16 calls at once by default); beyond that, `DispatchSaturated` is
returned immediately and treated the same as a timeout.

Found live during this work: the FIRST implementation used
`concurrent.futures.ThreadPoolExecutor`, whose worker threads are
non-daemon and get JOINED by a CPython `atexit` hook before the interpreter
may exit -- so a single abandoned straggler hung the daemon's own `stop`
command for the length of its sleep. Caught by the new end-to-end test
(`tests/integration/test_n34_deadline_probe_isolated_daemon.py`, which
starts a real isolated daemon and stops it after a call). Fixed by
rewriting the dispatcher onto plain `threading.Thread(daemon=True)` per
call, which is never waited on at interpreter exit.

Remedy 3 (defence in depth, explicitly requested alongside remedy 2): new
`daemon.chain.max_safety_input_bytes` config key (default 2 MiB), checked
once per chain execution against a handler's bulk-text `tool_input` fields
(Bash `command`, Write `content`, Edit `old_string`/`new_string`). A SAFETY
handler over the limit is denied/skipped BEFORE dispatch is even attempted.
Not the only guarantee -- the per-handler deadline bound already caps
worst-case wall clock regardless of size -- so raising or disabling it
(`null`) stays safe as long as `deadline_seconds` is enforced.
`secret_file_guard`'s own constant factor was deliberately left alone per
the coordinator: guard-defects is rewriting `secret_file_matching` onto a
shared bounded expander and will profile that cost there.

RED tests: `tests/unit/core/test_bounded_dispatch.py` (5 tests: completes
within budget, propagates an exception, times out without waiting for
completion, the stray worker still finishes in the background, saturation
fails fast) plus new cases in `tests/unit/core/test_chain.py` (a SAFETY
handler that oversleeps ITSELF denies within the deadline; a slow
advisory-only handler allows with an advisory the same way; pool saturation
denies; the size cap denies/skips/passes-through in each direction;
`deadline_seconds: null` leaves an oversleeping handler fully unbounded,
confirming the mechanism is disabled exactly like the old one). The
existing `test_deadline_exceeded_denies_a_safety_blocking_handler_not_yet_run`
test was updated to poll for the (now background) slow handler's completion
instead of asserting it synchronously, since it is no longer guaranteed to
have finished by the time `execute()` returns.

Also addressed the still-open combinatorial-harness/NUL-byte-sweep asks
from the team lead's earlier messages: reproduced the team lead's exact
example payload (`cat .vault-password; echo {a,b}...` x20) against every
SAFETY handler on this branch, including via a runtime-constructed literal
so the payload never appears verbatim in a Bash/Write call the daemon's own
guards would flag -- found no reproduction. Per the coordinator's
queue, the actual review-3 combinatorial bugs (exponential brace expansion,
the /\*\*/ FS walk, the enforce_llm_qa regex/braces issue) live on the
guard-defects/463 branches, which own those modules and are still fixing
them there; this branch's current code does not exhibit them, and no
xfail(strict=True) markers were added since strict xfail would itself fail
on a test that currently passes.

Config/docs: new `daemon.chain.max_safety_input_bytes` field in
`config/models.py` (ChainConfig), threaded through `router.route` and
`daemon/controller.py` (narrow config-slice DI, the same idiom as
`deadline_seconds`). `Timeout.SAFETY_INPUT_SIZE_CAP_BYTES` constant added.
Release note 42 and a new v3.67.0.yaml truth-changes entry
(`chain-deadline-per-handler-enforcement`) record the behaviour change.

QA: targeted gates (format, lint, type_check, pyright, error_hiding,
docs_qa, handler_reference, doc_truth, plan_qa) 9/9. Full regression
(tests/unit/core, tests/unit/daemon, tests/unit/config, the combinatorial
hostile-input harness): 4659 passed, 1 pre-existing unrelated skip. The two
isolated-daemon e2e suites (N24 strict_mode probe + N34 deadline probe): 6
passed.

N34 marked Remedied in NIGGLES.md and PLAN.md.
