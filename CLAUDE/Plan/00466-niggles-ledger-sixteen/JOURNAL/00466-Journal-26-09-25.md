# Plan 00466 — Journal 26-09-25

_Scaffolded by `mkplan.bash`; timestamps in this file are UTC._

> **Append-only activity log** for plan 00466. One file per day
> (`00466-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM, ONLY with
> `mkplan.bash --journal <plan-number> <category> <body-file>` (it stamps the
> real UTC time; a hand-written entry is denied); NEVER edit earlier entries
> (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` UTC 24h (the date is in the filename, also UTC); times increase
>   down the file. A file with no sentinel line above predates this system —
>   its times are local, zone unrecorded (legacy; never rewritten).
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff` | `correction`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - A `correction` corrects an earlier entry without touching it. Its `REF`
>   (`--ref`, required) names that entry: `HH:MM` in this file, or
>   `YY-MM-DD/HH:MM` in an earlier one. An entry a correction names in this
>   file no longer counts for time order, so a future-dated stamp can be
>   corrected at the honest time.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 02:05 · finding · —

## finding · N39 — diagnosed, not committed: main is clean, both defective branches are git-isolated

Reproduced N39 on `main` (`c92bfbc1`): sequential `pytest tests/unit -p no:xdist -q` gives 22451 passed, 0 failed. Main does NOT reproduce it.
Reproduced on `worktree-n466-guard-defects` (`ef0600ed`): same run gives 9
failed, 22606 passed — exactly the reported files (one, `test_lookup.py`, is
`tests/unit/rule_explain/test_lookup.py`, not `remote_docs/test_lookup.py`).

Root cause bisected to three exact test methods in
`tests/unit/handlers/pre_tool_use/test_project_containment.py`
(`TestFailsClosedOnEvaluationError::test_an_uninitialised_project_root_still_denies`
and `::test_an_evaluation_error_denial_uses_its_own_rule_id`, added by commit
`d0de526d1`; `TestChainLevelFailClosedBehaviour::test_an_evaluation_exception_still_denies_through_the_chain`,
added by commit `3e3164bd`): each layers a `monkeypatch.setattr` on
`ProjectContext.project_root` on top of the file's own class-wide autouse
`unittest.mock.patch` fixture for the SAME attribute. `monkeypatch`'s
finalizer runs after that fixture's `with patch(...)` has already exited and
restored the true original, so it overwrites it AGAIN with the intermediate
mock — permanently, for the rest of the pytest process. Confirmed with a
20-line repro isolated from this project's own code
(`untracked/scratch/repro_fixture_order/test_order.py`, not committed).
Every later `ProjectContext.project_root()` call then silently returns the
fake `/repo` instead of raising, explaining all four victim files (full
mechanism per file is in NIGGLES.md).

Could not commit the fix: this agent's own worktree is based on `main`,
which does not contain the defect, and `worktree-n466-guard-defects` is a
separate git-isolated worktree — verified directly that `git worktree add`
of a brand-new path is refused with "a worktree-isolated agent's git
operations must target its own worktree", so there is no route to commit
there from here either.

Mid-task, the coordinator widened N39's scope to three more defects found on
`worktree-n466-goal-flip` (tip `95ac6de9`, whole-suite `pytest tests/ -q`: 1
failed, 26782 passed, 13 errors): a docs-rewrite-mid-run (evidence points to
an EXTERNAL daemon restart for that worktree at `01:48:53`, second-precise
against `CLAUDE.md`'s mtime, not a test — pid 612977 was still running that
restart), a plain fixed-`asyncio.sleep(0.1)` race in
`test_bind_time_rmtree_failure_is_logged_not_swallowed[asyncio]` (the same
shape appears 13 times in that one file alone), and 10 acceptance tests
ERRORing because `blocking_gate_guard.py`'s skip-to-failure escalation fires
on file identity alone with no signal distinguishing RELEASING.md's own
Step 12.0 invocation from an ad hoc whole-suite run with no daemon running.
Same worktree-isolation constraint applies; all three are diagnosed to a
concrete fix recipe in NIGGLES.md, none committed.

NIGGLES.md's N39 entry is fully rewritten with the diagnosis, evidence and
fix recipes for all four defects (the original plus the three widened ones).
PLAN.md's N39 row stays "In progress" — nothing was actually fixed anywhere,
so marking it Remedied would misrepresent the state.

## 02:37 · finding · —

Coordinator widened N39's scope mid-task to cover three suite-isolation
defects found on worktree-n466-goal-flip: a docs-rewrite-mid-run, a
fixed-sleep timing race in test_event_socket_listeners.py, and
blocking_gate_guard.py's skip-to-failure escalation ERRORing on a plain run.

The docs-rewrite and the git-worktree-isolation constraint were diagnosed and
recorded in a prior journal entry today. This entry covers the coordinator's
follow-up: two of the three (the socket-listener race and blocking_gate_guard)
turned out to live on main itself, reachable from this main-based worktree,
and were fixed here rather than only documented.

Socket-listener race: added HooksDaemon.started_event (asyncio.Event, TDD'd),
set in start() after both bind steps complete. Converted the 13 original
asyncio.sleep(0.1)-then-assert call sites in test_event_socket_listeners.py to
await daemon.started_event.wait() bounded by Timeout.SOCKET_CONNECT. Swept the
rest of tests/ for the identical daemon-startup-race shape and converted 4
more files the same way (test_server_response_schema.py,
test_relay_event_socket_real_payloads.py,
test_event_socket_hook_event_name_enrichment.py, test_server.py,
test_log_level_override.py). Inspected test_server_liveness_reuse.py and
test_parallel_start_reuse.py and left them alone -- both already use a proper
bounded poll or an unrelated background-thread loop.

blocking_gate_guard.py: added HOOKS_DAEMON_RELEASE_GATE env-var gating so a
plain whole-suite run SKIPs a daemon-dependent blocking-gate file (exit 0)
while RELEASING.md's own release-gate invocation and CI's qa.yml (which now
export the variable) still escalate a skip to a hard failure. Verified both
modes end-to-end against test_playbook_harness.py. Added 6 unit tests in
tests/unit/scripts/test_blocking_gate_guard.py.

Docs-rewrite-mid-run: coordinator accepted the external-daemon-restart
evidence from the prior entry as the explanation, no test-side leak to fix.
Recorded as a procedure note (do not restart this project's daemon during a
test run) and sharpened tests/conftest.py's
no_test_writes_tracked_generated_docs fixture to name that specific external
cause (a pid-file-mtime comparison across the fixture's window) instead of
only the generic "suspect an external edit" text, when it applies. 3 new
regression tests for the extracted _daemon_pid_file_mtime helper.

The original N39 defect (test_project_containment.py's two-layer-patch leak)
remains diagnosed but unfixed on this branch -- it is being fixed on
worktree-n466-guard-defects by a different agent, per the fix recipe already
recorded in NIGGLES.md.

QA: 24/24 check_*.py-backed llm_qa.py tools pass (0 violations across all).
format and lint pass. All touched/added test files pass (132 + 6 + 3 = 141
tests green).
