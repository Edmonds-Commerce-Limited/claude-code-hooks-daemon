# N24 fix round 8 (Sonnet 5): review 4 blockers + majors

Commit: `b34731b0d`, on top of `7917e1269` (review 4's own commit `6801b4a96` added first, then this one).

## R4-B2 (blocker) — fixed

`TestCmdStop` and `TestCmdStopGenericException` (`tests/unit/daemon/test_cli_commands.py`)
patched only `os.kill`. Since the pidfd change, `cmd_stop` signals through
`os.pidfd_open` + `signal.pidfd_send_signal`, neither of which was patched, so
a real pid 12345 got a genuine SIGTERM/SIGKILL whenever that pid happened to
be live on the host — the N59 crash class.

- RED proof: `probe_n24_fix8_hazard.py` (spawned a real decoy child in place
  of `12345`, replayed `test_stop_timeout`'s exact patch set) — the decoy died
  with a real SIGTERM despite `os.kill` being patched.
- Fix: every test in both classes now uses `_UNREAL_PID` (moved to module
  scope). Added `_reject_unproven_real_signals`, an outermost-patch guard
  fixture applied to both classes that fails the test outright if
  `os.pidfd_open`, `signal.pidfd_send_signal`, `os.kill` or `os.killpg` is
  reached with any pid other than `_UNREAL_PID`.
- Guard self-check: regressed one test's pid back to a real-looking number in
  a scratch copy and confirmed the guard fires with `pytest.fail` *before*
  any real syscall — not a bypass, a hard stop.
- `tests/unit/daemon/test_cli_commands.py`: 49/49 pass.

## R4-B1 (blocker) — fixed

`utils/realpath.py`'s hand-written walk matched CPython 3.11's symlink-loop
handling but diverged from 3.13's rewrite: a loop followed by `..` resolved
differently, and a Write through it bypassed `project_containment` on 3.13
(confirmed both ways with the reviewer's own `probe_n24r4_venv313`).

- Deleted the custom `_join_realpath`/`_join_missing` walk. `realpath()` now
  delegates straight to `os.path.realpath(path, strict=False)`.
- Removed `test_a_deep_missing_path_costs_logarithmically_many_lstat_calls`
  (encoded the old fast path's O(log depth) guarantee, which no longer
  holds — traded back for correctness per the assigned fix, since the walk
  it verified was exactly the divergent code). `test_cost_grows_linearly_with_depth`
  stays and still passes (linear cost, well under the superlinear-ratio
  threshold).
- Added a loop-then-`..` case to `test_realpath.py`'s shape list, and a new
  `TestASymlinkLoopFollowedByDotDotOutOfTheRoot` in
  `test_project_containment.py`. Its oracle is `os.path.realpath` itself,
  computed at test time, rather than a hardcoded DENY — `os.path.realpath`'s
  OWN loop-then-`..` answer is version-dependent (3.11 bails out of the loop
  and appends the remainder unresolved, landing inside root; 3.13 backs out
  and keeps resolving, landing outside), and the handler's job is now just to
  agree with whichever answer the running interpreter gives.
- Verified on both interpreters: `.venv` (3.11) and the review's own
  `probe_n24r4_venv313` (3.13, `PYTHONPATH=src`, `ensurepip` + `pip install mdformat` to unblock the handler import chain). `test_realpath.py` +
  `test_project_containment.py`: 154/154 pass on 3.11, 154/154 on 3.13.

## R4-MA1 (major) — fixed

`init.sh`'s PreToolUse branch denied only a fixed allowlist of named
`error_type`s (`socket_timeout`, `malformed_response`, `connection_lost`,
`connect_backlog_full`, `socket_not_found`, `connection_refused`); anything
the transport's `except` clauses did not name explicitly (`PermissionError`
from a chmod'd socket, `NotADirectoryError`, an over-long socket path, any
other `OSError`) fell into the generic `except Exception` and then to
`emit_error_json`'s fail-open `else` branch.

- Inverted the default: PreToolUse now denies for every `error_type` except
  `invalid_hook_input` and the exact daemon-recovery command.
- Added a version-aware `verb` (`reached`/`responded` for the four
  post-connect types, `unreachable` for everything else including every
  newly-covered unclassified type) so the deny text never claims a daemon was
  "reached" when `connect()` itself never succeeded.
- New tests in `test_init_sh_pretooluse_fail_closed.py`
  (`TestUnclassifiedConnectErrorsFailClosedForPreToolUse`): a real
  `NotADirectoryError` (a socket-path component that is a plain file) and a
  real `OSError`/ENAMETOOLONG (a path past AF_UNIX's ~108-byte `sun_path`
  cap) both DENY end-to-end, the recovery command still isn't denied, and a
  non-PreToolUse event still fails open. RED-checked against the pre-fix
  `init.sh` (`6801b4a96`) with a standalone repro script — confirmed
  fail-open there, fail-closed on the fix.
- `test_init_sh_pretooluse_fail_closed.py`: 25/25 pass.

## R4-MA2 (major) — fixed

`emit_hook_error`'s PreToolUse deny (an installed project whose daemon could
not be reached at all) reused `context_msg` — the SAME fail-open text used
elsewhere, which says "Hook safety handlers are inactive" (false; they are
actively denying) and tells the agent to use the Skill tool (itself a
PreToolUse call, denied the same way — a loop for an unattended agent).

- Built a dedicated `_pretooluse_deny_msg`, used only when `_pretooluse_deny`
  is `"true"`, in both the `jq` and jq-less encoders. States plainly that
  this is a deny-for-safety (not "inactive"), names the one command that is
  actually allowed (`bin/hooks-daemon restart`), and the human fallback
  (`!`) — no Skill tool mention.
- Updated `tests/unit/test_ci_passthrough.py`'s
  `test_non_ci_installed_but_not_running_shows_restart_guidance` (asserted
  the OLD wording) and added
  `test_fallback_pretooluse_deny_text_is_honest_and_names_no_denied_tool` in
  `test_emit_hook_error_jqless.py`.
- `test_ci_passthrough.py`: 20/20 pass. `test_emit_hook_error_jqless.py`:
  8/8 pass.

## Wider verification

- `bash -n init.sh`: clean. `shellcheck init.sh`: clean.
- 42 init.sh/emit_hook_error-related test files (431 tests total): all pass.
- `ruff`, `black --check`, `mypy`, `pyright` on every touched file: clean.
- Daemon restarted from this worktree before the commit; `bin/hooks-daemon status` reports RUNNING, 31/31 per-event listeners active.
- Did NOT run the gate (per instructions) and did NOT touch anything
  outside the assigned four findings.

## Not touched (out of scope for this round)

Review 4's ledger items (mi-A launcher-resolution, mi-B "reached" wording on
`socket_not_found`/`connection_refused`, mi-C moved-clone fail-open, the two
pidfd NITs, nit 4's relay timeout label) are explicitly "not merge-blocking"
in the review and were not part of this assignment.
