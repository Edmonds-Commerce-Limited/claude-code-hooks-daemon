# N84/N90 landing merge (Opus 5.5)

Worktree: `untracked/worktrees/worktree-n466-n84-n90`. Merge commit
`c0bcecdd2` (main merged into the branch at `8cb6ba04a`). Nothing pushed, no gate
queued.

## Conflict resolutions

- **`CLAUDE/Plan/00466-niggles-ledger-sixteen/PLAN.md`**: I ran
  `merge_ledger_table.py`, which reported 97 rows and N84/N90 differing from main.
  I then made the N1 row edit (`| N1 |` to `| N1  |`). The merged ledger has 97
  distinct N-ids and no duplicates, which is exactly the union of HEAD's 90 ids
  and main's 97 ids. N84 and N90 both show Remedied.
- **`project_containment.py`** (security):
  - On main (N11/N24 guard-defects), `_resolved_root()` moved into the
    fail-closed wrapper `_offending_targets_or_error`. That wrapper passes
    `root` into `_offending_targets(hook_input, root)`.
  - On the branch (N90), `_offending_targets` returned early before it
    resolved the root.
  - Resolution: the early return is now inside the wrapper's `try`, before
    `_resolved_root()`. When no targets are named, it returns
    `([], None, None)` and `handle()` allows before it reaches its
    `root is not None` assert.
  - A raise from `_named_targets` or `_resolved_root` still returns the error
    tuple, which denies with `PROJECT_CONTAINMENT_EVALUATION_ERROR`.
  - `_offending_targets` now takes `named_targets` as a parameter so the
    targets are not parsed twice.
  - No path fails open.
- **`test_project_containment.py`**: kept both sides' classes,
  `TestAnUnresolvedRootDoesNotLockOutTargetlessCommands` and
  `TestProjectRootDoublePatchDoesNotLeakAcrossFiles`.
  - The branch's targeted-command test expected
    `pytest.raises(RuntimeError)`. That expectation predates N11 and is wrong
    under main, so the test now asserts `matches() is True` and a DENY
    carrying the evaluation-error rule id.
  - New test: `test_a_raise_while_naming_targets_still_denies` pins that the
    early return cannot turn a fault into an allow.
- **`test_daemon_smoke.py`**: the fixture teardown combines main's
  `--project-root` stop command with the branch's N84 check (pid read before
  stop, `stop_result`, `_assert_stopped_cleanly`).
  - mypy also reported `no-any-return` in `send_hook_event`. Main's copy has
    the same error, so it is not new. It sits in a file this merge touches, so
    I fixed it with a typed local.

## Release note

The brief said to use 110. **110 fails
`test_pending_release_notes_holding_area.py`**: its regex is `^\d{2}-`, and the
release-notes README says `NN` is a two-digit ordinal. Across main and every
worktree, the only unused two-digit numbers were 98 and 99, so the note is
`98-a-no-target-command-is-no-longer-denied-when-the-project-root-is-unresolved.md`.
It keeps `**Plan**: 00466` and `**Audience**: operators`. Its body now names
`_offending_targets_or_error()` as the site of the early return. The
coordinator should confirm 98 against its claims list.

## Results

- `tests/unit/handlers`, `tests/unit/core`, `tests/unit/utils`,
  `tests/integration/test_daemon_smoke.py` and
  `tests/integration/test_pending_release_notes_holding_area.py`, with the perf
  file deselected: 14171 passed and 1 failed. The failure was the 110 callout
  name. After the rename to 98, the holding-area and project_containment files
  pass (212 passed).
- `test_safety_handlers_hostile_input_performance.py`: 1 failed, 30 passed,
  not fixed (worktree-n466-superlinear owns it). The failing test is
  `test_no_handler_grows_superlinearly_on_a_deep_write_path`. It names four
  handlers, not only ProjectContainment: ProjectContainmentHandler 53x,
  PlanJournalGuardHandler 42x, InstalledPluginEditAdvisorHandler 43x and
  DocsQaEditHandler 35x, each for 8x path depth.
- ruff, black, mypy and pyright on the 3 changed Python files: clean.
- Worktree daemon restarted: RUNNING.
