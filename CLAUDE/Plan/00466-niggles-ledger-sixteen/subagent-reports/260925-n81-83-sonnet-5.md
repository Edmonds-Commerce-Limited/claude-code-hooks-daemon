## N81-N83 fix report

Branch: `worktree-n466-n81-83` (worktree at
`/workspace/untracked/worktrees/worktree-n466-n81-83`).

- **N83** (`21a134f1e`): `tests/unit/qa/test_llm_qa_live_daemon.py` excluded
  `tests` from the parametrisation at collection time instead of calling
  `pytest.skip` inside it, and added
  `test_tests_is_excluded_because_its_script_carries_no_live_daemon_marker`
  pinning why: `run_tests.sh`'s own text carries none of the
  `_LIVE_DAEMON_MARKERS`, since `tests` only reaches the daemon indirectly
  through `tests/acceptance`.

- **N82** (`637fc735a`): the registry already derives a handler's config key
  from its `HandlerID` constant (`_get_config_key_from_constant`, with a
  logged fallback to `_to_snake_case` only for an undeclared class) --
  `tests/unit/handlers/test_config_key_consistency.py`'s unconditional
  `pytest.skip` was replaced with a real assertion over every handler
  `iter_builtin_handler_classes()` yields, checking none of them fall back.

- **N81** (`e445fecc3`): added a fifth `sed_blocker` exemption, using
  `bash_write_destinations()` (the shared, public redirect-target parser
  `project_containment` and `get_written_file_paths()` are built on, not a
  second regex) -- a Bash command is exempt only when every AUTHORED
  destination ends `.md` and `_executes_sed` (already used elsewhere in the
  handler) is False. Flipped the strict `xfail` in
  `tests/unit/handlers/test_sed_blocker.py` to a passing test and added the
  four adversarial "stays denied" cases from the ledger entry (unquoted
  `$(sed -i ...)` heredoc body, heredoc piped to `bash`, mixed `tee a.md b.sh` target, `> x.md; sed -i ...` chain). `get_claude_md()` and the class
  docstring now state the Bash `.md` exemption is narrower than `Write`'s
  unconditional one.

- Ledger wrap-up (`e7c2d8bf3`): `PLAN.md` N81-N83 rows flipped to
  ✅ Remedied; `NIGGLES.md` entries carry each fix's commit SHA.

Targeted tests (`tests/unit/qa/test_llm_qa_live_daemon.py`,
`tests/unit/handlers/test_config_key_consistency.py`,
`tests/unit/handlers/test_registry_builtin_iteration.py`,
`tests/unit/handlers/test_sed_blocker.py`,
`tests/unit/core/test_bash_write_destinations.py`, plus the handlers
referencing `SedBlockerHandler` in `tests/unit/daemon/` and
`tests/unit/utils/`) all pass. ruff, black and mypy are clean on every
touched file; pyright reports 0 errors (run with
`--pythonpath .venv/bin/python` since `untracked/lsp-venv` is not
provisioned in this worktree). The worktree daemon was rebuilt via
`bin/hooks-daemon repair` (no venv existed yet) then restarted and verified
RUNNING before the N81 (src/) commit.

The release gate was queued in the background
(`bash /workspace/untracked/scratch/gate.sh worktree-n466-n81-83`); its
result lands in `/workspace/untracked/scratch/gate-worktree-n466-n81-83.out`.
