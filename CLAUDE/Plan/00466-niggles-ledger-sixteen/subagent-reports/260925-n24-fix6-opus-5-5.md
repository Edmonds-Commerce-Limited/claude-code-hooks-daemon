# N24 review 3 fix round 6 — progress notes

Predecessor (n24-5, cut off by usage limit) had already committed:

- `c91385ede` B1: `utils/realpath.py` now matches `os.path.realpath` exactly (differential test + containment e2e).
- `b406b205a` MA5 (partial, error_hiding) + mi1: typed `RootProof`; `stop` refuses on a root mismatch without deleting anything; `secret_file_matching`'s realpath fallback now logs.
- Uncommitted: vendored `remote-docs/code.claude.com/docs/en/interactive-mode.md` fetch (for MA4's doc check) + `.claude/REMOTE-DOCS.md` index row.

This session (fresh agent) continuing from there, in-order:

## MA3 — DONE

Removed the two unescaped backticks from `init.sh`'s double-quoted `python3 -c "…"` comment body (lines ~1976-1982). `tests/integration/test_client_owned_asset_lint.py -k shell` (shellcheck SC2006) now passes. Not yet committed.

## MA1 — DONE

`init.sh`: added `except BlockingIOError as e:` before the generic `except Exception`, classified as new `error_type='connect_backlog_full'`, added to the PreToolUse fail-closed allowlist tuple (alongside `socket_timeout`, `malformed_response`, `connection_lost`) and given its own `emit_error_json` context-lines branch. New test class `TestBacklogFullFailsClosedForPreToolUse` in `tests/integration/test_init_sh_pretooluse_fail_closed.py`, with a `backlog_full_socket` fixture that fills a real listen(100)-backlog AF_UNIX socket via non-blocking connects (same technique as `probe_n24r3_gil.py`) without ever accepting. RED confirmed against the pre-fix `init.sh` (KeyError on `permissionDecision` — fell to fail-open), GREEN after the fix. All 16 tests in that file pass.

The relay side needs no separate code change: production always passes `--fallback`, and `connect_fail` execs the bash forwarder with stdin intact before any bytes are consumed, so a relay-side connect timeout against a full backlog lands on this exact python-rung fix. Verified by reading `relay/hooks_relay.rs`'s documented contract; not re-driven end-to-end with a live wedged daemon in this session (review 3's probe already did that against the equivalent HEAD-built relay).

Also fixed **mi2** while in this file: both `# nosec B108` suppressions removed by dropping `dir="/tmp"` from `tempfile.mkdtemp(prefix="hd-", dir="/tmp")` — bandit's B108 flags the hardcoded `/tmp` literal, not `mkdtemp`'s own (equally short) default tempdir. Matches the pattern already used in `test_relay_guard_fail_open.py`. Cleanup (`shutil.rmtree` in `finally`) was already present in both fixtures — the review's "neither fixture cleans up" note no longer applies to this file's current content.

## Not yet started

MA2, MA4, remaining MA5 items (ratio self-test determinism; run CLAUDE.md guidance test + test_effort_restore on main), mi3 (release notes), mi5 (TOCTOU — optional/minor), remaining docs from the interactive-mode.md fetch (need to write the actual MA4 deny-message doc update once MA4 lands).

## Verification run so far

`source .venv/bin/activate && python3 -m pytest -q tests/integration/test_init_sh_pretooluse_fail_closed.py` → 16 passed.
`python3 -m pytest -q tests/integration/test_client_owned_asset_lint.py -k shell` → 3 passed.

Not yet committed as of this note. Not yet run: ruff/mypy/pyright/black on touched files, daemon restart, full targeted MA2-MA5 work.
