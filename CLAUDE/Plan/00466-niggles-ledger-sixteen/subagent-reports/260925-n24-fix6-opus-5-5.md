# N24 review 3 fix round 6 — progress notes

Predecessor (n24-5, cut off by usage limit) had already committed:

- `c91385ede` B1: `utils/realpath.py` now matches `os.path.realpath` exactly (differential test + containment e2e).
- `b406b205a` MA5 (partial, error_hiding) + mi1: typed `RootProof`; `stop` refuses on a root mismatch without deleting anything; `secret_file_matching`'s realpath fallback now logs.
- Uncommitted: vendored `remote-docs/code.claude.com/docs/en/interactive-mode.md` fetch (for MA4's doc check) + `.claude/REMOTE-DOCS.md` index row.

This session (fresh agent) continuing from there, in-order:

## MA3 — DONE

Removed the two unescaped backticks from `init.sh`'s double-quoted `python3 -c "…"` comment body (lines ~1976-1982). `tests/integration/test_client_owned_asset_lint.py -k shell` (shellcheck SC2006) now passes. Not yet committed.

## MA1 — DONE

`init.sh`: added `except BlockingIOError as e:` before the generic `except Exception`, classified as new `error_type='connect_backlog_full'`, added to the PreToolUse fail-closed allowlist tuple (alongside `socket_timeout`, `malformed_response`, `connection_lost`) and given its own `emit_error_json` context-lines branch. New test class `TestBacklogFullFailsClosedForPreToolUse` in `tests/integration/test_init_sh_pretooluse_fail_closed.py`, with a `backlog_full_socket` fixture that fills a real listen(100)-backlog AF_UNIX socket via non-blocking connects (same technique as `probe_n24r3_gil.py`) without ever accepting. RED confirmed against the pre-fix `init.sh` (KeyError on `permissionDecision` — fell to fail-open), GREEN after the fix. All 16 tests in that file pass.

The relay side needs no separate code change: production always passes `--fallback`, and `connect_fail` execs the bash forwarder with stdin intact before any bytes are consumed, so a relay-side connect timeout against a full backlog lands on this exact python-rung fix. Verified by reading `relay/hooks_relay.rs`'s documented contract; not re-driven end-to-end with a live wedged daemon in this session (review 3's probe already did that against the equivalent HEAD-built relay).

Also fixed **mi2** while in this file: both `# nosec B108` suppressions removed by dropping `dir="/tmp"` from `tempfile.mkdtemp(prefix="hd-", dir="/tmp")` — bandit's B108 flags the hardcoded `/tmp` literal, not `mkdtemp`'s own (equally short) default tempdir. Matches the pattern already used in `test_relay_guard_fail_open.py`. Cleanup (`shutil.rmtree` in `finally`) was already present in both fixtures — the review's "neither fixture cleans up" note no longer applies to this file's current content.

## MA2 — DONE (commit `9779def1a`)

`server.py::_handle_event_client`: added `_pre_tool_use_response_looks_valid` (server-side twin of `init.sh`'s check) and applied it to `_process_request`'s return value for the PreToolUse wire key only; anything that fails it (an `invalid_request`/`input_validation_failed` `{"error": ...}` envelope, a malformed `{"result": ...}`) is replaced with `_pre_tool_use_transport_deny_response()`. New `TestNonVerdictResponseFailsClosedOnPreToolUseSocket` (4 tests: error envelope denies, malformed result envelope denies, non-PreToolUse socket unaffected, a real judged deny still passes through). RED confirmed against pre-fix `server.py`, GREEN after. Fixed the stale docstring claiming PreToolUse "fails open with an empty `{}`" (nit 3).

## MA5 (ratio determinism + CLAUDE.md guidance) — DONE (commits `a3bde5fa8`, auto-commit `55ada58b1`)

- Added `counted_ratio` to `tests/scaling.py`: same `SIZE_FACTOR` ratio arithmetic as `scaling_ratio`, but over a caller-supplied operation COUNT instead of elapsed CPU time. Rewrote `test_the_ratio_separates_linear_from_quadratic_work` on explicit linear/quadratic operation counts — deterministic on any host, no clock involved. Ran 5x locally, all green.
- Ran the CLAUDE.md guidance test and `test_effort_restore` on **main** (`/workspace`, not this worktree): `test_every_earning_handler_has_a_section_in_claude_md` **passes on main, failed on this branch** (this branch's committed `CLAUDE.md` was missing the `failsafe-cron-session-advisor` section — confirmed via `git show HEAD:CLAUDE.md` vs `git show main:CLAUDE.md`). Ran `bin/hooks-daemon regenerate-docs` (the worktree-safe way to force the write `ClaudeMdInjector` otherwise skips in a linked worktree) — turned out to be moot, since my own `git commit`s had already triggered the project's own auto-commit hook (`55ada58b1 Auto: hooks daemon regenerated CLAUDE.md handler guidance`) which fixed it first. Test now passes.
- `test_effort_restore.py::test_opus_below_default_minimum_injects_high` **fails identically on main** (`'/effort medium' != '/effort high'`) — confirmed NOT branch-caused, left untouched per the task's own instruction.
- error_hiding (process_verification.py:178, secret_file_matching.py:284): already fixed by the predecessor in `b406b205a`; reconfirmed green (`test_repo_is_clean_under_widened_scope` passes).

## MA4 — DONE (commit `3d9542cb9`)

Owner decision implemented: PreToolUse now denies when the daemon cannot be reached at all (socket missing / connection refused / daemon won't start), not just when it was reached and went silent (MA1/N40).

- `emit_error_json` (post-connect-attempt path): added `socket_not_found`/`connection_refused` to the fail-closed tuple, with a dedicated context-lines branch.
- `emit_hook_error` (pre-connect path, fires when `ensure_daemon` itself fails): the STANDARD branch (installed project, no CI_ENFORCED/REPO_UNCONFIGURED/VENV_MISSING/NOT_INSTALLED/VERSION_MISMATCH) now denies too. Added `_hooks_daemon_stdin_is_recovery_command` (reads stdin, mirrors `_is_daemon_recovery_command`'s allowlist) — **gated to only the standard case**, because several existing tests call `emit_hook_error` with no stdin of their own and an unconditional read hung them (found via a direct repro script; pytest's own runs didn't hang, likely due to devnull'd stdin, but a standalone script did — do not trust "tests passed" alone here, I only caught this by running the exact failing scenario outside pytest).
- Verified via `code.claude.com/docs/en/interactive-mode.md` (vendored by predecessor) that a `!`-prefixed shell-mode command "Doesn't require Claude to interpret or approve the command" — never a tool call, so this deny (and PreToolUse generally) never touches it. Stated in the commit message; did not duplicate into a second doc file given budget.
- Fixed 3 existing tests whose assertions encoded the OLD fail-open contract for this exact scenario: `test_ci_passthrough.py::test_non_ci_installed_but_not_running_shows_restart_guidance` (the daemon-installed-but-not-running case — added a paired recovery-command-stays-open test too), `test_emit_hook_error_jqless.py`'s PreToolUse+daemon_startup_failed case (added 2 new tests: denies, recovery command stays open), and renamed/rewrote `test_init_sh_pretooluse_fail_closed.py`'s `TestDaemonNotReachableKeepsExistingPath` → `TestDaemonNotReachableFailsClosedForPreToolUse` (added a short-socket-path fixture — pytest's own `tmp_path` is too deep for AF_UNIX's ~108-byte cap, which silently changed `FileNotFoundError` into `OSError` and made the new deny test fail for the wrong reason until I caught it).
- Ran the FULL init_sh/ci_passthrough/relay-guard/discovery test surface after this change (not just the directly-touched files) — genuinely necessary: three files outside my initial edit set broke, all now fixed. See commit `3d9542cb9` message for the full list.
- Did NOT touch: the catch-all final `else` in `emit_error_json` for a truly unclassified `Exception` type (e.g. a bare `OSError`) — stays fail-open, matching what was explicitly asked (socket_not_found/connection_refused/daemon won't start only). Did NOT add a dedicated end-to-end `connection_refused` test (only `socket_not_found` and `daemon_startup_failed` got dedicated new tests) — same code path/tuple/context branch as `socket_not_found`, so risk is low, but a successor should add one if being thorough.
- Release notes 45 (this change) and 46 (mi3: the relay/per-event-socket PreToolUse deny from N40 review 2 + this round's MA2, previously undocumented) — commits `3d9542cb9`, `1a6b76ad0`.

## Not yet started (handed off)

- **mi4**: absolute CPU-time bounds (`_MAX_SECONDS`/`_SMALL_MAX_SECONDS` in `tests/unit/handlers/test_safety_handlers_hostile_input_performance.py`) remain beside the ratios — review says convert to ratios or work counts, or at minimum size N per handler / state the limit in `scaling.py`. Not started.
- **mi5**: the µs-scale TOCTOU between each root proof and `os.kill` (`cli.py:797`→`813`, `:848`→`860`) — `os.pidfd_open` + `signal.pidfd_send_signal` on Linux. Not started.
- **Gate re-run**: have not re-run the full QA gate (explicitly told not to run the full suite/a gate this round — only targeted tests). A successor (or the next review round) should run it before declaring READY. Every targeted test I touched or that plausibly overlapped my changes is green (see commit messages for exact test files/counts run each round).
- Did not separately verify `test_relay_guard_foreign_checkout.py` against my MA1/MA4 changes (ran `test_relay_guard_fail_open.py` instead, which passed) — low risk, same transport contract, but unverified.

## Commits this session (chronological)

`650e966be` (MA1+MA3+mi2), `316cccb1d` (vendor doc + report commit), `9779def1a` (MA2), `a3bde5fa8` (MA5 ratio determinism), auto-commit `55ada58b1` (CLAUDE.md regen), `3d9542cb9` (MA4), `1a6b76ad0` (mi3 release note).

## Verification run so far

All targeted pytest runs green after each change (see individual commit messages above for exact scope per round); ruff/black/mypy/pyright clean on every touched Python file (2 pre-existing, unrelated mypy `no-any-return` findings confirmed present at the same lines on `HEAD` before my edits, not introduced by me); shellcheck (`test_client_owned_asset_lint.py -k shell`) green after MA3 and again after MA4's larger `init.sh` edits; daemon restarted before every `src`/`init.sh` commit.
