# N24 landing: merge of main into worktree-n466-n24 (Opus 5.5)

Brief: `untracked/scratch/briefs/n24-land-1.md`. Branch head before the merge: `fe0121862`.
Nothing pushed. No gate queued.

## Conflict resolutions

- **`CLAUDE/Plan/00466-niggles-ledger-sixteen/PLAN.md`**: resolved with
  `merge_ledger_table.py` (97 rows; it reported `N24`/`N25` differ from main
  and `N34` exists only on the branch). Then an Edit of the N1 row made the
  table formatter rerun. Checked afterwards: the row set is exactly the union of
  main's 96 rows and the branch's 90 rows, with no duplicates and no conflict
  markers.
- **`CLAUDE/UPGRADES/UNRELEASED/post-upgrade-tasks/README.md`**: the task index
  is the union of both sides. It now has main's `01`, `02-audit…` and `03-add-modelsettings…` rows plus
  N24's `02-review-new-denials…` row. The filename was kept because
  `NIGGLES.md` cites it.
- **`src/claude_code_hooks_daemon/utils/secret_file_matching.py`** (security): I took
  main's structure in all three hunks and put N24's `first_matching_glob` in
  place of each per-pattern `path_matches_globs` loop. The two give the same
  answer: the first pattern in order that matches.
  - `_token_mention`, literal check: keeps main's `if context != "bash": continue`
    content-context gate.
  - `_token_mention` symlink-alias tail: keeps main's glob-shaped-token skip
    and the per-call `realpath_cache`.
  - `_expand_glob_token`: keeps main's `matches_iter` (the bounded recursive walker
    with its deadline), `max_expansions`, and the ENOENT-only fail-close
    (any other `OSError`, and every `ValueError`, propagates to the guard's
    fail-closed net). I dropped N24's older `except (OSError, ValueError): continue`
    on purpose: that is the fail-open main's review 4 closed.
  - N24's `protecting_pattern` (symlink-loop deny, realpath resolved once, NUL-safe)
    merged cleanly and is unchanged.
- **`tests/integration/test_handler_config_blocking.py`**: both sides had
  made the same fix: request `project_context` in the sed-blocker and
  disabled-handler tests. I took main's signatures (no unused `tmp_path`) and
  merged both docstring rationales, N11 plus N24's fail-closed-on-raise. No test was lost.
  Test names are unique.

## Broken by the merge, and fixed

`tests/unit/handlers/pre_tool_use/test_secret_file_guard.py` had 2 failures
(main's N11 tests against N24's code):

1. `test_the_live_nul_byte_path_still_denies` failed with a real fail-open. N24
   made `protecting_pattern` treat a NUL-bearing path as spelled (so it no longer raises).
   Main's guard used to deny that path through the raise, so after the merge
   `Read /proj/a\0b` was ALLOWED. Fix:
   `secret_file_guard._evaluate` now denies any path argument that contains a NUL byte. It
   uses `_ERROR_ROUTE` and a new `_NUL_PATH_DETAIL` constant. N24's util-level
   contract (`path_is_protected` does not raise on NUL) still holds. The test was RED
   before the fix and GREEN after.
2. `test_read_route_exception_still_denies` was patching `sfm.path_is_protected`, but the Read
   route now calls `sfm.protecting_pattern`, so the patch injected nothing.
   The test now patches `protecting_pattern`. The sibling Grep-directory test's stale
   `path_is_protected` patch was updated the same way.

## Release notes

N24's seven notes collided with main's `40`–`46`. I renamed them with `git mv` to
`86`, `87`, `88`, `89`, `91`, `92`, `93`, skipping 90 as the brief said.
Each still reads `**Plan**: 00466` / `**Audience**: operators`. No prefix
is duplicated now. (`85` is main's settings-repair note.)

## Verification (worktree venv)

| Suite                                                          | Result                 |
| -------------------------------------------------------------- | ---------------------- |
| `tests/unit/utils` + `tests/unit/core`                         | 5147 passed            |
| `tests/unit/handlers` (after fix)                              | 8951 passed            |
| `tests/unit/daemon`                                            | 2310 passed            |
| `tests/integration`                                            | 4712 passed, 4 skipped |
| `tests/integration/test_pending_release_notes_holding_area.py` | 87 passed              |

The 4 integration skips are environment-conditioned skips (tool availability
and similar). None is in a file this merge touched.

ruff, black --check, mypy (15 src files) and pyright (0 errors) are clean over
all 61 Python files the merge changed. I restarted the worktree daemon and it reports
`Daemon: RUNNING`.

## Final SHA

The merge commit is the worktree's HEAD after this report was committed. The
team-lead message names it.
