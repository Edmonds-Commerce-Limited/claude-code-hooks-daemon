# N12 report: hand-sent hook probes are marked, and `hooks-daemon probe` sends them

**Agent**: n466-n12 (Opus 5.5)
**Branch**: `worktree-n466-n12`
**Commits**:

- `8fa4c354`: the helper, the docs and the guard
- `555c9280`: `probe_as`
- `76db5173`: transport-verify marking and the `--file` docs
- `64c63b3c`: merge of main

## Outcome

N12 is fixed. The marker no longer costs a probe its handler: a probe names
the thread it stands for with `probe_as`, and there is no unmarked-probe
escape anywhere. Main (through `05277333`) is merged in, and both conflicts
were resolved by keeping both sides. Nothing is merged to main or pushed.

## What changed

### Helper: `hooks-daemon probe <event> --json <payload> | --file <path> [--as main|sub]`

- The code is in `src/claude_code_hooks_daemon/daemon/hook_probe.py`. The
  verb is `cmd_probe` in `daemon/cli.py`.
- It sends the payload through the project's own `.claude/hooks/<bash_key>`
  as a `bash` subprocess, the same route the acceptance harness uses.
- It fills in whatever the payload leaves out:
  - `synthetic_source: manual-probe` (the `MANUAL_PROBE` constant);
  - `probe_as` on events that can carry `agent_id`: `main` by default, `sub`
    with `--as sub`, which also sets `agent_id: manual-probe-agent`;
  - `hook_event_name`, `cwd`, and a fresh `session_id` of the form
    `manual-probe-<hex8>`.
- It refuses the following with exit 2, sending nothing:
  - a marker the classifier would ignore (empty, or not a string);
  - a `probe_as` that contradicts `--as`;
  - an `agent_id` on a main probe, or a non-probe `agent_id` on a sub probe;
  - `--as` on an event with no scope.
- A caller's own non-empty marker is kept. The helper prints a note only when
  that marker is not probe-class, because then `probe_as` is inert. The old
  always-on caveat is gone.
- Exit codes:
  - 0: the daemon answered. A Stop block's exit 2 counts as an answer.
  - 1: no verdict.
  - 2: bad input.
- `response_decision` and `response_text` moved into `hook_probe`. The
  acceptance playbook harness imports them from there.

### Design: `probe_as`

A probe answers two separate questions:

- `synthetic_source` says whether it belongs in the real record. A probe does
  not.
- `probe_as` says which thread a scoped handler should judge it as.

Before this change, `scope_admits` refused every synthetic event for `MAIN`
and `SUB`. Measured: a marked Stop probe answered `{}`, while the same
payload unmarked was blocked.

- **Where it lives.** `daemon/synthetic_traffic.py` holds `PROBE_AS_FIELD`,
  `ProbeThread`, `PROBE_CLASS_SOURCES`, `PROBE_AGENT_ID` and `probe_thread()`.
  `core/handler_scope.py` holds `acts_as_main_thread` and `acts_as_subagent`,
  which `scope_admits` uses.
- **Only a probe-class MARKER may name a thread.** `PROBE_CLASS_SOURCES` is
  `manual-probe` and `transport-verify`, declared by name. The session-shape
  fallback never counts. A harness run, a cron tick or a supervisor event
  keeps the refusal whatever it carries.
- **`main`** requires no `agent_id`.
- **`sub`** requires exactly `agent_id == "manual-probe-agent"`. That is a
  documented fixed identity, deliberately not the 17-character shape Claude
  Code mints. A real teammate's id is refused, so a probe cannot borrow one.
- **A contradictory claim is refused whole**, not half-honoured.
- **Real traffic ignores `probe_as`.** Only a synthetic event consults it.
- **Consumers of `agent_id` and of probe events.** Each one was checked, and
  every sensor that records state about real agents now declines a synthetic
  event:
  - `subagent_report_persistence`: `matches` returns False for a synthetic
    event, so no report file is written for the probe agent;
  - `subagent_cache_aggregator`: the status-line totals are not fed by
    probes;
  - `goal_injection`: returns ALLOW early. The goal ledger is project-wide,
    so a probe would otherwise flip a real session's goal;
  - `auto_continue_stop`: a probe never records the human-blocked marker that
    silences the failsafe cron, and its `stop-events.jsonl` line is tagged
    `"synthetic"`.
  - The `orchestrator_simulate` project handler uses `acts_as_main_thread`
    in place of its own agent_id and synthetic checks.
  - `teammate_reap_advisor` and `cron_stop_enforcer` are MAIN-scoped and
    only read the event, so they are covered by scope.

The design is also recorded briefly in `HANDLER_DEVELOPMENT.md` ("Probing the
Live Handler: The design, in brief") and in full in `DEBUGGING_HOOKS.md`.

### `install/transport_verify.py` marks its probes

- Each of its three probes carries `synthetic_source: transport-verify`
  (`TRANSPORT_VERIFY`, which is probe-class): PreToolUse, status-line and
  Stop.
- The PreToolUse and Stop probes also carry `probe_as: main`, so the Stop
  probe still reaches the MAIN-scoped `auto_continue_stop` it exists to see
  block. The status-line payload has no thread.
- Tested in `tests/unit/install/test_transport_verify.py`
  (`TestEveryProbeIsMarked`), which captures what each probe actually sent.
- Verified live against the worktree daemon. All three probes passed. The
  Stop record is `auto-continue-stop` / `deny`, and every record reads
  `"synthetic": "transport-verify"`.

### Documentation

- **`CLAUDE/DEBUGGING_HOOKS.md`** is the canonical section ("Probing a
  Handler by Hand"). It covers the marker, `probe_as`, the fixed probe agent,
  the probe-class sources, the sensors that ignore probes, the `--file` rule
  and the guard.
- **Every Stop probe is marked and carries `"probe_as":"main"`**, in
  `DEBUGGING_STOP_HOOK.md` and `.claude/agents/transcript-inspector.md`. That
  includes the `nc -U` socket sends.
- **Guarded-command probes use `--file`**, in `LLM-INSTALL.md` (step 5,
  success criterion 3 and troubleshooting), `LLM-UPDATE.md` and
  `docs/guides/GETTING_STARTED.md`. The payload is shown as a JSON block to
  save with the Write tool, then sent with `probe PreToolUse --file`.
  - Verified live: both the destructive-git and sed payloads return
    `decision: deny`.
  - Writing the `.json` files with Write is not blocked.
- **Also updated:**
  - `HANDLER_DEVELOPMENT.md`
  - `AcceptanceTests/GENERATING.md`
  - `Worktree.md`
  - `README.md`
  - `docs/guides/TROUBLESHOOTING.md`
  - `CLAUDE/UPGRADES/upgrade-template/README.md`
  - the `CLAUDE/CLAUDE.md` routing row
- **Release note** 36 (`CLAUDE/UPGRADES/UNRELEASED/release-notes/36-...`)
  covers the helper, `probe_as`, transport-verify and the sensors.

### Guards

**`tests/integration/test_documented_hook_probes_are_marked.py`** applies
three rules. None has an exemption, and the unmarked-probe annotation is
gone:

1. Every documented dispatch sets `synthetic_source`. A dispatch is any of
   these:
   - a pipe into `.claude/hooks/<name>`, including `\` or leading-`|`
     continuations;
   - a `<` or heredoc redirect;
   - an `nc -U` send of a `hook_input` envelope. A `_system` message is
     excluded.
2. A document with such a command also shows `hooks-daemon probe`.
3. **New:** every inline probe is judged as a Bash PreToolUse, and fails if
   denied. An inline probe is an `echo` payload or `probe --json`, and a prose
   line is judged by its code span. The judge is the real `DaemonController`
   with this repository's own `.claude/hooks-daemon.yaml` handler section.
   - A vacuity test proves the judge denies a guarded inline probe.
   - Before the doc fix, the rule failed on exactly the six
     destructive-git sites. I then checked all 42 inline probes in the
     corpus, and the judge flagged nothing else.
   - Judging by the real handlers means a new guard is covered automatically,
     with no pattern list to maintain.

**`tests/acceptance/test_documented_stop_probe.py`** extracts the Stop probe
from `DEBUGGING_STOP_HOOK.md` and `transcript-inspector.md`. It asserts the
probe carries the marker and `probe_as: main`, sends it through the live
`.claude/hooks/stop`, and asserts a block, `R-STOP-NO-REASON` and exit 2.

**End-to-end controller tests** are in `tests/unit/daemon/test_hook_probe.py`.
Each case below is also asserted to be logged synthetic:

- a marked `probe_as: main` Stop is blocked by `auto_continue_stop`;
- a marked Stop with no `probe_as` is still refused by MAIN;
- `playbook-probe` carrying `probe_as` is ignored;
- a `probe_as: sub` CronDelete is denied by
  `subagent_cron_delete_blocker`.

## Remaining findings (not fixed)

1. **`tests/acceptance/test_stop_hook_hard_block.py` sends three unmarked
   probes through the live daemon.** They are two Stop probes and one
   SubagentStop probe, with sessions `phase9-block-probe`,
   `phase9-subagent-block-probe` and `phase9-allow-probe`. No classifier rule
   matches those sessions, so each acceptance run writes them into
   `verdicts.jsonl` as real traffic. This is the same class as N12.
   - The remedy is `synthetic_source` plus `probe_as: main` on the Stop
     probes, and a check of which scope the SubagentStop blocker needs.
   - I left it alone because it changes what a live acceptance test sends.
     It is worth its own ledger entry.
2. **`goal_injection` may conflict with the goal-flip branch** if that branch
   is still in flight. This branch adds an early synthetic-event return at
   the top of `handle`.

## QA

- Targeted QA on the merged tree
  (`llm_qa.py format lint type_check pyright magic_values error_hiding docs_qa`):
  7/7 PASSED.
- pytest on the merged tree, all passing (3 relay-binary skips). The run
  covered every test file this branch touches, plus:
  - `test_transport_toggle`, `test_relay_guard_fail_open` and
    `test_subprocess_spawns_are_bounded`;
  - `test_documented_commands_are_not_self_denied` and `test_doc_truth_check`;
  - the acceptance `test_documented_stop_probe` and `test_playbook_harness`,
    run against a restarted daemon. Their stale-daemon fingerprint check
    correctly refused the pre-merge daemon.
- The worktree daemon was restarted and RUNNING before each commit.
- Full QA was not run; that is the coordinator's gate.
