# Plan 00213 — Journal 26-08-12

> **Append-only activity log** for plan 00213. One file per day
> (`00213-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 12:05 · action · — — plan scaffolded

Plan 00213 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 13:33 · finding · P1 — Phase 1 evaluation complete, ADAPT recommended

Ran the proposal's own stated verification independently rather than trusting
its table: extracted the 13 fenced bash blocks from `PROPOSAL.md` §3.1–3.12
(script, not hand-edit) into `_planlib.inc.bash` (628 lines). `bash -n`
exited 0 with EMPTY stdout and stderr. `shellcheck -x -S style` exited 0
with zero output at any severity (stricter than the daemon's own
`run_shell_check.sh` gate, which only requires zero error/warning). Static
cross-reference of all `plan_*`/`_plan_*` identifiers: 24 defined, 24
referenced, zero dangling, zero unused. Went a step further than the
proposal's own claims and sourced the library for a dynamic smoke test
(double-source guard, `_plan_fingerprint_present` prefix-rejection,
`_plan_strip_cr`, and `_plan_find_repo_root`'s boundary-refusal behaviour
against a throwaway nested-repo fixture under `mktemp -d`) — all passed.

Important caveat found on close reading: of the proposal's 3 named artefacts
(library, `plan_script_qa` handler, `test-planlib.bash` suite), only the
library is delivered as complete runnable code. The QA handler is a rules
table + 2 illustrative snippets; the test suite is 4 stated principles with
no runnable file. The proposal's own verification table is scoped
correctly (library only) but is easy to misread as covering everything.

Overlap assessment (`mkplan.bash`, `deploy-plan-workflow`, all 30 `plan_qa`
checks across 3 enforcement surfaces): minimal and mostly additive. Every
existing check operates on `PLAN.md`/README/journal/git-metadata hygiene at
plan **creation** time; nothing inspects executable content. `planlib`
addresses a different lifecycle stage — safe **execution** of operator-run
scripts filed inside a plan folder. Also found: `PlanWorkflowQaConfig
.extra_root_files`'s docstring (added Plan 00153, 2026-07-12, predates this
proposal) already names `_planlib.bash` as its example of a legitimate
plan-root exception — circumstantial but a striking corroborating detail.

Recorded decision in `PLAN.md` Technical Decisions: **ADAPT** — accept the
library alone via the existing `deploy_plan_workflow_if_enabled` seam,
behind config, defaulted inert; treat `plan_script_qa` + `test-planlib.bash`
as a separate, honestly-scoped follow-up plan rather than folding them into
this plan's Phase 2. Surfaced (not resolved) an open architectural question
for the human: `planlib` has zero runtime coupling to anything the daemon
governs (explicitly operator-invoked, per proposal §9) — unlike
`mkplan.bash`, which manipulates daemon-owned state. Whether that makes it
in-scope for a *hooks daemon* at all, versus a separately-vendored library
the daemon merely helps distribute, is left for a human to decide.

Full detail in `EVALUATION.md` (new supporting doc in this plan folder, per
the EXTRACT convention). Ticked Task 1.1–1.3. Status flipped Not Started →
In Progress (Phase 1 done, Phase 2 still gated on human authorisation — not
begun). Did not touch Phase 2 tasks, did not move the plan, did not set a
terminal status, per this session's explicit scope (Phase 1 only, run as an
isolated worktree agent — no daemon restart available here).

## HANDOFF

Phase 1 is done. Next session (if authorised): re-scope Phase 2 to land
`_planlib.inc.bash` only (Tasks 2.1/2.2/2.4 as originally scoped fit this),
and open a NEW plan for `plan_script_qa` + `test-planlib.bash` rather than
treating Task 2.3 as covering them — that task as currently worded
understates the work by a wide margin. A human still needs to weigh the
operator-invoked-vs-daemon-governed scope question in Technical Decisions
before Phase 2 starts.

## 15:15 · action · P2 — Phase 2 executed (human-authorised): config seam, deploy path, test suite, dogfooding

Human explicitly authorised Phase 2 this session ("yes we should be using
it — this is the dogfooding zone, we have to dogfood as much as possible").
`plan_script_qa`/`test-planlib.bash`-the-handler stayed explicitly out of
scope per the ADAPT decision — only the library landed.

**Duplicate plan found and resolved (not by this agent)**: a sibling agent
(`plan-lib-architect`) turned out to be sharing this exact worktree and
flagged `CLAUDE/Plan/00199-hooks-daemon-plan-lib/` — an independent plan
against the SAME proposal, filed 5 days earlier, unaware of 00213. It
authored `PROPOSAL-ASSESSMENT.md` (an integration analysis reaching the same
ADAPT conclusion) and superseded 00199 into
`CLAUDE/Plan/Cancelled/00199-hooks-daemon-plan-lib/` (commit `f4f44927`)
before this agent began writing code. Added a Context & Background pointer
to that assessment in `PLAN.md` rather than re-deriving its findings.

**Shared-index hazard**: confirmed via `git worktree list` + `git status
--porcelain` that `plan-lib-architect` was genuinely staging its 00199
supersede in this SAME `.git/index` concurrently. Every commit below used
`git add <exact files>` + `git commit -- <same exact files>` (never a bare
`git commit`, never `git add -A`), verified with `git status --porcelain`
before and `git show --stat --format="" HEAD` after each one. No collision
occurred; `plan-lib-architect` confirmed it is done and staying out of
`00213-*`/`src/`/`tests/`.

**Task 2.1** — `PlanWorkflowScriptsConfig` nested at `plan_workflow.scripts`
in `config/models.py`: `enabled` (default `false`), `root_marker` (NO
default — a `model_validator` raises if `enabled` is true and `root_marker`
is empty or `.git`), `delegate`, `check_flag`, `force_color_var`, `scrubber`,
`track_run_logs`. 22 new/updated pytest cases in `tests/config/test_models.py`.

**Task 2.2** — `_planlib.inc.bash` extracted the SAME way Phase 1 verified it
(13 fenced blocks from `PROPOSAL.md` §3.1–3.12, script not hand-edit; 627
lines, byte-identical to a fresh independent extraction; `bash -n`
stderr-empty and `shellcheck -x -S style` both re-confirmed clean at the
final `install/templates/` location). Deployed through
`deploy_plan_workflow_if_enabled` → `bootstrap_plan_workflow` (new
`deploy_scripts_library` param) — the SAME seam as `mkplan.bash`, gated on
`plan_workflow.scripts.enabled` AND the parent `plan_workflow.enabled`.
Mode `0o644` via a NEW `_PLANLIB_MODE` constant — deliberately not reusing
`_MKPLAN_MODE` (`0o755`), since the library is sourced, never executed
(regression-tested: asserts the execute bit is absent). Added
`_planlib.inc.bash` to `plan_qa`'s built-in `_EXPECTED_ROOT_FILES` (not
`extra_root_files` — it is now daemon-owned, matching `mkplan.bash`) and
updated the two docstrings that anticipated the file under the old
`_planlib.bash` name.

**Task 2.3** — wrote `tests/unit/install/test_planlib_library.py` from
scratch (47 pytest cases, no prior test to inherit): sources the library via
subprocess and exercises it as REAL bash, not a Python re-implementation.
Covers root resolution incl. the nested-repo boundary refusal (the exact
incident PROPOSAL.md §1.1 describes) and the cwd-independence regression
test (runs the calling script BY PATH from an unrelated repo with its OWN
different marker, asserts `PLAN_REPO_ROOT` still resolves correctly), the
change gate + `_plan_assert_change_allowed`, every leg mode-mismatch guard,
the `BASH_SUBSHELL` misuse guard (asserts the WHOLE run dies, not just the
subshell), ssh-key-load ordering enforcement (the check only — no live
agent), no-TTY paths via `setsid` (`_plan_tty_openable`, `plan_confirm`
refusal, `-y` bypass), flag parsing, and `plan_finish`'s
exit-agrees-with-text contract. 100% line/branch coverage of
`install/plan_workflow.py`. Explicitly does NOT cover (documented in the
test file's module docstring as a manual checklist, since the shipped
library itself is kept byte-identical to the verified extraction and does
not carry this prose): live interactive TTY prompts, real ssh-agent
interaction, and signal-timing (INT/TERM/HUP) against a live
`plan_start_log` tee session — Phase 1 already did a one-off manual
perturbation of the HUP trap by hand (EVALUATION.md §1.3); reproducing that
as an automated CI perturbation was judged not worth the flakiness risk of
synthesising signal timing against a backgrounded `tee | awk` pipeline in a
container. Spot-checked (ad hoc, not committed) that the nested-repo
boundary test genuinely discriminates: removed the `.git` boundary check
from a scratch copy of the library and confirmed the test would flip from
pass to the exact wrong-resolution failure it exists to catch.

**Task 2.4** — appended (did not rewrite) a `plan_workflow.scripts` entry to
the existing `CLAUDE/UPGRADES/UNRELEASED/config-changes/v3.53.0.yaml`
(4 sibling-plan entries already present). `recommended: false`,
`dormant: true` — explicitly NOT oversold as universally beneficial.
Judged a `truth-changes` entry not applicable: this is a wholly new opt-in,
not a change to an existing documented workflow.

**Dogfooding** — re-checked before writing any code (independent repo
search: `find CLAUDE/Plan -type f \( -name "*.bash" -o -name "*.sh" \)`
across the whole tree found only `mkplan.bash` ×2 and one unrelated old
benchmark script) and confirmed against `plan-lib-architect`'s
`PROPOSAL-ASSESSMENT.md` §7.1, which reached the identical finding 5 days
earlier: this repo is a pure software project with ZERO plan-folder
deploy/verify/triage orchestrator scripts to run the library's operational
primitives against. Declined to manufacture one (explicit instruction from
both the task brief and the coordinator). Instead enabled
`plan_workflow.scripts` (root_marker: `pyproject.toml`) in THIS repo's own
`.claude/hooks-daemon.yaml`, mirroring the existing
`ccy.deploy_supervisor: true` precedent ("dogfood the enabled path" with
nothing else to exercise in self-install mode). Ran the real
`deploy-plan-workflow` CLI against this worktree and verified:
`_planlib.inc.bash` landed at `CLAUDE/Plan/_planlib.inc.bash`
byte-identical to source, mode `0644`, and the `plan-qa --sweep` CLI
produced ZERO findings mentioning it (only 2 pre-existing, unrelated
staleness advisories). Committed the deployed artifact as tracked source,
matching how `mkplan.bash` is already tracked in this repo.

**Verified, this worktree only**: targeted pytest suites (config, install,
plan_qa, the new library-behaviour suite — all green), `mypy`/`ruff`/`black`
clean on every touched Python file, `shellcheck -x -S style` + `bash -n`
(stderr-asserted-empty) clean on `_planlib.inc.bash`, the project's own
`scripts/qa/run_shell_check.sh` passing (55 scripts, 0 issues), and the live
`.claude/hooks-daemon.yaml` re-parsing cleanly through `Config.load`.

**OUTSTANDING — cannot be done from this worktree agent**: full
`./scripts/qa/run_all.sh`, a daemon restart + `status` showing `RUNNING`
(this agent cannot restart the shared daemon), and client-mode verification
against `scripts/dummy-client-repo.sh`. The merging session must run these
before treating Phase 2 as fully verified. `plan_script_qa` +
`test-planlib.bash`-the-standalone-handler remain explicitly out of scope,
per the task brief and the ADAPT decision — a human should file a new plan
for them if wanted.

Six commits this session, in order: `24af1af6` (Task 2.1 config seam),
`6a1b2ea2` (`_EXPECTED_ROOT_FILES`), `33fecdc6` (Task 2.2 deploy path +
library file), `da1e00c3` (Task 2.3 test suite), `f62863ae` (Task 2.4
config-changes), `cd4145fb` (dogfood config + deployed artifact). PLAN.md
tasks 2.1–2.4 ticked; Success Criteria updated to distinguish
worktree-verified from merge-time-outstanding. Status left at `In Progress`
(not moved, not set terminal) per this session's worktree constraints.
