# Plan 00337 — Journal 26-09-07

> **Append-only activity log** for plan 00337. One file per day
> (`00337-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 07:20 · action · — — plan scaffolded

Plan 00337 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 07:24 · finding · — — the raw stop-event evidence

Filed straight from a live reproduction rather than a report. The session that
created this plan spent four hourly cron ticks doing nothing, then armed the
suppression by accident. Raw record (`untracked/stop-events.jsonl`,
2026-09-07):

```
{"t":"00:59:27","decision":"allow","prefix":"","marker":"absent"}
{"t":"01:00:55","decision":"allow","prefix":"","marker":"absent"}
{"t":"01:01:12","decision":"allow","prefix":"","marker":"absent"}
{"t":"01:32:53","decision":"allow","prefix":"","marker":"absent"}
{"t":"02:32:53","decision":"allow","prefix":"","marker":"absent"}
{"t":"03:34:41","decision":"allow","prefix":"","marker":true}
```

Two readings to keep straight, because the first one is a trap:

`reason_prefix` is `""` on ALL of these, and that is **not** a symptom.
`auto_continue_stop.py:550` passes `""` literally on the Branch 2 ALLOW path.
An early pass at this treated the empty prefix as evidence the handler could
not see the stop text and nearly filed that as the root cause. The real signal
is `marker_written` being ABSENT: `_maybe_record_human_blocked_marker` returns
`None` at line 675 when no pattern matches, and `_log_stop_event` only adds the
key when it is not None. Absent = no pattern matched.

## 07:26 · finding · P3 — the phrases exist nowhere the agent reads

`_HUMAN_BLOCKED_PATTERNS` has four entries. The generated `CLAUDE.md` block
documents `R-FAILSAFE-CRON-SUPPRESSED` — that the marker exists, and that a
real message CLEARS it — but never how to ARM it.
`docs/guides/HANDLER_REFERENCE.md` (3283, 3402) does name the phrases, but that
is the human reference tree. The handler's own `get_claude_md()` mentions human
input, cron and suppression zero times.

So the agent can only comply by reading daemon source, which is exactly what
happened here: four turns burned, then the source was read, then the next stop
armed on the first try.

## 07:28 · finding · P3 — widening has already been tried once and missed again

`git log -S` is the wrong tool here (it counts occurrences, so an edit INSIDE
the tuple is invisible); `-G` shows the real history:

```
923fd583 Plan 00314 Tasks 1.1-1.3: marker-write observability and pattern widening
cd02e32d Plan 00298: zero-token failsafe cron cadence for human-input blockage
```

00298 introduced the set; 00314 widened pattern 4 to add `human` after a
2026-09-01/02 dogfood miss; today's wording missed all four anyway. Two misses,
one widening, and 00314 still has more widening queued. That is the argument
for changing the mechanism rather than the regexes.

## 07:30 · finding · P1 — Plan 00314 shipped but reads "Not Started"

`923fd583` is titled "Plan 00314 Tasks 1.1-1.3" and carries the observability
field, the pattern widening, and 235 new lines in
`tests/unit/handlers/stop/test_auto_continue_stop.py`. It even includes
00314's own JOURNAL entry — so the executor journalled the work and never
flipped the task boxes. `PLAN.md` still shows Status "Not Started" and all four
tasks `⬜`.

Its Task 1.4 ("live dogfood — arm the marker with a matching stop") is
satisfied by today's `marker_written: true`. So 00314 is done in substance and
stale in bookkeeping — and a plan in that state invites a second agent to redo
shipped work. Phase 1 closes it out.

## 07:32 · decision · P4 — backoff must have a cap, not silence

Considered making the cron back off indefinitely after N unproductive ticks.
Rejected: the cron exists to recover a session interrupted by a rate limit or
an API error, and in that case a LATER tick genuinely does help once the limit
lifts. Unbounded backoff would remove the feature's reason to exist. Exponential
with a ceiling (~4h) cuts the waste while preserving recovery, and a real user
message resets it — mirroring how a user message already clears the marker.

## 07:34 · handoff · — — filed, not started

Nothing implemented; this is a proposal built on evidence, not a fix. Suggested
order is NOT the phase order: Phase 1 is quick bookkeeping that stops duplicate
work, Phase 2 is the cheapest real fix and would alone have prevented today's
failure, Phase 4 is the most robust because it needs no cooperation from the
agent, and Phase 5 is a measurement that may well exonerate the code.

## 08:30 · decision · P4 — the ledger is the authority on "done", not the agent

Owner steer mid-session: should the cron stop once we hit a human blocker, or
once work is completely done? With the crucial caveat — "the completely done
bit is tricky because agents stop randomly all the time, why we have so many
stop systems" — and the observation that the `/goal` system is effective at
keeping agents running.

The two cases separate, and giving them the same answer would be a mistake:

- **Blocked on human** — safe to suppress. Terminal until input arrives, and a
  user message already clears the marker.
- **Work complete** — must NOT suppress on the agent's word. A false "done" is
  the commonest way an agent stops wrongly, and the cron is the net that
  catches it. Suppressing on self-report disables the net precisely when it is
  needed.

Verified before relying on it: `_goal_ledger_challenge()` (auto_continue_stop.py
:633) calls `GoalLedger.live_plan_numbers(plan_dir)`, which derives what is
owed from plan **Status on disk** — daemon-side state, not self-report, and it
names displaced goals the single-slot upstream `/goal` has forgotten. This
session is the demonstration: the stop hook blocked a stop and named Plan 00336
while it was In Progress.

So the cron should ask the LEDGER whether work is owed, not the agent whether
it is finished. Task 4.1 now carries the resulting truth table. This supersedes
the original Phase 4 framing, which keyed backoff on "consecutive unproductive
ticks" — that is a heuristic; the ledger is a state signal.

Recorded limit (Task 4.1b): the ledger tracks PLAN goals, so non-plan work is
invisible to it and "no goals owed" is not proof of idleness. Hence the
no-owed/no-declaration case backs off rather than silencing.

## 14:20 · action · T1.1/T1.2 — 00314 verified against the code, then archived

Checked `923fd583` against the current source before ticking anything, because
a commit message is not proof the code survived:

- `_HUMAN_BLOCKED_PATTERNS` pattern 4 carries `human` in its alternation
  (`(?:owner|user|human)`), which is the widening 00314 Task 1.2 describes.
- `_maybe_record_human_blocked_marker` returns the `write_marker` result, and
  the stop-event record carries `marker_written` — Task 1.1's observability.

Task 1.4's live dogfood is satisfied by the record itself: 16 stop events in
`untracked/stop-events.jsonl` carry `"marker_written": true`.

Stated as a limit rather than glossed: only the ARMING half is live-observed.
The suppression half — a delivered tick being dropped because the marker is on
disk — is pinned by 22 unit tests but was not observed live this session; the
daemon's in-memory log ring had rolled and the restarts cleared it.

Archived atomically in `9def2583`: `git mv` into `Completed/`, index row,
00297 aged out of the 30-row window into `Completed/README.md`, and the
statistics recounted from disk (45 root, 278 `Completed/`, 7 `Cancelled/`).

## 14:40 · finding · T1.3 — systemic, but the rot is in the HEADER, not the boxes

Cross-referenced all 22 live plans reading `Status: Not Started` against
commits naming them that touch files outside `CLAUDE/Plan/`. Two plans have
shipped work behind a "Not Started" header:

```
00110  15 subject-leading commits, Phases 2-6 delivered
00314  Tasks 1.1-1.3 delivered in 923fd583   (the trigger for this plan)
```

00110 is the more interesting instance and it sharpens the finding. It has 8
ticked boxes and 24 unticked — so its executor DID maintain the checkboxes and
still never touched the header. "Shipped but reads Not Started" is therefore
not one failure but two independent ones, and the header is the field that
rots hardest, because ticking a box is adjacent to the work while flipping a
header is a separate act nothing prompts.

Checked and dismissed: 00311 also has a commit touching `src/`, but the commit
is an opportunistic docs-QA link fix credited to the plan, not delivery of any
of its five tasks. A commit NAMING a plan is not evidence that plan advanced —
worth stating, because the naive version of this check would report it.

Per Task 1.3's own instruction this is a finding for its own plan, not scope
creep here.

## 14:55 · finding · T2.3 — the premise is weaker than the plan assumed

Task 2.3 asks for the claim behind Phase 2 to be confirmed: that today's
four-tick failure would not have happened had the arming vocabulary been in
resident guidance. It does not survive contact with the current text.

One of the four arming phrases is ALREADY in the generated `CLAUDE.md` block.
`get_claude_md()` says:

> Use `STOPPING BECAUSE: need user input` and ask your question

and `_HUMAN_BLOCKED_PATTERNS[2]` is `need(?:s|ed)? user input`. So the agent
had a working arming phrase in front of it all session and stopped four times
without reaching for it.

The reason is visible once stated: the phrase is presented as **how to ask a
question**, and today's agent was not asking one — it was waiting. Nothing
told it the phrasing had a CONSEQUENCE, so it had no reason to want it. An
agent that used it would arm the marker by accident, not by understanding.

That re-scopes Phase 2 rather than killing it. Adding vocabulary is not the
fix; stating the consequence is — "phrase your stop this way and the failsafe
cron stops ticking" is the sentence that was missing. And it argues FOR Phase
3: a phrase whose effect is invisible gets used by luck, which is exactly the
failure mode an explicit token removes.

## 15:20 · finding · T5.2 — the "regular cadence" is not regular

Task 5.2 called the `R-STOP-AFTER-TOOL-ERROR` events recurring "at roughly
ten-minute intervals" the more suspicious signal, on the reasoning that a
regular cadence suggests something systematic. Measured over all 27 of today's
occurrences, the gaps are not regular:

```
gaps (s): 524 613 482 [23973] 508 3557 551 3294 1929 1682 751 995 557
          1212 1360 708 1125 879 564 639 595 1179 810 794 985 630
excluding the overnight outlier: min 482  median 794  max 3557
mean 1077  stdev 800  coefficient of variation 0.74
```

A timer produces a CV near zero. 0.74 is organic. The eye picked out the
cluster around 500-900s and did not see the 3557s ones. Nothing systematic is
firing on a schedule; the events track how often the agent trips a PreToolUse
block and then ends its turn, which in a long working session is naturally
bunched. Task 5.2 is answered, and the answer exonerates the code.

## 15:30 · finding · T5.1 — the ledger cannot answer the question it was asked

Task 5.1 asks for each DENY to be classified "productive continue" vs "wasted
turn" by correlating against the transcript. That correlation cannot be done
with what is recorded. A row is:

```json
{"decision": ..., "reason_prefix": ..., "stop_hook_active": ..., "timestamp": ...}
```

No session id, no turn id, no transcript offset — nothing that joins a row to
the turn it came from. So the classification Phase 5 rests on is blocked on an
instrumentation gap, not on effort.

Two things ARE measurable from the ledger alone, and one of them corrects this
plan's own evidence.

**The row count over-reports.** 1291 pairs across the whole ledger (43 of
today's 183 rows) are the same reason and decision logged twice, 51-162 ms
apart (median 79 ms), differing only in `stop_hook_active` flipping False to
True. That is one logical stop written twice. So the Evidence section's "28
DENY in ~34 minutes" is inflated; today's 183 raw rows are about 140 logical
stops.

**A third of stops are followed by another within ten seconds.** After
de-duplicating the sub-second re-fires, the gaps between consecutive stops
today fall out as:

```
<10s     50
10-60s    2
1-5m     63
5-30m    22
>30m      5
median 133s
```

The 1-5m and 5-30m bands are model turns doing real work. The 50 gaps under
ten seconds are not — nothing useful happens in ten seconds. Whether they are
the hook firing repeatedly for one stop or the model emitting a text-only turn
and stopping again is exactly what the missing turn id would settle, and the
difference matters: the first is a hook defect, the second is agent behaviour.

Recorded as a new Task 5.0: add a turn/session discriminator to the ledger
before attempting the classification, because without it any number produced
for Task 5.1 would be a guess wearing a percentage sign.

## 15:45 · finding · P2 — the guidance IS written; the rendering drops it

Chased where the arming vocabulary actually goes, because the plan's Evidence
says it is "documented in the wrong direction" and the mechanism behind that
turned out to be more specific than the phrasing suggests.

`failsafe_cron_blockage_suppressor.get_claude_md()` names the shape verbatim —
"blocked only on human input" — and the handler is not missing from the
injector: its provenance marker is present in `CLAUDE.md`.

```
stop name: auto-continue-stop            in markers: True
suppressor name: failsafe-cron-blockage-suppressor  in markers: True
```

But grepping `CLAUDE.md` for that phrase returns exactly one line, the rules
table row for `R-FAILSAFE-CRON-SUPPRESSED`, whose Fix column reads "Nothing to
do -- this is expected. Send a real message to clear the marker". So the
handler's prose was COMPRESSED to a table row on the way in, and the
compression kept how to CLEAR the marker and dropped how to ARM it.

The practical consequence for Phase 2: putting the sentence in the
suppressor's `get_claude_md()` would change nothing, because that is where it
already is. It has to land in `auto_continue_stop`'s own
`### Stop Explanation Required` section, which is rendered in full — and which
is also where the agent is already looking when it decides how to word a stop.
Task 2.1 already names that section; this is why it is the only option rather
than the convenient one.

## 16:00 · thought · P3/P4 — the precedent is real but it does not transfer whole

Task 3.3 asks for `ASKING BECAUSE:` to be cited as the in-project precedent
for an explicit agent-facing declaration. Read rather than assumed, and it
supports the principle while breaking on the mechanism.

`ask_user_question_blocker` (Plan 00108) requires the prefix on every
`question` string and DENIES the tool call without it, with a `strict` /
`advisory` mode pair so a project can dogfood the convention before turning on
hard blocking. Its own docstring says it "mirrors the Stop handler's
`STOPPING BECAUSE:` convention", so the family already exists.

Where it does not transfer: `ASKING BECAUSE:` is a **gate**. The agent wants
something (to ask a question), and the declaration is the price. Phase 3's
token is a **state marker** — the agent gains nothing by declaring, and the
daemon cannot deny a stop for failing to declare a state the agent may
genuinely not be in. So the token can never be mandatory the way the precedent
is; it will always be opt-in.

That has an ordering consequence worth stating before anyone builds it: Phase
3 makes arming RELIABLE for an agent that co-operates, and does nothing at all
for one that does not. Phase 4 — the ledger-driven backoff — is the half that
needs no co-operation, which is what makes it the load-bearing fix and Phase 3
the optimisation on top. The plan's Goals already say this ("even when the
agent declares nothing"); this is the argument for why the phase ORDER should
not be read as a priority order.

The `strict`/`advisory` rollout, on the other hand, transfers directly and is
worth reusing: ship the token recognised-but-unadvertised first, confirm it
arms, then document it.

## 16:15 · decision · T4.4 — the backoff folds into the suppressor, on position

Task 4.4 asks whether a third state-holder is justified against folding the
backoff into `failsafe_cron_blockage_suppressor`. Read both, and the answer is
not a preference — it falls out of where each handler sits in the event flow.

A backoff has to do four things: see every delivered tick, see every real user
prompt (to reset), hold a counter across ticks, and be able to drop a tick.
`failsafe_cron_blockage_suppressor` already does all four:

- `matches()` fires on the canonical cron prompt AND on any real prompt while
  a marker exists, so both edges of the reset are already on its path.
- `handle()` already branches on `is_cron_prompt`, already clears state on a
  genuine prompt, and already returns DENY to erase a tick before the model.
- It has an injectable clock (`self._clock`), which a backoff needs and which
  tests already substitute.
- It is fail-open throughout, which a cadence controller must be — a backoff
  bug that suppressed everything would silently disable recovery.

`recovery_cron_advisor` owns the cron's LIFECYCLE (create, reuse, delete
advice). It never sees a delivered tick as a prompt, so it cannot count them.
A third handler would have to re-derive the tick-recognition and the
prompt-reset that already exist here.

One thing that does NOT fold, and should be designed rather than assumed: the
backoff counter cannot live in the existing marker. The marker is cleared on
any real prompt and is absent whenever the agent has declared nothing — and
the truth table's bottom row ("no goals owed, no declaration → back off hard")
is precisely the case with NO marker. So the counter needs its own persistence
with its own lifetime; sharing the file would make the state vanish exactly
when it is needed.

## 16:30 · finding · T4.1 — the ledger answers a narrower question than the plan assumes

Read `live_plan_numbers` rather than trusting the summary in Task 4.1, and it
is narrower in two ways that both bias it towards "nothing owed" — the
direction that makes the cron back off.

```python
live = {
    e.plan_number
    for e in entries
    if e.retired_at is None and states.get(e.plan_number) == _STATE_IN_PROGRESS
}
```

**It only knows LEDGERED plans.** `entries` is the ledger's own contents, so a
plan is visible only if a `/goal` was ever set for it. Work on a plan that
never got one is invisible.

**Only `In Progress` counts.** The state resolver maps `Not Started` to
`_STATE_OTHER`; only `PlanStatus.IN_PROGRESS` yields `_STATE_IN_PROGRESS`.

Those combine into a concrete failure the truth table would walk straight
into, and it is the same rot Plan 00341 was just filed for: a plan being
ACTIVELY WORKED whose header still reads `Not Started` is not counted as owed.
The cron would read "no goals owed", find no declaration either, and take the
bottom row — back off hard — on a session that is working normally. That is
the opposite of what the row is for.

Two consequences for Phase 4, both recorded rather than fixed here:

- Task 4.1b's stated limit ("the ledger tracks PLAN goals, so non-plan work is
  invisible") is real but understates it. Even PLAN work is invisible if the
  plan was never ledgered or its header has rotted.
- This is a dependency on Plan 00341, not a coincidence. 00341 makes
  `Not Started` falsifiable; until it lands, the ledger's "owed" signal has a
  known blind spot that Phase 4 would inherit and amplify — a stale header
  currently costs a reader some confusion, and under Phase 4 it would cost the
  session its safety net.

