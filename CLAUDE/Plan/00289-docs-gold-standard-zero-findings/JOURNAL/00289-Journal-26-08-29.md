# Plan 00289 — Journal 26-08-29

> **Append-only activity log** for plan 00289. One file per day
> (`00289-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 13:27 · action · — — plan scaffolded

Plan 00289 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 14:05 · decision · Task A — root CLAUDE.md @-imports converted with distillation audit

All eight `@`-imports in root `CLAUDE.md` replaced with plain markdown links
(owner chose convert + distil over allowlisting). Per-import
distilled-vs-already-resident verdicts:

- `@CLAUDE/development/RELEASING.md` (x2): NOTHING added — the human-gated
  rule, state-file contract (`untracked/release-state.json`,
  `publish_authorised`), both blocking gates and the forbidden-commands table
  are already resident in CLAUDE.md's release section. Added an R4a
  safety-critical-restatement HTML marker to make that compressed restatement
  auditable, per DocumentationStrategy R4a.
- `@CLAUDE/CodeLifecycle/Features.md`: already resident — daemon-restart
  verification (the section's own Non-Negotiable Rule block) and TDD
  red/green/refactor (Engineering Principles). Added one orientation clause:
  "the full testing pyramid; unit tests alone are never done".
- `@CLAUDE/CodeLifecycle/Bugs.md`: the one genuinely missing load-bearing
  rule was reproduce-with-a-failing-test-BEFORE-fixing; added as the link's
  orientation clause.
- `@CLAUDE/CodeLifecycle/General.md`: already resident (QA Requirements +
  restart mandate); orientation clause "autofix, full QA and daemon restart
  before every commit".
- `@CLAUDE/PlanWorkflow.md` (x2): already resident — >2h plan threshold
  (Plans section), status icons + task grammar (`plan_qa_edit`/`plan_workflow`
  generated guidance), mkplan route (`plan_number_helper` guidance),
  terminal-state atomic commit (`plan_qa_commit_gate` guidance). Converted
  with no additions; the Documentation-list entry became a bare bold path
  matching its siblings.
- `@.claude/HOOKS-DAEMON.md`: generated doc; the resident `<hooksdaemon>`
  block IS the always-loaded summary. Plain link, no additions.

`docs-qa --lint CLAUDE.md`: 0 findings.

## 14:20 · finding · Task B — dogfooding bug: plan dir's own CLAUDE.md misread as a plan save

Writing the thinned `CLAUDE/Plan/CLAUDE.md` tripped `markdown_organization`'s
plan-mode interception: it scaffolded a spurious `00290-claude/PLAN.md` and
advanced the plan counter, because `_PLAN_ROOT_EXCLUDED_FILES` excluded only
`readme`. Fixed immediately per the dogfooding rule (TDD, one-line fix,
daemon restart verified RUNNING, counter restored, spurious folder removed)
in commit 7b3e8985.

## 14:22 · decision · Task B — CLAUDE/Plan/CLAUDE.md thinned to 27 lines

Disposition: THIN (not register). The lifecycle steps, completion checklist
and quick-reference commands duplicated `CLAUDE/PlanWorkflow.md` (the
canonical home) and were replaced by pointers. Retained tersely, as unique
local conventions with no other home: GitHub-issue plan sources +
close-on-complete, the commit-plan-folder-alongside-work guard, the
`PLAN-v1`/`CRITIQUE-v1` supersede naming convention, and a one-line
atomic-archive reminder pointing at the checklist.

## 14:35 · decision · Task B — src/CLAUDE.md (27 lines) and tests/CLAUDE.md (19 lines) thinned

Both are client-repo edit guards. Disposition: THIN (not register) — the
guard statement is the module-local content; the step-by-step bug-report
procedure they carried duplicated `BUG_REPORTING.md` (canonical) and the
project-handler/test instructions duplicated `CLAUDE/PROJECT_HANDLERS.md`
(canonical). Each is now guard + one-line alternatives with links. Relative
links preserved so they resolve inside a client's `.claude/hooks-daemon/`
checkout.

## 14:45 · decision · Task B — status_line/CLAUDE.md thinned to a 26-line edit-guard pointer

Disposition: THIN (not register). `CLAUDE/Architecture/StatusLine.md` already
owns the full concurrency contract verbatim (its "Concurrency & Thread
Safety" section), so the module doc's near-duplicate restatement became a
pointer naming the three rules in one clause plus the conforming reference
files. The hand-written handler table was a prose index duplicating the
generated `.claude/HOOKS-DAEMON.md` roster (R7d forbids prose indexes;
R5 forbids restating derived rosters) — replaced with a pointer to the
generated doc. StatusLine.md's back-reference updated to describe the module
doc as an edit-guard pointer rather than a "package-level checklist".

## 14:55 · decision · Task B — qa/CLAUDE.md thinned from 277 to 31 lines

Disposition: THIN (not register, not promote). The bulk was a hand-written
API mirror of `runner.py` — class/method rosters, per-tool output-format
samples, JSON schema examples, timing figures — which R7d explicitly
disqualifies and whose canonical home is the module source and its
docstrings (CLAUDE/CLAUDE.md's "What NOT to Put Here" already routes API
documentation to docstrings). Nothing was promoted because nothing general
was homeless: the content restated what the code states. Retained the
genuinely local facts: entry points (import path, `run-qa-runner.sh`
invocation), exit codes, the resilient-execution invariant, the
this-is-not-the-project-QA-gate distinction (pointer to CLAUDE/QA.md), and
the test file location.

## 15:10 · decision · Task B — strategies/tdd/CLAUDE.md PROMOTED to CLAUDE/Code/StrategyPattern.md

Disposition: PROMOTE + THIN (not register). The 705-line doc was general
strategy-pattern architecture documentation — the archetype every
language-aware domain (lint, security, qa_suppression, pipe_blocker,
comments, error_hiding) must copy — so it fails R7d's outside-reader test
for a module-local home: its intended reader is an agent building a
strategy set ANYWHERE, not just editing this subtree. The depth now lives
in `CLAUDE/Code/StrategyPattern.md` (routing row added to CLAUDE/CLAUDE.md);
the module doc is a 30-line pointer keeping only the directory's edit
guards.

Deliberately NOT carried into the promoted doc, per R5/R7d (pointed at
their sources instead):

- the 11-language "Registered Languages" table (rosters live in
  `registry.py:create_default()`; the human summary is root CLAUDE.md's
  supported-languages table)
- the per-language `compute_test_filename` example roster (each strategy's
  docstring records its convention)
- the File Index and test-file listing (prose indexes of the filesystem —
  forbidden, rot fastest)
- the verbatim Protocol code block (protocol.py is the contract; the doc
  describes the shape and points)

No live references to the old path existed outside archived plan folders.

## 15:20 · decision · Task C — release-agent.md duplicate examples replaced with a pointer

The flagged `release-agent.md:487-489` span was the tail of a worked
v2.12.0 rename example copied verbatim from
`CLAUDE/UPGRADES/upgrade-template/BREAKING-CHANGES-TEMPLATE.md` (Example 2).
Rather than ssot-quoting it, the agent file's whole inline "Examples"
section (both the v2.11.0 removal and v2.12.0 rename examples) was replaced
with a pointer to the template — R7c says agent files carry role framing +
pointers, and the template is the canonical home for its own worked
examples. Also skipped: `.claude/ccy/CLAUDE.md` (already registered; its
lingering budget advisory is the sweep-side registration bug the src half
of this plan owns).

## 16:05 · action · SRC half — Task 1 done: pointer-resolves absolute-path fix

TDD (RED confirmed, then GREEN): `_resolves()` in
`docs_qa/checks/pointer_resolves.py` now tries the literal absolute path
first (`Path(target).exists()`) before falling back to the repo-root-relative
join — fixes the double-root bug (`/workspace/x` under `project_root ==
/workspace` was joined as `/workspace/workspace/x`). Two new tests in
`tests/unit/docs_qa/checks/test_pointer_resolves.py`
(`test_fully_qualified_absolute_path_link_resolves`,
`test_fully_qualified_path_does_not_double_root`). 29/29 passing.

## 16:20 · action · SRC half — Task 2 done: module-doc-budget registered-doc tiering

TDD: replaced the test that encoded the buggy behaviour
(`test_registered_under_block_tier_is_advise`, which asserted a 50-line
registered doc was ADVISE) with
`test_registered_well_under_advisory_tier_is_clean` plus a new
`test_registered_over_advisory_under_block_tier_is_advise`, and a matching
sweep-side regression test. Fix: `_tiers_for()` now gives a registered doc
its own advisory tier (`plan_qa`'s `DEFAULT_PLAN_DOC_ADVISORY_LINES`, 350
lines) instead of reusing the unregistered 40-line routing-table budget.
Confirmed both EDIT and SWEEP arms shared the bug (both route through
`_finding_for`/`_tiers_for`), so one fix covers both. 26/26 passing.

## 16:35 · decision · SRC half — Task 3: scope_exclude_globs added

New `documentation.qa.scope_exclude_globs` config field
(`config/models.py`, `docs_qa/policy.py`) plumbed into
`docs_qa/corpus.py::_is_excluded` via `_matches_scope_exclude`, checked
against both the full rel-path and the bare basename. This project's
`.claude/hooks-daemon.yaml` now sets:

- `CLAUDE/UPGRADES/v[0-9]*/**` — frozen per-version upgrade guides (live
  siblings README.md/upgrade-template/UNRELEASED/truth-changes/config-changes
  unaffected)
- `PLAN-v[0-9]*.md` — superseded plan drafts kept beside a live PLAN.md
- `*archived*.md` — self-labelled archived records

Rejected a broader `*-v[0-9]*.md` full-path pattern first: it also matched
LIVE docs whose enclosing directory happened to be version-named (e.g.
`CLAUDE/Plan/00104-v3.10.0-.../PLAN.md`), verified via a repo-wide scan
before settling on the narrower basename-only patterns. The old
`grandfather_allowlist: ["CLAUDE/UPGRADES/**"]` entry removed — superseded
by the scope exclusion for the frozen dirs, and was never meant to cover
the live siblings. Full decision record in PLAN.md's Technical Decisions.
Tests: 76/76 passing in `tests/unit/docs_qa/test_corpus.py`, plus
`test_policy.py`/`test_documentation_config.py` updated.

## 16:40 · action · SRC half — Task 4 done: upgrade-template dead link fixed

`CLAUDE/UPGRADES/upgrade-template/README.md`'s "Full changelog" link was
`../../CHANGELOG.md` (wrong depth, resolves to non-existent
`CLAUDE/CHANGELOG.md`); fixed to `../../../CHANGELOG.md`. Checked
`BREAKING-CHANGES-TEMPLATE.md`'s apparently-similar links: they sit inside
fenced ` ```markdown ` example blocks, never seen by `extract_link_targets`
(fence-aware) — no real defect there.

## 16:45 · handoff · SRC half complete — sweep at zero

Sweep after src-half fixes alone: 34 → 3 (2 module-doc-budget on files the
docs half was concurrently thinning, 1 duplicate-block on
`release-agent.md` which the docs half then resolved in
Plan 00289 Task C). Re-ran `docs-qa --sweep --json` after the docs half's
Task C commit landed: **zero findings, whole repo**. QA suite
(`./scripts/qa/llm_qa.py all`) running in background; daemon restart
verification and final commits to follow once it completes.
