# Plan 00454 — Journal 26-09-23

_Scaffolded by `mkplan.bash`; timestamps in this file are UTC._

> **Append-only activity log** for plan 00454. One file per day
> (`00454-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` UTC 24h (the date is in the filename, also UTC); times increase
>   down the file. A file with no sentinel line above predates this system —
>   its times are local, zone unrecorded (legacy; never rewritten).
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 18:54 · action · — — plan scaffolded

Plan 00454 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 18:55 · action · — — crons re-established, failsafe reused

The session restarted on a model change, dropping every cron. Recreated the
declared `issue-sdlc` job (**`fcb832e5`**, `:23`) and one failsafe recovery
cron (**`3c3c53c3`**, `:47`). This plan reuses `3c3c53c3` — one per session,
never one per plan.

## 18:55 · decision · — — scope read from a bare "continue"

The owner had been offered two precise unblocking phrases ("2′" for Plan 00388,
"do the two small ones" for #53) and replied "continue". Read as go-ahead on
the work already flagged as needing no design call — #53's message — and NOT
on 00388, which needs an approach chosen. Stated to the owner so a different
reading can be corrected.

## 18:55 · finding · — — the published triage was wrong; the reporter was right

Checking the message's advice before rewriting it overturned a claim already
published on #53. That comment said the skill install "does not force a full
re-clone" because its probe no-ops when healthy — relayed from a verification
sub-agent, and true of the code in general, but not of THIS state:

- `_installation_is_healthy` globs every `venv-*` and passes only if one both
  exists (`-x`) and imports the package. The other view's venv fails `-x`
  (dangling interpreter symlink) or the import (editable path points at the
  other view's source). It cannot pass.
- `install.sh:149` then escalates to `--force` unprompted.
- Root `install.sh:72` runs `rm -rf "$DAEMON_DIR"`, deleting the other venv.

Posted a correction on #53 immediately. Same error class as Plan 00422 N17,
and worse for landing in public: a sub-agent's general claim accepted without
checking it against the specific state reported.

## 18:55 · finding · T1.2 — the safe command exists and was built for this

A same-version upgrade is the fix to name. Verified the whole chain rather
than assuming it:

- skill `upgrade.sh` is a shim: fetches root `scripts/upgrade.sh` and execs it
  — no venv resolution, so no `repair`-style paradox.
- root `upgrade.sh` deletes nothing relevant (its one `rm -rf` is a nested-
  install cleanup at line 657) and never re-clones.
- `upgrade_version.sh:336` — the idempotent same-version path — calls
  `ensure_venv` first, commented "concurrent environments (container vs host,
  different Pythons) don't clobber each other".

Pinning the upgrade to the clone's own version (`_clone_version()`, a `grep`
of `version.py` needing no venv) keeps it on that path: build the missing
venv, change nothing else.

## 18:55 · decision · — — dedupe recorded rather than re-run

No scout dispatched. This slice was already checked against prior art in the
same session — `find-plan venv` (ten plans) plus direct reads of 00313, 00100
and 00431. Distinct from N16, which was filed with no check at all.
