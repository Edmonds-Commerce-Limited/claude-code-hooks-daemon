# Plan 00327 — Journal 26-09-08

> **Append-only activity log** for plan 00327. One file per day
> (`00327-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 15:15 · action · T1.1 — raw capture via remote-docs, kept untracked

Executed under Plan 00362 Task 2.11 (R1 in its defect ledger). Captured
`https://code.claude.com/docs/en/hooks.md` with
`bin/hooks-daemon remote-docs add --verbatim`; the body hashes to
`c30a50b8…` (317 632 bytes), which differs from `META.json`'s `d514bf57…`
AND from the `e2462deb…` this plan's Overview quotes — upstream moved again
between 2026-09-03 and today. The capture was then removed from the
`remote-docs/` tree (and its generated index) so it stays untracked as this
task requires; the body lives at `untracked/hooks-raw.md` in the worktree.
The previously audited raw text was still on disk in the main checkout at the
same path, with a hash matching `META.json`, so the diff is exact rather than
reconstructed.

## 15:20 · finding · T1.2 — the delta is four prose hunks, zero claims

`diff -u` between the two raw texts is 42 lines. Full worklist and per-event
table in `DELTA-2.1.252-to-2.1.263.md`. Takeaway: PermissionRequest's
`permission_suggestions` prose was narrowed (the array is no longer described
as the dialog's exact "always allow" list), the `setMode` bypass note was
re-sentenced, and DirectoryAdded's does-not-fire list gained "or inside one".
None of those sentences is the source of any vendored claim; no heading was
added or removed, so the 33 files still cover every documented event.

## 15:25 · action · T2.1, T2.3 — extraction verified, QA re-run

Every schema re-checked against the refreshed text (the two events whose
sections changed re-read in full): 33 UNCHANGED, 0 CHANGED, 0 NEW-UPSTREAM.
`llm_qa.py hook_contract` and `input_contract` both 0 violations with no
stale allowlist entry. `META.json` regenerated by script from the captured
bytes (`fetch_date`, `docs_bytes`, `docs_sha256`, version 2.1.263,
`event_count`), and `TestMetaProvenance` pins those values so the next
refresh has to move both together.

## 15:30 · handoff · P3 — what is left for this plan

Task 3.1's "advisory silent on a fresh session" half is not yet observed:
the daemon was deliberately not restarted from the worktree. Tasks 3.2 and
3.3 (the `contract-status` command and trimming the guide) are untouched and
remain this plan's own scope, not Plan 00362's.

## 15:50 · finding · T3.2 — the verb's first real run caught a second upstream move

`contract-status` (`daemon/contract_status.py`, TDD from
`tests/unit/daemon/test_contract_status.py`, fetcher injected) reported
CHANGED on its first run: upstream is `ac2f68e8…` at 317 650 bytes, 18 past
this morning's `c30a50b8…`. The main checkout's `untracked/hooks-raw.md` is
still the 2.1.252 text, so the diff against it showed the four known hunks
plus one: the ConfigChange WSL sentence's link retargeted from
`/docs/en/settings#available-settings` to
`/docs/en/settings-reference#wslinheritswindowssettings` — exactly +18
bytes, sentence text unchanged, no vendored claim derived from it. Recorded
as hunk 5 in the delta document; `META.json` and `TestMetaProvenance` moved
to the new hash together; `contract-status` then exits 0.

## 15:55 · action · T3.1, T3.3, QA — loop closed

- Task 3.1: `TestVendoredMetaIsCurrent` drives the handler against the real
  `META.json` with the installed version pinned to 2.1.263 (which is what
  `claude --version` reports here) on a `startup` session: empty context.
  The maintainer advisory now names `hooks-daemon contract-status` as its
  first step.
- Task 3.3: guide steps 1-2 replaced by the command and an exit-code table;
  the RAW-fetch rule and steps 3-7 (renumbered 1-5) kept.
- `input_contract` had one violation inherited from main: Plan 00362 Task
  1.4's `scratchpad_dir` read on PreToolUse (`db1378ee7`). The field appears
  nowhere in the upstream text, so per this plan's Non-Goals it is a docs
  gap, not a claim: `INPUT-ALLOWLIST.yaml` entry with reason and link. Both
  contract checks now 2/2 through `llm_qa.py`; `type_check`, `lint`,
  `format` 3/3.
- `tests/unit/qa/test_audit_error_hiding.py` has two failures naming
  `pipe_blocker.py:766` and `scripts/upgrade_version.sh:812`; neither file
  is in this plan's working set, so they belong to whichever plan touched
  them (Plan 00362's echd-capture work is the likely owner).
- Callout: `UNRELEASED/release-notes/27-contract-status-verb.md`.

## 16:00 · handoff · — plan Complete on branch, not merged

Code, contract refresh, guide and callout in `8246f7f0`; PLAN.md ticks and
this entry in the following commit. Branch
`agent-a184203a05f3b797c-6bbda017`, not pushed, not merged; the coordinator
archives the folder and adds the README row.
