# Plan 00278 — Journal 26-08-27

> **Append-only activity log** for plan 00278. One file per day
> (`00278-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 06:39 · action · — — plan scaffolded

Plan 00278 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 06:52 · decision · — — plan filed, design settled

- Feature requested by joseph: a session downgraded from fable to opus must
  not run opus at fable's low effort — the supervisor should inject
  `/effort xhigh` on detecting the downgrade.
- Dedupe scout checked 35 live plans: no overlap; Plan 00035 (Blocked) is
  related infrastructure only.
- Recovery cron: reusing the session's existing recurring failsafe cron
  (id f07b4ca0); no new cron created.
- Design: sensor = context sidecar (already carries model_id; add nullable
  `effort`); actuator = supervisor tick loop, modelled on the Plan 00269
  goal-injection family; ranked model-family downgrade detection
  (fable/mythos > opus > sonnet > haiku).

## 06:58 · decision · — — scope widened to bundle prevention

joseph (mid-turn): also steer security-related work into an Opus subagent so
the fable main context never accumulates downgrade-triggering material;
bundle both ideas in one plan since dogfooding is cross-related. PLAN.md
retitled "Model-Downgrade Resilience", Phase 3 (delegation advisory, design
task first) inserted, closure renumbered to Phase 4.

## 07:04 · action · T1.1 — sidecar effort field shipped

RED (4 KeyError failures) → GREEN: `ContextSidecarHandler` payload now
carries `effort` (string | null) from the live `effort.level` Status field
only; malformed/absent → null. 43/43 sidecar tests pass; daemon restarted
RUNNING.

## 07:20 · action · P2 — supervisor effort-restore family implemented

TDD (17 new tests in test_effort_restore.py, 10 RED → all GREEN, full
supervise suite 399/399): SidecarReading gains model_id/effort;
family ranking fable/mythos>opus>sonnet>haiku; machine tracks
(session, family), opens a pending episode on ranked downgrade, clears on
recovery/xhigh/session change; decide_once fires WOULD_EFFORT →
`/effort xhigh` subordinate to compact/continue/goal with the same
idle+empty-box gates; success-only cap marking at both host call sites;
state rides export/import for the worker round-trip. Full QA launched in
background.

## 07:28 · decision · — — three more directions from joseph (mid-turn)

1. Per-model MINIMUM effort config (fable: low, opus: high, sonnet: high) —
   Task 2b.2; guide agent dispatched to check for an official mechanism
   first (Task 2b.1).
2. Field report untracked/hooks-daemon-fable-cyber-flags.md vetted (already
   sanitised at source; reporting project's own plan numbers stripped on
   import) → FIELD-REPORT-fable-cyber-flags.md in this plan folder;
   untracked original deleted. Its daemon-handler proposals folded into
   Task 3.1.
3. Model restore: inject `/model fable` a configured interval after the
   downgrade (session-sticky fallback, but manual flip-back works) —
   Task 2b.3, with flip-flop backoff.

## 07:48 · action · T2b.2 — per-model effort floors shipped (one unified family)

Guide agent confirmed NO official per-model effort mechanism (Decision 4).
Reworked the effort family: `CompactPolicy.min_effort_levels` (defaults
fable=low, opus=high, sonnet=high, haiku=low; env override
`CCY_MIN_EFFORT_LEVELS="fam=level,..."`, junk-tolerant parse); pending key
recomputed from every fresh reading — target = xhigh during a downgrade
episode, else the family floor; fires `/effort <target>` only when live
effort ranks strictly BELOW target (joseph's invariant: RAISE-ONLY — pinned
by test_never_lowers_effort_above_floor); 180s re-inject cooldown covers the
sidecar's stale-effort lag after a fire; unknown effort injects only inside
a downgrade episode. New state (downgrade_episode, last_fired key/ts) rides
export/import. 406/406 supervise tests. Also recorded joseph's 2b.3
amendment: after a successful `/model fable` flip-back, effort resets DOWN
to fable's floor (the one sanctioned lowering — fable xhigh eats allowance).

## 08:30 · decision · — — two more features from joseph

1. Downgrade SNAPSHOT capture (Task 3b.1): on a detected cyber downgrade,
   write a diagnostic snapshot (fallback record + bounded preceding
   transcript window, redacted) to a configurable reports dir so projects
   can diagnose why they get flagged and tune their delegation config.
2. Config surface (Task 3c.1): every 00278 feature configurable with the
   additive/replace (clobber-or-extend) convention.

## 09:20 · action · T2b.3 — model flip-back + post-flip effort reset shipped

WOULD_MODEL family: after `CCY_MODEL_RESTORE_SECONDS` quiet delay (default
900s; "off" disables) an open downgrade episode injects `/model <from>`;
lifetime cap 2, 3600s flip-flop backoff; host marks success which arms the
post-flip reset — when the sidecar confirms OUR restore landed, effort
lowers to the restored family's floor (the one sanctioned lowering); a
recovery we did not cause is left alone. 415/415 supervise tests; format
autofix; full QA 25/25.

## 13:45 · action · P3 merge + dogfood (main session)

Phase 3 branch merged --no-ff (86aeb926); three conflicts resolved keeping
both sides (PLAN Decision 5 — agent's executed version + the Phase 3d
deferral note; both manifest entries; all three error-hiding exclusions).
Daemon restarted RUNNING, zero log errors. Dogfood (Task 3.3):
model_fallback_detector silent on this clean session's start (correct);
flaggable_work_advisor fired once on a two-seed-term Bash command, naming
Agent(subagent_type: "hooks-daemon-opus-security") with the
delegate-before-reading guidance, then stayed quiet (rate limit). Decision 6
records the daemon-YAML vs supervisor-env config split; 3c.1 complete.

## 2026-08-27 — Phase 3/3b/3c(daemon): the two advisory handlers (worktree agent)

Executed in isolated worktree branch agent-a0418c50fb220e49b-3c789121, TDD
(RED confirmed before implementation; 42 new unit tests).

- Added `model_fallback_detector` (SessionStart, priority 63, ADVISORY,
  default-enabled): streams the transcript JSONL with a cheap substring
  pre-filter, detects `model_refusal_fallback` records plus assistant
  `fallback` content blocks, injects the loud degraded-model alert (original/
  fallback model + refusal category + restart guidance), and writes a
  secret-redacted diagnostic snapshot (record + bounded preceding window) to
  `snapshot_dir` (default untracked/reports). Once per session per distinct
  record; snapshot failure degrades to an advisory mention.
- Added `flaggable_work_advisor` (PreToolUse, priority 58, ADVISORY,
  ships disabled via get_default_enabled): path-glob / Bash-mention /
  2+-topic-term triggers, delegate-before-reading advice naming
  `quarantine_agent` (default hooks-daemon-opus-security), additive|replace
  merging, once-per-session-per-path rate limit.
- Plumbing: HandlerID + Priority constants, package exports, init_config
  template entries, project yaml entries, dogfooding + template-consistency +
  guidance-coverage gates updated, CLAUDE.md/HOOKS-DAEMON.md regenerated via
  `regenerate-docs`, HANDLER_REFERENCE.md sections, config-changes
  vUNRELEASED manifest entries for every new key.
- Deviation: flaggable_work_advisor is dogfood-ENABLED in this repo's yaml
  (topic seed only, no globs) because the guidance-coverage gate requires an
  earning handler's section to be present in the regenerated CLAUDE.md and
  the injector only renders enabled handlers — same pattern as
  goal_injection. Upstream default remains disabled.
- Recorded Decision 5 in PLAN.md; ticked 3.2 and 3b.1; 3c.1 marked 🔄 with
  the daemon-side half delivered.

## 2026-08-27 — Phase 3 QA closure (worktree agent)

Full `llm_qa.py all` run from the worktree (worktree src on PYTHONPATH,
venv symlinked into worktree untracked/): 24/25 green including tests
(14,600+ passed, coverage back over 95% after edge-branch tests took both
new modules to ~100%), error_hiding (two recorded exclusions for the
detector's documented fail-silent contract), semgrep, project_handlers
(69 passed). The one red, smoke_test, was the temporary worktree daemon
having exited between runs — restarted it and `llm_qa.py smoke_test`
passes 3/3; effectively a clean suite. A daemon was started FROM the
worktree source and reported RUNNING with both new handlers loaded (the
CLAUDE.md/HOOKS-DAEMON.md guidance blocks were regenerated through the
same controller path via `regenerate-docs`); the main /workspace daemon
was never touched. Temporary worktree daemon stopped afterwards.

## 09:20 · action · T4.1/T4.2 — Phase 4 closed; live dogfood catch

Background QA on the merged tree: 25/25 PASSED (commit f0a9dc33 pushed);
daemon RUNNING; supervisor lockstep test green in the suite. Tasks 4.1 and
4.2 ticked.

Best possible dogfood datum: on this morning's session start
`model_fallback_detector` fired FOR REAL — the platform had silently
substituted this session's model yesterday 17:16 (category: cyber,
scope: session), and the new handler surfaced it loudly and wrote two
diagnostic snapshots to untracked/reports/. Exactly the silent-degradation
scenario the plan exists to catch. Human informed; session restart is the
cure. Plan stays In Progress pending Phase 3d (deferred by design) / T4.3.
