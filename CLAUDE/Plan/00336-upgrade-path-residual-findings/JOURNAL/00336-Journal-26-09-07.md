# Plan 00336 — Journal 26-09-07

> **Append-only activity log** for plan 00336. One file per day
> (`00336-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 00:56 · action · — — plan scaffolded

Plan 00336 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 01:20 · finding · P1 — the "old default" baseline is the NEW default on the documented path

Found while fixing the six v3.62.0 field defects, not reported by the client.
`scripts/install/config_preserve.sh` Step 5 carries the comment "Save the old
example config before checkout (for diff baseline)" and copies
`$EXAMPLE_CONFIG` (`$DAEMON_DIR/.claude/hooks-daemon.yaml.example`) into
`$OLD_DEFAULT_CONFIG`. On the Layer 1 path that comment is false: `upgrade.sh`
checks the target out first and never preserves the pre-checkout example, so
the "old" default is the NEW one.

Verified only that Layer 1 does no such copy:

```
$ grep -n "hooks-daemon.yaml.example\|EXAMPLE_CONFIG\|OLD_DEFAULT" scripts/upgrade.sh
(no output)
```

The consequence is REASONED, not yet measured, and Task 1.2 exists to measure
it before anything is changed. Expected: with `OLD_DEFAULT == NEW_DEFAULT`, a
user value equal to the old default differs from the new one and is therefore
classified as a customisation and preserved — so a changed default never
propagates. The direction is the safe one (preserve rather than silently
change), which is likely why it has gone unnoticed.

## 01:22 · decision · P2 — shipped the full second pass, not a resume

Layer 2 now fingerprints its own source before Step 6's checkout and, on
detecting the swap, re-execs the target's script at the END of the run for a
full idempotent second pass. A resume-at-Step-7 design would avoid doing the
work twice, but it needs `SNAPSHOT_ID`, `CONFIG_BACKUP`, `OLD_DEFAULT_CONFIG`
and `CURRENT_VERSION` handed to the child and Steps 1-6 restructured into a
skippable region — a refactor of the 1200-line script that upgrades every
client, which cannot be end-to-end verified here.

Chosen deliberately: the second pass is exactly the manual re-run the field
report confirmed recovers the situation, performed automatically. Recorded so
the next reader knows it is a trade, not an oversight. Task 2.1 revisits it.

Probed `exec` against the rollback trap before relying on it — the EXIT trap
does NOT fire on `exec`, so the re-exec cannot trigger a spurious rollback:

```
$ trap 'echo "TRAP FIRED rc=$?"' EXIT; echo before; exec bash -c 'echo replacement'
before
replacement          # "TRAP FIRED" never printed
```

## 01:24 · handoff · — — filed, not started

Everything in this plan is follow-up; the six defects themselves are shipped
(`b1217789`, `a9866261`) with QA 25/25 and live verification. Dedupe scout
checked 44 live plans and found no coverage. Start at Phase 1 — it is the only
item with an ongoing effect on every client.

## 01:38 · finding · T1.2 — measured: a changed default does NOT propagate

Correcting the 01:20 entry, which recorded the consequence as reasoned rather
than measured and hedged it as "expected". It is now measured, and the
reasoning held. Task 1.2 is complete.

Drove `ConfigDiffer` + `ConfigMerger` directly with a default that changed
between versions and a user who simply accepted the old one:

```python
OLD_DEFAULT = {"version": "1.0", "daemon": {"log_level": "INFO",    "timeout": 30}}
NEW_DEFAULT = {"version": "2.0", "daemon": {"log_level": "WARNING", "timeout": 30}}
USER        = {"version": "1.0", "daemon": {"log_level": "INFO",    "timeout": 30}}

diff   = ConfigDiffer().diff(USER, baseline)
merged = ConfigMerger().merge(NEW_DEFAULT, diff).merged_config
```

```
TRUE old default (intended):
   custom_daemon_settings = {}
   merged log_level       = WARNING
   new default propagated = True
NEW default as baseline (Layer 1 path):
   custom_daemon_settings = {'log_level': 'INFO'}
   merged log_level       = INFO
   new default propagated = False
```

So the effect is exactly as predicted: a user value equal to the OLD default
is misread as a deliberate customisation and pinned, and the new default never
arrives. Note the trap for whoever writes the fix — this is indistinguishable,
at the data level, from a user who genuinely set `INFO` on purpose. The
baseline is the ONLY thing that separates "accepted the default" from "chose
this value", which is why restoring a true old-version baseline (Task 1.3) is
the fix rather than any heuristic applied at merge time.

## 01:40 · handoff · P1 — Task 1.2 done, 1.1 and 1.3 open

Phase 1's premise is fully established: Layer 1 checks out before delegating
(line 419 vs 457, pinned by
`tests/integration/test_upgrade_runs_the_target_versions_steps.py`), Layer 1
never preserves the example config (grep returns nothing), and using the new
default as baseline demonstrably stops changed defaults propagating. What
remains is Task 1.1's regression test and Task 1.3's fix.

## 08:05 · action · T1.1 — RED, 7 of 9

`tests/integration/test_upgrade_old_default_baseline.py` written first. The two
that PASSED from the start are the consequence guards driving
`ConfigDiffer` + `ConfigMerger` with a correct baseline — they already worked,
which is the point: the Python side was never wrong, only the baseline fed to
it. The seven RED ones pin Layer 1's capture-before-checkout ordering, the
export, and the resolver's four behaviours.

## 08:12 · decision · T1.3 — handover, not `git show`

Two candidate fixes were on the table in PLAN.md. Chose Layer 1 preserving the
file and exporting a path, and rejected having Layer 2 read the old default out
of git (`git show <previous-ref>:.claude/hooks-daemon.yaml.example`).

Reason: on the Layer 1 path Layer 2 no longer knows the previous ref — its own
`ROLLBACK_REF` resolves to the TARGET, because the checkout already happened.
So the git approach would need Layer 1 to hand over the ref, which is the same
handover with an extra step and an extra failure mode. One mechanism, used by
both entry points.

Placement detail worth keeping: the preserved copy goes OUTSIDE `DAEMON_DIR`.
Layer 1's checkout is `git reset --hard` for a normal (non-self-install)
install, which would discard anything staged inside the tree.

The resolver is a CAPTURED function (`VAR=$(resolve_old_default_config ...)`),
so every progress line in it is `>&2`. This repo's v3.10.0 SEV-1 was one
missing `>&2` in exactly that shape, and there is a QA gate for it —
`audit_capture_corruption.py` reports no violations, and a test asserts stdout
is a single usable path.

## 08:20 · finding · T1.3 — live verification, both directions

Not just "tests pass". Ran the real `merge_custom_config` against a user who
had simply accepted the old default (`log_level: INFO`) while the default moved
to `WARNING`:

```
=== WITHOUT handover (baseline = new example) ===
  log_level: INFO        <- old value pinned, new default lost
=== WITH handover (baseline = old example) ===
  log_level: WARNING     <- new default propagates
```

Same code path, same inputs, one env var apart. QA 25/25, 18165 tests, daemon
restarted (pid 3665708) before the verification.

## 08:22 · handoff · P1 — Phase 1 complete

Tasks 1.1, 1.2 and 1.3 all done. Task 1.3's fix touches three files:
`scripts/upgrade.sh` (Step 3c capture + export),
`scripts/install/config_preserve.sh` (`resolve_old_default_config`), and
`scripts/upgrade_version.sh` (Step 5 uses the resolver). Phases 2-4 are
untouched; Phase 2 (the second-pass vs resume decision) is the next largest
item.

## 08:50 · decision · T2.1 — keep the second pass; the gap is narrower than it looked

Layer 1 checks out at line 440 and invokes Layer 2 at 478, so Layer 2 starts as
a FRESH process after the checkout — its script body and its sourced
`install/*.sh` libraries are all the new release. The "post-checkout steps run
from old code" problem is therefore confined to DIRECT Layer 2 invocation,
which the Defect 4 documentation fix now steers people away from, and where the
second pass already covers newly-added steps.

Considered a cheaper alternative: re-`source` the install libraries right after
Step 6's checkout, so the old script body would at least call new library
functions. REJECTED — `gitignore.sh` and `rollback.sh` declare `readonly`
variables, and re-assigning a readonly aborts under `set -e`. Selective
re-sourcing would work but is fragile as libs change.

Boundary worth recording (T2.4): only the SHELL is stale on the first pass.
Step 7 rebuilds the venv from the new checkout, so every `$VENV_PYTHON -m
claude_code_hooks_daemon...` call from Step 8 on runs the NEW Python. A fix in
`config_differ.py` reaches the upgrade that delivers it; a fix in
`config_preserve.sh` does not.

## 08:55 · finding · T2.5 — config merge never runs on a standard Layer 1 tag upgrade

Chasing the above turned up something larger. Under Layer 1 with a tag target,
Layer 2's Step 2 finds `ROLLBACK_REF == TARGET_VERSION`, takes the idempotent
fast path, and `exit 0`s at line 418. `preserve_config_for_upgrade` is called
from exactly ONE line — 861, Step 10 — which the fast path never reaches.

Verified three ways rather than inferred: Layer 1 contains no config-merge call
(`grep` returns nothing); the fast path region 269-418 contains none either;
and the only call site in the whole `scripts/` tree is line 861.

The fast path is otherwise comprehensive — venv, hooks, slash commands, skills,
plan workflow, core docs, ccy supervisor, relay, daemon restart, post-install
checks — which is why this is easy to miss.

Impact deliberately NOT overstated: this is staleness, not data loss. The
config is left untouched and the Pydantic schema supplies defaults for absent
keys, so runtime behaviour still follows the new defaults; the config FILE just
never gains the new entries. The config-optimisation review may be the intended
adoption mechanism. Open question for the owner, recorded as T2.5.

## 09:00 · finding · T1.3 — CORRECTION to the 08:20 entry and commit c85d5e90

The Phase 1 commit message says the baseline "on the documented upgrade path
... was the wrong one". That is too strong, and the T2.5 finding above is why:
on the documented Layer 1 TAG path, Step 5 does not run at all, so the baseline
bug cannot bite there.

Precise conditions, which the fix still covers:

- Direct Layer 2 with a tag → `ROLLBACK_REF` is the PRE-checkout ref, so it
  differs from the target → slow path → Step 5 runs BEFORE Step 6 → the
  on-disk example genuinely IS the old default. The baseline was already
  correct here, and the new resolver is a harmless no-op (no handover set, so
  it falls back to exactly the previous behaviour).
- Layer 1 to a NON-tag ref (a branch, a SHA, `HOOKS_DAEMON_UPGRADE_REF=main`),
  or any case where `git describe --tags --exact-match` fails (shallow clone,
  tags not fetched) → `ROLLBACK_REF` differs from the target → slow path →
  but the checkout has ALREADY happened → Step 5 copies the NEW default. This
  is the case the fix corrects.

So the fix is correct, narrower than the commit message claimed, and harmless
on every other path. The overstatement is corrected here rather than by
rewriting the earlier entry, per the append-only rule.
