# Plan 00416 — Journal 26-09-15

> **Append-only activity log** for plan 00416. One file per day
> (`00416-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 15:12 · action · — — plan scaffolded

Plan 00416 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 17:52 · action · P1 — both Phase 1 halves merged into main

Task 1.1 (cron teeth) and Task 1.2 (tier mechanism) were built in parallel
worktrees and are now merged. Neither touched the other's files; the only
cross-branch overlap in the whole four-branch batch was `cli.py` and
`error_hiding_exclusions.json` shared with 00417, and both auto-merged.

Task 1.1's agent died to a Claude session limit ("resets 5:40pm UTC") with a
one-line ruff-format change unstaged and no completion report. Its branch was
otherwise complete and committed, so the change was committed as it stood
rather than re-derived — the interruption was external, not a defect in the
work.

Verified myself rather than taking the agents' word:

- `compute_tier` clamps a tampered `declared_tier=ACTION_REQUIRED` to `INFO`
  and logs at ERROR. Tier inflation is impossible by construction, which is
  the whole point of the owner's Option B.
- `TestAbsentSessionCronsNeverBlocks` holds: an absent `session_crons` is
  "no information", never "no crons exist".
- This session's own `CronList` already carries the declared `issue-sdlc`
  job, so merging the teeth cannot trap this session — checked BEFORE
  merging, not after.

## 17:53 · finding · — — the sub-agents staged no release notes

None of the four branches wrote a callout into `UNRELEASED/release-notes/`,
so a release run today would have shipped two operator-visible behaviour
changes silently. Three callouts written and the config-changes manifest
extended with both cron-enforcer keys, marked `recommended: true` — a project
that leaves them off keeps exactly the failure mode this plan exists to close.

Worth generalising: "stage the release-bound consequence" is the plan's last
step, and a sub-agent that finishes a TASK does not think of itself as
finishing a PLAN. The gap is structural, not carelessness.

## 19:20 · finding · T2.1 — Task 2.1 cannot be built, and Task 1.1 is the proof

Phase 2's first task says to point `persistent_cron_assertor` at the cron
checker as its verifier. It cannot be done, and the reason is already written
into this plan: `session_crons` is not delivered to `SessionStart`. That is the
entire justification for Task 1.1 existing at `Stop` rather than earlier.

A SessionStart verifier would therefore have no input. Worse than useless: a
verifier that can never fail can never compute `ACTION_REQUIRED`, so the wiring
would yield a permanently-`INFO` advisory that LOOKS like it participates in
the tier system while being structurally incapable of doing so. That is the
tier inflation problem in reverse and just as misleading.

Rewritten as a decision rather than an attempt. Either the cron case stays out
of the computed tier — defensible, since the tier system is for conditions
checkable at session start and this one provably is not — or the assertor gets
a different verifier, which requires finding some SessionStart-visible signal
about crons, and none is known to exist.

Noted because the failure is only visible if you hold Task 1.1's constraint and
Task 2.1's instruction in mind at once, and they sit in different phases. The
next agent would have started building before hitting it.
