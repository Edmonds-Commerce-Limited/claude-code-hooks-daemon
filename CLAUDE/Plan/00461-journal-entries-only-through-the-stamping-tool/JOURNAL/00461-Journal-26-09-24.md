# Plan 00461 — Journal 26-09-24

_Scaffolded by `mkplan.bash`; timestamps in this file are UTC._

> **Append-only activity log** for plan 00461. One file per day
> (`00461-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` UTC 24h (the date is in the filename, also UTC); times increase
>   down the file. A file with no sentinel line above predates this system —
>   its times are local, zone unrecorded (legacy; never rewritten).
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 13:30 · action · — — plan scaffolded

Plan 00461 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 13:31 · decision · —   — owner directive: hand-written entries are forbidden

The owner, on learning that this session's journal entries were appended by
hand: "journal entries by hand are NOW FORBIDDEN or we have ACHIEVED
NOTHING". It is a dogfooding failure. `mkplan.bash --journal` shipped in
v3.66.0, and the coordinator plus five sub-agents still wrote every entry
through Edit/`date -u`, with one heredoc stamped 40 minutes ahead of the
clock.

The response is a DENY, not an advisory. An advisory, in effect, already
existed (`PlanJournalling.md`'s "prefer"), and it did not change what
anyone did. Also found: `mkplan.bash --help` does not mention `--journal`,
so checking the usage could not have surfaced it.

This entry is the first in this session written through `--journal`: the
body was written to a scratch file with the Write tool, then passed as the
body-file argument. The ledger item is 00422 N21. The recovery cron reuses
`3c3c53c3`.

## 13:39 · finding · T1.1   — RED: every hand-write route is allowed today

RED for Task 1.1, against a stub `plan_journal_guard` whose `matches()` returns
False — i.e. today's behaviour: every hand-write route is ALLOWED.

`tests/unit/handlers/pre_tool_use/test_plan_journal_guard.py`: 30 failed, 29
passed. The 29 passing are the allowances (mkplan `--journal`, `git mv`,
`git add`, reads, a deletion-only conflict-marker Edit, a fenced entry-shaped
line, the gate conditions), which a do-nothing guard satisfies trivially.
The failures that matter:

```
TestEditWriteSurfaceIsDenied  edit append / Write creating a day-file /
                              Write adding an entry / archived plan's journal
TestBashSurfaceIsDenied       cat >> <<'EOF', cat <<'EOF' >>, tee -a,
                              printf >>, echo >, cp, mv, dd of=,
                              git show > (redirect in a git segment),
                              python3 -c open(...,'a'), Path.write_text,
                              /usr/bin/python3 -c, perl -pi, in-place sed,
                              bash -c "echo >>", absolute path,
                              write hidden after an unrelated git segment
TestDenyMessage               exact `--journal 461` command, two-step body
                              file pattern, UTC + 40-minute rationale
```

`tests/unit/plan_qa/test_model.py` fails at import: `journal_entry_headings`
does not exist yet. It is the shared entry-heading parser, extracted from the
two duplicate copies in `journal_entry_ordering` and
`journal_entry_future_dated`, and the guard counts entries with it.

Design: one new PreToolUse handler, `plan_journal_guard` (priority 31, PLANNING
tag so the registry injects the plan dir and `plan_qa`). The Bash surface reuses
`core.utils.get_bash_write_targets`. A `git` segment's RELOCATION targets
(`git mv`) are subtracted, but its redirects still count. The only addition is
a per-stage interpreter / in-place-editor check, because a quoted `-c` program
is one shlex token and the write-target detector cannot see inside it.

## 13:59 · action · T1.3   — --journal documented as THE way; day-file seeding bug fixed

T1.2 GREEN and committed (`a79e7a97`): `plan_journal_guard` is at 100% line
and branch coverage, and the RED list from the earlier entry all passes.

T1.3 is done in the working tree. `--journal` is now THE way in every surface
a client receives:

- `mkplan.bash --help` prints the journal usage. It says this is the only
  way, gives the two-step example, and lists the categories. Both copies were
  checked by `TestHelpDocumentsJournalMode`, which was RED first.
- `PlanJournalling.md` (both copies) has a new "Appending an entry" section.
  "prefer" is gone. The POLICY section names the one journal rule that
  denies. Two claims that the guard made false were corrected: that the
  category set is client-extensible, and that journal enforcement is only
  ever advisory.
- `PlanWorkflow.core.md` (both copies) has a "Journal entries" section, and
  it tells coordinators to put the two-step pattern into dispatch briefs.
  This is 00422 N21 remedy 3.
- `_JOURNAL_TEMPLATE_.md` (both copies): the preamble names the tool, and the
  seeded entry no longer says "log progress here".
- `HANDLER_REFERENCE.md` entry and summary row, release note 11, and
  `UNRELEASED/config-changes/v3.67.0.yaml`, which is `recommended: true`.

Found along the way and fixed under TDD. When `--journal` opened a new
day-file, it copied the template's seeded `## {{TIME}} · action — plan scaffolded` entry into it, so every day-2 file of every plan claimed the plan
was scaffolded that day. Reproduced in a scratch repo. Now that `--journal` is
mandatory, that would have hit every multi-day plan. `run_journal_mode` now
keeps only the template preamble. The RED test is
`test_a_day_file_it_creates_holds_only_the_entry_it_was_asked_for`.

Also observed: while I built that scratch fixture, the MAIN checkout's
`plan_number_helper` denied `mkdir mkj/CLAUDE/Plan/00007-probe` under
`untracked/scratch/`. It resolves a relative mkdir target against the
workspace root instead of the command's cwd. I used the sanctioned scaffolder
inside the scratch repo instead, and the coordinator will decide whether it
goes on the ledger.

## 15:23 · action · T1.4   — full QA 36/36 green; live deny from the worktree daemon

Full QA is green in the worktree: `QA: 36/36 PASSED`, with tests at 25779
passed, 0 failed, 23 skipped and coverage 95.1%. The worktree daemon was
restarted first, and there was no commit during the run.

It took three runs. Two failures, each fixed at its cause:

1. `error_hiding`: the guard returned None inside an `except` on an
   unreadable day-file. It now reads through
   `utils.path_predicates.read_text_or_reason`, which gained an `errors`
   passthrough and is read with `errors="replace"`. A stray byte no longer
   blinds the guard, and an unreadable file stands it down with the reason
   logged. Commit `6f877772`.
2. `smoke_test` found no daemon. The worktree daemon has
   `idle_timeout_seconds: 600`, and this session's hooks go to the MAIN
   daemon, so the worktree daemon idled out mid-run. I restarted it, and a
   bounded keep-alive (`timeout 3600`) sent a harmless Read event to it every
   two minutes during the run. This comes from the environment, not the code.
   Any worktree QA run longer than 10 minutes will hit it, so it is worth a
   ledger line.

LIVE, against the running worktree daemon (payloads piped into the
worktree's `.claude/hooks/pre-tool-use`; this session's own Edits go to the
main daemon, which does not have the handler yet):

```
Edit adding "## 14:30 · finding" to this journal -> deny
  BLOCKED [R-JOURNAL-HAND-WRITTEN-ENTRY]: a plan journal entry written by hand ...
  CLAUDE/Plan/mkplan.bash --journal 461 <category> untracked/scratch/journal-461-entry.md --title "short title"
cat >> <this journal> <<'EOF' ... EOF                       -> deny (same rule)
CLAUDE/Plan/mkplan.bash --journal 461 finding ... && tail    -> allow
```
