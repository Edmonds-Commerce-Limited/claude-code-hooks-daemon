# Plan 00279 — Journal 26-08-27

> **Append-only activity log** for plan 00279. One file per day
> (`00279-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 07:40 · action · — — plan scaffolded

Plan 00279 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 09:25 · decision · — — plan filed from joseph's mid-turn directives

Requirements: first-class generic agent install (not plan-workflow ad hoc);
config-gated deploy on daemon start/config change; removal advisory + CLI on
disable; `hooks-daemon agents list|status|install|remove` CLI whose commands
are surfaced to project agents; version metadata in agents; internal
agent→version→content-md5 ledger across ALL shipped versions so upgrades
overwrite pristine-outdated files but NEVER a project-customised one
(customisation strongly discouraged, loudly warned). First payloads:
migrate hooks-daemon-plan-dedupe-scout; add hooks-daemon-opus-security
(spec imported, generalised, as HANDOVER-opus-security-agent.md; untracked
original deleted). Recovery cron: reusing session cron f07b4ca0.
Implementation dispatched to a worktree-isolated subagent per joseph's
instruction; main session continues Plan 00278.

## 11:05 · action · T1.1–T1.3, T2.3 — Phase 1 core subsystem built (worktree implementer)

- `install/agent_assets.py` via TDD (33 tests in
  `tests/unit/install/test_agent_assets.py` written first, RED confirmed):
  registry (`SHIPPED_AGENTS`), version/md5 ledger, classification
  (absent | current | outdated | customised), deploy/remove engine,
  `sync_agents` + `deploy_agents_if_enabled`.
- Agents source dir is `src/claude_code_hooks_daemon/install/templates/agents/`
  — under `install/templates/` because the repo's markdown_organization config
  already allows bundled template markdown there; a fresh top-level
  `src/claude_code_hooks_daemon/agents/` dir would need a main-checkout config
  change a worktree agent must not make.
- `git mv`'d the dedupe-scout template into the agents dir and added the
  `<!-- hooks-daemon-agent-version: 1.1.0 -->` marker; harvested all five
  historic template md5s from git history into the spec so existing pristine
  installs classify OUTDATED (upgradeable), never customised.
- Authored `hooks-daemon-opus-security.md` v1.0.0 generalised from the
  handover's Appendix A (estate specifics stripped; project-provided boundary
  doc wording; SUMMARY/DETAIL contract + subagent-owned git cycle kept).
- Config: `AgentsConfig`/`AgentAssetGateConfig` (`agents.opus_security.enabled`,
  default false) added to `config/models.py`.
- Migrated `plan_workflow._deploy_dedupe_agent` onto the subsystem (deliberate
  behaviour change: customised copies now kept + loudly warned, previously
  overwritten). Updated the affected unit test, LLM-INSTALL.md ownership table
  and the client_owned_assets manifest entry.

## 11:35 · action · T2.1–T2.2, T3.1–T3.2 — lifecycle wiring, CLI, docs

- Controller `initialise` now ends with `_sync_agent_assets` (best-effort:
  OSError logged, daemon start never blocked) — 3 new tests in
  `tests/unit/daemon/test_controller_agent_sync.py`.
- CLI `hooks-daemon agents list|status|install|remove` added (`cmd_agents` in
  `daemon/cli.py`) — 9 tests in `tests/unit/daemon/test_cli_agents.py`,
  written before implementation. `install <name>` refuses a disabled gate
  with the exact key to enable; `remove` refuses customised files.
- Docs: `docs/guides/AGENT_ASSETS.md`; config-changes vUNRELEASED entry for
  `agents.opus_security.enabled` (dormant, opt-in). No HANDLER_REFERENCE
  change — no handler options exist for this subsystem.

## 12:20 · handoff · P4 — QA green in worktree; Phase 4 left for main session

Full `llm_qa.py all` in the worktree: **24/25 PASSED**. Fixes en route:
fail-open catch in `controller._sync_agent_assets`
(OSError/ValueError/TypeError, logged at ERROR) with a documented
`error_hiding_exclusions.json` entry; venv resolution in the worktree needed
`untracked/venv` + `untracked/venv-workspace-py311-81c29529` symlinks to the
main checkout's venv (local only, git-ignored). The single remaining failure
is `smoke_test` (0/3): it probes the LIVE daemon socket, and this worktree
must not start/restart the shared daemon — that is exactly Phase 4 (Task 4.1:
daemon restart RUNNING + dummy-client-repo verification), which stays with
the main session. Branch `agent-a01f76f8ba723c1b0-7534cab4`, commits
daa73a3c → 3c665566 → 4f79a33a (+ this closing journal commit). Not pushed,
not merged.

## 13:10 · action · T4.1 — merge + Phase 4 verification (main session)

Branch merged --no-ff (6679da75), pushed. Daemon restarted RUNNING; startup
sync live-upgraded the dogfood dedupe scout to v1.1.0 (the real
outdated→overwrite path). Client-mode verification in dummy-client-repo:
deploy on gate-enable, customised file refused by both remove and install,
pristine remove, disable ⇒ removal advisory naming the exact command.
Dogfood fix found + shipped (3a7e5eff): warning-family messages were emitted
twice (engine logging + CLI print) — CLI no longer re-prints them; TDD'd.
Full QA on merged main: 25/25 PASSED. Remaining: fold in the
frontmatter-sanitisation review (joseph's field feedback: the shipped
description surfaced flaggable vocabulary into the MAIN context — an Opus
worktree agent is tidying all coordinator-visible surfaces and bumping the
agent version), then T4.2 archive.
## 13:20 · action · T3.1 — context-cleanliness pass on the opus-security agent asset (v1.1.0)

Field feedback: Claude Code surfaces every agent's frontmatter `name` and
`description` into the MAIN agent's system context. The v1.0.0 description
enumerated the agent's own trigger vocabulary, so merely SHIPPING the agent
contaminated the coordinator context it exists to protect — and the coordinator
may run a model whose API-side content-safety classifier false-positives on
exactly that.

Done in worktree `agent-a1d5fc92d85106b47-90baaaf2`, commit `86e4ab31`:

- Frontmatter `description` rewritten with zero enumerated trigger terms —
  neutral operational language only (quarantine execution space,
  safeguard-sensitive, content-safety classifier, delegate before opening the
  content, returns a clean summary, defer to the project's
  delegation-boundary document). `model: opus` and the tool list unchanged.
- Body reviewed under the softer rule (it loads only in the quarantine
  subagent's own context, so boundary vocabulary is permitted where routing
  needs it): four gratuitous enumerations replaced by an
  "adversary-side mechanics vs declared effect" formulation that routes just as
  well. The two-file SUMMARY/DETAIL contract, the subagent-owned git cycle and
  the clean-summary rule are all intact and untouched.
- Every OTHER coordinator-visible surface added for this agent was audited and
  was ALREADY clean — no change needed: the CLI/warning strings in
  `install/agent_assets.py`, `docs/guides/AGENT_ASSETS.md`, the
  `agents.opus_security.enabled` entry in
  `CLAUDE/UPGRADES/UNRELEASED/config-changes/vUNRELEASED.yaml`, the
  `client_owned_assets.py` glob entry, `config/models.py` docstrings and the
  `CLAUDE/LLM-INSTALL.md` row. Only the agent file itself leaked.
- Version discipline: registry version 1.0.0 → 1.1.0, the v1.0.0 content md5
  added to a new `_OPUS_SECURITY_HISTORIC_VERSIONS` ledger so an existing
  pristine deployment classifies OUTDATED (upgradeable) instead of CUSTOMISED
  (frozen). TDD: the pinning test went in first and failed on the version
  assertion before the bump.

Worktree gotcha worth recording: the venv symlinked in from the main checkout
resolves `claude_code_hooks_daemon` to `/workspace/src`, so tests silently
exercise the MAIN checkout's source and a worktree edit looks like it did
nothing. Prefix runs with
`PYTHONPATH=<worktree>/src` — that is what makes the worktree's own template
file and registry the ones under test.
