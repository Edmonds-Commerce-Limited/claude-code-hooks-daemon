# Plan 00190 — Journal 26-07-28

> Append-only activity log for plan 00190 (day-file for 26-07-28). Append new
> entries at the BOTTOM; never edit earlier entries or earlier day-files.

## 09:00 · action · — — hostile Opus review captured

Ran an adversarial Opus review of the symlink implementation → `opus-review-1.md`
(committed `e120363c`). Verdict SHIP-WITH-FIXES: one HIGH (absolute link dangles
across host↔container path views, reproduced), plus MEDIUMs.

## 09:20 · decision · P3 — triage outcome

- **Fix now**: HIGH relative-link, MEDIUM string-config coercion, MEDIUM
  is_file() revert.
- **Ignore (accepted)**: SSoT destructive-edit hazard — the symlink direction is
  deliberate; not adding a doc warning per instruction.
- **Skip**: `_repo_toplevel`/`_get_git_toplevel` DRY extraction — both individually
  correct; extracting a core helper + its tests is disproportionate surface here.

## 09:40 · action · P3 — fixes implemented + verified

- `dest.symlink_to(os.path.relpath(src, dest.parent))` — relative target. New
  test relocates the tree (copytree symlinks=True) to a fresh prefix and asserts
  the link still resolves there (not back to the original prefix), plus a
  `readlink().is_absolute()` guard.
- `_normalised_symlink_files()` — a bare-string option becomes `[value]`; a
  non-list/non-string is warned + skipped. Test with `_symlink_files = ".env.local"`
  asserts the link is still created.
- Source check `exists()` → `is_file()` — directory source now skipped. Test with
  a `confdir` entry asserts nothing is linked.

20/20 worktree_create tests pass; targeted regression 79 pass. Black / Ruff /
MyPy(src) / Bandit / magic-values / error-hiding all green.
