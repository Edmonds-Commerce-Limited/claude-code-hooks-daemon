# Plan 00399 — Journal 26-09-24

_Scaffolded by `mkplan.bash`; timestamps in this file are UTC._

> **Append-only activity log** for plan 00399. One file per day
> (`00399-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM, ONLY with
> `mkplan.bash --journal <plan-number> <category> <body-file>` (it stamps the
> real UTC time; a hand-written entry is denied); NEVER edit earlier entries
> (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` UTC 24h (the date is in the filename, also UTC); times increase
>   down the file. A file with no sentinel line above predates this system —
>   its times are local, zone unrecorded (legacy; never rewritten).
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 18:26 · decision · —

T1.1 decided as option 2 (unattended ruling relayed by the coordinator), then built.

Verified before building: the `PreCompact` record exists in live traffic (the
`compaction-signal` handler fires on every PreCompact in
`untracked/logs/hooks/verdicts.jsonl`, and a `.compacting` marker was on disk)
and the supervisor reads it (`load_compaction_signal` -> `reading.compacting`;
105 `compaction detected` lines in `untracked/supervise/decision.log`). The
live transcript shows every compaction as `trigger: manual`, 20 of 21 `/compact`
commands carrying the `🤖 [ccy-supervisor` prefix and 1 human
(`prep for release`), so attribution by prefix fits the field.

No submit-time hook exists for a built-in `/compact`: the supervisor's
injection at 15:52:20.90 was followed by PreCompact at 15:52:20.97 with no
UserPromptSubmit between them. So the queued window stays a stated limit.

RED before the fix: 15 tests failed (no `load_compaction_origin`, no `trigger`
/`origin` in the record, reason carried no origin). The keystroke-exactness
and legacy-reason tests passed before and after, as characterisation.
