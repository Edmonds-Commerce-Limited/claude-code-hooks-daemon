# Plan 00393 — Journal 26-09-13

> **Append-only activity log** for plan 00393. One file per day
> (`00393-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 13:30 · action · — — plan scaffolded

Plan 00393 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 13:31 · action · — — session crons, recorded not re-created

Exactly one failsafe recovery cron is live, so it is reused rather than
duplicated. All three IDs predate this plan and predate the terminal crash:

```text
57ccaf1d  17 * * * *  failsafe recovery   (reused for this plan)
482c9b7f  23 * * * *  issue-sdlc          (declared in persistent_crons)
f7c00aa1  41 * * * *  background watchdog
```

## 13:31 · finding · N1 — the ledger opened because the answer was "not tested"

Asked whether the persistent-cron system had worked across a terminal crash.
It had not been exercised at all: the session was RESUMED, not restarted, so the
crons came back with identical IDs and no `CronCreate` call. Full evidence is in
Plan 00392's journal, which is the canonical copy and is not repeated here.

Chasing why the no-persistence limit had not bitten is what exposed N1 — that
two of the three live crons have no declaration to fall back on when it does.
The entry is in `PLAN.md`; this is the provenance, not a second copy.
