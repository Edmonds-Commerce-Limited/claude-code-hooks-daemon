# Plan 00282 — Journal 26-08-27

> **Append-only activity log** for plan 00282. One file per day
> (`00282-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 15:23 · action · — — plan scaffolded

Plan 00282 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 15:25 · finding · P1 — dedupe scout: 00070 is a subset, not a superset

The plan-dedupe scout confirmed Plan 00070 fixed the SAME class of crash in
`registry.py` (the `is not None` guard, PyYAML `priority: null` edge) but left
BOTH generators using the vulnerable `.get(PRIORITY, instance.priority)`. So
00282 closes the gap 00070 left open — file it, don't merge.

## 15:26 · finding · P1 — a one-element sort does not reproduce it

First RED attempt used a SINGLE null-priority handler and did NOT crash: Python
never calls the comparison for a one-element list, so `handlers.sort(...)` was a
no-op. The real crash needs TWO handlers in the same section so the sort
actually compares `None` with an `int`. Both RED tests now register two
handlers; both fail with the reported `TypeError` at the exact sort sites
(`docs_generator.py:192`, `playbook_generator.py:314`).

## 15:26 · action · P2 — fix landed (GREEN)

- `resolve_priority(handler_config, fallback)` in `constants/config.py`:
  `isinstance(value, int)` → the value, else the fallback. This handles absent
  AND `None` identically AND respects an explicit `0` (a valid priority). One
  helper now backs all three sites: `docs_generator.py`, `playbook_generator.py`,
  and `registry.py` (refactored from its inline guard — behaviour-preserving).
- Fixed the misleading `constants/config.py` module docstring (it recommended
  the membership form, wrong against a `model_dump()` result).
- Secondary: both generator CLI wrappers now `logger.exception(...)` the full
  traceback (the bare `{e}` named nothing).
- Left `config/validator.py` alone deliberately: it validates RAW parsed YAML
  (omitted key genuinely absent), so its membership form is correct there —
  recorded as a Non-Goal.

Verified: docs+config+generator suites 130 passed; registry+constants 264
passed. Daemon restarted RUNNING (registry.py is core runtime). `generate-docs`
and `generate-playbook` both exit 0 on this repo with NO doc drift
(`.claude/HOOKS-DAEMON.md` / `CLAUDE.md` unchanged). Full QA running.

## 15:44 · action · T3.1 — full QA green (clean run), committing the fix

Full QA re-ran clean (no concurrent foreground pytest to clobber the shared
`.coverage`, which is what caused the earlier false 92%): **25/25 PASSED**,
14900 passed / 0 failed / 5 skipped, coverage 95.1%. The run survived a
supervisor compaction mid-flight; its buffered stdout only landed when the
process exited. Diff re-reviewed post-compaction and intact: `resolve_priority`
helper, three sites refactored, docstring corrected, both CLI wrappers now
`logger.exception(...)`. Committing the fix (src + tests + this journal), then
completing the plan atomically in a follow-up commit that records the delivery
hash and archives the folder.
