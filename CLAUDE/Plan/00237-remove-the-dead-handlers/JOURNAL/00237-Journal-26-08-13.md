# Plan 00237 — Journal 26-08-13

> **Append-only activity log** for plan 00237. One file per day
> (`00237-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 18:41 · action · — — plan scaffolded

Plan 00237 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 19:30 · action · — — plan filled in

The dedupe scout confirmed nothing live already covers handler removal, the
retired-handler registry, or Stop chain priority ordering.

Phases follow RISK, not the audit's cohort order: five handlers that cannot
fire or have no consumer first (deleting them changes no observable
behaviour), then five that DO fire, then the shadowed Stop advisories, then
the two MERGE verdicts — last because one carries a live side effect that must
be relocated before its handler goes.

## 19:35 · finding · P1 — every Phase 1 claim re-verified before deleting

Checked each structural claim against the source rather than trusting the
audit table:

- `usage_tracking.matches()` — literally `return False` under a
  `# DISABLED: Handler needs architectural rework` comment. Confirmed.
- `cleanup` — repo-wide search for a writer of `temp/hooks/` returns only the
  reaper itself. Confirmed.
- `yolo_container_detection` — `show_on_session_start` defaults False and the
  dogfood config does not set it. Confirmed, with a caveat below.
- `subagent_completion_logger` / `notification_logger` — the only references
  to their JSONL files are the writers plus doc mentions. No code reader.

## 19:40 · decision · T1.3 — "cannot fire" is too strong for yolo

The audit called it structurally unable to fire. That is accurate for a
default install, but a client who sets `show_on_session_start: true` DOES get
the banner. The honest framing is "off by default, and nothing turns it on".

Removing it anyway, because Plan 00128 already made this judgement once: it
turned the banner off on the grounds that the container is surfaced better by
the status-line icon and by `hooks-daemon check`. This completes that decision
rather than making a new one. The `RETIRED_HANDLERS` entry names the loss for
anyone who had flipped the flag.

## 19:55 · finding · P1 — the guards caught the removals, as designed

After deleting the first three, four tests failed — every one a genuine
consequence, not a flake:

- `test_no_unknown_handlers_in_config` — `init_config` still generated a
  `session_end: {cleanup: ...}` block.
- `test_config_has_all_event_types` — the dogfood config no longer had a
  `session_end` section at all.
- `test_handler_reference_check` — `HANDLER_REFERENCE.md` still documented
  `cleanup` in a section I had missed.
- `test_config_key_consistency` — referenced `HandlerID.SESSION_CLEANUP`.

Worth recording as the system working rather than as noise. A removal touches
roughly eight places (handler, tests, `__init__`, two constants modules, the
config template, the dogfood config, the reference doc, the quick-reference
table) and the guards found the three I missed.

## 20:00 · decision · P1 — an event with no handlers keeps its config section

`test_config_has_all_event_types` wanted a `session_end` section. Two ways to
satisfy it: drop the event from the expected set, or keep an empty section.

Kept the empty section (`session_end: {}`, matching what `init_config` now
generates). SessionEnd is still a registered, dispatchable event — the hook
fires and reaches the daemon — it simply has no handlers today. Dropping it
from the expected set would have weakened an invariant to make a symptom go
away, and would leave a project-level SessionEnd handler with no documented
place to live.

## 20:05 · action · P1 — checkpoint

Daemon RUNNING after restart; 699 tests pass across the affected slice.
Committing three of the five Phase 1 removals before starting the two
loggers, whose footprint spans another ~20 files.

## 20:35 · action · T1.4/T1.5 — the two loggers removed

Footprint was the ~20 files predicted. Beyond the handler + test pairs:
`constants/handlers.py` (two `HandlerIDMeta` blocks + `_KNOWN_HANDLER_KEYS`),
`constants/priority.py`, `install/handler_profiles.py` (both were
strict-profile-only), `daemon/init_config.py`, the dogfood config, two docs,
`error_hiding_exclusions.json`, and eight test files.

Two things that were NOT mechanical:

- `daemon/controller.py` cited `notification_logger` as the precedent for the
  verdict log's fail-open contract, and `error_hiding_exclusions.json` cited it
  twice more. A removed handler that other code points at as an EXAMPLE leaves
  dangling references that no import graph catches.
- `make_notification_input` in the integration conftest lost its last caller.
  Removed it too — a fixture builder with no consumer is the same shape as the
  handlers this plan is deleting.

## 20:45 · finding · T1.7 — the DBF guard found two real bugs on its first run

Wrote the guard the plan's premise implies: a manifest documenting a handler as
`removed` must also carry a `RETIRED_HANDLERS` entry. The manifest is the
release-process record of a removal, so anchoring to it covers every future
removal without anyone remembering to.

It failed immediately on six handlers retired BEFORE the registry existed:

```
assert not {'eslint_disable', 'go_qa_suppression_blocker',
            'php_qa_suppression_blocker', 'python_qa_suppression_blocker',
            'remind_validator', 'validate_sitemap'}
```

Verified rather than assumed — all six were still REJECTED by
`ConfigValidator.validate_and_raise` today, so an unedited v2.x config has been
tipping client daemons into DEGRADED MODE for every release since v2.9.0, over
removals we performed deliberately. Registry entries added; a config naming
every retired handler now validates cleanly.

Second bug, found while writing the manifest entry: `UNRELEASED/config-changes/`
held `transcript-archiver-removal.yaml`, but `RELEASING.md` Step 6 globs
`v*.yaml`. That file would never have been moved at release — Plan 00233's whole
client-facing removal note was set to sit in staging forever, with no error at
any point to say so. Merged into `v3.53.0.yaml`, mis-named file deleted, and a
second guard now asserts staged `*.yaml` == staged `v*.yaml`.

Worth stating plainly: neither bug was on this plan's task list, and neither
would have been found by reading. The guard found both within a minute of
existing. That is the whole argument for DBF over a manual sweep.

## 20:50 · thought · T1.7 — vacuity, twice

Both guards can pass by looking at nothing, so both say so explicitly:

- the registry guard has a companion test feeding it a synthetic manifest with
  an undocumented removal, asserting the detector FIRES;
- the stranding guard skips (rather than passes) when no manifest is staged.

An assertion that X does not appear is worthless without a paired proof that X
CAN appear. That is the same rule Plan 00236 applied to the shadowing guard.

## 21:05 · finding · P2 — two of the "five that DO fire" do not fire

Probed the LIVE daemon rather than reading the config:

```
printf '{"hook_event_name":"Stop","stop_hook_active":false,
         "transcript_path":".../stop-probe.jsonl","session_id":"probe-stop-1",
         "cwd":"/workspace"}' | ./.claude/hooks/stop
{}
```

An empty response. Five handlers are registered under `stop:` in the dogfood
config; on an ordinary stop, exactly one of them runs. `auto_continue_stop`
(priority 10, terminal) matched, allowed the stop, and broke the chain — so
`task_completion_checker` (20), `hedging_language_detector` (30),
`dismissive_language_detector` (58) and `remind_prompt_library` (100) all
produced nothing.

This moves `task_completion_checker` out of Phase 2's premise. The audit
described it as redundant with what `auto_continue_stop` enforces; the sharper
fact is that it is not reached at all in the ordinary case. Same for
`remind_prompt_library`'s Stop leg — though that one also runs on SubagentStop,
where nothing terminal precedes it, so it is only half-shadowed.

Stated precisely, because the imprecise version is wrong: they are unreachable
on the ORDINARY stop, not absolutely. `auto_continue_stop` declines to match in
two narrow cases (confirmed re-entry, an AskUserQuestion turn), and the chain
runs on through in those.

## 21:10 · action · P1 — the QA gate found four leftovers, all mine

First full run after the logger removal: 17/21. Every failure was a leftover
from a removal, and every one was caught by a guard rather than by review:

- `error_hiding` (2) — stale exclusion entries for `cleanup_handler.py` and
  `stats_cache_reader.py`, deleted with their handlers in the previous commit.
  The stale-exclusion rule is itself a DBF guard: an exclusion that matches no
  finding is one that could silently cover a future re-introduction.
- `doc_truth` (1) — README still listed `yolo_container_detection` under
  Session Management.
- `tests` (2) — the two tests that enforce the above.
- `format` (1) — black auto-fixed the new guard file.

Also repointed two comments that named a deleted handler as their fail-open
PRECEDENT (`daemon/controller.py`, `error_hiding_exclusions.json` x2), and
corrected `docs/guides/VERDICT_LOG.md`, which located `verdicts.jsonl` by
saying "same directory as notifications.jsonl and subagent_completions.jsonl".
Rewriting that to name `stop-events.jsonl` instead would have been a NEW false
statement — that file lives directly under the untracked dir, not in
`logs/hooks/`. Checked before writing; the doc now states the path outright.

Deleted the two orphaned logs themselves: 4.1 MB that nothing read. The
manifest tells clients to do exactly this, so the dogfood repo does it too.

## 21:20 · finding · T2.5 — bash_error_detector, measured

Queried the verdict log (`hooks-daemon verdicts`) rather than arguing from
impression. Retained window, 44,165 records, of which 43,557 are status renders
predating the Plan 00236 exclusion. Of the 606 BEHAVIOURAL records:

```
  bash-error-detector            274   45.2% of behavioural
  lint-on-edit                   103   17.0%   (allow=90, deny=13)
  markdown-table-formatter        55    9.1%
  plan-qa-edit                    25    4.1%
  auto-continue-stop              24    4.0%
```

274 fires, `allow=274`. Not one of them restricted anything. It is the single
most active behavioural handler in the daemon by a factor of 2.7 over the next
— and the next one, `lint-on-edit`, actually denies 13 times, which is work.

The mechanism, read from source: `handle()` lowercases stdout+stderr and does a
substring test for `error`, `failed`, `failure`, `fatal`, `warning`, `warn`,
`deprecated`, plus "stderr was non-empty". Substring, not word — so
`error_hiding_exclusions.json` in an `ls` listing trips it. It then echoes the
FULL command back into context and asks the agent to "review the output
carefully".

The corroboration is first-hand and unusually direct: it fired on the `grep`
that listed the removals, on `git status --porcelain`, on the `jq` that read
the QA report, and — best of all — on the very command that produced the table
above, because the string `bash-error-detector` contains "error". The output it
asks me to re-read is output I requested one line earlier and can see.

Confirms REMOVE for Task 2.5 rather than narrow+rate-limit. A rate limit would
reduce the volume of an advisory whose content is "look at the thing you are
looking at"; the defect is not the frequency.

Two figures with a caveat attached, since the report says so itself: this is a
bounded rolling window, not a lifetime total, and "never fired" in it is not
evidence a handler is pointless. Neither claim above rests on an absence — both
rest on a handler firing 274 times and allowing every one.

## 21:30 · finding · T2.1/T2.2 — two handlers advising things that are false

Read both rather than trusting the audit rows, and each turned out to have a
defect sharper than "redundant".

`task_tdd_advisor` — its 30-line payload ends with

```
4️⃣  VERIFY:
   • Run full QA suite: ./scripts/qa/run_all.sh
```

and this project's own `enforce_llm_qa` project handler BLOCKS a direct
`run_all.sh` invocation, directing agents to `llm_qa.py` instead. So the
advisory instructs the agent to run a command the daemon then denies. Its two
`📖 REFERENCE` targets — `@CLAUDE/CodeLifecycle/Features.md` and
`@CLAUDE/PlanWorkflow.md` — are both EAGERLY `@`-imported from `CLAUDE.md`
(lines 205 and 509), so their full text is already resident in every session
before the handler says anything.

`remind_prompt_library` — `matches()` is `return True`, no gating of any kind.
After every sub-agent completion it says:

```
  npm run llm:prompts -- add --from-json <prompt-file>
📖 See: CLAUDE/PromptLibrary/README.md
```

There is no `package.json` in this repository at all, and no
`CLAUDE/PromptLibrary/` directory. Both were checked. It fired 15 times in the
retained verdict window, every one pointing at a script that cannot be run and
a file that cannot be read.

The common shape: an advisory nobody re-reads decays into confidently wrong
instructions, and nothing in the system notices, because an advisory that is
merely WRONG still returns allow and still looks healthy in every log. That is
worth stating as the reason this cohort is being removed rather than repaired —
repairing them means committing to re-verify their prose against the repo
forever.

## 21:40 · finding · T2.4 — post_clear_auto_execute breaks when sessions share a daemon

The audit's case was "its originating plan is Cancelled as unachievable and
rates the surviving code marginal". Reading the code, there is a firmer reason.

Its whole contract is "fires once per session". It implements that with ONE
slot of instance state:

```python
self._last_session_id: str | None = None
...
if session_id != self._last_session_id:
    return True
```

The daemon is explicitly designed for multiple Claude Code processes with the
same (hostname, project root) to SHARE one daemon instance — CLAUDE.md's
"Parallel sessions share one daemon (Plan 00127)". Two concurrent sessions
alternating prompts flip `_last_session_id` on every dispatch, so `matches()`
returns True for every prompt of both, and each is told "the user has just
started a fresh session (likely after /clear)" when they did not.

So the failure mode is not "marginal value" — it is guidance that becomes
FALSE exactly in the configuration this project encourages (the
`agent_isolation_advisor` and the `multithread_indicator` status segment both
exist because several threads at once is normal here). A one-slot cache cannot
express per-session state in a multi-session process.

Fixable in principle — a bounded per-session map, as `command_hints` already
does for its TTL — but that is repair, and repair was Plan 00236's remit. The
verdict stands at REMOVE: it fired once in the retained window, and the thing
it exists to prevent (an agent idling after /clear instead of executing) is
already covered by the resident instruction set.

## 21:55 · action · P2 — five removed; the guards found nine leftovers

Removed `bash_error_detector`, `task_tdd_advisor`, `task_completion_checker`,
`remind_prompt_library`, `post_clear_auto_execute` with their tests, constants,
priorities, profile entries, config-template lines, dogfood config blocks and
reference-doc sections, plus a `RETIRED_HANDLERS` entry and a `config-changes`
row each.

The full suite then failed 9 + 10 errors — every one a genuine leftover:

- four integration test classes importing deleted modules
  (`test_post_tool_use.py`, `test_stop_handlers.py`,
  `test_all_handlers_response_validation.py`);
- five classification entries in `test_claude_md_guidance_coverage.py` and one
  in `test_blocking_handler_evasion.py`, both of which have a "does this name a
  handler that still exists?" guard — which is exactly what caught them;
- `test_handler_profiles.py::test_strict_enables_all`, whose FIXTURE used
  `task_completion_checker` as its sample Stop handler. Swapped it for
  `validate_eslint_on_write` rather than deleting the assertion: the test's
  claim is "strict enables every handler in the file", and a fixture naming a
  handler that no profile lists makes that claim unprovable rather than false.

Three orphaned conftest builders went too — `make_post_tool_bash_input`,
`make_subagent_stop_input` (and earlier `make_notification_input`) lost their
last callers. Same shape as the handlers themselves.

## 22:00 · thought · P2 — what the empty event sections are telling us

Three events now ship zero handlers: SessionEnd, Notification, SubagentStop.
That is not an accident of this plan's scope — all four handlers those events
carried were logging or reminding, and none of them read anything back.

Kept all three as declared, empty sections rather than deleting them. The event
is still registered, the hook still fires, the daemon still dispatches (probed:
each returns `{}`), and a project handler can still be added under it. Deleting
the section would make an empty event indistinguishable from an unsupported
one.

## 22:10 · finding · P3/P4 — two things the plan's task list had wrong

**Phase 3.** The plan said "remove the Stop registration (the nitpick
pseudo-event already delivers both) or move them below priority 10". Checked
which leg owns what before touching either:

```
stop/hedging_language_detector.py:214:    def get_claude_md(...)   -> full section
stop/dismissive_language_detector.py:301: def get_claude_md(...)   -> full section
nitpick/hedging_language.py:114:          def get_claude_md(...)   -> None
nitpick/dismissive_language.py:117:       def get_claude_md(...)   -> None
```

So it is exactly inverted from what you would guess: the leg that never RUNS
owns the resident CLAUDE.md guidance, and the leg that does run returns None.
The coverage test even records the nitpick pair as exempt *because* "the
resident section already carries the guidance" — that section belongs to the
shadowed twin.

A straight deletion would therefore have silently dropped both resident
sections, which are the half that survives compaction and matter far more than
the fire-time advisory. Task 3.1 is now "MOVE get_claude_md first", with the
deletion second.

Re-prioritising below 10 is rejected outright: that band is the safety range,
and putting an advisory ahead of a terminal safety handler to win a race is the
wrong fix for the wrong problem.

**Phase 4.** Task 4.1 says the counter-advance side effect must be relocated
BEFORE `validate_plan_number` goes "or plan numbering breaks". Searched for
other callers first:

```
handlers/pre_tool_use/markdown_organization.py:575:  record_plan_allocation(plan_folder, int(next_number))
```

`markdown_organization` already advances the counter on the path where it
creates a plan folder itself, and `mkplan.bash` advances it too. So
`validate_plan_number` is one of at least three writers, not the sole one, and
the relocation is narrower than the plan feared — it needs to cover the paths
the other two do NOT (a plan folder created by a bare `mkdir` or a direct
Write, which is exactly what validate_plan_number matches on). Still a real
task; not the single-point-of-failure the task line implies. Will confirm the
trigger overlap properly when Phase 4 starts rather than acting on this now.

## 22:20 · finding · P3 — the live leg imports from the dead one

Third inversion in the same pair, and this one is structural rather than
editorial:

```
nitpick/hedging_language.py:17:    from claude_code_hooks_daemon.handlers.stop.hedging_language_detector import (
nitpick/dismissive_language.py:17: from claude_code_hooks_daemon.handlers.stop.dismissive_language_detector import (
```

with the comment "Reuses compiled patterns from the Stop-event handler to
maintain a single source of truth". The nitpick handlers take their entire
pattern set — MEMORY_PATTERNS, UNCERTAINTY_PATTERNS, WEAK_CONFIDENCE_PATTERNS —
from the Stop classes.

So the leg that runs depends on the leg that cannot. Deleting the Stop modules
outright would break the working detectors at import time, which the daemon
restart would catch loudly — but it means Phase 3 is not a deletion, it is a
DEPENDENCY INVERSION: the patterns and the `get_claude_md()` bodies move into
the nitpick modules, and the stop modules go afterwards.

Worth naming the general shape, because this is now three for three on this
pair: the dead code held the single source of truth, the resident guidance, and
the pattern definitions, while the live code held only the loop that used them.
"Which one runs" and "which one is authoritative" had drifted completely apart,
and nothing in the type system or the tests could see it — both modules import
cleanly and both are unit-tested in isolation. It took a live chain trace
(Plan 00236) to notice at all.

## 22:35 · finding · P3 — the nitpick leg imports four of five pattern sets

Counted the pattern sets on each side before planning the move:

```
stop/dismissive_language_detector.py:  NOT_OUR_PROBLEM, OUT_OF_SCOPE,
                                       SOMEONE_ELSES_JOB, DEFER_IGNORE,
                                       PREMATURE_STOP
nitpick/dismissive_language.py:        imports the first FOUR only
```

`PREMATURE_STOP_PATTERNS` has no importer outside the Stop module. So the
running leg has never detected premature-halt language at all — "natural
checkpoint", "pausing here", "ready to continue on your cue" — and deleting the
Stop module without carrying that set across would remove the capability
silently, with every test still green, because the nitpick tests only ever
exercise the four sets they import.

Which is pointed, given this session: the premature-halt category is exactly
what the user pulled me up on earlier ("stopping because you are not
finihsed?"). The detector for it exists, is enabled in config, and does not
run.

Phase 3 must therefore carry FIVE sets across and wire PREMATURE_STOP into the
nitpick `_CATEGORY_PATTERNS` list — which makes that category live for the
first time, rather than merely preserving it. Noting explicitly that this is a
behaviour CHANGE, not a pure refactor, so it needs its own test rather than
riding on the existing ones.

## 21:05 · progress · P3 complete — inversion landed, five sets live

Phase 3 done end to end. The move itself was mechanical; three things in it
were not.

**The `get_claude_md()` bodies needed adapting, not copying.** Both sections
opened by describing the Stop event ("At Stop time your last message is
scanned") which is now false — the nitpick pseudo-event fires per turn.
Rewritten to "Your messages are scanned". Both also gained the Plan 00225
quoting rule ("a QUOTED phrase is a mention, not a hedge"), which the Stop
copies never carried even though the behaviour has been in the nitpick handlers
since 00225. So the resident guidance had been describing the wrong trigger AND
omitting a real behaviour, for as long as the pair has existed.

**`PREMATURE_STOP_PATTERNS` is now wired.** RED test written first and
confirmed failing, four cases: the halt fires, `awaiting your instruction`
fires, a QUOTED occurrence does NOT fire (the 00225 rule must hold for the new
category too, or carrying it across would reintroduce the unsatisfiable-advisory
bug), and ordinary completion prose does not fire. GREEN after wiring the fifth
tuple into `_CATEGORY_PATTERNS`.

**The shadowing guard was rewritten around a synthetic probe** (Task 3.3). The
old version proved the hazard using the two handlers this plan deletes — so
finishing Phase 3 would have deleted the evidence that the hazard exists, at
exactly the moment the tree looked clean. The probe is defined in the test
file, matches unconditionally, and emits a sentinel. It gained a THIRD test
the old shape could not express: the same probe at priority 5 (below
`auto_continue_stop`) DOES reach the response. Without it, the other two tests
are equally consistent with "Stop dispatch is simply broken", which would send
the next reader after the wrong bug.

## 21:10 · finding · P3 — the guidance-coverage table was right and useless

Flipping the exemptions in `test_claude_md_guidance_coverage.py` exposed
something worth recording, because no guard could have caught it.

The table exempted the nitpick pair with the reason "batch-audit twin of
stop/<x>, whose resident section already carries the guidance". Every word of
that was TRUE. It was also useless: the twin holding the section was the one
behind the terminal handler, so the covered handler was dead and the live one
was exempt. Both handlers agreed with their table rows; every assertion in the
file passed.

This is the same failure mode the file's own `TestAnExemptHandlerCannotQuietlyDeny`
docstring already records for `ValidateEslintOnWriteHandler` — an exemption
that names a covering section which does not actually cover the case. There the
gap was the section's CONTENT (nine languages, TypeScript not among them);
here it is the section's REACHABILITY. Added a paragraph to the module
docstring saying so, and naming `test_stop_chain_terminal_shadowing.py` as the
guard that can see it. A "which section covers this?" check cannot detect a
section attached to a handler that never runs; only a chain-level check can.

## 21:15 · progress · P3 — reference sweep, one wrong claim caught

Swept every non-historical reference: `constants/priority.py` (two entries),
`daemon/init_config.py` (the shipped config template), `.claude/hooks-daemon.yaml`,
`docs/guides/HANDLER_REFERENCE.md` (section plus two summary-table rows),
`.claude/skills/optimise/invoke.sh` (its "Stop Quality" area scored 4 handlers,
THREE of which this plan has now removed — repointed to the nitpick keys and
the pseudo-event enable flag), and two test docstrings. Left CHANGELOG.md,
RELEASES/ and completed plans alone: those are history and are still accurate
about what was true then.

Caught myself writing a false statement in the `stop_hook_helpers.py` docstring
— I claimed nitpick setup reads transcripts through the same helpers, as
justification for keeping the module. Checked before committing to it: the only
importers are `auto_continue_stop`, the package `__init__`, and its own tests.
Rewrote the justification to the true one (public `utils` export available to a
project-level Stop handler). The module stays either way, but the reason has to
be a real one.

`tests/unit/core/test_hook_result.py` needed the same care for a different
reason: its regression docstring named four handlers as the users of
`hookSpecificOutput.additionalContext`, and Plan 00237 has now removed all
four. The test must NOT be deleted with them — it pins Claude Code's response
contract, not our use of it, and the bug it guards is silent by construction
(a dropped context still returns allow and still looks healthy in every log,
which is how it survived to begin with). Docstring rewritten to say that
explicitly, so the next reader does not remove it as dead.

## 21:20 · verification · P3 — daemon RUNNING, live Stop probe correct

`./bin/hooks-daemon restart` → RUNNING (PID 1100370). Probed the live socket:

```
echo '{"hook_event_name":"Stop","stop_hook_active":false}' | ./.claude/hooks/stop
→ {"decision": "block", "reason": "You stopped without explaining why. ..."}
   exit=2
```

`auto_continue_stop` alone, behaving correctly, with the hard-block exit-2
contract intact. Daemon log clean — the only lines matching /error/ are the
handler NAME `ErrorHidingBlockerHandler` and my own grep commands echoed back
by the payload logger.

The DBF manifest/registry guard passed with both new `removed:` rows in place
(35 tests). That guard is now the thing that would have caught this whole
class: a handler removed from `src/` without a config-changes row, or a row
without a `RETIRED_HANDLERS` entry, fails the suite rather than shipping a
client config into degraded mode.

## 21:12 · incident · P3 — I deleted 37 lines of resident guidance, and the daemon committed it

Caught this by checking a thing I did not need to check, which is the only
reason it was caught at all.

After regenerating docs I grepped `CLAUDE.md` for the two language-detector
sections, expecting to see them re-emitted under the nitpick handlers' names.
They were absent — and `git status` showed CLAUDE.md CLEAN. The daemon's
injector auto-commits on restart, so the deletion had already landed:

```
f66fe876 Auto: hooks daemon regenerated CLAUDE.md handler guidance
 CLAUDE.md | 37 -------------------------------------
 1 file changed, 37 deletions(-)
```

**Root cause.** `DaemonController.initialise()` built the injector's handler
list as `[h for chain in self._router._chains.values() for h in chain._handlers]`.
Pseudo-event handlers do NOT live in the router's chains — they are registered
into a separate `HandlerChain` owned by `PseudoEventDispatcher` and dispatched
through it. So the injector has never been able to see a nitpick handler. The
moment I moved the guidance from the Stop twins (in the router) onto the
nitpick twins (not in the router), it stopped reaching CLAUDE.md.

**Why every guard was green.** The guidance-coverage suite asserts that a
handler recorded in `_EARNS_GUIDANCE` RETURNS content. Both handlers did. 21/21
QA passed. `check_repo_hygiene.py` has an `orphaned-handler-guidance` rule, but
it covers the opposite direction — a section with no handler behind it. Nothing
anywhere asserted the direction that actually broke: a handler with guidance
and no section.

**Fixes, in DBF order — the guard first.**

1. `PseudoEventDispatcher.all_handlers()`, and the controller now extends the
   injector's list with it. The accessor's docstring says why it exists, so the
   next person to add a non-router dispatch surface has a fighting chance.
2. `TestGuidanceActuallyReachesClaudeMd` in the coverage suite: every
   `_EARNS_GUIDANCE` handler's display name must appear as a provenance marker
   in this repo's own CLAUDE.md. Measured before writing it — exactly 2 of 54
   markers missing, both the nitpick pair, zero other false positives, so it
   could be written as a strict assertion rather than an allowlist. It reads
   the markers through the injector's own `handler_names_in_guidance()`, never
   a local regex copy.
3. A vacuity companion (`test_the_markers_are_readable_at_all`), because an
   empty marker set would make the coverage assertion pass while checking
   nothing — the same shape of hole this incident just came through.
4. Unit tests for `all_handlers()` directly, including the two-pseudo-event
   case (a one-registration implementation would pass a single-chain test).

After the fix, restart re-emitted both sections: `+42 insertions`, five more
lines than were lost, because the sections now also carry the Plan 00225
quoting rule and the premature-halt bullet that the Stop copies never had.

**The lesson worth keeping**: "the handler returns guidance" and "the guidance
reaches the agent" are different properties, and only the first was guarded.
This is the same class as the whole plan — a thing that looks live, is
enabled, is tested, and produces nothing. I spent the session removing
handlers with that shape and then created one, in the fix for it.

## 21:30 · finding · P3 — how many consumers share the injector's blind spot?

Having claimed in `all_handlers()`'s docstring that three consumers walk the
EventRouter's chains and therefore cannot see pseudo-event handlers, I went and
counted instead of leaving it as a plausible sentence. Exactly two exist:

```
daemon/controller.py:265  all_handlers = [... for chain in self._router._chains ...]   <- the injector (FIXED)
daemon/controller.py:872  for event_type, handlers in self._router.get_all_handlers()  <- get_handlers()
```

Acceptance-test collection is NOT one of them: `playbook_generator.py` builds
from `HandlerRegistry` (which discovers handler CLASSES by walking the package)
rather than from the router. So my docstring overstated the blast radius by
one. Correcting it to name what I actually verified — an overstated docstring
is the same defect as a hedge, just phrased confidently.

The second consumer, `get_handlers()`, feeds `generate-docs`. Checked the
generated file: `.claude/HOOKS-DAEMON.md` has sections for PreToolUse,
PostToolUse, SessionStart, PreCompact, UserPromptSubmit, PermissionRequest,
Stop, Status, WorktreeCreate, WorktreeRemove, Plugin and Project — and no
Nitpick section at all. So the two nitpick handlers are active, enabled, and
absent from the document `CLAUDE.md` describes as "the current active handler
summary, generated from live config".

That is the same writer/reader gap, pre-existing rather than caused by this
plan, and it is NOT covered by the guard I just added (which checks CLAUDE.md
guidance markers, not the generated handler table). Filing it as its own task
rather than either widening this phase silently or dropping it.

## 21:38 · correction · P3 — I got the second consumer's mechanism wrong

Correcting the 21:30 entry rather than editing it (journals are append-only).

I wrote that `get_handlers()` "feeds generate-docs". It does not. Checked both
paths properly:

```
get_handlers()                 -> server.py:1138, the daemon's live "handlers"
                                  IPC action. Walks router chains.
cmd_generate_docs -> DocsGenerator -> iterates EVENT_TYPE_MAPPING against
                                  config.handlers.model_dump(). Never touches
                                  the router at all.
```

So there are still exactly two surfaces that omit the nitpick handlers, but for
two DIFFERENT reasons: one walks the router (which they do not join), and one
reads the `handlers:` config section (which they are not in — they live under
`pseudo_events:`). "Same blind spot" was a tidy story and the wrong one.

The conclusion is unchanged and the task stands, but a fix aimed at the
mechanism I first named would have fixed one surface and left the other, while
looking complete. This is the second time in an hour that checking a plausible
sentence changed it — first the `stop_hook_helpers` docstring, now this. Both
were claims I could have shipped without anyone noticing.

Also settled the third claim from 21:30: acceptance-test collection is
genuinely unaffected, because `playbook_generator.py` builds from
`HandlerRegistry` — it walks handler CLASSES from the package rather than any
runtime registration, so it never depended on either surface.

## 21:46 · correction · P3 — and the third claim was wrong too, caught by running it

Second correction to the 21:30 entry. I claimed acceptance-test collection was
unaffected because `playbook_generator.py` builds from `HandlerRegistry` rather
than the router. I established that by reading its imports. Then I ran it:

```
./bin/hooks-daemon generate-playbook | grep -ci nitpick
→ 0
```

Zero. Both nitpick handlers define `get_acceptance_tests()` with real cases,
and neither reaches the playbook. The reason is visible at
`playbook_generator.py:164-173`: it iterates `EVENT_TYPE_MAPPING`, then keeps
only handlers whose MODULE PATH contains that event dir name. `nitpick` is not
in `EVENT_TYPE_MAPPING`, so `handlers/nitpick/*` matches no iteration and is
dropped — the same event-type-mapping mechanism as `DocsGenerator`, not the
registry-vs-router distinction I had reasoned my way to.

So the tally is THREE enumeration surfaces, not two, and my original docstring
instinct was right while both mechanisms I named for it were wrong:

| Surface                             | Sees nitpick? | Why not                          |
| ----------------------------------- | ------------- | -------------------------------- |
| CLAUDE.md guidance injection        | now yes       | walked router chains (FIXED)     |
| `get_handlers()` (live IPC)         | no            | walks router chains              |
| `generate-docs` (HOOKS-DAEMON.md)   | no            | EVENT_TYPE_MAPPING × config.handlers |
| `generate-playbook` (acceptance)    | no            | EVENT_TYPE_MAPPING × module path  |

The acceptance one is the worst of the three: the release process has a
BLOCKING acceptance gate, and these handlers' declared tests have never been
in the playbook it generates. A handler can therefore ship, and go on shipping,
with acceptance tests that are never run and never reported missing.

Method note, because it is the actual lesson: I read the code and drew a
confident conclusion, wrote it in a journal as settled, and it was wrong.
Running the command took four seconds. Reading imports tells you what a module
COULD do; only running it tells you what it DOES.

## 21:58 · finding · P4 prep — the counter side effect has three writers, not one

Established the Phase 4 Task 4.1 premise directly (the scout I dispatched for
this never reported, so I did it by hand):

```
record_plan_allocation()  defined  handlers/utils/plan_numbering.py:185
                          called   markdown_organization.py:575
                          called   validate_plan_number.py:217
mkplan.bash               advances hooksdaemon.latestPlanNumber itself (line 54),
                          in bash, not through the Python helper
```

So the counter has THREE independent writers, and deleting
`validate_plan_number` removes one of them, not the only one. That is a much
lower-risk starting position than the plan assumed — the recommended creation
path (`mkplan.bash`) advances the counter itself, and `markdown_organization`
covers a second path.

What that does NOT settle, and what Phase 4 must settle before deleting
anything: whether any flow reaches `validate_plan_number.matches()` WITHOUT
also reaching `markdown_organization`. If such a flow exists, deleting the
handler silently stops advancing the counter for it, and the symptom would be a
number collision much later — the worst kind of regression to trace back.
Comparing the two `matches()` implementations is the first Phase 4 task, before
any deletion.

Noting the shape rather than the specifics, because it recurs: a side effect
buried in an ADVISORY handler is load-bearing infrastructure wearing an
advisory's clothes. Nothing about `validate_plan_number` being non-blocking
tells you it also mutates repo state.

## 22:04 · finding · P4 prep — the two counter writers cover DIFFERENT flows

Followed up the 21:58 finding by reading both call sites in context, which
changes the conclusion. The three writers are not redundant with each other:

| Writer                       | Fires when                                                        |
| ---------------------------- | ----------------------------------------------------------------- |
| `mkplan.bash`                | the RECOMMENDED path — scaffolder run explicitly                  |
| `markdown_organization:575`  | the REDIRECT path — a flat plan file is written and intercepted, so the handler creates the numbered folder itself |
| `validate_plan_number:217`   | the DIRECT path — an agent writes `CLAUDE/Plan/NNNNN-name/...` or `mkdir`s the folder by hand |

So my 21:58 reading ("a much lower-risk starting position") was too
comfortable. `markdown_organization` does not back up `validate_plan_number`;
it covers a flow `validate_plan_number` never sees, and vice versa. Deleting
`validate_plan_number` outright WOULD stop advancing the counter for
hand-created plan folders — and the symptom would be a duplicate plan number
appearing much later, with nothing pointing back at this change.

Task 4.1 therefore stands exactly as written: relocate the side effect FIRST,
and to something that fires on the same trigger (`plan_qa_edit` sees PLAN.md
writes, which is the closest surface), then delete. Not "probably fine because
mkplan.bash exists" — mkplan.bash covers the path agents are TOLD to use, which
is not the same as the path they take.

Two readings of the same two call sites, opposite conclusions. The difference
was reading the enclosing function rather than the calling line: at line 575
the surrounding code is `plan_folder.mkdir(...)` + `plan_file.write_text(...)`,
which makes it obvious this is the redirect path constructing the folder
itself. `grep` showed me the call; only the context showed me the flow.

## 22:35 · progress · P3.6 — all four surfaces, one registry, and a fourth dead thing found

Task 3.6 landed. Shape: `pseudo_events/registry.py` is now the single place
that knows which handlers belong to which pseudo-event, and all four surfaces
read it (Decision 7). Verified by RUNNING each command, not by reading code —
the rule I earned the hard way twice today:

```
generate-playbook   grep -ci nitpick  →  0  before,  8  after
generate-docs       "### Pseudo Nitpick (2 handlers)" section now present
hooks-daemon handlers  "pseudo-events:" group with both handlers
CLAUDE.md           both guidance sections (fixed earlier this phase)
```

**A fourth dead thing, found while wiring the filter.** My first cut of the
registry documented that it applied "both gates the dispatcher applies", and
its per-handler test failed. Two causes, both real:

1. The nitpick handlers carry TWO config-key spellings — `dismissive_language`
   in `hooks-daemon.yaml`, `dismissive_language_nitpick` in `HandlerID`. I had
   filtered on the `HandlerID` one, which never matches the file.
2. More seriously: `PseudoEventConfig.handler_configs` was PARSED and read by
   nothing. `_register_pseudo_events` built its chain from the factory list
   unconditionally, so setting `enabled: false` on a nitpick handler in config
   did precisely nothing — no warning, no error, the handler kept firing. Every
   reader of `handler_configs` in `src/` was checked: the definition, an
   unrelated plugins loader, an unrelated validator. No consumer.

That is the same defect class this entire plan is about — a config key users
can set that reaches nothing — and I nearly wrote a docstring asserting the
gate worked. The registry now applies it, dispatch filters through the same
call, and the key means the same thing everywhere.

Keyed the registry on the CONFIG FILE spelling rather than `HandlerID`, since
a filter has to match what the file says. Recorded that in the module docstring
so the next person does not "fix" it back.

Rejected `EVENT_TYPE_MAPPING`: adding `nitpick` there would have satisfied two
of the four surfaces in one line, and made the router treat a pseudo-event as
dispatchable. Convenient for the generators, wrong for the thing the map is
for.

One test of mine was wrong again, in the harmless direction this time: I
asserted the docs table would contain each handler's DISPLAY name, but that
table lists CONFIG KEYS in every other row. The implementation was right and
the expectation was wrong; fixed the test and added one asserting the rows are
grouped under their own "Pseudo Nitpick" heading rather than folded into a real
event's table — folding them in would tell a reader they can add a handler
under `stop:` and have it fire, which is exactly the confusion that left two
handlers shadowed there for months.

## 22:50 · finding · P4 prep — plan_completion_advisor is covered, and then some

Compared `plan_completion_advisor` against the `terminal-placement-hint`
plan_qa check line by line, rather than trusting Plan 00234's verdict.

| | `plan_completion_advisor` | `terminal-placement-hint` |
| --- | --- | --- |
| Trigger | Write/Edit of `CLAUDE/Plan/NNNNN-*/PLAN.md` outside `Completed/` | same edit stage, via `plan_qa_edit` |
| Fires on | `**Status**: Complete[d]` ONLY | ANY terminal status — Complete, Cancelled, Superseded |
| Archive target | hardcodes `Completed/` | picks `cancelled_dir` for Cancelled, else `completed_dir` |
| Says | git mv, README row, statistics | git mv, README row, statistics |
| Level | advisory | ADVISE |

So the plan_qa check is a strict SUPERSET: same trigger surface, same three
remediation steps, and it also covers the two terminal statuses the handler
ignores while routing Cancelled plans to the right directory — which the
handler would get wrong if it fired on them at all.

Independent corroboration I did not plant: this handler is already recorded in
`test_claude_md_guidance_coverage.py` as exempt from resident guidance, with the
reason "its three steps are already resident under plan_qa_commit_gate's
terminal-state-atomic invariant, which also ENFORCES them". That verdict was
written before this plan existed, by someone applying a different criterion,
and lands in the same place.

Verdict: **SAFE TO FOLD**, and unlike `validate_plan_number` there is no side
effect to relocate first — `handle()` returns an advisory string and nothing
else, `get_claude_md()` is already None. Task 4.3 is a clean deletion; Task 4.1
and 4.2 are the ones that need care.

## 22:58 · finding · P4 prep — validate_plan_number's replacement fires LATER, and harder

Read `counter_sanity.py` properly. Its header settles a question the plan had
glossed:

```
"""Check ``counter-sanity`` (Stage 2, block; sin D1). ..."""
```

**Stage 2**, not Stage 1. It runs on the STAGED tree at `git commit`, whereas
`validate_plan_number` is a PreToolUse handler firing when the folder is
created. So the two do NOT "co-fire on the same tool call" — the fold moves the
signal from creation-time to commit-time, and from ADVISE to BLOCK.

That trade is defensible in itself: later feedback, but stronger, and still
before anything reaches history. What it does NOT do is replace the side
effect. `counter-sanity` READS the counter; `validate_plan_number` WRITES it.
Delete the handler without relocating `record_plan_allocation` and the counter
silently stops advancing for hand-created folders — after which
`counter-sanity` starts blocking commits for a drift the deletion caused. The
guard would fire correctly, on a problem that only exists because its partner
was removed. That is a bad failure mode: correct-looking enforcement pointing
at the wrong cause.

Contrast with Task 4.3 (`plan_completion_advisor`), which is pure advisory with
no side effect and folds cleanly. The two MERGE verdicts in Phase 4 are not the
same shape at all, and treating them as one bullet was hiding that.

Revised understanding of the Phase 4 order:

1. Task 4.3 first — clean deletion, no side effect, strict superset already in
   place. Lowest risk, banks a win.
2. Task 4.1 next — relocate `record_plan_allocation` to an edit-stage surface
   (`plan_qa_edit` sees the same PLAN.md writes), and prove the counter still
   advances for a hand-created folder BEFORE deleting anything.
3. Task 4.2 last — only once 4.1 is proven, since it is the deletion that
   depends on it.

## 23:10 · verification · P5 prep — an unedited v3.52 config still validates clean

Task 5.4's core assertion, checked directly rather than waiting for the
client-mode fixture. Synthesised a config naming ALL NINETEEN retired handler
keys in their historic event sections, and ran it through the production
`ConfigValidator`:

```
errors with all 19 retired handlers named: []
errors with a genuinely unknown key added:
  ["Unknown handler 'definitely_not_a_real_handler' at
    'handlers.pre_tool_use.definitely_not_a_real_handler'. ..."]
```

The second line is the point. A clean first result on its own would be equally
consistent with handler-name validation being switched off entirely — in which
case the registry would be doing nothing and I would have "confirmed" it. The
paired negative shows the check is live and discriminating: retired keys are
tolerated BECAUSE they are registered, not because nothing is looked at.

So a client upgrading from v3.52 without editing their config gets no errors
and no degraded mode, which is the whole contract `RETIRED_HANDLERS` exists to
honour: removing a handler is our decision, not a mistake in their file.

Probe config deleted after use rather than left in `untracked/` — it was a
question, and the answer belongs here.

The full client-mode fixture run (`scripts/dummy-client-repo.sh`) is still
Task 5.4 and still worth doing: this proves the validator's verdict, not that a
real install starts, deploys its wrapper and serves hooks.

## 23:20 · incident · this project's OWN release-safety handler is shadowed

Ran `validate-project-handlers` as a routine post-change check and read the
output properly instead of skimming for "OK":

```
stop/release_blocker.py -> ReleaseBlockerHandler
  - Priority: 12
  - Terminal: True
```

Priority 12 is ABOVE `auto_continue_stop` at 10. Probed the live daemon on two
Stop shapes rather than inferring it:

```
ordinary stop (no explanation)      mentions release-blocker: False
stop WITH a valid explanation       mentions release-blocker: False
```

`ReleaseBlockerHandler` — "Blocks Stop event during releases until acceptance
tests complete", written after v2.13.0 when an agent repeatedly tried to skip
acceptance testing — has never been able to fire. A release-safety guard that
cannot guard.

**Root cause is in its own docstring**, and it is not a mistake by whoever
wrote it:

```
Priority: 12 (before AutoContinueStop at 15)
```

It WAS before AutoContinueStop. The daemon still ships 15 in its config
template (`init_config.py`). This project's `.claude/hooks-daemon.yaml` later
set `auto_continue_stop` to 10 — and that edit silently disabled a project
handler registered on the assumption of 15. Nobody noticed, because a shadowed
handler and a handler that simply did not match produce identical output:
nothing.

So the hazard this plan has been documenting all evening is not hypothetical
and not limited to the two handlers I deleted. It has been live in this repo,
on the guard that protects the RELEASE process, which is about the worst place
for it.

Fix is safe: its `matches()` is narrow (release files modified AND not
stop_hook_active), so moving it below 10 blocks only during an actual release
and lets every ordinary stop fall through to `auto_continue_stop`. Verified my
working tree touches none of `RELEASE_FILES`, so the change cannot trap this
session.

DBF, and this is the part that matters more than the priority: the guard I
wrote today (`test_stop_chain_terminal_shadowing.py`) proves the hazard EXISTS
using a synthetic probe — it does not check whether this project has actually
fallen into it. It would not have caught this. A guard that documents a trap
without checking the floor is half a guard. Adding a check over the real
registered Stop chain, so any handler above the terminal one fails the suite by
name.

Sequencing: finishing the in-flight 3.6/3.7 commit first, then this as its own
unit, so the release-blocker fix is not buried inside an unrelated change.

## 23:45 · incident · the release guard had TWO bugs, and the first one hid the second

Fixed Task 3.8 by moving `release-blocker` to priority 8, restarted, dirtied
`CHANGELOG.md`, probed the live socket — and it STILL did not fire.

Second bug, and it is worse than the first:

```
/proc/<daemon-pid>/cwd -> /
```

`matches()` ran a bare `git status --short`. The daemon is a long-lived
process whose working directory is `/`, so git ran outside any repository,
exited non-zero, and hit the handler's `# Silent allow on git error` branch.
`matches()` returned False for every release there has ever been.

**The ordering bug was hiding it.** Had I fixed the priority, seen the guard
listed in the right place, and moved on — which is exactly what "fix the
priority" looks like when you stop at the code — I would have shipped a
handler that was reachable and still incapable of firing, with the obvious
defect visibly fixed. That is the more expensive outcome of the two.

Fixed with `git -C <root>`, resolving the root from the event's `cwd` and
falling back to `ProjectContext.project_root()`. The fail-safe stays — a
release guard must never trap a session because git misbehaved — but it now
LOGS instead of returning silently, so a handler that has stopped working says
so rather than looking identical to one with nothing to report.

Verified in both directions against the live daemon, not in a unit test:

```
CHANGELOG.md dirty  -> {"decision":"block","reason":"🚫 RELEASE IN PROGRESS: ..."}
CHANGELOG.md clean  -> auto_continue_stop's ordinary explanation block
```

First time this handler has ever fired. It was written after the v2.13.0
release, when an agent repeatedly tried to skip acceptance testing, and it has
never once been able to stop that happening.

**Both bugs were PINNED BY THEIR OWN TESTS**, which is the part worth keeping:

- `test_priority` asserted `priority == 12` with the rationale "before
  AutoContinueStop at 15". The constant was right when written; the thing it
  was chosen against moved, and the test kept passing. Rewritten to read the
  configured `auto_continue_stop` priority and assert the RELATION, so the two
  cannot drift apart again.
- `test_matches_returns_false_when_no_release_files_modified` asserted
  `mock_run.assert_called_once_with(["git", "status", "--short"], ...)` — it
  asserted the cwd-dependent invocation exactly. The test defended the bug.
  Now pins `["git", "-C", <root>, "status", "--short"]`.

A test that asserts a CONSTANT cannot notice when the thing that constant was
chosen against has moved. A test that asserts a CALL SHAPE defends whatever
shape was there when it was written. Both of these looked like good coverage —
60 project-handler tests, all green, across a handler that did nothing.
