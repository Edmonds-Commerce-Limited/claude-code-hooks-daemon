# Plan 00312 — Journal 26-09-01

> **Append-only activity log** for plan 00312. One file per day
> (`00312-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 20:21 · action · — — plan scaffolded

Plan 00312 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 20:23 · action · — — plan filed from owner request (mid-release)

- Owner: a single accidental Ctrl+C now kills background agents in Claude
  Code; wants the first ^C intercepted while rapid repeat still interrupts.
  Design direction recorded: the ccy supervisor's PTY position makes it the
  natural interception point (swallow lone 0x03, forward on double-press,
  loud hint, config + decision-log). Filed but NOT started — a v3.59.0
  release is in flight and the supervisor file already carries uncommitted
  release changes; implementation follows the release.
- Session failsafe recovery cron: reusing the single session cron id
  `892eef87`; nothing new created.

## 20:35 · action · T2.1 — guard implemented TDD (owner pulled it pre-release)

- Owner: "review my ctrl c request, lets get this done before we do this
  release" — implementation pulled forward of the v3.59.0 finalisation.
- RED: `tests/unit/supervise/test_input_ctrlc_guard.py` (28 tests: pure
  `CtrlCGate` machine, env resolution, `_forward_io` end-to-end) failed on
  missing `CtrlCGate`. GREEN: full supervise suite passes (588 tests).
- Implementation in `claude-supervise.py`: `CtrlCGate.filter()` — a chunk
  that is EXACTLY `b"\x03"` is a press; first press swallowed + window armed
  (default 2.0s), second press inside the window forwarded and disarmed,
  expiry re-arms. Any other chunk (paste burst, coalesced `\x03\x03` spam)
  passes through untouched — the escape hatch can never be delayed by read
  coalescing. Env config: `CCY_CTRL_C_GUARD` (default on; 0/false/no/off
  disables), `CCY_CTRL_C_WINDOW_SECONDS` (default 2.0; garbage/non-positive
  falls back). Wired in `supervise()`: swallow posts the status-line hint
  ("Ctrl+C intercepted — press Ctrl+C again within 2s to interrupt") via
  `StatusMessagePoster`, and both swallow and forward events go to the
  decision log (`ctrl-c guard: ctrl-c-swallowed|ctrl-c-forwarded`).
- Test-rig finding: two `os.write`s back-to-back coalesce into one
  `b"\x03\x03"` read, which the gate (correctly) passes through as spam —
  the sequenced-press tests space writes by 0.1s so each press is its own
  chunk.

## 20:35 · finding · T2.2 — host tier does NOT hot-reload; live dogfood deferred

The gate lives in `_forward_io`, which runs in the long-lived PTY HOST — the
content-hash hot-reload contract (.claude/ccy/CLAUDE.md) covers only the
`--worker` subprocess. Restarting the host to pick the change up would drop
the live `claude` child, exactly what the two-tier split exists to avoid. So
Task 2.2 (live single-^C/double-^C dogfood) happens at the NEXT supervised
session start; this session continues on the pre-guard host code.
