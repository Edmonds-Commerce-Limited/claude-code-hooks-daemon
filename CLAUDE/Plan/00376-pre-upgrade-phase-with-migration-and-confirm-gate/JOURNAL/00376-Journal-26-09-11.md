# Plan 00376 — Journal 26-09-11

> **Append-only activity log** for plan 00376. One file per day
> (`00376-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 12:46 · action · — — plan scaffolded

Plan 00376 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 12:47 · decision · — — owner ruled against deprecation windows

The owner's words: "i dont like delay - its over complex … lets have proper
semver and lets have a pre upgrade process so agents can properly understand
what is going to happen and what they need to do, and also give them ability to
confirm (with owner if needs be) that the upgrade should proceed."

The reasoning that survived scrutiny, recorded because a future agent will be
tempted to reintroduce a window as "the safe option":

- A window substitutes for being unable to reach your consumers. This daemon IS
  installed inside the consumer's repository and upgraded by an agent with
  write access to it, so the substitute is strictly worse than the thing it
  substitutes for.
- For a rename specifically, a window makes the defect — two live names for one
  concept — correct by policy for a release, and invites a new reader to key on
  the spelling about to disappear. It ADDS a failure mode.
- It does not avoid the break. Under `CLAUDE/development/RELEASING.md:966`
  ("Breaking API changes, removed features" = MAJOR) the version owed is the
  same whenever the removal lands. The window buys delay and nothing else.

## 12:48 · finding · P1 — the existing gate is inert, and three surfaces are broken

Surveyed the upgrade pipeline before designing. The gap is not documentation:
the machinery is missing, and what looks like a gate does not fire.

- `scripts/upgrade_version.sh:733-861` — Step 5a prints "REQUIRED READING" and
  asks yes/no, but `:808` tests `[ ! -t 0 ]` and falls through at `:814` to
  "Review upgrade guides after upgrade". **Every agent-driven upgrade takes
  that branch.** The gate protects a human at a terminal and nobody else.
- It would not be pre-install regardless: `scripts/upgrade.sh:550` checks the
  new version out, then delegates to Layer 2 at `:608-629`. Step 5a runs with
  the new code already on disk.
- `CLAUDE/UPGRADES/UNRELEASED/post-upgrade-tasks/README.md:20` — "There is no
  runner. Nothing executes these tasks automatically." Nothing in the upgrade
  path reads the directory, `.claude/skills/hooks-daemon/upgrade.md` never
  mentions it, and it has no schema test (contrast
  `tests/integration/test_pending_release_notes_holding_area.py`). Plans are
  REQUIRED to write there as part of their definition of done, and those files
  are read by nobody.
- `install/upgrade_compatibility.py:351-373` scans only
  `CLAUDE/UPGRADES/v{major}/`, never `UNRELEASED/`.
- The only thing that scans the project at all is `check-worktree-seed`
  (`scripts/upgrade.sh:870-891`); it reports and never writes. There is no
  "this project uses X" detection anywhere.

Takeaway shaping the plan: Phase 4 exists because a surface plans are obliged
to feed and nothing reads is worse than no surface — it manufactures the
appearance of a safety net.

## 12:49 · thought · T1.1 — the bootstrapping constraint is why this does not already exist

Pre-upgrade instructions ship WITH the incoming version but must run BEFORE it
is installed. `post-upgrade-tasks/` has no such problem, which is the most
likely reason it exists and a pre-upgrade peer does not. Task 1.1 is that
problem, and it should be settled before any of the surface is built —
constructing `pre-upgrade-tasks/` first and then discovering it cannot be read
in time would waste the whole phase.

## 12:50 · action · — — session crons

Reused the existing failsafe recovery cron rather than creating a second:
`75ba7c5e` (hourly at :37). The background-process watchdog was deleted when
its work finished; it is recreated on demand.

## 12:58 · finding · T1.1 — that guess was WRONG; the constraint does not apply

Verified instead of assuming, and the assumption did not survive. The incoming
version's whole tree is on disk before anything is deployed:

- `scripts/upgrade.sh:550` — "Step 6: Land the daemon dir on the target version
  FIRST (before Layer 2)."
- `scripts/upgrade.sh:608` — "Step 8: Delegate to Layer 2 (now available after
  checkout)", running `$DAEMON_DIR/scripts/upgrade_version.sh` — i.e. the NEW
  version's script.
- The existing compat check already depends on this, reading the NEW
  `CHANGELOG.md` from `$DAEMON_DIR` to learn what the target removed.

So "pre-upgrade" in this codebase means pre-DEPLOY, not pre-fetch, and a
`pre-upgrade-tasks/` manifest in the incoming tree is readable exactly where
the compat check already runs. Phase 1 collapses from "invent a fetch
mechanism" to "site the phase and define abort semantics".

The real consequence surfaced by the same check: the daemon dir is ALREADY on
the target checkout when the phase would run, so an abort is not free. That is
now Task 1.2, and it is a harder question than the one it replaced.

## 13:00 · finding · — — a pre-upgrade phase already exists, built by Plan 00062

Plan 00062 ("Breaking Changes Lifecycle", Complete) delivered
`install/upgrade_compatibility.py` as its Phase 4 "Pre-Upgrade Validation", and
its goal 6 — "make upgrade script require confirmation of reading breaking
change docs" — is the Step 5a gate that is now inert for agents.

The dedupe scout did not surface this because it reads only LIVE plans, and
00062 is Completed. Worth remembering as a scouting limitation: for "does this
machinery already exist", completed plans are the more likely home.

00062's Non-Goal is carried forward deliberately: "Automatic config migration
(too risky - user confirmation required)". This plan detects and proposes; it
never silently rewrites a user's repository.

## 13:05 · action · — — entry order restored, and a gap the sweep does not see

The `12:50 · action · session crons` entry above was appended before the 12:58
and 13:00 findings were written, so inserting those left it sitting BELOW them
— breaking this journal's own grammar ("times increase down the file"). It has
been moved back to its chronological slot with its text unchanged.

Worth recording as a possible gap: `journal-append-only` fired correctly on the
edit, but `plan-qa --sweep` reported `0 findings` against a file whose entry
times ran 12:49 → 12:58 → 13:00 → 12:50. Nothing checks the ordering invariant
the preamble states, so a malformed journal passes QA.

The background-process watchdog cron has since been recreated as `2e49f8d7`
(hourly at :13) to cover a running full-QA job.
