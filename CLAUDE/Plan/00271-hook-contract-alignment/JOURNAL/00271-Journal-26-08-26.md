# Plan 00271 — Journal 26-08-26

> **Append-only activity log** for plan 00271. One file per day
> (`00271-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 05:51 · action · — — plan scaffolded

Plan 00271 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 06:40 · action · P1 — execution start (worktree agent)

- Executing in isolated worktree `.claude/worktrees/agent-a31082bd6ba927bc5-095ae460`
  on branch `agent-a31082bd6ba927bc5-095ae460`. Daemon restart verification is
  DEFERRED to the merge reviewer on main — the dogfood daemon must not be
  restarted from a worktree.
- Venv: the worktree has no venv of its own; symlinked
  `untracked/venv -> /workspace/untracked/venv-workspace-py311-81c29529` so the
  QA scripts resolve an interpreter. All pytest/QA runs use
  `PYTHONPATH=<worktree>/src` so the worktree's code (not the main checkout's
  editable install at `/workspace/src`) is what is imported and tested.
- Set PLAN.md status to In Progress.

## 06:45 · finding · T1.1 — raw docs fetch and verbatim verification

- Fetched https://code.claude.com/docs/en/hooks.md RAW (curl, no summarising
  layer) to `untracked/hooks-raw.md`: 291,536 bytes,
  sha256 279dd7dc054bb686426d8af376b9380f5705639bd90a5a1fde8e67cd126062eb —
  byte-identical size to the audit's fetch, so the same doc revision.
- Verified the audit's contract table against the raw markdown directly
  (decision-control table at "#### Decision control"; per-event sections read
  verbatim). Confirmed verbatim: `permissionDecision` allow/deny/ask/defer +
  `updatedInput`; PermissionRequest `decision.behavior` (sic, quoted from the
  docs) allow/deny only, with `updatedInput`/`updatedPermissions` (allow) and
  `message`/`interrupt` (deny); UserPromptSubmit top-level `decision: "block"`
  + `reason` + `suppressOriginalPrompt` + `sessionTitle`; SessionStart
  `hookSpecificOutput.additionalContext`/`initialUserMessage`/`sessionTitle`/
  `watchPaths`/`reloadSkills`; PreCompact in the top-level `decision: "block"`
  group with `systemMessage` discarded; DirectoryAdded documented as an event.
  NO `permissionDecision: "escalate"` anywhere in the raw text — the
  fabrication lesson holds.
- Count correction vs the audit: the raw doc's event sections enumerate **31**
  events (the audit's prose said 30, but its own table lists all 31 —
  the prose count was off by one). Vendoring all 31.

## 07:10 · action · T1.1–T1.5 — guard built

- Vendored `contracts/claude-code-hooks/`: 31 per-event JSON files (each with
  block mechanism, can_block/ask_capable, top-level + hookSpecificOutput field
  tables with enums, discarded fields, and a VERBATIM input example extracted
  mechanically from the raw doc's fenced json blocks) + `META.json`
  (docs URL, fetch date, byte size, sha256, last-audited Claude Code 2.1.246 —
  matches the installed `claude --version`).
- DEVIATION from Task 1.8's stated path: `contracts/claude-code-hooks/REFRESH.md`
  is blocked by the `markdown_organization` handler (contracts/ is not an
  allowed markdown location). The refresh procedure lives at
  `docs/guides/HOOK-CONTRACT-REFRESH.md` instead; `META.json.refresh_procedure`
  points there. Content unchanged, raw-fetch-only rule + fabrication incident
  recorded as mandated.
- TDD: `tests/unit/qa/test_check_hook_contract.py` written first (RED confirmed:
  ImportError, `untracked/red1.txt`), then `scripts/qa/check_hook_contract.py`
  implemented — 31 unit tests GREEN. Rules: undocumented-schema-field/enum-value/
  claim/emitted-token, missing-refusal-claim, capability-table-drift,
  event-missing-from-catalogue, unexpressed-capability/enum-value/universal-fields,
  dead-letter-field, stale-allowlist-entry, malformed-allowlist-entry.
  Status/StatusLine is skipped by design (out-of-contract feature).
- Wired into `scripts/qa/run_all.sh` (check 23 + summary table row) and
  `scripts/qa/llm_qa.py` (`hook_contract` tool, before smoke_test which must
  stay last).

## 07:20 · finding · T1.6 — RED run recorded (guard proven before any fix)

Ran `check_hook_contract.py` against the unmodified tree BEFORE seeding the
allowlist: exit 1, **59 findings**. Full output preserved below. Coverage of
the audit's nine load-bearing drifts, item by item:

1. `unexpressed-capability:PreToolUse:hookSpecificOutput.updatedInput`
2. `unexpressed-enum-value:PreToolUse:hookSpecificOutput.permissionDecision=defer`
3. `undocumented-enum-value:PermissionRequest:hookSpecificOutput.decision.behavior=ask`
   + `undocumented-claim:PermissionRequest:ask`
4. `undocumented-schema-field:PermissionRequest:hookSpecificOutput.additionalContext`
   + `unexpressed-capability:PermissionRequest:hookSpecificOutput.decision.{message,updatedPermissions,interrupt}`
5. `missing-refusal-claim:UserPromptSubmit:deny`
   + `unexpressed-capability:UserPromptSubmit:{decision,reason,suppressOriginalPrompt}`
6. `unexpressed-capability:SessionStart:hookSpecificOutput.{additionalContext,initialUserMessage,sessionTitle,watchPaths,reloadSkills}`
7. `missing-refusal-claim:PreCompact:deny` + `unexpressed-capability:PreCompact:{decision,reason}`
   + `dead-letter-field:PreCompact:systemMessage`
8. `event-missing-from-catalogue:DirectoryAdded:catalogue`
9. `undocumented-emitted-token:{ConfigChange,PostToolBatch,PostToolUseFailure,TaskCreated,UserPromptExpansion}:decision=deny`
   + `missing-refusal-claim` on all six wired extras (TeammateIdle's documented
   JSON block is `continue: false`, so it gets the refusal-claim finding, not
   the emitted-token one) — plus TaskCompleted, a superset of the audit's list.

Moderate/cosmetic drifts also reported: PostToolUse
`classifierContext`/`updatedToolOutput`/`updatedMCPToolOutput`, grouped
universal-field gaps per bespoke event, dead-letter `systemMessage` on
SessionEnd/Notification/WorktreeCreate/WorktreeRemove, `guidance` as an
undocumented schema field on four events, UserPromptSubmit `sessionTitle`.

```text
$ untracked/venv/bin/python scripts/qa/check_hook_contract.py   # exit=1
Found 59 hook-contract violation(s):
(full list preserved in the commit that adds contracts/claude-code-hooks/ —
identical to the 59 seeded ALLOWLIST.yaml entries, whose ids ARE the findings)
```

Then seeded `ALLOWLIST.yaml` with all 59 findings, each entry carrying a
reason and a link to its Phase 2/3 fix task (2.1–2.8, 3.1, 3.2). Checker now
exits 0: `No hook-contract violations (59 recorded allowlisted gap(s))`.
`llm_qa.py hook_contract` → `✅ 0 violations (59 allowlisted gaps recorded)`.

## 08:20 · action · T1.7 — contract_staleness advisory shipped

TDD (17 unit tests RED→GREEN): `handlers/session_start/contract_staleness.py`,
sibling of `version_check`. Compares `claude --version` (24h cache, 5s
timeout, fail-silent) against `META.json.last_audited_claude_code_version`
resolved relative to the daemon source tree; advises the REFRESH procedure
when the installed binary is newer. Registered: HandlerID/Priority(60)
constants, session_start package, `.claude/hooks-daemon.yaml` + `.example`,
`init_config.py` default, HANDLER_REFERENCE.md entry + summary row,
`_EXEMPT_FROM_GUIDANCE` verdict, error-hiding exclusions (3 entries),
config-changes vUNRELEASED entry (`recommended: true`). Live observation of
the advisory is DEFERRED to the merge reviewer (worktree must not restart the
dogfood daemon).

## 09:10 · action · P2 — all nine load-bearing drifts fixed (guard green incrementally)

Each task: unit tests first, fix, targeted suites green, allowlist entries for
that drift DELETED so the guard now enforces it. Checker went
59 → 57 → 54 → 40 → 35 → 30 → 29 recorded gaps across the commits.

- **2.1** PermissionRequest ask claim removed everywhere: REFUSAL table, schema
  enum (allow|deny), substitute routing; PermissionRequest moved to the
  BLOCKING tier (`PermissionRequestHandlerBase = BlockingHandler`);
  `auto_approve_reads` retyped to `BlockingResult`.
- **2.2** UserPromptSubmit: top-level `decision: "block"` + `reason` in schema
  and a dedicated formatter; event → blocking tier; the four UserPromptSubmit
  handlers retyped to `BlockingResult`.
- **2.3** Wired extras: bespoke schemas + formatters. UserPromptExpansion/
  PostToolUseFailure/PostToolBatch/TaskCreated/ConfigChange emit
  `decision: "block"` (+ hso additionalContext where documented);
  TeammateIdle/TaskCompleted emit `continue: false` + `stopReason`. The old
  undefined `{"decision": "deny"}` token is now schema-REJECTED on all seven.
  All seven → blocking tier. NEW recorded gap: `dead-letter-field:ConfigChange:
  systemMessage` (advisory path still emits it; docs say accepted-but-discarded).
- **2.4** PreCompact: schema gains `decision: "block"` + `reason`; routed
  through the top-level-block formatter; `systemMessage` kept + commented as
  the documented dead-letter; event → blocking tier; `compaction_signal`
  retyped.
- **2.5** `Decision.DEFER` + `HookResult.updated_input`; PreToolUse schema enum
  gains `defer`, hso gains `updatedInput`; `GatingResult` literal + `defer()`
  factory; DEFER added to chain restrictive set, verdict-log blocking set and
  the decision-capability factory map; an ALLOW carrying updatedInput is no
  longer treated as a silent allow. **Unblocks Plan 00270** (notified at
  hand-off).
- **2.6** PermissionRequest deny reason (+context) now emitted on the
  DOCUMENTED `decision.message`; `updatedPermissions`/`interrupt` expressible
  in the schema; ALLOW/ASK advisory context stays on the daemon's
  additionalContext extension (recorded gap).
- **2.7** SessionStart: schema gains the documented hookSpecificOutput
  (additionalContext/initialUserMessage/sessionTitle/watchPaths/reloadSkills);
  advisory context now emitted on BOTH `hookSpecificOutput.additionalContext`
  (documented Claude channel) and `systemMessage` (previous behaviour
  preserved). DEVIATION: the task's live `debug_hooks.sh` verification cannot
  run from a worktree — dual emission is documented-valid on both fields, so
  the risk of the migration is bounded; LIVE VERIFICATION DEFERRED to the
  merge reviewer on main (observe a SessionStart advisory once after restart).
- **2.8** DirectoryAdded catalogued (`wired=False`), tracked in
  `EXPECTED_UNWIRED`, EventKey literal + completeness-test list extended,
  `DirectoryAddedHandlerBase` (advisory) added; wiring end-to-end left as the
  Plan 00170 burn-down pattern prescribes.
- **2.9** truth-changes vUNRELEASED entry covering every client-observable
  change (new deny surfaces, tier moves, decision.message routing, SessionStart
  channel, defer/updatedInput, the guard itself).

Legacy tests that ENCODED the drifted contract were updated to the documented
truth (test expectations are documentation; Claude Code defines the contract):
dropped-refusal carried tables, decision-capability event lists, tier
known-assignments, SessionStart "must not have hookSpecificOutput" assertions,
PermissionRequest reason-survives channel, PreCompact deny shape.

## 09:30 · action · P3 — burn-down, cosmetics, coordination, docs, follow-up

- **3.1** All 29 remaining allowlist entries re-reasoned as KEPT with final
  rationale (universal fields YAGNI; dead-letter systemMessage documented;
  PostToolUse rewrite fields YAGNI per plan Non-Goals; UserPromptSubmit
  sessionTitle/suppressOriginalPrompt no carrier; WorktreeCreate hso
  worktreePath is the HTTP-hook form — the daemon is a command hook).
- **3.2** `guidance` commented as a daemon-internal extension at all four
  schema declarations; Stop `decision: "block"` now always carries a `reason`
  (named fallback constant, unit-tested); StatusLine commented out-of-contract
  by design.
- **3.3** Plan 00170 PLAN.md Dependencies gained the coordination note: Phase 4
  builds on `META.json` + `check_hook_contract.py` + `contract_staleness`,
  never a parallel mechanism; interface files named there.
- **3.4** Docs: CLAUDE.md + HANDLER_DEVELOPMENT.md tier tables updated
  (Gating = PreToolUse only, with defer; the blocking-tier event list);
  ARCHITECTURE.md gained "The Vendored Hooks Contract" section.
- **3.5** Follow-up plan FILED: Plan 00273 hook-input-payload-validation
  (scaffolded via mkplan.bash; README row added).

## 10:00 · action · P3 — full QA cycle in the worktree

First `./scripts/qa/llm_qa.py all` (run with the worktree's `src/` first on
PYTHONPATH so this tree's code, not the main checkout's editable install, is
under test): 17/24, with every failure triaged:

- `format` — black autofix reformatted 7 files; committed, re-run clean.
- `tests` — one legacy sweep case (`pre_compact` in the cannot-block list of
  `test_validate_project_handlers_decision_capability`) encoded the drifted
  contract; updated (PreCompact now blocks) — coverage 95.1%.
- `sensitive_content` — 26 hits: the vendored input examples carried the
  docs' own example UUIDs (session/turn/message ids), which trip the
  `session-uuid` public pattern. Replaced with the policy's all-same-digit
  placeholders (all-zero; all-a/all-b where two must stay distinguishable).
  Examples otherwise verbatim.
- `canonical_callers`, `project_handlers`, `semgrep` — venv RESOLUTION only:
  the worktree has no fingerprint venv, so `resolve_venv.sh` found nothing.
  Symlinked the main checkout's fingerprint venv into the worktree's
  untracked/; all three then pass individually (project handlers: 69 tests).
- `smoke_test` — probes the LIVE daemon socket; no daemon runs for this
  worktree by design. ENVIRONMENTAL, deferred to the merge reviewer on main.

Second full run kicked off after the fixes — verdict recorded below.

## 10:10 · handoff · — — merge-review checklist (for main)

1. `git merge --no-ff agent-a31082bd6ba927bc5-095ae460` (or cherry-pick).
2. `./bin/hooks-daemon restart` + `status` (RUNNING) — restart verification
   was deliberately NOT done from the worktree.
3. Observe one SessionStart advisory live (Task 2.7's deferred verification:
   context should arrive via hookSpecificOutput.additionalContext AND
   systemMessage; confirm no doubling artefacts in the UI, then decide whether
   to drop the systemMessage half in a follow-up).
4. `./scripts/qa/llm_qa.py all` on main (smoke_test now runs against the live
   daemon; expect full pass).
5. `./bin/hooks-daemon generate-docs` (HOOKS-DAEMON.md picks up
   contract_staleness + behaviour strings).
6. Then flip Task 2.7 ✅ and the plan to Complete (git mv to Completed/ +
   README row + stats in ONE commit).
7. Plan 00270 could not be notified directly (its agent's worktree is gone) —
   whoever resumes it: PreToolUse updatedInput/defer are expressible once this
   branch merges.
