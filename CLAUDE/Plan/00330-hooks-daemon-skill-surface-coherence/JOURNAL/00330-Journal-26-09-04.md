# Plan 00330 — Journal 26-09-04

> **Append-only activity log** for plan 00330. One file per day
> (`00330-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 11:08 · action · — — plan scaffolded

Plan 00330 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 15:50 · finding · P1 — Phase 1 inventory measured (Tasks 1.1–1.3), `e5f48101`

Written up in `SURFACE-INVENTORY.md`. The numbers that change the plan's
framing:

- **No human types `/hooks-daemon` at all, here.** 25 workspace transcripts
  record `/compact` ×129, `/effort` ×18, `/model` ×16, `/goal` ×13,
  `/release` ×9 — and `/hooks-daemon` ×0 with any subcommand. The 14,641
  transcript lines mentioning `hooks-daemon` are agents running
  `bin/hooks-daemon <verb>` in Bash (`restart` 1,578, `status` 1,344,
  `plan-qa` 1,159, `harvest-background` 816 …). Human-use evidence for
  Task 1.1 is therefore indirect (doc mentions + CLI-verb use), and the
  classification is a proposal: route 6 (`install`, `upgrade`, `optimise`,
  `restart`, `health`, `bug-report`), document 10, retire none.
- **Registry is 116 discovered handlers, not 110**; checklist names 22
  (20 + the nitpick pair, which it spells by YAML key so the Plan 00323
  resolver test never sees them). Fair denominator 96 after excluding 14
  status-line + 6 daemon-integrity handlers; gap 74; 13 of the registry are
  default-off by design and the checklist already recommends enabling one
  (`lsp_enforcement`).
- **Tags are the only registry-side category signal and they do not
  partition.** A precedence rule over tags classifies 63/96; 33 remain
  (11 untagged, 22 tagged only `workflow`/`advisory`). `auto_continue_stop`
  carries `planning`, so event rules must precede tag rules.

Dead end worth recording: `bin/hooks-daemon` cannot resolve a venv in a
fresh worktree (looks under `untracked/venv-*`); `uv sync --frozen --extra
dev` then `.venv/bin/python -m claude_code_hooks_daemon.daemon.cli <verb>`
works for `handlers`, `plan-qa --lint`, `docs-qa --lint`, `init-config
--stdout`. Both lints report 0 findings on the new document and PLAN.md.

## 15:50 · handoff · P1 — Phase 1 complete; Phases 2–4 wait on seven owner decisions

The decisions are the last section of `SURFACE-INVENTORY.md`: opt-out
mechanism, default-off policy, area names/count, `report` vs `bug-report`,
housekeeping step list and order, unconfirmed-action set, and whether
`idle_housekeeping_advisory` is the Phase 3 vehicle. Nothing in Phases 2–4
was started.
