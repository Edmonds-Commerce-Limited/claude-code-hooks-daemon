# Plan 00330 — Journal 26-09-08

> **Append-only activity log** for plan 00330. One file per day
> (`00330-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 11:20 · action · P3 — housekeeping pass as data, CLI verb, advisory reuse (`59b11a0a`)

`daemon/housekeeping.py` is a pure step list: 12 report-only steps, then
`format-markdown` and `regenerate-docs` (unconfirmed), then six HELD steps,
then `optimise` last. `plan_pass(apply)` assigns RUN/HELD; `render_procedure`
prints the sub-agent procedure. `cmd_housekeeping` (`--apply STEP`, `--list`,
`--reports-dir`, `--project-root`) prints it and runs nothing. The idle
advisory's "candidate audits" list is now derived from `report_only_steps()`
and points at `bin/hooks-daemon housekeeping`.

Two choices worth recording:

- `optimise` is a step with no CLI verb (`cli_argv=()`, `via_skill=
  "optimise"`), invoked through its existing skill entry point — the other
  agent owns the checklist and `optimise-invoke.sh`, so nothing here touches
  them.
- A step's verb is tested against the real argparse (`--help` exits 0), so a
  renamed CLI verb fails the housekeeping tests, not a client's pass.

## 11:40 · action · T1.1 — Decision 4 surface trim (`f458057e`)

SKILL.md routes 8 arms (`install`, `upgrade`, `optimise|optimize`,
`housekeeping`, `report`, `restart|bug-report`, `health`, help). The 10
documented-only subcommands live in a "Capabilities (CLI verbs)" section
naming the exact `bin/hooks-daemon` verb; `health.md`, `regen-docs.md`,
`rule-explain.md`, `check.md`, `dev-handlers.md` now show the CLI form, and
`housekeeping.md` is new. `SkillCommand.RULE_EXPLAIN` became
`CliCommand.EXPLAIN_RULE`, so the terse block-message pointer prints
`bin/hooks-daemon explain-rule <ID>`.

Findings from the tests that guard the surface:

- `test_skill_scripts_are_referenced` demands `init-handlers.sh` be named
  AND invoked with arguments by some skill markdown; `dev-handlers.md` keeps
  a `bash .../init-handlers.sh "$@"` line for that reason rather than the
  script being deleted (Non-Goals forbid removing the capability).
- `test_cli_config_validate.TestSkillTextRoutesOnlyKnownVerbs` asserted
  `config-validate`/`validate-config` were ROUTED; rewritten to assert the
  skill DOCUMENTS both spellings and the parser accepts the alias.
- Deployed `.claude/skills/hooks-daemon/` is byte-copied from source;
  `diff -r` clean.

## 11:50 · handoff · P3 — Phase 3 complete; Phase 4 gate not started here

Release-notes callout `29-one-housekeeping-command-and-a-smaller-skill-surface.md`
written. Phases 2 and 4 are with the concurrent agent. Not done on purpose:
generated `.claude/HOOKS-DAEMON.md` / `CLAUDE.md` block (the advisory's
`get_claude_md` text changed) — regenerates on the next daemon restart in
the main checkout; no restart from this worktree.
