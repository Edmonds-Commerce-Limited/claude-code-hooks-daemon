# Plan 00458 — Journal 26-09-24

_Scaffolded by `mkplan.bash`; timestamps in this file are UTC._

> **Append-only activity log** for plan 00458. One file per day
> (`00458-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` UTC 24h (the date is in the filename, also UTC); times increase
>   down the file. A file with no sentinel line above predates this system —
>   its times are local, zone unrecorded (legacy; never rewritten).
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 11:13 · action · — — plan scaffolded

Plan 00458 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 11:14 · finding · — — how the real cause was found

00422 N20 was first filed as "the acceptance probes cannot pass in a
worktree whose daemon is running". It rested on the Plan 00456 agent's A/B
and on the coordinator's own run, which passed on main and failed in that
worktree. The contradiction came from `worktree-n18-n19`, which reported
35/35 QA with its daemon running. A direct run of the same two tests there
passed too (2 passed, 14.3s). So what distinguished the failing worktree
was not being a worktree. It was its name, `worktree-issue-53-venv`.

Searching the guards' skip lists for `venv` found `"venv/"` matched with
`skip_dir in file_path` in `qa_suppression.py:162` and
`comment_changelog.py:316`. Those are the two handlers behind the 14
failing probes. A wider search found four more sites with the same shape.
`strategies/lint/common.py`'s `matches_skip_path` docstring already
describes this failure ("`venv/` inside `myvenv/`"), so it was fixed once,
in one sibling.

Lesson: the first diagnosis was accepted because two runs agreed with it.
A third data point, which had been available the whole time, disproved
it.

Dedupe: no live plan touches handler skip lists. The two nearest are
00422 N11 (probe fixture location) and the lint fix that introduced
`matches_skip_path`. Recovery cron: `3c3c53c3`.

## 11:22 · finding · T1.1 — audit beyond the six named sites

Searched `src/claude_code_hooks_daemon` for `in file_path`/`in path` tests.
Confirmed the six named sites. Found six more `in file_path` sites and
classified each:

- `strategies/tdd/common.py:26` (`matches_directory`) — used by every
  per-language TDD strategy's `_SOURCE_DIRECTORIES`/`_SKIP_DIRECTORIES`
  (11 languages, ~22 call sites). Already both-side segment-bounded (it
  builds `/{directory}/` itself), so it does NOT have the substring-collision
  half of the defect — only the project-relativity half. Converting it
  touches far more call sites than this plan scoped, with no existing
  regression coverage for the venv-style collision there. Left as a
  documented follow-up, not fixed here.
- `handlers/post_tool_use/validate_eslint_on_write.py:328-331` (`is_worktree`,
  iterating `(WORKTREES_DIR, CLAUDE_WORKTREES_DIR)`) — same bare-`in` shape
  over a 2-tuple, but the result only feeds a log line
  ("Detected worktree file..."), never a skip/gating decision. No behavioural
  fail-open, so out of scope.
- `handlers/pre_tool_use/daemon_docs_guard.py:67`, `plan_qa_edit.py:117`,
  `plan_time_estimates.py:117`, `markdown_organization.py:902` — each tests
  ONE fixed, already both-side-slash-bounded pattern (e.g. `f"/{plan_dir_rel}/"`),
  not a directory LIST. Not the class this plan targets (Goals: "every
  skip-list and directory-classification site"); single-pattern containment
  checks are a narrower, different shape.

Decision: fix scope stays at the six named sites. The detector (T1.2) is
written broad enough to catch a NEW list-shaped site if one appears, but
does not retroactively demand fixing `matches_directory` or the single-
pattern sites above.

Also settled the matcher's project-root-resolution source: reuse
`utils.path_exclusion.resolve_project_root()` (best-effort, returns `None`
when `ProjectContext` is uninitialised — exactly the existing idiom
`handler_excludes_path` already uses for `daemon.exclude_paths`), rather
than inventing a second way to ask "what project is this". `project_root=None`
falls back to matching the raw `file_path` unchanged — preserves lint's
current behaviour and every existing unit test for the six handlers that
constructs them directly without initialising `ProjectContext`.

## 11:31 · action · T1.2/T1.3 — detector RED, shared matcher GREEN

Detector `scripts/qa/check_skip_list_substring.py`: an AST rule that flags a
comprehension's (or explicit `for`'s) own loop variable compared unchanged,
via bare `in`, against something whose identifier looks like a path
(`file_path`, `abs_path`, ...). Ran red on exactly the six named sites
(`d693e13c`), nothing else — confirmed the audit's site list was complete,
not just plausible. Registered in `llm_qa.py` and `run_all.sh` (step 30).

Shared matcher `src/claude_code_hooks_daemon/utils/path_segments.py::matches_path_segment`
(`25c4573e`): segment-bounded (lint's existing fix, moved here) plus
project-relative (`project_root=` resolves `file_path` against it first;
outside/unresolvable returns `False` — "not skipped", the fail-closed
default for a blocking guard per the team-lead spec). `strategies/lint/common.py::matches_skip_path`
is now a thin re-export (`project_root` omitted — unchanged behaviour, its
own 33 tests still green).

Moved all six sites onto it (`ebb31e61`): `qa_suppression.py`,
`comment_changelog.py`, `comment_size.py`, `british_english.py` (its
`CHECK_DIRECTORIES` entries carry no trailing slash at all, so patterns are
built as `f"{d}/"` at the call site), `security/common.py` and
`tdd/common.py` (both had LEADING-slash patterns, stripped: a leading slash
never lands at the start of a relative path's first segment).
`check_skip_list_substring.py` is green; 1045 tests pass across the touched
suites, including new per-site cases (`myvendor/`/`autodocs/` not matching
their real skip name, a genuine skip dir under the project root still
matching, a project living under an ancestor directory sharing the skip
name still guarded, and the literal 00422 N20 `...-venv` worktree
reproduction for `QaSuppressionHandler`, `CommentChangelogHandler` and
`CommentSizeHandler`).

Next: Task 1.4 — Security doc instance, release note, full QA.

## 11:32 · action · T1.4 — security doc instance, release note

Added an Instance to `CLAUDE/Security/AsymmetricSiblingProtection.md`
("The worktree that shared a suffix with a skip list"), naming
`check_skip_list_substring.py` as its Defence. Explicitly recorded as
having NO `declared-invariant-pairs` row: that registry's relations assert
agreement between two named symbols, and this class is a SHAPE (a bare `in`
test against a skip list) that can recur at a site with no sibling to
compare against — a standalone shape-detector covers a seventh future site
the way a `reaches` row over the six known ones would not.

Release note 08 (06/07 reserved for parallel work per the team-lead spec):
client-facing, states plainly that a client may see new blocks in
directories that were wrongly skipped before.

Daemon in this worktree restarted for final QA. Next: run
`llm_qa.py all` and fix anything red.

## 12:09 · action · handoff — Phase 1 complete, QA 36/36

First `llm_qa.py all` run (daemon restarted before the T1.4 commit) scored
34/36: `smoke_test` and 21 `tests/acceptance/*` failed on a stale
source-fingerprint mismatch (the daemon's loaded code predated the last
commit), not a real defect. Restarted the daemon again after all commits
landed and re-ran: `tests: 25584 passed, 0 failed, 24 skipped, coverage
95.1%`; `smoke_test: 3/3`; `QA: 36/36 PASSED`. Confirmed the acceptance
probes genuinely EXECUTED (not skipped) by running
`test_playbook_harness.py::TestTheDeclaredProbesBehaveAsDeclared` directly
-- 13.52s, PASSED -- which is the literal 00422 N20 regression test.

Report committed:
`subagent-reports/260924-plan458-impl-sonnet-5.md`. Phase 1 (Tasks 1.1-1.4)
done. Handing off to the team lead for Phase 2 (merge --no-ff, daemon
restart in the main checkout, mark 00422 N20 remedied).

## 12:13 · action · review feedback — every skip-list entry, not just venv

Team lead (independent review): the same bare-substring bug applies to
every entry of each list (`build/`, `dist/`, `vendor/`, `migrations/`,
`node_modules/`, ...), not only `venv/`, and per-site tests should prove
that rather than one example entry.

Added a parametrized `x<entry>` collision test (not skipped -- entry
present but not segment-bounded) and a "directly under root" positive test
for every entry of the list actually in play, across all six sites:
`PYTHON_QA_SUPPRESSION_SKIP_DIRECTORIES` (qa_suppression, 5 entries),
`DEFAULT_SKIP_DIRECTORIES` (comment_changelog + comment_size, 9 entries
each), `SKIP_PATTERNS` (security/common, 10 entries incl. the non-directory
`.env.example`), `COMMON_TEST_DIRECTORIES` (tdd/common, 4 entries) and
`CHECK_DIRECTORIES`'s default three (british_english). Imported each list
directly from its source module rather than hand-duplicating the entries,
so the tests stay in sync if a list changes. 336 tests pass across the six
touched files; detector still green.

## 12:33 · action · handoff — full QA re-run after the every-entry tests

Daemon restarted, full `llm_qa.py all` re-run:
`tests: 25664 passed, 0 failed, 24 skipped, coverage 95.1%`; `QA: 36/36
PASSED` (25664 vs the earlier 25584 -- the 80 new parametrized cases).
Report updated. Handing back to the team lead.

## 12:35 · action · owner follow-up — matches_directory routed through the matcher

Team lead: merged to main; "no half measures" -- `strategies/tdd/common.py`
`matches_directory` (11 per-language TDD strategies'
`_SOURCE_DIRECTORIES`/`_SKIP_DIRECTORIES` all share it) was left absolute
rather than project-relative, so the team-lead's own example
(`/srv/vendor/app/`) had every file misclassified. Merged `main` into this
branch first (clean, no conflicts -- `b8ce4b49` onward, 00459/00460 plan
folders picked up). Daemon restarted.

Fix: `matches_directory` now strips any leading `/` from each pattern
(never lands at the start of a relative path otherwise) and delegates to
`matches_path_segment(file_path, patterns, project_root=resolve_project_root())`
-- same call shape as `is_in_common_test_directory` two lines above it.
Public signature unchanged, so none of the 22 per-language call sites
needed touching. 338 tests in `tests/unit/strategies/tdd/` and 194 in
`tests/unit/handlers/test_tdd_enforcement.py` pass unchanged (project_root
resolves to `None` in those tests -- same absolute-path fallback behaviour
as before).

Four new tests reproduce the owner's exact scenario both directions (skip
classification AND source classification): a real skip/source dir inside
the project still matches; a project living under an ANCESTOR directory
sharing the pattern's name (`/srv/vendor/app/`, `/home/u/src/proj/`) is
no longer misclassified.

Detector question: `check_skip_list_substring.py` was never meant to see
this site and correctly did not flag it before the fix -- `matches_directory`
was already segment-bounded (it built `/{directory}/` itself before its own
`in` test), so it carried only the project-relativity half of the defect,
not the bare-substring-collision shape the detector targets. Extending the
detector to catch "a directory classifier used the absolute path" would be
a different, harder rule (data-flow: was `project_root` threaded through a
given call), not an AST shape -- out of scope here and left unextended.
Still green post-fix (nothing left to see: the `in` test is gone).

## 12:54 · action · handoff — full QA after the matches_directory fix

Committed (`fae188fa`), daemon restarted, full `llm_qa.py all` in the
foreground: `tests: 25668 passed, 0 failed, 24 skipped, coverage 95.1%`;
`QA: 36/36 PASSED`. Report updated. Handing back to the team lead.
