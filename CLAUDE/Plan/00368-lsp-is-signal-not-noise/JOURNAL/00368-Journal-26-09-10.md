# Plan 00368 — Journal 26-09-10

> **Append-only activity log** for plan 00368. One file per day
> (`00368-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 08:20 · finding · P1 — what the diagnostics stream was made of

`pyright --project /workspace --outputjson` before the exclude: 3,011
errors across the tree. 2,056 of them were under `untracked/` (two live
worktrees, the dummy-client fixture, venvs), `CLAUDE/Plan/` (archived
probes and code) and `CLAUDE/AcceptanceTests/fixtures/`. The remaining 955
are real project files, 900+ of them in `tests/`, dominated by
`reportOperatorIssue` (334) and `reportAttributeAccessIssue` (185): Optional
results used without narrowing, and attribute access on `Handler`-typed
fixtures. mypy passes on all of it, which is why nothing had ever counted
them. The language server that had been killed for the config change
respawned before the new file was written and kept the old view; killed
again after the write. No recovery cron: the supervisor's goal covers
resumption and the session already declined one for 00172.

## 08:22 · action · — — plan scaffolded

Plan 00368 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 09:05 · finding · T3.1 — baseline for the `tests/unit/handlers/**` partition

`pyright --project . --pythonpath <worktree venv python> --outputjson` in
worktree `worktree-plan-00368c`. The `--pythonpath` override is needed
because this worktree has no `untracked/venv` symlink (the venv is the
fingerprint-keyed directory only), so `venvPath`/`venv` in
`pyrightconfig.json` do not resolve and `pytest` shows as 127 missing
imports; that is a provisioning gap, not a code defect, and the config is
untouched. Whole tree: 938 errors. This partition: **433 errors in 50
files**. By rule:

| rule                         | count |
| ---------------------------- | ----- |
| reportOperatorIssue          | 306   |
| reportOptionalMemberAccess   | 67    |
| reportAttributeAccessIssue   | 33    |
| reportPossiblyUnboundVariable| 8     |
| reportGeneralTypeIssues      | 8     |
| reportArgumentType           | 7     |
| reportInvalidTypeForm        | 2     |
| reportReturnType             | 2     |

Per-file listing with line numbers saved to
`untracked/scratch/pyright-handlers.txt`. Heaviest: `test_npm_command.py`
(55), `test_pipe_blocker_comprehensive.py` (50),
`test_markdown_organization.py` (30). Shapes: `result.reason` (a
`str | None`) used with `in`/`.lower()`/`.startswith()` without narrowing
(~380); `next(..., None)` results accessed as concrete handler subclasses
while typed `Handler`; `handler.handler_id.config_key` on a
`str | HandlerIDMeta`; `mock: any` (the builtin) as a type annotation;
`result` assigned only inside a `for` loop. Fix plan: explicit
`assert x is not None` / `isinstance` narrowing, `MagicMock` annotations,
loop results initialised before the loop. Six parallel sub-agents, one
file group each, with Edit only.
## 09:09 · action · T2.2 — lsp_noise_checker handler finished, in this worktree

Picked up Task 2.2 already in progress (uncommitted) in worktree
`worktree-plan-00368a`: `LspNoiseCheckerHandler` (SessionStart, advisory,
Python-only relevance), rule IDs `R-LSP-CONFIG-EXCLUDE` /
`R-LSP-SERVER-STALE`, 29 unit tests, `get_claude_md()`,
`get_acceptance_tests()`, handler-registry wiring (`constants/handlers.py`,
`constants/priority.py` at 69 widening `ADVISORY_MAX`, `constants/rule_ids.py`,
`session_start/__init__.py`), `.claude/hooks-daemon.yaml`(.example) enablement,
`docs/guides/HANDLER_REFERENCE.md`, `CLAUDE/HANDLER_DEVELOPMENT.md` and
`docs/guides/CONFIGURATION.md` priority-table rows, `pyrightconfig.json` +
`tests/unit/test_pyright_config.py` gaining the full `CORE_VENDORED_BUILD_DIR_NAMES`
set (so this repo's own config satisfies its own new handler), the
config-changes manifest (`v3.64.0.yaml`) and release-notes callout 19. Found
and fixed one drift the previous pass missed: `CONTRIBUTING.md`'s
`ssot-quote` copy of the priority-range table still read `56-66` against
`HANDLER_DEVELOPMENT.md`'s updated `56-69`. Removed a stray untracked
`.claude/reports/v2.2.0-to-v2.15.2/ADVISORY.md` (an old, unrelated
config-migration scratch artefact, confirmed unrelated before deleting).
29/29 new unit tests pass; `pyright` reports zero errors in every file this
task touched (938 pre-existing errors remain elsewhere, Phase 3's to fix).
`./scripts/qa/llm_qa.py all` running to confirm the rest of the suite.

## 09:15 · finding · — — first `llm_qa.py all` run: two real gaps, one self-inflicted

First full run (while the daemon was mid-restart, self-inflicted -
restarting concurrently with a live acceptance-test run broke 13 socket-
dependent tests; re-ran clean afterwards) surfaced two genuine gaps
unrelated to that noise: `lsp_noise_checker.py`'s `_get_project_root`
needed a `scripts/qa/error_hiding_exclusions.json` entry (identical
best-effort-None-on-RuntimeError contract as
`hook_registration_checker._get_project_root`, Plan 00142's precedent) and
`LspNoiseCheckerHandler` needed a `_EARNS_GUIDANCE` entry in
`tests/integration/test_claude_md_guidance_coverage.py` (T3: the
fix-the-noise-source principle is a standing rule for the rest of the
session, not just the exclude gap this check happens to catch at start).
Both fixed. A pre-existing, unrelated repo-hygiene drift also surfaced:
`CLAUDE/Plan/README.md` is one completed row over its 30-row retention
window (`test_completed_rows_stay_within_the_retention_window`) - not
caused by this branch, left alone, flagged in the final report.

## 09:20 · decision · T2.2 — owner ruling: every supported language, not Python alone

Team lead relayed the owner's correction mid-task: `lsp_noise_checker`
must not be Python-only - "LSP should be signal, not noise" applies to
every language server Claude Code runs. Restructured as a Strategy
Pattern registry (`strategies/lsp_noise/`, mirroring the `tdd` archetype
exactly: `protocol.py`, `common.py`, one `{language}_strategy.py`, a
`registry.py` with `create_default()`) behind a handler reduced to a thin
orchestrator with zero language logic. A follow-up correction: PHP must be
a REAL strategy, researched rather than assumed, and if intelephense
genuinely has no per-project knob the advisory must print the exact
settings snippet, never say "unsupported".

Research (no WebFetch tool available to this subagent; used `curl` via
Bash against public docs and GitHub directly instead, everything captured
to `untracked/scratch/` before being deleted as scratch): fetched
`code.claude.com/docs/en/discover-plugins.md` for the official
language→plugin→binary table, then the ground truth -
`anthropics/claude-plugins-official`'s `.claude-plugin/marketplace.json` on
GitHub - which shows EVERY official LSP plugin (`pyright-lsp`,
`typescript-lsp`, `gopls-lsp`, `rust-analyzer-lsp`, `php-lsp`) ships with NO
`settings`/`initializationOptions` at all. That single fact splits the five
languages into three real shapes: Python/TypeScript read their own project
file regardless of who launches the server (pyright/tsserver always have);
Go/Rust have no exclude list at all, so the check becomes "does a
non-project tree hold this language's source files" (Rust's fix routes
through `Cargo.toml`'s `[workspace] exclude`, a real editable knob; Go's
routes through a nested `go.mod` module boundary); PHP's intelephense
(confirmed via `bmewburn/intelephense-docs`, not phpactor - the owner's
placeholder guess) reads NO project file whatsoever, so the only real fix
is a project-scope LSP plugin re-registering `.php` with its own
`settings` (`plugins-reference.md`'s "first server registered wins the
extension" rule), which the strategy now scans for under
`.claude/plugins/*/` and, when absent, prints in full.

Implementation: 5 strategies + `common.py` + `protocol.py` + `registry.py`,
TDD throughout (test file before source for every new module, including
bare `__init__.py` - the daemon's own `tdd_enforcement` does not exempt
dunder files). Moved two structurally-identical private view dataclasses
(`_ConfigView` used by Python and TypeScript, `_OverrideView` used by PHP)
into `common.py` as `ConfigView`/`OverrideView` after
`qa/strategy_pattern_checker.py` flagged them: it scans every class
defined in a `*_strategy.py` file for `get_acceptance_tests()`, so a
private helper dataclass living beside the real strategy class read as a
second, non-compliant "strategy". Handler rewritten as pure orchestration:
scans the process table ONCE per session (not once per language) via a
`known_names` union, then filters per strategy for staleness. Rewrote the
handler's test file around fake strategies for orchestration coverage,
independent of any one language's file-reading specifics (which stay
covered in each strategy's own test file). PLAN.md, config-changes
manifest, release-notes callout 19, HANDLER_REFERENCE.md and
`constants/handlers.py`/`init_config.py`/`.claude/hooks-daemon.yaml`(.example)
comments all updated from "Python projects only" to the five-language
table. 137/137 lsp_noise + handler tests pass; mypy and pyright both clean
on every touched file; `strategy_pattern_checker` clean except two
pre-existing, unrelated violations in `comment_size.py`/
`staged_lint_gate.py` (Plan 00347, untouched by this branch).

## 09:52 · finding · — — full QA surfaced 3 real, narrow issues after the restructure; all fixed

Two `tests/unit/strategies/lsp_noise/` files (`_ConfigView`/`_OverrideView`)
tripped `strategy_pattern_checker`'s "every class in a `*_strategy.py` file
needs `get_acceptance_tests()`" scan — moved both into `common.py` as
`ConfigView`/`OverrideView` (they were structurally near-identical
private dataclasses anyway). A whole-suite pytest run then collapsed to 9
collected tests total: `tests/unit/strategies/lsp_noise/` and
`tests/unit/strategies/tdd/` share filenames
(`test_common.py`/`test_protocol.py`/`test_registry.py`/
`test_acceptance_tests.py`/`test_python_strategy.py`/`test_go_strategy.py`/
`test_php_strategy.py`) and neither had `__init__.py`, so pytest's default
import mode collided on the bare module names — every OTHER sibling
strategy-domain test dir (`lint`, `security`, `comments`, `error_hiding`,
`pipe_blocker`, `qa_suppression`) already carries an `__init__.py` for
exactly this reason; `tdd/` is the sole legacy exception. Added
`tests/unit/strategies/lsp_noise/__init__.py`, matching the majority
convention. `php_strategy._load_json_object`'s tolerant-parse pattern
(same shape as `blockage_marker.read_marker`) needed its own
`error_hiding_exclusions.json` entry. Last: a real bug, not tooling —
`get_claude_md()`'s prose read "a module/workspace boundary", and the
literal substring `/workspace` (this repo's own self-install root) inside
that hyphen-free compound tripped
`test_guidance_does_not_hardcode_the_dogfood_root` for real, since a
client install has no such directory. Reworded to "a module or workspace
boundary" throughout (three sites, one split across a string-literal line
break the first `replace_all` pass missed). Re-ran targeted suites clean;
full `./scripts/qa/llm_qa.py all` kicked off once more (daemon left
running throughout, not restarted mid-run) to confirm before commit.
## 10:00 · handoff · — — shutdown: state of every in-flight branch (00291, 00367, 00368)

Laptop shutdown; all agents stopped. Every branch is pushed; the last two
were WIP-committed by the coordinator without QA. Resume by re-dispatching
one agent per branch from these heads:

- `worktree-plan-00291` @ 4b05307a: Task 4.2 done, QA 26/26 on its own base,
  but it conflicts with main in scripts/upgrade.sh, daemon/cli.py,
  install/config_cli.py, config_migrations.py, truth_changes.py (the
  `include_unreleased` parameter). Next: merge origin/main into the branch,
  resolve keeping both sides, QA, then the coordinator archives on main in
  one commit (git mv + README row/stats + Status Complete).
- `worktree-plan-00367` @ 41391a66 (WIP, un-QA'd): Phase 4 merge gate
  (`worktree.merge_to_main_requires_human_approval`, `approve-merge`,
  handler `merge_to_main_approval`) mid-build; the 11 Worktree.core.md
  rewrites and callout 18 extension not yet verified. The sweep-fix for the
  unreadable-file `continue` is already on main (e19340a2).
- `worktree-plan-00368a` @ 3e76073a (WIP, un-QA'd): Task 2.2 restructured
  as a per-language strategy registry under
  `strategies/lsp_noise/` (python, typescript, go, rust, php); registry
  wiring, rule IDs, HandlerID/Priority constants, docs, callout 19 and the
  handler tests were still being reconciled when stopped.
- `worktree-plan-00368b` @ 8d7aeb82: partition A at 0 pyright errors except
  one deliberate case in `core/result_types.py` (its docstring rejects the
  change pyright wants); full QA was running when stopped. Includes the
  `register_all` config-type fix request.
- `worktree-plan-00368c` @ 0cd03543: partition B done, 433 → 0, report
  written.

Merge order once green: 00291 → 00367 → 00368a → 00368b → 00368c (expect
conflicts between the two sweep branches and 00368a only in
`pyrightconfig.json`/`test_pyright_config.py`). Then full QA 26/26, daemon
restart, and the human re-invokes `/release`.

## 10:03 · handoff · — — correction: 00291 is merged into main

The 00291 agent had already merged origin/main into its branch (35e96847,
QA 26/26) before the shutdown note above was written; the conflict list
there is stale. The branch is now merged into main (f0896ced). Remaining
for 00291: the atomic archive on main only — git mv to Completed/, README
row + stats, Status Complete, one commit. Merge order for the rest is
unchanged: 00367 → 00368a → 00368b → 00368c.
## 10:05 · handoff · T3.1 — partition A shutdown handoff

Machine shutdown imminent; stopping at the nearest safe point per team
lead's instruction, WIP-committing and pushing without running QA.

Partition A (everything except tests/unit/handlers): 0 pyright errors
under this partition, with one documented exception left in place --
`src/claude_code_hooks_daemon/core/result_types.py`'s two
`reportIncompatibleVariableOverride` errors, which that file's own
docstring explicitly anticipates and instructs NOT to fix by widening the
field (would undo the type-safety guarantee the three `HookResult` tiers
exist to provide). Flagged for the team lead as a policy decision, not a
defect; see the batch-12 commit message on `worktree-plan-00368b` for the
full reasoning.

14 commits landed on `worktree-plan-00368b` covering ~140 pyright errors
fixed across roughly 45 test files plus one source fix
(`src/claude_code_hooks_daemon/utils/hook_registration.py`: `dict` ->
`Mapping` for read-only params). All batches verified with pyright + ruff
+ black + pytest (or `bin/hooks-daemon test-project-handlers` for
`.claude/project-handlers/` files) before committing.

New at hand-off: the team lead (relaying partition B) flagged a further
src typing gap in my partition -- `register_all` in
`src/claude_code_hooks_daemon/handlers/registry.py` declares
`config: dict[str, dict[str, dict[str, Any]]]` but its body also reads
event-level `enable_tags`/`disable_tags` list/str values off that dict,
so the declared type is dishonest. NOT started -- give it a proper type
(TypedDict or `dict[str, dict[str, Any]]`) as the next unit of work.
Partition B already has a test-side workaround (`dict[str, Any]` fixture
annotations) that can stay as-is once the src type is fixed.

`./scripts/qa/llm_qa.py all` was launched in the background before the
shutdown notice arrived and had not finished when this entry was written
-- its result is unknown; the next agent should re-run it after picking
up this partition.

Remaining pyright errors outside my partition (`tests/unit/handlers/`,
partition B) are not mine to fix. `WIP handoff before shutdown` commit
follows this entry.

## 10:40 · handoff · T3.1 — partition B closed out: 433 -> 0

Resumed in worktree `worktree-plan-00368c` with 49 of 50 files already
fixed (uncommitted, from the prior agent's parallel sub-agents) via
`getattr(..., None)` + `assert x is not None` narrowing. Baseline re-run
(`pyright --project . --pythonpath <worktree venv python>`) found only
`test_registry.py` still failing: 3x `reportArgumentType` on
`register_all(config=...)` calls, unrelated to the Optional-narrowing
pattern. Cause: `register_all`'s declared parameter type
(`dict[str, dict[str, dict[str, Any]]] | None`, in
`src/claude_code_hooks_daemon/handlers/registry.py`) models every
per-event entry as a handler-name-keyed nested dict, but the runtime
code also reads event-level `enable_tags`/`disable_tags` keys straight
off that same dict as a plain `list[str]`/`str` -- a shape the type
doesn't capture. Not a `src/` fix (partition A's territory, and the
type is still technically correct for the handler-config case); fixed
test-side by annotating the three local `config` fixtures
`dict[str, Any]`, which is bidirectionally compatible with any nested
value shape and needed no `cast`/`type: ignore`.

Verified: `pyright --project . --pythonpath <venv>` -> 0 errors under
`tests/unit/handlers/`. `pytest tests/unit/handlers -q` -> 6256 passed,
1 skipped, 1 xfailed (expected, Plan 00260 Task 3.1 marker). Committed
`7be5d9ee` (all 51 files: 49 prior-agent + `test_registry.py`), pushed
to `origin/worktree-plan-00368c`.

`./scripts/qa/llm_qa.py all` (whole repo, not partition-scoped): 24/26
PASSED. The 2 failures are both pre-existing on `main`, from Plan 00367
(`d4791899`/`68c1510e`), and outside `tests/unit/handlers/`: an
`error_hiding` `silent-continue` in
`src/claude_code_hooks_daemon/docs_qa/checks/unenforced_approval_gate.py`,
and 12 failing/errored tests in `tests/acceptance/`,
`tests/integration/test_plan_index_navigability.py` and
`tests/unit/qa/test_audit_error_hiding.py` (none under my partition).
`type_check` (mypy) passed with 0 errors -- the whole-repo pyright gate
itself isn't wired into `llm_qa.py` yet, so this run doesn't surface
partition A's remaining errors either; that will show once Plan 00368's
pyright-gate task lands. Partition B (`tests/unit/handlers/**`) is
done: 0 pyright errors, full green pytest, nothing left for this
partition to fix.
## 12:10 · handoff · T3.1 — partition A closed out; merged origin/main first

Resumed from `8d7aeb82`. `git fetch origin && git merge origin/main`
conflicted only in this journal day-file (the two shutdown-note branches
interleaving); resolved by putting all three handoff entries back in
chronological order (10:00, 10:03, this file's own 10:05), nothing
dropped. Merge commit `75e3102d`.

Full `pyright --project . --outputjson` afterward: 432 errors, all either
under `tests/unit/handlers/` (partition B) or two new-to-me gaps in my own
partition that the batch-14 "0 errors" claim had missed:

- `src/claude_code_hooks_daemon/strategies/lint/__init__.py`
  (`reportUnsupportedDunderAll`): `LintStrategyRegistry` is exported in
  `__all__` but only reachable via the lazy `__getattr__` (deliberate,
  avoids a circular import, pinned by
  `tests/unit/strategies/lint/test_init_lazy_import.py`). Fixed with a
  `TYPE_CHECKING`-only import of the same name — invisible at runtime, so
  the lazy path and its tests are untouched, and pyright now sees the
  `__all__` entry as bound.
- `tests/unit/install/test_agent_assets.py::test_outdated`
  (`reportAssertAlwaysTrue`): a dead `historic = spec.historic_versions[0]`
  variable (always-truthy 2-tuple type) asserted for no reason — the
  property it gestured at is already `test_dedupe_agent_carries_
  historic_versions`'s whole job. Removed the dead variable and the
  tautological assert.

Also did the `register_all` config-type fix flagged at hand-off:
`config: dict[str, dict[str, dict[str, Any]]]` → `Mapping[str,
Mapping[str, Any]]` — the innermost `dict[str, Any]` was dishonest because
the body also reads `enable_tags`/`disable_tags` (list/str) off the
event-level dict, exactly like `tag_skip_reason`/`handler_is_enabled` in
the same file already type it. Read-only parameter (only `.get()`'d), so
`Mapping` costs nothing.

**`result_types.py`'s two documented `reportIncompatibleVariableOverride`
errors**: did not just trust the docstring's "don't widen" warning —
tried the one typed alternative that looked plausible, `Final[Literal[
...]]` on the narrowed override (pyright exempts `Final` attributes from
its invariance check). Reproduced in a throwaway pydantic 2.13 model
matching this file's shape and it is actively worse than doing nothing:
Pydantic v2 treats a `Final`-annotated field carrying a default as a
**class variable, not a model field** — `Narrow(decision=Decision.DENY)`
silently keeps the class default (constructor argument dropped on the
floor), and direct mutation raises `AttributeError: ... is a ClassVar ...
cannot be set on an instance`, which is exactly the write path
`merge_pseudo_results` depends on. Confirmed via a real interpreter run,
not just pyright. So this stays the one documented exception to Task 3.1's
"every pyright error" — a pyright/mypy variance disagreement on a
deliberately covariant field, with the only pyright-silencing workaround
proven (not assumed) to break the feature at runtime.

QA: first `./scripts/qa/llm_qa.py all` came back 25/26 — the only failure
was 10 acceptance tests (`test_playbook_harness.py`,
`test_stop_hook_hard_block.py`, `test_tool_use_error_recovery.py`)
erroring at setup because this worktree's own daemon had no live socket
yet; all 10 pass individually, confirming it was the missing daemon, not a
regression. `bin/hooks-daemon restart` (confirmed RUNNING, PID 178212),
reran full QA: **26/26 PASSED**, 21,747 tests, 0 failed, 95.4% coverage.
Daemon still RUNNING (same PID) afterward.

Full report: `subagent-reports/sweep-a-report.md`. Partition A (Task 3.1
minus `tests/unit/handlers/`) is done; ready to merge once partition B
lands.

## 12:17 · action · T2.2 — resumed from the 3e76073a handoff: two merges, QA verified green

Resumed this branch from the WIP handoff (`3e76073a`). Task 2.2's own
code was already complete and correct as left: all five strategy files,
`common.py`/`protocol.py`/`registry.py`, the handler, wiring
(`constants/handlers.py`/`priority.py`/`rule_ids.py`,
`session_start/__init__.py`), `.claude/hooks-daemon.yaml`(.example),
`HANDLER_REFERENCE.md`'s five-language mechanism table, the `56-69`
priority range in sync across `HANDLER_DEVELOPMENT.md`/`CONTRIBUTING.md`,
`explain-rule` for both `R-LSP-CONFIG-EXCLUDE`/`R-LSP-SERVER-STALE`,
callout 19 and the config-changes manifest all checked by direct
inspection and matched the journal's own account — none of the "last seen
diagnostics" (missing constants, undefined `_ConfigView`, unresolved
imports) reported to this agent at dispatch were still true; that report
predated the 09:20-09:52 restructure and fix-up already recorded above.

Two sequential `git merge origin/main`s were needed, not one: origin/main
advanced a second time (Plan 00367 archived, Plan 00291 archived, Plans
00369/00370 filed) while this session was mid-review, discovered by a
second `git fetch` after the first merge showed an unexpectedly-populated
`.claude/reports/v2.2.0-to-v2.15.2/ADVISORY.md` that the 09:09 entry above
says was deleted as stray scratch — it turned out to be genuinely tracked
on main (a real, unrelated config-migration advisory file), not scratch;
left in place, uninvestigated further as out of scope. First merge
(journal-only conflict, resolved by chronological concatenation). Second
merge conflicted on `CLAUDE.md` (auto-generated; resolved by taking
origin/main's tracked copy wholesale, then `bin/hooks-daemon
regenerate-docs` rebuilt it correctly from this branch's actual handler
set, fixing a stale `.claude/HOOKS-DAEMON.md` summary line that had
carried the pre-restructure "Python project's pyright config" wording)
and on `CLAUDE/UPGRADES/UNRELEASED/config-changes/v3.64.0.yaml` (add/add;
resolved by keeping all five `added:` entries — this branch's
`lsp_noise_checker` plus 00367's four plan-close/merge-approval keys —
under one list, per the dispatch instruction).

QA: three full `./scripts/qa/llm_qa.py all` runs. The first two, run while
three to five sibling worktrees (00367, 00368b, 00369, 00370, and the main
checkout) were concurrently running their own full QA suites and daemon
restarts on this shared host, each showed 25/27 with a different,
non-reproducing set of acceptance/integration-test failures (daemon-socket
timing) alongside the expected pyright pre-existing-error failure; every
one of those tests passed cleanly in isolation and a standalone
`llm_qa.py tests` run was 0 failed. The third, after the second merge,
came back 26/27 clean: pyright is the sole expected failure (942 errors,
up from 938 — the second merge brought in a few more pre-existing files;
Phase 3's job on other branches), confirmed zero pyright errors in every
lsp_noise file both before and after. mypy also clean on all touched
files. 387 targeted tests (lsp_noise strategies + handler + the
guidance-coverage integration test) pass. Daemon restarted and confirmed
RUNNING after each merge. Both merge commits and the regenerate-docs
commit pushed to `origin/worktree-plan-00368a`.
