# Plan 00414 — Journal 26-09-24

_Scaffolded by `mkplan.bash`; timestamps in this file are UTC._

> **Append-only activity log** for plan 00414. One file per day
> (`00414-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM, ONLY with
> `mkplan.bash --journal <plan-number> <category> <body-file>` (it stamps the
> real UTC time; a hand-written entry is denied); NEVER edit earlier entries
> (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` UTC 24h (the date is in the filename, also UTC); times increase
>   down the file. A file with no sentinel line above predates this system —
>   its times are local, zone unrecorded (legacy; never rewritten).
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 18:44 · action · —   — declared-absent protected path reported once, live-verified

Built on branch worktree-d-fresh under the unattended rulings recorded in PLAN.md.

Live reproduction: this worktree's config names `secret_word_list_path` explicitly, and `secret-meta` reports the file absent, so `sensitive_content`'s word-list source is inert here (the `sensitive_content` QA gate also reports 0 terms). Before the change the SessionStart hygiene advisory said nothing about it.

Change: `secret_file_hygiene_checker` now reports a protected path the config names explicitly (the `sensitive_content` word-list option, or a path-shaped literal in `secret_file_guard`'s `protected_paths`, with that handler enabled) that `stat()` cannot find. A dangling symlink counts as absent. The report names the guard left inert. It is told once, keyed by a sha256 of the findings in the daemon's untracked dir (`secret_file_absence_cache.json`), the `gitignore_safety_checker` cache shape. A glob or a bare file name is a pattern, not a declared path, and is never reported.

Live: two SessionStart probes through the worktree's hook script. The first carried the ABSENT section naming the word list and "INERT until it exists"; the second was silent.

Defence Before Fix: no practical code-reading detector exists for "a guard is inert with no report". It is a runtime property of which files exist in a checkout, not a code shape. The detector is this handler itself, which reads config and disk. The toolchain gap is recorded in the delivery report.

Shared-file note: `secret_file_guard` and `utils/secret_file_matching.py` are untouched. The hygiene checker only calls `resolve_configured_patterns`/`path_is_protected` read-only. Three error-hiding inventory entries were added for the handler's best-effort cache and config reads, modelled on the `gitignore_safety_checker` entries.
