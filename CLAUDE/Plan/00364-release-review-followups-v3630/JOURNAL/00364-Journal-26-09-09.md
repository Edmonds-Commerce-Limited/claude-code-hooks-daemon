# Plan 00364 — Journal 26-09-09

> **Append-only activity log** for plan 00364. One file per day
> (`00364-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 13:54 · action · — — plan scaffolded

Plan 00364 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 13:58 · decision · — — every non-blocking v3.63.0 review finding, in one place

The release shipped with all blockers fixed and the non-blocking findings
sitting only in `untracked/agent-reports/`. The owner's rule is that no known
defect is left unfiled, so this plan collects them. The four review reports
are imported verbatim into `review-reports/` so the plan folder is
self-contained after `untracked/` is gone.

Dedupe scout checked 17 live plans: no overlap. Plan 00291 touches upgrade
hardening but none of these items.

Triage against the tree before filing: every listed item confirmed still
open (`_git_lines` returns `[]` on failure, `staged_changes()` unmemoised,
`ord(end)` range materialised before the cap, kotlin docstring still says
`-script`, `cron_cadence` still hand-rolls a uuid name, 76 `str(scratch_path`
wrappers, echd-capture "Tee" comment, SKILL.md description missing
`optimise`, `dev-handlers.md` literal `"$@"`, `venv_lock` bare `stat()`).

Priority-20 collision: `plan_done_requires_holding_area` (project handler,
PreToolUse, priority 20) shares the slot with built-ins `GIT_BRANCH`,
`GIT_STASH`, `GIT_MESSAGE_BACKTICK` and `NITPICK_HEDGING`; the controller
warns and registers anyway.

Failsafe recovery cron for this session: `8cc209e9` (hourly at :23).

Execution: one worktree agent per phase, Phases 1-4 in parallel, Phase 5
after (it touches the deployed hook wrappers, which the others' acceptance
runs depend on).

## 14:12 · action · P1 — Phase 1 implemented, all six tasks, TDD throughout

Worktree `worktree-plan-00364-p1-core`, branch of the same name. Every task
started from a failing test.

- **T1.1** `venv_lock._mkdir_lock` now catches `FileNotFoundError` around the
  stale-age `stat()` and retakes the lock: the holder's `rmtree` landing
  between our `mkdir` and our `stat` means the lock is FREE, not that
  anything is broken. The retry is bounded by the same deadline, and only
  the first one is immediate, so a lock NAME that can never be stat'ed (a
  dangling symlink) ends in `VenvLockTimeout` rather than a hot spin — the
  second new test builds exactly that. `cmd_repair`'s "'uv' not found"
  message moved down to the `uv` spawn inside `_repair_venv_locked`, which
  is the only place that can mean it; the post-sync verification spawn got
  its own message naming the venv interpreter; the outer handler now reports
  whatever path was actually missing.
- **T1.2** `_git_lines` returns `list[str] | None`, and a small `_GitReads`
  collector records each failed read. `SlateReport` gained
  `undetermined_reason`; `is_clean` is False whenever it is set, `render()`
  prints `Slate: UNDETERMINED — …`, and `cmd_release_slate_check` returns
  `RELEASE_SLATE_UNDETERMINED` for it — checked BEFORE `--accept`, so
  acknowledging WIP still does not acknowledge blindness. JSON gained the
  same field. `RELEASING.md`'s exit-1 row now names a failing git listing.
- **T1.3** `config_skip_reason` / `tag_skip_reason` / `handler_is_enabled`
  extracted into `handlers/registry.py`. `register_all` calls the first two
  at the two points it already gated (they split there because gates 1-2
  precede construction and 3-4 need the instance's tags); the checklist
  calls the composite. `build_checklist` gained `disabled_handlers`, the
  fourth gate. Pinned by a cross-check test that registers handlers for a
  config and asserts the report agrees with registration, handler by handler.
- **T1.4** `_probe()` wraps construction, `get_default_enabled` and
  `get_relevance` together. A failure becomes a `ChecklistItem` with
  `construction_error` and the new `ItemStatus.UNASSESSED`, rendered under
  "could not be assessed" and counted separately in the JSON summary.
- **T1.5** `_LANGUAGE_MARKERS` keys are `HandlerTag` members;
  `_AGENT_BEHAVIOUR_EVENTS` / `_SESSION_ENV_EVENTS` are built from
  `EventID.*.config_key` and `pseudo_events.registry.NITPICK`.
- **T1.6** the `hook_input` + `dispatch_as_bash` pair is refused BEFORE
  `_derive_bash_payload()` runs, so the message names both instead of
  `tool_payload`, which the derivation had just populated.

## 14:20 · finding · P1 — two QA audits scan NOTHING from a worktree under `untracked/`

Not a Phase 1 regression, and worth a plan of its own. Both audit scripts
exclude any path containing `untracked`, and this worktree's absolute path is
`/workspace/untracked/worktrees/…`, so every file in it is excluded:

```
scripts/qa/audit_capture_corruption.py:  _EXCLUDE_DIR_PARTS = {"untracked", ...}
                                         _is_excluded -> any(part in ... for part in path.parts)
scripts/qa/audit_error_hiding.py:        _DEFAULT_EXCLUDE_PATTERNS = ("untracked/", ...)
                                         any(pattern in str(py_file) ...)
```

Takeaway: `test_default_scan_dirs_clean` fails its `len(files) > 0` guard,
and every one of the 127 live error-hiding exclusions reads as stale because
zero violations are collected to match them against. Both fail identically
with no Phase 1 change applied. The capture-corruption script at least prints
"no shell files found" on its CLI path; the audit consumed by the test does
not. Worth filing: a worktree is the project's own supported layout
(`WORKTREE_DIR_PATTERNS` names `untracked/worktrees/`), so the audits should
exclude the repo-relative segment, not any occurrence in the absolute path.

The third apparent failure, `test_sed_blocker` heredoc-markdown, is a
declared `xfail` (Plan 00260 Task 3.1) that only reads as F in a summary
grep. `tests/integration/test_forwarder_socket_stdin.py` fails because this
worktree's deployed forwarder dials the MAIN repo's daemon — which is Task
5.1 of this very plan.
## 15:05 · action · T4.1 — the staged-content bounds now cap the peak, not just the scan

`_staged_content_haystacks` asks git two questions instead of one.
`--numstat -z` first (one added-line count per path, no content at all), then
a patch call restricted to the paths that survived selection. A binary blob,
an excluded path, the secret list itself, a file the per-file bound already
excludes and everything past the whole-commit budget are all dropped before
git is asked for a byte of them.

Both stand-down log lines keep their wording, and each file is logged at most
once — a file dropped by the sizing pass never reaches the exact byte check.
The whole-commit stand-down fires once and names the first path left
unjudged, whichever pass stopped the walk; `_StagedSelection.stood_down_at`
carries the sizing pass's stop point into the scan so the two cannot each
name a different file for one truncation.

Two things the fix needed that the finding did not mention. Pathspec
chunking (`_MAX_PATHSPEC_ARGV_BYTES`, 64 KiB): a commit staging thousands of
files would build an argv past `ARG_MAX`, `run_git` reports the `OSError` as
a non-zero return code, and this handler reads that as "no staged diff" —
the change would have stood the guard down silently on exactly the large
commit it most needs to judge. And an unrestricted fallback for a path that
cannot round-trip: `run_git` decodes with `errors="replace"`, so a name
carrying a byte that is not valid UTF-8 comes back bearing U+FFFD and would
match no pathspec, so such a commit asks the broad question and every staged
path is still judged.

## 15:06 · thought · T4.1 — what the numstat bound does NOT bound, stated up front

`--numstat` counts LINES; the constants are BYTES. An added line costs at
least its own newline, so the count is a sound LOWER bound: a file whose line
count alone passes the byte bound cannot fit and is dropped unread. The
converse does not hold, so a file with few but very long lines (a minified
bundle on one line) still gets fetched before the exact byte count rejects
it. git offers no per-path byte figure in a diff; closing that would need a
second object-size query (`--raw -z` plus `cat-file --batch-check`, which
needs stdin support in `run_git`). Not done — the finding was about a
generated artefact being pulled in whole, and that case is closed.

## 15:07 · finding · T4.2 — the `refreshInterval: 1` recommendation is already published

No callout added; the task is closed as already-stated. It appears in
`RELEASES/v3.63.0.md:81`, `CHANGELOG.md:186`, and the audience-facing
`CLAUDE/UPGRADES/v3/v3.62.1-to-v3.63.0/release-notes/22-statusline-interval-temp-names-and-bug-report-tool.md`.

The reviewer read it as a tenfold increase every client is advised to adopt.
The notes frame it as the SUGGESTION catching up with a default the daemon
has shipped since Plan 00175, and `RELEASES/v3.44.0.md` already announced
lowering that default from 10 to 1 with the ~39 ms cached render as the
argument. Fact and rationale are both out; a fourth statement would be
duplication.

## 15:08 · finding · P4 — QA is 22/26 here, and neither cause is reachable from Phase 4

Two root causes, both reproduced directly rather than inferred.

**A. Every absolute path in a worktree under `untracked/` is excluded from
the audits.** `audit_error_hiding._DEFAULT_EXCLUDE_PATTERNS` contains
`untracked/` and matches by SUBSTRING against the absolute path, so the whole
worktree tree is skipped: `_collect_shell_files(DEFAULT_SCAN_DIRS)` returns
0, the error-hiding scan finds 0 findings, and all 172 reported violations
are `stale-exclusion` — every exclusion matches nothing. Fails
`error_hiding`, `capture_corruption`, and the two audit unit tests. Hits all
four phase worktrees equally.

**B. A worktree has no `.claude/hooks-daemon.env`.** It is gitignored, and
`.claude/init.sh:207` only enters self-install mode when it exists. Without
it `HOOKS_DAEMON_ROOT_DIR` falls back to `.claude/hooks-daemon`, the wrappers
hunt for a `scripts/lib/resolve_venv.sh` a self-install checkout never
deploys, and every wrapper call returns its "Hooks daemon not installed"
fallback. Fails `smoke_test`, both `test_forwarder_socket_stdin.py` cases and
`tests/acceptance/test_tool_use_error_recovery.py`. Worth carrying into Task
5.1 and into `scripts/setup_worktree.sh`.

Sharp edge inside B: the fallback response is `decision: block`, so the
`stop_no_explanation` probe PASSES for the wrong reason. The smoke gate is
vacuous in a worktree, not merely failing.

Deliberately not fixed here. B is Task 5.1's subject and a local override
would mask the defect that task exists to reproduce; A needs an audit-script
change that would collide with the other phases. Neither would have reached
26/26 alone.

## 15:09 · finding · P4 — `llm_qa.py` cannot find a worktree's venv

`scripts/qa/llm_qa.py:30` hardcodes `untracked/venv/bin/python`, while the
canonical resolver `scripts/lib/resolve_venv.sh` looks for `untracked/venv-*`
and `venv-include.bash:113` REFUSES to create anything at the legacy path.
The main checkout only works because its `untracked/venv` happens to be a
symlink to its fingerprint venv. Reproduced the same shape locally
(`UV_PROJECT_ENVIRONMENT=<fingerprint venv> uv sync --frozen --all-extras`,
then `untracked/venv` symlinked to it — both gitignored, both pointing at
the WORKTREE's own venv, never the main checkout's). Worth filing: the QA
entry point should use the resolver.

## 15:10 · handoff · P4 — Phase 4 complete, both tasks ticked

Commits on `worktree-plan-00364-p4-handlers`, not pushed. Task 4.1 shipped
with new unit coverage (`TestStagedContentIsBoundedBeforeItIsRead`,
`TestNumstatParsing`, and a `_GitSpy` that records each git call's argv AND
its stdout size, so "an oversized path's content never entered the process"
is asserted directly rather than inferred). Two existing tests that asserted
"exactly one diff call per dispatch" now assert one sizing call and one
content call; the property they guard is unchanged. Task 4.2 closed as
already-published, no callout added.

Report: `subagent-reports/260909-p4-handlers-opus.md`. Left running: this
worktree's own daemon (socket `untracked/daemon-d9e1d4029747.sock`). Next
agent needs nothing from me; the two QA causes above are inputs to Phase 5,
not blockers on Phase 4.
## 14:40 · action · P3 — Phase 3 complete, Tasks 3.1-3.7 on `worktree-plan-00364-p3-strategies`

All seven findings implemented with a failing test first. Six commits, one
per logical unit:

| Commit     | Task(s) |
| ---------- | ------- |
| `ff3f7c7d` | 3.1     |
| `528dd919` | 3.2 3.3 |
| `ffb5179a` | 3.4     |
| `ac2f0dad` | 3.6     |
| `b5d4f45b` | 3.5     |
| `e1820171` | 3.7     |

## 14:41 · finding · T3.1 — the review's 13.5s measurement reproduces exactly

The RED test for the bracket-range cap failed at **13.971 s** for twenty
wide-range tokens, against the review's measured 13.5 s, and at 0.753 s for
a single token against its 650 ms. After the fix the whole
`test_secret_file_matching.py` module runs in 0.99 s (it took 16.83 s to run
a SUBSET before).

The fix is behaviour-preserving by construction, which is worth recording
because it is why no fallback-semantics test needed changing: a range wider
than `_MAX_BRACKET_EXPANSIONS` produces more than that many DISTINCT members,
so it could never have survived the product cap either. Rejecting it early
and rejecting it late return the same `[token]`. Two new tests pin the
boundary from both sides (a range AT the cap still expands; one past it does
not), so the cheap check cannot silently narrow what the guard expands.

## 14:42 · decision · T3.5 — aggregated, not deleted; all eight drive cleanly

The task allowed either. Before wiring anything I drove all eight strategy
payloads through a live `PipeBlockerHandler` with this project's real config:
every one DENIES and both declared patterns (`Pipe to tail/head`,
`expensive`) are present in the reason. So there was no payload that could
not be made harness-driveable, and deleting would have thrown away eight real
probes.

Their `echo "<cmd> …"` shape looked wrong at first against the handler's own
comment, which says never to use `echo` — but that comment is about `echo` as
the PRODUCER. Here the truncation sits inside a quoted ARGUMENT, so the
producer classified is the command named in the string and a hook failing
open merely prints text. The comment now says so, since the aggregated tests
would otherwise read as violating it.

Aggregation goes through a new public `strategies()` accessor on the pipe
registry rather than reaching into `_strategies` (which `qa_suppression`
still does), and iterates the ACTIVE strategies so a project that filtered
the registry by language is not handed blocks for a strategy its config
switched off. Playbook: 10 -> 18 `PipeBlockerHandler` blocks, all carrying a
payload, `generate-playbook --format json` still exit 0.

## 14:43 · decision · T3.7 — widened past strategy files, and done without sub-agents

Two departures from the task text, both deliberate.

**Scope.** The task named strategy files; the identical wrapper also sat in
nine handler modules (`plan_workflow`, `sensitive_content`,
`project_containment`, `validate_instruction_content`, others). The finding's
stated harm is that the wrapper tells a reader `scratch_path` returns a
`Path`, and that harm is the same in a handler. Fixing only the strategies
would leave the next consistency sweep to rediscover the rest. 87 single-line
plus 5 wrapped occurrences, 70 files.

**Method.** The task offered parallel Haiku sub-agents with the `Edit` tool.
This agent has no `Task` tool, so that route was unavailable. Used a
paren-MATCHING script instead (`untracked/scratch/drop_str_wrapper.py`) — not
a regex, which cannot know where `str(` closes once the argument list carries
parentheses of its own, and which is the failure mode the sed ban exists for.
The script walks characters, tracks nesting and string literals, and refuses
to write any file whose result stops parsing. The five multi-line cases were
done by hand with `Edit`.

Verified after the fact rather than trusted: every one of the 104 removed
lines in the diff was read back and belongs to the wrapper removal, the only
non-`scratch_path` added lines are two blank lines, mypy is clean, and the
generated playbook is **byte-identical** across the edit.

## 14:44 · finding · P3 — four full-suite failures, all worktree-location artefacts

`pytest tests/unit tests/integration`: **20828 passed, 4 failed**, 8 skipped,
1 xfailed, in 15m16s. None of the four is caused by Phase 3, and each has a
located cause:

- `test_audit_capture_corruption::test_default_scan_dirs_clean` and
  `test_audit_error_hiding::test_the_live_exclusions_file_has_no_stale_entries`
  — both audits exclude any path with an `untracked` SEGMENT, and these
  worktrees live at `/workspace/untracked/worktrees/…`, so every file in the
  repo is excluded. The capture audit then collects zero shell files and
  trips its own vacuity guard; the error-hiding audit finds zero findings and
  reports **all 172** exclusions stale, including entries for files nobody in
  this plan has touched (`scripts/upgrade.sh`, `plugins/loader.py`).
- `test_forwarder_socket_stdin` (2 cases) — the forwarder looks for
  `.claude/hooks-daemon/scripts/lib/resolve_venv.sh`, which does not exist in
  self-install mode. `.claude/hooks-daemon/` holds only `untracked/` in the
  main checkout too, so this is not worktree-specific.

Every phase agent working in these worktrees will hit the same four. Worth
knowing before someone spends a session bisecting them.

## 14:45 · finding · P3 — the worktrees are missing the `untracked/venv` symlink

`./scripts/qa/llm_qa.py all` died with `FileNotFoundError` on
`untracked/venv/bin/python`. `llm_qa.py` hardcodes that path (line 30); the
main checkout satisfies it with a symlink to the real venv, and the worktrees
were created without one. Fixed locally with
`ln -sfn <worktree>/.venv untracked/venv` — inside gitignored `untracked/`,
so nothing reaches the repo.

Also note the interpreter the coordinator handed out
(`untracked/venv-workspace_untracked_worktrees_worktr-2ab4-…`) fails
`python -m pytest` with "No module named pytest" despite carrying a `pytest`
binary. `.venv` is the one that works and is what ran the suite above.

## 14:58 · handoff · P3 — QA 22/26, the 4 gaps are environmental and located

`./scripts/qa/llm_qa.py all`: **22/26 PASSED**, coverage 95.3%. Everything
that judges CONTENT is clean — format, lint, type_check, magic_values,
security, semgrep, sensitive_content, doc_truth, doc_snippets,
handler_reference, repo_hygiene, git_history, british_english, shell_audit,
canonical_callers, skill_refs, eacces_safe, python_var_guidance, hook_contract,
input_contract, dependencies, and project_handlers (87 passed).

The four failures are environment, and Phase 3 provably changed no code on
their paths: `git diff <branch-point> --name-only` over `handlers/stop/`,
`handlers/subagent_stop/`, `handlers/session_start/`, `core/`, `daemon/` and
`.claude/` returns nothing at all.

**Cause A, the `untracked/` path segment** (`capture_corruption`,
`error_hiding`). The decisive evidence is the error_hiding rule breakdown:
172 violations, **all of rule `stale-exclusion`, zero real findings**. The
audit found nothing to exclude, so all 177 declared exclusions read as stale.

**Cause B, the deployed hook wrapper** (`tests`, `smoke_test`). It looks for
`.claude/hooks-daemon/scripts/lib/resolve_venv.sh`, absent in self-install
mode, and returns a canned `decision: block`. I started a daemon for this
worktree first — safe, because the worktree has its own socket
(`untracked/daemon-d9e1d4029747.sock`) and `cmd_stop` kills only by this
worktree's own pid file, so the main checkout's daemon was never at risk. That
took the acceptance suite from 10 errors to 75 passed / 11 skipped / 4 failed.
The 4 that remain are all Cause B, as is `smoke_test`'s `stop_loop_guard`
probe (it expects not-block and receives the canned block; its two siblings
expect a block, so they pass regardless).

**Not fixed on purpose**: Cause B is the deployed hook wrappers, which is
Phase 5 Task 5.1's subject. Repairing the deployment from this branch would
collide with that phase.

Report: `subagent-reports/260909-p3-strategies-opus.md`. Release-notes callout
staged at `CLAUDE/UPGRADES/UNRELEASED/release-notes/02-lint-temp-dir-and-bracket-range-cost.md`
(renumber on merge if another phase also claimed `02`). Branch
`worktree-plan-00364-p3-strategies` is committed and NOT pushed; the
coordinator merges.

## 15:05 · action · P3 — reverted the daemon's own CLAUDE.md auto-commit

Starting the worktree daemon made it regenerate the auto-generated handler
block in `CLAUDE.md` and auto-commit it (`4ceea9a7`, 28 lines reordered in the
advisory list). Phase 3 was told not to touch that block; the commit was the
daemon's rather than an edit, but restarting is what triggered it. Reverted as
`1c2e3eae`, so `CLAUDE.md` on this branch is byte-identical to the branch
point.

Worth flagging for the other phases: any agent that restarts a daemon in its
worktree will get the same auto-commit, and three branches each carrying their
own copy of that reordering is pure merge-conflict surface.

## 15:12 · finding · P6 — main-checkout QA after the Phase 1 merge: 25/26, one contention failure

`./scripts/qa/llm_qa.py all` on main at `0560b5ff` (Phase 1 only) gave 25/26:
21,391 passed, 1 failed, coverage 94.3%. The failure was
`tests/acceptance/test_install_sh_end_to_end.py::test_install_sh_end_to_end_produces_running_daemon`,
which installs and starts a daemon; it ran while three worktree agents were
running their own full QA and daemons. Rerun alone on main after Phases 3
and 4 merged: 2 passed in 32 s. Contention, not a regression. The 94.3%
coverage figure is re-measured by the closure run once Phase 2 is merged.

Phases 1, 3 and 4 merged (`0560b5ff`, `735a4328`, `718e0375`); their
worktrees removed and branches deleted with `-d`. Phase 3's callout
renumbered 02 → 06 on merge to sit after Phase 1's 02-05.
## 14:30 · action · P2 — Phase 2 delivered: all ten tasks, TDD throughout

Worktree `worktree-plan-00364-p2-install`. Every task took a failing test
first; the RED numbers are recorded below where they are the point.

- **T2.1** `GitFacts.staged_changes()` holds its answer in `self._staged`
  (including the `None`-output case: a wedged git will not recover
  mid-decision, so re-asking per folder only multiplies the timeout).
  `row_folder_bijection` grew `_folder_level()`, called only once a finding
  exists. Measured RED on a 25-folder tree: **26 spawns clean / 27 with a
  finding, now 1**. Verdicts pinned unchanged in the same class — a stale
  folder still ADVISEs, one this commit creates still BLOCKs.
- **T2.2** `client_validator` composes the helper path from `DaemonPath` +
  `bin_wrapper` constants, read at call time. The discriminating test
  monkeypatches `ECHD_CAPTURE_NAME` and asserts the check follows the
  rename; the other two couple it to a real `deploy_echd_capture`.
- **T2.3** Three parts. `_DEFAULT_PSEUDO_EVENT_BLOCKS` is now pinned against
  `.claude/hooks-daemon.yaml.example` (the template a new project starts
  from), plus a test that every `RELOCATED_HANDLERS` target has a default
  block. `_find_similar_names` → public `ConfigValidator.find_similar_names`,
  both call sites updated. `_fill_block_defaults` raises `ValueError` for an
  unknown name.
- **T2.4** `(OSError, UnicodeDecodeError)`. RED reproduced the escape
  exactly: a lone `0x80` in `PLAN.md` raised `UnicodeDecodeError` out of the
  commit gate.
- **T2.5** `_METAS_BY_BASH_KEY` hoisted to module import; `daemon_down_stdout`
  escaped for double quotes (backslash first). Tested by running a generated
  forwarder under bash with a hostile catalogue entry and `HOME` set to a
  sentinel — the text comes back verbatim, unexpanded.
- **T2.6** `_DAEMON_ERROR_MARKER in err` ahead of the translation branch. Both
  arms pinned, so the narrowing cannot swallow the defect the branch exists
  for.
- **T2.7** `_write_atomically` (temp file + `Path.replace`, temp cleaned up on
  a failed rename) at all three write sites. The test fails the rename and
  asserts the original content survives byte-for-byte.
- **T2.8** `mktemp -d` for the last-resort dir; `--help` via awk that exits at
  the first non-comment line (and skips the shebang, which `cut -c3-` was
  printing as `/bin/bash`); header "tees" → "writes". `bin/echd-capture` kept
  in lockstep, and a new drift test asserts the two files are identical —
  it caught the stale deployed copy immediately, which is the point of it.
- **T2.9** SKILL.md `description` gained `optimise` AND `bug-report`: the
  generalised test (every `argument-hint` subcommand appears in the
  description) found the second omission the review had not. `dev-handlers.md`
  uses an `[args...]` placeholder.
- **T2.10** `logger.debug` → `logger.info` for an unstattable file, with a
  companion test that a steady-state revalidation logs nothing, which is why
  INFO is affordable.

## 14:32 · decision · T2.3 — the raise is scoped to the RELOCATION path, not the sweep

`_fill_block_defaults` is called from two places, and making it raise
unconditionally would have been wrong. `migrate_relocated_handler_keys`
passes a `RELOCATED_HANDLERS` target: an unknown one there IS a bug, and the
old `{enabled: True}` fallback silently retired the moved handler a second
time. But `scaffold_pseudo_event_blocks` walks every pseudo-event block in a
merged config, which may legitimately include one this constant never had to
scaffold — raising there would abort an upgrade over a user's own config.

So the sweep now SKIPS a block it has no defaults for (leaving it exactly as
written, since there is nothing to complete it from), and the raise only
fires on the relocation path. The new "every relocation target has a default
block" test makes the raise unreachable in shipped code, which is the review's
other suggested remedy; both are in, because the test is the guard and the
raise is the backstop.

## 14:34 · finding · P2 — the QA runner cannot find this worktree's venv

`scripts/qa/llm_qa.py` hardcodes `untracked/venv/bin/python`, which
`scripts/install/venv_resolver.sh` documents as the *retired* pre-v3.7.0
layout it deliberately refuses to fall back to. In a worktree the resolver
answers `untracked/venv-<hash>/bin/python`, so `llm_qa.py all` dies with
`FileNotFoundError` before running anything (`lint` alone survives because it
does not go through `VENV_PYTHON`).

Worked around locally with a gitignored symlink
`untracked/venv -> venv-<hash>`; the canonical venv also needed
`uv sync --extra dev` because it had no pytest/ruff/mypy. **Not fixed here** —
it is a QA-tooling defect outside Phase 2's scope, and the other three phase
worktrees will hit it identically. Worth its own task: `llm_qa.py` should
source `venv_resolver.sh` like everything else does.

Also note: `uv sync --active` WITHOUT `--extra dev` strips the dev tools out
of the venv. Use `--extra dev`.

## 14:36 · finding · P2 — five integration tests fail in a bare worktree, unrelated to this work

`test_daemon_smoke` (1), `test_forwarder_socket_stdin` (2) and
`test_plugin_daemon_integration` (2) fail because
`.claude/hooks-daemon/scripts/lib/resolve_venv.sh` does not exist — the
worktree's `.claude/hooks-daemon/` holds only `untracked/`, so no daemon is
deployed to answer them. None of the five touches any module Phase 2 changed.
Environmental, pre-existing, recorded here so the next agent does not re-derive
it. Everything else: **5833 passed, 6 skipped**.

## 14:38 · handoff · P2 — merge-ready, one numbering caveat

Phase 2 is complete on `worktree-plan-00364-p2-install`, committed, not
pushed. Report at `subagent-reports/260909-p2-install-opus.md`.

Caveat for the coordinator: the release-notes callout is
`CLAUDE/UPGRADES/UNRELEASED/release-notes/02-plan-qa-commit-gate-and-capture-helper.md`.
Phases 1, 3 and 4 are running concurrently and cannot see each other's
choices, so if two of them also picked `02-` the numbers will need
renumbering at merge — the ordinal is arrival order, and nothing enforces it
across worktrees.

## 15:26 · finding · P2 — final QA is 22/26, and one of the four was mine

Correcting the earlier handoff, which was written before the authoritative
run. `./scripts/qa/llm_qa.py all` after the daemon restart, on the committed
tree:

```
QA: 22/26 PASSED, 4/26 FAILED
❌ tests: 21390 passed, 6 failed, 21 skipped | coverage: 95.3%
❌ error_hiding: 172 violations
   capture_corruption: NO OUTPUT (run tool first)
❌ smoke_test: 2/3 probes passed (1 failed)
```

**One real regression, found and fixed.** The first run had a seventh test
failure, `test_skill_scripts_are_referenced::
test_a_script_that_reads_arguments_is_invoked_with_arguments`, and it was
mine: T2.9 replaced `"$@"` with `[args...]`, but that gate accepts only
`"$@"`, `$*` or an ANGLE-bracket placeholder. The review's suggested
`[args...]` is not the convention this repo already enforces. Fixed to
`<the arguments you were given>`, which satisfies both that gate and the new
one, and my own test now pins the angle bracket rather than merely banning
`"$@"`. Worth recording: the review's remedy was wrong here and the existing
test was right.

**The four remaining failures are environmental, and two are proven so.**

`error_hiding` reports 172, ALL of them `stale-exclusion`. Root cause:
`audit_error_hiding` skips a file when any exclude pattern is a SUBSTRING of
its absolute path, and one pattern is `untracked/`. This worktree is
`/workspace/untracked/worktrees/...`, so every file in it is skipped, zero
violations are collected, and every exclusion is then reported as matching
nothing. `audit_capture_corruption` has the identical bug via
`_EXCLUDE_DIR_PARTS` containing `"untracked"` matched against `path.parts` —
0 shell files collected, hence `NO OUTPUT`. Both bite every phase worktree.

Proof it is not this phase's doing: `git archive HEAD` into a pristine tree
and running the same audit there reports the SAME 172, including the three
entries for files Phase 2 touched (`client_validator._check_running_daemon`,
`corpus.load_cached_corpus`, `same_commit_plan_doc._plan_status`).

The four remaining test failures are the same two audits plus
`test_stop_hook_hard_block` (2) and `test_forwarder_socket_stdin` (2), which
need `.claude/hooks-daemon/scripts/lib/resolve_venv.sh`. That path is
untracked (`git ls-files .claude/hooks-daemon` is empty) and is absent from
the MAIN checkout too, so those two files fail there as well — not a worktree
artefact.

`smoke_test`'s `stop_loop_guard` probe expects `decision!=block` for
`stop_hook_active=true` and gets `block`. Phase 2 touches no handler and no
core file — `git diff --name-only f4253b3d..HEAD` is entirely plan_qa,
install, config/validator, docs_qa/corpus, skills and the capture helper.

## 15:28 · action · P2 — daemon started in this worktree, deliberately

`./bin/hooks-daemon restart` on its OWN socket
(`untracked/daemon-d9e1d4029747.sock`), so the live session's daemon is
untouched. Ten acceptance failures were purely "Daemon not running"; with it
up they drop to four. The daemon then auto-committed `f4253b3d` regenerating
the CLAUDE.md handler block — its own doing, not an edit of mine; the brief
said not to touch that block and I did not.

## 15:38 · action · P5 — Task 5.4 first, because everything else is measured through it

Both audits fixed before touching the relay, so the rest of Phase 5 could be
judged by a QA run that actually scans this checkout. `audit_error_hiding`
and `audit_capture_corruption` now match their exclusion patterns against the
path RELATIVE to the tree being scanned, not the absolute path — the
exclusions name directories inside a scanned tree, and the absolute match was
excluding whole checkouts by where they happen to live.

Two adjacent defects surfaced while doing it, both fixed with their own test:

- `format_violation_report` raised `KeyError: 'description'` on a
  stale-exclusion finding, which carries none. Only the text path, so the QA
  pipeline (JSON) never saw it — it broke exactly the run that had something
  to say.
- `run_audit` loaded exclusions from the SCRIPT's directory, so auditing any
  other tree reported all 177 as stale. It now reads them from the workspace
  under audit; in a real run those are the same directory.

And a moment of dogfooding: my first `_is_excluded` used
`try: relative_to except ValueError: relative = path`, which the audit itself
flags as `silent-fallback`. `Path.is_relative_to` says the same thing with no
handler. The gate caught its own author.

## 15:45 · decision · T5.5 — the resolver is authoritative, the legacy path is not a rescue

`llm_qa.py` now calls `scripts/lib/resolve_venv.sh`. The question worth
recording is what happens when the resolver is PRESENT but FAILS: it errors,
it does not fall back to `untracked/venv/bin/python`. Falling back there would
let a stale symlink answer for a venv the resolver had just rejected, which is
the silent-fallback class the fingerprint layout exists to remove (Plan 00103
Decision 2). The legacy path is consulted only when the resolver file is
missing entirely — a partial deployment, not a rejected venv. Either failure
names both places tried and the resolver's own stderr.

Confirmed live: from this worktree the resolver answers
`untracked/venv-workspace_untracked_worktrees_worktr-bb5b-py311-81c29529/bin/python`,
so the hand-made `untracked/venv` symlink is no longer load-bearing.

## 15:52 · decision · T5.2 — 51, and why not a lower number

Free PreToolUse slots per the live chain: 24-29, 32, 39, 51-54, 56, 59. The
band matters more than the gap: a plan-status gate is workflow enforcement
(36-55), not a safety guard, and 51 sits after `plan_qa_edit` (44) and
`plan_workflow` (46) so their advisories are attached before this handler's
terminal DENY lands.

The plan says to verify with `bin/hooks-daemon logs`. **That cannot witness
it either way**: the in-memory ring buffer starts after handler registration,
so a restart's log holds 6 records (socket + server lines) and no
`Registered project handler` or `priority collision` line ever reaches it. The
verification is therefore
`tests/integration/test_project_handler_priority_collisions.py`, which
reproduces the controller's own rule — register every library handler through
`register_all` against this project's real config, then ask whether each
discovered project handler's priority is already taken on its event. It fails
at 20, naming `block-git-message-backtick` and `lock-file-edit-blocker`, and
passes at 51.

## 16:05 · finding · T5.1 — the fix keeps every path a literal, and adds one

The obvious fix (resolve the relay dir from the hook's own location at
run time) is the one the plan text suggested and the one NOT taken. The guard
is a zero-spawn hot path by design, and `normalise_project_root` /
`recorded_untracked_dir` both depend on the literal being there to read back.

So the guard bakes one MORE literal — the hooks directory it was generated
for — and tests `${BASH_SOURCE[0]}` against it with a bash builtin:

```bash
if [[ "${1:-}" != "--no-relay" && "${BASH_SOURCE[0]}" == "/workspace/.claude/hooks/"* ]]; then
```

A worktree's inherited copy fails that test and falls through to `init.sh`,
which computes its own project-scoped socket. Zero spawns, one string
comparison, and the failure direction is safe: an unrecognised path costs one
legacy round trip, never a wrong answer.

Consequence worth knowing: a hook invoked by a RELATIVE path no longer
relays, even in the main checkout. Claude Code always invokes hooks
absolutely (`bash "$CLAUDE_PROJECT_DIR"/.claude/hooks/<event>` in
settings.json), so this is hand-run probes only, and it is pinned by a test
rather than left to be discovered.

The tracked forwarders were regenerated through the project's own path
(`python -m claude_code_hooks_daemon.install.forwarder_generator
--project-root /workspace --hooks-dir <worktree>/.claude/hooks`) — for the
RECORDED root, not this worktree's, so the committed artefact still belongs
to the main checkout. Exactly one line changed in each of 27 files; the four
relay-ineligible forwarders are untouched.

`recorded_project_root` joins `recorded_untracked_dir` because the artefact
now bakes two roots, and a comparison that declares only one reports the other
as machine-specific from every foreign checkout — the same defect Plan 00250
Task 2.4c fixed, one path along.

## 16:08 · action · T5.1 — the worktree now answers from its own daemon

`.claude/hooks-daemon.env` provisioned here the way `setup_worktree.sh` now
does it (via `install.py::create_daemon_env`, written fresh — never copied
across checkouts). Probe, from inside the worktree:

```
$ printf '{"tool_name":"Bash",...,"session_id":"p5-probe"}' | bash .claude/hooks/pre-tool-use
{}
$ ./bin/hooks-daemon logs -n 3 | grep -c p5-probe
1
```

The answer comes from THIS checkout's daemon, with no environment override —
the plan's Success Criterion, measured rather than reasoned about.

One transient on the way: the first run of
`tests/integration/test_forwarder_socket_stdin.py` after provisioning tripped
the conftest guard for rewriting `CLAUDE.md`. Cause was a daemon start during
the test (ClaudeMdInjector runs at startup, and the pid had changed); the
guard restored the file and the same test passed on the next run with the
daemon up. Worth knowing for the next agent: the first hook call in a freshly
provisioned worktree can start a daemon, and that start regenerates the
CLAUDE.md handler block.

## 16:35 · handoff · P5 — Phase 5 complete, 26/26 from inside the worktree

`./scripts/qa/llm_qa.py all` run from this worktree: **26/26 PASSED**
(21526 tests passed, 0 failed, 21 skipped, coverage 95.4%). The same command
reported 22/26 here at the start of the phase, and the four that failed were
the two audits, their unit tests, and a `smoke_test` that was vacuous — all
Phase 5 subjects, all now genuinely green rather than green-by-not-looking.

One intermediate run reported 4 failures, every one of them mine and fixed:
two magic `timeout=30` literals in the new integration test (now
`Timeout.REQUEST_DEFAULT`), six files needing black, and a release-note
sentence that quoted the retired `untracked/venv/bin/` path literally, which
`check_python_var_guidance` flags wherever it is not an immutable-history
directory. The callout says "the retired pre-v3.7.0 venv path" instead.

Daemon restarted in this worktree after the final change (pid 1334975); no
`priority collision` line — with the caveat from the 15:52 entry that this log
cannot show one either way, which is why the integration test exists.

Five commits on `worktree-plan-00364-p5-relay`, not pushed:
`fc79b291` (T5.4), `b8cab369` (T5.5), `eddd5ab6` (T5.2), `b0e62226` (T5.1),
`603f36f6` (T5.3 + plan/journal/callouts). Report:
`subagent-reports/260909-p5-relay-opus.md`.

For the next agent: `.claude/hooks-daemon.env` is provisioned in this worktree
but is gitignored, so any OTHER existing worktree still lacks it — re-run
`scripts/setup_worktree.sh` there rather than copying the file. Phase 6's
closure QA should be run from a worktree deliberately, since that is now the
case the suite covers honestly.
