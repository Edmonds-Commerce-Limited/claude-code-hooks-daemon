# Plan 00163 — Journal 26-07-14

> **Append-only activity log** for plan 00163. `PLAN.md` tracks the plan;
> this journal tracks what actually happened. Entry grammar:
> `## HH:MM · category · REF — title`; category ∈
> action|finding|decision|thought|blocker|handoff. Append at the bottom; never
> edit earlier entries (corrections are new entries).

## 09:11 · action · — — plan created via Fable synthesis

Plan 00163 scaffolded (counter → 163) by a Fable synthesis agent converging two
Opus brainstorms (data-model + enforcement), copied in-folder as
`BRAINSTORM-datamodel.md` / `BRAINSTORM-enforcement.md`. Committed `944d3a05`.

## 09:19 · action · T1.7,T1.8 — mkplan JOURNAL/ scaffolding + template (dogfood)

Phase 1 down-payment: added `_JOURNAL_TEMPLATE_.md` (bundle +
`CLAUDE/Plan/` dogfood copy) and taught `mkplan.bash` (bundle + deployed,
byte-identical, shellcheck clean) to scaffold `JOURNAL/` +
`NNNNN-Journal-YY-MM-DD.md` from it — gated on the template's presence so
non-journalling projects still get clean plans (Decision 10). Two new tests in
`test_plan_workflow.py` (present→scaffolds, absent→gated); 33 passed.

## 09:19 · decision · T1.9 — journal this plan retroactively

00163 predates the scaffolding, so this day-file was created by hand rather than
by mkplan — written via bash `cat` (not the Write tool) to keep the append-only
byte-invariant clean, mirroring how mkplan and future appends will write.

## 09:19 · handoff · P1 — next: plan_qa journal checks

Next Phase-1 units (not yet done): config `plan_workflow.qa.journal.*` (T1.1),
`PlanFolder.has_journal`/`latest_journal_date` (T1.2), and the EDIT/SWEEP
checks — `journal-dayfile-naming`, `journal-append-only`,
`journal-folder-present`, `journal-freshness` (T1.4/T1.6), all ADVISE.

## 14:05 · action · T1.1,T1.2,T1.3 — plan_qa journal plumbing landed

Config: added nested `PlanWorkflowQaJournalConfig` (`enabled`/`mode`/`dir_name`/
`freshness_days`/`enforce_on_completion`/`grandfather_before`) under
`plan_workflow.qa.journal`, threaded to `CheckContext` via one typed
`_with_journal` helper (dataclasses.replace — no `# type: ignore`). Model:
`PlanFolder.has_journal` + `latest_journal_date` (filename parse only) with a
`parse_journal_dayfile_name` splitting regex-match from calendar validity — so
the impossible-date case is a precondition (`is_valid_date` via stdlib
`calendar`), NOT a caught ValueError (error_hiding clean). Context: added
`file_content_before` + `today` to the EDIT builder.

## 14:05 · action · T1.4,T1.5 — EDIT checks live

`journal-dayfile-naming` (ADVISE, ratchet-able to BLOCK via `mode: block`) and
`journal-append-only` (ADVISE forever). `plan_qa_edit.matches()` broadened from
PLAN.md-only to also lint `{JOURNAL}/*.md`; `handle()` threads pre-edit content
+ today. 28 handler tests, 20 check tests green.

## 14:05 · action · T1.6 — SWEEP checks live + grandfathered

`journal-folder-present` (In Progress, number ≥ grandfather_before, no JOURNAL/)
and `journal-freshness` (JOURNAL/ newest day-file > freshness_days old, by
filename not git). This repo's config sets `grandfather_before: 163` — verified
`plan-qa --sweep` reports ZERO journal findings (legacy plans skipped, 00163's
own journal is fresh). Registry catalogue tests updated (EDIT 8→10, SWEEP 8→10).

## 14:05 · finding · P1 — markdown_table_formatter exemption still owed

The append-only check is now live, but the `markdown_table_formatter`
(PostToolUse) still rewrites JOURNAL/ day-files on Write/Edit — which would
false-trip append-only for users editing journals via the tools. Decision 6's
formatter exemption is the next unit. (This entry was appended via bash `cat`,
not the Write tool, to stay byte-stable meanwhile.)

## 14:40 · action · P1,T2.1 — Phase 1 committed + reference doc drafted

Committed `8a93079d` (27 files, +1666/-85). Phase 1 complete: config, model,
context, 4 ADVISE checks (2 EDIT + 2 SWEEP), plan_qa_edit journal wiring,
markdown_table_formatter JOURNAL/ exemption. Full QA 13/13 (10006 tests, 95.6%).
Also drafted `CLAUDE/PlanJournalling.md` (Task 2.1) with the POLICY-vs-CONVENTION
split. Fixed 3 pre-existing "clean tree" sweep fixtures that now legitimately
need a JOURNAL/ (In Progress + default grandfather_before 0) by adding an empty
JOURNAL/ dir to each.

## 14:40 · handoff · P2 — next: template reconciliation + docs regen

Remaining Phase 2: Task 2.2 (retire `## Notes & Updates` in the dual
byte-identical `_TEMPLATE_.md` for a curated `## Delivery & Milestones` stub;
update PlanWorkflow.md + completion checklist) and Task 2.3
(HANDLER_REFERENCE.md journal options + `generate-docs` so the injected
guidance and reference mention journals). Phase 3 (commit-gate coupling +
ratchet review) and Phase 4 (client rollout) are post-dogfood.

## 15:30 · blocker · T2.2 — dogfooding bug: snapshot write misdetected as a plan

Writing the daemon-owned template snapshot `.plan-template-default.md` (part of
Task 2.2) tripped `markdown_organization.is_planning_mode_write`: the file
contains `**Status**: Not Started` because it IS a plan template, so the handler
scaffolded a bogus `00164-plan-template-default/` and bumped the git plan counter
163→164. Contained: removed the untracked folder, reset the counter to 163.

## 15:30 · action · T2.2 — fixed the misdetection (TDD)

Root cause: `is_planning_mode_write` treated any `{plan_dir}/{name}.md` flat file
(except "readme") as a plan-mode save. Fix: exclude dot- and underscore-prefixed
basenames (`.plan-template-default.md`, `_TEMPLATE_.md`, `_JOURNAL_TEMPLATE_.md`)
— a real Claude Code plan save is never so prefixed. Added
`_DAEMON_OWNED_PLAN_FILE_PREFIXES` + `_is_daemon_owned_plan_file`; RED test
first, 179 markdown_organization tests green. Daemon restarted; live-verified the
snapshot write no longer scaffolds a plan and the counter stays 163.

## 15:30 · action · T2.2 — Notes & Updates retired for Delivery & Milestones

Replaced `## Notes & Updates` with a curated `## Delivery & Milestones` stub in
the bundled template, the dogfood template, and the byte-identical snapshot;
updated PlanWorkflow.md (template example, completion checklist, cron-ID → JOURNAL)
and Plan/CLAUDE.md. No plan_qa check asserts Notes & Updates; legacy plans untouched.

## 15:40 · finding · T2.2 — docs/PLAN_SYSTEM.md follow-on

Updated the normative section-description line in docs/PLAN_SYSTEM.md
(Notes & Updates → Delivery & Milestones + JOURNAL pointer). Five illustrative
full-plan example blocks in the same file (lines ~355/837/1134/1309/1437) still
show `## Notes & Updates` headers — deferred as a mechanical docs-refresh
follow-on rather than risk a half-migrated doc under the current bug-fix flow.
Tracked here; low urgency (examples, not normative).
