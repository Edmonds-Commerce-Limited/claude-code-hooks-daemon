# Plan 00192 — Journal 26-07-31

> **Append-only activity log** for plan 00192. One file per day
> (`00192-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 19:25 · action · — — plan scaffolded

Plan 00192 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 19:28 · action · — — failsafe recovery cron created

Cron `29cb15cb` — hourly at :23, non-durable, session-only, auto-expires
after 7 days.

## 19:28 · finding · — — root cause verified against a live agent shell

This surfaced from a *misdiagnosis*: an agent reported plan QA as "not
installed" and abandoned it. That was wrong. The feature is present and
working — the documented invocation is unrunnable, so the agent read the
failure as absence. This is the worst shape of failure: the daemon raises no
error, and the agent confidently concludes a working guardrail does not exist.

Verified in a live agent Bash shell:

| Check | Result |
| --- | --- |
| `$PYTHON` in agent shell | unset |
| `python3 -c "import claude_code_hooks_daemon"` | `ModuleNotFoundError` |
| Real interpreter | `untracked/venv-workspace-py311-81c29529/bin/python` |
| `plan-qa --sweep` via that interpreter | works — exit 1, 1 advisory finding |

With `$PYTHON` unset, `$PYTHON -m …cli …` expands to
`-m claude_code_hooks_daemon.daemon.cli …` and bash tries to execute `-m`.

The working sweep output, for the record:

```
Plan QA: 1 finding (0 block, 1 advise)
- [advise] journal-freshness: Plans whose JOURNAL/ has gone quiet (Plan 00163):
- 00163-plan-journalling: 17 days since last journal entry
- 00166-supervisor-multi-terminal-session-isolation: 16 days
- 00168-supervisor-compaction-injection-not-firing: 15 days
- 00170-universal-hook-coverage-and-enforcement: 15 days
- 00174-status-line-artefact-cadence-redesign: 14 days
- 00175-statusline-refresh-interval-first-class: 14 days
```

## 19:28 · finding · — — inventory: 17 sites, 12 inject into agent context

Handlers injecting `$PYTHON` into block reasons / `get_claude_md()`:
`plan_qa_edit`, `plan_qa_commit_gate`, `plan_qa_sweep`,
`daemon_location_guard`, `daemon_restart_verifier`, `task_tdd_advisor`,
`project_handler_load_checker`, `plan_workflow_asset_checker`,
`hook_registration_checker`, `background_process_tracker`,
`markdown_table_formatter`, `project_loader`.

Generator/error surfaces: `docs_generator.py` (**13 occurrences land in the
resident `CLAUDE.md` block every session**), `playbook_generator.py`,
`cli_acceptance_tests.py`, `cli.py`, `utils/error_formatter.py`.

The daemon is generating its own broken instructions into permanent context.

## 19:28 · finding · — — three structural gaps

1. `skills/hooks-daemon/scripts/daemon-cli.sh` is ALREADY a fully general
   forwarder (`"$PYTHON" -m …cli "$@"`), so `daemon-cli.sh plan-qa --sweep`
   would work today. But it is not deployed in self-install mode
   (`.claude/skills/` has no `hooks-daemon/`), it resolves the venv via
   `$DAEMON_DIR/scripts/lib/resolve_venv.sh` which does not exist under
   `.claude/hooks-daemon/` here, and it performs a network self-bootstrap per
   invocation — unacceptable for a hot-path command like `plan-qa --lint`.
2. Root `daemon.sh` is stale: hardcodes the legacy `untracked/venv/bin/python`
   instead of the fingerprint-keyed venv `CLAUDE.md` declares canonical,
   exposes a curated subcommand list rather than forwarding, mode `rwx--x--x`.
3. Deployed skill has zero plan-QA coverage — `rg 'plan-qa' src/…/skills/`
   returns nothing.

## 19:28 · decision · — — emit resolved absolute paths, never a shell variable

The correct pattern already exists here: `pipe_blocker` does not tell the agent
to run `echd-capture`, it resolves and prints the ABSOLUTE path
`/workspace/scripts/echd-capture`. Every `$PYTHON` site should do the same.
The daemon knows its own interpreter; the agent should never have to.

Rejected alternative: document how to set `$PYTHON`. That pushes venv
complexity onto every agent and every client project's docs, and still breaks
whenever the fingerprint changes.

## 19:28 · thought · — — incidental dogfooding observations, not yet triaged

Encountered while running this audit; neither is filed as a bug yet.

- `sed_blocker` blocked a read-only `sed -n '1,15p' <file>`. The handler's own
  guidance allows read-only stdout pipelines, but the argument form that reads
  a file directly is still denied. Worked around with `head -n`. Needs a
  decision on whether read-only `-n 'Np'` should pass.
- `pipe_blocker`'s suggested `extra_whitelist` regex is derived from the tail
  of the command string, producing non-pasteable entries like
  `` "^print('importable')"\b `` and `` "^^##\b" ``. Cosmetic.

## 19:36 · finding · — — field report corroborates from a SEPARATE client install

User supplied `untracked/hooks-daemon-python-problems.md`, since moved to
`FIELD-REPORT-client-a-v3.49.0.md` in this folder (per the CLAUDE.md rule that
reports never linger in `untracked/`).

It is from a **different project** — client-a infra repo, ccy/Podman, daemon
v3.49.0, client-mode layout, Python 3.13, different venv fingerprint. Same root
cause. That makes this install-independent, not a self-install quirk.

Claims I verified directly against this repo before accepting them:

- `init.sh:502-503` — verbatim match: `PYTHON_CMD` is *intentionally* NOT
  exported, "Hot path uses system python3". That decision is correct and should
  stand. It pins the bug precisely: the docs are written from inside the wrapper
  scripts' variable scope and handed to a reader who is not in that scope.
- `pyproject.toml` has **no `[project.scripts]`** — confirmed. No console entry
  point, so no `PATH` fallback exists when the documented line fails.
- Worktree gap — mechanism confirmed: `.claude/.gitignore:3` ignores
  `hooks-daemon/`, so a worktree has the tracked `hooks-daemon.yaml` but NOT
  `hooks-daemon/scripts/lib/`. `daemon-cli.sh` walks up, finds the yaml,
  anchors `DAEMON_DIR` to the worktree, lib missing → exit 5.

**Correction to my earlier count.** I reported 17 affected source sites; the
report says 29. The report is right — `rg -l '\$PYTHON' src/` returns 29. My 17
was scoped to `handlers/` + `daemon/` + `utils/` and missed `skills/*.md`,
`strategies/tdd/CLAUDE.md` and `src/CLAUDE.md`, all of which ship to clients and
are read by agents. Phase 3 widened accordingly.

New material the report adds beyond my audit: exit 127 with a misleading
`-m: command not found` (names neither Python nor the venv, so there is no
thread to pull); and the actively *harmful* recovery it provokes — `pip install`
into the wrong env, Dockerfile edits, building a replacement venv, all against
an installation that was never broken.

## 19:40 · decision · P0 — client-mode fixture is now standard practice

User: "i think when testing this, you need a dummy client repo in
./untracked/dummy-client-repo … lets make it part of your standard practice …
automate the process of setting this up".

Correct, and structurally necessary rather than convenient: this bug class is
mode-dependent. Self-install has no deployed wrapper to disagree with, so no
self-install test could ever have caught it.

Shipped `scripts/dummy-client-repo.sh` (shellcheck clean) with
`create`/`status`/`cli`/`python`/`destroy`. Design rules, deliberately strict:

1. Drives the PRODUCTION installer (`scripts/install_version.sh`) end-to-end.
   Never synthesises state — the v3.10.0 SEV-1 escaped because a gate faked
   install state instead of running the real chain.
2. Isolated by a dedicated `HOSTNAME=dummy-client-repo` so socket/PID/log never
   collide with the dogfood daemon.
3. FAIL FAST if the install leaves no RUNNING daemon.

Documented as standard practice in `CLAUDE/development/CLIENT-MODE-TESTING.md`,
wired into all three CodeLifecycle Definition-of-Done checklists, and pointed to
from `CLAUDE.md`.

## 19:40 · finding · P0 — bug reproduced in a genuine client install

```
$ grep -c 'PYTHON' untracked/dummy-client-repo/CLAUDE.md
9
$ grep -n '\$PYTHON' untracked/dummy-client-repo/CLAUDE.md | head -n 3
40:$PYTHON -m claude_code_hooks_daemon.daemon.cli status
41:$PYTHON -m claude_code_hooks_daemon.daemon.cli restart
42:$PYTHON -m claude_code_hooks_daemon.daemon.cli logs
```

A freshly-installed client receives **9 unrunnable lines** in its resident
`CLAUDE.md`. Dogfood daemon verified unaffected (PID 459, own socket).

Two bugs in my own script, caught by running it rather than trusting it:

- `cmd_status` did not `cd` into the fixture, so the CLI resolved
  **self-install** socket paths and misreported the fixture daemon as down —
  while the installer log clearly said "✓ Daemon is running". Worth noting as a
  general trap: daemon path resolution is CWD-dependent.
- `error_hiding_blocker` correctly blocked my `|| true` when capturing status
  output. Replaced with explicit `|| status_rc=$?` capture. The handler was
  right; the block improved the script.

An incidental confirmation for Phase 1: `install_version.sh`'s own "Next steps"
output already prints **fully resolved absolute interpreter paths**, not
`$PYTHON`. So the correct pattern is already precedented in this codebase's
installer — the handlers are the outlier.
