# Plan 00402 — Journal 26-09-24

_Scaffolded by `mkplan.bash`; timestamps in this file are UTC._

> **Append-only activity log** for plan 00402. One file per day
> (`00402-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM, ONLY with
> `mkplan.bash --journal <plan-number> <category> <body-file>` (it stamps the
> real UTC time; a hand-written entry is denied); NEVER edit earlier entries
> (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` UTC 24h (the date is in the filename, also UTC); times increase
>   down the file. A file with no sentinel line above predates this system —
>   its times are local, zone unrecorded (legacy; never rewritten).
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 18:22 · decision · —   — Option 3 ruled; standalone drift check built

Ruled option 3 unattended, per the coordinator's brief: a QA check, no runtime change.

Re-verified the plan's claims against today's code before building on them:
`DocsGenerator` is still reached only from `cmd_generate_docs` (and
`cmd_regenerate_docs` through it), and `_render_header` still stamps today's date
and the running version, so regenerating on restart would still destroy the
deployed-from marker. The committed doc is fresh today: `generate-docs` into
scratch diffed clean, marker included. So the defect is the missing detector, not
a live drift.

Placement: a standalone `scripts/qa/check_generated_doc_drift.py`, not a new
rule inside `check_doc_truth.py`. That script's fixture tests run against trees
with no daemon config, where `generate-docs` cannot run, so a drift rule there
would either fail those tests or skip silently. It regenerates through the real
CLI (`python -m ...cli generate-docs --project-root ROOT --output <tempdir>`),
with PYTHONPATH pinned to this checkout's `src/`, so the comparison is always
against the generator the change ships. It never writes the committed file.

Marker: excluded line-wise using `utils/deployed_version.VERSION_MARKER_RE`, the
same parser `upgrade.sh`'s reader uses, rather than a restated pattern. Its
absence is a separate rule, because otherwise excluding it would let a
marker-less doc pass.

Determinism under 00466 N7 (worktree-n466-goal-flip, which sorts HOOKS-DAEMON.md
ties by (priority, config_key)): ran that branch's generator twice against its
own tree, and got byte-identical output whose body matches its committed doc. The
only body difference from main is one docstring row that branch changed and
regenerated itself. This branch does not touch `docs_generator.py`.
