# Plan 00307 — Journal 26-09-01

> **Append-only activity log** for plan 00307. One file per day
> (`00307-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 17:34 · action · — — plan scaffolded

Plan 00307 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 17:36 · action · — — plan filed from owner field report

- Source: owner report from another project's session — subagents failed
  when their final report came back truncated to the main agent; suspected
  harness tool-result cap (~16k-token default). Owner direction: dogfood,
  then enforce by hooks that subagents/agent teams communicate large data by
  file, never direct messaging.
- Dedupe: Plan 00032 (Blocked upstream) covers WHEN to delegate, not the
  return channel; Plan 00264 is the same oversized-blob defect class on the
  GitHub-comment channel. Cross-referenced in PLAN.md, no supersession.
- Session failsafe recovery cron: single cron id `892eef87` (canonical
  plan-agnostic prompt; replaced the earlier plan-specific `a47de8ef`).

## 18:12 · finding · T1.1 — reproduction ran RED: harness middle-elision confirmed

- Probe (Haiku, no tools, ordered to return 400 sections inline) came back
  ~24k subagent tokens; the harness cut the MIDDLE of the payload with an
  explicit `... [2301 characters truncated] ...` marker (SECTION 0115→0122
  lost) while BOTH sentinels survived — so an end-marker check cannot prove
  an intact report. Full findings + verbatim re-run dispatch:
  `REPRODUCTION.md`. Task 1.1 ticked.

## 18:14 · decision · T2.1 — owner ruling: dispatches declare their plan folder

- Owner (mid-turn message): subagent invocations should declare which plan
  folder they are working in — that folder becomes the canonical location
  for reports etc.; if genuinely not plan work, the dispatch must say so
  explicitly AND declare where files go. Folded into Task 2.1 as the
  dispatch-declaration contract the PreToolUse handler enforces.

## 18:24 · decision · T2.2 — owner ruling: standard report folder + filename

- Owner: plan folders carry a standard `subagent-reports/` subfolder so
  reports are always in a known place, and filenames follow a known format
  `{yymmdd}-{agent-name}-{model}.md`. Added as Task 2.2 (convention +
  handler guidance naming the exact expected path per dispatch + docs +
  plan-QA recognition of the subfolder).

## 19:40 · action · T1.2 — hook-surface verification report filed

- Report: `subagent-reports/260901-explore-hook-surfaces-fable.md`. Confirms
  `last_assistant_message` on the SubagentStop contract (no transcript parse
  needed), the stale `subagent_id`/`subagent_type` input-schema fields vs the
  real `agent_id`/`agent_type`/`agent_transcript_path`/`last_assistant_message`,
  and the `agent_isolation_advisor` precedent for keying a PreToolUse handler
  on the `Task` tool. Task 1.2 ticked.

## 20:55 · action · T2.1/T3.1 — both handlers implemented, TDD, all green

- `DispatchDeclarationHandler` (PreToolUse on `Task`, `dispatch_declaration`,
  priority 48): silent when the prompt names a `CLAUDE/Plan/NNNNN-` path or
  pairs "not plan work" with a declared destination; otherwise injects the
  file-handoff contract as `additionalContext` (advisory default) or DENIES
  in opt-in `strict` mode. 12 unit tests, all pass.
- `SubagentReportSizeBlockerHandler` (SubagentStop,
  `subagent_report_size_blocker`, priority 15, first handler back on this
  event since Plan 00237): reads `last_assistant_message` directly off
  `hook_input`, DENIES above a configurable `threshold_chars` (default 4000
  — an order of magnitude below Task 1.1's measured ~24k-token harmful
  shape), fails open on any missing/non-string message, and skips on
  `stop_hook_active` re-entry. 10 unit tests, all pass. Updated the stale
  `SUBAGENT_STOP_INPUT_SCHEMA` (`core/input_schemas.py`) to also declare the
  real contract fields.
- Wire-up: `HandlerID`/`Priority` constants, `__init__.py` exports
  (`handlers/subagent_stop/__init__.py` no longer empty), example config +
  this repo's `hooks-daemon.yaml` (both enabled, advisory/non-strict for
  2.1), `_EARNS_GUIDANCE` entries, deny-expecting acceptance tests for both
  (needed for `test_acceptance_test_coverage.py` and
  `test_declared_behaviour_matches_source.py`'s BLOCKING-tag check).
  `test_claude_md_guidance_coverage.py`'s
  `test_every_earning_handler_has_a_section_in_claude_md` required a daemon
  restart (CLAUDE.md guidance is only regenerated on startup) — done,
  verified RUNNING (31/31 listeners).
- Docs: `PlanWorkflow.md` gets a "Subagent report handoff" section
  documenting `subagent-reports/` + the filename convention;
  `DirectoryRoles.md`'s plan-dir row now names it. `plan-qa --sweep` stays
  clean (0 findings) with the new `subagent-reports/` folder present in this
  plan.
- Full `pytest -q` run in progress at handoff time (background); mypy clean
  on both new files.

## 19:25 · finding · T4.1 — GREEN re-run PASSED; two tuning findings for T4.2

- Verbatim probe re-run against the live handlers: the 84,877-char inline
  return was BLOCKED at SubagentStop; the probe wrote the full output to a
  file and returned a ~1,100-char summary + path — no elision marker, no
  context flood. Enforcement overrode the probe's explicit "do not write
  files / use tools" instructions, which is the property we wanted.
- Tuning for T4.2: (1) the report landed at `untracked/` root under a
  self-chosen name, not the configured fallback dir or the filename
  convention — make the block reason's path guidance prescriptive; (2) the
  markdown-location handler rejected the probe's first write location — the
  size-blocker's remediation should name an always-writable path so the two
  handlers never argue. Details in `REPRODUCTION.md` GREEN section.

## 20:10 · action · T4.2 — tuned both handlers' remediation text (TDD)

- `subagent_report_size_blocker.py`: the deny reason now PRESCRIBES a
  concrete path — `_prescribed_fallback_path()` renders
  `untracked/agent-reports/{yymmdd}-{agent-name}-{model}.md` with today's
  date always filled in, `agent_type` from the hook input when present
  (else the documented `{agent-name}` placeholder), and `{model}` always a
  literal placeholder (no model field exists on the SubagentStop contract).
  New `_fallback_report_dir` config attr (default
  `untracked/agent-reports/`) mirrors `dispatch_declaration`'s option of the
  same name so a project can move both together. The message still mentions
  the plan-folder `subagent-reports/` convention as the preferred path when
  a plan folder was declared, but the fallback is now the concrete,
  always-actionable default.
- Confirmed `untracked/` is unconditionally allowed by
  `markdown_organization`'s built-in rules (rule 4 in `_check_builtin_paths`)
  regardless of subpath, so `untracked/agent-reports/...` was already
  genuinely writable — no handler config change needed there. Added a test
  (`test_prescribed_fallback_path_is_allowed_by_markdown_organization`) that
  instantiates `MarkdownOrganizationHandler` and asserts `matches()` is
  `False` for the exact rendered path, pinning finding 2 so the two handlers
  can never drift apart again.
- `dispatch_declaration.py`: aligned wording so both handlers tell one
  consistent story — the fallback-directory mention in both the deny/advise
  message and `get_claude_md()` now spells out the same
  `{yymmdd}-{agent-name}-{model}.md` filename convention already used for
  the plan-folder case, instead of naming only the bare directory.
- Tests: 4 new cases in `test_subagent_report_size_blocker.py` (concrete
  path render, placeholder fallback, configurable fallback dir, markdown-org
  cross-check), all TDD (written before the handler change, ran RED, then
  GREEN). `test_dispatch_declaration.py` unaffected (its assertions were
  already substring-based). Full unit suite for both handlers: 26/26 pass.
  `mypy` clean on both files. Targeted guidance/completeness/acceptance
  coverage suites (`claude_md_guidance`, `handler_completeness`,
  `declared_behaviour_matches_source`, `acceptance_test_coverage`): 269/269
  pass. Daemon restarted and verified RUNNING (31/31 listeners) so
  regenerated CLAUDE.md guidance reflects the new text.
- PLAN.md untouched (Task 4.1's soak continues per dispatch instructions).

## 20:25 · finding · T4.2 — third probe run: full convention compliance

- Post-tuning verbatim probe re-run: 86,719-char return blocked; the file
  landed at `untracked/agent-reports/260901-general-purpose-haiku.md` — the
  exact prescribed fallback path and filename convention, placeholders
  filled — with a three-line summary returned. Both T4.2 tuning findings
  verified closed; remaining T4.2 scope is passive soak across sessions.
- Journal hygiene note: this entry was first mis-inserted mid-file and
  removed again (two journal-append-only advisories fired); this bottom
  append is the correction. Entry timestamps in this file are not strictly
  monotonic because concurrent agents wrote with differing clocks.

