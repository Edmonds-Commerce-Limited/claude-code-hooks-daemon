# Plan 00415 — Journal 26-09-24

_Scaffolded by `mkplan.bash`; timestamps in this file are UTC._

> **Append-only activity log** for plan 00415. One file per day
> (`00415-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM, ONLY with
> `mkplan.bash --journal <plan-number> <category> <body-file>` (it stamps the
> real UTC time; a hand-written entry is denied); NEVER edit earlier entries
> (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` UTC 24h (the date is in the filename, also UTC); times increase
>   down the file. A file with no sentinel line above predates this system —
>   its times are local, zone unrecorded (legacy; never rewritten).
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 18:44 · action · —   — config fingerprint and combined verdict built, live-verified

Built on branch worktree-d-fresh under the three unattended rulings recorded in PLAN.md.

Reproduced first, against unchanged code: `check-source-fresh` exited 0 ("fresh") after `.claude/hooks-daemon.yaml` was edited under a daemon still reporting its startup fingerprint. `compute_current_project_fingerprint` also raised ValueError on a YAML syntax error, so "a freshness check never crashes on a broken config" was false for syntax errors; only schema errors were caught.

Defence Before Fix: the class is "a consumer reads one fingerprint out of the health payload and judges on it alone". The detector is the semgrep rule `freshness-verdict-read-piecemeal` (scripts/qa/semgrep/freshness-verdict.yaml), pinned by a planted fixture. Its first draft used a `$KEY` metavariable with a regex filter, and it TIMED OUT on daemon/cli.py during the directory scan. Semgrep reports a timeout as a warning, so the gate would have passed while blind. The shipped rule uses literal keys. It fired on the originating instance (cli.py:1245) and was committed red before the fix. Sweep by two techniques (semgrep over src/scripts, grep over tests): 3 consumer instances (cli.py, tests/acceptance/conftest.py, tests/acceptance/test_daemon_source_freshness.py), all fixed. `smoke_test` inherits the verdict through `check-source-fresh`.

Live end to end on the worktree daemon: a comment-only edit read FRESH three times; changing `idle_timeout_seconds` read STALE naming the config; a YAML syntax error was refused by the CLI's own config load with a named error, not a traceback; after the revert it read FRESH again.

Test adaptation: 7 start-path tests hand `_build_initialised_controller` a MagicMock config, which cannot be hashed; each now sets `model_dump.return_value = {}`.
