# Plan 00415 — Journal 26-09-15

> **Append-only activity log** for plan 00415. One file per day
> (`00415-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 14:46 · action · — — plan scaffolded

Plan 00415 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 14:55 · decision · — — graduated from ledger 00413 N17 rather than fixed in place

N17 was recorded with evidence and left unfixed deliberately: the remedy changes
the mechanism every acceptance test gates on, and 00413 had already rewritten a
lot of that surface in one pass.

Ledger 00413's own Non-Goals settle what to do with it — *"a niggle that needs
design graduates to its own numbered plan and leaves a pointer here"* — and this
needs design, not a patch. Two genuine forks, recorded as open questions in
PLAN.md: what to hash (file bytes versus the resolved model), and whether to
extend the existing fingerprint or publish a second one beside it.

**Failsafe recovery cron**: reused the existing session cron `7f2ce3b6` (hourly
at :47). `CronList` showed exactly one already present, so nothing was created —
one cron covers every plan in this session, and a second only double-fires.

## 14:56 · finding · — — the gap was deferred twice, not missed

The dedupe scout read 28 live plans and grepped 243 archived ones. Nothing live
covers this, and the prior art is pointed:

- **00371** built `compute_source_fingerprint` and placed config-only staleness
  in its non-goals, reasoning that it was "a different,
  already-advised-at-commit-time concern".
- **00395** detects a version change under a running daemon, and separately
  recorded that `.daemon-metadata.json` does not track `hooks-daemon.yaml`
  contents.
- **00389** reconciles config after a `git pull`, so it never sees a direct edit.

Three plans each stood next to this gap and stepped around it. Worth recording
because it changes what Task 1.2 is for: the reasons are already written down
and should be answered rather than rediscovered. 00371's is the one to engage
with — "advised at commit time" is true, but the acceptance harness does not run
at commit time, and it is the harness that consumes the verdict as a trust gate.
