# Plan 00313 — Journal 26-09-01

> **Append-only activity log** for plan 00313. One file per day
> (`00313-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 20:49 · action · — — plan scaffolded

Plan 00313 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 21:00 · action · — — plan filed from owner field report (mid-release)

- Owner tried a HOST-level Claude Code in this repo folder; the daemon was
  unimportable because the resolver reused the container-built
  `venv-workspace-py311-81c29529` (its editable `.pth` names
  `/workspace/src`). Root cause traced to `daemon/paths.py` resolution:
  the metadata step keys on lock_hash alone (identical across views) and
  the scan fallback takes the first executable `venv-*` regardless of the
  slug the naming design added precisely to separate root views.
- Session failsafe recovery cron: previous cron 892eef87 died with the
  container restart; recreated as `2bb01585` (hourly at :23).
- Dispatching a python-developer agent to execute Phase 1 (TDD fix).
  Dispatch declaration: reports to
  `CLAUDE/Plan/00313-venv-resolver-cross-view-reuse/subagent-reports/`.
