# Plan 00408 — Journal 26-09-24

_Scaffolded by `mkplan.bash`; timestamps in this file are UTC._

> **Append-only activity log** for plan 00408. One file per day
> (`00408-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM, ONLY with
> `mkplan.bash --journal <plan-number> <category> <body-file>` (it stamps the
> real UTC time; a hand-written entry is denied); NEVER edit earlier entries
> (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` UTC 24h (the date is in the filename, also UTC); times increase
>   down the file. A file with no sentinel line above predates this system —
>   its times are local, zone unrecorded (legacy; never rewritten).
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 20:05 · action · —   — Unattended pass: every assigned task terminal

Unattended pass by d-00408 on branch worktree-d-00408. All tasks are now terminal except Task 3.3, which was not in this pass's brief.

- Defects fixed test-first: T3.0 (plus the sibling sweep: eleven spellings in destructive_git, two in git_stash), T3.4, T3.5, T3.7, T3.8, T3.9 and T3.10. T3.2b is fixed with the new commit-stage check archival-links-resolve.
- T3.2 was built here too, then withdrawn before commit on the coordinator's instruction, because Plan 00466 N13 (branch worktree-n466-n13n14) delivers the same check. Two implementations of one check is forbidden. Mine covered no case theirs misses.
- While building T3.2, the live README's closing self-check was found stale: it read 454 + 13 = 467, but the figures above it give 455 + 13 = 468. The coordinator fixed it on main at cbe1c859.
- T2.1: merge_qa_report now follows the cold-index rule. Measured: 0.19s to refresh a warm index, 3.7s to build a cold one, against a 5s budget.
- T3.1: two items fixed (env, create_time) and two declined with reasons (tdd_enforcement casing, the config-path literal).
- T3.6: one item folded into release note 44, one convention recorded, and two declined because they would amend a published release.
- T1.1: done by a python-developer sub-agent; 35 modules now use HookInputField, and an AST test holds the line.
