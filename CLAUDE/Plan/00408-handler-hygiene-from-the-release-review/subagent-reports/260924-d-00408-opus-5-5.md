# Plan 00408: delivery report (d-00408, Opus 5.5)

**Branch**: `worktree-d-00408`. It is merged with main as of `3246f5b5`; the
one conflict (an import in `daemon/playbook_harness.py`) was resolved by
keeping both sides.
**Ready to archive**: no. Task 3.3 is still open. It was not in this pass's
brief, and nothing on this branch touches it.

## Per-task outcomes

| Task  | Outcome                                                                                                                                                                                                                    | Evidence                                                                                               |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| T1.1  | Fixed. 35 modules now read the hook payload through `HookInputField`. There were six `_CWD_FIELD` copies, not two.                                                                                                         | `tests/unit/constants/test_hook_input_field_single_source.py` (an AST scan of every `hook_input` read) |
| T2.1  | Decided. `merge_qa_report` follows the cold-index rule: it refreshes a warm index and stands down on a cold one. Measured on this repository: 0.19s warm, 3.7s cold, against a 5s budget.                                  | `test_a_cold_docs_index_is_not_built_inside_the_hook`                                                  |
| T3.0  | Fixed with a shared `remove_word_quoting`. The sibling sweep found 11 spellings in `destructive_git` and 2 in `git_stash`. The strict xfail now passes.                                                                    | `test_destructive_git_quoted_operands.py`, `TestGitStashQuotedWords`                                   |
| T3.1  | `env` and `create_time` fixed. The `_append_unique` casing item and the config-path literal were declined; the reasons are in PLAN.md.                                                                                     | `test_process_probe.py` (env shapes), `test_scan_survives_an_inaccessible_start_time`                  |
| T3.2  | Delivered by Plan 00466 N13 (branch `worktree-n466-n13n14`). My duplicate was withdrawn before commit. It covered no case theirs misses: both do edit ADVISE, commit BLOCK only when the index is staged, and sweep BLOCK. | pointer in PLAN.md                                                                                     |
| T3.2b | Decided: `archive-immutability` is the reason for the exclusion. The new COMMIT-stage check `archival-links-resolve` blocks a link the archival move broke and names the repoint.                                          | `tests/unit/plan_qa/checks/test_archival_links_resolve.py`                                             |
| T3.3  | Open. Not assigned.                                                                                                                                                                                                        | —                                                                                                      |
| T3.4  | Fixed. This is NOT ledger 00422 N28 (a relative mkdir resolved against the workspace root), which still waits on Plan 00464.                                                                                               | `test_a_quoted_creation_is_still_a_creation`                                                           |
| T3.5  | Fixed. The rule text now reads `cd`/`pushd`. `popd` and `cd -` were declined because they name no path.                                                                                                                    | `test_pushd_into_the_daemon_dir_is_matched` (formerly the xfail)                                       |
| T3.6  | Item 4 is folded into release note 44. Item 2's convention is recorded in `UNRELEASED/release-notes/README.md`. Items 1 and 3 were declined because they would amend a published release.                                  | PLAN.md                                                                                                |
| T3.7  | Fixed. `xargs git merge` is gated under the key `unnamed-branch`. `git pull <remote> <branch>` is gated too, unless the branch is the default branch itself.                                                               | `TestTwoMergesThatWereMissed`                                                                          |
| T3.8  | Fixed. `GitRepo.remote_urls()` reads every remote, and any one pointing at us engages the gate.                                                                                                                            | `test_a_clone_whose_only_remote_is_named_upstream_is_gated`                                            |
| T3.9  | Fixed. The option run and a `)`/backtick lookahead are in the pattern. Every quote and backslash is dropped from the path, and `//` and `/./` are collapsed.                                                               | `TestEverySpellingThatReallyChangesDirectory`                                                          |
| T3.10 | Fixed. A held lock whose pid cannot be read now reports `"unknown"`. The explicit `LOCK_UN` is gone.                                                                                                                       | `test_an_unreadable_lock_body_still_warns_with_an_unnamed_holder`, `test_a_failing_unlock_is_survived` |

Every behaviour change had a RED test first. For T3.2b the RED was at import
level, because the check module did not exist yet.

## Found on the way

- The live `CLAUDE/Plan/README.md` closing self-check was stale: it read
  `454 + 13 = 467`, but the figures above it give 455 and 468. It was reported
  to the coordinator and fixed on main at `cbe1c859`.

## Consolidation opportunity

Four guards now each normalise shell text for themselves.
`remove_word_quoting` in `utils/command_evasion.py` is used by
`destructive_git`, `git_stash` and `plan_number_helper`.
`daemon_location_guard` does its own blunter quote drop, `_path_text`. The
`utils/shell_lexer.py` on branch `worktree-plan-464-commit-gate-repo` keeps
each word's quoting as written, so once it merges, the right follow-up is for
all four to unquote the lexer's `Word` items instead of regex-scanning the raw
text. Task 3.3's allowlist of inert commands belongs on the same lexer (a
check on the segment head), not beside `strip_inert_spans`.

## QA

- `llm_qa.py format lint type_check pyright magic_values error_hiding docs_qa plan_qa`:
  all passed after the merge.
- pytest before the merge: `tests/unit` plus the affected integration files
  had 2 failures, both from the stale README above. After the merge,
  `tests/unit/plan_qa`, `tests/unit/constants`,
  `tests/unit/handlers/pre_tool_use` and `test_repo_hygiene_check.py` all
  pass.
- The worktree daemon was restarted before each commit and reported RUNNING.
