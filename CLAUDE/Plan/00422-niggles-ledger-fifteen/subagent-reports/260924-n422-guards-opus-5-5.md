# 00422 N25 and N29 — delivery report

**Branch**: `worktree-n422-guards` (worktree
`untracked/worktrees/worktree-n422-guards`), not merged, not pushed.
**Commits**, in order: `c503e590` N29 RED, `d2ed30a4` N29 fix, `6be0ae57` N25
RED, `319cdd35` N25 fix, `4abdbdbe` this report and the Security register
entry, `1cf338c6` follow-up RED (secret exemptions, subshell producers), then
the follow-up fix, which also updates this report.

## N29 — `write_clobber_guard` denied a rewrite of a file the session created

**Root cause.** `matches()` returned True only for a `Read` or for a `Write`
that would clobber. The chain calls `handle()` only when `matches()` is True.
The line in `handle()` that recorded an allowed `Write` sat under
`if not self.matches(...)`, so the daemon never ran it. A file the session
created with `Write` stayed unknown, and the next `Write` to it was denied.
It was not a session-identity or compaction problem. The unit test for the
promise called `handle()` directly, bypassing the gate, and passed.

**Fix.** `matches()` is True for every `Read`, `Edit` and `Write` that
carries a path. `handle()` records every allowed call and denies only a
`Write` that would replace an existing file the session does not know. A
denied `Write` is not recorded. Paths are normalised with `os.path.normpath`,
so `/a/b/../c` and `/a/c` are one record. Knowledge stays per session.

**Trade-off kept.** The record is taken at PreToolUse, as `Read` already was,
not at PostToolUse. A `Write` that a later-priority handler denies is still
recorded. That matters only if another actor then creates the file at the same
path. A PostToolUse recorder needs a second handler that shares state; the
ledger's candidate remedy suggested one if this is ever wanted.

**Defence (DBF).** The class: work in `handle()` under
`if not self.matches(...)`, which only a direct test call reaches. It is
caught by `scripts/qa/check_unreachable_handle_branch.py`, an AST rule wired
into `llm_qa.py` (`unreachable_handle_branch`) and `run_all.sh` (step 31).
Sweep: 8 such branches in `src/`. 7 are bare `return`s and 1 is the
originating instance; `.claude/project-handlers/` has none. The detector was
committed red first.

**Tests.** `tests/unit/handlers/pre_tool_use/test_write_clobber_guard_dispatch.py`
covers:

- Write-then-Write and Edit-then-Write in the same session: allowed.
- The same after another session's Write or Edit: denied.
- A denied clobber: not recorded.
- A `../` spelling of the same path: one file.

Every case goes through the matches-then-handle gate. The two existing suites
now judge decisions through that gate too; their four matches-means-deny
assertions encoded the defect. `tests/unit/scripts/test_unreachable_handle_branch_checker.py`
covers the detector.

## N25 — a shell reserved word read as the command

**Root cause.** Splitting `for f in a b; do grep x "$f" | head; done` on `;`
leaves ` do grep x "$f"`. `pipe_blocker._extract_producer` took that segment
whole, so the whitelist, anchored at `^grep`, missed. The unknown-command
template then offered `^do\b`.

**Audit** (Explore sweep, then each site probed through the worktree venv).
Besides `pipe_blocker`, 12 sites gave a different verdict behind a reserved
word:

- **Bypasses**:
  - `staged_lint_gate`
  - `merge_scope`, which feeds `merge_qa_report` and `daemon_sync_after_merge`
  - `verification_result_gate`, which feeds `bash_safe_mode`'s mutator scope
  - `compile_command_name_pattern`, used by `issue_filing_gate`,
    `quarantine_artefact_read_guard` and `command_hints`
  - `sed_blocker`
  - `worktree_file_copy`
  - `project_containment`
  - `bash_file_writes`, which feeds `plan_journal_guard`
- **False positives**:
  - `shell_segmentation._segment_command_word` (heredoc in a loop body)
  - `_segment_binary` (commit message inside an `if`)
  - `bash_flags` (`{ set -euo pipefail; }`)
  - `budget_exhaustion_detector`
  - both `secret_file_matching` exemption sites
- **Both directions**: `reference_repo_freshness`.
- **No effect**: sites with unanchored patterns, such as `destructive_git`,
  `git_stash`, `curl_pipe_shell` and `sudo_pip`. The full table is in the
  audit report, which the daemon saved outside git.

**Fix.** One primitive lives in `utils/command_evasion`:

- `SHELL_RESERVED_COMMAND_PREFIXES`: `do then else elif if while until ! { time`.
- `RESERVED_WORD_PREFIX`, which also accepts `time -p`.
- `COMMAND_POSITION`, which is `^\s*` plus the prefix plus `ENV_PREFIX`.
- `strip_reserved_word_prefix()`.

`(` is deliberately excluded: it is an operator, and a subshell's `set` does
not persist. The sites use the primitive as follows:

- The anchored patterns take `COMMAND_POSITION` or `RESERVED_WORD_PREFIX`.
- The first-word readers call `strip_reserved_word_prefix` first.
- `bash_flags` accepts only `{`, `!` and `time` before `set`, because a `set`
  behind `then`, `do` or `else` may never run.
- `pipe_blocker` handles a pipe fed by a whole compound command
  (`...; done | tail`, `fi`, `esac`, `}`) with its own deny. That deny names
  no whitelist line and says to move the pipe into the body or capture the
  whole output. The reserved-word set also backstops that routing, so a
  whitelist suggestion can never name one.

**Secret exemptions (follow-up, at the coordinator's direction).** Both
`secret_file_matching.is_exempt_invocation` and `is_encrypted_target_invocation`
(the caller that hands words to `_is_encrypted_target_reader`) now look past a
leading run of `time`, `time -p` and `!`. They do it through a second, narrower
primitive, `command_evasion.strip_transparent_reserved_words`. Those words
change neither which command runs, whether it runs, nor what it reads.

Every other reserved word stays fail-closed. `then`, `do`, `else`, `elif`,
`if`, `while`, `until` and `{` only occur inside a compound command, and both
exemptions accept exactly one simple command. Stripping those words would judge
a fragment of the compound as if it were the whole command.

A non-exempt command behind `time` stays denied: `cat`, `ansible-vault view`,
and a protected path in positional position. Note that the coordinator's
example, `ansible-vault view`, is never exempt even bare, since `view` is a
denied subcommand. The tests therefore use `ansible-vault encrypt`,
`ansible-playbook` and `secret-meta` as the exempt shapes. Declared row
`secret-exemptions-reach-the-transparent-word-strip` was added, red first.

**Subshell producers (follow-up).** `pipe_blocker._command_inside` strips
`(` openers and reserved words alternately until neither is left. It then drops
the unmatched `)` closers. `(grep x f) | head`, `( (grep x f) ) | head`,
`((grep x f))`, `( ! grep x f )` and `(pytest | grep FAIL) | head` are allowed.
`(pytest) | head`, `( (pytest) ) | head` and `(cd src && pytest) | head` are
denied and named as pytest. An unknown producer's suggestion and capture
snippet carry no parentheses. `(` stays out of the shared primitive.

**Defence (DBF).** 9 rows were added to `scripts/qa/declared-invariant-pairs.yaml`:

- 6 `reaches: strip_reserved_word_prefix`.
- 3 `interpolates` of `COMMAND_POSITION` or `RESERVED_WORD_PREFIX`.

They were committed red first (`6be0ae57`, 9 violations) and are green after
the fix. The `319cdd35` commit message says 10 rows; the true count was 9.
The follow-up adds a tenth row (secret exemptions), so 18 rows hold in total. The instance is recorded in `CLAUDE/Security/AsymmetricSiblingProtection.md`.
Blind spot: a new site that reads a first word is not covered until someone
adds a row for it.

**Tests.**

- `tests/unit/utils/test_reserved_word_command_heads.py` checks each audited
  site against 11 wrappings (for/do, then, else, elif, if, while, until, !,
  `{ }`, time, time -p). The verdict must match the unwrapped command. It
  also checks the primitive itself, and that a conditional or subshell `set`
  is still not credited.
- `tests/unit/handlers/pre_tool_use/test_pipe_blocker_reserved_words.py`
  checks:
  - the field report;
  - each reserved word: a whitelisted producer allowed, `pytest` still denied
    and named as itself;
  - an unknown producer suggested by its own name, never the reserved word;
  - the compound deny naming no whitelist line, in both verbose and terse
    form.

**Live check.** The PreToolUse payloads were fed straight to the worktree
daemon, because this session's hook traffic goes to the main daemon (N23).
The loop-body `grep | head` was allowed, and `...; done | tail -5` got the
compound deny. In the follow-up, `( (grep x f) ) | head` was allowed and
`(pytest tests/) | head` was denied. The follow-up probes carried the
`synthetic_source` marker (00466 N12); the first two did not, so they are
logged as real traffic in the worktree daemon's `verdicts.jsonl`.

## QA (targeted, as briefed; no `llm_qa.py all`)

- These checks pass: `format`, `lint`, `type_check`, `pyright`,
  `magic_values`, `error_hiding`, `declared_invariant_pairs` (18 rows),
  `dangerous_invocation_corpus` (20 rows), `unreachable_handle_branch`
  (612 files) and `handler_reference`.
- pytest passed on:
  - the new suites;
  - the existing suites of every touched module (2201 passed);
  - after the follow-up, `tests/unit/handlers`, `utils`, `core`, `scripts`
    and `qa` (12905 passed, 1 skipped, 3 xfailed);
  - integration tests matching secret, pipe, exempt or vault (193 passed);
  - the integration and daemon tests that name `write_clobber_guard`
    (693 passed).
- The worktree daemon was restarted and reported RUNNING before each commit.

## Release notes

- `CLAUDE/UPGRADES/UNRELEASED/release-notes/21-guards-now-see-a-command-behind-do-then-and-other-shell-reserved-words.md`
- `CLAUDE/UPGRADES/UNRELEASED/release-notes/22-rewriting-a-file-you-created-or-edited-is-no-longer-denied-as-a-clobber.md`

## Worth knowing at integration

- The N25 fix makes several guards fire on commands inside loop and `if`
  bodies that they missed before. That is intended, and release note 21 says
  so.
- The coordinator queue plans to merge three shell parsers after 463, 464 and
  this branch land. This branch adds two primitives to `command_evasion`:
  `strip_reserved_word_prefix` and `strip_transparent_reserved_words`. It also
  adds one pipe_blocker-local subshell resolver. All three belong in that
  merged parser.
