# Plan 00422 — Journal 26-09-18

> **Append-only activity log** for plan 00422. One file per day (UTC).
> Entries are headed `## HH:MM · category · REF` with times increasing down
> the file; corrections are new entries, never rewrites.

## 06:15 · handoff · — — the ledger is now blocked only on the owner

Three plans closed entries this session: 00444 (N14's two `gitfacts` edges),
00445 (N6 fault 2) and 00446 (N15). Current standing across all fifteen
entries:

- **Terminal (11)**: N2, N5, N6, N8, N9, N10, N12, N13, N15 resolved or
  closed; N3 and N14 have their remediable halves delivered.
- **Owner-gated, stated and waiting (4)**: N1 (Priority constants), N4
  (session-scoped cron pause), N7 (unattributed effort drop), N11
  (`untracked/acceptance/` root).
- **Residues that are design questions, not work items (2)**: N3 item (2),
  whose advisory has expired; and N14's two declared edges
  (`utils/goal_ledger.py` → `plan_qa.model`/`PlanDoc`, and
  `docs_qa/checks/module_doc_budget.py` → `plan_qa.types` tier constants).

**There is no remaining entry an agent can close without an owner decision.**
That is a block, not a completion, and the distinction matters: this ledger
stays OPEN as the destination for new niggles, so a future session should
append to it rather than treat it as finished. What it should NOT do is
re-loop the six remaining entries looking for work — they were each stated
once, and re-deriving them costs context and changes nothing.

**A note for whoever answers the four questions**: they are genuine choices
with more than one defensible answer, which is why they were escalated rather
than decided. Answering any one of them unblocks that entry alone.

## 06:15 · finding · N15 — the remedy menu was wrong in a way worth generalising

Recorded in full on the N15 entry. Both listed remedies were costed against
the wrong surface — one against a CALL rather than its COVERAGE, the other
against a payload field the event does not carry.

Generalising: this ledger's entries are written at the moment a defect is
SEEN, and at that moment the remedy is a guess made without opening the code
the remedy would touch. The diagnosis half has been reliable all session; the
remedy half has now been wrong or mis-costed often enough that it should be
read as a starting hypothesis rather than a plan. That is the eleventh
instance of the dominant theme, and the first time the theme has landed on the
ledger's REMEDIES specifically rather than its premises.
