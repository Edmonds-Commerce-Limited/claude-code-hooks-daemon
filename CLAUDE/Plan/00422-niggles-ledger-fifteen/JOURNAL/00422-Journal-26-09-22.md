# Plan 00422 — Journal 26-09-22

_Timestamps in this file are UTC._

## 18:38 · finding · N16 — two hourly crons, one protected, one not

Filed **N16** from the receiving end of three consecutive no-op `issue-sdlc`
ticks in one session.

Verified before recording, not asserted:

- `failsafe_cron_blockage_suppressor` recognises a delivered tick by
  `CANONICAL_CRON_PROMPT_MARKER` (lines 197 and 269) and can drop it before it
  reaches the model, under two rules — `R-FAILSAFE-CRON-SUPPRESSED` (an
  `[awaiting-human]` marker is live) and `R-FAILSAFE-CRON-BACKED-OFF` (session
  producing nothing, owes no ledgered work).
- Cadence state is failsafe-scoped by filename:
  `CADENCE_FILENAME = "failsafe-cron-cadence.json"` in `utils/cron_cadence.py`.
- Grepping `src/` for the `issue-sdlc` tick prompt returns two hits and
  **neither is suppression** — `issue_filing_gate.py` mentions it in a deny
  reason, `cron_enforcement.py` uses it for declared-vs-delivered
  reconciliation.

The tick observation behind it: preconditions green, whitelist skipped 0 of 10,
and all three selection rules empty because **all ten open issues carry
`agent-needs-human`**. The loop is saturated by construction and will re-derive
that hourly until a human clears the gate.

## 18:38 · decision · N16 — the remedy is deliberately NOT symmetric

Recorded the asymmetry in the entry rather than proposing "do what the failsafe
cron does". The failsafe backoff reads purely LOCAL state, so it decides at
`UserPromptSubmit` for zero tokens. Issue eligibility is REMOTE state, and a
handler that queried GitHub on the prompt path would be a worse trade than the
one `cron_cadence` makes.

Costing it against the failsafe implementation would repeat N15's error
exactly — that entry closed with "neither listed remedy as written (both were
costed against the wrong surface)". Naming the difference up front is the whole
value of the entry.

Cheapest remedy carried into Task 4.3: have the AGENT write a consecutive-no-op
marker at the moment it concludes "no eligible issue", reusing the
`[awaiting-human]` marker mechanism, so the suppressor can back off on local
state alone.

**Accepting the current cost is an explicitly valid outcome.** The defect is
that the asymmetry exists by omission rather than by decision — not that the
hourly cadence is wrong. Neither cron should be deleted; both runbooks say so.

## 18:38 · thought · — — plan-doc-size advisory, not acted on

Adding the N16 row and Task 4.3 pushed `PLAN.md` from 19,512 to 20,035 bytes,
over the 18,000-byte advisory threshold. Not acted on in this tick.

The bulk is the Niggles table's Status column, where one-line summaries have
grown into paragraphs — each of which already has a full write-up in
`NIGGLES.md`, so trimming them back to one line is the correct remedy and loses
nothing. Deliberately not done here: this is a Blocked, owner-gated ledger and
rewriting sixteen of its rows during an automated tick is scope the tick does
not carry. Flagged for the owner instead.
