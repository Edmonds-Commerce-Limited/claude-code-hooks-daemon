# Plan 00422 — Journal 26-09-17

> **Append-only activity log** for plan 00422. One file per day
> (`00422-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 10:52 · finding · N2 — three of this ledger's entries were never owner-gated

While walking the owner through what is blocked, I checked the entries' own
gating notes instead of repeating my earlier summary. N2 and N3 say "Not
owner-gated" outright and N6 says "Remedies 1–3 are un-gated". I had been
reporting the ledger as owner-gated wholesale across several stop lines, which
was wrong and left buildable work sitting still.

## 10:54 · finding · N2 — the recorded remedy was a no-op as written

N2's remedy (1) says to add `untracked/` to `exclude_paths`. Doing exactly that
and restarting changed nothing: the same scratch file was still denied under
`R-LINT-FAILURE`.

`_glob_to_regex` full-matches, so `untracked/` becomes `(?:.*/)?untracked/`,
which matches the directory path and never a file inside it:

```
'untracked/'       -> False
'untracked/**'     -> True
'/untracked/**'    -> True
'untracked'        -> False
```

This is the failure mode worth naming: the config edit LOOKS like the recorded
remedy, the daemon accepts it, nothing errors, and the guard carries on firing.
Had I written the line and moved on, N2 would have been marked resolved while
staying broken.

## 10:56 · action · N2 — resolved, verified in both directions

Shipped `/untracked/**` under `lint_on_edit.options.exclude_paths` — `**` to
reach the files, leading `/` to anchor at the repo root. Scoped to the handler
rather than `daemon.exclude_paths`, which would have exempted the directory
from every guard including `sensitive_content`.

Proved by writing, not by reasoning: the identical lint-failing bytes that were
denied twice were accepted after the restart. Opposite direction checked too —
`src/**`, `tests/**`, `CLAUDE/**` and a nested `sub/untracked/**` all still
lint, so the exclusion did not leak.

## 10:58 · finding · N3 — the nominated regression file fails the entry's own premise

N3 reads as a clean un-gated fix until you run it. Its regression file carries
**11** ordering regressions and only ONE is the future-dated case:

```
entries=20 regressions=11
  10:50 after 11:18   <- the future-dated correction
  ...ten more, all at stream joins
```

Three agents' streams were merged into that day-file, so the clock resets at
each join. The entry's closing criterion — "if the advisory still fires on that
day-file afterwards, the remedy did not work" — is therefore unreachable by
either named remedy, because ten findings have nothing to do with future-dating.

Two further things fell out of trying to implement it:

- **Remedy (1) cannot work at sweep stage at all.** It needs to know which
  entries are future-dated, but nothing in a file carries a machine-readable
  flag, and future-datedness cannot be recomputed later — every entry in a
  2026-09-16 file is in the past by the time a sweep reads it. That is the very
  reason `journal-entry-future-dated` is EDIT-only.
- **Remedy (2) is bigger than the entry assumed.** A `correction` category
  means editing the entry grammar in `_JOURNAL_TEMPLATE_.md`, which has two
  copies — one under `install/templates/`, shipped to every client.

So "not owner-gated" no longer clearly holds for N3, and it is the same shape
as the template question Plan 00427 raises for issue #45. Stopping here to ask
rather than pick a grammar change for every install unilaterally.

## 11:00 · action · N6 — fault 1 fixed, and the entry's own arithmetic corrected

`setup_worktree.sh` now measures the socket path before creating anything.

The entry says the cap is 108 and the path is "one byte too long". The enforced
limit is `_UNIX_SOCKET_PATH_LIMIT = 104` — macOS-safe, not Linux's 108 — so it
is 5 bytes over:

```
limit = 104
 109 over=  5  worktree-issue-42-remote-docs-add-overwrite
 100 over=  0  worktree-issue-44-plugins-examples
  85 over=  0  worktree-plan-00028
```

The middle row is a branch used earlier the same day, clearing the cap by four
characters. The margin is much tighter than "extreme paths only" implies.

The check calls the daemon's own helpers rather than reimplementing the rule in
bash, so it cannot drift. `get_socket_path` itself was unusable for this: it
sniffs self-install mode off disk and mkdirs as a side effect, so it cannot
answer for a directory that does not exist yet. Two small pure functions were
added instead — `prospective_socket_path` and `socket_path_overflow` — with the
limit still read from the one constant.

Both directions verified end to end, not just the failing one: the N6 branch is
refused at 109/104 with no directory and no branch created; `worktree-n6ok`
prints `✓ Socket path fits (79/104 bytes)` and reaches `=== Worktree Ready ===`.
That worktree was then removed (no commits of its own; the reaper's 900s
minimum age refused it, which is the right default — it cannot tell a fresh
start from a rebased one, but I created it 30 seconds earlier and could).

The guard fails OPEN: no resolvable Python, or a measurement that will not run,
warns and continues. A broken checker must not block worktree creation.

`tests/unit/daemon/`: 1989 passed, 1 skipped.

## 11:18 · finding · N7 — a remedy the entry missed, found sideways

While measuring MAIN/SUB discrimination for Plan 00423, a captured
`SubagentStop` payload turned out to carry `"effort": {"level": "medium"}`.

N7's three remedies all try to infer the effort level from the ACT of setting
it — and the selector produces no keystroke to infer from, which IS the niggle.
The daemon is handed the RESULT on every Stop/SubagentStop regardless of route,
so reading the state instead of the act makes the two paths indistinguishable.

Recorded as remedy 4, explicitly NOT as a settlement of the owner's open
question (whether an unexplained change counts as human — the payload says what
the level is, not who set it). Also recorded as unverified: whether the
supervisor process can see that payload at all, or whether a daemon-side
handler would have to relay it. That check comes before it is a candidate.

Worth noting how it was found: not by working N7, but by measuring an unrelated
plan and reading the whole payload rather than the one field I came for.

## 11:19 · thought · N3 — I just committed N3's own mistake, live

Writing the entry above, I inserted it after the 10:58 entry instead of
appending, and the checks fired immediately:

```
[advise] journal-entry-ordering: `11:00` appears after `11:17`
[advise] journal-append-only: Edit rewrites earlier journal history
```

Two things worth recording, because this is the entry N3 is about and I walked
into it while documenting it.

First, the guard worked exactly as designed and at the right moment: at EDIT
time, before the bytes were committed. N3's whole complaint is that once such
an entry reaches history it can never be corrected — and the reason I could
simply move mine is that it had not landed yet. That is the same distinction
N3's own upstream-cause note draws about heredoc appends bypassing the
Write/Edit guards: caught early is cheap, caught late is permanent.

Second, it is a small piece of evidence for Plan 00427's direction. I knew this
rule, had just written 300 lines about it, and still put the entry in the wrong
place by hand. A scaffolder that appends mechanically removes the judgement
call rather than reminding me to make it correctly.

## 11:51 · finding · N8/N9   — two defects I introduced today

Recorded N8 and N9, both defects I introduced today.

N8 — the socket-path guard I shipped this morning fails OPEN when it cannot
resolve a Python, and a fresh worktree has no venv. So it stood down on a
130-byte path (26 over the cap) while reporting healthy on every short path.
Inert exactly where it was needed, healthy everywhere else — the worst
distribution for a guard, because casual testing always passes.

N9 — `isolation: "worktree"` already gives an agent its own checkout, and my
brief then told it to run setup_worktree.sh, nesting a second worktree inside
the first. That produced N8's long path. Neither side can see the other:
the agent cannot tell its cwd is already an isolation worktree, and the script
does not know it is being run inside one.

Also cleared the duplicate-block advisory this ledger had been carrying: the
measurement fence lived in both NIGGLES.md and the journal. Resolved by
pointing at the journal rather than quoting it — an ssot-quote marker would
have needed a slugified anchor for a heading full of `·` and `—`, which is
guesswork, and it would have traded an advise-level finding for a
block-level quote-drift risk. Docs QA now reports 0 findings.

## 11:52 · finding · N8   — the remedy I recorded did not work as written

Correcting N8's remedy (1), which I recorded an hour ago without measuring it.

I wrote that the guard could run its measurement under system `python3` with
`src` on `PYTHONPATH`, "because the two helpers import only stdlib". The
premise is true of `paths.py` and the conclusion is still false:

```
PYTHONPATH=/workspace/src python3 -c "from claude_code_hooks_daemon.daemon.paths import ..."
ModuleNotFoundError: No module named 'pydantic'
```

Importing by PACKAGE path executes `claude_code_hooks_daemon/__init__.py`,
which imports `FrontController`, which imports pydantic. The module's own
imports were never the binding constraint — the package's were.

Loading the FILE directly does work, under bare `python3` with no venv:

```
direct module load: OK
  limit   = 104
  overflow= 0
```

So the remedy survives, but only in the `importlib.util.spec_from_file_location`
spelling, not the obvious one. N8 now records both, because an entry that names
a remedy which fails on first contact is worse than one that names none.

Worth noting how this surfaced: a hedging-language nitpick fired on my own
summary for asserting the stdlib claim confidently. The claim was checkable in
one command and I had not checked it.

## 12:22 · finding · —   — N2's remedy disabled the handler's own acceptance probes

The full QA run for Plan 00427 found a regression I shipped from this ledger.

34/35 checks passed; `tests` failed with 3 of 24975. One was mine and trivial
(a release note's `**Audience**` is a closed vocabulary — `operators | handler authors | client projects | everyone` — and I wrote free text). The other two
were N2.

N2's remedy, `lint_on_edit.options.exclude_paths: ["/untracked/**"]`, also
excluded `untracked/scratch/acceptance-test-lint-<lang>/`, where this very
handler's acceptance probes write their fixtures. So the exclusion switched the
handler off for its own eight declared DENY probes:

```
library:PostToolUse/LintOnEditHandler::Python lint - invalid code blocked:
  expected_decision=DENY but matches() returned False for its own declared input
```

**What I want to remember about how this got past me.** I closed N2 saying
"verified in both directions, by writing the file rather than by reasoning",
and that was true — I wrote a lint-failing file and watched it not be blocked,
then confirmed `src/**`, `tests/**`, `CLAUDE/**` and `sub/untracked/**` were
still linted. Every one of those paths was a path I thought of. Verification
that samples your own hypothesis cannot find the case outside it. What found
this was a test that enumerates the handler's declared inputs rather than mine.

The gap was also invisible for a whole session: plan QA and docs QA both
reported 0 findings across it, because neither drives a handler. A config
change that disables a guard is visible only to the check that exercises the
guard — worth remembering before treating "plan QA and docs QA are clean" as
coverage for a config edit again.

Repaired with depth-scoped patterns, measured against the real matcher BEFORE
shipping this time:

```
                                              /untracked/**   scratch/*
untracked/scratch/rung.py         (N2 case)   EXCLUDED        EXCLUDED
.../acceptance-test-lint-python/invalid.py    EXCLUDED        linted
.../acceptance-test-eslint-bash/authored.ts   EXCLUDED        linted
src/**, tests/**, sub/untracked/**            linted          linted
```

The dialect has no negation — `path_matches_globs` returns True on ANY match,
`merge_exclude_patterns` only unions — so depth is the only discriminator
available. That it works is a coincidence of layout, which is N11.

Two niggles appended: N10 (a QA checker's tests overwrite the real QA artefact,
which can mask a genuine failure) and N11 (probe fixtures share the human
scratch directory, which is what made N2 possible at all).

Daemon restarted; the three previously-failing modules now pass, 14 tests.
