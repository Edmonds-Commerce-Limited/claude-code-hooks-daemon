# Plan 00422 — Journal 26-09-24

_Scaffolded by `mkplan.bash`; timestamps in this file are UTC._

> **Append-only activity log** for plan 00422. One file per day
> (`00422-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` UTC 24h (the date is in the filename, also UTC); times increase
>   down the file. A file with no sentinel line above predates this system —
>   its times are local, zone unrecorded (legacy; never rewritten).
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 13:53 · action · — — plan scaffolded

Plan 00422 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 13:53 · action · —   — Wired shellcheck into llm_qa.py's TOOL_REGISTRY

Wired shellcheck into `llm_qa.py`'s `TOOL_REGISTRY` so the suite every agent
runs as "full QA" (`llm_qa.py all`, mandated by `enforce_llm_qa`) actually
covers it, closing the gap where only CI's separate `shellcheck` step ran it
and only after a merge.

- Added `tests/unit/qa/test_llm_qa_run_all_wiring.py`: a class guard that
  parses every `"${SCRIPT_DIR}/<script>"` invocation out of `run_all.sh` and
  asserts each has a matching `TOOL_REGISTRY` entry (or a named, reasoned
  `_KNOWN_GAPS` exclusion). It failed today on exactly `run_shell_check.sh`
  before the fix, confirming this was the only gap between the two suites.
- Added a `shell_check` `ToolConfig` wrapping `run_shell_check.sh`, in
  `run_all.sh` step-8 order, plus a `_summarize_shell_check` summariser
  reporting the issue count and files checked.
- Confirmed the missing-shellcheck path still fails VISIBLY: with shellcheck
  hidden from `PATH` (coreutils kept via a symlinked scratch bin so the
  script could still run), `run_shell_check.sh` exits 1 and writes
  `summary.passed: false` with `summary.error: "shellcheck not installed"`;
  `summarize_tool("shell_check", exit_code=1)` reports `passed=False` with a
  `⚠️ TOOL ERROR: shellcheck not installed` line — `_report_error`'s existing
  `("summary", "error")` path already covered this script's nested-error
  shape, so no new plumbing was needed for the fail-visibly requirement.
- `tests/unit/qa/` (651 tests) is green; `CLAUDE/QA.md` already states no
  hardcoded check count, so it needed no edit.
