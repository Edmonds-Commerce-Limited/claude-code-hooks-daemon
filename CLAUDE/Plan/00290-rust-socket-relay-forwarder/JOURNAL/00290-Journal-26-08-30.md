# Plan 00290 — Journal 26-08-30

> **Append-only activity log** for plan 00290. One file per day
> (`00290-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 08:54 · action · — — plan scaffolded

Plan 00290 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 09:02 · decision · — — plan filed, scope and origin recorded

Dedupe scout checked all 35 live plans: no existing plan covers this work
(closest prior art is completed Plans 00154/00156, which this builds on).

Origin: owner idea ("could we create sockets — on linux sockets are files —
and have the sockets consume the hook directly?"). In-session assessment:
the literal exec-a-socket form is impossible (Claude Code fork/execs the hook
command), but per-event sockets capture the intent and make 00154's Option C
relay dumber (no JSON wrapping). Owner approved: Rust relay, config-driven
fallback ladder (relay → `nc -U` → bash+python3), opt-in for this release.

Measured groundings (this machine): python3 transport spawn 13.5 ms median,
`nc` 0.7 ms, `bash -c true` 0.9 ms; installed `nc` is Unix-capable (`-U`);
no `socat` present.

Failsafe recovery cron: reusing the session's existing recurring cron
(id `0837855f`, hourly at :37) — verified via CronList; nothing created.

## 09:40 · action · P1 — execution started; Rust toolchain provisioned

Owner confirmed mid-session to proceed with implementation now. Container had
no Rust toolchain; provisioned ad hoc (owner authorised root installs +
Dockerfile changes): rustup stable 1.98.0 minimal profile,
`x86_64-unknown-linux-musl` target, apt `gcc` + `musl-tools`. Smoke-tested a
static musl hello build (451,200 bytes stripped, runs). Toolchain baked into
`.claude/ccy/Dockerfile` for image durability (commit d0677e5e). Status →
In Progress; Phase 1 design next via sub-agent orchestration.

## 10:05 · action · T1.1 — Phase 1 design delivered (fork agent)

`DESIGN-socket-relay.md` written after reading the real code: `daemon/server.py`
(single-socket bind/flock/liveness, newline-JSON protocol, `_process_request`),
`daemon/paths.py` (`get_socket_path`, hostname suffix, 108-byte limit
fallback), `.claude/init.sh` (`ensure_daemon`, jq-free `send_request_stdin`,
`emit_hook_error`, 30 s `CLAUDE_HOOKS_SOCKET_TIMEOUT`),
`install/client_owned_assets.py` (forwarders are deployed 1:1 client assets),
`config/models.py` (`DaemonConfig`/`Config`, `LayoutConfig` `extra="forbid"`
precedent), `constants/events.py` (`wired_event_metas()` as the socket set).

Key design choices (full rationale in the doc): per-event sockets at
`{untracked}/events{suffix}/{event}.sock` gated on any rung being enabled;
EOF-delimited framing so the relay is a pure byte pump; relay connects BEFORE
reading stdin so connect-fail can exec the bash forwarder with stdin intact;
mid-exchange failures emit fail-open `{}` (bash owns all other error
messaging); config under `daemon.transport` (not top-level — transport has a
natural home); settings.json untouched — the hot path is a generated pure-
builtin guard above `source init.sh`, omitted entirely when disabled so the
deployed forwarder is byte-identical to today; distribution via release
`SHA256SUMS` + plain-`rustc` build-from-source (no cargo, no crates).
Tasks 1.1/1.2 ✅.

## 10:18 · decision · P5 — owner ruling on distribution recorded

Owner (verbatim intent): clients get the OPTION of compiling themselves or
downloading precompiled from GitHub; source ships either way as it is in the
package anyway; being open source matters and precompiled binaries are a bit
of a departure. Recorded in PLAN.md Phase 5: build-from-source is the
first-class route, download is the convenience option, both explicit in
config, nothing implicit. Phases 2 (listeners) and 3 (relay) are running in
parallel agents at this point.

## 09:38 · action · P3 — Phase 3 delivered: relay binary, tests, build script

(Container clock reads 09:38 UTC while earlier entries say 10:18 — the agents
have skewed clocks; this entry is appended after them in real order.)

Tasks 3.1–3.3 shipped by the Phase 3 agent:

- `relay/hooks_relay.rs` (~290 lines): std-only, zero crates, no cargo.
  Connects BEFORE reading stdin; on any connect failure execs
  `/bin/bash <fallback> --no-relay` with stdin intact; after connect it is a
  pure byte pump (stdin → socket, `shutdown(SHUT_WR)` as EOF framing,
  response buffered then copied to stdout). One timeout budget spans the
  whole exchange (connect via helper thread + `recv_timeout`, since std's
  `UnixStream::connect` has no timeout; per-op `SO_RCVTIMEO`/`SO_SNDTIMEO`
  armed with the shrinking remainder). Mid-exchange failures emit fail-open
  `{}` + exit 0; diagnostic exit codes 10/11/12 (connect/timeout/IO+oversize)
  only without a fallback / with `--no-fallback`, per design §3.1. Response
  capped at 16 MiB (mirrors `SocketLimit.REQUEST_BUFFER_BYTES`).
- `relay/test_relay.py`: stub Unix-socket server speaking the EOF framing;
  9 cases — happy path, daemon-absent → fallback exec (argv `--no-relay` and
  stdin integrity asserted), connect-fail diagnostic exit 10, timeout →
  `{}`/exit 11, mid-exchange disconnect (4 MiB payload → EPIPE) → `{}`/exit
  12, 2 MiB payload+response round-trip (sha256-verified), oversized
  (16 MiB+1) response → exit 12. **9/9 pass** against the built binary.
- `relay/build.sh`: plain `rustc --edition 2021 -O -C strip=symbols
  -D warnings --target x86_64-unknown-linux-musl` + `strip`, asserts no ELF
  interpreter (static), prints size + sha256. **Built: 570,056 bytes
  stripped** (under the 1 MB ceiling; sha256 f6484c23eb2d…). Output stays in
  git-ignored `untracked/relay-build/` — no binary committed.

Deviations/notes: Task 3.2's "daemon-side acceptance test driving a real
event end-to-end" is deferred to Phase 6 (Task 6.2) per the coordinator's
scoping — nothing was wired into the live daemon config. aarch64 build
awaits a builder (design §3.2). The CI job is recorded as a note in design
§3.2 rather than as CI config (pipeline wiring is Phase 5, Task 5.1).

## 11:20 · action · P2 — Phase 2 delivered: multi-socket listener + hygiene

Tasks 2.1–2.2 shipped (TDD, tests-first):

- `config/models.py`: new `TransportConfig` (`extra="forbid"`) with
  `relay_enabled`/`nc_enabled`/`timeout_seconds`/`relay_binary` exactly per
  design §4, plus a `per_event_sockets_needed` property; attached to
  `DaemonConfig.transport`. Defaults verified byte-identical (13 tests,
  `tests/unit/config/test_transport_config.py`).
- `daemon/paths.py`: `get_event_socket_dir_from_untracked`,
  `get_event_socket_dir(project_dir)`, `get_event_socket_path_in_dir`,
  `get_event_socket_path(project_dir, event_file_name)` — mirrors
  `get_socket_path`'s hostname-suffix + `_UNIX_SOCKET_PATH_LIMIT` fallback
  (returns `None` when even the events dir would overflow, per §1.1). 8
  tests (`tests/unit/daemon/test_event_socket_paths.py`).
- `daemon/server.py`: `HooksDaemon._bind_event_sockets()` (called from
  `start()` right after the legacy socket is chmod'ed, so a start that loses
  the reuse race never reaches it), gated on
  `transport.relay_enabled or nc_enabled`; wholesale-removes a pre-existing
  `events{suffix}/` dir, then binds one `asyncio.start_unix_server` per
  `wired_event_metas()` entry via `functools.partial(self._handle_event_client,
  meta.json_key)` — best-effort per socket (an `OSError` on one bind logs
  ERROR and continues, matching §1.2). `_handle_event_client` implements the
  EOF framing from design §2: reads to EOF via a new bounded
  `_read_event_payload` helper (asyncio's `read(-1)` doesn't honour the
  stream `limit` the way `readline` does, so the cap is enforced by hand at
  `SocketLimit.REQUEST_BUFFER_BYTES`), parses the raw hook payload, synthesises
  `{"event": <json_key from listener identity>, "hook_input": ...}` and
  dispatches through the SAME `_process_request` the legacy socket uses — no
  new dispatch path. Malformed/oversized payloads and any handling exception
  fail open with `{}` (never leave Claude Code without valid JSON). `shutdown()`
  now closes every per-event server and `shutil.rmtree`s the whole events dir
  (unlinking each socket individually would be redundant once the directory
  is gone). Legacy socket protocol byte-for-byte untouched — verified by a
  dedicated test connecting to both sockets in the same run. 7 tests
  (`tests/unit/daemon/test_event_socket_listeners.py`), real asyncio Unix
  sockets, no mocking of the framing/dispatch path.
- `daemon/cli.py`: `cmd_status` now prints
  `Per-event listeners: N active (<dir>)` (or a "transport enabled but events
  dir not found" line) when `daemon.transport` has a rung enabled; silent
  (byte-identical to today) when disabled. 3 tests
  (`tests/unit/daemon/test_cli_status_event_listeners.py`). `cmd_health`
  (the `_system`/`health` socket query) was left untouched — enriching it
  would mean threading transport state through `DaemonController.get_health`,
  a larger change than Task 2.2 scoped; flagging for Phase 6 or a follow-up
  if the health JSON payload needs the same visibility, not just the human
  CLI table.

QA: `tests/daemon/test_server.py`, `tests/unit/daemon/`, `tests/unit/config/`,
`tests/daemon/test_paths.py`, and the touched `test_cli_*` files all green —
1649 + 92 + 31 = full suite passed, 2 pre-existing skips (root-only
environment quirks), 0 failures. `mypy` clean on `server.py`, `paths.py`,
`cli.py`, `models.py`. `ruff` clean on every touched file. `black --fast
--target-version py311` reformatted the two newest test files (line wraps
only, no logic change) — Python on this container is 3.11 but repo's
configured target is 3.13, so `--fast` was needed to skip black's AST
safety re-parse (documented quirk, not a deviation).

No config-driven default behaviour change: with `daemon.transport` at its
defaults (both rungs off) the daemon binds only the legacy socket exactly as
before — verified directly by
`test_disabled_transport_binds_no_event_sockets`. Phase 4 (bash forwarder
integration) and `relay/` remain untouched, per scope.

## 14:05 · action · P4 — Phase 4 delivered: bash forwarder integration (TDD)

Tasks 4.1–4.2 shipped, plus the design §6.3 installer probe. Note: clock
skew against the entries above (this agent's wall clock reads later in the
day) — appended at the bottom regardless, per journal convention.

**Task 4.1 — forwarder generation, config-driven, byte-identical by
default.** New `install/forwarder_generator.py`:

- `generate_forwarder_content(source, event_file_name, transport,
  untracked_dir)`: two INDEPENDENT, idempotent transforms, each gated on its
  own flag. `relay_enabled` inserts `build_relay_guard_block`'s pure-builtin
  guard (design §6.1 verbatim: `--no-relay` re-entry check, literal
  install-mode-aware `_rl_dir`, bash-builtin hostname-suffix reproduction
  `${HOSTNAME:-localhost}` lowercased/space-hyphenated, `-x`/`-S` gate, exec
  with `--fallback "${BASH_SOURCE[0]}"`) directly above `source init.sh`.
  `nc_enabled` appends the event's bash_key as a trailing literal arg to the
  file's `send_request_stdin`/`forward_stop_event` call
  (`append_nc_socket_arg`) — sidesteps needing a PascalCase→kebab mapping
  table at runtime, since each forwarder already knows its own filename at
  generation time. Both no-op when their flag is false; verified by pinning
  EVERY real deployed `.claude/hooks/*` file byte-identical under default
  config, and syntactically valid (`bash -n`) under every flag combination
  (148 cases across the two test files).
- `regenerate_deployed_hooks`/CLI `main()`: rewrites an already-deployed
  hooks dir in place, no-op when config resolves to defaults or is absent
  (fail-safe). Wired into `scripts/install/hooks_deploy.sh::deploy_all_hooks`
  as a step AFTER the existing plain-`cp` deploy (new function
  `regenerate_forwarders_for_transport`, 4th optional `venv_python` arg
  threaded through from `install_version.sh`/`upgrade_version.sh`'s
  `$VENV_PYTHON`), explicitly SKIPPED in self-install mode — this repo's own
  tracked `.claude/hooks/*`/`init.sh` must never be rewritten by a
  generator, matching `deploy_hook_scripts`'s existing self-install
  short-circuit. `git status .claude/hooks/ .claude/init.sh` confirmed clean
  throughout (this repo's config leaves transport off).
- `init.sh::send_request_stdin` gained the nc rung (design §6.2, detail
  deferred to Phase 4 per the design doc): only for the plain-passthrough
  shape (no `response_mode` — "status"/"worktree" always go through the
  unchanged python3 render_status/print_worktree, never duplicated in bash).
  Payload AND response are buffered via TEMP FILES, never a shell variable
  (the same control-character rule the python3 rung already follows). An
  empty/failed nc capture is NOT silently dropped — a diagnostic goes to
  stderr — and the buffered payload REPLAYS into the existing python3 rung
  via a single shared call site using `< "${_nc_replay_payload:-/dev/stdin}"`
  (falls back to the process's real stdin when no replay is needed — zero
  behavioural change on the default path, confirmed by the full 45-case
  `test_forwarder_jq_free.py` suite passing unmodified). `forward_stop_event`
  gained a matching optional 2nd arg threaded through. Both `error_hiding_blocker`
  false-positives hit during authoring (`|| true`, `&>/dev/null`,
  `>/dev/null 2>&1`) were resolved with explicit rc-capture + stderr
  diagnostics rather than worked around.

**Task 4.2 — fail-open preserved, acceptance-tested against the REAL relay
binary.** `tests/integration/test_relay_guard_fail_open.py` uses the actual
built binary at `untracked/relay-build/hooks-relay-x86_64-unknown-linux-musl`
(present on this machine from Phase 3) rather than a stub, for genuine
end-to-end coverage:

- **daemon-down**: per-event socket absent → guard's own `-S` test is false,
  falls straight to the legacy path and reaches the (fake) daemon over the
  legacy socket. A second, binary-level test pins the relay's own
  connect-fail→exec-fallback contract directly: `/bin/bash <fallback>
  --no-relay` with stdin bytes intact.
- **binary-missing**: `relay_binary` pointed at a non-existent path — the
  guard's `-x` test fails, falls straight through, legacy transport reached
  with the correct payload.
- **nc-missing**: a broken `nc` shim prepended to PATH (mirrors
  `_make_broken_jq_dir`'s pattern from `test_forwarder_jq_free.py`) — empty
  capture, payload genuinely replayed to python3, daemon receives the
  correct request.
- **`--no-relay` re-entry loop-safety**: a forwarder invoked with
  `--no-relay`, pointed at a "relay" that would fail loudly if ever run and
  a real listening socket that WOULD satisfy the guard's `-S` test — the
  guard is skipped entirely (fake relay never runs), legacy path reached.

6/6 pass. All pre-existing forwarder/init.sh/hooks_deploy integration tests
(87 cases) pass unmodified — the nc-rung refactor of `send_request_stdin`
changed no default-path behaviour.

**Installer probe (design §6.3, in scope per the assigning message).** New
`install/transport_probe.py::probe_transport` (read-only, cheap — no
mutation, no daemon spawn, no build/download): relay binary
present/executable/digest-verified (against a `sha256sum`-format manifest;
absent today since Phase 5 owns `SHA256SUMS.released` — reported as
"unknown", not a failure), `nc` on PATH and `-U`-capable (inferred from `nc
-h`'s own usage text, never an actual connection attempt), per-event socket
dir presence (diagnostic snapshot). `render_env_lines` renders the one
runtime fact `init.sh` actually reads (`HOOKS_DAEMON_NC_UNIX_CAPABLE`),
empty when neither rung is configured on. Wired as `bin/hooks-daemon
transport-probe [--project-root] [--json]` (`daemon/cli.py::cmd_transport_probe`
+ subparser registration). NOT yet wired into the installer's env-file
write step (`install_version.sh` Step 6) — that heredoc unconditionally
overwrites `hooks-daemon.env` today, so wiring the probe's output in cleanly
needs a small follow-up rather than a heredoc edit under this session's
scope; flagging for Phase 5/6 rather than rushing it. Verified end-to-end:
`bin/hooks-daemon transport-probe --project-root /workspace` (table) and
`--json` both run correctly against the real daemon-restarted state.

QA: 157 targeted tests (forwarder_generator ×2, transport_probe,
hooks_deploy_relay_guard, relay_guard_fail_open, forwarder_jq_free) +
684 broader `tests/unit/install/` + 97 daemon/config regression tests, all
green; `mypy`/`ruff` clean on every touched/new Python file; `shellcheck -x`
clean on `init.sh` and `scripts/install/hooks_deploy.sh`; `bash -n` clean on
both. Daemon restart verified (`bin/hooks-daemon restart` → RUNNING).
`git status .claude/hooks/ .claude/init.sh` empty throughout — this repo's
own deployed forwarders never drifted (config here leaves transport off,
per instruction — not touched).

Deviations: nc-rung wiring into `hooks-daemon.env` at install time (the
other half of §6.3) deferred as noted above. Everything else in the
assigning message's scope (4.1, 4.2, probe) shipped.

## action · P5 — Phase 5 delivered: distribution + release integration (Tasks 5.1-5.3)

Owner ruling honoured verbatim: build-from-source is first-class, download is
the convenience option, source ships either way, both routes are explicit
config choices, nothing implicit.

**Config**: `TransportConfig.relay_source: Literal["build","download"] | None`,
default `None` (extra="forbid" unaffected) — deliberately decoupled from
`relay_enabled`/`nc_enabled`; a project can enable the ladder without a
provisioning route (rung 1 just has nothing to exec) or set a route without
enabling the ladder. 3 new tests in `test_transport_config.py`.

**New module `install/relay_deploy.py`** (23 unit tests, all subprocess/network
touchpoints dependency-injected — no real toolchain or network access exercised):
`resolve_relay_binary_path`, `check_musl_toolchain` (`rustc --print target-list`
probe, no compile attempt), `deploy_relay_from_build` (runs the daemon clone's
own `relay/build.sh`, copies the stripped output to the resolved binary path,
`chmod 0o755`), `deploy_relay_from_download` (ALWAYS verifies sha256 against a
fetched `SHA256SUMS` before writing anything to disk; refuses + advisory on any
mismatch or fetch failure — `FetchFn` is injectable, real impl is a plain
`urllib.request.urlopen`), and the dispatcher `deploy_relay_if_configured`
(`relay_source: None` is a true no-op regardless of `relay_enabled`). Either
successful route writes a sidecar `<binary>.route` marker.

**`transport_probe.py`** gained `toolchain_present` and `deployed_route`
fields (backward-compatible dataclass defaults so existing direct-construction
tests kept working unmodified), read back from `relay_deploy`'s toolchain
check and route marker; `render_table` shows both new rows. 17 tests (4 new).

**Wiring**: `install_version.sh` gained Step 7b (after config deploy) calling
`deploy_relay_if_configured` via a python heredoc, matching the existing
plan-workflow/ccy-supervisor deploy pattern exactly — advisory-only, never
aborts the install. `upgrade_version.sh` gained the same call on BOTH the
idempotent fast path (after the ccy-supervisor refresh) and the full slow path
(new Step 14c, after config merge). Default config (`relay_source: null`) is a
genuine no-op on every path — `deploy_relay_if_configured` returns
`RelayDeployResult(False, None, ())` immediately. `bash -n` + `shellcheck -x`
clean on both scripts.

**Closed the Phase 4 deferral**: new `scripts/install/transport_env.sh`
(`append_transport_probe_env_lines`), called from `install_version.sh` Step 6
right after the base `hooks-daemon.env` heredoc write. Resolves
`daemon.transport` via `forwarder_generator.load_transport_config` (renamed
from a private `_load_transport_config` to a shared public helper — same
fail-safe "missing config -> defaults" behaviour, now reused by three call
sites) and, only when a rung is enabled, runs the probe and appends
`HOOKS_DAEMON_NC_UNIX_CAPABLE`. On a genuine fresh install this is a real
no-op (config doesn't exist yet at Step 6 — Step 7 deploys it) — pinned by
`tests/integration/test_transport_env.py` (4 cases: no venv python, no config,
both rungs off, nc-enabled-and-capability-probed), run as real bash
subprocesses against a stub `nc`, never mocking the shell layer itself.
`shellcheck -x` clean.

**Task 5.1 release-pipeline side**: new
`scripts/release/build_relay_release_assets.sh` — runs `relay/build.sh`,
copies the stripped output, and reuses `build_bootstrap_checksums.sh` verbatim
to produce a `SHA256SUMS` in the exact `sha256sum` line format
`relay_deploy.py`'s parser expects. Ran it end-to-end on this container:
570,056-byte stripped binary, matching Phase 3's measurement (`relay/build.sh`
itself untouched). `CLAUDE/development/RELEASING.md` Step 14 gained a new
sub-section: build only when a musl-capable `rustc` is on the release
machine, upload alongside the four skill-script artefacts (or
`gh release upload` if the release already exists) — explicitly optional,
never a release-blocking gate, since the download route having nothing to
fetch is a valid state (advisory, not a hard failure, at install time too).
No GitHub Actions release workflow exists in this repo (release is the
human-gated `/release` skill, not CI) — DESIGN §3.2's "CI job" language from
Task 3.3 was corrected to describe the actual implemented shape.

**Task 5.3 docs/manifests**: `daemon.transport` (all 5 keys) added to
`CLAUDE/UPGRADES/UNRELEASED/config-changes/v3.57.0.yaml` and a `was`/`now`
entry to `UNRELEASED/truth-changes/v3.57.0.yaml`; both validated by loading
through the REAL `ConfigMigrationManifest.from_dict`/`TruthChangeManifest.from_dict`
loaders (not just YAML-parsed). `check_repo_hygiene.py` and
`check_handler_reference.py` both clean. New "Transport (`daemon.transport:`)"
cross-cutting section in `docs/guides/HANDLER_REFERENCE.md`, placed after
"Directory Layout" (the Plan 00288 precedent for this doc's cross-cutting
sections) and before the per-handler tables — marked EXPERIMENTAL/opt-in,
documents the two-separate-decisions shape and the open-source posture note
(source always ships; a compiled binary is never the only form). `docs-qa
--lint` clean on it. `DESIGN-socket-relay.md` §3.2 rewritten to describe the
actual Phase 5 implementation (module names, wiring points, the
`hooks-daemon.env` closure) rather than the pre-implementation sketch; §6.3
extended with the two new probe fields. `docs-qa --lint` clean on it too.

**QA**: `tests/unit/config/test_transport_config.py` (18),
`tests/unit/install/test_relay_deploy.py` (23),
`tests/unit/install/test_transport_probe.py` (17),
`tests/integration/test_transport_env.py` (4) — 62 tests, all green.
`mypy`/`ruff`/`black --fast --target-version py311` clean on every
touched/new Python file (black reformatted only `relay_deploy.py`, line
wraps, no logic change — same Python-3.11-vs-3.13-target quirk noted in
earlier phases). `bash -n` + `shellcheck -x` clean on `install_version.sh`,
`upgrade_version.sh`, `scripts/install/transport_env.sh`,
`scripts/release/build_relay_release_assets.sh`. `bin/hooks-daemon restart`
verified RUNNING before this commit.

**Deviations from strict task order**: wrote the `relay_deploy.py`
implementation before its test file in one pass (not a strict RED-first TDD
cycle for that module) — caught up immediately with the full 23-case suite
before moving on, and iterated two real failures (a `HOME`-relative
toolchain-detection false-positive from this container's own `~/.cargo/bin/rustc`,
and a test-only YAML-`textwrap.dedent` indentation bug) to green rather than
weakening the assertions. Did not touch `relay/hooks_relay.rs`,
`daemon/server.py`, or `forwarder_generator.py`'s transforms, per the
assigning message's constraints.

## action · P6 — Phase 6 measurement + dogfood gate (Tasks 6.1/6.2)

Extended the Plan 00154 method (`bench_relay_forwarder.sh`, this plan's
`assets/`) against the real deployed `.claude/hooks/pre-tool-use` forwarder:
3 warmups + 60 recorded iterations per rung, fresh `date +%s%N` timing,
nearest-rank percentiles. Full write-up: [MEASUREMENT-relay.md](../MEASUREMENT-relay.md).

**Relay rung (a) — criterion MET**: p50 4.344 ms, p95 5.056 ms, both under
the ≤6 ms bar. Fresh python3 baseline on this container: p50 34.111 ms
(7.86× reduction). Relay binary deployed via the real `relay_deploy.deploy_relay_if_configured`
build route (570,056 bytes stripped, matches Phase 3/5 measurements),
forwarders regenerated via the real `forwarder_generator` CLI — the diff on
every one of the 31 deployed forwarders is exactly the guard block above
`source init.sh`, nothing else; `bash -n` + `shellcheck -x` clean on all.

**nc rung (b) — found broken, not benchmarkable as a benefit**: every call
hung ~30 s before falling through to python3 (6 iterations recorded, all
~30.05–30.07 s, before the harness's own timeout truncated the run and a 7th
hung `nc` process had to be killed by hand). Root cause diagnosed:
`init.sh::send_request_stdin`'s `nc -U -w <timeout>` invocation never shuts
down the socket's write half on stdin EOF (needs OpenBSD `nc`'s `-N` flag),
so the daemon's EOF-framed per-event protocol never sees EOF and never
responds until the `-w` timeout. A second, independent bug was hit while
wiring the measurement: `forwarder_generator.py::regenerate_deployed_hooks`
early-returns before applying ANY transform when `relay_enabled` is False,
even when `nc_enabled` is True — so the nc-only forwarder-side change
(`append_nc_socket_arg`) never runs through the production CLI/install path
(the underlying `generate_forwarder_content` function is correct when
called directly, which is how the one nc measurement window was obtained,
then reverted). Neither was patched — daemon/installer source is out of
this task's scope; both reported to the coordinator. `nc_enabled` stays
`false` in this repo's dogfood config; this is not a "no measured benefit"
call, it is "actively broken."

**Dogfood flip (Task 6.2)**: `.claude/hooks-daemon.yaml` `daemon.transport`
now `relay_enabled: true`, `relay_source: build`, `nc_enabled: false`,
`timeout_seconds: 30`, `relay_binary: null`. Daemon restarted (per-event
listeners: 31 active), relay binary deployed via the build route,
forwarders regenerated. Live-path proof: a real PreToolUse payload through
the deployed forwarder returns a valid `{}` verdict in single-digit
milliseconds. Fail-open proof: stopped the daemon, ran the same forwarder
again — relay connect-fail exec'd the fallback with stdin intact, which
reached `ensure_daemon`, auto-started the daemon, and returned a valid
verdict; daemon confirmed RUNNING again afterward (`bin/hooks-daemon
status`, 31 per-event listeners active).

**QA gate**: `plan-qa --sweep` — 1 advisory (the pre-existing, known Plan
00135 path-existence finding; not new). `docs-qa --sweep` — clean, 0
findings. `scripts/qa/llm_qa.py all` run; see next journal entry / commit
message for the result once it completes (no source under `src/` was
touched this session — only this plan's assets, `.claude/hooks-daemon.yaml`,
`.claude/hooks-daemon.env`, and the 31 deployed forwarders).

Did not flip Plan 00290's own status to Complete or archive it — the
coordinator owns that decision after reviewing the soak, per the assigning
message.

## action · P4 fix — two Phase 6-found defects fixed (TDD)

Clock skew against the entries above (this agent's wall clock reads later);
appended at the bottom regardless. Fixed both defects MEASUREMENT-relay.md
diagnosed, with a failing reproduction test first for each.

1. **nc rung ~30s hang**: `.claude/init.sh`/`init.sh::send_request_stdin`'s
   `nc -U -w` invocation was missing `-N` (shutdown the socket's write half
   on stdin EOF) — the daemon's EOF-framed per-event socket never saw the
   half-close, never responded, and every nc-rung call sat until the `-w`
   timeout. Added `-N` (kept `-w`'s "final net reads" role as the overall
   response-wait budget once `-N` makes the write-side shutdown real).
   Reproduced first: `test_relay_guard_fail_open.py::test_nc_rung_round_trip_completes_promptly`
   drives a REAL EOF-framed server (the daemon's actual per-event protocol)
   through a generated forwarder with `nc_enabled` — before the fix it hit
   the test's own 15s subprocess timeout (genuinely reproducing the hang,
   not a synthetic assertion); after the fix the whole test file runs in
   0.3s.
2. **`regenerate_deployed_hooks` ignored `nc_enabled`**: it early-returned
   `[]` whenever `relay_enabled` was False, even when `nc_enabled` was True
   — silently skipping the nc-only rung's forwarder-side transform through
   the production CLI/install path entirely, though the underlying
   `generate_forwarder_content` implemented it correctly when called
   directly. Fixed the gate to `not (relay_enabled or nc_enabled)` — the two
   rungs are independent by design (`TransportConfig`/design §4-§5).
   Reproduced first: `test_forwarder_generator_cli.py::test_nc_only_enabled_config_rewrites_every_forwarder`.

**Test-isolation fix found and fixed along the way** (not one of the two
assigned defects, but required to make the round-trip test meaningful): this
repo's own `.claude/hooks/*`/`hooks-daemon.yaml` are, per this same journal's
entries above, mid dogfood-flip (relay enabled, real per-event sockets live)
— `test_relay_guard_fail_open.py` previously read `.claude/hooks/*` as its
source template and pinned `HOOKS_DAEMON_ROOT_DIR` to this repo's own root,
so its generated test forwarders picked up the live guard content and its nc
rung resolved sockets under the REAL `/workspace/untracked/events*/`
directory — 5 of 7 tests in the file failed transiently from this
contamination alone (confirmed: reverting to a self-contained forwarder
template and deriving `HOOKS_DAEMON_ROOT_DIR` from each test's own isolated
root fixed all 5). This makes the suite's outcome a function of the code
under test again, not of this repo's current deploy state — load-bearing
given Plan 00290's own dogfooding intentionally keeps this repo's transport
config live.

QA: `test_relay_guard_fail_open.py` (7/7), `test_forwarder_generator_cli.py`
+ `test_forwarder_generator.py` + `test_transport_probe.py` +
`test_hooks_deploy_relay_guard.py` (111/111) all green. `mypy`/`ruff` clean
on `forwarder_generator.py`. `shellcheck -x` and `bash -n` clean on
`init.sh`. Daemon restart verified RUNNING (with the concurrently-flipped
dogfood config still live — untouched by this fix). Did NOT touch
`.claude/hooks/*`, `.claude/hooks-daemon.yaml`, or `relay/test_relay.py`
(concurrently modified by the Phase 6 agent) — scope stayed to `init.sh`,
`install/forwarder_generator.py`, and their own tests, per the assigning
message.

## action · P6 — nc rung re-measured; dogfood flip reverted, two new bugs found

Pulled `4a8c2e50` and re-measured rung (b) end to end: 60 clean iterations,
p50 22.426 ms / p95 23.439 ms — genuinely functional now, a real 35 %
improvement over the python3 baseline, still ~5x slower than relay.
`MEASUREMENT-relay.md` and `assets/results/{nc.us,summary.json}` updated;
committed (`9b33a568`).

Then ran the full `llm_qa.py all` gate against the Task 6.2 dogfood diff
(relay enabled, all 31 forwarders regenerated with the relay guard) and hit
42 test failures. Investigated each rather than assuming pre-existing noise,
and found **two genuine, severe bugs caused by the dogfood diff itself**:

1. **Stop/SubagentStop hard-block exit-code-2 contract silently defeated.**
   `.claude/hooks/stop` relies on bash's `forward_stop_event` to translate
   the daemon's `decision=block` JSON into exit code 2 (Claude Code's hard
   re-entry contract). The relay guard `exec`s the relay binary in place of
   the *entire* wrapper, so that translation never runs. Verified directly:
   `tests/acceptance/test_stop_hook_hard_block.py::test_stop_hook_exits_2_on_block`
   — daemon correctly returns `"decision": "block"` in the JSON, but the
   wrapper's `returncode` is 0, not 2, once the relay path is taken.
2. **Relay guard's baked-in socket path breaks this repo's own
   daemon-isolation test fixtures.** ~38 of the 42 failures were
   `test_forwarder_jq_free.py` parametrized cases
   (`AssertionError: daemon socket received nothing`) — the guard's
   `_rl_dir`/`_rl_sock` are literal absolute paths baked in at
   forwarder-generation time (by design, for speed), so a test that spins
   up an isolated fake daemon and expects the wrapper to talk to it instead
   silently reaches the REAL live per-event socket.

Everything else in the 25-check run (`magic_values`, `security`,
`error_hiding`, `sensitive_content`) was confirmed pre-existing/unrelated —
none of the flagged files (`cli.py`, `secret_file_matching.py`,
`upgrade_version.sh`, an unrelated Plan 00292 doc) are in this task's diff.

Reported both bugs to the coordinator rather than patching
`init.sh`/`forwarder_generator.py`/`tests/` myself (daemon/installer/test
source is out of this task's scope; `tests/CLAUDE.md` also forbids editing
the daemon's own test suite directly). **Reverted the dogfood flip**: all 31
`.claude/hooks/*` forwarders reset to canonical HEAD content (`git status`
confirmed 0 diff), `.claude/hooks-daemon.yaml` reverted to HEAD (no
`transport:` block — byte-identical default), `.claude/hooks-daemon.env`
already clean, daemon restarted and confirmed RUNNING with no per-event
listeners (default behaviour). Spot-checked the 4 directly-affected tests
(`test_stop_hook_exits_2_on_block`, one `test_forwarder_jq_free` param, both
`test_dogfooding_hook_scripts` cases) — all pass again post-revert.

**Coordinator ruling**: Stop/SubagentStop will be excluded from the relay
guard (bash path keeps the exit-2 translation) and the guard's socket path
will become overridable (`${HOOKS_DAEMON_EVENTS_DIR:-<literal>}`) so test
fixtures can redirect it — both being implemented by the Phase 4 author.
Task 6.2's dogfood flip is deferred until that fix lands; re-flip + final
QA gate to follow once it does.

## action · P4 fix — two dogfood-flip bugs fixed (TDD)

Clock skew against entries above; appended at the bottom regardless. Fixed
both bugs the Phase 6 dogfood attempt exposed, failing test first for each.

1. **Stop exit-code-2 contract regression**: `generate_forwarder_content`
   now NEVER inserts the relay guard for `stop`/`subagent-stop`, at any
   config — new `RELAY_EXCLUDED_EVENT_FILE_NAMES` constant in
   `forwarder_generator.py`, checked alongside the existing anchor/idempotency
   gate. `forward_stop_event`'s daemon `decision=block` -> exit-code-2
   translation (Plan 00101 Phase 9) happens in bash, after the transport
   call returns; the relay `exec`s the process directly with no equivalent
   translation, so exec'ing it for these two events would silently drop
   Claude Code's hard-re-entry contract. The nc rung is unaffected — it
   only replaces the transport beneath `send_request_stdin`, so the
   exit-code translation still runs afterward. Proven adversarially in
   `test_relay_guard_fail_open.py::test_stop_events_keep_exit_code_2_contract_even_with_relay_enabled`:
   a relay binary and a listening per-event socket ARE present (would be
   selected if the exclusion broke), and the generated stop/subagent-stop
   forwarder still exits 2 with the daemon's block reason on stderr — the
   relay's own `exit 99`/stderr marker never appears. Daemon-side per-event
   binding for these two events is left unchanged (still bound like every
   other wired event) — documented in DESIGN-socket-relay.md §1.1 as
   deliberate: an unused listener is harmless, and special-casing the BIND
   side would only add a second place this invariant could drift from the
   generator.
2. **Baked literal broke test-isolation**: the guard's events-dir and relay
   binary are now `${HOOKS_DAEMON_EVENTS_DIR:-<literal>}` /
   `${HOOKS_DAEMON_RELAY_BINARY:-<literal>}` — still pure parameter
   expansion, zero spawns, zero behaviour change when unset (production
   forwarders never set these env vars). Mirrors `CLAUDE_HOOKS_SOCKET_PATH`'s
   existing override pattern for the legacy socket. Proven both directions
   in `test_relay_guard_fail_open.py`: `test_env_override_redirects_relay_away_from_baked_default`
   (baked default points at nothing; override redirects to a fixture socket
   that IS reached) and `test_env_override_absent_falls_through_to_legacy`
   (override points at an empty dir; falls through regardless of what the
   baked default would have resolved to). The `--no-relay` loop-safety test
   was simplified to use the env override directly instead of post-hoc
   string-patching the generated content.

Design doc updated: §1.1 gained the Stop/SubagentStop exclusion rationale;
§6.1's guard-block snippet and prose gained the env-override mechanism and
the reason it exists (a dogfooded repo's own real per-event sockets had
silently answered a test's synthetic payload instead of its fake server).

QA: `test_forwarder_generator.py` + `test_forwarder_generator_cli.py` +
`test_transport_probe.py` + `test_relay_guard_fail_open.py` +
`test_hooks_deploy_relay_guard.py` — 130/130 green (up from 97+7 before this
fix; net +26 new/adjusted tests). `mypy`/`ruff` clean on
`forwarder_generator.py`. `black --fast --target-version py311` reformatted
the two touched test files (line-wrap only). `docs-qa --lint` clean on
DESIGN-socket-relay.md. Daemon restart verified RUNNING. Did NOT touch
`.claude/hooks-daemon.yaml` or deployed `.claude/hooks/*` — Phase 6 re-flips
after this lands, per the assigning message.

