# Plan 00452 — Journal 26-09-23

_Timestamps in this file are UTC._

## 21:45 · finding · T5.2 — the blocker was stale, and the answer is "not worth changing"

Task 5.2 was recorded as blocked because no session had run in a worktree.
That had stopped being true: this session's sub-agent transcripts include
three worktree agents. The history still could not answer it, though. All
three were dispatched alone, and a lone sub-agent's first request reads
nothing in any directory. Sharing happens between siblings, so the question
needed siblings.

Ran six trivial Haiku probes in three sequential pairs: main checkout,
separate worktrees, and main checkout again with the same `description`.
Detail and table are in `RESEARCH-cache-measurements.md`. Outcome:

- A worktree sibling shares 8,306 tokens against 9,717 for a same-directory
  one: ~1.4k, first request only. Task closed as not worth changing.
- **Same-directory siblings with identical prompts share exactly 9,717 of
  ~29k.** About 19.4k is written fresh on every dispatch, and neither the
  prompt, the description nor the directory moves it. The remaining
  per-dispatch variable is the harness's roster reminder, which the
  coordinator cannot hold constant.
- Side observation: that roster keeps listing agents that finished or were
  reaped hours earlier.

**A claim I had to correct before committing.** The first draft said the
only non-zero first-request reads came from batches of siblings, over "43"
requests. Both figures were from memory of a partial table. Checked against
the transcripts: 54 requests, 11 non-zero, 10 following a same-model
dispatch within the TTL, one exception at 8,689 tokens with no such
predecessor, and two zero reads despite having one. The rule stated in the
research document is now exactly that, and no stronger.

Housekeeping: both probe worktrees were removed after checking zero commits
and a clean tree. `worktree-reap` declined because they were under its
15-minute minimum age. The four in-process probes were stopped.

## 21:50 · blocker · P4 — 00452 is now blocked on human input

With 5.2 closed, every open task waits on one input this repository cannot
produce: a transcript from a HUMAN-PACED project (Task 2.4). Tasks 4.1 and
4.3 need it for `P` and for a replay where warming could pay. 4.2 is
deliberately withheld until 4.3 can show a win. The tool to produce the gap
profile exists (`hooks-daemon cache-gaps`). What's missing is the owner
running it on, or sharing, a project whose gaps are set by a person.

## 22:05 · finding · T1.2 — that blocker entry was premature: a ticked task was not done

I checked the open TASKS and not the unticked success criteria, and called
the plan blocked. Criterion 2, "shows all three values", pointed straight at
it. Task 1.2 was ticked as "Render MAIN, SUB and TOTAL with the TTL per
side", while the renderer produced main and `sub NN%`, with no TOTAL and no
sub-side TTL. The ticked text said what was intended, not what shipped.

**Establishing TOTAL was possible needed one fact first.** The payload
carries `cache_write_tokens` but no read count, so a true total needs
`hit_ratio` to be token-weighted. Checked against this session's own
captured Status payloads and main transcript: 0.991145 and 0.991157 from the
payload, against 0.991149 and 0.991160 as read/(read+write) from the
transcript. A request-based ratio gives 0.9969. Implied reads agree within
0.03%.

**Then a real bug, found by reading the sensor before building on it.**
`subagent_cache_aggregator.totals_for` summed every record carrying usage.
Claude Code writes one record per content block, each with the whole
request's usage (the probes earlier today showed two identical usage records
for a one-word reply). Over this session's sub-agents: 4,276 records for
2,119 requests, zero mismatched duplicates, writes inflated 1.5–2.3x. TOTAL
would have added that inflated sub figure to main. Fixed first, TDD: dedupe
on message id, and a record with no id is still counted.

Shipped TOTAL (token-weighted) and the sub-side TTL (`5m`, `1h` or `5m+1h`,
read from what was written). No total is shown with no write count or a
ratio of exactly 1.0, where main reads are undetermined. The test fixture
is chosen so a naive average (57.5%) cannot pass for the weighted figure
(71%). My own first fixture had one wrong expected value (25% where the
arithmetic gives 20%), caught before the run.

End-to-end check for criterion 2: the segment rendered from this session's
real captured payload plus 50 real sub-agent transcripts recorded through
the fixed sensor. The sidecar matched an independent per-request sum
exactly, 196,015,138/7,812,380. Expected 99.12 / 96.17 / 98.70%; rendered
`⚡ 99% 1h sub 96% 5m total 99%`.

Also qualified the research doc's main-vs-sub table, which counts records
and so carries the same weighting.

**What stays blocked:** criterion 1 (reconcile with `/usage`, a command
only the owner can run in the session) and everything behind Task 2.4.

## 22:45 · action · — — CI green; one self-inflicted QA error, recorded

`1a7a8a0a` passed CI run 35925979259 on all five jobs.

The local full QA run exited 1 first. All other stages passed; the tests
stage showed 0 failed, 22 errored, all in acceptance tests driving the live
daemon plus one status-line unit test. **Cause: I restarted the daemon
while that QA run was in progress**, which pulls the socket and pid file
out from under its integration tests. `hooks-daemon restart` printed exactly
that warning. I read it afterwards rather than checking for a QA lock
first. The unit test's presence made it worth checking rather than assuming:
it instantiates every status-line handler and calls `explain_segment()`,
so a handler reporting live state can raise mid-restart. It passed alone,
in its directory (602), and all 27 tests across the errored files passed
against the stable daemon. CI is the clean full run.

## 22:45 · blocker · — — 00452 is blocked on human input, now for real

Checked this time against the success criteria as well as the tasks. Open:

- **Criterion 1**, hit ratio reconciles with `/usage`. Only the owner can
  run `/usage` in the session. (The payload's ratio does reconcile with
  the transcript, per the 22:05 entry, but the criterion names `/usage`.)
- **Criterion 3 and Task 2.4**, a gap histogram from a HUMAN-PACED project.
  This session's crons hold every gap under the 1h TTL.
- **Criterion 4, Tasks 4.1 and 4.3**, blocked on 2.4. **Task 4.2** is
  deliberately withheld until 4.3 can show a win on replay.
- **Criterion 5** (advisory rules trace to Phase 3) has nothing to judge
  yet. The only candidate advisories, 4.4 and 5.3, were cancelled.
