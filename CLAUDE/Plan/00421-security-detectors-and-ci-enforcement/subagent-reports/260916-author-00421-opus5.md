# Report: authoring PLAN.md for Plan 00421

**Agent**: author-00421 (Opus 5) · **Dispatched by**: team-lead

## Deliverable

`/workspace/CLAUDE/Plan/00421-security-detectors-and-ci-enforcement/PLAN.md`

29,014 bytes — under the 35,000-byte limit. The size advisory fires at 25,000
and I compressed the evidence restatements twice, but 26 tasks across six
phases genuinely is the bulk, and further cuts would have deleted the concrete
instructions the four rulings require.

**Plan QA**: 0 block, 6 advise. The only 00421 finding is the missing README
index row (`row-folder-bijection`, plus the `stats-recount` that follows from
it), which the lead is handling centrally. The other four advisories belong to
neighbouring agents' plans (00411, 00412, 00418, 00419).

No commits. No `CLAUDE/Plan/README.md` edit. No edits under `00412-`. No time
estimates. All content written with `Write`/`Edit`.

## What I found when I checked qa.yml myself

The defence-location ruling's claim holds exactly as stated. I verified all
three legs independently:

- **`.github/workflows/qa.yml` runs no `check_*.py`.** The `qa` job's steps are
  black, ruff, mypy, `run_pyright_check.py`, daemon start, relay build, two
  toolchain warms, `pytest tests/`, `pytest .claude/project-handlers`, coverage
  upload, bandit, deptry; plus separate `shell` (shellcheck) and `daemon-load`
  (handler import probe) jobs. `qa.yml:120` states the reason in its own words:
  `run_all.sh` resolves its interpreter through the daemon's fingerprint-keyed
  venv resolver, so it is "a self-install-mode entrypoint, not a CI one".

- **The three unwired checks are exactly the three named.** 20
  `scripts/qa/check_*.py` files on disk; 17 invoked by `run_all.sh`. The
  missing three are `check_github_urls.py`, `check_python_var_guidance.py` and
  `check_eacces_safe_predicates.py` — all three present in `TOOL_REGISTRY`. So
  the generic wiring test goes RED on three today, as predicted.

- **`check_authored_path_stat.py`'s wrapper has no real-tree assertion.**
  `tests/unit/scripts/test_authored_path_stat_checker.py` is 213 lines with no
  reference to the project root or the real tree — fixtures only. Register row
  1 is the only row not enforced in CI.

**Bonus confirmation for Task 1.1**: `llm_qa.py:96-107` already resolves
`venv_python()` at run time rather than import time *specifically so wiring
tests can import `TOOL_REGISTRY`*. The route the new test needs is already
open, and the docstring says so.

## Task list, in the order I settled on

### Phase 1 — Make a Detector binding, and say so correctly

First, for the reason the lead gave: seven more Detectors on an unenforced
binding is the same defect at seven times the scale.

- **1.1** Generic wiring test, RED first. Do not narrow it to make it pass.
- **1.2** Close the three files it names (add `run_all.sh` steps).
- **1.3** Real-tree assertion for check 25, shown to have teeth by
  reintroducing the class's defect on a scratch commit.
- **1.4** Rule text in four places: the three "wired into `run_all.sh`" sites
  (`CLAUDE/Security/README.md:89-98`,
  `CLAUDE/Routine/00001-security-review-full/ROUTINE.md:102-103`, 00412 Task
  3.4), plus the misattributed founding story (it was
  `check_project_handler_tests.py`, not Plan 00244, and the exit code gated
  correctly — only the summary line lied).

### Phase 2 — Register hygiene

- **2.1** Class 14 page + row. Check 29 conforms and is under-reported — the
  opposite error from the rest of this plan, and equally damaging to a reader
  who trusts the table.
- **2.2** Interim gap rows for classes 3/5/7/9/10/15, the Plan 00246 git guard,
  and class 2, each naming the actual mechanism and the method 3.2 gap.

### Phase 3 — Migrate the Runner-hosted rules as one unit

- **3.1** Relocate `tests/subprocess_ast.py` into `scripts/qa/` and migrate
  both spawn guards with it (class 15 + the Plan 00246 git guard).
- **3.2** Migrate classes 3, 5, 7, 9, 10.
- **3.3** Flip interim rows as each Detector lands; wiring test green after
  every flip.

### Phase 4 — Degraded mode

The ruling's own order: cheapest link first.

- **4.1** Drift reporters speak on `parse_failed`.
- **4.2** Advisory content — handler names/count + whether HEAD's config
  validates; also state that the early return covers every event type.
- **4.3** `STATUS_LINE` degraded-mode test.
- **4.4** `CurlPipeShellHandler` + `DangerousPermissionsHandler` into the net.
- **4.5** Declared config-independence property + its Detector.
- **4.6** Open the handed-back decision as a `DECISION-*.md`.

### Phase 5 — Forwarder interpolation, option B

- **5.1** Sites `:273`/`:280` **plus the sixth site** `append_nc_socket_arg:558`.
- **5.2** Regenerate the 27 guarded forwarders.
- **5.3** The four named test updates.
- **5.4** The effect-level Detector: bash execution, both branches,
  red-on-reversion, plus the artefact-shape rule.
- **5.5** The `reaches` row with an honest `reason`, and
  `AsymmetricSiblingProtection.md` corrected to six sites and moved to fixed.

### Phase 6 — The seven unwatched classes

One task each: 8 `exemption-scope-drift`, 11
`check-enumerates-from-the-registry-it-polices`, 12
`write-time-guard-with-no-batch-equivalent`, 13
`executable-content-outside-the-lock`, 16
`response-trusted-beyond-the-request-validated`, 17
`authored-or-argument-path-resolved-without-containment`, 18
`tri-state-default-favours-capability`.

### Why 4 and 5 sit ahead of 6

Both close defects live in shipped code — the forwarder's `$`/backtick sites
run a command substitution every time the daemon is down — where Phase 6 is
greenfield. I stated that rationale in the Overview so a later reader does not
"optimise" the order back.

## The handed-back decision

Recorded under a dedicated **"Open decisions this plan must get ruled"**
section, not as a task to build: per-handler degraded mode (run a handler with
its parsed `options:` while the document as a whole is invalid; honour
`enabled: false` in the safety net), together with the `secret_file_guard` /
Plan 00272 "no agent escape hatch" contradiction it would resolve, and any
denying form of option C riding with it. Task 4.6 writes the request document;
getting it ruled is the deliverable.

## What I could not turn into a concrete task

- **The fourth ruling (dotted module path) produces no work at all** — that is
  its finding, not a gap on my side. It is determinate that nothing changes in
  the guard, its config or its tests. I carried it as a Non-Goal plus the three
  authoring rules for whoever touches `scripts/qa/check_sensitive_content.py`:
  keep imports as statements; name the module in slash form in prose; simulate
  the failure via the package-level `sys.modules` key or object-form
  references. F-PRIV-4 itself is not a 00421 task — the ruling unblocks it, but
  it belongs to whoever owns that worklist row.

- **Phase 6's per-class Detector specs are one-liners, not designs.** The 00412
  worklist gives a Detector *hypothesis* per class (e.g. an exemption-dialect
  checker with 3 rules; `bool | None` field analysis over `config/models.py`
  for class 18), and I recorded those. The rule set for each still has to be
  designed when the task is picked up. I deliberately did not invent detail the
  rulings do not contain.

- **Two rule-text corrections are scheduled rather than fixed in place.** The
  ruling says the Plan 00244 misattribution in the two guard docstrings is
  corrected "when those modules are next touched", and those modules move in
  Phase 3. I put all four sites in Task 1.4 and noted that the two docstrings'
  "why a test rather than a `scripts/qa/` checker" rationale — which the ruling
  overturns — goes with them in Phase 3.

## Housekeeping note for the lead

`CronList` reports no scheduled jobs in *this agent's* session. The failsafe
recovery cron belongs to your session, so I did not create one from here — a
session-only cron in a transient subagent would be a no-op at best and a
duplicate at worst. Worth confirming on your side.
