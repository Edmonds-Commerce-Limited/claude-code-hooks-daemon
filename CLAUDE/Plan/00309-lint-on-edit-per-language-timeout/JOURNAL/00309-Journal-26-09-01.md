# Plan 00309 — Journal 26-09-01

> **Append-only activity log** for plan 00309. One file per day
> (`00309-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 18:09 · action · — — plan scaffolded

Plan 00309 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 18:10 · action · — — plan filed from owner field report

- Owner (php-qa-ci client project): `php-qa-ci qa -t phpstan -p {file}` is
  ~5s warm but exceeds the hardcoded 15s `Timeout.LINT_CHECK` when cold or
  lock-contended, and lint_on_edit silently allows on timeout. Plan: make
  the extended-lint timeout configurable per language
  (`options.timeouts.PHP: 30`), keep fail-open semantics, make a fired
  timeout visible. Implementation delegated to a worktree-isolated agent
  (main index busy with Plan 00307's implementing agent).
- Session failsafe recovery cron: reusing the single session cron id
  `892eef87`; nothing new created.

## 18:35 · action · P1 — implemented, tested, docs updated

- Added `LintOnEditHandler._timeouts` option (`options.timeouts.<Language>:
  <seconds>`), lazily validated on first use into `_resolved_timeouts`
  (`_apply_timeouts`/`_timeout_for`): case-insensitive language-name match
  against `_all_language_names` captured BEFORE the `languages` filter
  narrows the registry; unknown-language keys and non-positive/non-numeric
  values are logged (`logger.warning`) and ignored, never crash. `_run_lint_command`
  now passes `timeout=self._timeout_for(language_name)` to `subprocess.run`
  instead of the hardcoded `Timeout.LINT_CHECK`.
- Timeout advisory (Task 1.3): the `TimeoutExpired` branch now names the
  language, the budget spent, and the exact config key
  (`handlers.post_tool_use.lint_on_edit.options.timeouts.<Language>`) in
  both the `context` advisory AND a `logger.warning` — fail-open (ALLOW) is
  unchanged.
- TDD: wrote `TestPerLanguageTimeout` in
  `tests/unit/handlers/post_tool_use/test_lint_on_edit.py` FIRST (11 new
  tests, RED confirmed against the file before the source edit), then
  implemented until green. Full file: 69/69 passing. Full `tests/unit`
  suite: 663 passing (handlers/post_tool_use + strategies/lint scope);
  `mypy`/`ruff` clean on the touched file.
- Docs: `.claude/hooks-daemon.yaml` and `.claude/hooks-daemon.yaml.example`
  gained a commented `options.timeouts` example under `lint_on_edit`;
  `get_claude_md()` gained a paragraph on the new option (verified live via
  `bin/hooks-daemon explain-handler lint_on_edit` after
  `HOOKS_DAEMON_VENV_PATH=/workspace/.venv bin/hooks-daemon restart` —
  **caveat**: that daemon process resolves the shared `/workspace/.venv`
  editable install, which points at the MAIN repo's `src/`, not this
  worktree's — so the live `explain-handler` output still shows the OLD
  text even though this worktree's source and its own pytest run (with
  `PYTHONPATH` pointed at the worktree `src/`) are both correct and
  green. This is a venv-sharing artifact of the worktree setup, not a defect
  in the change; the coordinator's own daemon (built from the merged branch)
  will pick up the new text normally.
- Task 2.2 (field-report shape): covered as unit-level tests
  (`test_configured_language_uses_its_own_timeout` et al.) that assert the
  `timeout=` kwarg passed to a mocked `subprocess.run` is 30 for a
  PHP-configured budget and 15 for unconfigured Python — no real sleep in
  the suite, per the plan's own constraint. A literal wall-clock
  "would-have-timed-out-at-15-but-completes-at-30" integration test was not
  added on top of this, since it would need a real subprocess and a real
  multi-second delay to be meaningful, which the plan explicitly rules out.
