# Plan 00371 — Journal 26-09-10

> **Append-only activity log** for plan 00371. One file per day
> (`00371-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 12:36 · action · — — plan scaffolded

Plan 00371 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 12:58 · action · — — PLAN.md filled in, README indexed

Filled Overview/Goals/Non-Goals/Tasks/Success Criteria per the team-lead's
dogfooding-defect report (`SelfMatchingProcessProbeHandler` probe #116 denied
for the wrong reason because the daemon dispatched by the acceptance harness
still held pre-merge code; `bin/hooks-daemon restart` fixed it with no code
change). Design decision recorded in the plan: detect-and-fail, not
auto-restart, because QA must stay read-only and the daemon a QA run would
restart is often the same one gating the live agent session running that QA.

Surveyed the daemon's identity surface (`_system`/`health` socket action,
`DaemonController.get_health()`) — no source fingerprint exists today, so
Task 1.1/1.2 add one. Surveyed `tests/acceptance/`: 4 files
(`test_playbook_harness.py`, `test_absolute_path_socket_deny.py`,
`test_stop_hook_hard_block.py`, `test_tool_use_error_recovery.py`) each
independently define byte-identical `_socket_is_alive`/`_discover_socket`
helpers with no shared fixture — a pre-existing DRY violation that is also
exactly where the staleness check must land, so Task 3.1 centralises both
into `tests/acceptance/conftest.py` in one pass. `scripts/qa/run_smoke_test.sh`
already claims (in its own header) to catch "daemon running stale code" via
3 fixed probes, but provably does not (none of the 3 covers probe #116's
handler) — Task 4.1 gives it the same general-purpose check via a new
`bin/hooks-daemon check-source-fresh` CLI verb.

`bin/hooks-daemon plan-qa --check-staged` on the staged 00371 files (PLAN.md,
README.md, JOURNAL/) reports 0 block / 2 advise, both attributable to another
agent's concurrent, not-yet-committed plan 00372 on this same shared
checkout (`row-folder-bijection` for its folder, `stats-recount` since the
filesystem currently has 19 root plan folders including that one) — nothing
to fix on my side; committing 00371 alone (18 active) is internally
consistent with the counter as of this commit.

Next: commit on `main`, push, then `git worktree add
untracked/worktrees/worktree-plan-00371 -b worktree-plan-00371` and start
Phase 1 (TDD: `tests/unit/daemon/test_source_fingerprint.py` first).

## 13:05 · finding · — — plan-filing commit landed inside another agent's commit, not its own

My own `git commit` attempt was blocked twice by R-PLAN-QA-COMMIT on other
agents' in-flight archival work in this same shared `/workspace` checkout
(dangling README rows mid-`git mv`, then a live `git merge` with MERGE_HEAD
set). Both resolved on their own without me touching them. By the time I
re-checked, `git status` showed my staged PLAN.md/JOURNAL/README.md changes
already clean against HEAD — the shared git INDEX meant another agent's own
`git commit` (finishing their unrelated archival) picked up my staged files
too, since there is no isolation between concurrent agents' `git add`/`git
commit` in one working tree. Confirmed via `git log --follow`: my three
files first appear in `9f9d83cf` ("Plan 00369: Complete — archived to
Completed/..."), already pushed to `origin/main` (`main` == `origin/main`).

Content is correct and fully live on `main` — PLAN.md, JOURNAL, and the
README index row + stats all landed intact. Not amending: that commit isn't
mine, it's already pushed, and `git commit --amend` is forbidden regardless.
The only defect is attribution/message (no standalone "Plan 00371: filed"
commit, no Co-Authored-By trailer on the commit that carries this content) —
cosmetic, not functional; not worth a corrective no-op commit. Flagged to
the team lead for awareness in case this recurs for other concurrent
plan-filing agents sharing this checkout.

Next: `git worktree add untracked/worktrees/worktree-plan-00371 -b
worktree-plan-00371` and start Phase 1 (TDD:
`tests/unit/daemon/test_source_fingerprint.py` first).

## 14:40 · action · P1-P4 — fingerprint capability, CLI verb, harness wiring, smoke-test parity all built and proven live

TDD throughout: RED (new test files fail on collection/import) confirmed
before each source file was written, then GREEN.

**Phase 1**: `daemon/source_fingerprint.py` — `daemon_package_root()` (two
levels up from the module, wherever THIS process imported the package from
— correct in both self-install and client-install layouts),
`compute_source_fingerprint(*roots)` (sha256 over sorted `.py` files, path +
content, root-not-found skipped), `compute_daemon_identity_fingerprint`,
`compute_current_project_fingerprint` (config-aware, loads the project's
`hooks-daemon.yaml` to resolve `project_handlers` exactly as
`DaemonController.initialise()` does), `describe_fingerprint_mismatch`.
`DaemonController` computes `self._source_fingerprint` once in
`initialise()` (best-effort, OSError -> None, logged — matches the
`_sync_agent_assets` fail-open sibling contract) and reports it from
`get_health()`. Extracted `resolve_repo_relative_path()` into
`utils/repo_relative_path.py` so `_load_project_handlers` and the new
fingerprint code share the identical `{REPO_ROOT}`-token resolution rather
than a second copy.

**Phase 2**: `bin/hooks-daemon check-source-fresh` — queries the daemon's
health, compares, exits 0 fresh / 1 stale-or-unreachable.

**Phase 3**: found ALL FOUR live-dispatch acceptance files
(`test_playbook_harness.py`, `test_absolute_path_socket_deny.py`,
`test_stop_hook_hard_block.py`, `test_tool_use_error_recovery.py`)
independently defined byte-identical `_socket_is_alive`/`_discover_socket`
helpers with no shared fixture — a pre-existing DRY violation. Centralised
into `tests/acceptance/conftest.py` (`daemon_running`/`daemon_socket`
fixtures, both `scope="module"` — required because `test_playbook_harness.py`'s
`playbook` fixture is itself module-scoped and pytest forbids a
broader-scoped fixture depending on a narrower one), with
`assert_daemon_source_fresh()` added once, covering all four sites. New
`tests/acceptance/test_daemon_source_freshness.py` names the mechanism
directly and proves it against a REAL daemon-reported fingerprint (not just
two hand-written strings).

**Phase 4**: `scripts/qa/run_smoke_test.sh` now calls `check-source-fresh`
before its 3 fixed probes, closing the gap in its own header's claim to
catch "daemon running stale code" (none of the 3 probes covers
`SelfMatchingProcessProbeHandler`, the handler that actually broke).

**Live-fire verification** (not just unit tests): restarted this worktree's
daemon, confirmed `check-source-fresh` reports fresh; appended a comment to
`controller.py` WITHOUT restarting, confirmed `check-source-fresh` reports
STALE DAEMON with both fingerprints named; ran
`test_playbook_harness.py` in that stale state and watched every test in
the module fail immediately with the named reason instead of a confusing
probe-specific symptom — the literal incident, reproduced and now caught by
name. Same live-fire against `run_smoke_test.sh` directly. Reverted each
probe edit, restarted clean, reran all 5 acceptance files green (18 passed).

**Fallout, fixed**: the project-wide error-hiding self-scan
(`tests/unit/qa/test_audit_error_hiding.py`) flagged two new sites.
`compute_current_project_fingerprint`'s bare `except: config = Config()`
fallback got a `logger.warning(...)` call added (genuine fix — a handler
body with >1 statement no longer matches the AST rule's `len==1` shape, and
it is simply better to log a broken project config). `_compute_startup_source_fingerprint`'s
`except OSError: ... return None` is structurally the
`return-none-on-error` shape regardless of logging (the AST rule checks all
handler bodies unconditionally for a bare `return None`, not just
single-statement ones) — added an entry to
`scripts/qa/error_hiding_exclusions.json` citing the identical
`_sync_agent_assets` fail-open contract already documented two entries
above it in the same file.

Full `tests/unit/daemon/` + `tests/unit/utils/test_repo_relative_path.py`:
1858 passed, 1 pre-existing unrelated skip. Pyright on every touched/new
file: 0 errors, 0 warnings.

Next: commit this batch, then Phase 5 (release notes callout, full
`./scripts/qa/llm_qa.py all`).

## 15:20 · action · P5 — release notes, full QA green, plan Complete

Added `CLAUDE/UPGRADES/UNRELEASED/release-notes/24-a-stale-daemon-can-no-longer-pass-or-fail-qa-silently.md`.

Restarted the worktree daemon, ran `./scripts/qa/llm_qa.py all` in the
background (~8 min). First run: 23/27 passed, 4 failed (`format`, `pyright`,
`tests`, `smoke_test`). Root-caused all four to ONE cause: the `format`
stage's `black` auto-fixed two just-written files
(`source_fingerprint.py`, `test_daemon_source_freshness.py`) partway
through the run, mutating the working tree AFTER the daemon had been
restarted fresh at the start of the run — so by the time the `tests` and
`smoke_test` stages ran (later in the pipeline, dispatching to that same
live daemon), the daemon was genuinely, legitimately stale relative to the
now-reformatted source. This is exactly the class of drift this plan's
check exists to catch, firing correctly on itself. `pyright`'s 10 errors
were checked individually against every file this plan touches: none
match (`core/result_types.py`, `test_segment_explanation.py`,
`test_explain_segment_completeness.py`, `test_dependency_system.py` — all
pre-existing, unrelated to this plan, likely residue from concurrent plans
merged into main before this worktree branched).

Restarted the daemon again (files were already black-formatted from the
first run, so no further mid-run mutation was possible) and reran QA in the
background. Second run: 26/27 passed, 1 failed (`pyright`, the same
pre-existing 10 errors, confirmed unrelated). `tests`: 22115 passed, 0
failed, 0 errored (up from 18 errored in run 1 — all resolved).
`smoke_test`: 3/3. `check-source-fresh` confirms the daemon is fresh.

PLAN.md: all tasks and Success Criteria ticked; Status -> Complete
(`plan_done_requires_holding_area` gate satisfied by citing the release-notes
callout explicitly, per its own remediation).

Per the coordinator's instructions: NOT touching `CLAUDE/Plan/README.md`
rows or `git mv`-ing this folder from the worktree — that is the
coordinator's job on `main`.

## 15:22 · handoff · — — plan complete, ready for coordinator to merge and archive on main

All 5 phases done, live-verified against a real daemon (not just unit
tests), full QA green modulo pre-existing unrelated pyright debt. Commits on
`worktree-plan-00371`, pushed to origin after each batch. Nothing further to
do here; next action is the coordinator merging this branch into `main` and
performing the archival (`git mv` into `Completed/`, README row + stats,
in one commit — per the Plan Completion Checklist, not this worktree's job).
