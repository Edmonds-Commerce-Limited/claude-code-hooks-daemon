# Plan 00173 — Journal 26-07-17

> **Append-only activity log** for plan 00173. One file per day
> (`00173-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 14:34 · action · — — plan scaffolded

Plan 00173 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 14:36 · finding · — — supervisor architecture confirmed

Origin: user pointed at anthropics/claude-code#43596 (Ctrl+Z suspends Claude,
hard to recover in a container) and asked whether the ccy supervisor can
intercept it, plus a status-line message with a TTL. Failsafe recovery cron
`2a71237b` created (hourly :37, non-durable).

- `_forward_io` (`.claude/ccy/claude-supervise.py:2192`) forwards stdin →
  `master_fd` byte-for-byte. Outer terminal is raw (`tty.setraw`, ~2379) so
  `ISIG` is OFF — Ctrl+Z arrives as a `0x1a` byte, not a SIGTSTP at the outer
  level. Interception point: strip `0x1a` before `os.write(master_fd, data)`.
- Source of truth is the tracked monolithic `.claude/ccy/claude-supervise.py`
  (pure dogfooding, no `src/` copy — `install/ccy_supervisor.py:10`); tests load
  it via `tests/unit/supervise/_load.py`. Editing it directly is correct.
- Proven atomic-write helper already exists: `write_supervisor_status` (`:933`)
  — per-pid temp `.{name}.{pid}.tmp` + `Path.replace()`.
- `supervisor_indicator.py` is the mirror read pattern (fail-silent) to copy.

## 14:37 · decision · — — scope confirmed with user (three requirements)

1. Message channel built as a GENERAL reusable mechanism; Ctrl+Z is its first
   consumer.
2. Message files MUST be thread/process safe.
3. Thread safety made a first-class, DOCUMENTED concern across all supervisor
   and status-line code (entry-point header comments + architecture docs), not
   just this feature.

Concurrency model: the `supervise/` files are touched by the supervisor host,
its `--worker` subprocess, the daemon (status handlers, separate process), and
multiple sessions sharing one daemon (Plan 00127). Rules adopted: atomic-replace
writes only (temp `.{name}.{pid}.{tid}.tmp` + `os.replace`, last-writer-wins);
fail-silent defensive reads; `threading.Lock` for in-process shared state.

## 15:05 · action · P1 — Ctrl+Z guard implemented (RED→GREEN)

- RED: `tests/unit/supervise/test_input_ctrlz_guard.py` (12 tests) — failed on
  missing `strip_suspend`. Two layers: pure `strip_suspend` byte-filter + a
  functional `_forward_io` drive over a stdin pipe + master socketpair (the
  master is bidirectional like a real PTY) proving `0x1a` never reaches the
  child while surrounding bytes pass unchanged.
- GREEN: added `_SUSPEND_BYTE = 0x1A` + `strip_suspend()` (fast-path returns the
  same object when no suspend byte present) near the input-byte constants, and
  applied it in `_forward_io`'s stdin branch. Kept EOF handling intact: a
  suspend-only chunk forwards nothing but is NOT the empty-read EOF branch.
- 71 passed (new + `test_input_line_guard` regression). `mypy` on the supervisor
  exits 0. The Pyright `int | None` warnings in `supervise()` (2402–2490) are
  PRE-EXISTING (present at HEAD) and mypy — the actual QA gate — accepts them.

## 15:07 · finding · P1 — live dogfood is deferred to next launch

The running supervisor process has the OLD code loaded; the edit to
`claude-supervise.py` only takes effect when ccy relaunches (the
`ccy_supervisor_integrity` handler flags a stale running supervisor by source
fingerprint). So Task 4.4 (press Ctrl+Z, confirm no suspend) verifies on the
next session launch, not mid-session. No `__version__` bump was made, so the
supervisor-version-lockstep test stays green.

## 15:30 · action · P2 — message-channel writer + poster (RED→GREEN)

- Added the GENERAL supervisor→status-line channel in `claude-supervise.py`:
  `_STATUS_MESSAGE_FILENAME`/`_STATUS_MESSAGE_TTL_SECONDS`(=10)/`_MIN_INTERVAL`
  (=1)/`_CTRL_Z_NOTICE_TEXT`, `write_status_message()` (atomic temp
  `.{name}.{pid}.{tid}.tmp` + `os.replace`, fail-safe → None), and
  `StatusMessagePoster` (lock-guarded rate limit; injected `wall_clock` +
  `monotonic` for deterministic tests; file write done OUTSIDE the lock).
- Wired via a new `on_suspend: Callable[[], object] | None` param on
  `_forward_io` (fired once per chunk that lost a suspend byte) → in
  `supervise()` bound to `poster.post(_CTRL_Z_NOTICE_TEXT)`. Poster is
  co-located at `sidecar_dir.parent` (== daemon untracked dir) so the daemon's
  status handler reads the same file.
- Type note: the poster returns `Path | None`, so `on_suspend` is typed
  `Callable[[], object]` (fire-and-forget) — the original `-> None` annotation
  was a genuine mypy error, now fixed.
- 9 channel tests + 2 wiring tests; 326 supervise tests pass; mypy clean.

## 15:32 · decision · P2 — TTL = 10s, rate-limit = 1s

The status line renders on Status events (~per turn / periodic), not on
keypress. 10s TTL is long enough that the message survives to the next render in
the common case, short enough that a stale notice clears within a render or two.
1s min-interval means a Ctrl+Z key-mash rewrites the file at most once/second.
Both are named constants, revisable once live dogfood shows real render cadence.

## 15:55 · action · P3 — status-line reader handler (RED→GREEN + live dogfood)

- Added `StatusMessageHandler` (`src/.../handlers/status_line/status_message.py`):
  fail-silent reader of `supervise/status-message.json`, renders `| <text>`
  when present + unexpired, nothing otherwise. `HandlerID.STATUS_MESSAGE` +
  `Priority.STATUS_MESSAGE = 16`; registered in `__init__.py`,
  `.claude/hooks-daemon.yaml`, regenerated `.claude/HOOKS-DAEMON.md`.
- Guarded `expires_at`: `bool` excluded (subclass of int); non-numeric / missing
  → no segment. `_now()` uses WALL clock to match the supervisor's
  `time.time() + ttl`.
- 14 handler tests. Fixed a test-only bug: the `_write` helper used Python
  `repr` (single quotes) → invalid JSON; switched to `json.dumps`.
- 433 tests pass (status_line + dogfooding config). Daemon restarted RUNNING,
  no load errors.
- LIVE dogfood of the reader: wrote a message file, piped a Status event to
  `.claude/hooks/status-line` → rendered `⛔ Ctrl+Z ignored — use /exit to quit`
  between the clock (🕐) and git branch (⎇). Expired message → not shown. Test
  file removed afterward.

## 15:57 · handoff · — — next: Phase 0 docs, then QA gate

Phases 1–3 committed and green. REMAINING: Phase 0 (thread-safety first-class
DOCS — entry-point header comment on `claude-supervise.py`, status_line
`CLAUDE.md` note, and architecture-doc sections) and Task 4.2 (full QA via
`llm_qa.py all`). The supervisor-side Ctrl+Z live test re-verifies on next ccy
relaunch (running supervisor still has pre-edit code).
