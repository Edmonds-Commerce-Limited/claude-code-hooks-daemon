# Plan 00243 — Journal 26-09-07

> **Append-only activity log** for plan 00243. One file per day
> (`00243-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```

## 21:50 · decision · T1.2/T1.3 — the plan's task order was backwards

Task 1.2 says "convert to a literal command every prose test that can be one —
the Write/Edit tests are the bulk". Attempting it first would have been wrong,
and the reason is this project's own rule.

A file written through **Bash** bypasses the content guards that run BEFORE a
`Write`. That is the documented behaviour of `error_hiding_blocker`,
`qa_suppression`, `security_antipattern`, `comment_changelog` and
`sensitive_content` — they key on the `Write`/`Edit` tools, so a `>`, `tee` or
heredoc reaches disk unexamined. So converting

    Use the Write tool to create /repo/x.py with content '<guarded content>'

into

    echo '<guarded content>' > /repo/x.py

does not produce a harness-executable version of that test. It produces a test
that asserts DENY against a path the handler deliberately does not judge — a
guaranteed failure, asserting the opposite of the handler's contract.

The audit already pointed here: its bottom line calls the structured field
"Task 1.3's instinct is correct". So Task 1.3 shipped first, and Task 1.2 is
re-scoped from "regex prose into shell" to "populate the declared field".

## 21:53 · finding · T1.4 — Task 0.2's guard caught the new field unprompted

`ToolPayload` was added to `AcceptanceTest`, and
`test_playbook_generator_json_field_coverage.py` failed immediately:

    AcceptanceTest field(s) ['tool_payload'] are declared but never emitted by
    generate_json, so any harness reading the JSON cannot see them

That test was written under Task 0.2 for defect 2 (`generate_json` dropping
`harness_cannot_produce`), and its docstring says the instance-fix would have
left the class of defect intact, so it derives the expectation from the
dataclass instead. It just paid for itself on the first new field added after
it was written — no one had to remember the JSON dict is hand-built.

Worth recording as evidence for the shape, not just the instance: a guard
keyed to a DERIVED expectation catches the next occurrence; a guard keyed to
the known instance catches nothing.

## 21:56 · decision · T1.3 — mutual exclusion rather than a precedence rule

`harness_cannot_produce` and `tool_payload` could have been given a precedence
("skip wins"), which is what `_skip_block` does for its two skip reasons. They
are not analogous. Those two are both reasons NOT to run, and one genuinely
dominates — no tool install makes an unproducible test runnable.

These two point in opposite directions: one says the input cannot be produced
at all, the other says exactly how to produce it. There is no correct
precedence because one of them is simply false whenever both are set. So
`__post_init__` raises. A silent precedence would have let the wrong one sit
in the tree indefinitely.

## 21:58 · handoff · T1.2 — the remaining conversion, and what it costs

Phase 1 is now one task from done and that task is bulk data entry, not
design: populate `tool_payload` on the 76 prose tests the audit counted (five
grammars, one Write payload), plus the ~26 that describe their content in
English and need a payload invented to match what the handler asserts.

The five handlers that declare zero tests and inherit everything from
`strategies/` still matter here — the audit's warning that a per-handler
converter misses them applies unchanged to a per-handler payload pass. The
strings live in the 75 strategy files.

Nothing in Phase 2 (the harness itself) is blocked by finishing 1.2 first: the
harness must read `tool_payload` when present and SKIP when absent, so it can
be built against the field before every test declares one.
