# Plan 00243 — Journal 26-08-14

> **Append-only activity log** for plan 00243. One file per day
> (`00243-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 13:21 · action · — — plan scaffolded

Plan 00243 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 13:35 · finding · P1 — the 49 "failures" were the harness guessing, not the daemon

Re-measured against a freshly generated playbook rather than trusting the
number from the release run:

```
playbook blocks parsed : 207
  skipped (load/observable): 38
  executed via production wrapper: 169
  matched expected decision: 120
  MISMATCHES: 49
```

Grouped, the 49 are entirely one shape — 44 `tool=Bash expected=deny got=allow`
and 5 `tool=Write expected=deny got=allow`. Sampling the commands shows why:
they are English instructions ("Use the Write tool to write file_path='...'
with content '...'"), not shell commands. The prototype regexes a payload out
of that prose and falls back to sending it as Bash when the phrasing differs.
Nothing was wrong with the daemon.

## 13:38 · decision · P4 — the DBF half is the doc-snippet check

Verifying the dataclass turned up `CLAUDE/CodeLifecycle/Features.md`
documenting `AcceptanceTest(test_id=..., hook_input={...})`. Neither keyword
exists, and three of the five required fields were absent — the documented
example raises `TypeError` on construction. Fixed by hand against a real
shipped example (`sudo_pip`), but the hand-fix is the symptom, not the fix:
nothing validates constructor keyword arguments in documented Python snippets.
That check is Phase 4 and is the part of this plan that keeps paying.

## 13:40 · handoff · — — filed, not started

Plan filed with the prototype tracked alongside it as
`PROTOTYPE-playbook_exec.py` — evidence and a starting point, NOT shipped
code. Deliberately left Not Started: it changes a release gate, which is the
human's call to schedule.
