# Plan 00308 — Journal 26-09-01

> **Append-only activity log** for plan 00308. One file per day
> (`00308-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 17:53 · action · — — plan scaffolded

Plan 00308 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 17:55 · action · — — plan filed from owner request

- Owner (mid-turn): projects are not encouraged enough to enable/configure
  handlers after daemon upgrades — the same manual prompt is needed every
  time; formalise it (e.g. `/hooks-daemon config-optimisation`) and autorun
  it post-upgrade. PLAN.md filed building on the existing `optimise` skill
  rather than forking its analysis.
- Session failsafe recovery cron: reusing the single session cron id
  `892eef87` (canonical plan-agnostic prompt); nothing new created.

## 19:40 · finding · T1.1 — encouragement-surface audit

Every surface a project learns about handler config today was mapped; full
findings in `subagent-reports/260901-implementation-sonnet.md`. Core gap:
`/optimise` was already a complete analysis+apply engine — zero automatic
trigger existed anywhere in install/upgrade, so it only ran when a human
remembered to ask for it verbatim.

## 19:55 · decision · T2.1 — promote /optimise, do not fork

Chose to promote the existing `optimise` skill rather than add a parallel
`config-optimisation` command — the plan's own Non-Goal forbids
reimplementing the five-area analysis. Added a Step 0 (diff current config
against `CLAUDE/UPGRADES/config-changes/v*.yaml` manifests newer than the
last recorded run) and a Step 7 (always record the run) to
`.claude/skills/optimise/invoke.sh`.

## 20:05 · action · T3.2 — session-start reminder handler

New SessionStart handler `config_optimisation_reminder` (priority 67),
`claude_code_hooks_daemon.config_optimisation.state` module, and CLI
subcommand `record-config-optimisation-run`. TDD throughout — test file
written before each source file. Modelled directly on
`skill_opportunity_detector`'s state-file/advisory pattern (same
fail-open-to-silence contract).

## 20:15 · action · T3.1 / T3.3 — upgrade + install flow wiring

Wired the mandatory step into
`src/claude_code_hooks_daemon/skills/hooks-daemon/upgrade.md` (new Step 8),
`scripts/upgrade.sh` / `scripts/upgrade_version.sh` (new
`--skip-config-optimisation` flag, forwarded via the pre-existing
`UPGRADE_FLAGS` env channel that `--force` already used), and replaced
`LLM-UPDATE.md`'s manual 5-step "What to Do with New Handlers" walkthrough
with one numbered mandatory step pointing at `/optimise`. Added the
equivalent closing step to `LLM-INSTALL.md`.

## 20:25 · action · — full verification pass

`pytest tests/unit` (14707 passed; 2 pre-existing skips; 1 pre-existing
xfail; 3 pre-existing semgrep-not-installed errors, unrelated),
`pytest tests/integration -k "registry or config or handler_count or
docs_generation or init_config"` (125 passed after fixing the example
config + the `_EXEMPT_FROM_GUIDANCE` coupling my new handler triggered),
`ruff` + `mypy` clean on all touched Python. Also fixed two error-hiding
self-audit regressions caused by these edits: a genuine new
log-and-continue pattern in `config_optimisation/state.py` (added an
exclusion mirroring `skill_scan/state.py`'s identical, already-exempted
pattern) and a stale line-keyed exclusion in `scripts/upgrade.sh` whose
target line shifted 373 → 382 when new code landed above it (updated the
line number only — the underlying, unrelated, pre-existing suppressed code
was not touched).

## 20:30 · thought · — owner design question mid-turn

Owner asked why T3.2's session-start state-tracking reminder is needed at
all if the upgrade flow (T3.1) already "bakes in" the review — reads as
overly complex. Answer: T3.1 makes `/hooks-daemon upgrade` invoke
`/optimise` automatically, but that path needs an agent in the loop and can
be skipped (`--skip-config-optimisation`) or bypassed entirely by the
curl+script-only upgrade path, which never talks to a Claude Code session at
all. T3.2 is the safety net for exactly those cases — it was an explicit,
separate PLAN.md task written by the owner, not an addition invented during
implementation.
