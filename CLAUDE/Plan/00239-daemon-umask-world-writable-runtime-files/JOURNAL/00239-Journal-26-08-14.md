# Plan 00239 — Journal 26-08-14

> **Append-only activity log** for plan 00239. One file per day
> (`00239-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 07:00 · action · — — plan scaffolded

Plan 00239 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

Failsafe recovery cron for this session: `cae1a725` (`:37` hourly,
non-durable). Reused, not duplicated.

## 07:10 · finding · T1.1 — the defect reproduced, and the report's headline did not

Live daemon: `Umask: 0000`. Against every other process in the container
(`0022`), so the daemon overwrites its own rather than failing to inherit one —
which also means no entrypoint, shell or unit file can influence it, exactly as
the field report says.

Measured group/other-**writable** artefacts under `untracked/` (excluding venvs)
— note `-perm /077` is the wrong filter here, it catches `0644`; the bug shape
needs `-perm /022`:

```
666 untracked/background-processes.jsonl
666 untracked/context-sidecar/<session>.json
666 untracked/daemon-<host>.pid
666 untracked/daemon-<host>.socket-path
666 untracked/gitignore_safety_cache.json
666 untracked/logs/hooks/verdicts.jsonl
666 untracked/stop-events.jsonl
666 untracked/thread-registry/<session>.json
666 untracked/thread-registry/probe-session.json
666 untracked/version_check_cache.json
777 untracked/__pycache__
777 untracked/context-sidecar
777 untracked/payload-capture
777 untracked/thread-registry
```

10 files + 4 directories.

**The report's headline artefact cannot be reproduced.** It leads with 8.3 MB of
`transcript_archiver` conversation archives at `0666`; Plan 00233 removed that
handler and `untracked/transcripts/` does not exist. `src/.../handlers/pre_compact/`
now holds only `compaction_signal.py` and `hello_world.py`.

That is not a reason to downgrade the fix, but it does move the argument: the
sensitive-content case now rests on `payload-capture/` (0777), which the report
does not mention, plus the verdict log and `stop-events.jsonl`. Worth stating
plainly rather than repeating the report's framing, because a fix justified by
evidence that no longer exists is a fix nobody can re-verify.

## 07:12 · decision · T1.3 — 0o007, not the report's 0o077

The report's preferred Option A is `os.umask(0o077)`. That is wrong for this
project, and the report flags the exact caveat without being able to resolve it
from outside ("if any deployment genuinely relies on a second user reading
daemon runtime files"). It does. Three independent pieces of evidence:

- `server.py:657` — the socket is `chmod(0o660)` AFTER bind. Group, not
  owner-only. A mode chosen deliberately so a second process can connect.
- `CLAUDE.md` — a host and a container sharing a bind-mounted `untracked/`
  resolve the same `(hostname, project root)` and **deliberately share one
  daemon**. Those two contexts routinely run as different UIDs.
- `.claude/init.sh` — the forwarder, running as whoever launched Claude Code,
  reads `PID_PATH` (line 575, `cat "$PID_PATH"`) and the `.socket-path`
  discovery file (478–482).

Under `0o077` the PID file and `.socket-path` become `0600` and the shared-daemon
setup breaks for the non-owning UID. `0o007` keeps group and strips other:
files `0660`, dirs `0770`. Both explicit-mode sites are unaffected —
`0o600 & ~0o007 == 0o600` for the lock, and the socket's mode is a post-bind
`chmod` that no umask touches.

The exposure the report is actually about — "any local user can read every
archive" — is *other*-access, and `0o007` removes it completely.

## 07:18 · finding · T1.2 — 1 of 98 create sites passes an explicit mode

Enumerated every runtime create under `src/claude_code_hooks_daemon/` (mkdir,
makedirs, write_text, write_bytes, os.open, open-for-write, tempfile, replace),
excluding `install/` and `skills/`:

- **98 sites total**, of which **35** are directory creates (all `0777` today).
- **Exactly one** passes an explicit mode: `server.py:727`,
  `os.open(str(lock_path), os.O_RDWR | os.O_CREAT, 0o600)`.

The heaviest concentrations with no explicit mode: `daemon/cli.py` (19),
`daemon/paths.py` (8), then `thread_registry.py`, `context_sidecar.py` and
`markdown_organization.py` at 4 each.

**This settles the A-vs-B design question the report leaves open.** Its Option B
is "keep `umask(0)` and pass explicit modes at every sensitive create". At 97
unguarded sites that is not a fix, it is a permanent audit obligation — and one
the codebase has already failed once, since the `umask(0)` contract requires
exactly that discipline and got it in a single place out of 98. The umask is the
one choke point that fixes every current and future create at once, which is the
SINGLE SOURCE OF TRUTH principle applied to permissions.

Option C's explicit modes therefore stay deliberately narrow — defence in depth
on `payload-capture/` only, the one artefact whose contents are known-sensitive,
so the posture survives someone later "restoring" the textbook `umask(0)` line.
Not a sweep of 97 sites.
