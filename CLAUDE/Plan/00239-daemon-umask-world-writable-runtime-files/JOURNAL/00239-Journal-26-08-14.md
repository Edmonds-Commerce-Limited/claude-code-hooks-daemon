# Plan 00239 — Journal 26-08-14

> **Append-only activity log** for plan 00239. One file per day
> (`00239-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 07:00 · action · — — plan scaffolded

Plan 00239 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

Failsafe recovery cron for this session: `cae1a725` (`:37` hourly,
non-durable). Reused, not duplicated.

## 07:10 · finding · T1.1 — the defect reproduced, and the report's headline did not

Live daemon: `Umask: 0000`. Against every other process in the container
(`0022`), so the daemon overwrites its own rather than failing to inherit one —
which also means no entrypoint, shell or unit file can influence it, exactly as
the field report says.

Measured group/other-**writable** artefacts under `untracked/` (excluding venvs)
— note `-perm /077` is the wrong filter here, it catches `0644`; the bug shape
needs `-perm /022`:

```
666 untracked/background-processes.jsonl
666 untracked/context-sidecar/<session>.json
666 untracked/daemon-<host>.pid
666 untracked/daemon-<host>.socket-path
666 untracked/gitignore_safety_cache.json
666 untracked/logs/hooks/verdicts.jsonl
666 untracked/stop-events.jsonl
666 untracked/thread-registry/<session>.json
666 untracked/thread-registry/probe-session.json
666 untracked/version_check_cache.json
777 untracked/__pycache__
777 untracked/context-sidecar
777 untracked/payload-capture
777 untracked/thread-registry
```

10 files + 4 directories.

**The report's headline artefact cannot be reproduced.** It leads with 8.3 MB of
`transcript_archiver` conversation archives at `0666`; Plan 00233 removed that
handler and `untracked/transcripts/` does not exist. `src/.../handlers/pre_compact/`
now holds only `compaction_signal.py` and `hello_world.py`.

That is not a reason to downgrade the fix, but it does move the argument: the
sensitive-content case now rests on `payload-capture/` (0777), which the report
does not mention, plus the verdict log and `stop-events.jsonl`. Worth stating
plainly rather than repeating the report's framing, because a fix justified by
evidence that no longer exists is a fix nobody can re-verify.

## 07:12 · decision · T1.3 — 0o007, not the report's 0o077

The report's preferred Option A is `os.umask(0o077)`. That is wrong for this
project, and the report flags the exact caveat without being able to resolve it
from outside ("if any deployment genuinely relies on a second user reading
daemon runtime files"). It does. Three independent pieces of evidence:

- `server.py:657` — the socket is `chmod(0o660)` AFTER bind. Group, not
  owner-only. A mode chosen deliberately so a second process can connect.
- `CLAUDE.md` — a host and a container sharing a bind-mounted `untracked/`
  resolve the same `(hostname, project root)` and **deliberately share one
  daemon**. Those two contexts routinely run as different UIDs.
- `.claude/init.sh` — the forwarder, running as whoever launched Claude Code,
  reads `PID_PATH` (line 575, `cat "$PID_PATH"`) and the `.socket-path`
  discovery file (478–482).

Under `0o077` the PID file and `.socket-path` become `0600` and the shared-daemon
setup breaks for the non-owning UID. `0o007` keeps group and strips other:
files `0660`, dirs `0770`. Both explicit-mode sites are unaffected —
`0o600 & ~0o007 == 0o600` for the lock, and the socket's mode is a post-bind
`chmod` that no umask touches.

The exposure the report is actually about — "any local user can read every
archive" — is *other*-access, and `0o007` removes it completely.

## 07:18 · finding · T1.2 — 1 of 98 create sites passes an explicit mode

Enumerated every runtime create under `src/claude_code_hooks_daemon/` (mkdir,
makedirs, write_text, write_bytes, os.open, open-for-write, tempfile, replace),
excluding `install/` and `skills/`:

- **98 sites total**, of which **35** are directory creates (all `0777` today).
- **Exactly one** passes an explicit mode: `server.py:727`,
  `os.open(str(lock_path), os.O_RDWR | os.O_CREAT, 0o600)`.

The heaviest concentrations with no explicit mode: `daemon/cli.py` (19),
`daemon/paths.py` (8), then `thread_registry.py`, `context_sidecar.py` and
`markdown_organization.py` at 4 each.

**This settles the A-vs-B design question the report leaves open.** Its Option B
is "keep `umask(0)` and pass explicit modes at every sensitive create". At 97
unguarded sites that is not a fix, it is a permanent audit obligation — and one
the codebase has already failed once, since the `umask(0)` contract requires
exactly that discipline and got it in a single place out of 98. The umask is the
one choke point that fixes every current and future create at once, which is the
SINGLE SOURCE OF TRUTH principle applied to permissions.

Option C's explicit modes therefore stay deliberately narrow — defence in depth
on `payload-capture/` only, the one artefact whose contents are known-sensitive,
so the posture survives someone later "restoring" the textbook `umask(0)` line.
Not a sweep of 97 sites.

## 07:55 · decision · T1.3 — REVERSED: 0o077, not 0o007. I was wrong.

Sent an adversarial reviewer at the 07:12 decision with instructions to refute
it rather than confirm it, and worktree isolation so it could not touch the
index. It refuted it, and I verified every load-bearing claim myself before
accepting.

The three premises of the `0o007` argument, each checked:

- **"The socket's 0o660 proves group access is deliberate."** `server.py:655`,
  the comment immediately above the chmod, reads *"owner read/write, group read,
  world none"* — which describes `0o640`. The code writes `0o660`. An author
  reasoning about group WRITE on an IPC socket does not write "group read" above
  it. It is a typed-from-memory idiom. Confirmed further: `grep -rE
  "chown|chgrp|SO_PEERCRED|getpeereid"` over `src/`, `scripts/` and `init.sh`
  returns exactly ONE hit, and it is a config-comment string. Group access to a
  file whose group is never set is not a design.
- **"A host and a container share one daemon at different UIDs."** Documented,
  but already impossible today: `server.py:727` opens the start-lock
  `os.open(..., 0o600)` and leaves it on disk for reuse, so a second UID's
  `start` gets EACCES regardless of umask. The daemon is also a descendant of
  every process that reads its files, and nothing setuids.
- **"init.sh reads PID_PATH and .socket-path as the launching user."** True —
  and same-UID, for the reason above.

So the premise I was most confident in — the one I told the user I had
"independently established" — was the weakest of the three. It was real
evidence read correctly and reasoned from wrongly: `0o660` is a fact about the
code, not a fact about intent, and I promoted it to intent because it fit.

`0o007` is never safer than `0o077`, and on a host with a shared primary group
(`staff`, `users`, a service account) it leaves the verdict log,
`stop-events.jsonl` and `payload-capture/` group-readable AND group-writable.
`0o077` is deployment-invariant. Both explicit-mode sites survive it.

## 08:00 · finding · T2.4 — the daemon world-writables entire SOURCE CHECKOUTS

The review surfaced something neither the field report nor Phase 1 caught, and
the proof was sitting in the reviewer's own isolation worktree:

```
777 .claude/worktrees/agent-<id>/
666 .claude/worktrees/agent-<id>/README.md
666 .claude/worktrees/agent-<id>/pyproject.toml
666 .claude/worktrees/agent-<id>/.claude/settings.json
--- same files in the main repo ---
644 /workspace/README.md
644 /workspace/pyproject.toml
644 /workspace/.claude/settings.json
```

`worktree_create` runs `git worktree add` as a child of the umask-0 daemon, so
git creates the whole checkout under umask 0. This is not a runtime-file
problem, it is a SOURCE TREE problem, and it inverts the fix: a checkout should
be `0644`/`0755`, so under a bare `0o077` it would become `0600`/`0700` —
functional for one user but wrong for source, and it would surface as spurious
mode churn wherever `core.fileMode` is true. That subprocess needs a
*permissive* `0o022`, making it the one place the daemon needs a LOOSER umask
and the one place it currently exercises none.

Two further umask-governed sites, both the same tmp-then-replace shape:

- `settings_repair.py:105-106` replaces the **git-tracked**
  `.claude/settings.json` with a temp file, so the tracked file inherits the
  temp's mode. Its sibling `hook_command_migration.py:178` rewrites in place and
  is immune; the two differ for no reason.
- `retention.py:62-64` does the same to `verdicts.jsonl` and
  `stop-events.jsonl`, so a manual `chmod` is undone at the next trim.

**One correction to the reviewer**, checked rather than accepted: it stated
`settings_repair` resets the mode "on essentially every SessionStart". Live,
`.claude/settings.json` is `0644`, so that path has not been firing here. The
mechanism is real; the claimed frequency is not evidenced. Taking that at face
value would have put a false frequency claim into the plan.

## 08:02 · thought · — the delegation that worked, and why

Three agents this session. The two I fired in the shared checkout, in the
background, with "audit this" prompts returned nothing across four idle pings
and left a stale `index.lock`. The one I fired synchronously, in a worktree,
told to REFUTE a specific named conclusion, overturned my decision and found a
class of exposure nobody had looked at.

The difference is not the model. It is that the third had a falsifiable target
and nowhere to hide: "attack this claim" produces evidence, "audit this area"
produces a summary. Worth remembering next time I reach for a background agent
to go and look at something.

## 07:30 · finding · T2.4 — both mechanisms I had in mind for the worktree umask are unsound

Handler dispatch does NOT run on the asyncio loop thread. `server.py:1085`:

```
hook_result = await loop.run_in_executor(None, self.controller.dispatch, hook_input)
```

`None` = the default `ThreadPoolExecutor`, so handlers run concurrently in
worker threads. That rules out both candidates for scoping a looser umask to
`git worktree add`:

- **A `temporary_umask` context manager** sets process-global state. Another
  handler creating a file in a second worker thread during the window would
  silently inherit the wrong mask. A race that only shows up under concurrent
  dispatch, i.e. exactly when agents are being spawned.
- **`subprocess(preexec_fn=...)`** — CPython documents `preexec_fn` as unsafe in
  the presence of threads (it can deadlock on locks held across the fork). The
  daemon has threads by construction.

Neither is a "probably fine" — both are the shape of bug this project's
FAIL-FAST rule exists to keep out. If T2.4 goes ahead the mechanism has to be
post-hoc and thread-local by nature: walk the new checkout and mirror the owner
bits down to group/other with write masked off (0600→0644, 0700→0755), which is
what `umask 022` would have produced, computed rather than inherited.

## 07:31 · finding · T2.4 — my "spurious mode churn" claim was wrong

The plan argues a `0o077` worktree "would surface as spurious mode churn
wherever `core.fileMode` is true". Measured, in a throwaway repo with
`core.fileMode=true`:

```
chmod 600 f.txt   ->  git status --porcelain: (empty)
chmod 755 f.txt   ->  git status --porcelain:  M f.txt
```

git tracks ONLY the executable bit (100644 vs 100755). Dropping group/other
READ is invisible to git. I wrote that claim from a general recollection of
`core.fileMode` without checking it, and it is false.

What survives of the T2.4 case is narrower and worth stating honestly: after the
umask fix a daemon-created worktree is `0600`/`0700` — strictly SAFER than
today's `0666`/`0777`, and invisible to git. The only real cost is cross-UID
readability: in this project's own ccy setup the daemon runs as root in a
container over a bind-mounted `/workspace`, so a `0700` worktree cannot be
opened by the host user. That is a usability question about one directory, not
a security one, and it should be decided on a measurement after the umask
lands — not bundled into the security fix on the strength of an argument I have
now had to retract once.

T2.4 is therefore deferred behind Phase 2's other tasks and re-decided in
Phase 3 against a real restarted daemon.

## 07:50 · finding · T3.1 — measured against the live daemon, and the old files stay wrong

Restarted; `Umask: 0077`. Files the daemon recreated on that restart:

```
600 untracked/daemon-<host>.pid            (was 666)
600 untracked/daemon-<host>.socket-path    (was 666)
660 untracked/daemon-<host>.sock           (unchanged — explicit post-bind chmod)
```

Every pre-existing artefact was still `666`/`777`, because a umask governs
CREATES and nothing else. So I moved the sensitive ones aside and drove a real
dispatch through the production forwarders (`.claude/hooks/stop`,
`.claude/hooks/pre-tool-use`) to force fresh creates:

```
600 untracked/logs/hooks/verdicts.jsonl
600 untracked/stop-events.jsonl
700 untracked/thread-registry
700 untracked/context-sidecar
```

`untracked/logs` and `untracked/logs/hooks` still read `755` — I checked rather
than assumed, and their mtime is 2026-01-27, i.e. they predate the fix and were
never recreated. Consistent with the helper's documented scope.

The 44,179-line verdict log I moved aside is the firing data a later handler-value
argument depends on, so it was merged back by truncating the NEW file in place
(`>` keeps the inode, hence the 0600) rather than moving the old file back, which
would have restored the 666 along with the content.

**This is the whole case for Task 3.4.** Nothing here retro-fixes an existing
install: every daemon already deployed keeps its world-writable files until
someone chmods them. A write-time fix cannot see what is already on disk.

## 07:52 · finding · T3.2 — cross-UID was already blocked by the SOCKET, not the umask

Created a second user and had it try the two things the "keep group access"
argument was built on:

```
600 root:root untracked/daemon-<host>.pid
660 root:root untracked/daemon-<host>.sock

$ su permprobe -c 'cat .../daemon-<host>.pid'
cat: ...: Permission denied
$ su permprobe -c 'echo ... | nc -U .../daemon-<host>.sock'
nc: ...: Permission denied
```

The socket refusal is the important one: it is `0660 root:root`, it is set by an
explicit post-bind `chmod` that this plan never touches, and no code anywhere
sets a shared group. So a second UID could not use this daemon BEFORE the change
either — and, decisively, **the group-preserving `0o007` would not have changed
that**, because group access to a file whose group is only ever the daemon's own
primary group buys nothing.

That closes the 07:12 argument empirically rather than by reasoning. The thing I
argued for on 07:12 would have cost real security and bought exactly nothing
measurable.

What the change DOES remove is cross-UID *reading* of runtime files — in the ccy
setup (root daemon in a container, non-root user on the host, shared bind-mounted
`untracked/`) the host user can no longer read the verdict log or the PID file.
That is the intended fix, not a casualty: those files hold hook payloads and
decision records. It belongs in the upgrade note (T3.5).

## 08:05 · action · T3.3 — QA 21/21 and client mode verified

`llm_qa.py all` → **21/21 PASSED**, 12,270 tests, coverage 95.1%.

Client mode via `scripts/dummy-client-repo.sh` (production installer, not
synthesised state) — this change alters deployed runtime behaviour, so a
self-install pass alone would not be evidence:

```
Umask: 0077                                          (client daemon)
600 .../daemon-dummy-client-repo.pid
600 .../daemon-dummy-client-repo.socket-path
600 .../daemon-dummy-client-repo.sock.start.lock     (the explicit-mode site, intact)
```

`hooks-daemon check-permissions` in the client repo: clean, exit 0. A fresh
install is now correct from its first run, which is the property that matters —
the dogfood repo could only ever demonstrate the remediation path.

## 08:11 · finding · T3.3 — a residual: CLI-written files use the SHELL's umask, not the daemon's

The client-mode listing showed three files at `0644` rather than `0600`:
`cleanup_status.json`, `.python-cmd-cache`, `.gitignore`. Two of those are the
installer's (already out of scope — Plan 00165). The third is not, and I checked
the code rather than assuming:

`_reap_stale_runtime_files` calls `write_cleanup_status`, and its own docstring
says it "Runs on EVERY start attempt — including the Plan 00127 reuse path,
**which returns before the fork**". So it executes in the `hooks-daemon start`
CLI process, which has the invoking shell's umask (`0022` here), not the
daemonised child's `0077`.

Measured consequence: `0644` — other-READABLE, not other-writable, so not the bug
class this plan is about. But it is umask-dependent in a way the daemon does not
control: a user whose shell has `umask 000` would get `0666` for these.

**Left out of scope deliberately, and visibly.** Applying the daemon umask at CLI
entry would change the mode of everything every subcommand writes, which is a
much wider blast radius than the defect justifies. The batch guard already covers
the case that matters: under a permissive shell umask `check-permissions` flags
the file and `--fix` tightens it. That is the DBF answer — the guard can see it,
so it does not need a bespoke fix.

This also means the plan's original success criterion "zero **other-readable**
artefacts" does not hold as written, and I have corrected the criterion rather
than tick it. Zero other-WRITABLE does hold, and that was always the real target.

## 08:12 · decision · T2.4 — closed with NO code change, measured against the fixed daemon

Drove a real `WorktreeCreate` through the production forwarder against the
restarted daemon:

```
before (old umask):  777 .claude/worktrees/agent-<id>/   666 README.md  666 pyproject.toml
after  (0o077):      700 .claude/worktrees/umask-probe-*/ 600 README.md  600 pyproject.toml
```

The exposure this task existed for is gone. Both arguments for going FURTHER —
making the checkout `0644`/`0755` like the main repo — had already failed:

- the "spurious mode churn under `core.fileMode`" claim was mine and is false
  (git tracks only the executable bit);
- both mechanisms for scoping a looser umask to the subprocess are unsound
  under thread-pool dispatch (`temporary_umask` races, `preexec_fn` is
  documented thread-unsafe), leaving a post-hoc chmod walk as the only safe one.

So what remained was: pay for a walk over every file of every new worktree, plus
handler complexity, to buy a cross-UID READ convenience (a host user opening a
container-created worktree) that nobody has reported wanting. That is speculative
generality, and the honest answer is no. If it is ever asked for, a documented
`chmod -R` or a config option is the fix — not code shipped on a guess.

Worth naming what almost happened: this task was written into the plan as a
confident "needs a permissive 0o022", and both of its supporting arguments
dissolved when checked. Had I implemented it when it was written, I would have
shipped a thread-unsafe umask race to solve a problem that did not exist.

Cleaned up: both worktrees removed after confirming each had no dirty files and
no commits off main, and their merged branches deleted with `git branch -d`.
