# Plan 00262 — Journal 26-08-19

> **Append-only activity log** for plan 00262. One file per day
> (`00262-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 11:36 · action · — — plan scaffolded

Plan 00262 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 11:55 · finding · — — filed from a self-inflicted incident

Provenance matters here, so recording it plainly: this plan exists because I
caused the problem, not because anyone audited for it.

Resuming after a compaction during Plan 00261, I started a QA run without
checking whether one was already in flight. There was — the pre-compaction run
was still going. Two suites raced for about seven minutes, both driving the same
`tests/` tree and both writing `untracked/qa/coverage.json`. I killed both and
re-ran one clean.

The worse half is what happened EARLIER in the same session: a
`test_install_sh_end_to_end` failure from the same cause was written off as
interference and moved past. That is the real damage a contended run does — not
the wasted minutes, but that it teaches you to discount failures on a gating
check.

Two details that shape the fix, both verified rather than assumed:

- `--read-only` does not run tools (`llm_qa.py:604`, the loop is guarded by
  `if not read_only`), so the lock belongs on the executing path only. Locking
  read-only too would block the one command useful for inspecting a live run.
- `pyproject.toml:114` records coverage `parallel` mode as disabled because it
  "causes fork bomb when running pytest with --cov". So concurrency was never
  supported at the coverage layer — this is not a small overlap to tidy.

Killing the runs also had a wrinkle worth remembering: `kill -- -<pgid>` on the
bash wrapper's group did NOT reap the suite. `llm_qa.py` runs in its own process
group, so the wrapper died and the QA kept going. Needed a second pass against
the python process groups.

## 11:57 · decision · — — Phase 2 is scoped to DECIDE, not to fix

The acceptance-test worktree collision (`test_install_sh_end_to_end.py:67-98`
mutating the shared `.git/worktrees/`) is real but secondary, for two reasons.

First, Plan 00105 Task 1.3 shows it is a deliberate v3.11.0 trade-off — the
clone helpers were added for the upgrade tests and `_create_daemon_worktree` was
explicitly "retained for install tests where worktrees still work". It was a
choice, not an oversight, and re-opening a considered decision needs a reason
beyond noticing it.

Second, Phase 1 largely subsumes it: once the suite runner refuses to run twice
at once, the collision is no longer reachable by the route that actually bit us.
What remains is someone invoking pytest on that file directly, in parallel.

So Phase 2 must record a decision either way, INCLUDING "leave it, 00105's
trade-off stands". A task that can only be closed by changing code quietly
pressures the next agent to change code.

## 11:58 · action · — — dedupe and cron state

Dedupe scout checked all 36 then-live plans: nothing covers either finding.
Nearest misses were Plan 00257 (QA + releases, but that is `release_blocker`
handler behaviour, not concurrent runner instances) and Plan 00189 (worktrees,
but the `WorktreeCreate` handler, not test isolation).

`CronList` shows exactly one failsafe recovery cron — `8326de0b`, hourly at :23
— already covering this session, so none was created for this plan. A second
would only double-fire on the same session. A background-process watchdog
`1df3ca44` (hourly at :41) is also live.
