# Plan 00218 — Journal 26-08-12

> **Append-only activity log** for plan 00218. One file per day
> (`00218-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 15:59 · action · — — plan scaffolded

Plan 00218 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 16:05 · finding · P0 — briefed premises verified, one of three is FALSE

Working in isolated worktree `agent-ad3d35fcc36d13959-4c36fb42`. No recovery
cron: this thread is a sub-agent with no Cron tools; the parent session owns
that.

1. **"The 500-char cap exists only in the integration test."** TRUE.
   `tests/integration/test_plan_index_navigability.py` declares
   `MAX_LINE_CHARS = 500`; `test_no_index_row_is_a_paragraph` is its only
   consumer. A repo-wide grep finds no second definition of this limit (other
   `500` hits are unrelated — retry delays, `plan-doc-size`'s warning-lines
   tier, `pipe_blocker`'s echo cap).

2. **"The `plan_qa` catalogue has no row-length rule."** TRUE. Enumerated the
   registry directly: **35 registered specs, 30 distinct check ids**. The brief
   said 31; the gap is just how dual COMMIT+SWEEP registrations are counted —
   five checks register twice. None measures line length.

3. **"`plan-doc-size` enforces the sibling concern at BOTH edit and commit
   time."** **FALSE.** `plan-doc-size` is registered at `Stage.EDIT` only;
   there is no commit-stage size check at all. The asymmetry argument as
   briefed does not hold in that form. The narrower version still stands and is
   what this plan rests on: `plan_qa` owns a fast-loop surface for the plan
   document shape rules it has, and owns nothing at all for the index.

## 16:12 · finding · T4.1 — calibration: current index is clean at 500

Ran the candidate rule over `CLAUDE/Plan/README.md` BEFORE wiring anything:

```
file bytes: 100,732   lines: 1,176
naive all-lines >500: 0 -> []
top 12 line lengths: [485, 465, 462, 460, 456, 441, 432, 422, 415, 410, 406, 398]
```

Zero flagged, matching the brief's expectation. The naive all-lines definition
agrees exactly with the existing test because it *is* the existing test's
definition — the test iterates every line, not parsed rows.

Takeaway: headroom on the worst line is 15 characters. Thin, which is itself an
argument for the rule having a fast loop.

`git log` corroborates: `90724043 Fix: two plan-index rows exceeded the
500-char navigability limit` is the immediately preceding commit.

## 16:20 · decision · P3 — mode overrides level; this settles the EDIT question

Coordinator flagged mid-task that a check's declared `Level` is not what decides
whether it blocks — the surface's *mode* overrides it. Confirmed in this
project's `.claude/hooks-daemon.yaml`:

```
edit_mode: block
commit_gate_mode: warn      # warn-first rollout; flip to block after clean dogfooding
sweep_mode: advise
```

and `sweep_mode` is typed `Literal["advise", "off"]`, so a SWEEP registration
can never block by construction.

This resolves the brief's open question rather than complicating it. I was asked
to think hard about whether the EDIT surface justified extending the handler.
It does, for a reason only visible in the config: **EDIT is the only surface
whose live mode matches the strictness of the existing test.** That test FAILS
the suite; `commit_gate_mode: warn` downgrades every commit finding to
advisory. Commit-gate-only would have shipped a fast loop strictly weaker than
the batch guard it is meant to shorten.

Honest claim to carry forward: under current config this rule DENIES at edit
time and ADVISES at commit time. Not "blocks over-long rows" generally.

## 16:26 · finding · T3.1 — widening the edit gate is safe

`PlanQaEditHandler._is_lintable_plan_file` admits only `PLAN.md` and journal
day-files, so the index README is currently never linted. Before widening it I
checked whether any sibling EDIT check would then misfire on a README:
`archive_immutability`, `template_metadata`, `terminal_placement_hint` and
`path_existence` all scope through `edit_target()`, which returns `None` for
anything that is not a plan document; the journal checks scope through
`journal_edit_target()`, same story. So no sibling can fire on the index.

The classifier already models `PlanFileKind.PLAN_INDEX` (`plan_qa/paths.py`),
so the index is a first-class concept in this package. The extension is wiring,
not a new concept — which is what makes it proportionate.
