# Plan 00293 — Journal 26-08-30

> **Append-only activity log** for plan 00293. One file per day
> (`00293-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 20:23 · action · — — plan scaffolded

Plan 00293 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 20:25 · decision · — — plan filed from owner direction

Owner direction recorded verbatim in PLAN.md overview: disable-at-source for
never-wanted tools (Artifact the poster child), transcript-scanning analyser
for never-used tools, tools-vs-tokens report with the decision left to
projects, dogfooded here. Dedupe scout checked 36 live plans: no coverage
(00116/00161/00176 tangential only). Failsafe recovery cron: reusing session
cron `0837855f`; nothing created.

## 20:39 · finding · T1.1 — Phase 1 research complete; RESEARCH-tool-disable.md written

Load-bearing answer: source-level disables (`enableArtifact: false`,
`CLAUDE_CODE_DISABLE_ARTIFACT=1`, `--tools`, bare-name `permissions.deny`)
remove the tool's schema from context; specifier/parameter deny rules are
call-time refusal only, zero token savings. Measured this session's rendered
schemas (transcribed to scratchpad, tiktoken cl100k): Artifact = 6,038 tokens
(64% of the ~9.4k tool-definition budget); 11 built-ins arrive deferred at ~8
tokens each, so deferral already covers rarely-used tools — full disable is
for never-wants and upfront-loaded tools only. Artifact is upfront-loaded.

Reddit lead (r/ClaudeCode 1w2ja43) fetched via Lightpanda (reddit blocks curl
and WebFetch; mirrors are Anubis-gated): all three disable routes CONFIRMED
against primary docs; ~10k Artifact claim right order of magnitude (rendered
size is feature-gated per session); "skill can re-enable it" comment
unverified folklore; /chrome 22k number unverified. Dead ends logged: nested
`claude -p` has no login credential in this container (no live /context A/B
— deferred to Phase 4 dogfood); binary string extraction over-counts because
descriptions are assembled from conditional template fragments at runtime.

T1.2: 52 PreToolUse handlers reviewed. `artifact_publish_blocker` is the ONE
source-disable candidate; everything else polices content/semantics of tools
the project wants. Key nuance: the handler's allow-list/read-deny-publish
granularity is INEXPRESSIBLE in permissions.deny (`Artifact(action:publish)`
is unsound — an omitted `action` defaults to publish and omitted params never
match), so the owner must choose: keep list (+6k tokens/session) or disable
outright and demote the blocker to a backstop. Five open questions for the
owner recorded at the end of the research doc.

## 20:52 · decision · T1.3 — owner directed: extend blocker with source-disable, dogfood here

Owner (verbatim intent): "extend artifact publish blocker to include full
artifact block - updates claude settings directly to fully disable the tool -
optional extra feature. dog food directly on this repo." This answers research
open question 1 (disable outright) and carves the Artifact case out of the
Phase 3 advisory: enforcement lives in the blocker itself.

## 20:53 · action · T1.3 — source_disable option TDD'd green

RED first (7 new failures), then implementation: `source_disable` option on
`ArtifactPublishBlockerHandler` (ships off). On the first PreToolUse event of
any kind after daemon start — deliberately not requiring an Artifact call,
since a holding disable means none ever comes — the handler ensures
`.claude/settings.json` has `"enableArtifact": false`. Additive merge, no-op
when already false, atomic temp+replace with copymode (settings_repair's
pattern), one-shot backup `settings.json.bak.pre-artifact-source-disable`,
warnings-not-raises on unreadable/malformed files. Deny reason gains a
source-disable note only when the option is on; get_claude_md and
HANDLER_REFERENCE updated. 33/33 tests green; black/ruff/mypy clean.

## 20:54 · action · T1.4 — dogfooded: this repo's settings now disable Artifact

Enabled `options.source_disable: true` in `.claude/hooks-daemon.yaml`,
`bin/hooks-daemon restart` → RUNNING, and the next tool call's PreToolUse
event wrote `"enableArtifact": false` into `.claude/settings.json` (other
keys intact, backup created). From the next session onward the Artifact tool
— and its ~6k-token schema — should be absent; the blocker stays as the
in-session backstop for live sessions. Remaining verification for Task 4.1:
a `/context` spot check in a fresh interactive session to confirm the schema
is actually gone (this container's nested claude has no login).

## 20:52 · decision · — — owner rulings on the Artifact question + tooling

Coordinator's transcript measurement grounding the decision: across all 12 of
this project's session transcripts, Artifact was invoked 3 times total in one
session, only `action: list` — zero publishes ever, ~6k tokens paid per
session per agent for it.

Owner rulings (verbatim intent): (1) "add a fully disable option to the hooks
daemon config" — instructed an agent DIRECTLY, work in flight (config option
+ artifact_publish_blocker adaptation visible in the working tree);
(2) "we should ship some tooling to allow projects to scan for tools they
don't use which could be disabled — a future useful feature we can run every
now and then" — confirms Phase 2/3 scope as designed (analyser + tool-report
CLI as a shipped, recurring facility, not a one-off).
