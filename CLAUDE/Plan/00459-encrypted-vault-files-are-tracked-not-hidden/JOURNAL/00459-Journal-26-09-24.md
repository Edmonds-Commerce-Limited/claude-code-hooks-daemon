# Plan 00459 — Journal 26-09-24

_Scaffolded by `mkplan.bash`; timestamps in this file are UTC._

> **Append-only activity log** for plan 00459. One file per day
> (`00459-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` UTC 24h (the date is in the filename, also UTC); times increase
>   down the file. A file with no sentinel line above predates this system —
>   its times are local, zone unrecorded (legacy; never rewritten).
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 12:01 · action · — — plan scaffolded

Plan 00459 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 12:03 · decision · — — owner report; the fix is a content check, not a name rule

The owner pasted a client session's `secret_file_hygiene_checker` output
listing three files as "not gitignored" and "git-tracked", with the advice to
untrack them. Two were Ansible Vault vars files and one was a `*.secrets`
template. Their words: the project "seems to be told not to track its own
ansible vault files - which is WRONG". The client's real paths are kept out
of this repository, which is public. The plan uses generic shapes.

Traced to Plan 00272's default globs (`secret_file_guard.py:325`):
`*vault_pass*` matches `vault_passwords.yml`, and `*.secret*` matches any
`*.secrets`. Name-based selection cannot tell the encrypted vars file apart
from the plaintext password file, which really must stay untracked. The
first-line format header can. It is checked at the time of each use because
`ansible-vault decrypt` writes plaintext in place.

Dedupe: Plan 00272 (Complete) introduced the guard; no live plan touches
secret-file selection. Recovery cron reuses `3c3c53c3`.
