# Task 2.11 audit report — vendored hook contract vs upstream (R1)

**Branch**: `agent-a7caa99eb07f990c9-bc7d7afc` (worktree
`/workspace/.claude/worktrees/agent-a7caa99eb07f990c9-bc7d7afc`)
**Work commit**: `9beec2f9`; the Plan 00327 tick and this report follow in
the next commit on the branch.

## Result in one line

The upstream hooks documentation changed between the 2.1.252 audit and Claude
Code 2.1.263, but none of the 33 vendored schemas' claims drifted: 33
UNCHANGED, 0 CHANGED, 0 NEW-UPSTREAM. R1 is closed as "measured, nothing
wrong" rather than "assumed".

## Procedure followed (Plan 00327 Phase 1 / `docs/guides/HOOK-CONTRACT-REFRESH.md`)

1. Raw capture via `bin/hooks-daemon remote-docs add https://code.claude.com/docs/en/hooks.md --verbatim`
   (`fidelity: verbatim`, `fetch_method: https-get`). The response body,
   frontmatter stripped, hashes to
   `c30a50b8192dadf4e6ba016e451685f57a6d1d2c360d268887a9a94022d29f3e`
   (317 632 bytes) — equal to the capture's own `source_sha256`, so the file
   used for the diff IS the response body. Neither the `d514bf57…` in
   `META.json` nor the `e2462deb…` Plan 00327's Overview quotes: upstream
   moved twice.
2. The previously audited raw text was still on disk at
   `/workspace/untracked/hooks-raw.md` with a hash exactly matching
   `META.json.docs_sha256`, so the diff is against the real last-audited
   state, not a reconstruction.
3. `diff -u`: 42 lines, 4 hunks, all prose; no heading added or removed; no
   change to the `#### Decision control` table or any event's decision-control
   or output section. Every hunk was read in the RAW text (no summarising
   fetch layer anywhere in the chain).
4. `META.json` regenerated by a script from the captured bytes (`fetch_date`,
   `docs_bytes`, `docs_sha256`, `last_audited_claude_code_version` 2.1.263,
   `event_count` from the directory) — no hash typed by hand.
5. `llm_qa.py hook_contract`: 0 violations, 30 allowlisted gaps, none stale.
   `llm_qa.py input_contract`: 0 violations, 3 allowlisted, none stale.
6. The capture was then removed from `remote-docs/` (and the generated
   `.claude/REMOTE-DOCS.md` index) so it stays untracked as Plan 00327 Task
   1.1 requires and Plan 00326 D20 settled; the body is at
   `untracked/hooks-raw.md` in the worktree.

## The four changed upstream sections

| #   | Section                                          | Upstream now says                                                                                                                                                                                                                                            | Claim affected |
| --- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------- |
| 1   | `#### PermissionRequest input`                   | `permission_suggestions` contains "the permission updates Claude Code suggests for this request"; "the array isn't an exact list of the options you see" — the dialog can withhold a suggested option or offer one with no entry (e.g. switch to auto mode). | none           |
| 2   | PermissionRequest `setMode` note                 | Re-sentenced: bypass availability cites "user, `--settings`, or managed settings"; the `disableBypassPermissionsMode` / restricted-mode no-op is a separate sentence; never-persisted rule unchanged.                                                        | none           |
| 3   | PermissionRequest `updatedPermissions` paragraph | "A hook can echo one of the `permission_suggestions` it received as its own `updatedPermissions` output." (the "equivalent to selecting that 'always allow' option" clause removed)                                                                          | none           |
| 4   | `### DirectoryAdded` does-not-fire list          | "You add a directory that is already a working directory or inside one" (was: "…already a working directory; the add fails with an error")                                                                                                                   | none           |

No schema text, note, enum, field list or `input_example` derives from any of
these sentences (grep of the contract tree for the old phrasing: no hits).

## Per-schema verdicts

All 33 UNCHANGED. The full table (block mechanism, blocks, ask, decision
enum, hook-specific fields, discarded fields, verdict) is the supporting
document Plan 00327 Task 1.2 asked for:
`CLAUDE/Plan/00327-hooks-contract-refresh-audit/DELTA-2.1.252-to-2.1.263.md`.
The two events whose sections changed (`PermissionRequest`, `DirectoryAdded`)
were re-read in full against their JSON; the other 31 are verified by the
diff — an unchanged section cannot have changed a claim derived from it.

## Tests and QA

- `tests/unit/qa/test_check_hook_contract.py::TestMetaProvenance` (new,
  RED-then-GREEN): pins `last_audited_claude_code_version`, `docs_sha256`,
  `docs_bytes`, and that `event_count` equals the number of vendored files
  (33). The next refresh must move META and the pin together. Since no claim
  CHANGED there is no per-claim corrective test to write.
- `pytest tests/unit/qa/test_check_hook_contract.py tests/unit/handlers/session_start/test_contract_staleness.py`: 56 passed.
- `ruff check` + `ruff format --check` on the touched test: clean.
- `mypy --strict` on the touched test: clean with `MYPYPATH=scripts/qa`
  (the only diagnostic without it is the pre-existing `sys.path` import of
  `check_hook_contract`, unrelated to this change).
- Daemon not restarted; no `sed`, no stash, no destructive git.

## Plan bookkeeping

- Plan 00327 status → In Progress; Tasks 1.1, 1.2, 2.1, 2.2, 2.3 ticked with
  `9beec2f9`; Milestones A and B recorded. Task 3.1's file half (`META.json`)
  is done; its "advisory silent on a fresh session" half is NOT observed
  because the daemon was not restarted from the worktree. Tasks 3.2 and 3.3
  (the `contract-status` command; trimming the guide) remain Plan 00327's own
  scope.
- Journals: `00327-Journal-26-09-08.md` (new day-file) and an entry appended
  to `00362-Journal-26-09-08.md`.
- Plan 00362 `PLAN.md` Task 2.11 is NOT ticked here — left for the
  coordinator to avoid twenty agents editing the same file.
- No release-notes callout, per the brief: no claim changed.

## Follow-ups (none blocking the release)

1. **Observe `contract_staleness` silent on a fresh session** after merge
   and restart (Plan 00327 Task 3.1, second half). Expected: installed
   2.1.263 == audited 2.1.263 → no advisory.
2. **`permission_suggestions` declared `required`** in
   `src/claude_code_hooks_daemon/core/input_schemas.py:97` while the docs
   (both states) call it optional. No live payload is rejected (the example
   always carries it, `additionalProperties` is permissive), so this is a
   read-surface strictness note for Plan 00273's inventory, not a defect this
   delta introduced. Not a contract change, so no test was added.
3. **`docs/guides/HOOK-CONTRACT-REFRESH.md` step 1** names `curl` only; the
   `remote-docs add --verbatim` route Plan 00327 Task 1.1 endorses (and this
   audit used) should be named alongside it, with the "then keep it untracked"
   consequence spelt out — the tree capture has to be removed afterwards or
   it becomes an unreviewed-licence vendored document. Belongs with Plan
   00327 Task 3.3.
4. **Worktree bootstrap**: `scripts/setup_worktree.sh` only creates NEW
   worktrees, so an agent dropped into an existing one has to run the
   `uv sync --frozen --extra dev` step by hand and symlink `untracked/venv`
   for `llm_qa.py` (which hardcodes `untracked/venv/bin/python`). Worth a
   line in `CLAUDE/Worktree.md` or an idempotent mode on the script; noted
   for Plan 00358's owner.
