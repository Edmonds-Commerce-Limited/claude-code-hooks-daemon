# Plan 00283 — Journal 26-08-28

> **Append-only activity log** for plan 00283. One file per day
> (`00283-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 10:08 · action · — — plan scaffolded

Plan 00283 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 10:11 · decision · — — origin, channel choice, prerequisites

Originates from joseph: the every-prompt `standing_authorisations` injection is
spammy, and a real typed input is stronger than hook meta-context. Channel
decision (joseph, via AskUserQuestion): **supervisor-typed with hook-context
fallback**. Cadence direction (joseph): first full, then reinforce every 15 min
OR 5 prompts, whichever first.

Delegated prerequisites, both complete:

- **Dedupe scout**: no live plan covers this. Plan 00280 (Not Started) touches
  the same handler for model-cap advisory text only and explicitly preserves
  the cadence — orthogonal; coordinate at merge.
- **Signalling map** (`goal_injection` + `claude-supervise.py`): shared
  `daemon_untracked_dir()/context-sidecar`; session-keyed non-`.json` signal
  file, `{session_id, ts, …}` body, atomic-replace, fail-open; supervisor
  consumes at an idle choke point only; armed-detection via the supervisor
  status file under `untracked/supervise/`; `*.model-switch-intent` is the
  closest existing template; shared header constants get a lockstep test.

## 10:11 · action · — — failsafe recovery cron

CronList showed no cron in this session's store (earlier ticks came from a
prior/compacted context). Created exactly one recurring failsafe recovery cron:
**id `0837855f`** (`37 * * * *`, non-durable, session-only). Not a heartbeat.

Next: Phase 1 (cadence rework, handler-only, TDD).

## 10:39 · action · P1 — Phase 1 complete (cadence rework)

TDD RED→GREEN. Rewrote the handler: `_SessionState` dataclass (delivered_once,
last_delivery_ts, prompts_since_last), injectable `_clock`, `_prompt_interval`
(5) / `_interval_minutes` (15) defaults, `_is_automated_prompt` marker check,
`_state_for` (bounded FIFO), `_is_due`. First human prompt → full text;
thereafter silent until 5 prompts OR 15 min, then short form. Automated turns
(FAILSAFE RECOVERY CHECK / 🤖 [ccy-supervisor]) neither count nor deliver — kills
the every-cron-tick spam AND is the Phase 2/3 loop-guard. Test class
`TestDecayNotCooldown` → `TestCadence` + `TestAutomatedPromptsAreIgnored`; 37
pass, handler 98% covered. Daemon restarted RUNNING; full QA 25/25 (14,907
tests, 95.1%).

## 10:39 · finding · — — dogfooding bug found + fixed (separate commit)

Full QA surfaced `test_effort_restore.py::test_model_restore_cap` failing — NOT
my change (supervisor module). Root cause: it builds `CompactPolicy` via the
default factory, which reads `CCY_FLAG_COMPACT` from the ambient env
(`_flag_compact_enabled_from_env`); a ccy-supervised session exports
`CCY_FLAG_COMPACT=1` (Plan 00281), enabling flag-compact and firing a payload
past the model-restore cap the test asserts silence at. Green in CI (var unset),
red in-session. Environmental pollution, not a product defect. Fixed with a
supervise-wide autouse conftest clearing the var; verified 498 pass with the var
set. Committed separately as `4c16553c` (Fix: supervise tests hermetic against
ambient CCY_FLAG_COMPACT).
