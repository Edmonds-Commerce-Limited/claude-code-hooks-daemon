# Plan 00277 — Journal 26-08-26

> **Append-only activity log** for plan 00277. One file per day
> (`00277-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 15:14 · action · — — plan scaffolded

Plan 00277 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 16:30 · action · P1-P3 — post-release execution, all but one task closed

Executed immediately after v3.55.0 published, per the never-drop-findings
rule. Highlights:

- T2.1: `block_once` was block-once-per-DAEMON-LIFETIME shared across
  sessions (in-memory HandlerHistory + Plan 00127 daemon sharing); the
  contradictory live observations resolved once daemon restarts were
  accounted for. Fixed per-session: `session_id` on
  `HandlerDecisionRecord`, controller attribution, filtered
  `count_blocks_by_handler`, `lsp_enforcement` passes the event session.
  TDD (2 RED tests first); 1399 affected tests green.
- T3.1: thread registry held exactly ONE entry (this session) while many
  sub-agents ran — sub-agents never emit Status, so they never register.
  Advisory correct; acceptance test premise wrong; rewritten with the
  two-interactive-sessions precondition.
- T3.2: reproduced the hint firing on `true; agent-browser --version` —
  compound-segment detection works; batch-3 saw TTL rate limiting.
- T3.4 attempt: dummy-client fixture + `llm:lint` + synthesized broken.ts
  Write → dummy daemon answered `{}`. Deny branch still unverified; task
  stays open with this evidence.
- T3.5/T3.6: recorded as rulings in PLAN.md (quoted-string producer
  attribution boundary; /goal livelock is upstream Claude Code behaviour,
  remedy documented).
- T1.3: error-hiding allow samples rewritten lint-clean (Python
  logging.exception function, Go os.Open err check).
