# Plan 00347 — Journal 26-09-08

> **Append-only activity log** for plan 00347. One file per day
> (`00347-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 01:45 · action · — — plan scaffolded

Plan 00347 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

Failsafe recovery cron for this session: **`5398b3f9`** (hourly at :23,
non-durable). `CronList` showed exactly one already present, so it is REUSED
rather than duplicated — the cron is per-session and plan-agnostic, and a
second would only double-fire on the same session. A background-process
watchdog `ef8543f1` (hourly at :17) is also live.

## 01:47 · finding · — — how this plan was found, and what nearly hid it

Not filed from a review. Plan 00346 put CI on the locked toolchain, which
turned the `Format (black)` job green and left `Tests + coverage` as the only
remaining failure on `main` — and only then was that failure legible as a
separate defect rather than part of the noise.

The originating instance is fixed at `f17fabcd`. What made it worth a plan
rather than a one-line fix was measuring the blast radius afterwards: 73 stat
calls under `handlers/`, of which ~12 judge a path the caller supplied.

**Three things about this were not obvious and are worth carrying forward.**

It never reproduces locally. This container runs as root, so mode bits do not
apply and every one of those paths stats fine. The suite passed 25/25 here
while CI was red. Any test written against real permissions is therefore
worthless — it must FORCE the error, which is what `f17fabcd` does.

It is not a crash. I initially recorded it as "takes the hook down", then
checked `chain.py:291` and found the chain catches it. That made the finding
WORSE, not better: with `strict_mode: false` — the client default — the
handler's guard just silently stops applying, so a DENY handler treats an
unreadable parent directory as an exemption. This repo runs `strict_mode: true`
and gets the opposite failure, a spurious block on legitimate work. Two
different wrong answers from one defect, split by config.

The fix's first draft was itself wrong, and the repo caught it. `except OSError:
destination_is_dir = False` failed `audit_error_hiding.py`: "assigns a fallback
value with no logging or re-raise — failure becomes indistinguishable from
success." Correct call. Without a record, "the guard decided this is not a
directory" and "the guard could not look" read identically to whoever is asking
why a policy did not fire. The warning log is load-bearing.

## 01:47 · decision · P3 — a gate, not twelve patches

The obvious shape was to wrap each of the ~12 call sites in `try`/`except`.
Rejected: it treats instances, and the thirteenth call site reintroduces the
bug with nothing to catch it.

`scripts/qa/check_canonical_callers.sh` already solves this shape for venv
resolution — go through the canonical helper, or carry an inline
`# canonical-resolver-exempt: <reason>` marker. Phase 3 copies that, which also
makes the daemon-controlled sites record their justification in place instead
of being indistinguishable from oversights.

The scope boundary matters as much as the fix. Most of the 73 read
daemon-controlled paths — config, install dirs, caches — where `EACCES` is
genuinely exceptional and SHOULD surface. Blanket-guarding them would be the
error-hiding antipattern this project blocks, so Task 2.2 makes the
classification the deliverable rather than an implementation detail.

`hooks-daemon-plan-dedupe-scout` checked 41 live plans and found no overlap;
nearest by theme is 00252 (guards for premises no write-time hook sees), which
is about git premises and file-arrival routes, not stat robustness.
