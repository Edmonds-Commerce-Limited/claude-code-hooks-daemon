# Plan 00390 — Journal 26-09-12

> **Append-only activity log** for plan 00390. One file per day
> (`00390-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 22:23 · action · — — plan scaffolded

Plan 00390 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 22:28 · decision · — — failsafe recovery cron: reusing the session's

`CronList` run before opening this plan showed exactly one recurring failsafe
recovery cron, id `75ba7c5e` (hourly at :37). Reused; nothing created. The
canonical prompt is plan-agnostic ("the active plan/task"), so one cron already
covers this plan and every other one in the session — a second would only
double-fire against the same session.

## 22:29 · finding · N1 — how it was found, and the probe that isolated it

Not hunted for. It surfaced while building a client-shaped fixture tree for
Plan 00386's end-to-end verification: that fixture needs a `.claude/` directory
containing a real `HOOKS-DAEMON.md`, and the `Write` was denied by
`markdown_organization` — with a message whose own allow-list includes
`./untracked/`, the tree the file was in.

The isolating probe was written in the same turn rather than reasoned about:

```
DENIED : untracked/scratch/e2e-386/project/.claude/HOOKS-DAEMON.md
ALLOWED: untracked/scratch/e2e-386/plain-note.md
```

Same tree, same turn. That rules out "`untracked/` is not really on the list"
and points at the nested `.claude/` segment as the deciding factor.

Left unfixed for now so the Plan 00386 work it interrupted could continue. The
detour is recorded here so the next reader knows the fixture's eventual shape
was a workaround, not a preference.
