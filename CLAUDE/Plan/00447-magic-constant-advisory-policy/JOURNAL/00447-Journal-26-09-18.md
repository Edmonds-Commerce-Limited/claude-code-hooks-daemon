# Plan 00447 — Journal 26-09-18

_Scaffolded by `mkplan.bash`; timestamps in this file are UTC._

> **Append-only activity log** for plan 00447. One file per day
> (`00447-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` UTC 24h (the date is in the filename, also UTC); times increase
>   down the file. A file with no sentinel line above predates this system —
>   its times are local, zone unrecorded (legacy; never rewritten).
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 12:37 · action · — — plan scaffolded

Plan 00447 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 13:05 · action · — — failsafe cron reused, not created

`CronList` showed a recurring failsafe recovery cron already live for this
session: **f5473232** (hourly, :47). Reused it and created nothing — one cron
covers every plan in a session, and a second only double-fires.

## 13:05 · finding · — — what the bundle actually says

Triage evidence for issue #49, measured against the Claude Code build installed
on this machine rather than taken from the report:

- `CLAUDE_CODE_THRIFTY_SONIC` is present. Four occurrences: a string-table
  entry, an export-map entry, and two in the resolver.
- The resolver:

  ```js
  function J(){ let e=u(); return LG()? e.bashFirstSessionAssignmentCompiledOnly()
                                        : e.bashFirstSessionAssignment() }
  function O(){ let e=wee(nt()), n=We(e), r=Tm(n,"thrifty_sonic",e);
    if(r===!1) return "none";
    if(lae(n)||eu()?.[x]===!0||r) return "forced";
    return Obt(e)? "cohort":"none" }
  function Oyt(){ if(a.CLAUDE_CODE_THRIFTY_SONIC!==void 0) return a.CLAUDE_CODE_THRIFTY_SONIC;
    switch(J()){ case"forced":return!0; case"none":return!1; case"cohort":return I(x,!1) } }
  ```

  So the env var is an OVERRIDE on a per-session cohort assignment.
- The string table places it under **GrowthBook** with the experiment key
  `thrifty_sonic`, among a family of bash-first flags: `CLAUDE_CODE_COZY_TEAPOT`
  (a `"strict"`/`"relaxed"` steer variant, read by `lqr()` as
  `a.CLAUDE_CODE_COZY_TEAPOT ?? u().bashFirstSteerVariant()`),
  `CLAUDE_CODE_BASALT_COVE`, `CLAUDE_CODE_GORSE_PLOVER`,
  `CLAUDE_CODE_AMBER_ASTROLABE`, `CLAUDE_CODE_LARCH_CISTERN`, plus
  `bashActFirstEnabled`.
- **642** distinct `CLAUDE_CODE_*` tokens in the bundle.

## 13:05 · decision · — — what was deliberately NOT concluded

`a.CLAUDE_CODE_THRIFTY_SONIC` is returned directly where the other branches
return booleans, which raised the question of whether a string `"0"` would be
truthy and pin the behaviour ON — the opposite of what the reporter wants. That
was NOT concluded, and must not be repeated as if it were: a zod env schema
exists in the bundle (`z.boolean().optional()` and friends), and the sibling
`COZY_TEAPOT` is clearly a parsed string union, so `a` holds typed values. The
accepted type of THIS key was not established.

Grep cannot settle parser behaviour. That is the identical wall issue #46 /
Plan 00428 hit, and recognising it as the SAME wall is what turned this from
"add a seventh check" into a policy question — which is why 00428 is a subset
of this plan rather than a sibling.

