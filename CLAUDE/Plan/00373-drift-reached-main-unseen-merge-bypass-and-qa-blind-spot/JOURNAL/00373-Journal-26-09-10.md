# Plan 00373 — Journal 26-09-10

> **Append-only activity log** for plan 00373. One file per day
> (`00373-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 14:29 · action · — — plan scaffolded

Plan 00373 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 14:32 · finding · P1 — how the drift got in, and why nothing said so

Found while surveying state after the release slate came back green on every
axis but worktrees. `CLAUDE/Plan/00372-worktree-reap-two-defects/` existed at
the ACTIVE location holding only `subagent-reports/build-report.md`, while
the README row for 00372 pointed at `Completed/`.

Sequence, from `git log -- CLAUDE/Plan/00372-worktree-reap-two-defects/`:

```
a85e00e8 Merge branch 'worktree-plan-00372': the plan's build report
7c315045 Plan 00372: subagent build report
69803a2c Plan 00372: Complete — archived to Completed/
```

The worktree branch was cut before `69803a2c`, so it wrote the report to the
path the plan no longer occupied, and the merge landed it. Four plan-QA
findings resulted — `no-new-collisions`, `row-folder-bijection`,
`location-status-coherence`, `stats-recount`.

Two independent gates were missing, either of which would have caught it:

1. `plan_qa_commit_gate.matches()` calls `is_git_commit(tokenise(command))`.
   `git merge` auto-commits without invoking `git commit`, so the gate never
   ran. Same for `docs_qa_commit_gate` and `staged_lint_gate`.
2. `scripts/qa/llm_qa.py`'s `TOOL_REGISTRY` has no `plan_qa` or `docs_qa`
   entry, though both are shipped CLI verbs that exit 1 on findings and
   support `--json`. So the drift survived `27/27 PASSED`, green CI, and
   `release-slate-check`.

## 14:33 · action · T1.1 — plan tree repaired

`git mv` of the orphaned `subagent-reports/` into
`Completed/00372-worktree-reap-two-defects/`, plus `rmdir` of the emptied
active folder. `plan-qa --check-staged` went from four findings to
`0 findings — plan tree is clean`. Committed and pushed as `7a722965`.

## 14:35 · finding · P1 — a stale arithmetic line in the plan statistics

While updating Plan Statistics for 00373, the reconciliation paragraph ended
`358 + 13 = 371. ✅` against a stated 359 distinct numbers and 372 total — a
line left behind by an earlier increment. `stats-recount` checks the labelled
counts, not prose arithmetic, so it never saw it. Corrected to
`360 + 13 = 373` alongside this plan's own increments.
