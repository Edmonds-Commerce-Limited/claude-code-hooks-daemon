# Plan 00257 — Journal 26-08-18

> **Append-only activity log** for plan 00257. One file per day
> (`00257-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 13:40 · action · — — plan scaffolded

Plan 00257 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 14:05 · finding · P1 — the bug reproduced in our own harness before any fix

Wrote the RED test rather than trusting the review that reported it. Two of the
three failed exactly as predicted, and the second is the one that matters — it
is not a naming complaint, it is destruction:

```
assert report.deleted == ()
E  AssertionError: a force delete ran on an unproved branch
E  assert ('feat',) == ()
```

The third test — a legitimate `--protected-ref origin/main` — passed from the
start, and that is the whole reason the fix could not be a blind
`branch_ref(protected_ref)`. `refs/heads/origin/main` is a malformed object
name. Writing it as part of the RED batch meant the constraint was visible
before the implementation rather than discovered by breaking it.

## 14:05 · decision · P1 — why this was latent for a release and is not now

Worth recording because it changes how the severity reads. The ambiguous base
is not new. What is new is that a mis-verdict can now reach a force delete.

Before: bad base -> tier `merged` -> `delete_argv_for_tier` returns
`git branch -d` -> git re-runs its OWN ancestry check -> refuses. The bug was
real and fully contained by a tool we did not write.

After Plans 00249/00253: the same bad base can land on `merged-unpushed` or
`merged-not-in-head`, which return `--force` precisely so git's refusal is
bypassed. The tiers are correct; they simply removed the net.

This is the LESSONS.md entry from earlier this cycle ("the battle-tested tool's
checks compose with yours") arriving from the other direction — that entry
observed git's delete-time check catching what our proof missed. Here we
switched that check off for two tiers and immediately lost a real defence.

## 14:05 · finding · P2 — a fixture that neutralised an input nobody reads any more

The three QA failures had one cause, and it is a better lesson than the fix.

`test_stop_chain_terminal_shadowing` stubbed `subprocess.run` to feign a clean
working tree, and its docstring said so, because `release_blocker` inferred a
release from modified version files. That handler was changed THIS CYCLE to
read `untracked/release-state.json` instead — correctly, since a working tree
cannot express intent. Nothing updated the fixture, and nothing could notice:
the stub still ran, still succeeded, and isolated nothing.

The failure surfaced at the worst possible moment — QA went red the instant a
real release wrote the state file, which is exactly when RELEASING.md requires
it green. Fixed by rooting the walk at an empty `tmp_path` and patching
`ProjectContext.project_root`, so it isolates against what the handler now
reads. It passes with a release in flight, which is the only proof worth having.

## 14:05 · finding · P2 — chasing it found a production defect, not just a test one

The two acceptance failures were not the same fixture problem. `release_blocker`
is terminal at priority 8, so it shadows `auto_continue_stop` at 10 entirely —
and during a release an agent that hit a `tool_use_error` was told "a release is
in progress", which says nothing about the error it just hit. The specific,
actionable directive (Read the file, then retry) was replaced by an unrelated
one.

Stood the guard down for that case only, reusing `get_transcript_reader` rather
than re-parsing the transcript so the two handlers cannot drift apart on what "a
tool error" means. Both handlers DENY, so the release loses nothing: the session
continues either way and the guard fires again on the next stop attempt, which
by then carries no tool error.

The remaining negative-control test now skips during a release with a full
explanation. That is deliberate and narrow: its real assertion — Branch 2.5 must
not fire on a clean turn — still executes, and only the question of WHICH branch
answered is unobservable while a terminal guard sits ahead of it.

## 14:05 · handoff · — — code blockers closed, release still held

Both release blockers are fixed, verified and committed. QA needs a full re-run.
Task 1.3 (should an ambiguous protected ref REFUSE rather than merely resolve?)
and Task 2.4 (is the stale-fixture shape mechanically checkable?) are open and
deliberately not rushed into a release.

The release remains held on Plan 00256's docs round, per the human's condition.

## 16:40 · finding · P4 — the guard had a hiding place, and something was in it

Three sub-agents dispatched to re-derive the review findings all terminated
without reporting, so I did the review in the main thread. One of the three
lost labels was real, and it was the one about the guard rather than about the
code it guards.

`test_git_spawns_are_bounded` read only `["git", ...]` literals. Its docstring
defends that: argv assembled elsewhere would mean guessing, and a checker that
guesses is untrustworthy. Right principle, drawn one step too wide —
`[_GIT_BINARY, "-C", ...]` beside `_GIT_BINARY = "git"` in the SAME file needs
no guess, the value is an assignment two screens up.

`plan_qa/gitfacts.py:132` sat in that gap, and it was not a harmless one:

```
result = subprocess.run(
    [_GIT_BINARY, "-C", str(self._repo_root), *args],
    capture_output=True, text=True, timeout=Timeout.GIT_CONTEXT, check=False,
)
```

It carried a timeout but no `GIT_OPTIONAL_LOCKS=0`, and one of the commands it
runs is `git diff --cached`, which refreshes and rewrites `.git/index`. The
module is the plan-QA fact source, so that spawn happens at the COMMIT GATE —
the daemon taking the index lock at the precise moment the agent needs it for
the commit being gated. That is the exact failure Plan 00246 existed to remove,
surviving inside the release that claims to have removed it.

The file predates this release (Plan 00144) and was untouched by 00246's sweep,
because the sweep could only fix what the guard could see. So the changelog
sentence "an AST guard now covers all of `src/`" was false when written.

## 16:40 · decision · P4 — resolve constants, do not evaluate expressions

Widened the scanner to resolve module-level string constants only, from the
module BODY, including the annotated `Final[str]` form this codebase actually
uses. Deliberately NOT resolving an imported name or one assigned inside a
function: following either would mean crossing a file boundary or control flow,
which is where reading really does become guessing.

Proof it is calibrated rather than merely wider: run against all of `src/` it
returns exactly one spawn — the real one — and no false positives.

Task 4.4 left open: an import-graph check ("does this module import subprocess
at all?") would be strictly stronger than matching call shapes, and would not
need a constant resolver. Not rushed into a release.

## 16:40 · thought · — — what this says about the earlier finding count

Worth recording next to the "24 spawns across 12 files" correction made
earlier today for Plan 00256. That figure was measured with the guard's own
scanner, so it inherited the scanner's blind spot: the honest number was 25.
A measurement is only as trustworthy as the instrument, and the instrument was
the thing under review.

## 17:30 · finding · — — the acceptance gate ran in the main thread, and the batching assumption was wrong

The human chose to restart the session so Step 12's 153 delegable tests could be
delegated. The daemon's own `release_blocker` then refused the Stop, because it
only stands down at `last_completed_step >= 13` — which is Task 3.1, the abort
deadlock, arriving in practice rather than in theory. I did not edit the guard
to release myself: an agent wanting to skip the acceptance gate is precisely
the pattern that handler exists to catch, and its example-context.md says so.

What made the gate tractable was testing an assumption instead of accepting it.
`CLAUDE.md` warns that a denied tool call cancels every sibling in the turn, and
I had planned ~122 single-test turns around that. Issuing two deny-expected
`echo` probes in one turn returned TWO independent denials, each with its own
reason. The cancellation caveat is real but applies to MUTATING siblings — a
batch of read-only probes each get judged. That collapsed ~122 turns into ~13.

## 17:30 · finding · — — three tests cannot be executed as written

Not failures; gaps in what the playbook can express, which is Plan 00243's
subject:

- Tests 24, 25, 106, 114, 116 already carry `harness_cannot_produce` or absent
  `required_tools`, and behaved accordingly (Claude Code resolves a relative
  path before dispatch; rustc/kotlinc/swiftc are not installed here). Declared
  skips, correctly declared.
- Test 22's `command` field holds a PATH while the real instruction sits in
  `description` ("Use the Write tool to write ..."). Executing the command
  field runs nothing. Done correctly via the Write tool, it blocks.
- Test 173 needs the `Grep` TOOL, which is not available in this session
  (searching goes through Bash). Its handler is verified anyway by test 175,
  which blocks an `rg` symbol lookup.
- Tests 187/188 (agent isolation advisory) could not be exercised: sub-agent
  dispatch returned no result for five agents this session, so no peer thread
  could be made active.

## 17:30 · decision · — — the secret-word tests, run without writing the secret down

Tests 52 and 53 require content containing a term from the gitignored secret
word list. Typing one into a tool call would put it in the transcript — the
exact disclosure the handler exists to prevent. Instead a throwaway probe read
the first active term at runtime, dispatched a Write payload and a
`git commit -m` payload to the live socket, and printed only the verdict.

Both denied. The deny reason named only the index ("secret word list") and a
containment check confirmed the term did NOT appear in the reason. That is the
whole contract, verified without ever rendering the term. The probe was deleted
afterwards.

Worth noting the calibration step caught my own error rather than a defect: the
first run reported `decision=None` for both, which looked like a handler
failure. It was my parsing — the daemon nests its verdict under
`hookSpecificOutput`. A known-blocking command sent through the same path
exposed that immediately. Without the calibration I would have filed a false
critical finding against a handler that was working perfectly.

## 17:30 · handoff · — — Step 12 complete, release held at the publish gate

Every executable playbook test passed. No failures, no fixes required, so no
FAIL-FAST restart was triggered. Remaining: Step 13 (commit and push), then
STOP — Steps 14 and 15 need an explicit human "yes, publish" and
`publish_authorised` is still false.

## 18:15 · finding · — — the STOPPING BECAUSE prefix does not survive markdown emphasis

Third guard to misfire on me during this release, and the cheapest to fix.

`auto_continue_stop` requires a final message prefixed `STOPPING BECAUSE:`. I
wrote exactly that, but bolded it — `**STOPPING BECAUSE: ...**` — because the
surrounding report was formatted markdown and a bare lead-in looked wrong. The
matcher saw a message starting with `**` and re-fired the "you stopped without
explaining why" block.

The guidance in CLAUDE.md says "prefix your final message with
`STOPPING BECAUSE:`" and says nothing about formatting, so bolding it is a
natural reading rather than a mistake. Claude Code renders assistant text as
markdown, which makes emphasis on the most important line of the message the
obvious thing to do.

Remedy is a lead-in strip before the prefix test: leading whitespace is already
tolerated, and `*`, `_`, `#` and `>` belong in the same set. Note the closing
`**` too — a stripper that only handles the opening run still leaves the
trailing marker inside the reason text.

NOT fixed here, deliberately. Steps 1-13 are committed and pushed and the tag
has not been cut. A handler change now would move HEAD, so v3.54.0 would be
tagged over source that never went through the QA and acceptance gates it just
passed. It goes in the next bundle with the other two open items.
