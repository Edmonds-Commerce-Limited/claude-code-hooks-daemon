# Plan 00468 — Journal 26-09-24

_Scaffolded by `mkplan.bash`; timestamps in this file are UTC._

> **Append-only activity log** for plan 00468. One file per day
> (`00468-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM, ONLY with
> `mkplan.bash --journal <plan-number> <category> <body-file>` (it stamps the
> real UTC time; a hand-written entry is denied); NEVER edit earlier entries
> (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` UTC 24h (the date is in the filename, also UTC); times increase
>   down the file. A file with no sentinel line above predates this system —
>   its times are local, zone unrecorded (legacy; never rewritten).
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 17:59 · action · — — plan scaffolded

Plan 00468 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress with `mkplan.bash --journal` as it happens.

## 18:12 · action · —

Task 5.1 (G3, G4, G5) done on branch worktree-p468-docs. Vendored plugins, plugins-reference, discover-plugins, plugin-marketplaces and hooks with remote-docs add. The canonical home is CLAUDE/ClaudeCodePlugins.md, with a human summary at docs/guides/CLAUDE_CODE_PLUGINS.md. HooksSystem.md and ARCHITECTURE.md now list plugin hooks/hooks.json as a live hook source, and CLAUDE_PLUGIN_DATA is in the env table. The bare word plugin is now qualified across the docs, in the HOOKS-DAEMON.md generator heading and in the init_config template comment, with RED-first tests. The vendored hooks.md carries upstream example UUIDs, so it is excluded from the session-uuid pattern in sensitive_content.

## 18:26 · decision · —

The coordinator rejected the hooks.md exclude_paths entry: no new QA exclusions, and a path entry would be needed for every vendored page. It is replaced by a rule. Public patterns stand down for the body of a faithful vendored copy: a markdown file in the remote-docs tree whose body still hashes to source_sha256. All 16 existing captures already satisfy this, so there is no schema change or backfill. One predicate, remote_docs.provenance.is_faithful_vendored_copy, serves the Write path, the git commit staged-content scan (the whole index blob) and scripts/qa/check_sensitive_content.py. The secret word list and file names are never stood down. The tests were written RED first. Release note 37 is added.
