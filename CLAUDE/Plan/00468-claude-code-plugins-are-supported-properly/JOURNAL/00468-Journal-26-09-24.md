# Plan 00468 — Journal 26-09-24

_Scaffolded by `mkplan.bash`; timestamps in this file are UTC._

> **Append-only activity log** for plan 00468. One file per day
> (`00468-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM, ONLY with
> `mkplan.bash --journal <plan-number> <category> <body-file>` (it stamps the
> real UTC time; a hand-written entry is denied); NEVER edit earlier entries
> (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` UTC 24h (the date is in the filename, also UTC); times increase
>   down the file. A file with no sentinel line above predates this system —
>   its times are local, zone unrecorded (legacy; never rewritten).
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 17:59 · action · — — plan scaffolded

Plan 00468 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress with `mkplan.bash --journal` as it happens.

## 18:13 · action · —

Task 1.2 done on branch worktree-p468-core.

- New test `tests/unit/install/test_shipped_settings_carry_no_dogfood_keys.py` runs every route the template reaches a client by: the SETTINGS_JSON_SOURCE pin in both shell installers, the fresh-install copy in `deploy_settings_json` (real bash), the three-way merge (worst case, `old_default={}`), and `install.py`'s generator.
- RED: `plansDirectory` failed three routes. Checked against the b00ecd02 template too: the merge would have delivered `enabledPlugins` and `extraKnownMarketplaces`, so the test catches the original P1 shape.
- `plansDirectory` review: dogfood-only. The plan workflow is opt-in (`plan_workflow.enabled` defaults false, and a stock install deploys no CLAUDE/Plan/), and its directory is configurable, so no single value is right for every client. `install.py` never set it, so the routes also disagreed. Moved to the tracked `.claude/settings.local.json`. Existing clients keep theirs (client-owned key in the merge).
- `markdown_organization._check_claude_code_sync` now reads `settings.local.json` over `settings.json`, as Claude Code does; otherwise the move would deny this checkout if `enforce_claude_code_sync` were switched on.
- Docs: SELF_INSTALL.md section (G6); LLM-INSTALL planning setup gains a plansDirectory step. Release note 30.

## 18:29 · action · —

Task 2.1 done: `utils/claude_config.py` (`claude_config_dir()`, `config_dir_within()`) and `utils/claude_plugins.py` (`resolve_enabled_plugins()` -> `PluginInventory`).

Decisions, each from the vendored-to-be upstream docs (plugins-reference, settings-reference, managed-settings; fetched into untracked/scratch/p468-core/ for this work, Task 5.1 vendors them properly):

- Enablement precedence managed > local > project > user; no entry anywhere falls back to `defaultEnabled` (marketplace entry, then plugin.json, default true). Only file-based managed settings (`managed-settings.json` + `managed-settings.d/*.json`) are read.
- A project/local install applies only when `projectPath` resolves to the project root; a project-bound install beats a user one.
- `strict: false` marketplace entries are the whole declaration (the official LSP plugins, e.g. pyright-lsp, declare `lspServers` only there); a strict:false entry plus manifest components is unresolved, as Claude Code refuses to load it.
- Agent ids follow plugins-reference: subfolders joined with colons, frontmatter `name` replaces only the file part, manifest `agents` files drop subfolders; missing frontmatter loads with no fields.
- Not resolved (reported in `unresolved` when a settings file enables them): `@skills-dir`, `@synced`, `--plugin-dir` plugins, MDM/server-managed settings. This checkout's user-scope `phpantom-lsp` (no marketplace, no install record) lands there.

Frontmatter: `markdown_format.parse_frontmatter_lenient()` (P6's fallback) lives beside `parse_frontmatter_yaml()` so there is still one frontmatter module.

`project_containment._claude_home()` now calls `claude_config_dir()`; docstring corrected (G10): it is the daemon's environment, fixed at start, and per-call reading does not track a session.

Live read-only check against this container: DBF resolves (enabled_by local, install_scope project) with both agents and `defence-before-fix:dbf`; `conformance-reviewer` frontmatter carries `isolation: worktree`; pyright-lsp resolves its `.py`/`.pyi` server from the marketplace entry.

## 18:38 · action · —

Task 2.2 done.

- `subagent_tool_resolution`: new `resolve_agent_definition()` -> `AgentDefinition` (source, path, frontmatter, can_write, `isolation`, `declares_worktree_isolation`); `resolve_agent_can_write()` now wraps it. Tiers: project, user (`<config_dir>/agents`, G13), built-in, then plugin (P2) for any id containing `:`. The `home_dir` keyword is gone; callers pass `config_dir`.
- P6: project/user/plugin agent files are read with `parse_frontmatter_lenient`; this repository's real `code-reviewer.md` now resolves read-only (test pins it against the tracked file).
- Plugin agents with missing frontmatter resolve writable: plugins-reference says Claude Code ignores every field of such a file, so `tools` is absent and the full set is inherited.
- P7: `agent_isolation_advisor` checks the definition last, only once two live threads are counted, so the common single-thread path does no file reads.
- G12: `subagent_report_paths._sanitise` is now public `sanitise_component`; the size blocker's prescribed path uses it.
- Handlers' test seam `_home_dir` renamed `_config_dir` (dispatch_declaration, size blocker); agent_isolation_advisor gains `_project_root`/`_config_dir`.
- Shared test fixture `tests/claude_plugin_fixture.py` installs a fake DBF-shaped plugin under a tmp config dir.

RED evidence: resolver tests failed on import, isolation-advisor plugin/project tests failed `True is False`, size-blocker tests failed before the rename. The handler-level dispatch_declaration plugin test was written after the resolver tier landed, so it passed first time; the audit's reproduction (`{}` for the plugin agent) is its RED.

Live read-only check against /workspace with the real config: `defence-before-fix:conformance-reviewer` -> plugin, can_write False, isolation worktree; `independent-searcher` -> plugin, False; `code-reviewer` -> project, False.

Open question for the lead: a project-scope install records `projectPath: /workspace`, so in a git worktree (a different project root) the resolver treats DBF as not active. Whether Claude Code does the same in a worktree session is not verified.
