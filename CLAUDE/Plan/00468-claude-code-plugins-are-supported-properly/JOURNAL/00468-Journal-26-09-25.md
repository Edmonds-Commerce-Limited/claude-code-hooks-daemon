# Plan 00468 — Journal 26-09-25

_Scaffolded by `mkplan.bash`; timestamps in this file are UTC._

> **Append-only activity log** for plan 00468. One file per day
> (`00468-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM, ONLY with
> `mkplan.bash --journal <plan-number> <category> <body-file>` (it stamps the
> real UTC time; a hand-written entry is denied); NEVER edit earlier entries
> (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` UTC 24h (the date is in the filename, also UTC); times increase
>   down the file. A file with no sentinel line above predates this system —
>   its times are local, zone unrecorded (legacy; never rewritten).
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff` | `correction`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - A `correction` corrects an earlier entry without touching it. Its `REF`
>   (`--ref`, required) names that entry: `HH:MM` in this file, or
>   `YY-MM-DD/HH:MM` in an earlier one. An entry a correction names in this
>   file no longer counts for time order, so a future-dated stamp can be
>   corrected at the honest time.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 10:10 · correction · 26-09-24/18:42   — the 18:42 T3.1 entry is out of order in the 24 September day-file

The 24 September day-file lists the `18:42` Task 3.1 entry last, after the
`21:32` handoff. It belongs between `18:38` and `18:50`. It was written at
18:42 on `worktree-n466-docs-corpus`, and merging that branch through main
into `worktree-p468-core` appended it below entries already on this branch.
The time stamp and the text are correct; only its position is wrong. It is
not moved, because the day-file is append-only.

## 13:40 · correction · 26-09-24/18:42   — merge interleaving, not a bad stamp

The `18:42 · action · T3.1` entry appears after `21:32 · handoff` in the
26-09-24 day-file because it was written on `worktree-n466-docs-corpus`
(a sibling branch) and only reached this journal when Integration B2 merged
that branch in — the two branches' entries were interleaved by their
original wall-clock time rather than reordered to the merge's own commit
order. The `18:42` stamp is correct for when that entry was actually
written; nothing here was fabricated or needs to move. This correction
voids the ordering check's complaint about that entry rather than
restating its content.
