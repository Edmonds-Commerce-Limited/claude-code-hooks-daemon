# Plan 00468 Task 5.1: documentation for Claude Code plugins (G3, G4, G5)

Branch `worktree-p468-docs`. It is not merged.

## G3: vendored docs

These pages were captured with `bin/hooks-daemon remote-docs add` from the
URLs listed in the vendored `llms.md`:
`https://code.claude.com/docs/en/{plugins,plugins-reference,discover-plugins,plugin-marketplaces,hooks}.md`.
They landed under `remote-docs/code.claude.com/docs/en/` with
`fidelity: converted`, and `.claude/REMOTE-DOCS.md` was regenerated.
`remote-docs check` reports everything fresh.

The vendored `hooks.md` carries upstream example UUIDs (for example
`550e8400-...`). The `session-uuid` public pattern flags 28 of them in the
`sensitive_content` QA check. A vendored file cannot be edited, so it is now a
narrow `exclude_paths` entry in `.claude/hooks-daemon.yaml`, with the reason
recorded. The exclusion covers that one file, not all of `remote-docs/`.

## G4: the canonical section and the tables

- **Canonical home**: `CLAUDE/ClaudeCodePlugins.md`. This follows R2: the agent
  tree owns the depth. It covers:

  - The two meanings of "plugin" (this is the glossary line).
  - Choosing a scope, and what project scope means for collaborators.
  - The trust gate.
  - Plugin hooks running in parallel.
  - `updatedInput` replacing input the daemon judged. It quotes the vendored
    precedence line, `deny` > `defer` > `ask` > `allow`, from
    `hooks.md § PreToolUse decision control`.
  - Namespaced agents and skills.
  - `CLAUDE_PLUGIN_ROOT` and `CLAUDE_PLUGIN_DATA`.

  Every upstream claim links to a heading anchor in the vendored copy. Only
  the last section mentions Plan 00468, as a one-line pointer.

- **Human summary**: `docs/guides/CLAUDE_CODE_PLUGINS.md` is a terse summary
  that links to the canonical file (R3). It contains no plan references. It is
  routed from `docs/CLAUDE.md`, `README.md` § Documentation and
  `GETTING_STARTED.md` § Next Steps.

- **Tables**:

  - `CLAUDE/Code/HooksSystem.md`: "Settings File Locations" is now "Hook
    Sources". It adds a row for the Claude Code plugin `hooks/hooks.json` and
    a row for skill and subagent frontmatter, then links to the canonical file.
    The env table gains `CLAUDE_PLUGIN_DATA`.
  - `CLAUDE/ARCHITECTURE.md` § Configuration Locations: gains a Claude Code
    Plugin Hooks row and a paragraph saying plugin hooks are a live hook
    source.

- **Other links**: `CLAUDE/CLAUDE.md` gets a routing row, and
  `CLAUDE/LLM-INSTALL.md` gets a short "Claude Code Plugins" pointer.

The docs describe only behaviour that exists today. They do not claim any
Phase 2 to 4 feature, such as advisories, a resolver or LSP gating.

## G5: terminology

- **Sites the lead named**:
  - `CLAUDE/LLM-UPDATE.md:989`: the heading and text now say "Daemon plugins
    (handler modules …, not Claude Code plugins)".
  - `docs/guides/GETTING_STARTED.md:178`: the config comment is qualified.
- **`.claude/HOOKS-DAEMON.md`**: the generator is
  `src/claude_code_hooks_daemon/daemon/docs_generator.py`. The fallback section
  key `plugin` now renders as `### Daemon Plugin (N handler)`, through
  `_EXTRA_SECTION_HEADINGS`. This was test-first: two tests in
  `tests/unit/daemon/test_docs_generator.py` failed before the change and pass
  after it. The file was regenerated, and its diff is the single heading line.
- **Also fixed: the client config template.**
  `src/claude_code_hooks_daemon/daemon/init_config.py` labelled `plugins:` as
  "Custom project-specific handlers" in both templates. It now says "Daemon
  plugins: custom handler modules (not Claude Code plugins)". This was also
  test-first, in `tests/unit/daemon/test_init_config.py`. It is the most
  client-visible bare use, because every generated
  `.claude/hooks-daemon.yaml` carries it.
- **Also qualified in the docs**:
  - `docs/guides/CONFIGURATION.md`: the "Plugin System" section became "Daemon
    Plugins (handler modules)", with a pointer. The subheadings, the config
    comment and the path-exemption text were qualified too.
  - `CLAUDE/ARCHITECTURE.md`: the principle line, the §6 heading plus a
    one-line gloss, the diagram box, and "Project-Specific Handlers (Plugins)".
  - `CLAUDE/HANDLER_DEVELOPMENT.md` § Plugin Configuration.
  - `CLAUDE/QA.md` § Plugin Handlers.
  - `CLAUDE/AcceptanceTests/GENERATING.md`.
  - `CLAUDE/Code/WorkspaceResolution.md`.
  - `docs/CONFIG-VALIDATION.md`.
- **Left as is**: the "library/plugin separation" jargon in `CLAUDE/QA.md` and
  `CLAUDE/AgentTeam.md`. It is contributor-only QA vocabulary for project
  handlers and never meets a Claude Code plugin reader. Versioned upgrade
  guides and completed plans are frozen records. No anchors pointed at any
  renamed heading; I checked by grep.

## Release note

`CLAUDE/UPGRADES/UNRELEASED/release-notes/35-using-claude-code-plugins-alongside-the-daemon-is-now-documented.md`
(audience: client projects).

## QA

- `./scripts/qa/llm_qa.py docs_qa format british_english sensitive_content plan_qa`:
  all 5 checks passed.
- pytest passed on:
  - doc-truth, documented-commands and all remote-docs tests;
  - `test_docs_generator.py` and `test_sensitive_content.py`;
  - every test that imports `init_config`.
- ruff, black and mypy are clean on the two touched modules.
- The worktree daemon was restarted and was RUNNING before each commit.

## For the coordinator

- Task 4.1 says to "document the limit in the security docs". That limit, the
  `updatedInput` replacement, now has its canonical text in
  `CLAUDE/ClaudeCodePlugins.md#a-plugin-pretooluse-hook-can-replace-input-the-daemon-judged`.
  Task 4.1 should link there rather than restate it.
- When the Phase 2 to 4 features land, the "Daemon support for Claude Code
  plugins" section of `CLAUDE/ClaudeCodePlugins.md` is where to describe them.
  The advisory names should go there once they are final.
- Task 4.1's `health` line and SessionStart advisory will change what the
  daemon tells a user about plugin hooks. The sentence "The daemon never sees a
  plugin hook" stays true, because it describes Claude Code's execution model.
