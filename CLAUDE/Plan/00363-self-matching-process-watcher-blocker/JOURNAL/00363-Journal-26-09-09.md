# Plan 00363 — Journal 26-09-09

> **Append-only activity log** for plan 00363. One file per day
> (`00363-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 08:52 · action · — — plan scaffolded

Plan 00363 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 09:10 · decision · T1.1 — the detection test is the tool's own semantics

The obvious implementation is a table of "bad shapes": `pgrep -f` with a bare
word, `ps | grep` with no `-v`. That table has to be maintained against every
remedy anyone invents, and it still gets `run_0[3-9]` wrong.

`pgrep -f` matches an extended regular expression against a command line, and
the waiting shell's command line IS the command. So the classifier asks
exactly the tool's own question: **does the pattern, read as a regex, match
the text of the probe's own command line?** One test, and every documented
remedy falls out of it without being enumerated:

- `[p]rovision.bash` does not match `[p]rovision.bash` — the bracket trick.
- `run_0[3-9]` never matched its own spelling either, and is allowed for that
  reason rather than by a special case.
- `pgrep -x` compares the WHOLE line instead of searching within it, so it is
  answered before the regex test is reached.

The one ERE/Python divergence that bites here is anchors: ERE treats `^`/`$`
as ordinary characters anywhere but the ends, so the literal pattern
`'$PATTERN'` (single-quoted, nothing expanded) would read as "end of string,
then PATTERN" and find no match where the real tool finds one.
`_as_python_regex` re-spells those two.

## 09:35 · decision · T2.1 — deny the one-shot too

The filed plan proposed advisory for a one-shot probe, denying only inside a
loop. The imported incident report overrules that on its own timeline: the
agent ran the same `pgrep -f` BY HAND twice during the night, was told "still
running" both times, and believed it. The bug produced a false positive in the
check meant to catch the bug. An advisory would have been unread in the waiter
and unheeded at the prompt.

Rule IDs follow the report rather than the plan's first draft:
`R-PGREP-SELF-MATCH` (deny), `R-UNBOUNDED-LIVENESS-LOOP` (advise),
`R-PGREP-UNRESOLVED-PATTERN` (advise). Nothing had shipped under the earlier
`R-PROCESS-PROBE-*` spellings, so the rename cost nothing.

## 09:50 · finding · T3.2 — Rule C must not flag its own remedy

The deny message tells you to wait on an artefact:
`until grep -q "PLAY RECAP" run.log; do sleep 10; done`. That is a
`while`/`until` loop with a sleep-only body and no cap — precisely Rule C's
shape. The discriminator is the CONDITION: Rule C fires only when the
condition contains a process probe. A loop waiting on a log marker has none,
so it stays silent, which is what makes the advice coherent.

Two consequences worth recording:

- A loop written inside a quoted `bash -c` script is deliberately NOT reported
  by `classify_liveness_loops`. Those are the shapes that BOUND a loop
  (`timeout 3600 bash -c '…'`), so reporting them would advise against the
  fix. Probes inside such a script are still classified — that recursion does
  happen.
- `done` closes a loop only in COMMAND position. As an argument it is an
  ordinary word, and `until grep -q done run.log; do sleep 5; done` was
  closing the loop on the operand and losing the loop actually open.

## 10:05 · finding · T1.2 — two scanner bugs the corpus caught

- `2>&1` was being split on its `&` by the operator table, breaking the word
  so a redirect could be read as a pattern operand. Redirections are now
  matched before operators.
- `\pgrep -f run_02` resolved to the name `\pgrep` and matched nothing. A
  backslash suppresses alias expansion; bash still runs pgrep. `_basename`
  now strips the same grouping/escape prefixes
  `shell_segmentation._command_word` does. Surfaced by adding the handler to
  the blocking-handler evasion suite, not by inspection.

## 10:20 · handoff · P3 — Rule B not delivered

Phase 3 Task 3.1 (`R-WAIT-ON-WRAPPER-PID`) is left unticked. The instruction
was to attempt it only once Rules A and C were green under full QA, and the
QA state did not clear that bar in this cut.

`repo_hygiene` fails on `CLAUDE/UPGRADES/config-changes/v3.63.0.yaml`
carrying `date: "UNRELEASED"`. That file is inherited from main unchanged: the
v3.63.0 release is in flight and the manifest belongs to the release agent, so
it was left alone rather than edited from under a concurrent worker.

## Release v3.63.0 (main thread)

Rules A and C merged into main at `003f3036` and shipped in v3.63.0 (release commit `3cdc2e11`, tag pushed, GitHub release with five bootstrap assets). Callout 31 folded into `RELEASES/v3.63.0.md`; the v3.64.0 config-changes manifest entry folded into `CLAUDE/UPGRADES/config-changes/v3.63.0.yaml`. Live main-thread probes: the incident loop denied with the `[p]rovision.bash target-host` rewrite; the rewrite allowed. Rule B (Phase 3) remains.
