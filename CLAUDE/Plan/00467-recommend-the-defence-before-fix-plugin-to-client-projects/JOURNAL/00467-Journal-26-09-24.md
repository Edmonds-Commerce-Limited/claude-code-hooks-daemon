# Plan 00467 — Journal 26-09-24

_Scaffolded by `mkplan.bash`; timestamps in this file are UTC._

> **Append-only activity log** for plan 00467. One file per day
> (`00467-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM, ONLY with
> `mkplan.bash --journal <plan-number> <category> <body-file>` (it stamps the
> real UTC time; a hand-written entry is denied); NEVER edit earlier entries
> (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` UTC 24h (the date is in the filename, also UTC); times increase
>   down the file. A file with no sentinel line above predates this system —
>   its times are local, zone unrecorded (legacy; never rewritten).
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 17:25 · action · — — plan scaffolded

Plan 00467 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress with `mkplan.bash --journal` as it happens.

## 17:25 · action · T1.1   — plugin reviewed and installed at project scope

The owner asked to install the Defence Before Fix plugin, dogfood it, and later recommend it to all hooks-daemon projects.

- Reviewed before installing: v0.1.1, MIT, no hooks or MCP. Both agents are read-only (Read, Grep, Glob, Bash). The skill's only extra grant is its own `refresh-spec.bash`, which fetches five method documents from `defence-before-fix.github.io` into the plugin data directory, with a vendored fallback.
- Installed at project scope with `claude plugin marketplace add Defence-Before-Fix/claude-plugin --scope project` and `claude plugin install defence-before-fix@defence-before-fix --scope project` (commit `b00ecd02`). The CLI also reordered two existing `settings.json` keys, with no value changed.
- `plugin details` projects about 290 tokens always-on.
- Dedupe scout: 43 live plans checked, no duplicate. Prior art is 00133/00308 (suggesting daemon features on upgrade) and 00426 (plugin config examples).
- First finding, before any run: the skill's toolchain discovery reads `composer.json`/`package.json` keys and a PHP/TypeScript register. This repository's Python detectors (`scripts/qa/check_*.py` behind `llm_qa.py`, catalogued in `CLAUDE/Security/`) are invisible to it.
- The method itself agrees with `CLAUDE/CodeLifecycle/Bugs.md`, which defers to the same canonical specification.

## 17:43 · action · T1.3   — upstream PR and issue filed; plugin-support audit dispatched

The owner confirmed the direction ("this is all great dog fooding… we have to support plugins properly") and asked for the work to run unattended.

- Working clone: moved from `untracked/repos/` to `untracked/work/claude-plugin`, because `untracked/repos/` is governed as read-only references by `reference_repo_freshness`.
- The owner asked for direct pushes as the current git user. They are impossible: the only identity, `lts-bob`, has pull-only access (`push: false`). So the change went through a fork (`lts-bob/claude-plugin`) and a PR: Defence-Before-Fix/claude-plugin#1 (README: load a local checkout with `--plugin-dir`).
- Task 1.3, first shortfall filed: Defence-Before-Fix/claude-plugin#2. Toolchain discovery reads only the composer/package declarations and the PHP/TS recipes, so a Python or bespoke-detector project cannot declare its defence entry point.
- Plugin-support audit dispatched (report: subagent-reports/260924-plugin-audit-opus-5-5.md). 00466 N9 (docs_qa judges the gitignored plugin cache) is the first daemon defect the install exposed, and is being fixed.
