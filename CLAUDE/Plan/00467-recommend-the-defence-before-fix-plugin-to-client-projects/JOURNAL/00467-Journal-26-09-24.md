# Plan 00467 — Journal 26-09-24

_Scaffolded by `mkplan.bash`; timestamps in this file are UTC._

> **Append-only activity log** for plan 00467. One file per day
> (`00467-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM, ONLY with
> `mkplan.bash --journal <plan-number> <category> <body-file>` (it stamps the
> real UTC time; a hand-written entry is denied); NEVER edit earlier entries
> (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` UTC 24h (the date is in the filename, also UTC); times increase
>   down the file. A file with no sentinel line above predates this system —
>   its times are local, zone unrecorded (legacy; never rewritten).
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 17:25 · action · — — plan scaffolded

Plan 00467 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress with `mkplan.bash --journal` as it happens.

## 17:25 · action · T1.1   — plugin reviewed and installed at project scope

The owner asked to install the Defence Before Fix plugin, dogfood it, and later recommend it to all hooks-daemon projects.

- Reviewed before installing: v0.1.1, MIT, no hooks or MCP. Both agents are read-only (Read, Grep, Glob, Bash). The skill's only extra grant is its own `refresh-spec.bash`, which fetches five method documents from `defence-before-fix.github.io` into the plugin data directory, with a vendored fallback.
- Installed at project scope with `claude plugin marketplace add Defence-Before-Fix/claude-plugin --scope project` and `claude plugin install defence-before-fix@defence-before-fix --scope project` (commit `b00ecd02`). The CLI also reordered two existing `settings.json` keys, with no value changed.
- `plugin details` projects about 290 tokens always-on.
- Dedupe scout: 43 live plans checked, no duplicate. Prior art is 00133/00308 (suggesting daemon features on upgrade) and 00426 (plugin config examples).
- First finding, before any run: the skill's toolchain discovery reads `composer.json`/`package.json` keys and a PHP/TypeScript register. This repository's Python detectors (`scripts/qa/check_*.py` behind `llm_qa.py`, catalogued in `CLAUDE/Security/`) are invisible to it.
- The method itself agrees with `CLAUDE/CodeLifecycle/Bugs.md`, which defers to the same canonical specification.

## 17:43 · action · T1.3   — upstream PR and issue filed; plugin-support audit dispatched

The owner confirmed the direction ("this is all great dog fooding… we have to support plugins properly") and asked for the work to run unattended.

- Working clone: moved from `untracked/repos/` to `untracked/work/claude-plugin`, because `untracked/repos/` is governed as read-only references by `reference_repo_freshness`.
- The owner asked for direct pushes as the current git user. They are impossible: the only identity, `lts-bob`, has pull-only access (`push: false`). So the change went through a fork (`lts-bob/claude-plugin`) and a PR: Defence-Before-Fix/claude-plugin#1 (README: load a local checkout with `--plugin-dir`).
- Task 1.3, first shortfall filed: Defence-Before-Fix/claude-plugin#2. Toolchain discovery reads only the composer/package declarations and the PHP/TS recipes, so a Python or bespoke-detector project cannot declare its defence entry point.
- Plugin-support audit dispatched (report: subagent-reports/260924-plugin-audit-opus-5-5.md). 00466 N9 (docs_qa judges the gitignored plugin cache) is the first daemon defect the install exposed, and is being fixed.

## 18:00 · finding · —   — Plugin audit: P1 acted on, the rest to Plan 00468

The plugin-support audit is back (subagent-reports/260924-plugin-audit-opus-5-5.md): 8 defects, 3 of them release-blocking, 16 gaps and 13 areas fine. Every finding is filed in Plan 00468, which owns the daemon-side fixes.

- P1 had to be acted on at once. The project-scope install put `enabledPlugins` and `extraKnownMarketplaces` into `.claude/settings.json`, which the installers ship to every client as the template. So the next release would have enabled this plugin in every client, against this plan's own non-goal. The install moved to local scope: the two keys are now in the tracked `.claude/settings.local.json`, which no installer ships, and `settings.json` is back byte-for-byte to its content before the install. The dogfood itself is unchanged. 00468 Task 1.2 adds the test that keeps it that way.
- Task 1.2 data point (F2): the plugin's vendored SPEC and DETECTOR-SPEC match `remote-docs/` and a fresh fetch byte for byte. Always-on cost is about 290 tokens, and an invocation about 1.8k, against the 1.6k projected.
- Upstream: the two DBF agents have no `Write` tool but are told to write a report file. That goes upstream as 00468 Task 2.4.

## 20:27 · finding · —   — Task 1.2 evaluated by hand; 4 more instances of the hostile-PATH class; drafts 01-06

Task 1.2 evidence is in EVALUATION.md, and Task 1.3's upstream drafts are in upstream-drafts/. Task 1.2 is not ticked: auto-trigger was judged from the description only.

- **Run mode.** `/dbf` was followed by hand. The skill and both agents were not offered to this sub-agent session. A headless `claude -p --plugin-dir <cache>` probe in a scratch repository confirmed that the plugin loads (its init event lists `defence-before-fix:dbf` and both agents), but it stopped at "Not logged in", so no model turn ran and there is no live auto-trigger evidence.
- **Detector discovery (criterion 1).**
  - SKILL.md:42-58 reads composer.json and package.json keys only.
  - The register has no shell row. Its only Python detector graded green is pylint, which this repo does not run.
  - The fallback picks a register tool without looking at `scripts/qa/`. That contradicts SPEC section 2's "checked the Detectors the project already runs".
- **Real run (criterion 2).** The defect was the `resolve_venv.sh` watchdog `sleep` (766677c1).
  - Class: a shell path that must survive a hostile PATH but depends on a PATH command.
  - An emulated independent searcher (Explore agent on sonnet, the plugin agent's body verbatim) found 4 candidate instances:
    - `venv_bootstrap.sh:443` and `:474` (`date`);
    - `install/venv.sh:427` (`date`);
    - `install/daemon_control.sh:40-43` (`pgrep`).
  - I reproduced the `if [ "$(date +%s)" -ge N ]` idiom silently taking the else branch under `set -euo pipefail`.
  - A detector was proposed (a declared surface inventory plus a `scripts/qa` checker) and not built. The instances are unfixed and are handed to the coordinator for the niggles ledger.
- **Spec duplication (criterion 4).** All versions agree: SPEC 1.0.1, DETECTOR 1.0.0 and TOOLING 0.2.0, all published 2026-09-08. A `--force` fetch matched all five plugin copies byte for byte.
  - Correction to the 18:00 entry: remote-docs' SPEC.md and DETECTOR-SPEC.md are HTML conversions (`fidelity: converted`), not byte-identical.
  - TOOLING-SPEC and the project prompt are identical by `source_sha256`.
- **Token cost (criterion 5, bytes / 4).**
  - Always-on is 1,137 bytes, about 285 tokens, which matches the projection.
  - The ~1.6k projection is the SKILL.md body alone. The mandatory reads make a floor of about 5.7k per invocation, and about 20k here, because the register must be read.
- **Report conventions (criterion 6).** The emulated searcher did not write its named file. It cited the harness's "do not write report files" note, and the report survived only through the Plan 00460 auto-save. This is new evidence for upstream #3.
- **Plan 00463.** `conformance-reviewer` is always a worktree sub-agent that must run the project's entry point twice, so it needs the targeted `llm_qa.py <tool>` form.
- **Upstream (Task 1.3).** Existing issues are #2 (toolchain discovery) and #3, which is the Write-less agents; the brief called it "the plugin's name in hook output". Drafts, none filed:
  - 01: the fallback skips the project's detectors;
  - 02: the trigger fires on every red QA run;
  - 03: the reviewer's full-gate cost;
  - 04: `refresh-spec` never sees `CLAUDE_PLUGIN_DATA` (per the vendored plugins reference, it is not in the Bash tool's environment), so it caches in `~/.cache`, and the prompt prints version `-`;
  - 05 and 06: comments on #2 and #3.
