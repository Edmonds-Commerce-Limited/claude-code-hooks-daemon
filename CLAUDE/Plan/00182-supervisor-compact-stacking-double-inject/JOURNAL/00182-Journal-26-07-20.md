# Plan 00182 — Journal 26-07-20

> **Append-only activity log** for plan 00182. One file per day
> (`00182-Journal-YY-MM-DD.md`). `PLAN.md` tracks the plan (what/why,
> current state, tasks); this journal tracks what actually *happened* —
> findings, decisions, dead-ends, hand-off state — the linear lifecycle a plan
> document structurally cannot carry.
>
> **Entry grammar** — append new entries at the BOTTOM; NEVER edit earlier
> entries (corrections are new entries):
>
> ```
> ## HH:MM · category · REF   — optional short title
> ```
>
> - `HH:MM` local 24h (the date is in the filename); times increase down the file.
> - `category` ∈ `action` | `finding` | `decision` | `thought` | `blocker` | `handoff`
> - `REF` = optional task/phase ref (`T1.2`, `P1`) or `—`.
> - Bodies may embed fenced logs/diffs/snippets — no size limit — with a
>   one-line takeaway above the fence.
> - End a working session with a `handoff` entry so the next agent's entry
>   point is the last entry of the newest day-file.

## 15:28 · action · — — plan scaffolded

Plan 00182 created via `mkplan.bash`; `JOURNAL/` initialised. Next:
fill in `PLAN.md`, then log progress here as it happens.

## 15:40 · finding · — — root cause corrected by sub-agent

My initial code trace blamed the `AWAIT_COMPACTING` give-up → MONITOR
re-inject path. A review sub-agent (16 tool uses into the IPC layer)
found the ACTUAL mechanism, which I then re-verified line-by-line: the
double-`/compact` is a host↔policy-worker IPC desync, NOT a state-machine
defect. `PolicyWorker.decide()` (`claude-supervise.py:2362`) had no
correlation id / stale-reply drain, and `_WORKER_READ_TIMEOUT_SECONDS`
(2.0, line 125) == `_DEFAULT_POLL_SECONDS` (2.0, line 1822). A slow
`/proc`-scanning tick times out → host falls back + injects #1 → next
tick reads the buffered stale `WOULD_COMPACT` reply and `_apply_decision`
(line 2101) injects #2 with no host-state guard. PLAN.md + README row
rewritten from the wrong mechanism to this verified one before any code.

## 16:05 · action · T1.1,T2.1,T2.2 — Phase 1+2 committed (57a78f37)

RED tests added in `test_policy_worker.py` (stale-reply drain,
only-stale→None, matching-reply, worker echoes id, roundtrips) +
`_apply_decision` guard tests. Fix: per-request `tick_id` on
`TickFacts`/`TickOutcome` (+ serialization + worker echo);
`decide()` stamps each request and drains any id-mismatched reply
(returns None → safe fallback if only stale arrives); `_apply_decision`
gains `host_state` and suppresses a `WOULD_COMPACT` while already
`AWAIT_COMPACTING`. 344 supervise tests green; QA 12/13 (format
auto-fixed by the check), 10411 tests, 95.3% cov.

## 16:20 · decision · T3.1 — throttle the /proc scan, don't decouple the timeout

Two Phase-3 options: (a) throttle `cached_own_session_ids`'s full
`/proc` environ scan, or (b) make the read timeout distinct from the
poll interval. Chose (a): decoupling the timeout risks blocking the host
past a tick or increasing fallback; throttling attacks the ROOT latency
(the scan reads every `/proc/<pid>/environ` each tick). Guarded so an
EMPTY cache always re-scans (discovery never starved). TTL 30s → new
subagent sessions still recognised within 30s; the accumulate-set keeps
known ids forever. RED (missing attr) → GREEN; 346 supervise tests pass.

## 16:25 · handoff · — — Phase 3 QA in flight

Phase 1+2 committed (57a78f37). Phase 3 (throttle) implemented + tests
green locally; full QA running. Next: on QA green, flip Phase 4 boxes,
commit Phase 3, then the true dogfood is a ccy relaunch under sustained
CRITICAL context (the supervisor is a standalone process — a daemon
restart does not reload it).
