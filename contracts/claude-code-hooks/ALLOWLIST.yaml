# Reasoned allowlist for the hook-contract QA check (Plan 00271 Decision 2).
# Every remaining entry is a KEPT gap from the Task 3.1 burn-down: each
# carries the reason it is deliberate and the task that decided it. The
# checker FAILS on an entry whose drift disappears — delete it with the
# change that closes the gap.
entries:
  - id: "dead-letter-field:ConfigChange:systemMessage"
    reason: "KEPT (Plan 00271 Task 3.1): the docs say Claude Code accepts but DISCARDS systemMessage on this event; the daemon's advisory path still emits it, which is documented-valid. Removing the emission would silently drop handler context with no replacement channel."
    link: "Plan 00271 Task 3.1"
  - id: "dead-letter-field:Notification:systemMessage"
    reason: "KEPT (Plan 00271 Task 3.1): the docs say Claude Code accepts but DISCARDS systemMessage on this event; the daemon's advisory path still emits it, which is documented-valid. Removing the emission would silently drop handler context with no replacement channel."
    link: "Plan 00271 Task 3.1"
  - id: "dead-letter-field:PreCompact:systemMessage"
    reason: "KEPT (Plan 00271 Task 3.1): the docs say Claude Code accepts but DISCARDS systemMessage on this event; the daemon's advisory path still emits it, which is documented-valid. Removing the emission would silently drop handler context with no replacement channel."
    link: "Plan 00271 Task 3.1"
  - id: "dead-letter-field:SessionEnd:systemMessage"
    reason: "KEPT (Plan 00271 Task 3.1): the docs say Claude Code accepts but DISCARDS systemMessage on this event; the daemon's advisory path still emits it, which is documented-valid. Removing the emission would silently drop handler context with no replacement channel."
    link: "Plan 00271 Task 3.1"
  - id: "dead-letter-field:WorktreeCreate:systemMessage"
    reason: "KEPT (Plan 00271 Task 3.1): the docs say Claude Code accepts but DISCARDS systemMessage on this event; the daemon's advisory path still emits it, which is documented-valid. Removing the emission would silently drop handler context with no replacement channel."
    link: "Plan 00271 Task 3.1"
  - id: "dead-letter-field:WorktreeRemove:systemMessage"
    reason: "KEPT (Plan 00271 Task 3.1): the docs say Claude Code accepts but DISCARDS systemMessage on this event; the daemon's advisory path still emits it, which is documented-valid. Removing the emission would silently drop handler context with no replacement channel."
    link: "Plan 00271 Task 3.1"
  - id: "undocumented-schema-field:PermissionRequest:hookSpecificOutput.additionalContext"
    reason: "KEPT (Plan 00271 Task 3.1): deliberate daemon extension carrying ALLOW/ASK advisory context on PermissionRequest; the deny reason now travels on the documented decision.message field."
    link: "Plan 00271 Task 3.1"
  - id: "undocumented-schema-field:PermissionRequest:hookSpecificOutput.guidance"
    reason: "KEPT (Plan 00271 Task 3.2): guidance is a deliberate daemon-internal extension field, commented as such in response_schemas.py; Claude Code ignores unknown keys so it is harmless on the wire."
    link: "Plan 00271 Task 3.2"
  - id: "undocumented-schema-field:PostToolUse:hookSpecificOutput.guidance"
    reason: "KEPT (Plan 00271 Task 3.2): guidance is a deliberate daemon-internal extension field, commented as such in response_schemas.py; Claude Code ignores unknown keys so it is harmless on the wire."
    link: "Plan 00271 Task 3.2"
  - id: "undocumented-schema-field:PreToolUse:hookSpecificOutput.guidance"
    reason: "KEPT (Plan 00271 Task 3.2): guidance is a deliberate daemon-internal extension field, commented as such in response_schemas.py; Claude Code ignores unknown keys so it is harmless on the wire."
    link: "Plan 00271 Task 3.2"
  - id: "undocumented-schema-field:UserPromptSubmit:hookSpecificOutput.guidance"
    reason: "KEPT (Plan 00271 Task 3.2): guidance is a deliberate daemon-internal extension field, commented as such in response_schemas.py; Claude Code ignores unknown keys so it is harmless on the wire."
    link: "Plan 00271 Task 3.2"
  - id: "unexpressed-capability:PostToolUse:hookSpecificOutput.classifierContext"
    reason: "KEPT (Plan 00271 Task 3.1, plan Non-Goals): output rewriting/classifier annotation is a documented capability no daemon feature needs; add a HookResult carrier when a feature does (YAGNI)."
    link: "Plan 00271 Task 3.1"
  - id: "unexpressed-capability:PostToolUse:hookSpecificOutput.updatedMCPToolOutput"
    reason: "KEPT (Plan 00271 Task 3.1, plan Non-Goals): output rewriting/classifier annotation is a documented capability no daemon feature needs; add a HookResult carrier when a feature does (YAGNI)."
    link: "Plan 00271 Task 3.1"
  - id: "unexpressed-capability:PostToolUse:hookSpecificOutput.updatedToolOutput"
    reason: "KEPT (Plan 00271 Task 3.1, plan Non-Goals): output rewriting/classifier annotation is a documented capability no daemon feature needs; add a HookResult carrier when a feature does (YAGNI)."
    link: "Plan 00271 Task 3.1"
  - id: "unexpressed-capability:UserPromptSubmit:hookSpecificOutput.sessionTitle"
    reason: "KEPT (Plan 00271 Task 3.1): sessionTitle/suppressOriginalPrompt have no HookResult carrier and no daemon feature needs them yet (YAGNI)."
    link: "Plan 00271 Task 3.1"
  - id: "unexpressed-capability:UserPromptSubmit:suppressOriginalPrompt"
    reason: "KEPT (Plan 00271 Task 3.1): sessionTitle/suppressOriginalPrompt have no HookResult carrier and no daemon feature needs them yet (YAGNI)."
    link: "Plan 00271 Task 3.1"
  - id: "unexpressed-capability:WorktreeCreate:hookSpecificOutput.worktreePath"
    reason: "KEPT (Plan 00271 Task 3.1): hookSpecificOutput.worktreePath is the HTTP-hook form; the daemon is a COMMAND hook, whose documented form is the raw path on stdout — carried as the top-level worktreePath envelope key the forwarder prints raw (Plan 00188)."
    link: "Plan 00271 Task 3.1"
  - id: "unexpressed-universal-fields:Notification:universal"
    reason: "KEPT (Plan 00271 Task 3.1): no daemon feature emits continue/stopReason/suppressOutput/terminalSequence on this event; declaring fields nothing emits would weaken the schema for no capability. terminalSequence is the first candidate if a notification feature ever needs it."
    link: "Plan 00271 Task 3.1"
  - id: "unexpressed-universal-fields:PermissionRequest:universal"
    reason: "KEPT (Plan 00271 Task 3.1): no daemon feature emits continue/stopReason/suppressOutput/terminalSequence on this event; declaring fields nothing emits would weaken the schema for no capability. terminalSequence is the first candidate if a notification feature ever needs it."
    link: "Plan 00271 Task 3.1"
  - id: "unexpressed-universal-fields:PostToolUse:universal"
    reason: "KEPT (Plan 00271 Task 3.1): no daemon feature emits continue/stopReason/suppressOutput/terminalSequence on this event; declaring fields nothing emits would weaken the schema for no capability. terminalSequence is the first candidate if a notification feature ever needs it."
    link: "Plan 00271 Task 3.1"
  - id: "unexpressed-universal-fields:PreCompact:universal"
    reason: "KEPT (Plan 00271 Task 3.1): no daemon feature emits continue/stopReason/suppressOutput/terminalSequence on this event; declaring fields nothing emits would weaken the schema for no capability. terminalSequence is the first candidate if a notification feature ever needs it."
    link: "Plan 00271 Task 3.1"
  - id: "unexpressed-universal-fields:PreToolUse:universal"
    reason: "KEPT (Plan 00271 Task 3.1): no daemon feature emits continue/stopReason/suppressOutput/terminalSequence on this event; declaring fields nothing emits would weaken the schema for no capability. terminalSequence is the first candidate if a notification feature ever needs it."
    link: "Plan 00271 Task 3.1"
  - id: "unexpressed-universal-fields:SessionEnd:universal"
    reason: "KEPT (Plan 00271 Task 3.1): no daemon feature emits continue/stopReason/suppressOutput/terminalSequence on this event; declaring fields nothing emits would weaken the schema for no capability. terminalSequence is the first candidate if a notification feature ever needs it."
    link: "Plan 00271 Task 3.1"
  - id: "unexpressed-universal-fields:SessionStart:universal"
    reason: "KEPT (Plan 00271 Task 3.1): no daemon feature emits continue/stopReason/suppressOutput/terminalSequence on this event; declaring fields nothing emits would weaken the schema for no capability. terminalSequence is the first candidate if a notification feature ever needs it."
    link: "Plan 00271 Task 3.1"
  - id: "unexpressed-universal-fields:Stop:universal"
    reason: "KEPT (Plan 00271 Task 3.1): no daemon feature emits continue/stopReason/suppressOutput/terminalSequence on this event; declaring fields nothing emits would weaken the schema for no capability. terminalSequence is the first candidate if a notification feature ever needs it."
    link: "Plan 00271 Task 3.1"
  - id: "unexpressed-universal-fields:SubagentStop:universal"
    reason: "KEPT (Plan 00271 Task 3.1): no daemon feature emits continue/stopReason/suppressOutput/terminalSequence on this event; declaring fields nothing emits would weaken the schema for no capability. terminalSequence is the first candidate if a notification feature ever needs it."
    link: "Plan 00271 Task 3.1"
  - id: "unexpressed-universal-fields:UserPromptSubmit:universal"
    reason: "KEPT (Plan 00271 Task 3.1): no daemon feature emits continue/stopReason/suppressOutput/terminalSequence on this event; declaring fields nothing emits would weaken the schema for no capability. terminalSequence is the first candidate if a notification feature ever needs it."
    link: "Plan 00271 Task 3.1"
  - id: "unexpressed-universal-fields:WorktreeCreate:universal"
    reason: "KEPT (Plan 00271 Task 3.1): no daemon feature emits continue/stopReason/suppressOutput/terminalSequence on this event; declaring fields nothing emits would weaken the schema for no capability. terminalSequence is the first candidate if a notification feature ever needs it."
    link: "Plan 00271 Task 3.1"
  - id: "unexpressed-universal-fields:WorktreeRemove:universal"
    reason: "KEPT (Plan 00271 Task 3.1): no daemon feature emits continue/stopReason/suppressOutput/terminalSequence on this event; declaring fields nothing emits would weaken the schema for no capability. terminalSequence is the first candidate if a notification feature ever needs it."
    link: "Plan 00271 Task 3.1"
