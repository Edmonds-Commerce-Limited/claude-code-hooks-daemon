{
  "event": "PreToolUse",
  "block_mechanism": "permission-decision",
  "can_block": true,
  "ask_capable": true,
  "top_level_output_fields": [
    "continue",
    "stopReason",
    "suppressOutput",
    "systemMessage",
    "terminalSequence"
  ],
  "top_level_decision_enum": null,
  "hook_specific_output_fields": {
    "permissionDecision": {
      "enum": [
        "allow",
        "deny",
        "ask",
        "defer"
      ]
    },
    "permissionDecisionReason": {},
    "updatedInput": {},
    "additionalContext": {}
  },
  "discarded_fields": [],
  "conditional_input_fields": {
    "agent_id": "Present ONLY when the hook fires inside a subagent call, and documented upstream as the way 'to distinguish subagent hook calls from main-thread calls'. No input_example here can ever show it: every example depicts a main-thread call, where it is absent by definition. Upstream names PreToolUse and PostToolUse explicitly as the tool events that fire inside a subagent.",
    "agent_type": "Present when the session uses --agent or the hook fires inside a subagent. For subagents the subagent's type takes precedence over the session's --agent value. Absent from the example for the same reason as agent_id.",
    "prompt_id": "In the example, but absent until the first user input of the session. Requires Claude Code v2.1.196 or later.",
    "scratchpad_dir": "In the example, but absent when the session has no scratchpad or the temp directory is unavailable. Requires Claude Code v2.1.257 or later.",
    "effort": "In the example, but present only when the current model supports the effort parameter."
  },
  "notes": "Four outcomes: allow/deny/ask/defer, plus updatedInput to rewrite the tool's input before execution (replaces the ENTIRE input object). Precedence across hooks: deny > defer > ask > allow. For 'defer', permissionDecisionReason, updatedInput and additionalContext are ignored.",
  "input_example": {
    "session_id": "abc123",
    "prompt_id": "00000000-0000-0000-0000-000000000000",
    "transcript_path": "/home/user/.claude/projects/.../transcript.jsonl",
    "cwd": "/home/user/my-project",
    "scratchpad_dir": "/tmp/claude-1000/-home-user-my-project/abc123/scratchpad",
    "permission_mode": "default",
    "effort": {
      "level": "high"
    },
    "hook_event_name": "PreToolUse",
    "tool_name": "Bash",
    "tool_input": {
      "command": "npm test",
      "description": "Run test suite",
      "timeout": 120000,
      "run_in_background": false
    },
    "tool_use_id": "toolu_01ABC123..."
  }
}
