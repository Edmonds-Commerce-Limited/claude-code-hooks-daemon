{
  "event": "Stop",
  "block_mechanism": "top-level-decision",
  "can_block": true,
  "ask_capable": false,
  "top_level_output_fields": [
    "continue",
    "stopReason",
    "suppressOutput",
    "systemMessage",
    "terminalSequence",
    "decision",
    "reason"
  ],
  "top_level_decision_enum": [
    "block"
  ],
  "hook_specific_output_fields": {
    "additionalContext": {}
  },
  "discarded_fields": [],
  "conditional_input_fields": {
    "agent_id": "Present ONLY when the hook fires inside a subagent call. A main-thread Stop carries no agent identity, which is what makes its ABSENCE the usable signal.",
    "agent_type": "Present when the session uses --agent or the hook fires inside a subagent. Note a session-wide --agent makes this present on a MAIN-THREAD stop too, so agent_type alone does not mean 'in a subagent' -- agent_id does.",
    "effort": "In the example, but present only when the current model supports the effort parameter.",
    "session_crons": "In the example, but conditional in practice -- an ABSENT list is not the same fact as an EMPTY one. One entry per session-scoped wakeup sourced from CronCreate, ScheduleWakeup and /loop, each with id, schedule, recurring and prompt. Note `prompt` is CAPPED at 1000 characters with an in-string '... [+N chars]' marker, so exact-equality matching against a declared prompt longer than that never succeeds.",
    "background_tasks": "In the example, but conditional in practice -- treat an absent list as 'unknown', never as 'no tasks running'. `description` and `command` are capped at 1000 characters with the same marker."
  },
  "notes": "reason is REQUIRED when decision is 'block'. hookSpecificOutput.additionalContext is non-error feedback that continues the conversation.",
  "input_example": {
    "session_id": "abc123",
    "transcript_path": "~/.claude/projects/.../00000000-0000-0000-0000-000000000000.jsonl",
    "cwd": "/Users/...",
    "permission_mode": "default",
    "effort": {
      "level": "high"
    },
    "hook_event_name": "Stop",
    "stop_hook_active": true,
    "last_assistant_message": "I've completed the refactoring. Here's a summary...",
    "background_tasks": [
      {
        "id": "task-001",
        "type": "shell",
        "status": "running",
        "description": "tail logs",
        "command": "tail -f /var/log/syslog"
      }
    ],
    "session_crons": [
      {
        "id": "cron-001",
        "schedule": "0 9 * * 1-5",
        "recurring": true,
        "prompt": "check the build"
      }
    ]
  }
}
