# CCY session data - NEVER commit sensitive files
# Only the whitelisted (!) files below are safe to track
*
!.gitignore
!Dockerfile
!ccy.env
!claude-supervise.py
# CLAUDE.md deliberately NOT whitelisted: ccy's startup gate refuses to launch
# when it is tracked. Kept on disk untracked; see untracked/fedora-desktop-ccy-CLAUDE-bug.md
