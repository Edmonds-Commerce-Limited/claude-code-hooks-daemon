# Release v2.23.2 - Git Handler CWD Fix and Script Permission Corrections

**Release Date:** 2026-03-16
**Type:** Patch Release

## Summary

Version 2.23.2 fixes a bug where `GitFilemodeCheckerHandler` and `GitContextInjectorHandler` ran git subprocess calls without specifying a working directory. Because the daemon process runs from `/`, all git commands silently failed. Both handlers now use `ProjectContext.project_root()` as `cwd`. Additionally, 7 shell scripts that were missing the executable bit have been corrected in the git index.

## Highlights

- **Git handler CWD fix**: Handlers that run git commands now use the authoritative project root as working directory
- **Script permission corrections**: 7 shell scripts fixed with `git update-index --chmod=+x`

## Changes

### Fixed

- **Git handlers use `ProjectContext.project_root()` as cwd**: `GitFilemodeCheckerHandler` and `GitContextInjectorHandler` previously ran git subprocess calls without specifying a working directory. Because the daemon process runs from `/`, all git commands failed with "not a git repository" errors. Both handlers now use `ProjectContext.project_root()` as `cwd`, with fallback to the process working directory when `ProjectContext` is not yet initialised. Tests updated to mock `ProjectContext.project_root()` alongside `subprocess.run`.

- **Shell script executable permissions**: Fixed missing executable bit on 7 shell scripts (`install/mode_guard.sh`, `install/test_helpers.sh`, `.claude/skills/mode/invoke.sh`, and four `skills/hooks-daemon/scripts/` scripts). Permissions updated in the git index via `git update-index --chmod=+x`.

## Upgrade Instructions

Standard upgrade — no breaking changes:

```bash
cd .claude/hooks-daemon
git fetch origin
git checkout v2.23.2
./scripts/install/upgrade.sh
```

## Test Statistics

- **Total Tests**: 7344 passing
- **Coverage**: 95.0% (minimum required)

## Installation

```bash
git clone -b v2.23.2 https://github.com/Edmonds-Commerce-Limited/claude-code-hooks-daemon.git .claude/hooks-daemon
cd .claude/hooks-daemon && ./scripts/install/install.sh
```

## Links

- **Full Changelog**: [v2.23.1...v2.23.2](https://github.com/Edmonds-Commerce-Limited/claude-code-hooks-daemon/compare/v2.23.1...v2.23.2)
- **Previous Release**: [v2.23.1](https://github.com/Edmonds-Commerce-Limited/claude-code-hooks-daemon/releases/tag/v2.23.1)
