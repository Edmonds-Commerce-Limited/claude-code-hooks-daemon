# Release v2.23.3 - Health Check Self-Install Fix and Daemon Start Progress Output

**Release Date:** 2026-03-16
**Type:** Patch Release

## Summary

Version 2.23.3 fixes a false-positive failure in `health_check.sh` where the presence of a `hooks-daemon.env` file was incorrectly used as a proxy for self-install mode detection, and adds a progress message to `daemon_control.sh` so daemon startup no longer appears to hang during upgrades.

## Highlights

- **Health check self-install false positive fixed**: Normal installations with a `hooks-daemon.env` file no longer incorrectly fail the self-install mode check
- **Daemon start progress output**: Upgrade script no longer appears hung during handler loading

## Changes

### Fixed

- **Health check self-install false positive**: `scripts/health_check.sh` previously used the presence of a `.env` file as a proxy for self-install mode detection. This was incorrect — normal installations can also have a `hooks-daemon.env` file, causing false-positive failures. The script now reads `self_install_mode` directly from the YAML config, matching the logic already used in `upgrade.sh`.

- **Daemon start progress output**: `scripts/install/daemon_control.sh` now emits a progress message before the blocking daemon start call. Previously, callers (such as the upgrade script) would see no output during handler loading, making the script appear to hang. The new message makes it clear the daemon is starting and may take a moment.

## Upgrade Instructions

Standard upgrade — no breaking changes:

```bash
cd .claude/hooks-daemon
git fetch origin
git checkout v2.23.3
./scripts/install/upgrade.sh
```

## Acceptance Testing

PATCH release — no handler changes, acceptance testing not required.

## Test Statistics

- **Total Tests**: 7344 passing
- **Coverage**: 95.0% (minimum required)

## Installation

```bash
git clone -b v2.23.3 https://github.com/Edmonds-Commerce-Limited/claude-code-hooks-daemon.git .claude/hooks-daemon
cd .claude/hooks-daemon && ./scripts/install/install.sh
```

## Links

- **Full Changelog**: [v2.23.2...v2.23.3](https://github.com/Edmonds-Commerce-Limited/claude-code-hooks-daemon/compare/v2.23.2...v2.23.3)
- **Previous Release**: [v2.23.2](https://github.com/Edmonds-Commerce-Limited/claude-code-hooks-daemon/releases/tag/v2.23.2)
