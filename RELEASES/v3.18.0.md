# v3.18.0 — Minor: truth-changes project-doc reconciliation, single-daemon project-root scoping

**Release date**: 2026-06-04
**Type**: MINOR release
**Acceptance Testing Gate (RELEASING.md Step 12)**: MINOR release with narrowly-scoped handler changes. Acceptance was verified via the Step 12.0 H-1 deterministic gate plus targeted live verification of the one changed handler and the new CLI command, rather than the full ~89-test playbook. The only handler change is `plan_number_helper.get_claude_md()` (added always-on guidance — no `matches()`/`handle()` change); the new `check-truth-changes` CLI and `install/truth_changes.py` loader are covered by unit + CLI-acceptance tests; the single-daemon enforcement scoping fix is daemon-internal and covered by the H-1 gate, daemon-load, and dedicated regression tests.

## Summary

v3.18.0 delivers the **truth-changes** project-doc reconciliation mechanism (Plan 00118). When the daemon changes a documented truth — a workflow, command, or convention a consuming project's own docs assert — a per-version `was → now` entry now ships with the daemon, and the upgrade flow drives the project's LLM to reconcile its own docs. The release also fixes a container-mode single-daemon enforcement bug that could terminate a daemon serving a different project when PID namespaces are shared, and adds supporting release governance and docs.

## Highlights

- **Truth-changes mechanism** — per-version `CLAUDE/UPGRADES/truth-changes/v{X.Y.Z}.yaml` (`was`/`now`; `now: ~` retires a truth), an `install/truth_changes.py` range loader, and a `check-truth-changes --from --to` CLI. The `upgrade.md` flow reconciles the project's own docs over the version range it crossed.
- **Always-on plan-numbering guidance** — `plan_number_helper.get_claude_md()` now states the next plan number comes from `git config --local hooksdaemon.latestPlanNumber` (was `None`), so the injected `<hooksdaemon>` guidance matches the handler's block message.
- **Backfilled truth-changes** — v3.12.0 (completion dates → commit hashes), v3.16.0 (plan-number folder-scan → git counter), v3.17.0 (workflow-state subsystem + `docs/WORKFLOWS.md` removed, `now: ~`).
- **Container PID-namespace safety** — single-daemon enforcement now only terminates daemons serving the *same* project root; a daemon whose root cannot be determined is left running (fail-safe).

## Changes

### Added

- **Truth-changes project-doc reconciliation mechanism (Plan 00118)** — New per-version `CLAUDE/UPGRADES/truth-changes/v{X.Y.Z}.yaml` files record statements that **were true** about working in a project but became false: a two-key `was`/`now` list, where `now: ~` retires a truth with no replacement. A new `install/truth_changes.py` range loader (mirroring `config_migrations`) and a `check-truth-changes --from --to` CLI command surface the aggregated list for any version range (exit `0` = none, `1` = changes present, `2` = error). The `upgrade.md` skill flow gains a reconciliation step that feeds the project LLM the truth-changes for the `(from, to]` range it crossed and instructs it to update the project's own docs (`CLAUDE/`, `docs/`, `README*`, `AGENTS*` — never `.claude/hooks-daemon/` internals).
- **`plan_number_helper.get_claude_md()` now returns the git-counter current-truth (Plan 00118)** — The handler previously returned `None`, so the daemon injected no always-on guidance about plan numbering. It now renders a `<hooksdaemon>` section stating the next plan number comes from `git config --local hooksdaemon.latestPlanNumber` (folder scan only as an unset-bootstrap), matching the handler's own block message.
- **Backfilled truth-changes for v3.12.0, v3.16.0, v3.17.0 (Plan 00118)** — Reviewed v3.10.0..v3.17.0 (12 releases) and authored the three genuine downstream-facing truth-changes: plan-completion dates → commit hashes (v3.12.0), plan-number folder-scan → git counter (v3.16.0), and the removal of the workflow-state-across-compaction subsystem + `docs/WORKFLOWS.md` (v3.17.0, `now: ~`). Most releases are internal and yield none.

### Changed

- **`RELEASING.md` Step 6/7 governance for truth-changes (Plan 00118)** — Step 6 now moves `UNRELEASED/truth-changes/v*.yaml` into the live `truth-changes/` directory at release (with an ABORT condition if any remain); Step 7's Opus documentation-review checklist prompts for a new truth-changes entry whenever a release changes a documented truth.
- **Release flow mandates absolute paths (RELEASING.md)** — Every command in the release flow must use `/workspace/...` absolute paths and never `cd` into a subdirectory, after a v3.17.0 release attempt silently skipped tag + GitHub-release creation when a stray `cd` broke later relative-path commands.

### Fixed

- **Single-daemon enforcement scoped to the project root (container PID-namespace safety)** — In container mode, `enforce_single_daemon()` previously SIGTERM'd ALL `claude_code_hooks_daemon` processes regardless of which project they served. When a container shares PID-namespace visibility with its host or another container (e.g. a bind-mounted `untracked/`), the startup sweep could kill a daemon serving a completely different project, dropping that project's session mid-flight. `find_all_daemon_processes()` now takes an optional project-root filter — each candidate's root is derived from its `--project-root` flag or the venv path in its interpreter, and only same-project daemons are terminated. A daemon whose root cannot be positively determined is left running (fail-safe). Legacy system-wide behaviour is preserved when no filter is passed.
- **Reconciled the daemon's own docs against the backfilled truth-changes (Plan 00118 dogfood)** — Removed a stale "Workflow state persistence" feature claim from `README.md`, two broken `docs/WORKFLOWS.md` links and the removed-subsystem description from `docs/PLAN_SYSTEM.md`, a dated-completion example, and a dead acceptance-test scenario + fixture for the removed Workflow State Restoration handler.

### Removed

- None.

## Upgrade

```bash
/hooks-daemon upgrade
```

No configuration changes required. This release is fully backwards-compatible. After upgrading, the `upgrade.md` flow will run `check-truth-changes` over the version range you crossed and reconcile your project's own docs against any truth-changes — review and commit those doc edits separately from the daemon upgrade commit.

## Installation

### New Installations

```bash
mkdir -p .claude && cd .claude
git clone https://github.com/Edmonds-Commerce-Limited/claude-code-hooks-daemon.git hooks-daemon
cd hooks-daemon
git fetch --tags && git checkout v3.18.0
python3 -m venv untracked/venv
untracked/venv/bin/pip install -e .
untracked/venv/bin/python install.py
cd ../..
```

### Upgrading Existing Installations

```bash
cd .claude/hooks-daemon
git fetch --tags
git checkout v3.18.0
untracked/venv/bin/pip install -e .
untracked/venv/bin/python -m claude_code_hooks_daemon.daemon.cli restart
```

## QA gates

- **QA suite (Step 8)**: 13/13 PASSED.
- **Step 12.0 H-1 deterministic gates**: PASSED across `test_diagnostic_scripts.py`, `test_install_sh_end_to_end.py`, `test_tool_use_error_recovery.py`, `test_stop_hook_hard_block.py`, `test_skill_install_python_discovery.py`.
- **Step 12 acceptance gate**: H-1 gate (above) + targeted live verification — `check-truth-changes` surfaces the backfilled truth-changes with correct `is_removal` flags, `plan_number_helper.get_claude_md()` renders in the `<hooksdaemon>` block, and the single-daemon scoping fix is covered by `test_process_verification.py` / `test_enforcement.py`. Full ~89-test playbook not run — the only handler change is additive guidance (see Acceptance Testing Gate note above).

## Testing

- **Tests**: 8,458 passing
- **Coverage**: 95.0%
- **Type Safety**: MyPy strict mode compliant
- **Security**: Bandit scan clean

## Contributors

Generated from git log since v3.17.0:

- joseph

## Full Changelog

**Compare**: https://github.com/Edmonds-Commerce-Limited/claude-code-hooks-daemon/compare/v3.17.0...v3.18.0
