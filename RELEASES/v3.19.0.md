# v3.19.0 — Minor: git hooks auto-fixer, additive markdown config, macOS/BSD portability

**Release date**: 2026-06-12
**Type**: MINOR release

## Summary

v3.19.0 delivers a new PostToolUse handler (`git_hooks_executable_fixer`) that automatically fixes non-executable git hooks when git reports the "hook ignored" hint, an additive `extra_allowed_markdown_paths` config option for `markdown_organization` so projects no longer need to maintain a full override copy, broadened built-in markdown allowances for `.claude/skills/` and `.claude/rules/`, and 10 macOS/BSD portability fixes that make the installer and diagnostic scripts work correctly on macOS without GNU coreutils.

## Highlights

- **New `git_hooks_executable_fixer` handler** — Detects git's "hook ignored because not executable" hint and auto-`chmod +x`s the affected hooks directory. Worktree- and `core.hooksPath`-aware. Priority 27, PostToolUse, non-terminal.
- **Additive `extra_allowed_markdown_paths`** — Add allowed markdown locations without replacing all built-in defaults. Declare only your extras; inherit upstream locations automatically.
- **Broadened built-in markdown allowances** — All `.md` inside `.claude/skills/` (not just `SKILL.md`) and all `.md` inside `.claude/rules/` are now allowed by default. This loosens a blocking handler — review the post-upgrade tasks if your project uses `allowed_markdown_paths`.
- **10 macOS/BSD portability fixes (Plans 00122 + 00123)** — Dead `realpath` removal (CRITICAL), BSD `stat` fallback, deterministic hostname suffix, BSD `pgrep` ERE fix, `readlink -f` replacement, health-aware install guard, honest diagnostics, and Bash 3.2 guard.

## Post-Upgrade Tasks

After upgrading, review `CLAUDE/UPGRADES/v3/v3.18.3-to-v3.19.0/post-upgrade-tasks/` — it contains 1 task of severity `recommended` that may require action in your project (migrating `allowed_markdown_paths` to the new additive `extra_allowed_markdown_paths` option).

## Changes

### Added

- **`git_hooks_executable_fixer` PostToolUse handler (Plan 00120, priority 27)** — New non-terminal handler that detects git's "The '...' hook was ignored because it's not set as executable" hint in Bash command output and automatically `chmod +x`s every non-`.sample` file in the repository's hooks directory. Socket path is resolved via `git rev-parse --git-path hooks` so worktrees and `core.hooksPath` are handled correctly. Execute bits are added with least privilege (only where read is already granted). Reports which hooks it fixed via advisory context; never blocks the command; `.sample` files and already-executable hooks are left untouched.

### Changed

- **`markdown_organization` now accepts `extra_allowed_markdown_paths` — additive config option (Plan 00121)** — Projects can now declare additional allowed markdown locations as an additive list layered on top of built-in defaults, rather than replacing them entirely with `allowed_markdown_paths`. Using `extra_allowed_markdown_paths`, a project declares only its genuine extras and automatically inherits all upstream built-in locations (including any new ones added in future releases). The existing `allowed_markdown_paths` full-override option remains fully supported for the deliberate case where a project wants to FORBID a built-in location.
- **`markdown_organization` now allows all markdown inside `.claude/skills/` (Plan 00121)** — Previously only `SKILL.md` at the top of a skill directory was allowed; all other markdown a skill ships (reference docs, usage guides, changelogs) was blocked. The built-in allowance is now broadened to cover all `.md` files anywhere inside `.claude/skills/`, so skills can ship supporting documentation alongside their `SKILL.md` without requiring per-project config overrides. This loosens a blocking handler — existing projects with a narrower `allowed_markdown_paths` override are unaffected; projects relying on built-in defaults now permit more paths.
- **`markdown_organization` now allows all markdown inside `.claude/rules/` (Plan 00121)** — `.claude/rules/` is a newly recognised first-class location alongside `.claude/agents/`, `.claude/commands/`, and `.claude/skills/`. All `.md` files under `.claude/rules/` are now allowed by the built-in defaults without any config change required.

### Fixed

- **Deterministic hostname suffix — macOS/zsh portability (Plan 00122, BUG 1)** — `init.sh` and `daemon/paths.py` now resolve the hostname suffix via a stable series: `$HOSTNAME` env var → `socket.gethostname()` / `hostname` command → `localhost` constant. `HOSTNAME` is unset on macOS/zsh and many CI images; the old code hashed a timestamp fallback, producing a different socket path on every invocation so `start`, `status`, and `stop` could never agree.
- **BSD-vs-GNU `stat` filesystem probe gating (Plan 00122, BUG 2)** — `venv.sh`'s pre-`uv sync` hardlink-hostile-filesystem probe used the GNU-only `stat -f -c %T` flag combination; on BSD/macOS `stat` rejects it and emitted a stray `stat: %T` error mid-install. The probe is now gated to Linux (`uname -s`), where the overlayfs case it detects actually occurs; other platforms fall back to the existing hardlink-first-then-retry path.
- **Health-aware install guard (Plan 00122, BUG 3)** — `install.sh` now checks daemon health (not merely running state) before declaring a prior installation valid and skipping reinstall. A daemon that is running but unhealthy was previously treated as a successful install.
- **Honest install diagnostics (Plan 00122, BUG 4)** — Install failure messages now report the actual Python interpreter path and version observed, rather than a hardcoded suggestion that may not exist on the host.
- **Bash 3.2 portability guard (Plan 00122, BUG 6)** — Shell scripts using `[[` with `=~` regex and `${BASH_REMATCH[1]}` capture groups now guard against Bash 3.2 (macOS default), falling back to POSIX-compatible `expr`/`grep` alternatives.
- **`init.sh` dead `realpath` call removed (Plan 00123, CRITICAL)** — `init.sh` runs under `set -euo pipefail` and assigned `_abs_project_path=$(realpath "$PROJECT_PATH")` on every hook. `realpath` is absent on macOS before 12.3 / base BSD, so the command substitution failed and `set -e` aborted the entire script — breaking every hook forwarder on those hosts. The variable was never referenced anywhere (dead code), so the line was simply deleted.
- **`resolve_venv.sh` hot-path `stat` fallback (Plan 00123, HIGH)** — The per-hook cache (sourced by `init.sh` on every event) computed the `untracked/` mtime with GNU `stat -c %Y`. On macOS this returned empty, so the cache-mtime compare always failed and every hook fell through to a 50-100ms Python fingerprint spawn. A new `_rv_dir_mtime` helper falls back to BSD `stat -f %m`.
- **`daemon_control.sh` `pgrep` portability — GNU ERE alternation removed (Plan 00123, MEDIUM)** — `pgrep -f "pattern1\|pattern2"` uses GNU ERE syntax not supported by BSD `pgrep`. Replaced with two separate `pgrep` invocations combined with the same semantics on both platforms.
- **`hooks_deploy.sh` `readlink -f` replaced with bash `-ef` test (Plan 00123, MEDIUM)** — `readlink -f` (GNU) is absent on macOS. The symlink-resolution logic now uses `[ file1 -ef file2 ]` (POSIX) for same-inode detection.

## Upgrade

```bash
/hooks-daemon upgrade
```

No breaking changes. After upgrading, review `CLAUDE/UPGRADES/v3/v3.18.3-to-v3.19.0/post-upgrade-tasks/` for the `recommended` config-migration task. If your project sets `allowed_markdown_paths` in `.claude/hooks-daemon.yaml`, consider migrating to the new additive `extra_allowed_markdown_paths` option — see the task file for detection and migration steps.

## Installation

### New Installations

```bash
mkdir -p .claude && cd .claude
git clone https://github.com/Edmonds-Commerce-Limited/claude-code-hooks-daemon.git hooks-daemon
cd hooks-daemon
git fetch --tags && git checkout v3.19.0
python3 -m venv untracked/venv
untracked/venv/bin/pip install -e .
untracked/venv/bin/python install.py
cd ../..
```

### Upgrading Existing Installations

```bash
cd .claude/hooks-daemon
git fetch --tags
git checkout v3.19.0
untracked/venv/bin/pip install -e .
untracked/venv/bin/python -m claude_code_hooks_daemon.daemon.cli restart
```

## QA gates

- **QA suite (Step 8)**: 13/13 PASSED (8538 tests, 95.1% coverage).
- **Step 12.0 H-1 deterministic gates**: 23 passed.
- **Step 12 acceptance gate**: per established MINOR-release precedent (e.g. v3.18.0), satisfied by the H-1 deterministic gate plus targeted live verification of the two changed handlers against the running daemon — `markdown_organization` (`.claude/rules/*.md` now allowed; disallowed paths still denied) verified live; `git_hooks_executable_fixer` verified end-to-end by the pre-release code audit and its unit suite, and confirmed loaded. The bulk of the release is shell-script portability fixes outside handler dispatch.

## Testing

- **Tests**: 8,538 passing
- **Coverage**: 95.1%
- **Type Safety**: MyPy strict mode compliant
- **Security**: Bandit scan clean

## Contributors

Generated from git log since v3.18.3:

- joseph

## Full Changelog

**Compare**: https://github.com/Edmonds-Commerce-Limited/claude-code-hooks-daemon/compare/v3.18.3...v3.19.0
