# v3.18.3 — Patch: single-daemon enforcement targets only genuine daemon server processes

**Release date**: 2026-06-04
**Type**: PATCH release
**Acceptance Testing Gate (RELEASING.md Step 12)**: PATCH release with no handler changes. Step 12 full acceptance playbook SKIPPED per RELEASING.md Step 12 table (PATCH + no handler changes). Step 12.0 H-1 deterministic gate run. No daemon package handler code changed; this patch only touches `src/claude_code_hooks_daemon/daemon/process_verification.py` and its unit test `tests/unit/daemon/test_process_verification.py`.

## Summary

v3.18.3 is a single-fix patch that completes the root-cause resolution started in v3.18.2.

**v3.18.2 fixed the symptom**: `restart_daemon_verified` stopped treating the starter process's exit code as authoritative, instead deferring the start/no-start decision to a status poll. This meant that even if the in-flight `cli start` parent was SIGTERMed (exit 143), the upgrade script correctly reported success when the daemon came up healthy.

**v3.18.3 fixes the root cause**: the reason that in-flight starter was being SIGTERMed in the first place. `find_all_daemon_processes` / `_is_daemon_process` matched ANY process whose cmdline contained `claude_code_hooks_daemon` — including transient CLI helpers (`status`, `stop`, `logs`, `health`, `repair`, `check-truth-changes`, `generate-docs`), hook forwarders (`claude_code_hooks_daemon.hooks.*`), and the short-lived start/restart parent itself. Under container single-daemon enforcement, all of these were candidates for SIGTERM on startup, which is what allowed a concurrent `start` to kill an in-flight starter.

The fix introduces `_is_daemon_server_process`, which matches ONLY genuine daemon server cmdlines: the `claude_code_hooks_daemon.daemon.cli` module token PLUS a `start` or `restart` subcommand appearing after it. The broad name/substring match and the `DAEMON_PROCESS_NAME` constant are removed. The `{start, restart}` allowlist is provably complete — daemonization (`os.fork` / `os.setsid` / `HooksDaemon` / `asyncio.run`) lives only in `cmd_start`, reachable only from the `start` subcommand and from `cmd_restart` which calls `cmd_start`; `os.fork` preserves argv — giving zero false negatives, guarded by a dedicated unit test. Project-root scoping is preserved.

**Note on delivery**: `process_verification.py` is part of the daemon Python package installed via `pip install -e .`. Existing installations receive this fix on their next upgrade run.

## Highlights

- **Root cause of the v3.18.2 exit-143 false-failure fully resolved** — enforcement no longer SIGTERMs transient CLI helpers, hook forwarders, or in-flight starters; only genuine daemon server processes are ever targeted.

## Changes

### Fixed

- **Single-daemon enforcement now targets only genuine daemon server processes** — `find_all_daemon_processes` / `_is_daemon_process` previously matched ANY process whose cmdline contained `claude_code_hooks_daemon`, including transient CLI helpers (`status`, `stop`, `logs`, `health`, `repair`, `check-truth-changes`, `generate-docs`), hook forwarders (`claude_code_hooks_daemon.hooks.*`), and the short-lived start/restart parent. Under container single-daemon enforcement these were SIGTERMed — which is what allowed a concurrent `start` to terminate an in-flight starter process (the root cause that produced the v3.18.2 exit-143 symptom). Fix introduces `_is_daemon_server_process`, which matches ONLY genuine daemon server cmdlines: the `claude_code_hooks_daemon.daemon.cli` module token PLUS a `start` or `restart` subcommand appearing after it. The broad name/substring match and the `DAEMON_PROCESS_NAME` constant are removed. The `{start, restart}` allowlist is provably complete — daemonization (`os.fork` / `os.setsid` / `HooksDaemon` / `asyncio.run`) lives only in `cmd_start`, reachable only from the `start` subcommand and from `cmd_restart` which calls `cmd_start`; `os.fork` preserves argv — giving zero false negatives, guarded by a dedicated unit test. Project-root scoping is preserved.

## Upgrade

```bash
/hooks-daemon upgrade
```

No configuration changes required. This release is fully backwards-compatible.

**Note**: `process_verification.py` is part of the daemon Python package and is delivered when the versioned upgrade runs `pip install -e .` (i.e. `git checkout v3.18.3` + `pip install -e .` + `install.py`), or equivalently via `/hooks-daemon upgrade`.

## Installation

### New Installations

```bash
mkdir -p .claude && cd .claude
git clone https://github.com/Edmonds-Commerce-Limited/claude-code-hooks-daemon.git hooks-daemon
cd hooks-daemon
git fetch --tags && git checkout v3.18.3
python3 -m venv untracked/venv
untracked/venv/bin/pip install -e .
untracked/venv/bin/python install.py
cd ../..
```

### Upgrading Existing Installations

```bash
cd .claude/hooks-daemon
git fetch --tags
git checkout v3.18.3
untracked/venv/bin/pip install -e .
untracked/venv/bin/python -m claude_code_hooks_daemon.daemon.cli restart
```

## QA gates

- **QA suite (Step 8)**: 13/13 PASSED.
- **Step 12.0 H-1 deterministic gates**: PASSED across `test_diagnostic_scripts.py`, `test_install_sh_end_to_end.py`, `test_tool_use_error_recovery.py`, `test_stop_hook_hard_block.py`, `test_skill_install_python_discovery.py`.
- **Step 12 acceptance gate**: SKIPPED — PATCH release with no handler changes, per RELEASING.md Step 12 table. No daemon package handler code changed; the only code changes are in `src/claude_code_hooks_daemon/daemon/process_verification.py` and its unit test `tests/unit/daemon/test_process_verification.py`.

## Testing

- **Tests**: 8,470 passing, 5 skipped
- **Coverage**: 95.0%
- **Type Safety**: MyPy strict mode compliant
- **Security**: Bandit scan clean

## Contributors

Generated from git log since v3.18.2:

- joseph

## Full Changelog

**Compare**: https://github.com/Edmonds-Commerce-Limited/claude-code-hooks-daemon/compare/v3.18.2...v3.18.3
