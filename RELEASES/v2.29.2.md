# Release v2.29.2 - Stop Hook Reliability Fixes

**Release Date:** 2026-04-01
**Type:** Patch Release

## Summary

Version 2.29.2 delivers three targeted fixes to the `auto_continue_stop` handler and the `error_hiding_blocker` handler. Two bugs caused valid `STOPPING BECAUSE:` stop explanations to go undetected — one when the assistant produced multiple content blocks, and one when the transcript had not yet been fully flushed at the moment the stop event fired. A third change replaces line-number keys in the error-hiding exclusion list with function-name keys so exclusions remain valid after code is added or removed above an exclusion site.

## Highlights

- **Multi-block stop explanation detection** — `STOPPING BECAUSE:` is now recognised even when the assistant's response spans multiple content blocks (e.g. a thinking block followed by the reply text).
- **Race condition fix in stop explanation check** — a retry loop with backoff prevents false denials when the transcript has not yet been fully written at event time.
- **Drift-proof error-hiding exclusions** — `error_hiding_exclusions.json` now keys exclusions by enclosing function name instead of line number, so exclusions survive code edits that shift line numbers.

## Changes

### Fixed

- **`auto_continue_stop` stop explanation missed in multi-block assistant messages**: `_has_stop_explanation()` was reading the first content block only; when the assistant produced multiple text blocks (e.g. a thinking block followed by the response), the `STOPPING BECAUSE:` prefix in a later block was silently ignored, causing valid stops to be denied and triggering unnecessary retry loops. Fixed by scanning all content blocks, not just the first.

- **Race condition in `_has_stop_explanation` — retry when text not yet flushed**: Under load, the stop event sometimes arrives before the assistant's final message has been fully written to the transcript. Added a short retry loop with backoff so the handler waits for the text to appear rather than immediately treating an empty or missing message as a missing stop explanation.

### Changed

- **Error-hiding exclusion list uses function names instead of line numbers (drift-proof)**: The `error_hiding_blocker` handler's QA audit exclusion list (`error_hiding_exclusions.json`) previously matched by line number, which silently drifted out of sync whenever code was added or removed above an exclusion site. Refactored to match by enclosing function name instead, so exclusions are immune to line shifts and fail loudly if a function is renamed rather than silently matching the wrong code.

## Upgrade Instructions

### For Existing Users

```bash
cd your-project
.claude/hooks-daemon/scripts/upgrade.sh
```

No configuration changes required.

### For New Users

See [Installation Guide](../CLAUDE/LLM-INSTALL.md)

## Test Statistics

- **Total tests**: 7,550+
- **Coverage**: 95%+ required
- **Production handlers**: 71 across 13 event types

## Comparison

[Full diff: v2.29.1...v2.29.2](https://github.com/Edmonds-Commerce-Limited/claude-code-hooks-daemon/compare/v2.29.1...v2.29.2)
