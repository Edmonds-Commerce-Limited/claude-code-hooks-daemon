# v3.16.0 — Minor: git-anchored plan numbering, GitRepo utility, canonical Python discovery, Stop hook context-limit guidance

**Release date**: 2026-05-27
**Type**: MINOR release
**Acceptance Testing Gate (RELEASING.md Step 12)**: MINOR release with narrowly-scoped handler changes. Acceptance was verified via the Step 12.0 H-1 deterministic gate (23/23) plus targeted live verification of the changed handlers, rather than the full ~89-test playbook, because the changes are confined to plan numbering and a behaviour-preserving refactor: install/upgrade scripts and Python discovery (Plan 00110) and the Stop hook contract (Plan 00111) are covered by the H-1 gate; the git-anchored plan-numbering handlers (Plan 00112) were verified live (`plan_number_helper` returns the git-anchored next number, `markdown_organization` blocks disallowed paths); and the `git_filemode_checker` GitRepo migration (Plan 00113) is a refactor covered by unit tests and daemon-load.

## Summary

v3.16.0 delivers four plans focused on infrastructure quality and developer-experience improvements. Plan 00112 anchors plan numbering to `git config --local` so the counter survives branch switches and works correctly in nested repositories. Plan 00113 introduces a first-class `GitRepo` value object that consolidates all git-subprocess logic behind a single tested abstraction. Plan 00110 closes the host-a field report by canonicalising Python interpreter discovery across six install/upgrade call sites, fixing a hard failure on hosts that have `python3` (3.9) but not the hardcoded `python3.11` suggestion. Plan 00111 adds actionable context-limit guidance to the Stop hook's Branch 4 reason string.

## Highlights

- **Git-anchored plan numbering** — plan counter persisted in `.git/config` via `hooksdaemon.latestPlanNumber`; stable across branches, correct in nested repos, self-heals drift.
- **`GitRepo` utility** — SOLID value object for enclosing-repo resolution and `git config --local` read/write; replaces scattered inline git-subprocess calls in handlers.
- **Canonical Python discovery** — `python_discovery.sh` + `find_latest_python` in `paths.py` replace six WET call sites; auto-selects latest `python3.X` on hosts without the expected version.
- **host-a field fix** — `install.sh` no longer aborts with a hardcoded version suggestion that doesn't exist on the host; names the observed interpreter on failure.
- **Stop hook context-limit guidance** — Branch 4 now emits actionable advice instead of a bare signal, reducing agent confusion at the context ceiling.

## Changes

### Added

- **Git-anchored plan numbering via `git config --local hooksdaemon.latestPlanNumber` (Plan 00112)** — The latest plan number is now persisted per-repository in `git config --local hooksdaemon.latestPlanNumber`. The counter lives in `.git/config` (stable across branch switches) and is resolved from the target path, so plans created inside nested or vendor sub-repos use that repo's own counter instead of the parent project's. Read = counter+1; bootstrap from a filesystem scan only when the counter is absent; writes are a monotonic high-water mark that self-heals drift. Wired into the `validate_plan_number`, `plan_number_helper`, and `markdown_organization` handlers.
- **First-class reusable `GitRepo` utility (Plan 00113)** — New SOLID value object at `src/claude_code_hooks_daemon/utils/git_repo.py` providing enclosing-repo resolution and `git config --local` read/write, with a single bounded git-subprocess wrapper. Eliminates scattered ad-hoc `git config` subprocess calls and provides a shared, tested abstraction for all handlers that need git-repo context.

### Changed

- **Consolidated duplicated Python-version-discovery logic into canonical helpers (Plan 00110)** — A shared bash `python_discovery.sh` and Python `find_latest_python` in `paths.py` replace six WET call sites across `scripts/upgrade.sh`, `prerequisites.sh`, skill `install.sh`, `can_inline_bootstrap`, and `resolve_venv.sh`. All six sites now delegate to the canonical helper.
- **`plan_numbering` and `git_filemode_checker` migrated to use `GitRepo` utility (Plan 00113)** — Both handlers previously contained inline git-subprocess logic; they now depend on the `GitRepo` value object, removing duplication and benefiting from its test coverage.
- **Stop hook `auto_continue_stop` Branch 4 now includes context-limit guidance (Plan 00111)** — When the Stop hook fires with a context-limit signal, the Branch 4 reason string now contains actionable guidance on how to proceed, reducing agent confusion at the context ceiling.

### Fixed

- **Python interpreter discovery on hosts without the expected version (Plan 00110, host-a field report)** — `install.sh` previously aborted with a hardcoded `python3.11` suggestion on hosts that only had `python3` (3.9) alongside `python3.13`/`python3.14`. The script now auto-selects the latest available `python3.X` interpreter, and on genuine failure names the actually-observed interpreter (e.g. `python3.9 (3.9.21)`) instead of a fixed suggestion that may not exist on the host.

### Removed

- None.

## Upgrade

```bash
/hooks-daemon upgrade
```

No configuration changes required. The plan-numbering counter bootstraps automatically from a filesystem scan when absent — existing repos do not need any manual migration step.

## Installation

### New Installations

```bash
mkdir -p .claude && cd .claude
git clone https://github.com/Edmonds-Commerce-Limited/claude-code-hooks-daemon.git hooks-daemon
cd hooks-daemon
git fetch --tags && git checkout v3.16.0
python3 -m venv untracked/venv
untracked/venv/bin/pip install -e .
untracked/venv/bin/python install.py
cd ../..
```

### Upgrading Existing Installations

```bash
cd .claude/hooks-daemon
git fetch --tags
git checkout v3.16.0
untracked/venv/bin/pip install -e .
untracked/venv/bin/python -m claude_code_hooks_daemon.daemon.cli restart
```

## QA gates

- **QA suite (Step 8)**: 13/13 PASSED.
- **Step 12.0 H-1 deterministic gates**: 23/23 PASSED across `test_diagnostic_scripts.py` (12), `test_install_sh_end_to_end.py` (2), `test_tool_use_error_recovery.py` (2), `test_stop_hook_hard_block.py` (3), `test_skill_install_python_discovery.py` (4).
- **Step 12 acceptance gate**: H-1 gate (above) + targeted live verification of the changed handlers — `plan_number_helper` returned the git-anchored next number (`00114`), `markdown_organization` denied a disallowed `.md` write, and the git-anchored counter was confirmed in `.git/config`. Lifecycle/Stop changes (`git_filemode_checker`, `auto_continue_stop`) are covered by the H-1 gate, daemon-load, and unit tests. Full 89-test playbook not run — changes are narrowly scoped (see Acceptance Testing Gate note above).

## Testing

- **Tests**: 8,361 passing
- **Coverage**: 95.1%
- **Type Safety**: MyPy strict mode compliant
- **Security**: Bandit scan clean

## Contributors

Generated from git log since v3.15.1:

- joseph

## Full Changelog

**Compare**: https://github.com/Edmonds-Commerce-Limited/claude-code-hooks-daemon/compare/v3.15.1...v3.16.0
