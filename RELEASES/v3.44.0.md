# Release v3.44.0 - Supervisor Ctrl+Z Guard + Status-Line Message Channel

**Release Date:** 2026-07-17
**Type:** Minor Release

## Summary

This minor release closes a real upstream footgun: Ctrl+Z sent to the ccy PTY
supervisor could suspend the underlying Claude Code process
(anthropics/claude-code#43596). The supervisor now strips the Ctrl+Z SUSP
byte from forwarded stdin (with a belt-and-braces signal guard as a second
line of defence) and surfaces a status-line notice via a new general-purpose,
thread-safe supervisor→status-line message channel. It also lowers the
default status-line `refreshInterval` from 10 to 1 so idle-updated status
segments refresh promptly.

## Highlights

- **Ctrl+Z can no longer suspend a ccy-supervised Claude Code session.** The
  supervisor strips the SUSP byte (`0x1a`) from forwarded stdin before it
  reaches Claude's PTY, and a signal-level guard additionally swallows
  `SIGTSTP`/`SIGQUIT` (`SIG_IGN` on `SIGTTIN`/`SIGTTOU`) if such a signal is
  ever actually delivered. `SIGINT`/Ctrl+C is left untouched.
- **New thread-safe supervisor→status-line message channel** — a
  lock-guarded, rate-limited, atomic-replace TTL-bounded JSON channel. The
  Ctrl+Z-ignored notice is its first consumer, rendered attached to the
  supervisor's own top-hat status segment (no second top hat), painted
  black-on-orange at warning level for legibility.
- **`status_message` standalone status-line handler removed**, folded into
  `supervisor_indicator` — an internal consolidation with no loss of
  user-facing capability.
- **Status-line `refreshInterval` default lowered 10 → 1** in the daemon's
  `.claude/settings.json` template (rolled out automatically on install and
  upgrade), so idle-updated segments like the Ctrl+Z notice, clock, and
  multithread indicator now refresh within ~1s instead of trailing up to
  ~10s.
- Thread safety documented as a first-class concern across the supervisor
  and status-line code.

## Changes

## [3.44.0] - 2026-07-17

### Added

- **Ctrl+Z (SUSP) guard in the ccy PTY supervisor.** The supervisor
  (`.claude/ccy/claude-supervise.py`) now strips the Ctrl+Z SUSP byte
  (`0x1a`) from stdin before forwarding it to Claude's PTY, so it can never
  suspend the Claude process — closing the upstream footgun where Ctrl+Z
  suspends Claude Code with no easy way back
  (anthropics/claude-code#43596). A belt-and-braces signal guard also
  swallows `SIGTSTP`/`SIGQUIT` (and sets `SIG_IGN` on `SIGTTIN`/`SIGTTOU`)
  if such a signal is ever actually delivered to the supervisor process.
  `SIGINT`/Ctrl+C is deliberately left working so the session can still be
  interrupted normally.
- **General supervisor→status-line transient message channel.** A
  thread-safe, lock-guarded, rate-limited poster writes an atomically
  replaced, TTL-bounded JSON message (with a severity `level`) that the
  status line reads and renders. The first consumer is the Ctrl+Z-ignored
  notice ("use /exit to quit"), rendered attached to the supervisor's own
  top-hat segment as a single status segment — no second top hat — with
  warning-level messages painted black-on-orange for legibility.
- Thread safety is now documented as a first-class concern across the
  supervisor and status-line code (Plan 00173 Phase 0).

### Changed

- **Status-line `refreshInterval` default lowered from 10 to 1** (Claude
  Code's documented minimum) in the daemon's authoritative
  `.claude/settings.json` template, which `install_version.sh` and
  `upgrade_version.sh` copy verbatim into every client project on both
  fresh installs and upgrades. Idle-updated status-line segments (the new
  Ctrl+Z notice, the clock, the multithread indicator) now refresh within
  ~1s instead of trailing up to ~10s. Root cause: Claude Code only
  re-invokes the status command on discrete events (Ctrl+Z is not one)
  plus this optional timer, and the daemon's status render is ~39ms and
  git-cached, so a 1s timer adds negligible overhead. This is a
  `settings.json` template default, not a `hooks-daemon.yaml` key, and
  rolls out automatically via the existing template-overwrite mechanism.

### Removed

- **Standalone `status_message` status-line handler.** Folded into the
  `supervisor_indicator` handler so the Ctrl+Z-ignored notice renders as
  one attached segment on the supervisor's existing top hat instead of a
  second, independent top hat. This is an internal handler consolidation,
  not a loss of user-facing capability — the notice still renders,
  attached to the supervisor indicator.

See `CHANGELOG.md` for the complete entry.

## Upgrade Instructions

No breaking changes in this release — a standard upgrade is sufficient. See
`CLAUDE/UPGRADES/` for version-specific upgrade guides (none required for
this minor release).

## Installation

### New Installations

```bash
cd .claude
git clone -b v3.44.0 https://github.com/Edmonds-Commerce-Limited/claude-code-hooks-daemon.git hooks-daemon
cd hooks-daemon
python3 -m venv untracked/venv
untracked/venv/bin/pip install -e .
untracked/venv/bin/python install.py
```

### Upgrading Existing Installations

```bash
cd .claude/hooks-daemon
git fetch --tags
git checkout v3.44.0
untracked/venv/bin/pip install -e .
untracked/venv/bin/python -m claude_code_hooks_daemon.daemon.cli restart
```

## Testing

- **Tests:** 10,311+ passing
- **Coverage:** 95%+ required
- **Type Safety:** MyPy strict mode compliant
- **Security:** Bandit scan clean

## Contributors

- joseph <joseph@ltscommerce.dev>

## Full Changelog

**Compare:** https://github.com/Edmonds-Commerce-Limited/claude-code-hooks-daemon/compare/v3.43.1...v3.44.0
